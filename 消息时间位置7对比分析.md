# 消息时间显示功能 — 位置7（气泡外）深度对比分析（v3 优化版）

> **对比对象**：
> - **本插件 WeChatPlugin**：[MessageTimeHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m) + [PluginConfig.h](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.h)
> - **微信优化反编译版**：[123456.c](file:///www/wwwroot/ios/插件/微信优化反编译最新/123456.c) — 核心函数 `FUN_00039cac` ~ `FUN_0003cc68`（约 L34360~L35929）
>
> **v4 更新**：
> - 完全照抄反编译版：`initWithViewModel` 始终创建 label（无条件），`updateNodeStatus` disabled 时 `setHidden:YES`
> - 修复 hex 颜色字符串转 UIColor（`colorFromHex` 支持 8 位 ARGB）
> - 删除 `dispatch_async(main)` 手动触发 updateNodeStatus，避免与微信优化 dispatch block 冲突导致 `presentingModalViewController` 崩溃
> - 恢复 position 0/1 头像定位逻辑（之前随 `quickRelocateTimeLabel`/`addTimeLabelToCell` 被误删）

---

## 一、Hook 架构逐项对比

| # | Hook 目标类 | Hook 方法 | 功能说明 | 本插件 | 反编译版 | 谁更好 |
|---|------------|----------|---------|--------|---------|--------|
| 1 | `CommonMessageCellView` | `initWithViewModel:` | 在 CellView 初始化时立即创建时间标签（最早创建时机） | ✅ | ✅ | 平手 |
| 2 | `BaseMsgContentViewController` | `tableView:cellForRowAtIndexPath:` | 在 Cell 即将显示前被调用，两版均使用 dispatch_get_global_queue(0,0) 异步计算时间字符串并缓存到 viewModel | ✅ 全局队列异步 | ✅ `FUN_0003b270` → `FUN_0003c790` | 平手 |
| 3 | `CommonMessageCellView` | `updateNodeStatus` | ViewModel 数据更新时触发，两版均用于设置标签文字+frame+颜色+定位 | ✅ 读取 viewModel 缓存的 timeText | ✅ `FUN_0003a06c` | 平手 |
| 4 | `ChatTimeCellView` | `layoutSubviews` | 微信原生聊天时间分隔线的布局回调，本插件在此隐藏原版时间 | ✅ 隐藏原版时间 | ❌ | **本插件** |
| 5 | `ChatTimeViewModel` | `cellHeight` | 微信原生聊天时间分隔线的高度回调，本插件将高度置0以完全隐藏 | ✅ 隐藏原版时间 | ❌ | **本插件** |
| 6 | `CContact` | `m_nsNickName` | 好友昵称 Getter，本插件在此给好友昵称添加发送时间后缀 | ✅ 时间后缀 | ❌ | **本插件** |
| 7 | `TextMessageCellView` | `setFrameForBgImageView:` | 气泡背景图 frame 设置回调，位置7时气泡需向侧边扩展以容纳时间标签 | ✅ 气泡扩展 | ✅ `FUN_0003b058` | 平手 |

### 架构路径对比

```
┌─────────────────────────────────────────────────────────────┐
│ 本插件 （v4 最终版 — 完全复刻微信优化，仅7个Hook）           │
│                                                             │
│  initWithViewModel ──→ 始终创建空标签(40×30)，不检查开关      │
│  cellForRow ──→ dispatch_get_global_queue(0,0)             │
│       │         ├─ 获取 viewModel → messageWrap → createTime  │
│       │         ├─ 格式化时间字符串                           │
│       │         └─ 存到 viewModel 关联对象 @"messageTimeText" │
│  updateNodeStatus ──→ 读 viewModel 缓存的 timeText           │
│       │         ├─ 若 disabled → label.hidden=YES, return    │
│       │         ├─ 设 label.text + font + 颜色(hex→UIColor)    │
│       │         ├─ 计算 frame(textW+4, textH+4)                │
│       │         ├─ position 0/1 用 avatarFrame, 其他用 contentFrame│
│       │         └─ addSubview(若未添加)                       │
│  TextMessageCellView.setFrameForBgImageView: ──→ 气泡扩展(38pt)│
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 反编译版 — 2 条路径                                         │
│                                                             │
│  initWithViewModel ──→ 创建空标签(仅设font/color/圆角,frame=88×28) │
│  cellForRow ──→ dispatch_get_global_queue(0,0)             │
│       │         ├─ FUN_0003c790: 计算时间、去重比较、格式化  │
│       │         ├─ 存结果到 viewModel 关联对象              │
│       │         └─ dispatch_async(main) → updateNodeStatus  │
│  updateNodeStatus (FUN_0003a06c) ──→ 读取缓存时间串 → 设文字 → 设置frame → 定位 → 添加/移除view |
└─────────────────────────────────────────────────────────────┘
```

| 对比项 | 功能说明 | 反编译版 | 本插件（优化后） |
|--------|---------|---------|--------|
| 标签创建可靠性 | 标签在各种时机下能被正确创建的概率 | ✅ init + cellForRow ⭐⭐ | ✅ init + cellForRow ⭐⭐ |
| Cell 复用防错乱 | Cell 滚动复用时标签是否会残留到错误位置 | ✅ 不需要（见§9） | ✅ 不需要（见§9） |
| 主线程性能 | 标签创建和时间计算是否阻塞主线程 | ✅ 全局队列异步 | ✅ 全局队列异步 |
| 标签创建时机 | 标签首次创建的生命周期阶段 | cell 初始化 | cell 初始化（不再检查 showMessageTime） |
| disabled 行为 | 功能关闭时 label 如何处理 | updateNodeStatus 中 setHidden:YES | ✅ updateNodeStatus 中 setHidden:YES |
| Hex 颜色转换 | config 里的 hex 字符串如何转成 UIColor | NSUserDefaults → 反序列化 | ✅ `[config colorFromHex:]` 支持 6/8 位 |

---

## 二、位置7 定位算法深度对比

### 2.1 反编译版定位公式（FUN_0003a06c，updateNodeStatus）

#### 第一步：先给 label 设 frame（带 padding）

```c
// L34636-34646
boundingRect = [text boundingRectWithSize:CGSizeMake(CGFLOAT_MAX, CGFLOAT_MAX) ...];
// param_3 ≈ textBoundingWidth, param_4 ≈ textBoundingHeight

labelWidth  = max(30.0, min(param_3 + 4.0, 88.0));  // 宽度 +4 再上限88/下限30
labelHeight = param_4 + 4.0;                         // 高度 +4
[label setFrame:CGRectMake(0, 0, labelWidth, labelHeight)];
```

> 关键：反编译版给 label frame 的宽高都加了 **4pt**。这不是直接的"留白"，而是通过扩大 label 的 frame 来间接产生视觉间距。

#### 第二步：位置7 计算 center

```c
// L34893-34913 (case 7)
if (isSender) {
    dVar19 = CGRectGetMinX(contentView);   // cvLeft
    dVar17 = -0.5;
    // cx = cvLeft + labelWidth × (-0.5) = cvLeft - labelWidth/2
    //   = cvLeft - max(30, min(textW+4, 88)) / 2
} else {
    dVar19 = CGRectGetMaxX(contentView);   // cvRight
    dVar17 = +0.5;
    // cx = cvRight + labelWidth × 0.5 = cvRight + labelWidth/2
}

dVar18 = CGRectGetMaxY(contentView);        // cvBottom
// labelHeight = textHeight + 4
// cy = cvBottom + labelHeight × (-0.5) = cvBottom - labelHeight/2
//   = cvBottom - (textHeight + 4) / 2
```

#### 第三步：应用 offset

```c
// L34944-34955
[label setCenter:CGPointMake(cx, cy)];   // 首次定位
// 再读offset并叠加
cx_offset = readFloat("offsetX");
cy_offset = readFloat("offsetY");
newCX = cx + (isSender ? -cx_offset : +cx_offset);
newCY = cy - cy_offset;
[label setCenter:CGPointMake(newCX, newCY)];
```

#### 公式总结

| 角色 | X center | Y center |
|------|----------|----------|
| Sender | `cvLeft - (textW+4)/2` | `cvBottom - (textH+4)/2` |
| Receiver | `cvRight + (textW+4)/2` | `cvBottom - (textH+4)/2` |

#### 2pt 间距是怎样产生的

对于精确匹配文本的 label（如 `"12:34:56"` 7pt 字体，textW≈30pt, textH≈15pt）：

- label.frame = (0, 0, 34, 19) — 宽高各+4
- sender: cx = cvLeft - 34/2 = **cvLeft - 17**（实际间距 ≈ 2pt，因为 label 自身 frame 宽于文本）
- receiver: cx = cvRight + 34/2 = **cvRight + 17**（同上）
- cy = cvBottom - 19/2 = **cvBottom - 9.5**（≈2pt 上偏移，因为 label frame 高于文本）

> 反编译版通过**扩大 label frame (+4pt)** 来实现 2pt 间距，这是一个间接但巧妙的方式。

---

### 2.2 本插件定位公式（优化历程）

**优化前**有三套独立定位函数（全部已删除）：

| 函数 | 功能说明 | 策略 | 位置7公式(sender) | Y 公式 | 状态 |
|------|---------|------|-------------------|--------|------|
| **computeLabelFrame** (L379-475) | 框架布局定位，使用 frame 原点坐标计算标签 frame | frame 原点对齐 | `cvLeft - w - 2` | `cvBottom - h - 2` | ❌ 已删除 |
| **quickRelocateTimeLabel** (L478-578) | 轻量快速重定位，在 updateNodeStatus 时调用 | center，无padding | `cvLeft - w/2` | `cvBottom - h/2` | ❌ 已删除 |
| **addTimeLabelToCell** (L850-898) | 完整创建定位，在 willDisplayCell/didMoveToWindow 时调用 | center，+4 padding | `cvLeft - pw/2` | `cvBottom - ph/2` | ❌ 已删除 |

> **为什么 position 0/1 被删掉了**：`computeLabelFrame`、`quickRelocateTimeLabel`、`addTimeLabelToCell` 三个函数中各有一套 position 0~7 的定位实现。当用户要求"只留 init+cellForRow"时，这三个函数被视为冗余代码一并删除。但当时只给 case 0/1 写了 fallback（和 case 2/7 相同），忘了恢复头像定位逻辑。现已修复。

**优化后**仅保留 `updateNodeStatus` 中的内联定位逻辑：
- position 0/1：优先使用 `getAvatarView` → `avatarFrame` 定位在头像上方/下方
- position 2~7：使用 `contentFrame` 定位
- disabled 时：`label.hidden = YES` 后 return（照抄 `FUN_0003a06c` 路径 E）

#### 三套公式等价性验证

以 textW=30, textH=15 为例：

| 函数 | sender cx | receiver cx | cy | 实际间距 | 问题 |
|------|-----------|-------------|-----|---------|------|
| computeLabelFrame | x = cvLeft-32 | x = cvRight+2 | y = cvBottom-17 | ✅ 2pt | — |
| quickRelocateTimeLabel | cx = cvLeft-15 | cx = cvRight+15 | cy = cvBottom-7.5 | ❌ 0pt（紧贴边缘） | **无padding，间距丢失** |
| addTimeLabelToCell | cx = cvLeft-17 | cx = cvRight+17 | cy = cvBottom-9.5 | ✅ 2pt | — |

> 🔴 **quickRelocateTimeLabel 无 padding，间距为 0，与另外两套不一致！**

---

### 2.3 反编译版 v.s. 本插件 — 间距机制对比

| 对比项 | 功能说明 | 反编译版 | 本插件（优化前） | 本插件（优化后） |
|--------|---------|---------|---------------|---------------|
| 2pt间距实现方式 | 如何让标签与气泡边缘产生2pt视觉间距 | `frame = (0,0, textW+4, textH+4)` | 3套定位各有不同 | **updateNodeStatus 统一1套定位：`frame=(0,0,w+4,h+4)` → center自动2pt** |
| 视觉结果 | 实际间距 | **2pt 间距** | 0pt~2pt 混用 | **2pt 间距** ✅ |
| 一致性 | 是否始终一致 | ✅ 始终一致 | ❌ 不一致 | ✅ **已统一** |

> ✅ 用户说得对：反编译版也有 2pt 间距，并非"更紧凑"。我 v1 中判断错了。

---

### 2.4 反编译版定位是否"多套实现"？

反编译版**所有定位逻辑都集中在 `FUN_0003a06c`（updateNodeStatus）一个函数内**，但函数内部有多个执行路径：

| 路径 | 触发条件 | 坐标参照 |
|------|----------|----------|
| 路径 A | position=0/1，找到头像 | 头像 frame（midX, minY/maxY） |
| 路径 B | position=0/1，未找到头像 | contentView frame（fallback） |
| 路径 C | position=2~7 | contentView frame |
| 路径 D | position > 7（未知位置） | contentView.frame = cellFrame（fallback） |
| 路径 E | 模块关闭 / viewModel 不可用 | 隐藏 label 并 return |

这些是**同一函数内的分支**，不是独立的多套实现。真正的"多套实现"指的是本插件（优化前）：

| 本插件（优化前） | 功能说明 | 调用时机 | padding | 位置7 sender cx公式 |
|--------|---------|---------|---------|---------------------|
| `computeLabelFrame` (frame-based) | 框架布局定位，非标准位置回退 | addTimeLabelToCell 的 default 分支 | `kMessageTimeBaseSpacing=2` → 间距2pt | `x = cvLeft - w - 2` |
| `quickRelocateTimeLabel` (center) | 轻量快速重定位，updateNodeStatus 时调用 | updateNodeStatus | **`pw = w + 0`（无padding）** | `cx = cvLeft - w/2`（间距=0pt） |
| `addTimeLabelToCell` (center, +4) | 完整创建定位 | willDisplayCell / didMoveToWindow | `pw = w + 4` → 间距2pt | `cx = cvLeft - pw/2 = cvLeft - w/2 - 2` |

###   为什么 quickRelocateTimeLabel 与另外两套结果不同？

**直接原因**：`quickRelocateTimeLabel` 的 L503-504 行写的是：

```objc
CGFloat pw = w + 0;  // 注意：quickRelocate 未使用 padding!
CGFloat ph = h + 0;
```

而 `addTimeLabelToCell` 对应位置写的是：

```objc
CGFloat pw = labelW;  // textW + 4
CGFloat ph = labelH;  // textH + 4
```

`computeLabelFrame` 使用的是 `kMessageTimeBaseSpacing = 2.0` 作为 frame 间距。

**数值差异**：

| 以 textW=30, textH=15 为例 | `computeLabelFrame` | `quickRelocateTimeLabel` | `addTimeLabelToCell` |
|---|---|---|---|
| 使用的间距参数 | `kMessageTimeBaseSpacing = 2` | `pw = w + 0`, `ph = h + 0` | `pw = w + 4`, `ph = h + 4` |
| sender cx | `x = cvLeft - 30 - 2 = cvLeft - 32` | `cx = cvLeft - 30/2 = cvLeft - 15` | `cx = cvLeft - 34/2 = cvLeft - 17` |
| 标签最右边缘 | `cvLeft - 2`（2pt间距） | `cvLeft - 0`（0pt，紧贴） | `cvLeft - 17 + 17 = cvLeft` ?→ 但 padding 宽出2pt，实际文字右边缘 = cvLeft - 2 |

**根因**：`quickRelocateTimeLabel` 是比 `addTimeLabelToCell` 更早写的一版"快速版本"，它直接用 `label.frame.size` 作为 `pw/ph`，而 `label.frame` 在当时的旧架构中是在 `addTimeLabelToCell` 中设置的（已经+4了），但 `quickRelocate` 在 updateNodeStatus 被调用时可能 label.frame 尚未被 addTimeLabelToCell 设置（时序问题），导致使用了旧架构中的 `w+0` 而不是 `w+4`。

**优化后状态**：以上三个函数已全部删除。现在所有定位逻辑统一在 `updateNodeStatus` 中，使用 `frame=(w+4, h+4)` → `center = cvLeft - paddedW/2`，始终产生 2pt 间距。

| 对比 | 功能说明 | 反编译版 | 本插件（优化后） |
|------|---------|---------|--------|
| 定位函数个数 | 代码中有多少个独立的定位实现 | 1（单函数多分支） | **1（仅 updateNodeStatus 内联）** |
| 不同时机结果一致性 | 是否始终一致 | ✅ 始终一致 | ✅ **已统一** |
| 分支复杂度 | 定位逻辑的组织方式 | 高（同一函数内大量 case+goto） | 低（switch-case 清晰可读） |
| position 0/1 | 头像上方/下方的定位参照 | avatarFrame → 居中 | ✅ avatarFrame → 居中 **（已恢复）** |

---

## 三、反编译版"多路径"完整代码路径分析

```
FUN_0003a06c(updateNodeStatus 替换):
│
├─ 路径① viewModel==nil 或模块关闭 → hideLabel, return
│
├─ 路径② viewModel 有值但无 isSender → 用默认 sender/receiver 配色 + fallbackFrame
│     └─ goto LAB_0003a9dc (fallback 定位: contentView=cellFrame)
│
├─ 路径③ viewModel.isSender==YES (sender) + 各 position
│     ├─ pos=0: 找头像 → 有→用avatarFrame / 无→goto_3a908(fallback: contentView)
│     ├─ pos=1: 找头像 → 有→用avatarFrame / 无→goto_3a908(fallback: contentView)
│     ├─ pos=2: 重定向到 pos=7
│     ├─ pos=3: 用 contentView (GetMinX + w/2 → sender, GetMaxX - w/2 → receiver)
│     ├─ pos=4: 用 contentView (GetMaxX - w/2 → sender, GetMinX + w/2 → receiver)
│     ├─ pos=5: 用 contentView (GetMinY 参照)
│     ├─ pos=6: 用 contentView (GetMinY 参照)
│     └─ pos=7: 用 contentView (GetMinX - w/2 → sender, GetMaxX + w/2 → receiver)
│
├─ 路径④ viewModel.isSender==NO (receiver) + 各 position
│     └─ (同③，但 sender/receiver 坐标计算调换)
│
└─ 路径⑤ 未知position → fallback LAB_0003a9dc
```

---

## 四、气泡扩展逻辑对比（位置7）

| 维度 | 功能说明 | 本插件 `repl_TextMsgCell_setFrameBgImg` (L1171-1194) | 反编译版 `FUN_0003b058` (L34985-35031) | 结论 |
|------|---------|------------------------------------------------------|----------------------------------------|------|
| 触发条件 | 进入气泡扩展逻辑的前置检查 | `position==7 && showMessageTime` | `position==7`（无额外开关检查） | 反编译版更简洁 |
| isSender 获取 | 如何判断当前消息是发送方还是接收方 | `[viewModel valueForKey:@"isSender"]` + 多层 fallback | 直接 `FUN_000c6f20(viewModel)` | 本插件 fallback 更健壮 |
| sender: x | 发送方气泡的 x 坐标如何调整以向左扩展 | `x -= extWidth` | `x = x - extWidth` | ✅ 一致 |
| receiver: x | 接收方气泡的 x 坐标如何调整（接收方气泡向右扩展） | 不变 | 不变 | ✅ 一致 |
| sender & receiver: w | 无论收发，气泡宽度如何增加以容纳时间标签 | `w += extWidth` | `w = w + extWidth` | ✅ 一致 |
| 默认 extWidth | 未配置时的默认扩展宽度 | 38pt | 38pt | ✅ 一致 |
| 配置来源 | 扩展宽度值从哪里读取 | `config.messageTimeBubbleExtWidth`（默认0→代码中fallback到38） | `NSUserDefaults(@"bubbleExtensionWidth")`（默认38） | 等效 |
| viewModel 获取 | 如何从 self 获取 viewModel 对象 | 多层 `valueForKey` fallback | 单次 `FUN_000d1000()` 调用 | 本插件更健壮 |

---

## 五、复合消息（TextMessageSubViewModel）过滤

```c
// 反编译版 FUN_0003c628 (L35697-35749)
if ([viewModel isKindOfClass:[TextMessageSubViewModel class]]) {
    parentModel = [viewModel parentModel];
    subViewModels = [parentModel subViewModels];
    if (position < 7) {
        targetIndex = 0;  // 只在第一个子视图
    } else {  // position >= 7
        targetIndex = [subViewModels count] - 1;  // 只在最后一个子视图
    }
    return ([subViewModels indexOfObject:viewModel] == targetIndex);
}
return YES;  // 非 TextMessageSubViewModel 总是显示
```

| 消息类型 | 功能说明 | 本插件 | 反编译版 | 谁更好 |
|----------|---------|--------|---------|--------|
| 纯文本消息 | 普通的单条文字消息，只包含纯文本内容 | ✅ 显示 | ✅ 显示 | 平手 |
| 文字+链接卡片 | 一条消息包含文字和可点击的链接卡片（如公众号文章链接） | ❌ 两个子视图都显示标签（重复） | ✅ 仅最后一个子视图显示 | **反编译版** |
| 文字+表情 | 一条消息包含文字和表情符号的组合 | ❌ 多个子视图都显示 | ✅ 仅最后一个子视图显示 | **反编译版** |
| 文字+小程序分享 | 一条消息包含文字和小程序分享卡片的组合 | ❌ 多个子视图都显示 | ✅ 仅最后一个子视图显示 | **反编译版** |

---

## 六、全面逐项功能对比（本插件 ↔ 反编译版）

### 6.1 本插件独有功能（反编译版没有）

| 功能 | 功能说明 | 本插件 | 反编译版 | 谁更好 |
|------|---------|--------|---------|--------|
| 隐藏原版聊天时间分隔线 | 移除微信原生在消息之间显示的"上午/下午 HH:mm"时间分隔线 | ✅ | ❌ | **本插件** |
| 好友昵称添加时间后缀 | 在好友列表的昵称后面添加该好友首次联系的时间后缀 | ✅ | ❌ | **本插件** |
| 独立调试日志 | 消息时间模块独立日志 `messagetime.log` | ✅ | ❌ | **本插件** |

### 6.2 反编译版有 → 本插件有没有

| 功能 | 功能说明 | 反编译版 | 本插件 | 谁更好 |
|------|---------|---------|--------|--------|
| 复合消息过滤 | 当一条消息包含多个子视图（如文字+链接卡片）时，通过 `FUN_0003c628` 判断 `TextMessageSubViewModel`，位置0~6只在第一个子视图显示标签，位置7只在最后一个子视图显示，避免重复 | ✅ `FUN_0003c628` TextMessageSubViewModel 判断 | ❌ | **反编译版** 🔴 |
| 时间去重（同 session 比较） | 在同一个会话中，如果当前消息与上一条消息的发送时间相同，则不重复显示时间标签，通过全局字典 `DAT_0013ad68` 按 session 缓存并比较 | ✅ `FUN_0003bb04` + 全局字典 `DAT_0013ad68` | ❌ | **反编译版** 🟡 |
| 双时间格式切换 | 支持配置两种时间格式，用逗号分隔（如 `"HH:mm:ss,HH:mm"`），根据去重比较结果自动切换到第二种格式，实现更紧凑的显示 | ✅ 逗号分隔格式 + 根据去重结果自动切换 | ❌ | **反编译版** 🟡 |
| 中文星期支持 | 时间格式中的 `{EE}` 占位符自动使用 `zh_Hans_CN` locale，输出"周一"~"周日"中文星期格式 | ✅ `{EE}` → `zh_Hans_CN` locale | ❌ | **反编译版** |
| 暗色模式独立 sender/receiver 配色 | 在暗黑模式下，发送方和接收方各自有独立的文字色和背景色配置（sender.textColor.dark / receiver.textColor.dark），而非统一的 autoDark 提亮 | ✅ sender.textColor.dark + receiver.textColor.dark + backgroundColor.dark | ❌（用 autoDark 自动提亮） | **反编译版** — 更灵活 |
| 全局队列异步计算 | 在 `tableView:cellForRowAtIndexPath:` 中使用 `dispatch_get_global_queue(0,0)` 将时间格式化计算移到后台线程，避免阻塞主线程 | ✅ `dispatch_get_global_queue(0,0)` | ❌（主线程计算） | **反编译版** |
| initWithViewModel 早期创建 | 在 `CommonMessageCellView.initWithViewModel:` 中即创建时间标签（frame=88×28，font/color已设），比 willDisplayCell 更早，确保标签不会闪屏 | ✅ cell 初始化即创建占位标签 | ❌ | **反编译版** |
| adjustsFontSizeToFitWidth | 设置标签的 `adjustsFontSizeToFitWidth = YES`，当时间字符串较长时自动缩小字体以适应标签宽度 | ✅ YES（长文本自适应缩小） | ❌ | **反编译版** |
| cornerRadius 默认值 | 时间标签的圆角半径默认值（影响标签外观的圆润程度） | ✅ 8pt | ✅ 0pt | 视觉偏好，平手 |
| textAlignment | 时间标签文字的对齐方式（影响多行时的排列方向） | ✅ Center（居中对齐） | ✅ Natural（左对齐） | 视觉偏好，平手 |

### 6.3 两版都有的功能（实现对比）

| 功能 | 功能说明 | 本插件 | 反编译版 | 谁更好 |
|------|---------|--------|---------|--------|
| 总开关 | 控制整个消息时间显示模块的启用/关闭 | `WCP_ShowMessageTime` | `enabled` | 平手 |
| 位置选择 | 时间标签显示在消息气泡周围的哪个位置（0~7） | 0~7 | 0~7 | 平手 |
| offsetX/offsetY | 时间标签相对于默认位置的水平和垂直偏移量（精细调整） | ✅ | ✅ | 平手 |
| sender/receiver 独立文字色(light) | 浅色模式下，发送方和接收方的时间标签文字颜色可独立配置 | ✅ | ✅ | 平手 |
| sender/receiver 独立背景色(light) | 浅色模式下，发送方和接收方的时间标签背景颜色可独立配置 | ✅ | ✅ | 平手 |
| 气泡扩展(位置7) | 位置7时，气泡向侧边扩展以容纳时间标签的逻辑 | ✅ 一致 | ✅ 一致 | 平手 |
| fontSize 配置 | 时间标签文字的字体大小配置项 | ✅ 默认7pt | ✅ 默认7pt | 平手 |
| boldFont 配置 | 时间标签文字是否使用粗体字 | ✅ | ✅ | 平手 |
| 时间格式化 | 时间字符串的显示格式（如 HH:mm:ss） | 单一格式 | 双格式+locale | **反编译版** |
| 标签定位间距 | 标签与气泡边缘之间的视觉间距一致性 | **1套统一定位** ✅（已修复） | **1套始终2pt** | 平手（优化后一致） |
| 主线程性能 | 时间格式化计算是否异步 | ✅ **全局队列异步**（已修复） | ✅ 全局队列异步 | 平手（优化后一致） |
| 标签创建时机 | 标签首次创建的生命周期阶段 | ✅ **initWithViewModel**（已修复） | ✅ initWithViewModel | 平手（优化后一致） |

---

## 七、位置7 核心总结一览

```
┌─────────────────────────────────────────────────────────────────┐
│                    位置7 = 气泡旁边（外侧）                       │
├───────────────┬──────────────────────┬──────────────────────────┤
│    维度        │      本插件          │      反编译版             │
├───────────────┼──────────────────────┼──────────────────────────┤
│ 标签X(sender) │ cvLeft - pw/2        │ cvLeft - paddedW/2       │
│               │ = cvLeft - (w+4)/2   │ = cvLeft - (textW+4)/2   │
│ 标签X(rcver)  │ cvRight + pw/2       │ cvRight + paddedW/2      │
│               │ = cvRight+(w+4)/2    │ = cvRight+(textW+4)/2    │
│ 标签Y         │ cvBottom - ph/2      │ cvBottom - paddedH/2     │
│               │ = cvBottom-(h+4)/2   │ = cvBottom-(textH+4)/2   │
│ 间距实现      │ pw=w+4 L2=78-881     │ frame(0,0,w+4,h+4)       │
│ 视觉间距      │ 2pt ✅               │ 2pt ✅                   │
├───────────────┼──────────────────────┼──────────────────────────┤
│ 气泡sender x  │ x-38                 │ x-38          ✅一致     │
│ 气泡sender w  │ w+38                 │ w+38          ✅一致     │
│ 气泡rcver x   │ 不变                 │ 不变          ✅一致     │
│ 气泡rcver w   │ w+38                 │ w+38          ✅一致     │
│ 默认扩展      │ 38pt                 │ 38pt          ✅一致     │
├───────────────┼──────────────────────┼──────────────────────────┤
│ 定位实现数    │ 1套(统一在updateNodeStatus) │ 1套(始终2pt间距)         │
│ 定位一致性    │ ✅ 已统一                   │ ✅ 始终一致              │
├───────────────┼──────────────────────┼──────────────────────────┤
│ 复合消息      │ ❌ 每子视图都显示     │ ✅ 仅最后子视图显示       │
│ 时间去重      │ ❌                   │ ✅ 同session比较          │
│ 双格式        │ ❌                   │ ✅ 逗号分隔               │
│ 暗色独立配色  │ ❌                   │ ✅ sender/receiver.dark   │
├───────────────┼──────────────────────┼──────────────────────────┤
│ 隐藏原版时间  │ ✅                   │ ❌                        │
│ 时间后缀      │ ✅                   │ ❌                        │
│ 调试日志      │ ✅                   │ ❌                        │
└───────────────┴──────────────────────┴──────────────────────────┘
```

---

## 八、改进建议（按优先级）

### 🔴 P0 — 必须修
1. ~~**统一三套定位的 padding**~~ ✅ **已修复** — 优化后 updateNodeStatus 统一使用 `frame=(w+4, h+4)` → center 自动 2pt 间距。
2. ~~**position 0/1 头像定位恢复**~~ ✅ **已修复** — 删三套定位函数时误删，已在 updateNodeStatus 中恢复 avatarFrame 定位。
3. ~~**Hex 字符串转 UIColor**~~ ✅ **已修复** — `senderTextColorHex` 等属性返回 `NSString*` 而非 `UIColor*`，现在通过 `[config colorFromHex:]` 正确转换，`colorFromHex` 已支持 8 位 ARGB。
4. **添加 TextMessageSubViewModel 复合消息过滤** — 参考 `FUN_0003c628`，避免复合消息中出现重复标签。

### 🟡 P1 — 建议补
5. **添加时间去重机制** — 参考 `FUN_0003bb04` + 全局字典，同 session 时间相同则不重复显示。
6. ~~**cellForRow 使用全局队列异步计算**~~ ✅ **已修复** — 照抄反编译版，使用 `dispatch_get_global_queue(0,0)` 异步计算。

### 🟢 P2 — 锦上添花
7. 暗色模式独立 sender/receiver 配色
8. 双时间格式（逗号分隔）+ 中文星期支持

---

## 九、为什么微信优化不需要 Cell 复用防错乱

### 核心答案

**微信优化不需要 `prepareForReuse` 清理标签，是因为它的数据绑定在 `viewModel` 上，而不是 `cell` 或 `cellView` 上。**

### 详细分析

#### 旧架构的问题（优化前需要 prepareForReuse）

```
旧架构数据流：
  cell ──关联对象──→ cachedMsgWrap     ← 存在 cell 上，cell 复用后会过时
  cell ──关联对象──→ messageTimeCreateTime ← 存在 cell 上
  cellView ──关联对象──→ msgTimeLabel   ← 存在 cellView 上
```

旧架构在 `cellForRow` 中把 `cachedMsgWrap` 缓存到 `cell` 上。当 cell 被滚动复用、配置为新消息时，旧的 `cachedMsgWrap` 仍然留在 `cell` 上。`willDisplayCell` 读取缓存的 msgWrap 来创建标签——但如果 cell 被复用给一个不需要标签的新消息（如系统消息），旧的标签不会自动消失。

所以 `prepareForReuse` 必须手动：
- `removeFromSuperview` 旧标签
- 清空 `cachedMsgWrap`
- 清空 `messageTimeCreateTime`

#### 新架构为什么不需要

```
新架构数据流：
  viewModel ──关联对象──→ messageTimeText  ← 存在 viewModel 上!
  cellView ──关联对象──→ msgTimeLabel       ← 存在 cellView 上（仅style容器）
```

**关键区别：时间数据 (`messageTimeText`) 存储在 `viewModel` 上，而不是 `cell` 上。**

每次 cellForRow 触发时：
1. 微信创建/获取一个新的 viewModel（与当前消息一一对应）
2. 全局队列异步把这个 viewModel 对应的时间文本缓存到 viewModel 自身
3. `updateNodeStatus` 总是从**当前** viewModel 读取时间文本

当 cell 被复用时：
- CellView 重新走 `initWithViewModel:` → 新的 viewModel → 新标签被创建（或标签被复用但 text 在 updateNodeStatus 中会被更新）
- `updateNodeStatus` 读取的是**新 viewModel** 上的 `messageTimeText`
- 旧 viewModel 已被替换，其关联的数据随旧 viewModel 生命周期自动失效

**数据所有权链**：
```
每条消息 ←→ 一个 viewModel（强引用）
                └─ 关联对象 messageTimeText（时间字符串）

每个 cell ←→ 一个 cellView（复用或新建）
                └─ 关联对象 msgTimeLabel（UILabel 容器）
```

viewModel 跟随消息，label 跟随 cellView。当 cell 复用时 cellView 被替换为新的 viewModel：
- 新 viewModel 上有对应新消息的 `messageTimeText`
- updateNodeStatus 读取新 viewModel → 正确的标签文字
- 没有任何数据交叉污染

### 对比总结

| | 旧架构 | 新架构 |
|--|--------|--------|
| 时间数据位置 | cell 关联对象 | **viewModel 关联对象** |
| 数据生命周期 | 跟着 cell（复用不变） | 跟着 viewModel（一消息一实例） |
| 复用后数据状态 | 旧数据残留在 cell 上 | 自动使用新 viewModel 的数据 |
| 需要 prepareForReuse | ✅ 必须清理 | ❌ **不需要** |

### 类比理解

这就像"租房"vs"买房"：
- 旧架构：你在房间（cell）的墙上贴了便签（时间数据）。房客换人（cell 复用），便签还是旧的。你必须手动撕掉。
- 新架构：你把便签贴在房客身份证（viewModel）上。房客换人，身份证也换了，便签自动跟着新身份证走了。