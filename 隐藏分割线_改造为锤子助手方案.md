# 隐藏分割线功能改造 — 对齐锤子助手 2.4.1 方案

> **目标**：将当前的「微信优化式全局 layoutSubviews Hook + 10维过滤」替换为「锤子助手式精准属性级 Hook」  
> **改造文件**：[UIPurifyHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/Layout/UIPurifyHook.m)  
> **干净行数变化**：删除 ~140 行 → 新增 ~65 行，净减少 ~75 行

---

## 一、现状诊断

当前分隔线隐藏功能位于 `UIPurifyHook.m`，由以下三部分组成：

| 组件 | 位置 (行号) | 行数 | 内容 |
|------|:-----------|:----:|------|
| 类缓存 + 静态变量 | L254-261 | 8 | `_WCTimeLineViewControllerClass` 等 3 个 Class 缓存 + 2 个 `_orig` 指针 |
| `hook_UIView_layoutSubviews` | L263-385 | 123 | 10维过滤 + PATH A/B 分流 + 父视图链三重保护 |
| `hook_WCTableView_getSeparator` | L387-391 | 5 | Mio 独家增强 |
| install 中的安装代码 | L497-516 | 20 | 类缓存赋值 + 2 个 MSHookMessageEx |

**全部拆除**，替换为锤子助手方案。

---

## 二、改造方案总览

```
改造前（微信优化风格）                        改造后（锤子助手风格）

UIView.layoutSubviews ──10维过滤──→ hidden    UITableView.separatorColor  ──→ 返回 clearColor
                                                  │
WCTableViewManager.getSeparator ──→ nil        UITableView.separatorStyle ──→ 返回 None(0)
                                                  │
                                               WCColor.seperatorColor     ──→ 返回 clearColor
                                                (微信自有颜色管理类的类方法)
```

| 维度 | 改造前 | 改造后 |
|------|:-------|:-------|
| Hook 数量 | 2 个 | 3 个 |
| 触发时机 | 每次 UIView.layoutSubviews | 仅 getter 调用时 |
| 循环/遍历 | 每次最多 2×11 层父视图链 | 无 |
| goto/标签 | 3 个 goto label | 无 |
| 运行时开销 | 每个 UIView 都执行 | 仅分割线渲染时触发 |
| 过滤维度 | 10 维 + 白名单 | 0（无过滤，精准命中） |

---

## 三、拆旧 — 删除清单

从 [UIPurifyHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/Layout/UIPurifyHook.m) 中删除以下内容：

### 3.1 删除静态变量声明（L254-261）

```objc
// ── 删除以下 8 行 ──
static Class _WCTimeLineViewControllerClass = nil;
static Class _FTSBrandContactCellClass       = nil;
static Class _MPSubscriptionViewControllerClass = nil;

// ── 函数声明 ──
static IMP _orig_UIView_layoutSubviews = NULL;
static IMP _orig_WCTableView_getSeparator = NULL;
```

### 3.2 删除 `hook_UIView_layoutSubviews` 函数体（L263-385）

```objc
// ── 删除 L263-385 全部 123 行 ──
static void hook_UIView_layoutSubviews(id self, SEL _cmd) { ... }
```

### 3.3 删除 `hook_WCTableView_getSeparator` 函数（L387-391）

```objc
// ── 删除 L387-391 全部 5 行 ──
static id hook_WCTableView_getSeparator(id self, SEL _cmd) { ... }
```

### 3.4 删除注释块（L244-252）

```objc
// ── 删除 L244-252 全部 9 行 ──
// ============================================================
// 分隔线隐藏 — 100% 复刻 FUN_0004627c (微信优化)
// ...
```

### 3.5 删除 install 中的安装代码（L497-516）

```objc
// ── 删除 L497-516 全部 20 行 ──
    // ⑦ 分隔线隐藏 — 100% 复刻微信优化 (FUN_000261e8 + FUN_00026248)
    
    // Step 1：类缓存预加载 (对齐 FUN_000461e8)
    _WCTimeLineViewControllerClass = NSClassFromString(@"WCTimeLineViewController");
    ...
    
    // Step 2：UIView.layoutSubviews 全局 Hook (对齐 FUN_00046248)
    MSHookMessageEx([UIView class], @selector(layoutSubviews), ...);
    
    // Step 3：WCTableViewManager.getSeparator → nil (Mio 独家，比微信优化更彻底)
    cls = objc_getClass("WCTableViewManager");
    ...
```

---

## 四、建新 — 新增清单

### 4.1 新增静态变量声明（替代 L254-261）

```objc
// ============================================================
// 分隔线隐藏 — 对齐锤子助手 2.4.1 (FUN_0075159c)
// 方案：精准 Hook 3 个 getter/setter，无过滤、无遍历、无版本锁
// ============================================================

// ── 3 个原始 IMP 指针 ──
static IMP _orig_UITableView_separatorColor = NULL;
static IMP _orig_UITableView_separatorStyle = NULL;
static IMP _orig_WCColor_seperatorColor     = NULL;
```

### 4.2 新增 Hook #1: `UITableView.separatorColor`

```objc
// ── Hook #1: UITableView.separatorColor → 对齐 FUN_007516c0 ──

static id hook_separatorColor(id self, SEL _cmd) {
    if (purifyReadConfig(@"HideSeparatorLine")) {
        return [UIColor clearColor];
    }
    return ((id (*)(id, SEL))_orig_UITableView_separatorColor)(self, _cmd);
}
```

**对标代码：** [锤子助手 FUN_007516c0](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754286-L1754302)  
**逻辑：** 开关打开 → `return [UIColor clearColor]`；开关关闭 → 调用原始实现

### 4.3 新增 Hook #2: `UITableView.separatorStyle`

```objc
// ── Hook #2: UITableView.separatorStyle → 对齐 FUN_00751724 ──

static UITableViewCellSeparatorStyle hook_separatorStyle(id self, SEL _cmd) {
    if (purifyReadConfig(@"HideSeparatorLine")) {
        return UITableViewCellSeparatorStyleNone;  // 0
    }
    return ((UITableViewCellSeparatorStyle (*)(id, SEL))_orig_UITableView_separatorStyle)(self, _cmd);
}
```

**对标代码：** [锤子助手 FUN_00751724](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754306-L1754320)  
**逻辑：** 开关打开 → `return 0 (None)`；开关关闭 → 调用原始实现

### 4.4 新增 Hook #3: `WCColor.seperatorColor`（类方法）

```objc
// ── Hook #3: WCColor.seperatorColor (类方法) → 对齐 FUN_00751774 ──

static id hook_WCColor_seperatorColor(id self, SEL _cmd) {
    if (purifyReadConfig(@"HideSeparatorLine")) {
        return [UIColor clearColor];
    }
    return ((id (*)(id, SEL))_orig_WCColor_seperatorColor)(self, _cmd);
}
```

**对标代码：** [锤子助手 FUN_00751774](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754324-L1754340)  
**逻辑：** 与 Hook #1 相同，但目标不同 —— 这是微信自有颜色管理类的 `+seperatorColor` 类方法

### 4.5 新增 install 中的安装代码（替代 L497-516）

```objc
    // ── 分隔线隐藏 — 对齐锤子助手 2.4.1 (FUN_0075159c) ──
    // 锤子助手有版本检查 >= 8.0.29，我们通过运行时 class 存在性代替版本锁
    {
        // Hook #1: UITableView.separatorColor → clearColor
        MSHookMessageEx([UITableView class], @selector(separatorColor),
            (IMP)hook_separatorColor, &_orig_UITableView_separatorColor);
        WPLog(@"UIPurify", @"[Hook] ✓ UITableView.separatorColor");

        // Hook #2: UITableView.separatorStyle → None
        MSHookMessageEx([UITableView class], @selector(separatorStyle),
            (IMP)hook_separatorStyle, &_orig_UITableView_separatorStyle);
        WPLog(@"UIPurify", @"[Hook] ✓ UITableView.separatorStyle");

        // Hook #3: WCColor.seperatorColor (类方法，需取元类)
        Class wcColorCls = objc_getClass("WCColor");
        if (wcColorCls) {
            Class wcColorMeta = object_getClass(wcColorCls);
            MSHookMessageEx(wcColorMeta, sel_registerName("seperatorColor"),
                (IMP)hook_WCColor_seperatorColor, &_orig_WCColor_seperatorColor);
            WPLog(@"UIPurify", @"[Hook] ✓ WCColor.seperatorColor");
        } else {
            WPLog(@"UIPurify", @"[Hook] ⚠ WCColor class not found");
        }
    }
```

**关键细节：** `WCColor.seperatorColor` 是类方法（`+`），所以 `MSHookMessageEx` 的第一个参数必须是 **元类**（`object_getClass(wcColorCls)`），而不是实例类。这与锤子助手 [L1754277-L1754278](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754276-L1754278) 完全一致。

---

## 五、完整改造后的代码

以下是 `UIPurifyHook.m` 中分隔线相关部分的**最终形态**：

```objc
// ============================================================
// 分隔线隐藏 — 对齐锤子助手 2.4.1 (FUN_0075159c)
// 方案：精准 Hook 3 个 getter，无过滤、无遍历、无版本锁
// ============================================================

// ── 3 个原始 IMP 指针 ──
static IMP _orig_UITableView_separatorColor = NULL;
static IMP _orig_UITableView_separatorStyle = NULL;
static IMP _orig_WCColor_seperatorColor     = NULL;

// ── Hook #1: UITableView.separatorColor → 对齐 FUN_007516c0 ──
static id hook_separatorColor(id self, SEL _cmd) {
    if (purifyReadConfig(@"HideSeparatorLine")) {
        return [UIColor clearColor];
    }
    return ((id (*)(id, SEL))_orig_UITableView_separatorColor)(self, _cmd);
}

// ── Hook #2: UITableView.separatorStyle → 对齐 FUN_00751724 ──
static UITableViewCellSeparatorStyle hook_separatorStyle(id self, SEL _cmd) {
    if (purifyReadConfig(@"HideSeparatorLine")) {
        return UITableViewCellSeparatorStyleNone;
    }
    return ((UITableViewCellSeparatorStyle (*)(id, SEL))_orig_UITableView_separatorStyle)(self, _cmd);
}

// ── Hook #3: WCColor.seperatorColor (类方法) → 对齐 FUN_00751774 ──
static id hook_WCColor_seperatorColor(id self, SEL _cmd) {
    if (purifyReadConfig(@"HideSeparatorLine")) {
        return [UIColor clearColor];
    }
    return ((id (*)(id, SEL))_orig_WCColor_seperatorColor)(self, _cmd);
}
```

install 方法末尾：

```objc
    // ── 分隔线隐藏 — 对齐锤子助手 2.4.1 (FUN_0075159c) ──
    {
        MSHookMessageEx([UITableView class], @selector(separatorColor),
            (IMP)hook_separatorColor, &_orig_UITableView_separatorColor);
        WPLog(@"UIPurify", @"[Hook] ✓ UITableView.separatorColor");

        MSHookMessageEx([UITableView class], @selector(separatorStyle),
            (IMP)hook_separatorStyle, &_orig_UITableView_separatorStyle);
        WPLog(@"UIPurify", @"[Hook] ✓ UITableView.separatorStyle");

        Class wcColorCls = objc_getClass("WCColor");
        if (wcColorCls) {
            Class wcColorMeta = object_getClass(wcColorCls);
            MSHookMessageEx(wcColorMeta, sel_registerName("seperatorColor"),
                (IMP)hook_WCColor_seperatorColor, &_orig_WCColor_seperatorColor);
            WPLog(@"UIPurify", @"[Hook] ✓ WCColor.seperatorColor");
        } else {
            WPLog(@"UIPurify", @"[Hook] ⚠ WCColor class not found");
        }
    }
```

---

## 六、锤子助手源码行级对标

| 锤子助手 | WeChatPlugin 改造后 | 说明 |
|----------|:-------------------|------|
| [L1754257 FUN_0075159c 入口](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754257) | install 末尾 `{...}` 代码块 | Hook 安装入口，锤子助手多了版本检查 |
| [L1754273 objc_getClass("UITableView")](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754273) | `[UITableView class]` | 获取 UITableView 类 |
| [L1754274 MSHookMessageEx("separatorColor")](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754274) | `@selector(separatorColor)` | Hook #1 |
| [L1754275 MSHookMessageEx("separatorStyle")](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754275) | `@selector(separatorStyle)` | Hook #2 |
| [L1754286-4302 FUN_007516c0](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754286) | `hook_separatorColor` | 返回 clearColor |
| [L1754306-4320 FUN_00751724](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754306) | `hook_separatorStyle` | 返回 None(0) |
| [L1754324-4340 FUN_00751774](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754324) | `hook_WCColor_seperatorColor` | WCColor 类方法 Hook |
| [L1754276-4278 object_getClass](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754276) | `object_getClass(wcColorCls)` | 获取元类来 Hook 类方法 |
| [L1933485-3490 FUN_00849420](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1933485) | `purifyReadConfig(@"HideSeparatorLine")` | 设置读取（实现方式不同但语义等价） |
| [L1754272 版本检查 != -1](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754272) | ✅ 无版本检查 | 锤子助手有版本锁，我们通过运行时 class 存在性兜底 |

---

## 七、改造步骤（操作清单）

### Step 1：删除旧代码

在 [UIPurifyHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/Layout/UIPurifyHook.m) 中删除以下行：

| 删除范围 | 行数 |
|----------|:----:|
| L244 ～ L252 | 9 行（注释块） |
| L254 ～ L261 | 8 行（静态变量） |
| L263 ～ L385 | 123 行（hook_UIView_layoutSubviews 函数体） |
| L387 ～ L391 | 5 行（hook_WCTableView_getSeparator 函数体） |
| L497 ～ L516 | 20 行（install 中的安装代码） |
| **合计** | **165 行** |

### Step 2：插入新代码

在删除位置插入上述「第五节」中的全部代码。

### Step 3：验证

1. **编译验证**：`make` 确保通过
2. **功能验证**：
   - 打开「界面净化 → 隐藏水平分割线」开关
   - 检查聊天列表、朋友圈、通讯录、设置页的分割线是否消失
   - 关闭开关，确认分割线恢复
3. **日志验证**：查看 WPLog 输出是否包含 `[Hook] ✓ UITableView.separatorColor` 等 3 条

---

## 八、改造优势总结

| | 改造前（微信优化风格） | 改造后（锤子助手风格） |
|--|:-----------------------|:------------------------|
| 代码行数 | ~165 行 | ~46 行 |
| 循环 | 双重 parent chain（最多 22 次遍历） | 0 |
| goto | 3 个（CLEANUP / CHECK_PARENT_CHAIN） | 0 |
| 运行时性能 | 每个 UIView.layoutSubviews 触发 | 仅分割线属性调用时触发 |
| 误伤风险 | 10维过滤仍有概率 | 零（精准 API 级命中） |
| 维护成本 | 高（过滤条件需跨版本调参） | 低（UIKit 标准 API 极少变动） |
| 版本兼容 | 无锁但需调参 | 无锁 + 无需调参 |
| 配置相容 | `HideSeparatorLine` → WCP_前缀 | **不变**（复用 purifyReadConfig） |

---

## 九、锤子助手版本检查的处理

锤子助手在 [L1754272](file:///www/wwwroot/ios/插件/锤子助手反编译/chuizizhushou2.4.1.dylib.c#L1754272) 有版本检查 `>= 8.0.29`，我们的策略：

```
锤子助手：版本号字符串比较 → 版本不够跳过安装
WeChatPlugin：无版本检查 + 运行时 class 存在性检测 → WCColor 不存在时仅打 WPLog 警告
```

`UITableView.separatorColor` 和 `separatorStyle` 是 iOS UIKit 标准 API（iOS 2.0+），永远存在。  
`WCColor.seperatorColor` 是微信私有类，我们通过 `objc_getClass("WCColor")` 的返回值为 nil 来做运行时分步降级（hook 安装但不影响其他 hook），比硬版本号更优雅。

---

## 十、不需要改动的部分

以下文件和配置**无需修改**，改造是纯替换 `UIPurifyHook.m` 中的分隔线 hook 实现：

| 文件 | 说明 |
|------|------|
| [PluginConfig.h](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.h) L70 | `hideSeparatorLine` 属性声明不变 |
| [WPUIPurifyVC.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/SettingEntry/WPUIPurifyVC.m) L30 | UI 开关入口不变（仍然是"隐藏水平分割线"开关） |
| [SettingEntryHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/SettingEntry/SettingEntryHook.m) L179 | `@"HideSeparatorLine": @"hideSeparatorLine"` 映射不变 |
| `UIPurifyHook.m` 其他 hook | ChatTime/Sys/Pat/Voice/YYAsync/MMGrow 全部保持不变 |

---

## 十一、一句话总结

> **150+ 行复杂视图遍历 → 3 个一行 if/else getter Hook。**  
> 覆盖范围不减（系统分割线 + 微信自定义颜色），性能开销降至零，维护成本趋近于零。