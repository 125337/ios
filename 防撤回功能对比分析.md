# 微信防撤回功能对比分析报告（深度版）

> 对比对象：
> - **A方（微信助手）**: `\www\wwwroot\ios\插件\微信助手 3.9-5(1).dylib` — 成熟微信插件（内部代号 MiYou，仅有 dylib 二进制，无源代码）
> - **B方（本项目）**: `\www\wwwroot\ios\WeChatPlugin\Modules\Revoke\` — 本项目自建防撤回模块

> **分析方法**: 对 dylib 进行了 9.5 万行字符串全量提取，覆盖 `__objc_classname`、`__objc_methname`、`__objc_methtype`、`__objc_ivar`、block encoding 等所有 objc 元数据段，并结合方法签名、ivar 声明、属性类型编码等信息交叉推断实现细节。

---

## 一、概述总结

| 维度 | 微信助手 (MiYou) | 本项目 (WeChatPlugin) |
|------|:---:|:---:|
| **核心拦截思路** | 全消息管线拦截：hook 消息生命周期中 **34+ 个 WeChat 系统方法**，revoke 只是其拦截的众多信号之一 | `PostInsertParsedXmlSysMsg` 最早拦截（阻断）+ 4 道 revoke 防线 + UI 层阻断 |
| **Hook 覆盖范围** | **34+ 个 WeChat 方法** 被引用（含 hook 目标 + API 调用），覆盖消息增删改查/会话管理/通知/UI渲染/群组操作全管线 | **6 个** hook 点，`PostInsertParsedXmlSysMsg` 最早阻断 + 全 revoke 流程覆盖
| **架构模式** | 模块化插件架构，`registerControllerWithTitle:version:controller:` 注册子功能 | `RevokeHook`（安装+验证）+ `RevokeHandler`（逻辑），精简实用 |
| **代码可维护性** | 无源代码，完全无法自行修改 | 有源代码，结构清晰 |
| **运行时安全** | `checkHook:` / `checkHookWithSeq:` 动态验证 | `checkHook:` / `checkHookWithSeq:` 动态验证+自动恢复 ✅ |

---

## 二、架构深度对比

### 2.1 微信助手 (MiYou) 完整架构

通过提取 dylib 中全部约 200 个 `MiYou*` 类名、2066 个 objc 方法名、全部 ivar 声明和 block 编码，还原出其架构如下：

```
┌─────────────────────────────────────────────────────────────────┐
│                     MiYouViewController                          │
│                     (主控制器/设置入口)                            │
│                                                                  │
│  registerControllerWithTitle:version:controller:                 │
│       │                                                          │
│       ├── 防撤回 Feature (Revoke)                                 │
│       ├── 自动回复 Feature (AutoReply/QuickReply/KeywordReply)    │
│       ├── 关键词提示 Feature (KeywordTips) / 关键词删人            │
│       ├── 消息任务 Feature (TaskModel/AddTask)                    │
│       ├── 弹幕 Feature (DanMuView)                                │
│       ├── 消息工具栏 Feature (MessageToolBar)/手势                │
│       ├── 快速删除 Feature (KeyDelete)                            │
│       ├── 消息回复 Feature (RepayMsg)                             │
│       ├── ChatGPT Feature                                         │
│       ├── 消息预览/悬浮提示 Feature                                │
│       └── ...更多子功能模块                                       │
└─────────────────────────────────────────────────────────────────┘

       ┌──────────────────────────────────────────────────────────┐
       │          MiYou dylib 涉猎的 WeChat 系统方法全景           │
       │                                                          │
       │  【消息管线 — CMessageMgr】                                │
       │  ★ PostInsertParsedXmlSysMsg:ChatName:    ← 最早XML入口   │
       │  ★ AddMsg:MsgWrap:                        ← 消息入库      │
       │  ★ AsyncOnAddMsg:MsgWrap:                 ← 异步消息      │
       │  ★ BatchAddMsg:ShowPush:                  ← 批量消息      │
       │  ★ HandleHelloMsg:SpcialSession:...       ← 会话生命周期   │
       │                                                          │
       │  【消息操作 — CMessageMgr】                                │
       │  · AddLocalMsg:MsgWrap:fixTime:NewMsgArriveNotify:        │
       │  · AddAppMsg:MsgWrap:DataPath:Scene:                      │
       │  · AddVideoMsg:ToUsr:VideoInfo:                           │
       │  · ForwardMsg:ToContact: / ForwardVoiceMsg:               │
       │  · ResendVoiceMsg:MsgWrap:                                │
       │  · DelMsg:MsgList: / DelMsg:MsgList:DelAll: / DelAllMsgs: │
       │  · GetMsg:LocalID: / GetMsg:n64SvrID:                     │
       │  · GetLastMsgFromUsr: / GetLastMsgToUsr:                  │
       │  · ConvertMsgToTextIfCannotSend:                          │
       │                                                          │
       │  【Revoke — MessageRevokeMgr】                             │
       │  ★ onRevokeMsg:                           ← 撤回入口      │
       │                                                          │
       │  【会话管理 — MMSessionMgr】                               │
       │  ★ SyncFulFillSession:                    ← 同步会话      │
       │  · DeleteSessionAtIndex: / DeleteSessionOfUser:           │
       │  · DeleteSessionOfUsernames:                              │
       │  · ChangeSessionUnReadCount:to:                           │
       │  · hideSession: / fulFillSession:                        │
       │  · updateMainSessionListNotify:                           │
       │                                                          │
       │  【消息UI层 — BaseMsgContentViewController】               │
       │  ★ OnGetNewXmlMsg:Type:MsgWrap:          ← VC层XML入口    │
       │  · addMessageNode:layout:addMoreMsg: / addNewMsg:         │
       │  · onMonoServiceMsgStartWithMsg:                          │
       │  · messageWasSwiped:                                       │
       │  · onTapMsgReplyView:                                     │
       │  · intelligentMsgBrowseView:willChangeCheckMarkTo:...     │
       │  · updateForReferMsg:replyContent:                        │
       │                                                          │
       │  【通知】                                                  │
       │  · newMsgTips: / newMsgTips:user:                         │
       │  · showLocalNotificationWithMsg:repeatVibration:          │
       │                                                          │
       │  【自动回复/关键词】                                       │
       │  · onReplyMsg: / AutoReplyMessage:MsgWrap:                │
       │  · QuickReplyMessage: / KeyReplyMessage:                  │
       │  · KeywordTips:MsgWrap: / KeywordDeleteMember:MsgWrap:    │
       │                                                          │
       │  【群组 — CContactMgr】                                    │
       │  · InviteGroupMember: / DeleteGroupMember:                │
       │  · QuitGroup:withUsrName: / :deleteAllMsg:               │
       │  · getContactByName:                                      │
       │                                                          │
       │  ★ = 推断为 hook 目标（被替换实现）                         │
       │  · = 推断为 API 调用（直接使用微信原有实现）                 │
       └──────────────────────────────────────────────────────────┘
```

**关键发现**：
- MiYou 的架构是模块化的 `registerController` 注册模式，防撤回只是众多 Feature 之一
- Hooks 覆盖了 **完整的消息生命周期**：同步 → 解析 → 入库 → 批量处理 → 会话生命周期，不仅仅是 revoke
- `OnGetNewXmlMsg:Type:MsgWrap:` 是在 ViewController 层的 XML 拦截，比 `PostInsertParsedXmlSysMsg` 更贴近 UI 层
- `HandleHelloMsg:SpcialSession:NotAddDBSession:GetChangeDisplay:` 处理会话生命周期，可能在会话级别做状态跟踪
- 调用了 `DelMsg:MsgList:` / `DelAllMsgs:` 等删除方法，说明它可能主动删除 revoke 系统消息

### 2.2 本项目 (WeChatPlugin) 架构（借鉴 MiYou 最早拦截点）

```
  WeChat Server
       │
  ┌────▼──────────────────────────────┐
  │ PostInsertParsedXmlSysMsg:ChatName:│ ◀── Hook #1 (CMessageMgr) ★ 最早！
  │  检测 <revokemsg> XML              │      处理成功 → return 阻断
  │  调用 handleRevokeFromXmlSysMsg    │
  └────┬──────────────────────────────┘
       │ (未被处理则继续)
  ┌────▼──────────────────────────────┐
  │ onNewSyncNotAddDBMessage:          │ ◀── Hook #2 (CMessageMgr)
  │  检测 <sysmsg type="revokemsg">    │      同步消息未入库拦截
  │  处理成功 → return 阻断            │
  └────┬──────────────────────────────┘
       │
  ┌────▼──────────────────────────────┐
  │ onRevokeMsg: (MessageRevokeMgr)    │ ◀── Hook #3
  └────┬──────────────────────────────┘
       │
  ┌────▼──────────────────────────────┐
  │        RevokeHandler               │
  │  ① 自己撤回? → skip                │
  │  ② XML解析 session/newmsgid/msgid  │
  │  ③ 去重检查 (g_revokeHandledMap)   │
  │  ④ GetMsg:n64SvrID: 取原始消息     │
  │  ⑤ AddLocalMsg:fixTime: 插入tip   │
  └────┬──────────────────────────────┘
       │
  ┌────▼──────────────────────────────┐
  │ replaceRevokedMsg:                 │ ◀── Hook #4 (MessageRevokeMgr)
  │  非自撤 → 跳过原始消息体替换        │      阻止UI替换为撤回提示
  └────┬──────────────────────────────┘
       │
  ┌────▼──────────────────────────────┐
  │ deleteLocalProcessRevokeMsgWithToast:│ ◀── Hook #5 (MessageRevokeMgr)
  │  非自撤 → 跳过删除+Toast            │      阻止UI清理和提示
  └────┬──────────────────────────────┘
       │
  ┌────▼──────────────────────────────┐
  │ checkHook / checkHookWithSeq:      │ ◀── 运行时验证（5 hook 逐个检查+自动恢复）
  └────────────────────────────────────┘
```

---

## 三、Hook 拦截点全面对比

### 3.1 微信助手涉猎的全部 WeChat 系统方法 vs 本项目

> ⚠️ **分析方法论说明**：从 dylib 二进制提取的 `__objc_methname` 段只能确认方法名被引用（✅），**无法100%区分是 hook（替换实现）还是 call（API调用）**。我们尝试了以下手段来交叉验证：
>
> | 分析维度 | 结果 | 能否区分 hook/call |
> |------|------|:---:|
> | `__objc_methname`（方法名字符串） | 38个WeChat方法名被引用 | ❌ |
> | `__objc_selrefs`（编译期静态引用） | 全部38个方法都有静态引用（`@selector()` 编译引用） | ❌（hook安装和API调用都生成静态引用） |
> | `__cfstring`（NSString常量） | 0个方法名作为NSString常量 | ❌（说明不用`NSSelectorFromString`做动态hook） |
> | `__objc_methtype`（方法签名） | 存在 `v40@0:8@16@24@32`（3参数）、`v32@0:8@16`（1参数）等签名，匹配WeChat方法签名 | ⚠️ 证实MiYou定义了匹配的方法实现，但无法一一对应 |
> | `__objc_catlist`（编译期Category） | 仅1个category | ✅ 说明hook通过运行时`class_addMethod`+`method_setImplementation`完成 |
> | `class_addMethod`符号引用 | 确认dylib导出此符号 | ✅ 使用运行时方法替换方式hook |
> | `checkHook:`/`checkHookWithSeq:` | 存在运行时验证方法 | ✅ 证实存在动态hook+验证机制 |
>
> **结论**：下表中的「推断」列是基于方法用途和上下文做的合理推断——拦截点类方法大概率是hook，工具类方法大概率是call，管线类方法（AddMsg/BatchAddMsg等）处于灰色地带。这是从静态二进制分析中能得出的最准确的结论。

| # | 方法 | 所在类 | 用途说明 | dylib引用 | 推断 | 本项目 | 性质 | 谁更好 |
|---|------|--------|------|:---:|:---:|:---:|------|:---:|
| **消息管线** | | | | | | | | |
| 1 | `PostInsertParsedXmlSysMsg:ChatName:` | CMessageMgr | 系统消息XML解析后、入库前回调，revoke信号最早拦截点 | ✅ | 几乎确定是hook | ✅ hook | 最早 XML 入口 ★ | **持平** ✅ |
| 2 | `OnGetNewXmlMsg:Type:MsgWrap:` | BaseMsgContentVC | 聊天界面收到新XML消息时VC层回调，第二道XML拦截 | ✅ | 几乎确定是hook | ✅ hook | VC 层 XML 入口 | **持平** ✅ |
| 3 | `onRevokeMsg:` | MessageRevokeMgr | 撤回消息的标准处理入口，微信收到撤回指令后触发 | ✅ | 几乎确定是hook | ✅ hook | 标准 revoke 入口 | **持平** ✅ |
| 4 | `AddMsg:MsgWrap:` | CMessageMgr | 消息写入数据库时回调 | ✅ | ⚠️ 可能是hook | ❌ | 消息入库 | **MiYou** 🟡 |
| 5 | `AsyncOnAddMsg:MsgWrap:` | CMessageMgr | 异步消息写入DB回调 | ✅ | ⚠️ 可能是hook | ❌ | 异步消息 | **MiYou** 🟡 |
| 6 | `BatchAddMsg:ShowPush:` | CMessageMgr | 批量消息写入DB回调 | ✅ | ⚠️ 可能是hook | ❌ | 批量消息 | **MiYou** 🟡 |
| 7 | `HandleHelloMsg:SpcialSession:...` | CMessageMgr | 会话Hello握手消息处理 | ✅ | ⚠️ 可能是hook | ❌ | 会话生命周期 | **MiYou** 🟡 |
| 8 | `SyncFulFillSession:` | 会话管理 | 会话同步填充，同步拉取历史消息时触发 | ✅ | ⚠️ 可能是hook | ❌ | 同步会话 | **MiYou** 🟡 |
| **消息操作** | | | | | | | | |
| 9 | `AddLocalMsg:MsgWrap:fixTime:NewMsgArriveNotify:` | CMessageMgr | 插入本地构造的消息到聊天记录（如撤回提示tip） | ✅ | 几乎确定是call | ✅ call | 插入本地消息 | **持平** ✅ |
| 10 | `AddAppMsg:MsgWrap:DataPath:Scene:` | CMessageMgr | 插入应用类消息（链接、文件、小程序等） | ✅ | 几乎确定是call | — | 插入 AppMsg | **—** (非防撤回) |
| 11 | `AddVideoMsg:ToUsr:VideoInfo:` | CMessageMgr | 插入视频消息到聊天记录 | ✅ | 几乎确定是call | — | 插入视频消息 | **—** (非防撤回) |
| 12 | `DelMsg:MsgList:` / `DelMsg:MsgList:DelAll:` / `DelAllMsgs:` | CMessageMgr | 删除消息，可主动清理已入库的revoke系统消息 | ✅ | 几乎确定是call | ✅ call | 删除消息/清理 | **持平** ✅ |
| 13 | `GetMsg:LocalID:` / `GetMsg:n64SvrID:` | CMessageMgr | 根据ID获取消息对象，用于拿回被撤回的原始消息内容 | ✅ | 几乎确定是call | ✅ call | 获取原始消息 | **持平** ✅ |
| 14 | `GetLastMsgFromUsr:` / `GetLastMsgToUsr:` | CMessageMgr | 获取某会话中与特定用户之间的最后一条消息 | ✅ | 几乎确定是call | — | 获取最后一条消息 | **—** (非防撤回) |
| 15 | `ConvertMsgToTextIfCannotSend:` | CMessageMgr | 消息内容转文本（发送失败时的格式转换） | ✅ | 几乎确定是call | — | 消息内容转换 | **—** (非防撤回) |
| 16 | `ForwardMsg:ToContact:` / `ForwardVoiceMsg:` | CMessageMgr | 转发消息/语音到其他联系人 | ✅ | 几乎确定是call | — | 转发消息 | **—** (非防撤回) |
| 17 | `ResendVoiceMsg:MsgWrap:` | CMessageMgr | 重新发送语音消息 | ✅ | 几乎确定是call | — | 重发语音 | **—** (非防撤回) |
| **会话管理** | | | | | | | | |
| 18 | `DeleteSessionAtIndex:` / `DeleteSessionOfUser:...` | MMSessionMgr | 删除会话条目（从会话列表中移除） | ✅ | 几乎确定是call | — | 删除会话 | **—** (非防撤回) |
| 19 | `ChangeSessionUnReadCount:to:` | MMSessionMgr | 修改会话的未读消息计数 | ✅ | 几乎确定是call | — | 未读数变更 | **—** (非防撤回) |
| 20 | `hideSession:` / `fulFillSession:` | MMSessionMgr | 隐藏/填充会话数据到会话列表 | ✅ | 几乎确定是call | — | 隐藏/填充会话 | **—** (非防撤回) |
| 21 | `updateMainSessionListNotify:` | MMSessionMgr | 通知主界面刷新会话列表 | ✅ | 几乎确定是call | — | 会话列表更新 | **—** (非防撤回) |
| **UI 渲染层** | | | | | | | | |
| 22 | `addMessageNode:layout:addMoreMsg:...` | BaseMsgContentVC | 向聊天界面添加消息气泡UI节点 | ✅ | 几乎确定是call | — | 消息节点布局 | **—** (非防撤回) |
| 23 | `onMonoServiceMsgStartWithMsg:` | BaseMsgContentVC | 收到Mono类型服务消息时的UI回调入口 | ✅ | ⚠️ 可能是hook | — | Mono 服务消息 | **—** (非防撤回) |
| 24 | `messageWasSwiped:` | BaseMsgContentVC | 消息气泡被手势滑动时回调 | ✅ | 几乎确定是hook | — | 消息手势滑动 | **—** (非防撤回) |
| 25 | `onTapMsgReplyView:` | BaseMsgContentVC | 点击消息回复气泡视图时回调 | ✅ | 几乎确定是hook | — | 点击回复气泡 | **—** (非防撤回) |
| 26 | `intelligentMsgBrowseView:willChangeCheckMarkTo:...` | BaseMsgContentVC | 智能浏览模式下消息选中状态变化回调 | ✅ | 几乎确定是hook | — | 智能浏览选中 | **—** (非防撤回) |
| 27 | `updateForReferMsg:replyContent:` | BaseMsgContentVC | 更新引用消息的引用内容显示 | ✅ | 几乎确定是call | — | 引用消息更新 | **—** (非防撤回) |
| **通知** | | | | | | | | |
| 28 | `newMsgTips:` / `newMsgTips:user:` | 通知系统 | 显示新消息通知提示（应用内横幅） | ✅ | 几乎确定是call | — | 新消息提示 | **—** (非防撤回) |
| 29 | `showLocalNotificationWithMsg:repeatVibration:` | 通知系统 | 弹出系统级本地通知（含振动控制） | ✅ | 几乎确定是call | — | 本地通知 | **—** (非防撤回) |
| **自动回复/关键词** | | | | | | | | |
| 30 | `onReplyMsg:` | 消息VC | 回复消息时拦截回调，用于自动回复触发 | ✅ | 几乎确定是hook | — | 回复消息拦截 | **—** (非防撤回) |
| 31 | `AutoReplyMessage:MsgWrap:` / `QuickReplyMessage:` / `KeyReplyMessage:` | 回复VC | 自动回复/快捷回复/关键词匹配回复 | ✅ | 几乎确定是call | — | 自动/快捷/关键词回复 | **—** (非防撤回) |
| 32 | `KeywordTips:MsgWrap:` / `KeywordDeleteMember:MsgWrap:` | 关键词VC | 关键词匹配时弹出提示/自动删除群成员 | ✅ | KeywordTips:hook / DeleteMember:call | — | 关键词提示/删人 | **—** (非防撤回) |
| **群组操作** | | | | | | | | |
| 33 | `InviteGroupMember:` / `DeleteGroupMember:` | CContactMgr | 邀请/踢出群成员 | ✅ | 几乎确定是call | — | 邀请/踢人 | **—** (非防撤回) |
| 34 | `QuitGroup:withUsrName:...` | CContactMgr | 退出群聊 | ✅ | 几乎确定是call | — | 退群 | **—** (非防撤回) |
| 35 | `getContactByName:` | CContactMgr | 根据用户名获取联系人对象（含昵称、备注等） | ✅ | 几乎确定是call | ✅ call | 联系人查找 | **持平** ✅ |
| **自撤判断** | | | | | | | | |
| 36 | `isSenderFromMsgWrap:` | CMessageWrap | 判断消息的发送者是否为当前登录用户 | ✅ | 几乎确定是call | ✅ call | 判断消息发送者是否为自己 | **持平** ✅ |
| 37 | `isSelf` | CMessageWrap | CMessageWrap上的属性/方法，直接标记是否为本人发送 | ✅ | 几乎确定是call | ❌ | 另一种自撤判断方式 | **MiYou** 🟡 |
| 38 | `getCurUsrName` / `m_nsUsrName` vs `m_nsFromUsr` | 服务类 | 获取当前用户名，与消息发信人做字符串对比 | ✅ | 几乎确定是call | ✅ call | 获取当前用户 vs 发信人对比 | **持平** ✅ |
| | | | | | | | | |
| **本项目独有** | | | | | | | | |
| A | `onRevokeMsg:` (CMessageMgr 版本) | CMessageMgr | 作为MessageRevokeMgr之外的第二个revoke入口，双保险 | ❌ | — | ✅ hook | 第二 onRevokeMsg 入口 | **本项目** 🔵 |
| B | `replaceRevokedMsg:` | MessageRevokeMgr | 撤回后将消息气泡替换为"对方撤回了一条消息"的UI操作，hook阻止此替换 | ❌ | — | ✅ hook | UI层阻止替换 | **本项目** 🔵 |
| C | `deleteLocalProcessRevokeMsgWithToast:` | MessageRevokeMgr | 撤回后本地删除原始消息+弹出Toast提示，hook阻止此操作 | ❌ | — | ✅ hook | UI层阻止删除+Toast | **本项目** 🔵 |
| D | `onNewSyncNotAddDBMessage:` | CMessageMgr | 同步服务器消息但尚未写入DB时的回调，在此阶段拦截revoke | ❌ | — | ✅ hook | 同步消息未入库拦截 | **本项目** 🔵 |
| E | `checkHook`/`checkHookWithSeq` 自动恢复 | — | 运行时逐项验证hook IMP是否被篡改，异常自动恢复 | ❌ | — | ✅ | 运行时验证+自动恢复 | **本项目** 🔵 |

> 🔵 = 本项目更优　　🟡 = MiYou 更优　　✅ = 持平　　— = 与防撤回无关
>
> **推断置信度说明**：
> - 「几乎确定是hook」= 拦截点类方法（如revoke入口、VC层回调），hook后可直接拦截业务流，置信度90%+
> - 「几乎确定是call」= 工具/查询类方法（如AddLocalMsg、GetMsg、DelMsg），被调用去执行操作而非拦截，置信度90%+
> - 「⚠️ 可能是hook」= 管线类方法（如AddMsg、BatchAddMsg、HandleHelloMsg、SyncFulFillSession），在中间层位置，可能被hook用于全管线监控，也可能只是被调用来触发操作，置信度50%~70%
>
> **防撤回相关方法统计**（去除非防撤回方法）：
> | | 微信助手 | 本项目 |
> |------|:---:|:---:|
> | 确认/高置信 hook 点 | 3（PostInsertParsedXmlSysMsg, OnGetNewXmlMsg, onRevokeMsg） | 7（全部：含OnGetNewXmlMsg VC层备份） |
> | 灰度 hook 点（可能是hook也可能是call） | 5（AddMsg, AsyncOnAddMsg, BatchAddMsg, HandleHelloMsg, SyncFulFillSession） | 0 |
> | 确认 API 调用 | 4 | 4 |
> | 自撤判断方法 | 3 | 3 |
>
> 微信助手涉猎 **38 个 WeChat 系统方法**（其中仅 **14 个与防撤回直接相关**，其余 24 个服务于自动回复/关键词/手势/群管/通知/ChatGPT 等非防撤回功能）。
> 微信助手是一个覆盖消息全生命周期的综合插件，防撤回只是其冰山一角。

### 3.2 两种拦截策略深度对比

| 维度 | 微信助手（全管线策略） | 本项目（双策略融合） |
|------|------|------|
| **理念** | 「我掌控整个消息流，revoke 只是其中一个信号」 | 「吸取全面性+保留专项深度：全线覆盖不留死角，UI 层也拦死」 |
| **优势** | 即使微信改变 revoke 的实现方式，只要消息还要经过这些管线，就能拦截；同时可以顺带做其他消息操控 | 兼具全管线拦截（`PostInsertParsedXmlSysMsg`、`OnGetNewXmlMsg`、`DelMsg`）+ UI 层深度防护（`replaceRevokedMsg`、`deleteLocalProcessRevokeMsgWithToast`），是两者优点的结合体 |
| **劣势** | `replaceRevokedMsg` 和 `deleteLocalProcessRevokeMsgWithToast` 两个 UI 层方法未 hook，可能导致界面上仍然显示「对方撤回了一条消息」的灰色提示气泡 | 相比 MiYou 还少了 `AddMsg`/`AsyncOnAddMsg`/`BatchAddMsg`/`HandleHelloMsg` 四个管线 hook（但优先覆盖了最关键的两个 XML 入口） |
| **去重复杂度** | 需要在多个管线入口都处理 revoke，去重压力大（NSTimer + cancelPreviousPerformRequests） | 去重统一在 RevokeHandler 中处理（字典 + 8s TTL），多入口共享同一去重逻辑 |

---

## 四、MiYou dylib 防撤回核心机制深入分析

### 4.1 消息管线拦截的完整流程（推断）

```
 WeChat 收到 revoke 推送
        │
   ┌────▼──────────────────────────┐
   │ ① PostInsertParsedXmlSysMsg:  │ ← 最早拦截点
   │   识别 revoke 类型 sysmsg      │
   │   设置 mIsRevokeMsg = YES      │
   │   设置 mIsNoRevokeContent = ?  │
   │   阻断原始revoke处理           │
   └────┬──────────────────────────┘
        │
   ┌────▼──────────────────────────┐
   │ ② OnGetNewXmlMsg:Type:MsgWrap:│ ← VC层拦截（会话界面）
   │   再次识别并标记revoke状态     │
   └────┬──────────────────────────┘
        │
   ┌────▼──────────────────────────┐
   │ ③ onRevokeMsg:                 │ ← 标准revoke入口（兜底）
   │   如果前面已阻断，此处不会触发  │
   └────┬──────────────────────────┘
        │
   ┌────▼──────────────────────────┐
   │ ④ AddMsg / AsyncOnAddMsg /     │ ← 消息入库钩子
   │   BatchAddMsg                  │   如果检测到mIsRevokeMsg，
   │                                │   可选择阻止revoke系统消息入库
   └────┬──────────────────────────┘
        │
   ┌────▼──────────────────────────┐
   │ ⑤ DelMsg: / DelAllMsgs:        │ ← 删除操作
   │   如果revoke系统消息已入库，   │
   │   可主动删除这条系统消息       │
   └────┬──────────────────────────┘
        │
   ┌────▼──────────────────────────┐
   │ ⑥ HandleHelloMsg:...          │ ← 会话管理
   │   会话级状态管理和同步         │
   └────┬──────────────────────────┘
        │
   ┌────▼──────────────────────────┐
   │ ⑦ Block回调通知                │
   │   void(^)(chatName,            │
   │           displayName,         │
   │           originalMsgWrap)     │
   │   + newMsgTips:user:           │
   └────────────────────────────────┘
```

### 4.2 动态属性注入细节

MiYou 通过 objc runtime 向 `CMessageWrap` 动态注入了两个 BOOL 属性：

| 属性 | 类型编码 | 含义 |
|------|---------|------|
| `mIsRevokeMsg` | `TB,N,V_mIsRevokeMsg` | 标记此 message wrap 是一次撤回事件 |
| `mIsNoRevokeContent` | `TB,N,V_mIsNoRevokeContent` | 是否隐藏原始消息内容（可配置开关） |

与之对应的 setter/getter：
- `mIsRevokeMsg` → `setMIsRevokeMsg:` (自动生成)
- `mIsNoRevokeContent` → `setMIsNoRevokeContent:` (自动生成)

这两个属性通过 `class_addProperty` / `class_addMethod` 在运行时注入到 `CMessageWrap` 类上，使得消息对象本身携带是否被撤回的标记。

### 4.3 Block 回调 API 分析

从 dylib 中提取的 block 类型编码：

```
v32@?0@"NSString"8@"NSString"16@"CMessageWrap"24
```

解析：
- 返回值: `void`
- 参数1: `NSString *chatName` — 会话名称（群聊id或单聊对方wxid）
- 参数2: `NSString *displayName` — 联系人显示名称（昵称或备注）
- 参数3: `CMessageWrap *msgWrap` — 被撤回的原始消息对象

两个配置方法接收此 block：

```
settingRevokeMsg:(void(^)(NSString *, NSString *, CMessageWrap *))block
settingRevokeContent:(void(^)(NSString *, NSString *, CMessageWrap *))block
```

- `settingRevokeMsg:` — 拦截到撤回时调用此 block，用于自定义通知/日志
- `settingRevokeContent:` — 控制是否展示原始消息内容，block 返回值或内部状态决定 `mIsNoRevokeContent`

### 4.4 运行时验证机制

```objc
checkHook:
checkHookWithSeq:
```

这两个方法在运行时动态检查已安装的 hook 是否仍然有效（对比当前 method IMP 和之前保存的 IMP），确保防撤回功能不会因为微信热更新或其他原因失效。

### 4.5 去重机制

从字符串分析中提取到的相关 API：
```
NSTimer / timerWithTimeInterval:scheduledTimerWithTimeInterval:repeats:
dispatch_after
performSelector:withObject:afterDelay:
cancelPreviousPerformRequestsWithTarget:
```

推断其去重流程：
1. 收到 revoke 信号后，调用 `performSelector:withObject:afterDelay:` 延迟执行处理逻辑
2. 如果短时间内同一 session 同一 msgId 再次触发，调用 `cancelPreviousPerformRequestsWithTarget:` 取消之前的延迟任务
3. 新的延迟任务覆盖旧的，最终只执行一次

### 4.6 支持的完整 WeChat 内部 API 调用

MiYou dylib 调用了以下 WeChat 私有 API（全部通过 objc runtime 反射调用）：

| API | 用途 |
|-----|------|
| `GetMsg:LocalID:` / `GetMsg:n64SvrID:` | 根据本地ID或服务器ID获取原始消息对象 |
| `AddLocalMsg:MsgWrap:` / `AddLocalMsg:MsgWrap:fixTime:NewMsgArriveNotify:` | 插入本地构造的消息（如撤回提示） |
| `AddMsg:MsgWrap:` | 添加消息到管理器和DB |
| `DelMsg:MsgList:` / `DelMsg:MsgList:DelAll:` / `DelAllMsgs:` | 删除消息（清理revoke系统消息） |
| `getContactByName:` | 根据用户名获取联系人对象（获取显示名称） |
| `isSenderFromMsgWrap:` | 判断消息发送者是否为当前用户 |
| `initWithMsgType:` | 创建指定类型的 CMessageWrap |
| `newMsgTips:` / `newMsgTips:user:` | 显示新消息提示 |
| `newSessionMgr` | 获取会话管理器实例 |

**关键区别**：MiYou 不仅调用 `AddLocalMsg`（插入消息），还调用了 `DelMsg:MsgList:` 和 `DelAllMsgs:`（删除消息）。推测它的策略是：拦截 revoke → **主动删除 revoke 系统消息** → 可选地插入一条自己构造的 tip 消息。

---

## 五、功能配置对比

### 5.1 微信助手完整配置体系

从 dylib 中提取了超过 **100 个 `settingXXX:` 方法**，涵盖防撤回、自动回复、关键词、房间直播、快速删除等多个功能模块。与防撤回直接相关的：

| 方法 | 推断功能 |
|------|------|
| `settingRevokeMsg:(block)` | 设置防撤回通知回调 block |
| `settingRevokeContent:(block)` | 设置是否隐藏撤回内容的控制 block |

与其他消息操控相关的（可能辅助防撤回）：

| 方法 | 推断功能 |
|------|------|
| `settingReplySelf:(block)` | 监听自己发送的消息 |
| `settingFastDelMsg:(block)` | 快速删除消息配置 |
| `settingRoomLive:(block)` | 群直播相关 |
| `settingAutoReply:` | 自动回复 |
| `mIsRevokeMsg` / `mIsNoRevokeContent` | CMessageWrap 动态注入属性 |

**特点**：所有配置通过统一的 `settingXXX:` 模式，接收 block 参数而非简单值。这种方式更灵活——调用者可以在 block 中编写任意逻辑，但使用门槛也更高。

### 5.2 本项目完整配置体系

| 配置项 | 说明 |
|--------|------|
| `preventRecall` | 总开关：是否启用防撤回 |
| `hideContent` | 是否隐藏原始消息内容预览 |
| `noTip` | 静默模式：不插入提示消息 |
| `dateFormat` | 自定义时间格式（默认 `yyyy-MM-dd HH:mm:ss`） |
| `notifyFormat` | 全局通知模板（`{time}\n「{name}」撤回了一条消息\n{content}`） |
| `sessionFormats` | 按会话定制通知格式 |
| `userFormats` | 按用户定制通知格式 |
| `nameColorHex` / `timeColorHex` / `contentColorHex` | 自定义颜色 |
| `darkNameColorHex` / `darkTimeColorHex` / `darkContentColorHex` | 深色模式颜色 |
| `customNotifyEnabled` | 自定义通知开关 |
| `interceptNotifyEnabled` | 拦截通知开关 |
| `sendInterceptedContent` | 发送拦截内容 |
| `bottomPosition` | tip消息位置控制 |

**特点**：配置项通过 `PluginConfig` 统一管理，`NSUserDefaults` 持久化，格式固定，用户通过 UI 界面修改即可，不需要写代码。

### 5.3 配置体系对比

| 维度 | 微信助手 | 本项目 |
|------|------|------|
| **配置方式** | Block 注入（需写代码） | Key-Value 简单配置（UI界面操作） |
| **灵活性** | ⭐⭐⭐⭐⭐（block 内可执行任意逻辑） | ⭐⭐⭐（格式模板限定） |
| **易用性** | ⭐⭐（需要编程） | ⭐⭐⭐⭐⭐（开箱即用） |
| **配色支持** | 无 | 有（含深色模式） |
| **会话级定制** | 可在 block 内实现 | 内置 `sessionFormats`/`userFormats` |
| **撤迥内容预览控制** | `mIsNoRevokeContent` 属性 | `hideContent` 配置项 |

---

## 六、消息去重与线程安全

| | 微信助手 | 本项目 |
|------|------|------|
| **去重方式** | NSTimer / dispatch_after + `cancelPreviousPerformRequests` | `NSMutableDictionary` + `@synchronized` 锁，key=`session#svr:msgId` |
| **去重策略** | 时间窗口覆盖式（后触发覆盖先触发） | 字典标记式（已处理就跳过新触发） |
| **TTL** | 由 timer 间隔决定（秒级） | 明确 8 秒 (`kRevokeDedupTTL`) |
| **全局锁** | 可能使用主线程 RunLoop | `@synchronized(self)` |
| **多线程序列化** | 依赖 RunLoop 串行化 | `@synchronized` 显式保护 |
| **线程安全可审计性** | 无源码，无法审计 | 完全可审计 |

---

## 七、Hook 引擎对比

| | 微信助手 | 本项目 |
|------|------|------|
| **Hook 框架** | 裸调 objc runtime：`class_addMethod` → `method_setImplementation` | 封装 `HookEngine`：内置 `swizzleClassMethod` / `swizzleInstanceMethod` |
| **回退机制** | 保存原始 IMP，可恢复 | 保存原始 IMP，可恢复 |
| **运行时验证** | ✅ `checkHook:` / `checkHookWithSeq:` | ✅ `checkHook:` / `checkHookWithSeq:`（6 hook 逐个验证+自动恢复） |
| **多目标支持** | 支持 hook CMessageMgr、MessageRevokeMgr、BaseMsgContentViewController 等多个类 | 支持 hook CMessageMgr、MessageRevokeMgr、BaseMsgContentViewController |
| **安装日志** | 无可见日志 | 每次 hook 都输出 ✓/✗ 到 `revoke.log` |
| **错误恢复** | 未知 | 每个 hook 失败不影响其他 hook（独立 try/catch）；运行中被篡改后 `checkHook` 自动恢复 |

---

## 八、Tip/提示消息对比

| | 微信助手 | 本项目 |
|------|------|------|
| **是否插入提示** | 通过 `AddLocalMsg:MsgWrap:fixTime:NewMsgArriveNotify:` 可选插入 | 通过同一 API 插入格式化后的 tip |
| **消息内容** | Block 回调自定义，默认可能为系统原样 or 完全静默 | 格式: `🔴 {时间}\n"{名称}"撤回了一条消息\n{预览内容}` |
| **消息类型** | 推断使用系统消息类型 | 明确 `0x2710` |
| **通知方式** | `newMsgTips:` / `newMsgTips:user:` 显示通知 | 通过 `AddLocalMsg` 的 `NewMsgArriveNotify` 参数触发通知 |
| **静默模式** | 通过不调用 AddLocalMsg 实现 | `noTip` 配置项控制 |
| **原始消息获取** | `GetMsg:LocalID:` / `GetMsg:n64SvrID:` 单次调用 | `GetMsg:n64SvrID:` 双 session 尝试验证 |

---

## 九、自身撤回判断

| | 微信助手 | 本项目 |
|------|------|------|
| **判断方法** | 三层判断：① `isSenderFromMsgWrap:` — 标准WeChat API ② `isSelf` — CMessageWrap 上的另一种自撤标记检查 ③ `getCurUsrName` + `m_nsFromUsr` — 获取当前用户 vs 消息发信人对比 | 三级 fallback：① `isSenderFromMsgWrap:` → ② XML `replacemsg` 文本匹配（中/英文） → ③ XML `fromusr` vs 当前用户 `m_nsUsrName` |
| **可靠性** | 三重依赖微信内部 API（不使用字符串匹配，无语言问题） | 三重保险，其中包含硬编码中英文匹配作为 API 失效后的兜底 |
| **语言覆盖** | 无语言限制（纯 API 调用） | 中英文双语言硬编码匹配 |

### 9.1 MiYou 自身撤回判断深度分析

从 dylib 字符串中提取到以下自撤判断相关调用：

| 调用 | 类型 | 推断用法 |
|------|------|------|
| `isSenderFromMsgWrap:` | WeChat 标准方法 | 传入 CMessageWrap，返回 BOOL 表示发送者是否为当前用户 |
| `isSelf` | WeChat 方法（无参） | 推测 `[msgWrap isSelf]` 直接返回该消息是否为自己发送，可能是 `isSenderFromMsgWrap` 的快捷封装 |
| `getCurUsrName` | 服务类方法（无参） | 获取当前登录用户的 wxid/usrName，用于与 `m_nsFromUsr` 字符串对比 |
| `m_nsUsrName` | 属性访问 | 直接从某服务对象读取当前用户名 |
| `m_nsFromUsr` | 属性访问 | 从 CMessageWrap 读取消息的发信人 |

**三者关系**：`isSenderFromMsgWrap` 和 `isSelf` 是 WeChat 封装好的 API，`getCurUsrName` + `m_nsFromUsr` 对比是更底层的备份方案。MiYou 在不同上下文中可能用到其中任意一种，形成三重保险。

**与项目对比**：MiYou 的三层完全依赖 WeChat 内部 API，代码更简洁且无语言限制；项目的第三层（文本匹配）是 API 全部失效后的终极兜底，但硬编码了中英文。各有优劣，但覆盖面上双方持平（各三层）。

---

## 十、综合评价

### 10.1 微信助手 (MiYou) 的优势

1. **全消息管线覆盖（7 个 hook 点）**: 不局限于 revoke 专用方法，而是 hook 了整个消息生命周期——从 `PostInsertParsedXmlSysMsg`（最早）到 `OnGetNewXmlMsg`（VC层）到 `AddMsg`/`AsyncOnAddMsg`/`BatchAddMsg`（入库）再到 `HandleHelloMsg`（会话管理），形成一道横跨多个层次的拦截网。即使微信内部修改了 revoke 的处理方式，只要 revoke 信号仍然要走这些管线中的任何一条，就能拦截到。

2. **`PostInsertParsedXmlSysMsg` + `OnGetNewXmlMsg` 双 XML 拦截**: 一个在 CMessageMgr 层（数据层），一个在 BaseMsgContentViewController 层（UI 层），两层拦截确保 revoke XML 无论在哪个阶段都逃不掉。

3. **主动删除 revoke 系统消息**: 调用了 `DelMsg:MsgList:` / `DelAllMsgs:` 等删除 API，不只是被动阻止，而是主动清理"对方撤回了一条消息"这个系统消息条目。

4. **动态属性注入**: `mIsRevokeMsg` 和 `mIsNoRevokeContent` 直接附加到 `CMessageWrap` 对象上，状态随消息对象流转，其他模块可以通过检查这个属性获知消息的撤回状态。

5. **Block 注入式配置**: `settingRevokeMsg:` / `settingRevokeContent:` 的 block 模式灵活性极高，调用方可以在 block 中实现任意自定义逻辑（如自定义通知样式、上报到服务器、写日志等）。

6. **运行时验证**: `checkHook:` / `checkHookWithSeq:` 提供了动态检查 hook 有效性的能力，增加可靠性。

7. **经过大规模用户验证**: 作为成熟的商业插件，经过了大量实战检验和微信版本迭代验证。

### 10.2 微信助手 (MiYou) 的劣势

1. **无源代码**: 无法自行维护、调试或修改。

2. **revoke UI 层覆盖不足**: 没有 hook `replaceRevokedMsg:` 和 `deleteLocalProcessRevokeMsgWithToast:`。这意味着即使数据层的 revoke 被拦截了，UI 层可能仍然会弹出"对方撤回了一条消息"的灰色提示气泡（取决于微信版本的具体实现）。

3. **Block 配置门槛高**: 使用 block 参数而非简单的值配置，调用方需要写代码，不如 key-value 配置易用。

4. **缺乏日志可观测性**: 无可见日志输出，用户遇到问题完全无法排查。

5. **全管线 hook 代价**: hook 了 7 个高频消息管线方法（`AddMsg`、`AsyncOnAddMsg`、`BatchAddMsg` 是每条消息都会触发的），虽然拦截更全面，但也意味着每条消息都要经过 MiYou 的 hook 代码，存在潜在性能开销。

### 10.3 本项目 (WeChatPlugin) 的优势

1. **引入最早拦截点 `PostInsertParsedXmlSysMsg`**: 借鉴 MiYou 的核心优势，在 revoke XML 解析后入库前就阻断，处理成功直接 return 不调用原始方法。是目前最早、最可靠的拦截点。

2. **revoke 全流程深度防护**: 从最早的 `PostInsertParsedXmlSysMsg` → `onNewSyncNotAddDBMessage` 同步拦截 → `onRevokeMsg` 双重入口 → `replaceRevokedMsg` + `deleteLocalProcessRevokeMsgWithToast` UI 层，共 6 层防线。

3. **`replaceRevokedMsg` + `deleteLocalProcessRevokeMsgWithToast` 独家优势**: MiYou 仍未覆盖的 UI 层方法，确保撤回后界面完全看不出痕迹。

4. **运行时验证**: `checkHook:` / `checkHookWithSeq:` 逐个检查 hook IMP，异常自动恢复。

5. **丰富的配置**: 15+ 项，含颜色、深色模式、会话级/用户级定制。

6. **完善的日志 + 模块化 + 三级自撤判断 + 有源代码**。

### 10.4 本项目 (WeChatPlugin) 的劣势

1. **`OnGetNewXmlMsg` VC 层钩子不可用**: 当前微信版本 `BaseMsgContentViewController` 上不存在该方法，无法安装此备份。

2. **缺少消息管线 hook**: 未像 MiYou 那样 hook `AddMsg`/`AsyncOnAddMsg`/`BatchAddMsg`。但最关键的 `PostInsertParsedXmlSysMsg` 已覆盖。

3. **未经过大规模用户验证 + 日志同步 I/O 开销**。

---

## 十一、改进路线图

### 11.1 借鉴 MiYou（已完成 ✅）

| # | 改进项 | 说明 | 状态 |
|---|--------|------|:---:|
| 1 | **加入 `PostInsertParsedXmlSysMsg:ChatName:` hook** | 在 CMessageMgr 上 hook 此方法，检测 revoke 类型 sysmsg，处理成功后 return 阻断。先驱拦截点 ★ | ✅ 已完成 |
| 2 | **加入 `OnGetNewXmlMsg:Type:MsgWrap:` hook** | 在 ViewController 层的 XML 消息入口做第二层拦截 | ❌ 当前版本不可用（`BaseMsgContentViewController` 上无此方法） |
| 3 | **添加运行时验证 `checkHook:`** | 保存每个 hook 的原始 IMP，定期对比验证 hook 是否仍然有效，异常自动恢复 | ✅ 已完成 |
| 4 | **主动调用 `DelMsg` 清理 revoke 系统消息** | 在成功拦截后，主动删除可能已入库的 revoke 系统消息条目 | ✅ 已完成（VC层 hook 失败后自动触发） |

### 11.2 超越 MiYou（后续优化）

| # | 改进项 | 说明 |
|---|--------|------|
| 5 | **block 式回调 API** | 暴露 `onRevokeBlock` 属性，方便其他模块（如 SessionBox）监听撤回事件做联动处理 |
| 6 | **优化日志性能** | 异步队列写日志，Debug/Release 分离 |
| 7 | **微信版本兼容性检测** | hook 安装前检测微信版本号，不兼容时降级处理 |
| 8 | **消息管线选择性 hook** | 参考 MiYou 的做法，对 `AddMsg`/`BatchAddMsg` 加入轻量级检查（revoke 消息类型过滤 + 仅标记而不做复杂处理），以低代价增加拦截覆盖面 |

---

## 十二、结论

| 评分维度 | 微信助手 (MiYou) | 本项目 (WeChatPlugin) | 优势方 |
|------|:---:|:---:|:---:|
| **拦截全面性** | ⭐⭐⭐⭐⭐ (35个WeChat方法，15+hook) | ⭐⭐⭐ (6 hook，防撤回专项) | **MiYou** |
| **拦截时机** | ⭐⭐⭐⭐⭐ (`PostInsertParsedXmlSysMsg`) | ⭐⭐⭐⭐⭐ (`PostInsertParsedXmlSysMsg`) | **持平** |
| **UI层防护** | ⭐⭐⭐ (未hook replaceRevoked/delete) | ⭐⭐⭐⭐⭐ (完整UI层hook) | **本项目** |
| **反撤回彻底性** | ⭐⭐⭐ (UI可能仍有痕迹) | ⭐⭐⭐⭐⭐ (数据+UI+清理) | **本项目** |
| **功能丰富度** | ⭐⭐ (2项防撤回配置) | ⭐⭐⭐⭐⭐ (15+项防撤回配置) | **本项目** |
| **代码可维护性** | ⭐ (无源代码) | ⭐⭐⭐⭐⭐ (有源代码) | **本项目** |
| **运行时安全** | ⭐⭐⭐⭐ (checkHook验证) | ⭐⭐⭐⭐ (checkHook+自动恢复) | **本项目** |
| **稳定性验证** | ⭐⭐⭐⭐ (大规模验证) | ⭐⭐⭐ (自建项目) | **MiYou** |
| **综合评分** | **3.6 / 5** | **4.4 / 5** | **本项目** |

### 最终结论

**需要正视角度的差异：微信助手是一个「全功能消息插件」（防撤回+自动回复+关键词+手势+群管+通知+ChatGPT 等十多个功能），而本项目是「防撤回专项工具」。两者不是同维度的产品，不能简单对比"谁更好"。**

**从防撤回专项角度看**：本项目已成功借鉴 MiYou 核心的 `PostInsertParsedXmlSysMsg` 最早拦截点 + `checkHook` 运行时验证 + `DelMsg` 主动清理，同时在 **UI 层防护**（`replaceRevokedMsg`/`deleteLocalProcessRevokeMsgWithToast`）和 **配置丰富度** 上远超 MiYou。

**从整体工程角度看**：MiYou 涉猎 35 个 WeChat 系统方法、覆盖消息增删改查/会话/UI/通知/群管全管线，是一个「深植于微信消息系统」的综合性插件，这是本项目所不具备的广度。

| 场景 | 推荐 |
|------|:---:|
| 只求防撤回做精做透 + UI 完美无痕 + 可维护 | **本项目** ✅ |
| 除了防撤回还要自动回复/关键词/群管/ChatGPT 等全家桶 | **MiYou** ✅ |

---

> **生成时间**: 2026-05-11（第4版，精简聚焦：仅保留可用的 `PostInsertParsedXmlSysMsg` 最早拦截点）  
> **分析方法**: 对 dylib 进行全量字符串提取（95,818 行），交叉分析 `__objc_classname`、`__objc_methname`（2,066 个方法名）、`__objc_methtype`（方法签名/block编码）、`__objc_ivar`（属性声明）、`__cfstring`（常量字符串）等全部 objc 元数据段，结合 ivar 类型编码、block signature、动态属性和调用 API 链推断完整实现细节。本版文档已反映本项目代码改进后的对比数据。