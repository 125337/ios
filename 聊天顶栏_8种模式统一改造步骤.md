# 聊天顶栏 — 8种显示模式 向微信优化统一改造步骤

> 基于《聊天顶栏_8种显示模式深度布局分析.md》
> 编译环境：**MRC**
> 微信基准：`\www\wwwroot\ios\插件\微信优化反编译最新\123456.c` `layoutSubviews`（L11130-L11667）
> 我的实现：`WeChatPlugin/Modules/ChatTopBar/MioChatAvatarTitleView.m` `layoutSubviews`（L95-L283）
> 常量化：A=avatarSize=30, N=nameFontSize=9, S=avatarSpacing=6, hS=S*0.5=3, W=210, H=44

---

## 前置准备：变量路由对照

| 微信 | 我的 | 值 | 说明 |
|------|------|---|------|
| `dVar17` | `avatarSize` | 30 | 头像大小 |
| `dVar16` | `nameFontSize` | 9 | 昵称字号 |
| `dVar12` (无分隔符) | `avatarSpacing` | 6 | 无分隔时全间距 |
| `dVar19` (有分隔符) | `halfSpacing` = S*0.5 | 3 | 有分隔时半间距 |
| `dVar18` | `avatarY` | 按mode | 头像Y坐标 |
| `bVar1` | — (name hidden逻辑) | — | 名字是否显示 |

### 微信路由分发（我们需对齐这层逻辑）

```
第一层：bVar1 赋值
  mode ∈ {1,4,5,6,7} → bVar1 = true（名字可见）
    {5,6,7} → avatarY = (H-A)/2 = 7         // 纯居中
    {1,4}   → avatarY = (H-A-4-N)/2+1 = 5.5 // 偏上给名留空间
  mode ∈ {0,2,3}   → bVar1 = false（名字隐藏）
    avatarY = (H-A)/2 = 7

第二层：(1L << (mode & 0x3f) & 0x6e) == 0 ?
  mask = 0 → Mode 0,4,7 → LAB_00010e9c（双方头像 + 分隔符布局）
  mask ≠ 0 → Mode 1,2,3,5,6 → 各自独立布局
```

### 我的当前路由（需改造）

```
mode==7 → 重叠
mode==5 → 名左 + 头像右
mode==6 → 右头像贴左 + 名全宽右对齐     ← 需改造
mode==0 → 左头像 + 名                    ← 需改造
mode==4 → 单头像居中 + 名下方            ← 需改造
mode==1/3 → 单头像居中 + 分隔符          ← 需改造
else → 双方头像 + 分隔符（默认 = Mode 2）← 需改造
```

---

## 阶段 1：修正 avatarY 计算公式

**当前问题**：用户代码中 Mode 1 和 Mode 4 用的 avatarY 公式与微信不同。

**微信公式**：
```
mode ∈ {1,4}   → avatarY = (H - A - 4 - N) / 2 + 1 = (44-30-4-9)/2+1 = 5.5
mode ∈ {0,2,3,5,6,7} → avatarY = (H - A) / 2 = 7
```

**当前代码**（MioChatAvatarTitleView.m L111-119）：

```objc
// 当前
if (mode == 1 || mode == 4) {
    CGFloat contentH = avatarSize + 3 + nameFontSize;   // 30+3+9=42
    avatarY = (totalH - contentH) * 0.5 + 1;            // (44-42)*0.5+1 = 2.0 ❌
}
```

**修改为**：

```objc
// 修改后
if (mode == 1 || mode == 4) {
    avatarY = (totalH - avatarSize - 4 - nameFontSize) * 0.5 + 1;  // = 5.5 ✅
} else {
    avatarY = (totalH - avatarSize) * 0.5;  // = 7.0 ✅
}
```

> 同时删除变量 `contentH`，它不再需要。

---

## 阶段 2：Mode 0 — 双方头像居中、无名字

> 涉及行：L180-L194

**微信行为**：双方头像水平居中，间距 S=6，无名字，无分隔符。

```
微信 Mode 0 (参考):
  leftAvatar.x  = (W - S - 2*A) / 2 = 72
  rightAvatar.x = S + A + leftX = 108
  avatarY = (H-A)/2 = 7
  名字 hidden, 分隔符 hidden
```

**当前代码**：左头像贴左 + 名字在其右侧。

**改造步骤**：

删除 Mode 0 的当前实现（L180-L194），新代码加入阶段 4 的"双方头像 + 分隔符"分支（因为微信 Mode 0 走 LAB_00010e9c 双头像路径）。

> 注意：微信 Mode 0 经过第二层路由 `mask=0`，进入 LAB_00010e9c。所以 Mode 0 和 Mode 4 应共享同一个"双方头像"处理分支。

**代码**：见阶段 5 统一处理。

---

## 阶段 3：Mode 2 — 左头像居中，无名字，无分隔符

> 涉及行：L232-L283（整个 default 分支）

**微信行为**（反编译代码实测）：Mode 2 在第一层 `uVar5 = 2`，`bVar1 = false`，进入 `uVar5-1 < 2` 路径：

```
微信 Mode 2:
  leftAvatar.x = (W-A)/2 = 90, centered
  rightAvatar hidden (frame=0,0,0,0)
  名字 hidden, 分隔符 hidden
```

**当前代码**：双方头像 + 分隔符（这是微信 LAB_00010e9c 的行为）。

**改造**：将 Mode 2 从 default 分支中分离出来，作为独立分支：

```objc
// 在 Mode 3 之后、default 之前插入
if (mode == 2) {
    self.rightAvatarView.hidden = YES;
    self.separatorView.hidden = YES;
    self.separatorTextLabel.hidden = YES;
    self.titleLabel.hidden = YES;

    self.leftAvatarView.frame = CGRectMake((totalW - avatarSize) * 0.5, avatarY,
                                            avatarSize, avatarSize);
    self.leftAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];
    return;
}
```

---

## 阶段 4：Mode 4 — 双方头像 + 名字下方

> 涉及行：L197-L211

**微信行为**：双方头像水平居中，间距 hS=3，名字在头像正下方居中。

```
微信 Mode 4:
  leftAvatar.x  = (W - hS - 2*A) / 2 = 73.5    (hS=3)
  rightAvatar.x = hS + A + leftX = 106.5
  avatarY = 5.5 (偏上)
  nameY = A + Y + 1 - Oy = 30+5.5+1 = 36.5
  名字居中, w=W
```

**当前代码**：单头像居中 + 名字下方。

**改造**：Mode 4 和 Mode 0 都走 LAB_00010e9c 双方头像路径，合并处理见阶段 5。

---

## 阶段 5：统一"双方头像"分支（Mode 0 + Mode 4 + 无mode的双方分隔符）

> 删除：Mode 0 独立分支（L180-L194）、Mode 4 独立分支（L197-L211）
> 修改：default 分支（L232-L283）的进入条件

微信的 LAB_00010e9c 处理三种情况（bVar2=分隔图标, bVar3=分隔文字）共四种子布局。Mode 0 和 Mode 4 都走此分支，区别仅在于：
- Mode 0: `bVar1=false` → 名字 hidden
- Mode 4: `bVar1=true` → 名字 visible，在头像下方

### 5.1 重写条件入口

删除现有的 Mode 0 和 Mode 4 独立 if 块，将它们的处理合并到 default 分支：

```objc
// 删除 L180-L211 的 Mode 0 和 Mode 4 独立处理

// 合并后的"双方头像"分支（替换 L232 开始的 default 逻辑）
// 进入条件：mode == 0 || mode == 4 || (default: mode == 2 after fix)
// 但对于微信对齐：mode 0 和 mode 4 走这里，mode 2 已独立处理

BOOL showName = (mode == 4);  // bVar1: Mode 4 显示名字, Mode 0 不显示
self.titleLabel.hidden = !showName;

// 双方头像布局（同原 default 分支，但增加模式判断）
self.leftAvatarView.hidden = NO;
self.rightAvatarView.hidden = NO;

if (hasSeparator) {
    // ... 分隔符子布局（见 5.2）
} else {
    // 无分隔符：双方头像居中
    CGFloat dualW = avatarSize * 2 + avatarSpacing;
    CGFloat startX = (totalW - dualW) * 0.5;
    self.leftAvatarView.frame  = CGRectMake(startX, avatarY, avatarSize, avatarSize);
    self.rightAvatarView.frame = CGRectMake(startX + avatarSize + avatarSpacing, avatarY, avatarSize, avatarSize);
    self.separatorTextLabel.hidden = YES;
    self.separatorView.hidden = YES;
}

// 名字标签（仅 Mode 4）
if (showName) {
    [self.titleLabel setFont:[UIFont systemFontOfSize:nameFontSize]];
    self.titleLabel.textAlignment = NSTextAlignmentCenter;
    self.titleLabel.frame = CGRectMake(0, avatarY + avatarSize + 1, totalW, nameFontSize);
    // 应用偏移
    CGRect f = self.titleLabel.frame;
    f.origin.x += config.chatNicknameOffsetX;
    f.origin.y -= config.chatNicknameOffsetY;
    self.titleLabel.frame = f;
}
```

### 5.2 分隔符子布局修正

微信有分隔符时的半间距 hS=S/2=3，无分隔符时全间距 S=6。当前代码一致。

但微信还有分隔符图标（separatorView）参与布局，当前代码 separatorView 被隐藏了。如果之后启用分隔符图标，需要修正公式。

**当前代码用 hS 的地方正确**，保持不变即可。

---

## 阶段 6：Mode 1 和 Mode 3 — 隐藏分隔符

> 涉及行：L213-L230

**微信行为**：
- Mode 1/3 均隐藏分隔符和分隔文字
- Mode 1 用 rightAvatar，Mode 3 用 rightAvatar（微信均用右头像）

```
微信 Mode 1: rightAvatar centered, 名字下方居中, 无分隔符
微信 Mode 3: rightAvatar centered, 无名字, 无分隔符
```

**当前代码**：显示分隔符，且 Mode 3 用左头像。

**改造**：

```objc
if (mode == 1 || mode == 3) {
    // Mode 1: 名字可见, Mode 3: 名字不可见
    BOOL showName = (mode == 1);

    self.leftAvatarView.hidden = YES;
    self.rightAvatarView.hidden = NO;
    self.separatorView.hidden = YES;       // 微信隐藏
    self.separatorTextLabel.hidden = YES;   // 微信隐藏
    self.titleLabel.hidden = !showName;

    self.rightAvatarView.frame = CGRectMake((totalW - avatarSize) * 0.5, avatarY,
                                             avatarSize, avatarSize);
    self.rightAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];

    if (showName) {
        [self.titleLabel setFont:[UIFont systemFontOfSize:nameFontSize]];
        self.titleLabel.textAlignment = NSTextAlignmentCenter;
        self.titleLabel.frame = CGRectMake(0, avatarY + avatarSize + 1, totalW, nameFontSize);
        // 应用偏移
        CGRect f = self.titleLabel.frame;
        f.origin.x += config.chatNicknameOffsetX;
        f.origin.y -= config.chatNicknameOffsetY;
        self.titleLabel.frame = f;
    }
    return;
}
```

---

## 阶段 7：Mode 6 — 左头像居中 + 名字右对齐（nameWidth 宽度）

> 涉及行：L162-L178

**微信行为**：

```
微信 Mode 6:
  leftAvatar.x = (W - A - nameW - 8) / 2 = (210-30-nameW-8)/2
  leftAvatar.y = 7
  名字.x = A + leftX + 8, textAlignment = Right
  名字.w = nameW (不是剩余宽度)
  名字.y = (A-N)/2 + Y - Oy = 17.5
```

**当前代码**：右头像贴左、名字全宽右对齐。

**改造**：

```objc
// Mode 6: left avatar (centered position) + name (right-aligned, nameWidth wide)
if (mode == 6) {
    self.rightAvatarView.hidden = YES;
    self.separatorView.hidden = YES;
    self.separatorTextLabel.hidden = YES;
    self.titleLabel.hidden = NO;
    self.leftAvatarView.hidden = NO;

    CGFloat nameWidth = [self calculateNameWidth];
    CGFloat gap = 8.0;

    // 头像 x: 整体居中偏移
    CGFloat avatarX = (totalW - avatarSize - nameWidth - gap) * 0.5;
    self.leftAvatarView.frame = CGRectMake(avatarX, avatarY, avatarSize, avatarSize);
    self.leftAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];

    // 名字：紧接头像右侧，右对齐，宽度为 nameWidth
    [self.titleLabel setFont:[UIFont systemFontOfSize:nameFontSize]];
    self.titleLabel.textAlignment = NSTextAlignmentRight;
    CGFloat nameX = avatarX + avatarSize + gap + config.chatNicknameOffsetX;
    self.titleLabel.frame = CGRectMake(nameX, avatarY + (avatarSize - nameFontSize) * 0.5 - config.chatNicknameOffsetY,
                                        nameWidth, nameFontSize);
    return;
}
```

---

## 阶段 8：修正 Mode 7 名字标签（可选）

> 涉及行：L124-L137

当前 Mode 7 的 `titleLabel.hidden = YES`（L135），而微信 Mode 7 中 `bVar1=true`，名字是可见的。

```
微信 Mode 7: 重叠头像 + 名字（居中，y=(A-N)/2+Y-Oy=17.5）
```

**如果希望完全对齐微信**，修改为：

```objc
// Mode 7 L135: 将 titleLabel.hidden = YES 改为
self.titleLabel.hidden = NO;

// 添加名字布局
if (!self.titleLabel.hidden) {
    [self.titleLabel setFont:[UIFont systemFontOfSize:nameFontSize]];
    self.titleLabel.textAlignment = NSTextAlignmentCenter;
    self.titleLabel.frame = CGRectMake(config.chatNicknameOffsetX,
                                        avatarY + (avatarSize - nameFontSize) * 0.5 - config.chatNicknameOffsetY,
                                        totalW - ABS(config.chatNicknameOffsetX), nameFontSize);
}
```

> 此阶段为**可选**，因为用户可能故意在 Mode 7 隐藏名字（重叠头像已占满视觉空间）。

---

## 改造后完整路由

```
mode==7 → 重叠（可含名字）
mode==5 → 名左 + 头像右              ✅ 不改
mode==6 → 左头像居中 + 名右对齐      ← 阶段 7
mode==2 → 左头像居中无名字           ← 阶段 3
mode==1 || mode==3 → 右头像居中无分隔符 ← 阶段 6
mode==0 || mode==4 → 双方头像分支    ← 阶段 5
  mode 0: 无名字
  mode 4: 名字下方
default → 双方头像 + 分隔符（非 mode 触发的通用路径）
```

---

## 各阶段影响范围

| 阶段 | 模式 | 严重度 | 改动行 | 依赖 |
|:---:|:---:|:---:|------|------|
| 1 | 1,4 | 🟡 | L111-L119 | 无 |
| 2+5 | 0 | 🔴 | L180-L194 删除，合并到 default | 阶段 1 |
| 3 | 2 | 🔴 | L232 前插入新分支 | 无 |
| 4+5 | 4 | 🟡 | L197-L211 删除，合并到 default | 阶段 1 |
| 5 | default | 🔴 | L232-L283 改写 | 阶段 2,4 |
| 6 | 1,3 | 🟢 | L213-L230 重写 | 阶段 1 |
| 7 | 6 | 🟡 | L162-L178 重写 | 无 |
| 8 | 7 | 🟢 | L135 改一行 | 无（可选） |

---

## 执行顺序

```
Step 1: 阶段 1 — 修正 avatarY 公式（影响 Mode 1,4）
Step 2: 阶段 3 — 插入 Mode 2 独立分支
Step 3: 阶段 7 — 重写 Mode 6
Step 4: 阶段 6 — 重写 Mode 1/3
Step 5: 阶段 2+4+5 — 删除 Mode 0/4 独立分支，改写 default 合并分支
Step 6: 阶段 8 — Mode 7 名字（可选）
```

> 为什么这个顺序：先修基础公式 → 再修独立模式 → 最后合并关联模式。

---

## 改造后的 layoutSubviews 完整代码

```objc
- (void)layoutSubviews {
    [super layoutSubviews];

    PluginConfig *config = [PluginConfig shared];
    NSInteger mode = config.chatDisplayMode;
    CGFloat totalH = self.bounds.size.height;
    CGFloat totalW = self.bounds.size.width;
    CGFloat avatarSize = config.chatAvatarSize;
    CGFloat avatarSpacing = config.chatAvatarSpacing;
    CGFloat halfSpacing = avatarSpacing * 0.5;
    CGFloat nameFontSize = config.chatNicknameFontSize;

    BOOL hasSeparator = (config.chatSeparatorText.length > 0);

    // avatarY: 微信两路公式
    CGFloat avatarY;
    if (mode == 1 || mode == 4) {
        avatarY = (totalH - avatarSize - 4 - nameFontSize) * 0.5 + 1;
    } else {
        avatarY = (totalH - avatarSize) * 0.5;
    }

    // ============ Mode 7: Overlap ============
    if (mode == 7) {
        self.leftAvatarView.frame = CGRectMake(0, avatarY, avatarSize, avatarSize);
        self.leftAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];

        CGFloat smallSize = avatarSize * 0.85;
        CGFloat offset = avatarSize * 0.7;
        self.rightAvatarView.frame = CGRectMake(offset, avatarY + (avatarSize - smallSize), smallSize, smallSize);
        self.rightAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:smallSize];

        self.separatorView.hidden = YES;
        self.separatorTextLabel.hidden = YES;
        self.titleLabel.hidden = YES;  // 或 NO（可选阶段 8）
        return;
    }

    // ============ Mode 5: 名字左 + 头像右 ============
    if (mode == 5) {
        self.separatorView.hidden = YES;
        self.separatorTextLabel.hidden = YES;
        self.rightAvatarView.hidden = YES;
        self.titleLabel.hidden = NO;

        CGFloat nameWidth = [self calculateNameWidth];
        CGFloat gap = 8.0;

        self.titleLabel.textAlignment = NSTextAlignmentLeft;
        [self.titleLabel setFont:[UIFont systemFontOfSize:nameFontSize]];
        self.titleLabel.frame = CGRectMake(
            (totalW - avatarSize - nameWidth - gap) * 0.5 + config.chatNicknameOffsetX,
            avatarY + (avatarSize - nameFontSize) * 0.5 - config.chatNicknameOffsetY,
            nameWidth, nameFontSize);

        self.leftAvatarView.frame = CGRectMake(
            CGRectGetMaxX(self.titleLabel.frame) + gap,
            avatarY, avatarSize, avatarSize);
        self.leftAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];
        return;
    }

    // ============ Mode 6: 左头像居中 + 名右对齐 ============
    if (mode == 6) {
        self.rightAvatarView.hidden = YES;
        self.separatorView.hidden = YES;
        self.separatorTextLabel.hidden = YES;
        self.titleLabel.hidden = NO;
        self.leftAvatarView.hidden = NO;

        CGFloat nameWidth = [self calculateNameWidth];
        CGFloat gap = 8.0;

        CGFloat avatarX = (totalW - avatarSize - nameWidth - gap) * 0.5;
        self.leftAvatarView.frame = CGRectMake(avatarX, avatarY, avatarSize, avatarSize);
        self.leftAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];

        [self.titleLabel setFont:[UIFont systemFontOfSize:nameFontSize]];
        self.titleLabel.textAlignment = NSTextAlignmentRight;
        CGFloat nameX = avatarX + avatarSize + gap + config.chatNicknameOffsetX;
        self.titleLabel.frame = CGRectMake(nameX,
            avatarY + (avatarSize - nameFontSize) * 0.5 - config.chatNicknameOffsetY,
            nameWidth, nameFontSize);
        return;
    }

    // ============ Mode 2: 左头像居中, 无名字 ============
    if (mode == 2) {
        self.rightAvatarView.hidden = YES;
        self.separatorView.hidden = YES;
        self.separatorTextLabel.hidden = YES;
        self.titleLabel.hidden = YES;

        self.leftAvatarView.frame = CGRectMake((totalW - avatarSize) * 0.5, avatarY,
                                                avatarSize, avatarSize);
        self.leftAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];
        return;
    }

    // ============ Mode 1, 3: 右头像居中, 无分隔符 ============
    if (mode == 1 || mode == 3) {
        BOOL showName = (mode == 1);

        self.leftAvatarView.hidden = YES;
        self.rightAvatarView.hidden = NO;
        self.separatorView.hidden = YES;
        self.separatorTextLabel.hidden = YES;
        self.titleLabel.hidden = !showName;

        self.rightAvatarView.frame = CGRectMake((totalW - avatarSize) * 0.5, avatarY,
                                                 avatarSize, avatarSize);
        self.rightAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];

        if (showName) {
            [self.titleLabel setFont:[UIFont systemFontOfSize:nameFontSize]];
            self.titleLabel.textAlignment = NSTextAlignmentCenter;
            self.titleLabel.frame = CGRectMake(0, avatarY + avatarSize + 1, totalW, nameFontSize);
            CGRect f = self.titleLabel.frame;
            f.origin.x += config.chatNicknameOffsetX;
            f.origin.y -= config.chatNicknameOffsetY;
            self.titleLabel.frame = f;
        }
        return;
    }

    // ============ Mode 0, 4 + default: 双方头像 + 可选名字/分隔符 ============
    BOOL showName = (mode == 4);
    self.titleLabel.hidden = !showName;
    self.leftAvatarView.hidden = NO;
    self.rightAvatarView.hidden = NO;

    if (hasSeparator) {
        self.separatorTextLabel.hidden = !config.chatSeparatorText.length;

        CGFloat totalContentW;
        if (config.chatSeparatorText.length > 0) {
            self.separatorTextLabel.text = config.chatSeparatorText;
            [self.separatorTextLabel sizeToFit];
            totalContentW = avatarSize + halfSpacing + self.separatorTextLabel.frame.size.width + halfSpacing + avatarSize;
        } else {
            totalContentW = avatarSize + avatarSpacing + avatarSize;
            self.separatorTextLabel.hidden = YES;
        }

        CGFloat startX = (totalW - totalContentW) * 0.5;
        self.leftAvatarView.frame = CGRectMake(startX, avatarY, avatarSize, avatarSize);

        if (!self.separatorTextLabel.hidden) {
            self.separatorTextLabel.frame = CGRectMake(startX + avatarSize + halfSpacing,
                (totalH - self.separatorTextLabel.frame.size.height) * 0.5,
                self.separatorTextLabel.frame.size.width,
                self.separatorTextLabel.frame.size.height);
            self.rightAvatarView.frame = CGRectMake(CGRectGetMaxX(self.separatorTextLabel.frame) + halfSpacing,
                avatarY, avatarSize, avatarSize);
        } else {
            self.rightAvatarView.frame = CGRectMake(startX + avatarSize + avatarSpacing,
                avatarY, avatarSize, avatarSize);
        }
        self.separatorView.hidden = YES;
    } else {
        CGFloat dualW = avatarSize * 2 + avatarSpacing;
        CGFloat startX = (totalW - dualW) * 0.5;
        self.separatorTextLabel.hidden = YES;
        self.separatorView.hidden = YES;
        self.leftAvatarView.frame  = CGRectMake(startX, avatarY, avatarSize, avatarSize);
        self.rightAvatarView.frame = CGRectMake(startX + avatarSize + avatarSpacing, avatarY, avatarSize, avatarSize);
    }

    self.leftAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];
    self.rightAvatarView.layer.cornerRadius = [self calculateCornerRadiusForSize:avatarSize];

    if (showName) {
        [self.titleLabel setFont:[UIFont systemFontOfSize:nameFontSize]];
        self.titleLabel.textAlignment = NSTextAlignmentCenter;
        self.titleLabel.frame = CGRectMake(0, avatarY + avatarSize + 1, totalW, nameFontSize);
    }

    [self applyPositionOffset];
}
```

---

## 改造后 8 种模式 vs 微信对照

| Mode | 改造前 | 改造后 | 对齐微信 |
|:---:|------|------|:---:|
| 0 | 左头像+名 | 双方头像居中无名字 | ✅ |
| 1 | 右头像+分隔符+名 | 右头像居中+名（无分隔符） | ✅ |
| 2 | 双方头像+分隔符 | 左头像居中无名字 | ✅ |
| 3 | 左头像+分隔符 | 右头像居中无名字 | ✅ |
| 4 | 单头像居中+名 | 双方头像居中+名 | ✅ |
| 5 | 名左+头像右 | 不变 | ✅ |
| 6 | 右头像贴左+名全宽 | 左头像居中+名 nameWidth 宽右对齐 | ✅ |
| 7 | 重叠 | 不变 | ✅ |

---

## 改造后验证清单

| T# | 模式 | 验北点 |
|----|:---:|------|
| V0 | 0 | 双方头像水平居中、无名字、无分隔符、间距 6px |
| V1 | 1 | 右头像居中、名字居中在下方、无分隔符 |
| V2 | 2 | 左头像居中、无名字、无分隔符 |
| V3 | 3 | 右头像居中、无名字、无分隔符 |
| V4 | 4 | 双方头像居中、名字居中在下方、间距 6px |
| V5 | 5 | 名字左对齐+头像右（与原来一致） |
| V6 | 6 | 左头像居中偏左、名字右对齐 nameWidth 宽 |
| V7 | 7 | 重叠头像（与原来一致） |

---

*文档生成时间：2026-05-27*
*基于：聊天顶栏_8种显示模式深度布局分析.md*