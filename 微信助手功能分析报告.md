# 微信助手 3.9-5 功能深度分析报告

> 分析目标: `微信助手 3.9-5(1).dylib` (Mach-O 64-bit arm64)
> 分析日期: 2026-05-10
> 分析功能: 快速置顶 / 备注 / 免打扰 / 会话盒子控制器
> 修订历史:
> - v2: 修正会话列表控制器类名（`WCAccountLoginController` 为登录页，非会话列表）
> - v3: 深度分析 `MGSessionBoxViewController` 自建控制器，补充 ObjC 属性编码、完整方法清单、手势冲突处理策略

---

## 一、dylib 整体架构概览

### 1.1 核心类结构

该 dylib 采用 Objective-C 编写，核心类如下:

| 类名 | 基类 | 实现协议 | 职责 |
|------|------|----------|------|
| `WeChatMiYouSeting` | NSObject | — | 主配置单例，管理所有功能的配置属性（属性编码方式与 `MiYouConfig` 联动） |
| `MiYouConfig` | NSObject | — | 配置持久化工具类，负责 NSUserDefaults 读写 |
| `MGSessionBoxViewController` | NSObject | `UIGestureRecognizerDelegate` / `UICollectionViewDelegate` / `UICollectionViewDataSource` | 会话盒子增强控制器（**自建类**，非 UIViewController），管理置顶/折叠/手势/会话盒子等增强功能 |
| `MiYouSettingViewController` | — | — | 插件设置主页面 |
| `MiYouNotificationViewController` | — | — | 通知/免打扰设置页面 |
| `MiYouViewController` | — | — | 插件功能主入口 |

> **关键发现**: `MGSessionBoxViewController` 继承自 `NSObject` 而非 `UIViewController`，它是一个**纯逻辑控制器**，通过向微信 `NewMainFrameViewController` 的 view 注入 `UICollectionView` 和 `UILongPressGestureRecognizer` 来实现功能。

### 1.2 Hook 机制

该 dylib 使用 **Method Swizzling** 方式 Hook 微信原生方法，核心模式为:

```
1. 通过 objc_getClass() 获取微信原生类
2. 通过 class_getInstanceMethod() 获取原始方法
3. 通过 method_setImplementation() 替换方法实现
4. 保存原始 IMP 以便回调原方法
```

### 1.3 微信会话列表控制器

> ⚠️ `WCAccountLoginController` 是微信的**登录页控制器**，不是会话列表控制器，绝不能作为 Hook 目标。

微信会话列表（聊天列表）的正确控制器类名:

| 类名 | 说明 | 验证来源 |
|------|------|----------|
| `NewMainFrameViewController` | **主选定的会话列表控制器** | 锤子助手 dylib + WCRefine dylib 交叉验证 |
| `MainFrameViewController` | 备选（旧版本微信） | 推测 |
| `WCNewMainFrameViewController` | 备选（带 WC 前缀） | 推测 |
| `WCAreaMainViewController` | 备选（iPad 分屏场景） | 推测 |

**交叉验证证据**:

| dylib | 发现的类名 |
|-------|-----------|
| `锤子助手2.4.1(1).dylib` | `NewMainFrameViewController`, `NewMainFrameRightTopMenuBtn` |
| `WCRefine 1.4-0(1).dylib` | `NewMainFrameViewController`, `NewMainFrameCell`, `MainFrameTableView`, `MainFrameHeaderLogic` |

**微信助手 dylib 的间接引用**:

| 引用 | 说明 |
|------|------|
| `FakeMainFrameCellData` | 引用了 MainFrame 前缀，间接确认微信使用 MainFrame 命名体系 |
| `getNewMainFrameViewController` | 微信内部获取会话列表 VC 的方法名 |

### 1.4 服务获取模式

通过 `MMServiceCenter` / `MMContext` 获取微信内部服务:

```objc
// 方式1: MMServiceCenter
Class scClass = objc_getClass("MMServiceCenter");
id center = [scClass defaultCenter];
id service = [center getService:[CContactMgr class]];

// 方式2: MMContext
Class ctxClass = objc_getClass("MMContext");
id activeCtx = [ctxClass activeUserContext];
id sessionMgr = [activeCtx newSessionMgr];
```

---

## 二、MGSessionBoxViewController 深度逆向分析

### 2.1 类声明还原（从 ObjC 编码提取）

```objc
@interface MGSessionBoxViewController : NSObject
    <UIGestureRecognizerDelegate,
     UICollectionViewDelegate,
     UICollectionViewDataSource>

@property (nonatomic, assign) BOOL isAddChatBox;                // TB,N,V_isAddChatBox
@property (nonatomic, assign) BOOL isBrandSessionHolderTop;     // TB,N,V_isBrandSessionHolderTop
@property (nonatomic, assign) BOOL isChatBoxTop;                // TB,N,V_isChatBoxTop
@property (nonatomic, assign) BOOL isManagerChatBox;            // TB,N,V_isManagerChatBox
@property (nonatomic, assign) BOOL mIsSessionGesture;           // TB,N,V_mIsSessionGesture
@property (nonatomic, assign) NSInteger chatBoxTopInList;       // Tq,N,V_chatBoxTopInList
@property (nonatomic, strong) NSMutableArray *filterSessionList; // T@"NSMutableArray",&,N,V_filterSessionList
@property (nonatomic, strong) UICollectionView *collectionView;  // 从协议实现推断

@end
```

### 2.2 为什么用 UICollectionView 而非 UITableView

| 原因 | 说明 |
|------|------|
| 横向滚动 | 会话盒子支持横向滑动浏览置顶会话，UICollectionViewFlowLayout 天然支持 |
| 自定义布局 | 每个会话项可以自定义大小和间距 |
| 不与微信原生冲突 | 微信会话列表用 `MainFrameTableView`(UITableView 子类)，用 CollectionView 不会产生 DataSource 冲突 |

### 2.3 完整方法清单

#### 2.3.1 会话盒子管理方法

| 方法 | 说明 |
|------|------|
| `addChatBoxSection` | 在设置页添加"会话盒子"配置区域 |
| `addSessionBoxSection` | 在设置页添加"会话盒子增强"配置区域 |
| `addCheckSession` | 添加检查会话功能 |
| `addClearSession` | 添加清除会话功能 |
| `addContactToChatBox:` | 将联系人添加到会话盒子 |
| `removeContactFromChatBox:` | 从会话盒子移除联系人 |
| `chatBoxSessions` | 获取会话盒子中的所有会话 |
| `isContactInChatBox:` | 判断联系人是否在会话盒子中 |
| `filterSessionList` / `_filterSessionList` | 过滤会话列表 |
| `setFilterSessionList:` | 设置过滤后的会话列表 |
| `checkLoadSessions` | 检查并加载会话 |

#### 2.3.2 设置方法

| 方法 | 说明 |
|------|------|
| `settingAddChatBoxEnable:` | 设置是否添加会话盒子 |
| `settingBrandSessionTop:` | 设置品牌号会话置顶 |
| `settingChatBoxTopEnable:` | 设置会话盒子置顶开关 |
| `settingManagerChatBoxEnable:` | 设置会话盒子管理模式开关 |
| `settingSessionGesture:` | 设置会话手势操作 |

#### 2.3.3 UIGestureRecognizerDelegate 方法（7个完整实现）

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `gestureRecognizerShouldBegin:` | YES | 允许手势开始识别 |
| `gestureRecognizer:shouldBeRequiredToFailByGestureRecognizer:` | NO | 其他手势不依赖自定义手势的失败 |
| `gestureRecognizer:shouldReceiveEvent:` | — | 手势是否接收事件 |
| `gestureRecognizer:shouldReceivePress:` | YES | 手势接收按压事件 |
| `gestureRecognizer:shouldReceiveTouch:` | YES | 手势接收触摸事件 |
| `gestureRecognizer:shouldRecognizeSimultaneouslyWithGestureRecognizer:` | **NO** | **不与微信原生手势同时识别（关键）** |
| `gestureRecognizer:shouldRequireFailureOfGestureRecognizer:` | NO | 自定义手势不依赖其他手势的失败 |

> **核心策略**: `shouldRecognizeSimultaneouslyWithGestureRecognizer:` 返回 `NO`，确保自定义长按手势和微信原生长按菜单互斥，避免两个菜单同时弹出。

#### 2.3.4 UICollectionView DataSource/Delegate 方法

| 方法 | 说明 |
|------|------|
| `numberOfSectionsInCollectionView:` | 会话盒子分组数 |
| `collectionView:numberOfItemsInSection:` | 每组会话项数量 |
| `collectionView:cellForItemAtIndexPath:` | 配置每个会话项 Cell |
| `collectionView:didSelectItemAtIndexPath:` | 点击会话项（打开聊天） |
| `collectionView:layout:sizeForItemAtIndexPath:` | 会话项大小 |
| `collectionView:layout:insetForSectionAtIndex:` | 分组内边距 |

### 2.4 工作原理流程图

```
┌──────────────────────────────────────────────────────────────┐
│                   NewMainFrameViewController                 │
│                    (微信会话列表控制器)                         │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              MainFrameTableView (UITableView)          │  │
│  │              (微信原生会话列表)                           │  │
│  │  ┌──────────────────────────────────────────────────┐  │  │
│  │  │  置顶会话区域                                     │  │  │
│  │  │  普通会话区域                                     │  │  │
│  │  └──────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  MGSessionBoxViewController (自建 NSObject 控制器)      │  │
│  │  ┌──────────────────────────────────────────────────┐  │  │
│  │  │  UICollectionView (横向滚动会话盒子)              │  │  │
│  │  │  [头像1] [头像2] [头像3] [头像4] ...              │  │  │
│  │  └──────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  UILongPressGestureRecognizer (注入的自定义长按手势)     │  │
│  │  → 弹出 ActionSheet: 置顶/备注/免打扰                    │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 2.5 初始化流程

```
dylib 加载 (__attribute__((constructor)))
    ↓
Hook NewMainFrameViewController 的 viewDidAppear:
    ↓
viewDidAppear: 触发时:
    ├── 1. 创建 MGSessionBoxViewController 实例
    ├── 2. 读取 WeChatMiYouSeting 配置
    ├── 3. 创建 UICollectionView + UICollectionViewFlowLayout
    ├── 4. 注册 UICollectionViewCell
    ├── 5. 加载会话数据 (filterSessionList)
    ├── 6. 将 CollectionView 添加到 NewMainFrameVC 的 view
    ├── 7. 注入 UILongPressGestureRecognizer 到 MainFrameTableView
    └── 8. 设置手势委托为 MGSessionBoxViewController
```

### 2.6 手势处理流程

```
用户长按 MainFrameTableView 上的会话
    ↓
UILongPressGestureRecognizer 触发
    ↓
MGSessionBoxViewController (UIGestureRecognizerDelegate):
    ├── gestureRecognizerShouldBegin: → YES
    ├── gestureRecognizer:shouldReceiveTouch: → YES
    └── gestureRecognizer:shouldRecognizeSimultaneously... → NO (不与原生手势共存)
    ↓
手势回调处理:
    ├── 获取触摸点 → [tableView indexPathForRowAtPoint:point]
    ├── 获取 indexPath
    ├── 通过 DataSource 获取 sessionInfo
    │   (getSessionInfoAtIndexPath: / logicGetSessionAtIndexPath:)
    ├── 从 sessionInfo 获取 m_nsUserName
    ├── 查询当前状态:
    │   ├── isContactSessionTop → 是否置顶
    │   ├── isChatStatusNotifyOpen → 是否免打扰
    │   └── m_nsRemark → 当前备注
    └── 弹出 UIAlertController ActionSheet
```

### 2.7 WeChatMiYouSeting 关联配置属性（从 ObjC 编码提取）

| 属性名 | ObjC 编码 | 类型 | 说明 |
|--------|-----------|------|------|
| `isAddChatBox` | `TB,N,V_isAddChatBox` | BOOL | 是否添加会话盒子 |
| `isBrandSessionHolderTop` | `TB,N,V_isBrandSessionHolderTop` | BOOL | 品牌号会话持有者是否置顶 |
| `isBrandTop` | `TB,N,V_isBrandTop` | BOOL | 品牌号是否置顶 |
| `isBrandShowRedDot` | `TB,N,V_isBrandShowRedDot` | BOOL | 品牌号是否显示红点 |
| `isChatBoxTop` | `TB,N,V_isChatBoxTop` | BOOL | 会话盒子是否置顶 |
| `isHelperTop` | `TB,N,V_isHelperTop` | BOOL | 辅助号是否置顶 |
| `isManagerChatBox` | `TB,N,V_isManagerChatBox` | BOOL | 是否进入会话盒子管理模式 |
| `isOpenBrandEnable` | `TB,N,V_isOpenBrandEnable` | BOOL | 品牌号功能开关 |
| `isOpenBrandHelper` | `TB,N,V_isOpenBrandHelper` | BOOL | 品牌号辅助开关 |
| `isOpenBrandNewEnable` | `TB,N,V_isOpenBrandNewEnable` | BOOL | 品牌号新功能开关 |
| `mIsAddDonotdisturbMenu` | `TB,N,V_mIsAddDonotdisturbMenu` | BOOL | 菜单中添加免打扰选项 |
| `mIsDonotdisturbState` | `TB,N,V_mIsDonotdisturbState` | BOOL | 免打扰全局状态 |
| `mIsBackEdgeGesture` | `TB,N,V_mIsBackEdgeGesture` | BOOL | 返回边缘手势 |
| `mIsMsgGestureEnbale` | `TB,N,V_mIsMsgGestureEnbale` | BOOL | 消息手势开关 |
| `mIsMsgGestureRightEnbale` | `TB,N,V_mIsMsgGestureRightEnbale` | BOOL | 消息右滑手势开关 |
| `mIsSessionGesture` | `TB,N,V_mIsSessionGesture` | BOOL | 会话手势开关 |
| `chatBoxTopInList` | `Tq,N,V_chatBoxTopInList` | NSInteger | 会话盒子在列表中的置顶位置 |
| `brandIdx` | `Tq,N,V_brandIdx` | NSInteger | 品牌号索引 |
| `brandIncoType` | `Tq,N,V_brandIncoType` | NSInteger | 品牌号图标类型 |
| `helperIncoType` | `Tq,N,V_helperIncoType` | NSInteger | 辅助号图标类型 |
| `filterSessionList` | `T@"NSMutableArray",&,N,V_filterSessionList` | NSMutableArray | 过滤后的会话列表 |
| `sessionDataList` | `T@"NSMutableArray",&,N,V_sessionDataList` | NSMutableArray | 会话数据列表 |
| `sessionList` | `T@"NSMutableArray",&,N,V_sessionList` | NSMutableArray | 会话列表 |
| `BrandList` | `T@"NSMutableArray",&,N,V_BrandList` | NSMutableArray | 品牌号列表 |
| `allBrandList` | `T@"NSMutableArray",&,N,V_allBrandList` | NSMutableArray | 所有品牌号列表 |
| `filterFriendList` | `T@"NSMutableArray",&,N,V_filterFriendList` | NSMutableArray | 过滤的好友列表 |
| `filterRoomList` | `T@"NSMutableArray",&,N,V_filterRoomList` | NSMutableArray | 过滤的群聊列表 |
| `mDonotdisturbList` | `T@"NSMutableArray",&,N,V_mDonotdisturbList` | NSMutableArray | 免打扰联系人列表 |
| `mDonotdisturbMsg` | `T@"NSString",&,N,V_mDonotdisturbMsg` | NSString | 免打扰自动回复消息 |
| `mDonotdisturbWorkingTime` | `T@"NSString",&,N,V_mDonotdisturbWorkingTime` | NSString | 免打扰工作时间 |
| `brandName` | `T@"NSString",C,N,V_brandName` | NSString | 品牌号名称 |

### 2.8 设置页面结构

微信助手使用微信原生的 `WCTableViewManager` 构建设置页面:

```objc
// 属性编码: T@"WCTableViewManager",&,N,V_tableViewManager
@property (nonatomic, strong) WCTableViewManager *tableViewManager;

// 添加 Section
[tableViewManager addSection:section];

// 添加 Cell (三种类型)
[tableViewManager addSwitchCellForSel:@selector(settingChatBoxTopEnable:)
                                title:@"会话盒子置顶"
                                  on:config.isChatBoxTop];

[tableViewManager addNormalCellForSel:@selector(settingBrandSessionTop:)
                                title:@"品牌号置顶"
                          rightValue:@""];

[tableViewManager addCenterCellForSel:@selector(showDonotdisturbList)
                                title:@"免打扰列表"];
```

**会话盒子设置区域** (`addSessionBoxSection`):

```
┌─────────────────────────────────────────┐
│ 会话盒子                                 │
├─────────────────────────────────────────┤
│ [开关] 添加会话盒子    settingAddChatBoxEnable: │
│ [开关] 管理会话盒子    settingManagerChatBoxEnable: │
│ [开关] 会话盒子置顶    settingChatBoxTopEnable: │
│ [开关] 品牌号置顶      settingBrandSessionTop: │
│ [开关] 辅助号置顶      settingIsHelperTop: │
│ [开关] 会话手势操作    settingSessionGesture: │
└─────────────────────────────────────────┘
```

**免打扰设置区域** (`addDonotdisturbSection`):

```
┌─────────────────────────────────────────┐
│ 免打扰                                   │
├─────────────────────────────────────────┤
│ [开关] 免打扰状态       settingDonotdisturbState: │
│ [开关] 菜单添加免打扰   settingAddDonotdisturbMenu: │
│ [→  ]  免打扰自动回复   settingDonotdisturbMsg │
│ [→  ]  免打扰工作时间   settingWorkingTime │
│ [→  ]  免打扰列表       showDonotdisturbList │
└─────────────────────────────────────────┘
```

---

## 三、快速置顶功能深度分析

### 3.1 功能描述

快速置顶功能允许用户在会话列表中快速将会话置顶或取消置顶，无需进入聊天详情页。支持:
- 普通会话置顶/取消置顶
- 公众号(品牌号)置顶/取消置顶
- 会话盒子(ChatBox)置顶
- 置顶会话折叠显示

### 3.2 涉及的微信原生 API

| API | 所属类 | 作用 |
|-----|--------|------|
| `TopSessionByName:` | SessionMgr | 置顶指定会话 |
| `UntopSessionByName:` | SessionMgr | 取消置顶指定会话 |
| `setContact:sessionTop:sync:` | SessionMgr | 设置联系人会话置顶状态并同步 |
| `isContactSessionTop` | CContact | 判断联系人是否已置顶 |
| `m_bIsTop` | MMSessionInfo | 会话是否置顶的标记 |
| `m_topSessions` | MMSessionInfo | 置顶会话列表 |
| `topSessions` | SessionMgr | 获取所有置顶会话 |
| `setTopSessions:` | SessionMgr | 设置置顶会话列表 |
| `canFoldTopSession` | SessionMgr | 是否支持折叠置顶会话 |
| `isBrandTop` | CContact | 品牌号是否置顶 |
| `isBrandSessionHolderTop` | CContact | 品牌号会话是否置顶 |
| `isChatBoxTop` | CContact | 会话盒子是否置顶 |
| `isHelperTop` | CContact | 辅助号是否置顶 |
| `chatBoxTopInList` | CContact | 会话盒子在列表中的置顶位置 |
| `isBrandSessionHolder` | CContact | 是否为品牌号会话持有者 |
| `rebuildMainSessions` | SessionMgr | 重建主会话列表 |
| `resortSessions` | SessionMgr | 重新排序会话 |
| `updateMainSessionList` | SessionMgr | 更新主会话列表 |
| `fulFillSession:` | SessionMgr | 完成会话填充 |
| `SyncFulFillSession:` | SessionMgr | 同步完成会话填充 |

### 3.3 实现原理

#### 3.3.1 置顶操作

```objc
- (void)topSessionByName:(NSString *)userName {
    id sessionMgr = [self getSessionMgr];
    id contactMgr = [self getContactMgr];
    id contact = [contactMgr getContactByName:userName];
    
    if ([contact isBrandSessionHolder]) {
        [contact setIsBrandTop:YES];
        [contact setIsBrandSessionHolderTop:YES];
    } else {
        [sessionMgr TopSessionByName:userName];
        [sessionMgr setContact:contact sessionTop:YES sync:YES];
    }
    
    [sessionMgr resortSessions];
    [sessionMgr updateMainSessionList];
}
```

```objc
- (void)untopSessionByName:(NSString *)userName {
    id sessionMgr = [self getSessionMgr];
    id contactMgr = [self getContactMgr];
    id contact = [contactMgr getContactByName:userName];
    
    if ([contact isBrandSessionHolder]) {
        [contact setIsBrandTop:NO];
        [contact setIsBrandSessionHolderTop:NO];
    } else {
        [sessionMgr UntopSessionByName:userName];
        [sessionMgr setContact:contact sessionTop:NO sync:YES];
    }
    
    [sessionMgr resortSessions];
    [sessionMgr updateMainSessionList];
}
```

### 3.4 配置属性

| 属性 | ObjC 编码 | 类型 | 说明 |
|------|-----------|------|------|
| `m_bIsTop` | — | BOOL | 会话是否置顶 |
| `isBrandTop` | `TB,N,V_isBrandTop` | BOOL | 品牌号是否置顶 |
| `isBrandSessionHolderTop` | `TB,N,V_isBrandSessionHolderTop` | BOOL | 品牌号会话持有者是否置顶 |
| `isChatBoxTop` | `TB,N,V_isChatBoxTop` | BOOL | 会话盒子是否置顶 |
| `isHelperTop` | `TB,N,V_isHelperTop` | BOOL | 辅助号是否置顶 |
| `chatBoxTopInList` | `Tq,N,V_chatBoxTopInList` | NSInteger | 会话盒子在列表中的置顶位置 |

---

## 四、备注功能深度分析

### 4.1 功能描述

备注功能允许用户在会话列表中快速修改联系人的备注名，无需进入联系人详情页。该功能通过在长按菜单中添加"修改备注"选项来实现。

### 4.2 涉及的微信原生 API

| API | 所属类 | 作用 |
|-----|--------|------|
| `m_nsRemark` | CContact | 联系人备注名属性 |
| `m_nsNickName` | CContact | 联系人昵称属性 |
| `m_nsAliasName` | CContact | 联系人别名属性 |
| `m_nsUserName` | CContact | 联系人用户名(wxid) |
| `getContactByName:` | CContactMgr | 通过用户名获取联系人 |
| `getContactByNameFromCache:` | CContactMgr | 通过用户名获取联系人(缓存版) |
| `updateContact:` | CContactMgr | 更新联系人信息 |
| `modifyDataItem:notify:` | CContactMgr | 修改联系人数据项并通知 |
| `chatProfileSwitchSetting:withType:andValue:` | CContactMgr | 聊天资料页设置开关 |
| `setM_nsRemark:` | CContact | 设置联系人备注名 |

### 4.3 实现原理

```objc
- (void)showEditRemarkForContact:(NSString *)userName {
    id contactMgr = [self getContactMgr];
    id contact = [contactMgr getContactByName:userName];
    
    NSString *currentRemark = contact.m_nsRemark ?: @"";
    NSString *nickName = contact.m_nsNickName ?: @"";
    
    UIAlertController *alert = [UIAlertController 
        alertControllerWithTitle:@"修改备注"
                        message:[NSString stringWithFormat:@"当前昵称: %@", nickName]
                 preferredStyle:UIAlertControllerStyleAlert];
    
    [alert addTextFieldWithConfigurationHandler:^(UITextField *textField) {
        textField.text = currentRemark;
        textField.placeholder = @"请输入备注名";
    }];
    
    [alert addAction:[UIAlertAction actionWithTitle:@"确定" style:UIAlertActionStyleDefault handler:^(UIAlertAction *action) {
        NSString *newRemark = alert.textFields.firstObject.text;
        [contact setM_nsRemark:newRemark];
        [contactMgr modifyDataItem:contact notify:YES];
    }]];
    
    [alert addAction:[UIAlertAction actionWithTitle:@"取消" style:UIAlertActionStyleCancel handler:nil]];
    [[self getTopViewController] presentViewController:alert animated:YES completion:nil];
}
```

### 4.4 CContact 关键属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `m_nsRemark` | NSString | 联系人备注名(可读写) |
| `m_nsNickName` | NSString | 联系人昵称 |
| `m_nsAliasName` | NSString | 联系人别名 |
| `m_nsUserName` | NSString | 联系人用户名(wxid) |
| `m_nsEncodeUserName` | NSString | 编码用户名 |
| `m_nsFullPY` | NSString | 全拼 |
| `m_nsShortPY` | NSString | 简拼 |
| `m_nsDisplayNamePY` | NSString | 显示名拼音 |
| `m_nsChatRoomUserName` | NSString | 群聊用户名 |
| `m_nsChatRoomMemList` | NSString | 群成员列表 |
| `m_nsChatRoomAdminList` | NSString | 群管理员列表 |
| `m_nsContent` | NSString | 内容 |
| `m_nsFromUsr` | NSString | 来源用户 |
| `m_nsToUsr` | NSString | 目标用户 |
| `m_nsTicket` | NSString | 票据 |
| `m_nsOwner` | NSString | 所有者 |
| `m_oSetting` | id | 设置对象 |
| `contactType` | NSInteger | 联系人类型 |
| `m_uiCreateTime` | unsigned int | 创建时间 |

---

## 五、免打扰功能深度分析

### 5.1 功能描述

免打扰功能允许用户快速设置会话的消息免打扰状态，支持:
- 单个会话免打扰开关
- 批量免打扰设置
- 免打扰工作时间设置(定时免打扰)
- 免打扰消息自定义回复
- 在会话列表长按菜单中添加免打扰选项

### 5.2 涉及的微信原生 API

| API | 所属类 | 作用 |
|-----|--------|------|
| `ChangeNotifyStatus:withStatus:sync:` | CContactMgr/CNotifyMgr | 修改通知状态并同步 |
| `isChatStatusNotifyOpen` | CContact | 聊天通知是否开启 |
| `setChatStatusNotifyOpen:` | CContact | 设置聊天通知开关 |
| `mIsAutoNotifyOpen` | BOOL | 自动通知开关 |

### 5.3 实现原理

#### 5.3.1 免打扰状态切换

```objc
- (void)settingDonotdisturbState:(id)sender {
    NSString *userName = [self currentSessionUserName];
    id contactMgr = [self getContactMgr];
    id contact = [contactMgr getContactByName:userName];
    
    BOOL isNotifyOpen = [contact isChatStatusNotifyOpen];
    NSInteger newStatus = isNotifyOpen ? 0 : 1;
    
    [contactMgr ChangeNotifyStatus:userName withStatus:newStatus sync:YES];
    [WeChatMiYouSeting sharedInstance].mIsDonotdisturbState = !isNotifyOpen;
}
```

#### 5.3.2 免打扰列表管理

```objc
- (void)showDonotdisturbList {
    NSMutableArray *list = [WeChatMiYouSeting sharedInstance].mDonotdisturbList;
    // ... 创建列表页面并展示
}

- (void)settingAddDonotdisturbMenu:(id)sender {
    BOOL isAddMenu = [WeChatMiYouSeting sharedInstance].mIsAddDonotdisturbMenu;
    // 在会话列表长按菜单中添加/移除免打扰选项
}
```

#### 5.3.3 免打扰消息自动回复

```objc
- (void)settingDonotdisturbMsg {
    NSString *msg = [WeChatMiYouSeting sharedInstance].mDonotdisturbMsg;
    // 弹出输入框让用户编辑免打扰自动回复消息
}
```

#### 5.3.4 免打扰工作时间

```objc
- (void)setDonotdisturbWorkingTime:(NSString *)timeRange {
    [WeChatMiYouSeting sharedInstance].mDonotdisturbWorkingTime = timeRange;
    // 解析时间范围，设置定时器
    // 例如: "09:00-18:00" 表示9点到18点免打扰
}
```

### 5.4 配置属性

| 属性 | ObjC 编码 | 类型 | 说明 |
|------|-----------|------|------|
| `mDonotdisturbList` | `T@"NSMutableArray",&,N,V_mDonotdisturbList` | NSMutableArray | 免打扰联系人列表 |
| `mDonotdisturbMsg` | `T@"NSString",&,N,V_mDonotdisturbMsg` | NSString | 免打扰自动回复消息 |
| `mDonotdisturbWorkingTime` | `T@"NSString",&,N,V_mDonotdisturbWorkingTime` | NSString | 免打扰工作时间 |
| `mIsAddDonotdisturbMenu` | `TB,N,V_mIsAddDonotdisturbMenu` | BOOL | 是否在菜单中添加免打扰选项 |
| `mIsDonotdisturbState` | `TB,N,V_mIsDonotdisturbState` | BOOL | 免打扰全局开关状态 |

---

## 六、三个功能的交互流程

### 6.1 会话列表长按菜单统一入口

三个功能都通过 `MGSessionBoxViewController` 向微信会话列表的 TableView 注入自定义长按手势来触发操作菜单:

```
用户长按会话
    ↓
自定义 UILongPressGestureRecognizer (注入到 MainFrameTableView)
    ↓
MGSessionBoxViewController (UIGestureRecognizerDelegate) 处理冲突:
    shouldRecognizeSimultaneouslyWithGestureRecognizer: → NO
    ↓
获取长按位置 → indexPathForRowAtPoint: → 获取 indexPath
    ↓
通过 DataSource 获取 sessionInfo:
  getSessionInfoAtIndexPath: / logicGetSessionAtIndexPath:
    ↓
从 sessionInfo 获取 userName (m_nsUserName)
    ↓
查询当前状态:
  ├── isContactSessionTop → 是否置顶
  ├── isChatStatusNotifyOpen → 是否免打扰
  └── m_nsRemark → 当前备注
    ↓
构建 UIAlertController ActionSheet:
  ├── 置顶会话 / 取消置顶
  ├── 修改备注
  └── 消息免打扰 / 取消免打扰
    ↓
用户选择菜单项
    ↓
执行对应操作:
  ├── TopSessionByName: / UntopSessionByName: + resortSessions
  ├── setM_nsRemark: + modifyDataItem:notify:
  └── ChangeNotifyStatus:withStatus:sync:
    ↓
刷新会话列表
```

### 6.2 会话列表滑动操作

通过 Hook `filteredMenuItems:` 方法，在会话列表左滑菜单中注入自定义操作项。

### 6.3 会话手势操作

通过 `settingSessionGesture:` 和 `mIsSessionGesture` 配置，支持自定义手势操作。

---

## 七、关键微信内部类与方法汇总

### 7.1 会话列表 UI 层

| 类名 | 说明 |
|------|------|
| `NewMainFrameViewController` | **会话列表主控制器**（微信主界面聊天列表页） |
| `MainFrameTableView` | 会话列表的 UITableView 子类 |
| `NewMainFrameCell` | 会话列表的 UITableViewCell 子类 |
| `FakeMainFrameCellData` | 会话列表 Cell 数据模型 |
| `MainFrameHeaderLogic` | 会话列表 Header 逻辑 |
| `NewMainFrameRightTopMenuBtn` | 会话列表右上角菜单按钮 |
| `WCTableViewManager` | 微信原生 TableView 管理器（用于构建设置页） |

> ⚠️ `WCAccountLoginController` 是**登录页控制器**，与任何会话列表功能无关。

### 7.2 会话管理

| 类/方法 | 说明 |
|---------|------|
| `MMNewSessionMgr` / `CSessionMgr` / `MMSessionMgr` | 会话管理器(不同版本类名不同) |
| `GetSessionInfoList` | 获取会话信息列表 |
| `GetSessionByUserName:` | 通过用户名获取会话 |
| `GetSessionAtIndex:` | 通过索引获取会话 |
| `GetSessionCount` | 获取会话数量 |
| `GetUserNamesOnSessionList` | 获取会话列表中的用户名 |
| `DeleteSessionOfUser:` | 删除指定用户的会话 |
| `DeleteSessionAtIndex:` | 删除指定索引的会话 |
| `ChangeSessionUnReadCount:to:` | 修改会话未读数 |
| `createSessionWithUserName:nickName:showRedDot:readAsRedDot:` | 创建会话 |
| `hideSession:` | 隐藏会话 |
| `fulFillSession:` | 填充会话数据 |
| `SyncFulFillSession:` | 同步填充会话数据 |
| `rebuildMainSessions` | 重建主会话列表 |
| `resortSessions` | 重新排序会话 |
| `updateMainSessionList` | 更新主会话列表 |
| `updateMainSessionListNotify:` | 更新主会话列表并通知 |

### 7.3 联系人管理

| 类/方法 | 说明 |
|---------|------|
| `CContactMgr` | 联系人管理器 |
| `getContactByName:` | 通过用户名获取联系人 |
| `getContactByNameFromCache:` | 通过用户名获取联系人(缓存版) |
| `getContactList:contactType:` | 获取指定类型的联系人列表 |
| `getAllContacts` | 获取所有联系人 |
| `getDicContacts` | 获取联系人字典 |
| `getSelfContact` | 获取自己的联系人信息 |
| `updateContact:` | 更新联系人信息 |
| `modifyDataItem:notify:` | 修改数据项并通知 |
| `deleteContact:listType:` | 删除联系人 |
| `deleteContactLocal:listType:` | 本地删除联系人 |
| `ChangeNotifyStatus:withStatus:sync:` | 修改通知状态 |

### 7.4 消息管理

| 类/方法 | 说明 |
|---------|------|
| `CMessageMgr` | 消息管理器 |
| `AddMsg:MsgWrap:` | 添加消息 |
| `AddLocalMsg:MsgWrap:` | 添加本地消息 |
| `GetMsg:LocalID:` | 通过本地ID获取消息 |
| `GetMsg:n64SvrID:` | 通过服务器ID获取消息 |
| `ClearUnRead:FromID:ToID:` | 清除未读消息 |

### 7.5 服务获取

| 类/方法 | 说明 |
|---------|------|
| `MMServiceCenter` | 服务中心 |
| `defaultCenter` | 获取默认服务中心 |
| `getService:` | 获取指定服务 |
| `MMContext` | 上下文 |
| `activeUserContext` | 获取活跃用户上下文 |
| `serviceCenter` | 获取服务中心 |
| `newSessionMgr` | 获取新会话管理器 |

### 7.6 会话信息获取（TableView DataSource 方法）

| 方法 | 说明 |
|------|------|
| `getSessionInfoAtIndexPath:` | 通过 indexPath 获取会话信息（首选） |
| `logicGetSessionAtIndexPath:` | 通过 indexPath 获取会话信息（备选1） |
| `sessionInfoForIndexPath:` | 通过 indexPath 获取会话信息（备选2） |
| `GetSessionInfoByIndex:` | 通过 index 获取会话信息（备选3） |

---

## 八、在现有项目中的实现方案

### 8.1 项目架构适配

当前项目 `WeChatPlugin` 采用模块化架构:

```
WeChatPlugin/
├── Config/          # 配置 (PluginConfig)
├── Core/            # 核心 (HookEngine, WPAlert)
├── Modules/         # 功能模块 (各Hook)
├── Registry/        # 注册中心 (HookRegistry, FeatureModuleRegistry)
├── Settings/        # 设置界面 (各Controller)
└── Tweak.m          # 入口
```

### 8.2 需要新增的文件

```
Modules/
├── SessionBox/
│   ├── WPSessionBoxController.h      # 会话盒子控制器头文件
│   ├── WPSessionBoxController.m      # 会话盒子控制器实现
│   ├── WPSessionBoxHook.h            # Hook 安装器
│   └── WPSessionBoxHook.m            # Hook 安装器实现
```

### 8.3 PluginConfig 需要新增的属性

```objc
#pragma mark - 快速置顶
@property (nonatomic, assign) BOOL quickPinEnabled;
@property (nonatomic, assign) BOOL foldTopSessionEnabled;
@property (nonatomic, assign) BOOL brandTopEnabled;
@property (nonatomic, assign) BOOL chatBoxTopEnabled;

#pragma mark - 快速备注
@property (nonatomic, assign) BOOL quickRemarkEnabled;

#pragma mark - 免打扰
@property (nonatomic, assign) BOOL quickMuteEnabled;
@property (nonatomic, assign) BOOL addMuteMenuItemEnabled;
@property (nonatomic, copy) NSString *muteAutoReplyMsg;
@property (nonatomic, copy) NSString *muteWorkingTime;
@property (nonatomic, strong) NSMutableArray *muteContactList;
```

### 8.4 WPSessionBoxController 核心设计

仿照微信助手的 `MGSessionBoxViewController`，自建 `WPSessionBoxController`:

```objc
@interface WPSessionBoxController : NSObject
    <UIGestureRecognizerDelegate,
     UICollectionViewDelegate,
     UICollectionViewDataSource>

@property (nonatomic, assign) BOOL isAddChatBox;
@property (nonatomic, assign) BOOL isChatBoxTop;
@property (nonatomic, assign) BOOL isManagerChatBox;
@property (nonatomic, assign) BOOL isSessionGesture;
@property (nonatomic, assign) NSInteger chatBoxTopInList;
@property (nonatomic, strong) NSMutableArray<NSString *> *filterSessionList;
@property (nonatomic, strong) UICollectionView *collectionView;
@property (nonatomic, weak) UIViewController *hostViewController;

+ (instancetype)shared;
- (void)attachToViewController:(UIViewController *)vc;
- (void)detachFromViewController;
- (void)reloadData;
- (BOOL)isContactInChatBox:(NSString *)userName;
- (void)addContactToChatBox:(NSString *)userName;
- (void)removeContactFromChatBox:(NSString *)userName;
- (NSArray *)chatBoxSessions;

@end
```

### 8.5 WPSessionBoxHook 安装器

```objc
// Hook NewMainFrameViewController 的 viewDidAppear:
// 在 viewDidAppear 时调用 [WPSessionBoxController shared] attachToViewController:

+ (void)install {
    const char *candidateNames[] = {
        "NewMainFrameViewController",
        "MainFrameViewController",
        "WCNewMainFrameViewController",
        "WCAreaMainViewController",
        "MainSessionViewController"
    };
    
    for (int i = 0; i < 5; i++) {
        Class cls = objc_getClass(candidateNames[i]);
        if (cls) {
            g_mainFrameClass = cls;
            break;
        }
    }
    
    if (!g_mainFrameClass) return;
    
    IMP orig = [HookEngine swizzleMethod:NSSelectorFromString(@"viewDidAppear:")
                                 inClass:g_mainFrameClass
                                 withIMP:(IMP)replaced_viewDidAppear];
    if (orig) orig_viewDidAppear = orig;
}
```

### 8.6 注册到 FeatureModuleRegistry

```objc
[FeatureModuleDescriptor moduleWithIdentifier:@"sessionbox"
                                        title:@"会话增强"
                                     subtitle:@"快速置顶/备注/免打扰"
                                 sectionTitle:@"通用"
                              controllerClass:[SettingSessionBoxController class]
                         hookInstallerClasses:@[[WPSessionBoxHook class]]]
```

---

## 九、注意事项与风险

### 9.1 兼容性

1. **微信版本差异**: 不同版本的微信内部类名可能不同（如 `MMNewSessionMgr` vs `CSessionMgr`），需要做兼容性检查
2. **API 稳定性**: 微信内部 API 没有公开文档，可能随版本更新而变化
3. **方法存在性检查**: 所有 `objc_msgSend` 调用前必须用 `respondsToSelector:` 检查
4. **控制器类名**: `NewMainFrameViewController` 已通过锤子助手、WCRefine 两个第三方 dylib 交叉验证，但微信版本更新后可能变化

### 9.2 数据安全

1. **备注修改**: 直接修改 `m_nsRemark` 后必须调用 `modifyDataItem:notify:` 通知微信同步到服务器
2. **置顶操作**: 使用 `setContact:sessionTop:sync:` 的 `sync:YES` 参数确保同步到服务器
3. **免打扰状态**: 使用 `ChangeNotifyStatus:withStatus:sync:` 的 `sync:YES` 确保服务器同步

### 9.3 手势冲突处理

1. **与微信原生手势互斥**: `shouldRecognizeSimultaneouslyWithGestureRecognizer:` 必须返回 `NO`
2. **手势注入时机**: 在 `viewDidAppear:` 中注入，使用 Associated Object 防止重复添加
3. **7 个委托方法全部实现**: 微信助手实现了全部 7 个 `UIGestureRecognizerDelegate` 方法，必须同样完整实现

### 9.4 Hook 策略选择

| 策略 | 优点 | 缺点 |
|------|------|------|
| Hook `handlelongGesture:` | 与微信原生手势共存 | 依赖微信已有该方法，且可能冲突 |
| **注入自定义长按手势** ✅ | 不依赖微信内部方法，更稳定 | 需要处理与原生手势的冲突 |
| Hook `viewDidAppear:` + 注入手势 | 最稳定，兼容性最好 | 需要正确找到 TableView |

> **推荐策略**: Hook `NewMainFrameViewController` 的 `viewDidAppear:`，然后创建 `WPSessionBoxController` 实例，向 `MainFrameTableView` 注入自定义 `UILongPressGestureRecognizer`，同时向 VC 的 view 添加 `UICollectionView` 会话盒子。这与微信助手 dylib 的 `MGSessionBoxViewController` 实现方式一致。

---

## 十、微信助手 dylib 完整类列表

以下是从 dylib 中提取的所有自定义类:

| 类名 | 基类/协议 | 分类 |
|------|-----------|------|
| `MGSessionBoxViewController` | NSObject <UIGestureRecognizerDelegate, UICollectionViewDelegate, UICollectionViewDataSource> | 会话增强 |
| `MGAudioUtils` | — | 音频工具 |
| `MGLog` | — | 日志 |
| `MGTipsRecordViewController` | — | 提示记录 |
| `MiYouConfig` | — | 配置管理 |
| `MiYouSettingViewController` | — | 设置主页 |
| `MiYouViewController` | — | 功能主页 |
| `MiYouNotificationViewController` | — | 通知设置 |
| `MiYouTableViewCell` | — | 通用单元格 |
| `MiYouSlideView` | — | 滑动视图 |
| `MiYouFloatingView` | — | 悬浮窗 |
| `MiYouDanMuView` | — | 弹幕视图 |
| `MiYouHudView` | — | HUD 提示 |
| `MiYouToastView` | — | Toast 提示 |
| `MiYouIconsViewController` | — | 图标设置 |
| `MiYouTaskModel` | — | 任务模型 |
| `MiYouTaskViewController` | — | 任务管理 |
| `MiYouAddTaskViewController` | — | 添加任务 |
| `MiYouKeyDeleteViewController` | — | 关键字删除 |
| `MiYouKeywordListViewController` | — | 关键字列表 |
| `MiYouKeywordTipsController` | — | 关键字提示 |
| `MiYouMassSendWriteViewController` | — | 群发消息 |
| `MiYouRepayMsgViewController` | — | 回复消息 |
| `MiYouMessageAction` | — | 消息操作 |
| `MiYouMessageToolBar` | — | 消息工具栏 |
| `MiYouPriUtil` | — | 权限工具 |
| `WeChatMiYouSeting` | — | 全局配置单例 |
| `MYChatGPT` | — | ChatGPT |
| `MYChatGPTViewController` | — | ChatGPT 页面 |
| `MYActionsViewController` | — | 操作列表 |
| `MYMultiSelectViewController` | — | 多选 |
| `MYMessageTask` | — | 消息任务 |
| `MYMotionManager` | — | 运动管理 |
| `MYReplyConfig` | — | 回复配置 |
| `MYFontViewController` | — | 字体设置 |
| `MYThemesViewController` | — | 主题设置 |
| `MYRoomTool` | — | 群聊工具 |
| `MYMsgListViewController` | — | 消息列表 |
| `MYEditViewController` | — | 编辑页面 |
| `CheckHelper` | — | 检测助手 |
| `CheckTipsView` | — | 检测提示 |
| `CheckFriendsSettingController` | — | 好友检测设置 |
| `ClearFriendsViewController` | — | 清理好友 |
| `DeleteViewController` | — | 删除页面 |
| `AddTaskViewController` | — | 添加任务 |
| `ActionsViewController` | — | 操作页面 |
| `FloatingView` | — | 悬浮窗 |
| `FontViewController` | — | 字体设置 |
| `TipsController` | — | 提示控制器 |
| `TipsView` | — | 提示视图 |
| `TipsRecordViewController` | — | 提示记录 |
| `ThemesViewController` | — | 主题设置 |
| `LMFileBrowserViewController` | — | 文件浏览 |
| `LMFileModel` | — | 文件模型 |
| `GroupTool` | — | 群聊工具 |
| `SpotlightUtil` | — | Spotlight 工具 |
| `WCThemeMgr` | — | 主题管理 |
