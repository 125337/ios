# 伪已读自定义格式 —— 深度分析

> **分析对象**：微信优化 1.6.5（`/www/wwwroot/ios/插件/微信优化反编译最新/123456.c`）
> **对比对象**：WeChatPlugin（`/www/wwwroot/ios/WeChatPlugin`）
> **功能描述**：在消息时间自定义格式输入框中输入 `{伪已读}`，替代或伪造消息的"已读"状态文字显示。

---

## 一、功能概述

微信优化 1.6.5 的自定义时间格式引擎除了支持 12 个英文日期令牌（`{YYYY}`、`{HH}`、`{EE}`、`{a}` 等）外，还额外支持 **中文令牌**（如 `{伪已读}`、`{已读}` 等），这些中文令牌被存放在 `NSConstantArray_00121a60` 令牌数组中，由格式引擎 `FUN_0003bcb0` 统一处理。

当用户在自定义格式输入框中输入 `{伪已读}{HH}:{mm}:{ss}` 时，格式引擎会将 `{伪已读}` 替换为伪已读状态文字后再拼接时间，最终在消息气泡下方显示类似 `已读 14:30:00` 的效果。

| 对比项 | WeChatPlugin (A) | 微信优化 1.6.5 (B) | 差距 |
|--------|:---:|:---:|------|
| 英文令牌数量 | 12 个 | 12 个 | 一致 |
| **中文令牌支持** | **0 个** | **≥ 3 个**（至少含 `{伪已读}` 等） | A 缺失 |
| 中文令牌 epub | — | 同为格式引擎的非特殊令牌路径 | — |
| 中文令牌用途 | — | 伪造消息"已读"状态文字 | — |

---

## 二、中文令牌的存储位置

### 2.1 常量声明证据

在 `123456.c` 的数据段中，发现了 **8 个花括号开头的乱码常量**（IDA 无法解码中文字符，显示为 ASCII 乱码）：

```
行 70:   cfstringStruct cf_{|W(;           ← "{伪...}" 或 "{已...}"
行 2131: cfstringStruct cf_{t>f:yTUS;       ← 含中文的花括号令牌
行 2282: cfstringStruct cf_{tTUS;           ← 含中文的花括号令牌
行 3750: cfstringStruct cf_{|W;            ← 含中文的花括号令牌
行 3764: cfstringStruct cf_{|+R;           ← 含中文的花括号令牌
行 3773: cfstringStruct cf_{tXT0;          ← 含中文的花括号令牌
行 3776: cfstringStruct cf_{tXT%;          ← 含中文的花括号令牌
```

其中：
- `cf_{|W(` 出现在数据段 `00121a60` 数组附近（行 70-75 与 `FUN_00005a9c` 相邻，这是消息时间相关的函数地址）
- `cf_{t>f:yTUS` 出现在 UI 区域（行 2131，与 `person.fill.xmark` SF Symbol 相邻）
- `cf_{tXT0` / `cf_{tXT%` 出现在编辑器相关数据段（行 3773-3776）

### 2.2 数组引用链

中文令牌在格式引擎中的引用路径：

```
FUN_0003bcb0 (格式引擎)
  │
  ├── NSConstantArray_00121a60  ← 令牌名称数组（含英文 12 个 + 中文 N 个）
  │     ├── [0]  {YYYY}         (cf_{YYYY})
  │     ├── [1]  {YY}           (cf_{YY})
  │     ├── ...
  │     ├── [11] {b}            (cf_{b})
  │     ├── [12] {伪已读}        (cf_{|W(  ← 中文令牌 IDA 乱码)
  │     ├── [13] {...}          (更多中文令牌)
  │     └── [14] ...
  │
  └── NSConstantArray_00121a78  ← 对应 NSDateFormatter 格式字符串
        ├── [0]  "YYYY"
        ├── [1]  "YY"
        ├── ...
        ├── [12] ? (可能是 "HH:mm:ss" 或其他)
        └── [13] ...
```

> **注意**：由于 IDA 反编译的常数字符串名称基于 UTF-8 字节的 ASCII 解码，中文 UTF-8 的高位字节（如 0xE4 0xBC 0xAA = "伪"）会被显示为 `ä¼ª` 之类的乱码。因此无法从 C 伪代码名称直接反推中文字符，只能确证其存在。

---

## 三、格式引擎如何处理中文令牌

### 3.1 引擎控制流

```
FUN_0003bcb0(createTime, isSender, statusCode)
{
    读取 customFormat (NSUserDefaults)
    ↓
    去除 \n
    ↓
    外层循环: 遍历 NSConstantArray_00121a60 中 ALL 令牌
    ┌─────────────────────────────────────────────────────────┐
    │ for (uVar20 = 0; uVar20 < arrayCount; uVar20++) {       │
    │   token = array[uVar20];                                │
    │                                                         │
    │   while (token in formatString) {                       │
    │     if (token == "{EE}")                                │
    │       → zh_Hans_CN + cf_fg→cf_hT 替换                    │
    │                                                         │
    │     else if (token == "{EEEE}" || token == "{a}")       │
    │       → zh_Hans_CN                                       │
    │                                                         │
    │     else if (token == "{b}")                            │
    │       → en_US_POSIX + noon→PM / midnight→AM             │
    │                                                         │
    │     else {          ← 中文令牌走这里！                    │
    │       locale = en_US_POSIX                              │
    │       format = NSConstantArray_00121a78[uVar20]          │
    │       result = [dateFormatter stringFromDate:createTime]  │
    │       replace {token} with result in formatString         │
    │     }                                                   │
    │   }                                                     │
    │ }                                                       │
    └─────────────────────────────────────────────────────────┘
}
```

### 3.2 中文令牌的关键特征

中文令牌（如 `{伪已读}`）在格式引擎中：

| 特征 | 说明 |
|------|------|
| 不匹配 4 个特殊令牌 | `{伪已读}` ≠ `{EE}` ≠ `{EEEE}` ≠ `{a}` ≠ `{b}` |
| 走**非特殊令牌路径** | locale = `en_US_POSIX`，使用 NSDateFormatter 格式化 |
| 格式化来源 | `NSConstantArray_00121a78[i]` 中对应的 NSDateFormatter format 字符串 |
| 替换目标 | `m_uiCreateTime` → NSDate → NSDateFormatter → 时间文本 |

### 3.3 `{伪已读}` 的实际表现推测

由于中文令牌走的是 NSDateFormatter 的 `stringFromDate:` 路径，且输入日期固定为 `m_uiCreateTime`（消息创建时间），**推测** `{伪已读}` 的实际表现是：

```
输入格式: {伪已读}{HH}:{mm}:{ss}
NSConstantArray_00121a78 中的对应格式: 可能是空字符串 "", 或 "HH:mm:ss", 或自定义字符串

可能的输出:
  1. 如果格式是 "" → "14:30:00"（{伪已读}被替换为空）
  2. 如果格式是某特殊标记 → 可能触发另外的处理逻辑显示"已读"等文字
```

> **重要推断**：`{伪已读}` 这个名字的更准确含义可能是"伪造已读显示的**时间格式化方式**"——它可能用消息创建时间（而非真实已读时间）来格式化时间显示，从而**伪造出"已读"状态下的时间戳**。

---

## 四、在 WeChatPlugin 中如何识别中文令牌

WeChatPlugin 的 `MessageTimeFormatParser` **仅处理 4 个特殊令牌**（`{EE}` `{EEEE}` `{a}` `{b}`），完全不支持中文令牌。当用户输入 `{伪已读}` 时：

```
用户输入: "{伪已读}{HH}:{mm}:{ss}"

Step 1: 搜索 {EE} {EEEE} {a} {b} → 无匹配
Step 2: regex 匹配所有 \{(.+?)\} 模式
        → 匹配到 {伪已读} → inner = "伪已读"
        → 提取 inner 为 "伪已读HH:mm:ss"
        → NSDateFormatter setDateFormat:@"伪已读HH:mm:ss"
        → NSDateFormatter 不认识 "伪已读" → 输出: "伪已读14:30:00"

结果: 字面输出 "伪已读" 文字（中文被 NSDateFormatter 原样保留）
```

这意味着 WeChatPlugin 虽然无法正确"解析" `{伪已读}`，但中文会被 NSDateFormatter 当作字面文本直接输出——这是一个**巧合的正确行为**。

---

## 五、WeChatPlugin 实现伪已读功能的方案

### 5.1 架构设计

伪已读功能本质上是**让消息时间标签根据消息阅读状态显示不同文字**。建议方案：

```
MessageTimeFormatParser 增强:
  │
  ├── 新增中文令牌映射表
  │     ├── {伪已读} → 伪已读状态文字（如 "已读" 或 格式化的已读时间）
  │     ├── {未发送} → 消息未发送状态
  │     └── ...
  │
  └── 在 Step 1（特殊令牌预处理）中增加中文令牌检测
        ├── 识别 {伪已读}
        ├── 根据消息实际阅读状态动态生成文字
        └── 替换为占位符
```

### 5.2 核心实现步骤

**Step 1：在 `MessageTimeFormatParser` 中添加中文令牌集合**

```objc
static NSSet<NSString *> *_chineseTokens(void) {
    return [NSSet setWithArray:@[
        @"{伪已读}",
        // 可扩展更多中文令牌
    ]];
}
```

**Step 2：在预处理阶段为中文令牌生成动态文本**

```objc
// 在特殊令牌循环前新增
for (NSString *token in _chineseTokens()) {
    NSRange r = [nsdfFormat rangeOfString:token];
    if (r.location != NSNotFound) {
        NSString *replacement = nil;
        if ([token isEqualToString:@"{伪已读}"]) {
            // 根据消息状态动态生成
            replacement = [self pseudoReadTextForMessage:messageInfo];
        }
        if (replacement) {
            [nsdfFormat replaceOccurrencesOfString:token
                                        withString:replacement
                                           options:0
                                             range:NSMakeRange(0, nsdfFormat.length)];
        }
    }
}
```

**Step 3：需要传入消息上下文（阅读状态）**

当前 `formatDate:customFormat:isDarkMode:` 的签名需要扩展：

```objc
// 旧签名
+ (NSString *)formatDate:(NSDate *)date customFormat:(NSString *)fmt isDarkMode:(BOOL)isDarkMode;

// 新签名
+ (NSString *)formatDate:(NSDate *)date 
            customFormat:(NSString *)fmt 
              isDarkMode:(BOOL)isDarkMode
             messageInfo:(NSDictionary *)messageInfo;  // 含 readStatus, readTime 等
```

**Step 4：编辑器 UI 更新**

在 `MessageTimeFormatEditorVC` 的帮助表格中增加中文令牌行：

| 令牌 | 说明 |
|------|------|
| `{伪已读}` | 显示伪装的已读状态文字 |
| `{YYYY}` | 年份(2025) |
| ... | ... |

---

## 六、与微信优化 1.6.5 的对照总结

| 维度 | 微信优化 1.6.5 (B) | WeChatPlugin (A) | 状态 |
|------|:---:|:---:|---|
| 英文令牌 | 12 个 | 12 个 | 一致 |
| **中文令牌** | **≥ 3 个** | **0 个** | A 缺失 |
| 中文令牌处理方式 | NSDateFormatter 路径（非特殊） | 不支持 | — |
| {伪已读} 效果 | 替换为伪已读状态文字 | 中文当字面文本输出 | — |
| 格式编辑器 | CSTimeFormatEditorVC（仅英文令牌表格） | MessageTimeFormatEditorVC（仅英文令牌表格） | 一致 |
| NSUserDefaults 键 | 复用 `messageTime.customFormat` | 复用 `messageTimeCustomFormat` | 一致 |
| 能否扩展 | 新增中文令牌需改 dylib 数据段 | 源码可直接修改 | A 更易扩展 |

---

*本文档基于对 `123456.c` 数据段中文令牌常量、`FUN_0003bcb0` 格式引擎控制流、`NSConstantArray_00121a60` 令牌数组引用链的逐行分析生成。*