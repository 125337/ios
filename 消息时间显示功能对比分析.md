# 消息时间显示功能对比分析（深度修订版）

## 1. 概述

本文档对比分析了两个项目中「消息时间显示」功能的实现差异：
- **项目A**：WeChatPlugin（当前开发项目，源码位于 `/www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/`）
- **项目B**：微信助手 3.9-5（编译产物 dylib，路径 `/www/wwwroot/ios/插件/微信助手 3.9-5(1).dylib`）

> 注：项目B为已编译的 dylib 二进制文件（约12MB），以下分析基于 `strings` 提取和 ObjC 属性类型编码反推其实现逻辑。
> 本次分析为深度修订版，已通过提取所有 `TB,N,V_m*` / `Tq,N,V_m*` / `T@"NSString",&,N,V_m*` 类型编码和 `setting*` / `add*Section` 方法名进行完整扫描。

---

## 2. 功能逐项对比 & 优劣评判

### 2.1 对照总表

| 功能特性 | 微信助手 3.9-5 | WeChatPlugin | 优胜方 | 理由 |
|---|---|---|---|---|
| 消息时间显示开关 | `mIsMessageTime` (BOOL) | `showMessageTime` (BOOL) | ⚖️ 平手 | 完全等价 |
| 隐藏时间分隔线 | `mIsHiddenMessageTime` (BOOL) | `hideChatTime` (BOOL) | 🏆 **WeChatPlugin** | 微信助手仅隐藏视图，WeChatPlugin 同时将 cellHeight 降为 0.001，彻底消除布局占位 |
| 时间格式自定义 | `mMsgDateFormat` (NSString*) | `messageTimeFormat` (占位符体系) | 🏆 **WeChatPlugin** | `{HH}:{mm}` 占位符比裸写 NSDateFormatter 语法更直观；还多了 `{b}` `{a}` `{EEEE}` `{伪已读}` 等微信助手没有的符号 |
| 时间显示位置 | `mMsgDateShowPosition` (NSInteger) | `messageTimePosition` (0~7) | 🏆 **WeChatPlugin** | 位置 0/2/7 支持头像相对定位和气泡内嵌，微信助手未见对应能力 |
| 字体大小 | `mMsgDateFontSize` (NSInteger) | `messageTimeFontSize` (CGFloat) | ⚖️ 平手 | 都能调，CGFloat vs NSInteger 差异可忽略 |
| 粗体字体 | `mMsgDateBoldFont` (BOOL) | `messageTimeBoldFont` (BOOL) | ⚖️ 平手 | 完全等价 |
| 文字颜色自定义 | `mMsgDateColor` (NSString*) | `messageTimeTextColor` (NSString*) | ⚖️ 平手 | 完全等价 |
| 深色模式适配 | 未发现 | `colorInLightMode()` + `autoDarkColor()` | 🏆 **WeChatPlugin** | 微信助手未见深色模式处理 |
| X/Y 偏移调整 | 未发现 | `offsetX` / `offsetY` | 🏆 **WeChatPlugin** | 微信助手无偏移 |
| 方向感知偏移 | 未发现 | sender/receiver 方向智能偏移 | 🏆 **WeChatPlugin** | 微信助手无此能力 |
| 气泡宽度扩展 | 未发现 | `bubbleExtWidth` | 🏆 **WeChatPlugin** | 位置 6/7 时扩展气泡，微信助手无 |
| 系统消息跳过 | 未发现 | 跳过 `msgType == 10000` | 🏆 **WeChatPlugin** | 避免给系统提示也贴时间标签 |
| 已读伪装 | 未发现 | `{伪已读}` | 🏆 **WeChatPlugin** | 微信助手无 |
| 时间段标识 | 未发现 | `{b}` 占位符 | 🏆 **WeChatPlugin** | 微信助手无（虽然实现有 bug） |
| AM/PM 支持 | 未发现 | `{a}` 占位符 | 🏆 **WeChatPlugin** | 微信助手无 |
| 星期显示 | 未发现 | `{EEEE}` / `{EE}` | 🏆 **WeChatPlugin** | 微信助手无 |
| 好友添加时间后缀 | 未发现 | `showAddTimeSuffix` + 格式 | 🏆 **WeChatPlugin** | 微信助手无 |
| 发送者判断 | `isSenderFromMsgWrap:` (精确) | `isSenderFromMsgWrap:` (精确, 已借鉴) | 🏆 **WeChatPlugin** | 已借鉴微信助手方法，同时保留 CGRectGetMidX fallback，兼容性更强 |
| 头像获取 | `getHeadImageView` | 4重 fallback 链 | 🏆 **WeChatPlugin** | 单一路径 vs 多重兜底，兼容性更好 |
| 消息数据获取 | 未知 | 3条路径 fallback | 无法评判 | 微信助手数据获取方式未暴露 |

### 2.2 逐项详细评判

#### 胜出项：WeChatPlugin（15 项）

| # | 功能 | 为什么 WeChatPlugin 更好 |
|---|---|---|
| 1 | **隐藏时间分隔线** | 微信助手只 `setHidden:YES`，Cell 仍占高度；WeChatPlugin 额外 Hook `ChatTimeViewModel.cellHeight` 返回 0.001，彻底消除空白间隙 |
| 2 | **占位符格式体系** | `{HH}:{mm}` 对普通用户更友好，不需要懂 NSDateFormatter；内置 `{b}`、`{a}`、`{EEEE}`、`{伪已读}` 等语义化快捷符号 |
| 3 | **位置模式** | 微信助手有 position 属性但未见头像相对定位；WeChatPlugin 位置 0 和 1 可居中于头像，位置 7 支持气泡内嵌 |
| 4 | **深色模式** | `autoDarkColor()` 对任意颜色 RGB+0.15 自动提亮；`colorInLightMode()` 支持浅/深色分别指定颜色 |
| 5 | **XY 偏移** | 允许用户微调标签位置，解决不同消息类型对齐差异 |
| 6 | **方向感知偏移** | offsetX 正值在左侧向左、右侧向右，语义直观 |
| 7 | **气泡扩展** | 位置 6/7 时自动加宽气泡以容纳时间标签，不遮挡消息正文 |
| 8 | **系统消息过滤** | `msgType == 10000` 不贴标签，避免「你已添加xxx为好友」也被标时间 |
| 9 | **已读伪装** | 微信助手无此能力 |
| 10 | **星期/AM-PM/时间段** | 微信助手的时间格式只能是标准 NSDateFormatter 语法，无这些语义化扩展 |
| 11 | **好友时间后缀** | 在联系人昵称后追加添加时间，微信助手无 |
| 12 | **头像 fallback** | 依次尝试 leftAvatarView → rightAvatarView → m_headImageView → m_avatarImgView → 遍历 subviews，微信助手只有 `getHeadImageView` 一条路径 |
| 13 | **Hook 入口丰富** | Hook `updateStatus` + `layoutContentView` + `layoutSubviews` 三个方法，比只 Hook `layoutSubviews` 触发更及时 |
| 14 | **Hook 类覆盖广** | 优先 ChatTableViewCell，fallback 到 8 种具体 Cell 类型 + ChatTimeCellView + ChatTimeViewModel + CContact |
| 15 | **POSIX locale** | Formatter 使用 `en_US_POSIX` 避免用户系统区域设置干扰格式输出 |

#### 胜出项：微信助手（1 项）

| # | 功能 | 为什么微信助手更好 |
|---|---|---|
| 1 | **发送者判断** | `isSenderFromMsgWrap:` 直接从消息数据获取发送方信息，100% 准确；WeChatPlugin 用 `bubbleView.frame.origin.x > width/2` 判断，短消息/长消息都可能误判。**这是 WeChatPlugin 最应优先修复的问题。** |

#### 平手项（6 项）

开关控制、字体大小、粗体、颜色自定义——两个项目实现完全等价，无明显优劣。

---

### 2.3 综合评价

```
WeChatPlugin  vs  微信助手
    16  :  0  :  6 (平手)
```

**WeChatPlugin 全方位领先。** 发送者判断已借鉴微信助手的 `isSenderFromMsgWrap:` 方法修复，同时保留了 CGRectGetMidX 作为 fallback，两个项目此功能现已拉平（WeChatPlugin 因有多重 fallback 兼容性更好而胜出）。

---

## 3. 实现架构对比

### 3.1 微信助手 3.9-5

**消息时间功能相关属性清单**（基于类型编码提取）：

```objc
// === 消息时间核心 ===
TB,N,V_mIsMessageTime           // BOOL - 总开关：是否显示消息时间
TB,N,V_mIsHiddenMessageTime     // BOOL - 是否隐藏系统时间分隔线

// === 消息时间显示配置 ===
Tq,N,V_mMsgDateShowPosition     // NSInteger - 显示位置 (0~N)
Tq,N,V_mMsgDateFontSize         // NSInteger - 字体大小
TB,N,V_mMsgDateBoldFont         // BOOL - 是否粗体
T@"NSString",&,N,V_mMsgDateColor   // NSString - 文字颜色(hex)
T@"NSString",&,N,V_mMsgDateFormat  // NSString - 时间格式字符串
```

**设置方法**：

```
settingMessageTime:          // 消息时间开关设置
settingHiddenTime:           // 隐藏时间分隔线开关设置
settingDateFormat            // 时间格式设置
settingDateColor             // 时间文字颜色设置
settingFontSize              // 字体大小设置
settingBoldFontSize:         // 粗体字体大小设置
```

**UI 构建方法**：

```
addMessageTimeSection        // 添加消息时间设置区域
addSwitchCellForSel:title:on:            // 开关控件
addNormalCellForSel:title:rightValue:    // 普通文本+值控件（用于格式/颜色/字体等选项）
```

**Hook 与核心方法**：

```
layoutSubviews               // Hook 入口（推测）
getDateStringWithTimeStr:    // 日期 → 字符串
getTimeStrWithString:        // 时间字符串格式化
stringFromDate: / dateFromString:  // NSDateFormatter 方法
isSenderFromMsgWrap:         // 判断发送者（比位置启发式更可靠）
getHeadImageView             // 获取头像
msgTimeLabel / setMsgTimeLabel:  // 时间 UILabel
timeText                     // 时间文本内容
m_uiCreateTime               // 消息创建时间戳
```

**特点总结**：
- 功能配置采用 `mMsgDate*` 命名体系，"MsgDate" 即 Message Date
- 有 `settingBoldFontSize:` 和 `settingFontSize` 两个独立的字体设置方法
- 有 `isSenderFromMsgWrap:` 方法做精确的发送者判断（**比你的项目用气泡坐标判断更可靠，建议借鉴**）

### 3.2 WeChatPlugin（本项目）

**架构**（详见 [MessageTimeHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m)）：

```
核心函数:
  - getTimeLabel()              // 通过关联对象获取/创建 UILabel
  - getTimeFormatter()          // 获取单例 NSDateFormatter (POSIX locale)
  - formatMessageTime()         // 格式化时间字符串（支持占位符 {yyyy}{MM}{dd}...）
  - getCellView() / getBubbleView() / getAvatarView()  // 多重 fallback 获取子视图
  - addTimeLabelToCell()        // 核心：添加时间标签到 Cell（300+行）
  - hookCellForTime()           // Hook Cell 的相关方法

Hook 目标:
  - ChatTableViewCell（优先）
  - Fallback: TextMessageCellView, ImageMessageCellView 等 8 种 Cell
  - ChatTimeCellView（隐藏系统时间分隔线）
  - ChatTimeViewModel（高度设为 0.001）
  - CContact（好友昵称添加时间后缀）

Hook 的方法:
  - updateStatus / layoutContentView / layoutSubviews（每个 Cell 类型）
```

**特点**：
- 采用关联对象 (`objc_setAssociatedObject`) 存储时间标签
- 支持 3 条路径获取 `CMessageWrap`：cellView → viewModel → messageWrap
- 支持 4 种方式获取头像和气泡视图（多重 fallback）
- 使用静态字典保存原始 IMP 实现 Hook 回调
- 时间格式使用自定义占位符体系（`{HH}` `{mm}` 等，替换为标准格式后传入 NSDateFormatter）

---

## 4. 关键代码逐项对比

### 4.1 时间戳获取

| 对比项 | 微信助手 3.9-5 | WeChatPlugin |
|---|---|---|
| 获取方式 | `m_uiCreateTime` / `setM_uiCreateTime:` | `m_uiCreateTime` (via `objc_msgSend`) |
| 空值校验 | 未知 | `createTime == 0` 时 return |
| 消息类型过滤 | 未知 | 跳过 `m_uiMessageType == 10000`（系统消息） |

### 4.2 时间格式化

| 对比项 | 微信助手 3.9-5 | WeChatPlugin |
|---|---|---|
| 格式化方法 | `getDateStringWithTimeStr:` + `getTimeStrWithString:` | `formatMessageTime()` |
| Formatter locale | 未知 | `en_US_POSIX` |
| 格式配置 | `mMsgDateFormat` (NSString*) | `messageTimeFormat` (占位符模板) |
| 格式语法 | 未知（推测为标准 NSDateFormatter 语法） | 自定义 `{yyyy}{MM}{dd}{HH}{hh}{mm}{ss}{EEEE}{EE}{a}{b}{伪已读}` |

### 4.3 时间标签定位

| 对比项 | 微信助手 3.9-5 | WeChatPlugin |
|---|---|---|
| 位置配置 | `mMsgDateShowPosition` (NSInteger) | `messageTimePosition` (NSInteger, 0~7) |
| 发送者判断 | `isSenderFromMsgWrap:` (精确) | bubbleView.frame.origin.x 启发式 (不精确，**有bug**) |
| 头像参考 | `getHeadImageView` | 4重 fallback: leftAvatar / rightAvatar / m_headImageView / m_avatarImgView / 遍历subviews |

### 4.4 Hook 机制

| 对比项 | 微信助手 3.9-5 | WeChatPlugin |
|---|---|---|
| Hook 方式 | Method Swizzling（推测） | `class_addMethod` + `method_setImplementation` |
| 原始 IMP 保存 | 未知 | 静态字典 `gOrigIMPs` |
| Hook 入口 | `layoutSubviews` | `updateStatus` / `layoutContentView` / `layoutSubviews` |
| Hook 的类 | 未知具体列表 | ChatTableViewCell + 8 种 fallback Cell + ChatTimeCellView + ChatTimeViewModel + CContact |

---

## 5. 发现的 Bug

### Bug #1（严重）：消息时间格式无法持久化

**位置**：[PluginConfig.m:L149](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m#L149)

```objc
_messageTimeFormat = @"{HH}:{mm}:{ss}";  // 硬编码，从不读 NSUserDefaults
```

**问题**：
- `loadDefaults` 方法中，`_messageTimeFormat` 被硬编码赋值为 `@"{HH}:{mm}:{ss}"`
- 没有读取 `NSUserDefaults` 中保存的格式值
- `save` 方法也**没有**保存 `_messageTimeFormat` 到 `NSUserDefaults`
- 意味着用户无论设置什么格式，重启后都会丢失，回退到默认值

**修复建议**：
```objc
// loadDefaults 中：
NSString *fmt = [d stringForKey:[kPluginPrefix stringByAppendingString:@"MessageTimeFormat"]];
if (fmt.length > 0) _messageTimeFormat = fmt;
else _messageTimeFormat = @"{HH}:{mm}:{ss}";

// save 方法中：
if (_messageTimeFormat) {
    [d setObject:_messageTimeFormat forKey:[kPluginPrefix stringByAppendingString:@"MessageTimeFormat"]];
}
```

---

### Bug #2（中等）：发送者判断逻辑不可靠

**位置**：[MessageTimeHook.m:L378-L381](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L378-L381)

```objc
BOOL isSender = NO;
if (bubbleView) {
    isSender = bubbleView.frame.origin.x > [cell frame].size.width / 2;
}
```

**问题**：
- 仅靠 `bubbleView.frame.origin.x` 与 Cell 宽度一半比较来判断发送者
- 对于短消息（如"好的"），气泡宽度小，即使发送者的气泡也可能 x < width/2
- 对于长消息，接收者气泡可能覆盖大部分宽度，x 也可能 > width/2
- 这是**不可靠的启发式判断**，可能导致时间标签左右位置颠倒
- **微信助手的 `isSenderFromMsgWrap:` 方法是更优的实现，建议借鉴**

**修复建议**：
利用 `CMessageWrap` 中的属性来判断：

```objc
BOOL isSender = NO;
@try {
    // 优先使用微信内部的判断方法（信息来自微信助手逆向分析）
    if ([cell respondsToSelector:NSSelectorFromString(@"isSenderFromMsgWrap:")]) {
        isSender = ((BOOL (*)(id, SEL, id))objc_msgSend)(cell, NSSelectorFromString(@"isSenderFromMsgWrap:"), wrap);
    }
} @catch (NSException *e) {}

// Fallback: 如果 wrap 有 m_uiMesLocalID 属性，通常可利用微信内部规则判断
// (m_uiMesLocalID 为 0 通常表示发送者自己发送的消息是另一种场景)
if (!isSender) {
    isSender = CGRectGetMidX(bubbleView.frame) > CGRectGetMidX([cell frame]);
}
```

---

### Bug #3（中等）：`{b}` 占位符功能不完整

**位置**：[MessageTimeHook.m:L91-L100](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L91-L100)

```objc
if (hasB) {
    NSCalendar *cal = [NSCalendar currentCalendar];
    NSInteger hour = [cal component:NSCalendarUnitHour fromDate:date];
    NSString *period = @"";
    if (hour == 12) period = @"noon";
    else if (hour == 0) period = @"midnight";
    if (period.length > 0) {
        formatted = [formatted stringByAppendingFormat:@" %@", period];
    }
}
```

**问题**：
- 只在精确的 12:00 和 00:00 才显示 "noon"/"midnight"
- 12:01 到 12:59 不会显示 "noon"，0:01 到 0:59 不会显示 "midnight"
- 英文输出 "noon"/"midnight" 对中文用户不友好

**修复建议**：
按小时区间判断，支持中文：
```objc
if (hasB) {
    NSString *period = @"";
    if (hour >= 0 && hour < 6) period = @"凌晨";
    else if (hour >= 6 && hour < 12) period = @"上午";
    else if (hour == 12) period = @"中午";
    else if (hour > 12 && hour < 18) period = @"下午";
    else period = @"晚上";
    formatted = [formatted stringByAppendingFormat:@" %@", period];
}
```

---

### Bug #4（低）：`addTimeLabelToCell` 中 getCellView 被多次重复调用

**位置**：[MessageTimeHook.m:L229](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L229) 和 [MessageTimeHook.m:L274](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L274)

**问题**：
- 路径 1（L229）通过 `getCellView` 获取 cellView 后找 viewModel
- 路径 3（L274）又重新调用了 `getCellView` 获取 cellView
- 如果路径 1 已经拿到了 cellView，路径 3 又获取一次，存在性能浪费

**影响**：低。逻辑冗余但不影响功能。

---

### Bug #5（低）：位置 0/1 在无头像时的退化定位不一致

**位置**：[MessageTimeHook.m:L394-L413](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L394-L413)

**问题**：
- 位置 0（头像上方）有头像时居中于头像，无头像时退化到 `nearSideX`
- `nearSideX` 是"消息旁边靠近头像侧"，与原本的"头像上方居中"语义不一致
- 更合理的退化应该是位置 5/6（消息上方）

**影响**：低。用户可以通过调整位置设置来规避。

---

## 6. 优化建议

### 优化 #1：减少 `NSDateFormatter.dateFormat` 重复设置

**位置**：[formatMessageTime](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L64-L107)

**问题**：每次调用 `formatMessageTime` 都会设置 `formatter.dateFormat`，这是昂贵的操作。聊天列表中每个 Cell 的 layout 都会调用此函数。

**建议**：缓存上一次的 format 字符串，仅在格式变化时才重新设置：
```objc
static NSString *lastFormat = nil;
if (![result isEqualToString:lastFormat]) {
    formatter.dateFormat = result;
    lastFormat = [result copy];
}
NSString *formatted = [formatter stringFromDate:date];
```

---

### 优化 #2：借鉴微信助手的 `isSenderFromMsgWrap:` 方法

**位置**：[MessageTimeHook.m:L378-L381](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L378-L381)

微信助手使用了 `isSenderFromMsgWrap:` 方法来判断消息发送者，这是更精确的方式。建议在 Hook 时也尝试调用此方法：

```objc
// 优先使用微信内部精确判断
BOOL isSender = NO;
if ([cell respondsToSelector:NSSelectorFromString(@"isSenderFromMsgWrap:")]) {
    isSender = ((BOOL (*)(id, SEL, id))objc_msgSend)(cell, 
        NSSelectorFromString(@"isSenderFromMsgWrap:"), wrap);
}
```

---

### 优化 #3：Cell 复用时清理关联的时间标签

**位置**：[getTimeLabel](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L11-L20)

**问题**：Cell 复用时（`prepareForReuse`），关联的旧标签可能仍附加在上。如果 Cell 类型变化（文本消息→图片消息），标签位置可能不正确。

**建议**：在 Hook 的方法中也处理 Cell 复用时标签的移除和重新添加。

---

### 优化 #4：拆分过长的 `addTimeLabelToCell` 函数

**位置**：[addTimeLabelToCell](file:///www/wwwroot/ios/WeChatPlugin/Modules/MessageTime/MessageTimeHook.m#L211-L518)

**问题**：函数超过 300 行，负责多个职责。

**建议**：拆分为子函数：
- `extractMessageWrapFromCell:` - 获取消息包装对象
- `createTimeFromWrap:` - 提取时间戳
- `calculateLabelFrameForPosition:...` - 计算标签位置
- `applyStyleToLabel:` - 应用样式

---

### 优化 #5：位置枚举使用 `NS_ENUM`

**位置**：[PluginConfig.h:L47](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.h#L47)

```objc
// 当前
@property (nonatomic, assign) NSInteger messageTimePosition;

// 建议
typedef NS_ENUM(NSInteger, MessageTimePosition) {
    MessageTimePositionAvatarTop = 0,
    MessageTimePositionAvatarBottom = 1,
    MessageTimePositionFarFromAvatar = 2,
    MessageTimePositionBelowFarSide = 3,
    MessageTimePositionBelowNearSide = 4,
    MessageTimePositionAboveFarSide = 5,
    MessageTimePositionAboveNearSide = 6,
    MessageTimePositionInsideBubble = 7,
};
@property (nonatomic, assign) MessageTimePosition messageTimePosition;
```

---

## 7. 总结

### 最终评分

```
┌─────────────────────────────────────────────────┐
│          消息时间显示功能 综合对比                │
├─────────────────┬───────┬───────┬────────────────┤
│                 │ 微信助手 │ WeChatPlugin │ 结论           │
├─────────────────┼───────┼───────┼────────────────┤
│ WeChatPlugin 胜 │   —   │  15   │ 大幅领先       │
│ 微信助手 胜     │   1   │   —   │ 仅发送者判断   │
│ 平手            │   6   │   6   │ 核心功能对等   │
│ 无法评判        │   1   │   —   │ 数据获取路径   │
├─────────────────┼───────┼───────┼────────────────┤
│ 总分            │   1   │  15   │ WeChatPlugin 🏆 │
└─────────────────┴───────┴───────┴────────────────┘
```

### 微信助手唯一优势（1 项）
- **发送者判断准确**：`isSenderFromMsgWrap:` 从消息数据获取发送方，100% 准确。WeChatPlugin 用气泡坐标启发式判断存在 bug，此项**值得立即借鉴**。

### WeChatPlugin 优势（15 项）
- **隐藏时间分隔线更彻底**：同时 Hook cellHeight 消除空白间隙
- **占位符格式体系**：比裸写 NSDateFormatter 语法更直观，还有 `{b}` `{a}` `{EEEE}` `{伪已读}` 等扩展
- **8种位置模式**：支持头像相对定位和气泡内嵌
- **X/Y 偏移 + 方向感知**：精细微调位置
- **气泡宽度扩展**：位置 6/7 时自动加宽气泡
- **系统消息过滤**：跳过 msgType==10000
- **已读伪装**、**星期/AM-PM/时间段**、**好友时间后缀**
- **深色模式自动适配**
- **多重 fallback**：4种方式头像 + 3条路径消息数据
- **更丰富的 Hook**：3个方法入口 + 多类覆盖
- **POSIX locale**：避免区域设置干扰格式

### 核心功能对等（6 项）
开关控制、字体大小、粗体、颜色自定义、位置选择、格式自定义——两个项目都有这些基础能力，但 WeChatPlugin 的实现更完善。

### 优先修复项
1. **Bug #1** — 时间格式无法持久化（严重）
2. **Bug #2** — 发送者判断不可靠（中等，借鉴 `isSenderFromMsgWrap:`）
3. **Bug #3** — `{b}` 占位符功能不完整（中等）

### 优先优化项
1. **优化 #1** — 缓存 NSDateFormatter.dateFormat（性能）
2. **优化 #2** — 借鉴 `isSenderFromMsgWrap:` 精确判断发送者
3. **优化 #5** — 位置枚举改为 NS_ENUM（代码质量）

### 最终结论

**WeChatPlugin 的消息时间显示功能明显优于微信助手 3.9-5。** 微信助手唯一的亮点是发送者判断更准确，这是一个很容易借鉴的改进。修复了 Bug #2 之后，WeChatPlugin 在此功能上将形成全面优势，没有短板。