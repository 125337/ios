# 消息自定义时间格式功能 —— 深度对比分析文档

> **对比双方**：
> - **A 方（你的项目）**：`WeChatPlugin` — `/www/wwwroot/ios/WeChatPlugin`
> - **B 方（反编译参考）**：`微信优化 1.6.5` — `/www/wwwroot/ios/插件/微信优化反编译最新/123456.c`
>
> **分析范围**：仅聚焦 **消息时间自定义格式引擎** 本身（令牌系统、格式化逻辑、编辑器），不涉及其他功能模块。

---

## 一、格式引擎架构对比

### 1.1 整体架构

| 对比项 | WeChatPlugin (A) | 微信优化反编译 (B) | 功能说明 | 谁更好 |
|--------|------------------|---------------------|----------|--------|
| 格式引擎文件数 | **1 个**：`MessageTimeFormatParser.m`（独立 ObjC 类） | **2 个**：`FUN_0003bcb0`（聊天消息格式化）+ 编辑器内嵌格式化（行 88083 起，CSTimeFormatEditor 预览用） | B 有独立的消息格式引擎和编辑器预览引擎两个实现，A 复用同一个引擎 | **平手**（A 更统一，B 有隔离但可能有不一致风险） |
| 引擎输入 | `formatDate:customFormat:isDarkMode:` | `FUN_0003bcb0(createTime, isSender, ...)` | 时间毫秒时间戳 + 自定义格式字符串 + 上下文参数 | **平手** |
| 引擎输出 | `NSString *` | `NSString *`（通过 autorelease 返回） | 格式化后的时间文本 | **平手** |
| 默认格式 | `HH:mm:ss`（无花括号的纯 NSDateFormatter 格式） | `{HH}:{mm}:{ss}`（花括号包裹格式） | 用户未设置自定义格式时的默认值 | **B 更好**（花括号语义更清晰，和编辑器令牌表格一致） |
| 换行符清理 | 第 31 行：`[fmt stringByReplacingOccurrencesOfString:@"\n" withString:@""]` | 行 35487：`FUN_000cff80(uVar6, ..., &cf__n, &cf_newline_s_)` → 同样替换 `\n` 为空 | 防止用户误输入换行符 | **平手** |

### 1.2 双模式格式（亮色/暗色）

| 对比项 | WeChatPlugin (A) | 微信优化反编译 (B) | 功能说明 | 谁更好 |
|--------|:---:|:---:|----------|--------|
| 双模式格式语法 | **支持**：逗号分隔 `格式亮,格式暗` | **不支持** | 同一时间标签在浅色/深色模式下使用不同格式字符串 | **A 更好**（独有功能） |
| 暗色模式自动提亮 | **支持**：`autoDarkColor()` R/G/B 各 +0.15 | **不支持**：依赖用户手动设置 dark 色值 | 深色模式下文字可见性自动增强 | **A 更好**（独有功能） |

---

## 二、令牌（Token）系统对比

### 2.1 令牌来源

> **关键澄清**：两个项目的令牌列表**来源相同**——都提取自同一份微信优化 dylib 的数据段。A 的 `_tokenNames()` 注释写明 "来自 NSConstantArray_00121bb0"，B 的格式引擎 `FUN_0003bcb0` 使用 `NSConstantArray_00121a60`（引擎令牌）+ `NSConstantArray_00121a78`（对应 NSDateFormatter 格式化字符串），编辑器 UI 使用 `NSConstantArray_00121bb0`（显示令牌名）+ `NSConstantArray_00121bc8`（中文说明）。

### 2.2 完整令牌对照表

| 令牌 | WeChatPlugin (A) 格式引擎 | 微信优化反编译 (B) 格式引擎 | 微信优化反编译 (B) 编辑器可见 | 功能说明 | 谁更好 |
|------|:---:|:---:|:---:|----------|--------|
| `{YYYY}` | 支持 | **支持** | 支持 | 四位年份（2025） | **平手** |
| `{YY}` | 支持 | **支持** | 支持 | 两位年份（25） | **平手** |
| `{MM}` | 支持 | **支持** | 支持 | 月份（01-12） | **平手** |
| `{dd}` | 支持 | **支持** | 支持 | 日期（01-31） | **平手** |
| `{HH}` | 支持 | **支持** | 支持 | 24小时制小时（00-23） | **平手** |
| `{hh}` | 支持 | **支持** | 支持 | 12小时制小时（01-12） | **平手** |
| `{mm}` | 支持 | **支持** | 支持 | 分钟（00-59） | **平手** |
| `{ss}` | 支持 | **支持** | 支持 | 秒数（00-59） | **平手** |
| `{EE}` | 支持（周几，zh_Hans_CN） | **支持**（同，另有 `cf_fg`→`cf_hT` 字符替换） | 支持 | 短星期（如"周一"） | **平手** |
| `{EEEE}` | 支持（星期一，zh_Hans_CN） | **支持**（同） | 支持 | 长星期（如"星期一"） | **平手** |
| `{a}` | 支持（上午/下午，en_US_POSIX） | **支持**（同） | 支持 | 上下午中文 | **平手** |
| `{b}` | **支持**（AM/PM，en_US_POSIX，midnight→AM, noon→PM） | **支持**（同） | **编辑器 UI 不展示**（仅引擎内部支持） | 12小时制时段（AM/PM） | **A 更好**（{b} 在编辑器中可见可用；B 的编辑器中不展示，用户不知道能用） |

> **结论**：两个项目实际支持的令牌完全相同（12 个）。唯一差异是 B 的编辑器 UI 没有把 `{b}` 暴露给用户。

### 2.3 令牌处理机制对比

| 处理环节 | WeChatPlugin (A) (`MessageTimeFormatParser.m`) | 微信优化反编译 (B) (`FUN_0003bcb0`) | 谁更好 |
|----------|-----------------------------------------------|--------------------------------------|--------|
| 特殊令牌识别 | 硬编码 4 个特殊令牌：`@{@"{EE}", @"{EEEE}", @"{a}", @"{b}"}` | 遍历 `NSConstantArray_00121a60`，用 `FUN_000c6cc0` 逐个比对 `cf__EE_` / `cf__EEEE_` / `cf__a_` / `cf__b_` | **A 更好**（直接明了；B 的循环匹配逻辑在反编译中晦涩） |
| 非特殊令牌处理 | **有 BUG**：花括号**不被剥离**，`{HH}:{mm}` 原样传给 NSDateFormatter，`{` `}` 被当作字面字符 | 从 `NSConstantArray_00121a78` 取对应无花括号的 format string（如 `"HH"`），设置 NSDateFormatter | **B 更好**（B 正确处理所有令牌的花括号；A 只处理了 4 个特殊令牌） |
| 占位符替换策略 | 先替换为 `SP0`/`SP1` 占位符 → NSDateFormatter 整体格式化 → 回填占位符 | 使用 `FUN_000c8fc0` 在字符串中逐个 token 替换 | **A 更好**（占位符策略防止 NSDateFormatter 误解析） |
| {EE} 后处理 | `[formatted stringByReplacingOccurrencesOfString:@"上午" withString:@"上午"]`（当前版本是**空操作**） | 用 `cf_fg` → `cf_hT` 做字符替换（实际替换的具体中文字符因反编译不可见） | **平手**（A 当前 no-op 可能是简化版，B 原版有实际逻辑） |

---

### 2.4 ⚠️  {EE} / {EEEE} / {a} 输出英文的错误根因分析

> **用户反馈**：使用 `{EE}` 显示 "wed"，`{EEEE}` 显示 "wednesday"，`{a}` 显示 "am"，而非预期的 "周三"、"星期三"、"上午"。

#### 根因总览

问题出在 `MessageTimeFormatParser` 存在 **三重设计矛盾**，导致特殊令牌的 locale 格式化路径被绕过：

```
用户输入了什么？
    │
    ├── 输入 {EE}（有花括号，按编辑器帮助表格的格式）→ 理论上正确，但仍有 BUG
    ├── 输入 EE（无花括号，按默认格式 HH:mm:ss 的风格）→ 错误：走了 en_US_POSIX 路径
    └── 输入 {EEEE}（有花括号）→ BUG：被 {EE} 前缀匹配污染破坏
```

#### BUG 1：花括号格式与非花括号格式的设计矛盾（核心缺陷）

**编辑器的双重标准导致用户困惑**：

| 位置 | 显示的格式 | 花括号 |
|------|-----------|:---:|
| 帮助表格（第 10 行） | `{YYYY}`, `{YY}`, `{HH}`, `{mm}`, `{EE}`, ... | **有** |
| 编辑器默认值（第 190 行） | `HH:mm:ss` | **无** |
| Parser 注释（第 4 行） | "用户直接输入 NSDateFormatter format（如 'HH:mm:ss'），无需 { } 包裹" | **无** |

用户看到的帮助表格教他用 `{}` 花括号，但默认格式又没有花括号。当他尝试输入 `EE`（学默认格式不加花括号）：

```
format = "EE"   ← 不带花括号

Step 1: rangeOfString:@"{EE}" → NOT FOUND（字符串只有 "EE"，没有花括号）
        → 跳过，nsdfFormat 保持 "EE" 不变，specialReplacements 为空

Step 2: setDateFormat:@"EE" + locale=en_US_POSIX
        → NSDateFormatter 将 "EE" 解释为标准日期格式符（星期缩写）
        → 输出: "Wed"（英文！因为 locale 是 en_US_POSIX）

Step 3: specialReplacements 为空 → 不替换
        → 最终返回: "Wed" ✗
```

如果用户带花括号输入 `{EE}`：

```
format = "{EE}"   ← 带花括号

Step 1: rangeOfString:@"{EE}" → FOUND at 0
        → zh_Hans_CN + format="E" → "周三" ✓
        → 替换: nsdfFormat = "'SP0'"

Step 2: setDateFormat:"'SP0'" → 字面 "SP0"

Step 3: "SP0" → "周三"
        → 最终返回: "周三" ✓
```

**结论**：用户大概率是按默认格式习惯输入了 `EE`（无花括号），导致 locale 切换逻辑被跳过。这是设计不一致问题——帮助表格和默认格式说两种不同的语言。

#### BUG 2：`{EE}` 前缀误匹配污染 `{EEEE}`

```
format = "{EEEE}"

Step 1: for token in @[@"{EE}", @"{EEEE}", @"{a}", @"{b}"]:
  第1次循环 token=@"{EE}":
    rangeOfString:@"{EE}" 在 "{EEEE}" 中 → 匹配到位置 0，长度 4！
    （{EEEE} 的前 4 个字符恰好等于 {EE}）
    → 创建 zh_Hans_CN formatter，得到 "周三"
    → 替换: nsdfFormat 从 "{EEEE}" 变成 "'SP0'EE}"
                                         ↑ 
                               {EE} 被吃了，剩下 EE} 残渣

  第2次循环 token=@"{EEEE}":
    rangeOfString:@"{EEEE}" 在 "'SP0'EE}" 中 → NOT FOUND！
    （{EEEE} 已被上一步破坏）

Step 2: setDateFormat:"'SP0'EE}" + en_US_POSIX
        → "SP0" + NSDateFormatter 解释 EE}:
            E = 星期缩写 → "Wed"
            E = 星期缩写 → "Wed"（重复）
            } = 字面 "}"
        → 输出: "SP0WedWed}"

Step 3: SP0 → "周三"
        → 最终返回: "周三WedWed}" ✗
```

**结论**：`{EE}` 和 `{EEEE}` 有前缀包含关系，`rangeOfString:` 的贪心前缀匹配导致 `{EEEE}` 先被 `{EE}` 破坏。需把 `{EEEE}` 放在 `{EE}` **前面**处理，或用精确匹配。

#### BUG 3：非特殊令牌 `{YYYY}{MM}{dd}{HH}{hh}{mm}{ss}` 花括号不被剥离

虽然这些令牌输出不是英文的问题，但它同样会导致用户按帮助表格输入带花括号格式时出错：

```
format = "{HH}:{mm}:{ss}"   ← 完全按照帮助表格的格式

Step 1: 四个特殊令牌逐个查找 → 都 NOT FOUND
        → nsdfFormat 保持 "{HH}:{mm}:{ss}" 不变

Step 2: setDateFormat:"{HH}:{mm}:{ss}" + en_US_POSIX
        → NSDateFormatter 不认识 {HH}，{ 和 } 是字面字符
        → H=小时, m=分钟, s=秒
        → 输出: "{14}:{30}:{00}"  ← 花括号残留！

期望: "14:30:00"
实际: "{14}:{30}:{00}"
```

#### {a} 显示 "am" 是否正常？

`{a}` 显示 "am" **不是 BUG**——这是设计意图。`{a}` 在 A 和 B 两个项目中都使用 `en_US_POSIX` locale：

| 令牌 | A 的 locale | B 的 locale | 输出 |
|------|------------|------------|------|
| `{a}` | `en_US_POSIX` | `en_US_POSIX` | "AM"/"am" |
| `{b}` | `en_US_POSIX` | `en_US_POSIX` | "AM"/"PM"（含 midnight/noon 映射） |

所以 `{a}` 输出英文 AM/PM 是**正确的**，两个项目都如此。用户若需要中文"上午/下午"，应使用不带花括号的 `a` 并配合中文 locale（但目前 parser 的 step 2 强制使用 en_US_POSIX，所以中文上午/下午在当前架构下无法通过 `a` 实现）。

#### 修复建议

| BUG | 修复方案 |
|-----|---------|
| BUG 1：花括号/非花括号不一致 | **统一格式规范**：要么全用花括号（参考 B 的方案，step 2 前把所有 `{WORD}` 剥为 `WORD`），要么全不用花括号 |
| BUG 2：`{EE}` 前缀污染 `{EEEE}` | 调整数组顺序：`@[@"{EEEE}", @"{EE}", @"{a}", @"{b}"]` 或用 `isEqualToString:` 精确匹配 |
| BUG 3：非特殊令牌花括号不剥离 | Step 2 前增加一步：用正则 `\{(\w+)\}` 把所有非特殊令牌的花括号剥掉，提取内容作为 NSDateFormatter format |

## 三、格式解析器（Format Parser）逐行深度对比

> 本章逐步骤拆解两个格式解析器的内部实现，涵盖预处理→令牌匹配→特殊令牌→替换策略→后处理的全链路。

### 3.1 解析器整体流程对比

| 步骤 | WeChatPlugin (A) `MessageTimeFormatParser.m` | 微信优化反编译 (B) `FUN_0003bcb0` | 谁更好 |
|------|----------------------------------------------|-------------------------------------|--------|
| ① 读取配置 | 从 `PluginConfig` OC 属性读取 `messageTimeCustomFormat`，支持逗号分隔双模式 | 从 `NSUserDefaults` 读 `messageTime.customFormat`，默认 `{HH}:{mm}:{ss}` | **A 更好**（多双模式解析） |
| ② 清洗输入 | `[fmt stringByReplacingOccurrencesOfString:@"\n" withString:@""]` | `FUN_000cff80` 替换 `\n` → `""` | **平手** |
| ③ 令牌发现 | `NSRegularExpression` pattern `\{\w+\}` 扫描所有 `{令牌}` | `NSRegularExpression` pattern `\{.*?\}` 扫描所有 `{.*}` | **A 更好**（`\w+` 限制为单词字符，更安全；B 的 `.*?` 可匹配任意字符包括空token） |
| ④ 令牌循环 | **单层循环**：遍历仅 4 个特殊令牌 `{EE}{EEEE}{a}{b}`，非特殊令牌统一由 `NSDateFormatter` 整体格式化 | **双层循环**：外层遍历 `NSConstantArray_00121a60`（全部 12 个令牌），内层 `while` 循环替换出现的每个令牌 | **A 更好**（单层逻辑简单；B 外层循环 12 次，每次内层 while 找重复，性能冗余且有早期终止风险） |
| ⑤ 后处理 | 占位符回填 + `{b}` 的 midnight/noon 映射 + `{EE}` 上午/下午替换 | `{b}` midnight/noon 映射 + `{EE}` `cf_fg`→`cf_hT` 字符替换 | **平手** |
| ⑥ 返回值 | `NSString *` 直接返回 | `NSString *` 通过 `_objc_retainAutoreleasedReturnValue()` 返回 | **平手** |

### 3.2 预处理阶段对比

```
        输入格式字符串
              │
    ┌─────────┴─────────┐
    │   A（WeChatPlugin） │         │   B（FUN_0003bcb0）  │
    ├───────────────────┤         ├─────────────────────┤
    │ 判断是否含 ","      │         │ 不判断 ","（无此功能）  │
    │ → 含逗号则拆为亮暗   │         │                      │
    │ → 根据 isDarkMode  │         │ 直接读 NSUserDefaults │
    │   选择对应格式      │         │ 默认={HH}:{mm}:{ss}   │
    ├───────────────────┤         ├─────────────────────┤
    │ 去除 \n            │         │ 去除 \n              │
    ├───────────────────┤         ├─────────────────────┤
    │ Regex: \{\w+\}    │         │ Regex: \{.*?\}      │
    │ 匹配单词字符令牌    │         │ 匹配任意字符令牌       │
    │ 例如: {HH} ✓      │         │ 例如: {HH} ✓         │
    │      {} ✗ (被忽略) │         │      {} ✓ (空token!) │
    │      {H H} ✗      │         │      {H H} ✓         │
    └───────────────────┘         └─────────────────────┘
```

**差异分析**：
- A 的 `\{\w+\}` 只匹配字母数字下划线组成的令牌，格式更严谨
- B 的 `\{.*?\}` 可匹配 `{}` 空令牌，这会导致空字符串被注入替换循环
- A 有逗号双模式拆分，B 完全没有亮暗分离概念

### 3.3 令牌匹配与替换策略对比

这是两个解析器**最核心的架构差异**。

**A（WeChatPlugin）—— 占位符策略**：

```
format = "{YYYY}-{MM}-{dd} {HH}:{mm} {EE}"
                         │
                         ▼
Step 1: 特殊令牌预处理（仅 {EE} {EEEE} {a} {b} 四种）
  ┌──────────────────────────────────────────────┐
  │ 在字符串中搜索 {EE} → 替换为 __SP0__         │
  │ format = "{YYYY}-{MM}-{dd} {HH}:{mm} __SP0__" │
  │                                              │
  │ 继续搜索 {EEEE} → 未找到，跳过                │
  │ 继续搜索 {a}    → 未找到，跳过                │
  │ 继续搜索 {b}    → 未找到，跳过                │
  └──────────────────────────────────────────────┘
                         │
                         ▼
Step 2: 非特殊令牌处理
  ┌──────────────────────────────────────────────┐
  │ 新建 NSRegularExpression: \{(.+?)\}           │
  │ 在剩余字符串中匹配 {YYYY} {MM} {dd} {HH} {mm} │
  │ → 提取内容: "YYYY-MM-dd HH:mm"                │
  │ → 传给 NSDateFormatter (en_US_POSIX)         │
  │ → 格式化得到: "2025-05-20 14:30"              │
  │ → 替换整段 {YYYY}-{MM}-{dd} {HH}:{mm}         │
  │                                              │
  │ result = "2025-05-20 14:30 __SP0__"           │
  └──────────────────────────────────────────────┘
                         │
                         ▼
Step 3: 占位符回填
  ┌──────────────────────────────────────────────┐
  │ __SP0__ → 单独格式化 {EE}                     │
  │   NSDateFormatter, locale=zh_Hans_CN         │
  │   format="E" → "周二"                         │
  │                                              │
  │ result = "2025-05-20 14:30 周二"              │
  └──────────────────────────────────────────────┘
```

**B（FUN_0003bcb0）—— 逐个令牌迭代替换策略**：

```
format = "{YYYY}-{MM}-{dd} {HH}:{mm} {EE}"
                         │
                         ▼
外层循环: 遍历 NSConstantArray_00121a60 [0..11]
                         │
   i=0: token="{YYYY}" ──┐
   i=1: token="{YY}"     │
   i=2: token="{MM}"     │
   ...                    │
                          ▼
              内层 while: 当前 token 在字符串中出现? 
                         │
         ┌───────────────┴───────────────┐
         │ YES:                           │ NO: 下一个token
         │   ① 判断是否特殊token            │
         │   ② 创建 NSDateFormatter        │
         │   ③ 设置 locale                 │
         │   ④ 从 NSConstantArray_00121a78 │
         │      取对应 format string       │
         │   ⑤ 格式化日期                   │
         │   ⑥ 替换字符串中的 {token}       │
         │   ⑦ 回到内层 while 检查是否还有  │
         └───────────────────────────────┘

问题: 每次替换一个 token 都会：
  - 创建一个新的 NSDateFormatter 对象
  - 设置 locale 和 format
  - 格式化 date
  - 在字符串中做一次 range 查找+替换
  
12 个 token × 每个出现一次 = 至少 12 次 NSDateFormatter 实例化
```

**关键差异**：

| 对比维度 | A（占位符策略） | B（逐个替换策略） | 分析 |
|----------|:---:|:---:|------|
| NSDateFormatter 实例化次数 | **2 次**（1 个 en_US_POSIX + 1 个特殊令牌用） | **N 次**（N = 格式中出现的令牌种类数，最多 12 次） | **A 更好**：NSDateFormatter 初始化开销大 |
| 字符串替换次数 | **1 次**正则批量替换 + 最多 4 次占位符回填 | 每个令牌 1 次 range+replace，最多 12 次 | **A 更好** |
| 令牌间依赖 | 无依赖，非特殊令牌整体格式化 | 令牌按数组顺序逐个替换，顺序决定行为 | **A 更好** |
| 扩展性 | 添加新令牌只需修改特殊令牌数组 + 对应的占位符回填逻辑 | 添加新令牌需要在 `NSConstantArray_00121a60` 和 `NSConstantArray_00121a78` 同步添加，并处理 locale 逻辑 | **A 更好** |

### 3.4 特殊令牌处理细节对比

#### {EE} — 短星期

| 步骤 | A | B | 差异 |
|------|---|---|------|
| locale | `zh_Hans_CN` | `zh_Hans_CN` | 相同 |
| NSDateFormatter format | `@"E"` | 从 `NSConstantArray_00121a78` 取（值为 `"E"`） | 结果相同 |
| 后处理 | `stringByReplacingOccurrencesOfString:@"上午" withString:@"上午"` → **no-op** | `cf_fg` → `cf_hT` 中文字符替换 | B 有实际字符转换，A 当前版本注释掉了真实逻辑 |

#### {EEEE} — 长星期

| 步骤 | A | B | 差异 |
|------|---|---|------|
| locale | `zh_Hans_CN` | `zh_Hans_CN` | 相同 |
| format | `@"EEEE"` | 从数组取（值 `"EEEE"`） | 相同 |
| 后处理 | 无 | 无 | 相同 |

#### {a} — 上下午（中文）

| 步骤 | A | B | 差异 |
|------|---|---|------|
| locale | `en_US_POSIX` | `en_US_POSIX` | 相同 |
| format | `@"a"` | 从数组取 | 相同 |
| 后处理 | 无 | 无 | 相同 |

#### {b} — AM/PM

| 步骤 | A | B | 差异 |
|------|---|---|------|
| locale | `en_US_POSIX` | `en_US_POSIX` | 相同 |
| format | `@"b"` | 从数组取 | 相同 |
| midnight映射 | `midnight` → `AM` | `midnight` → `AM` | 相同 |
| noon映射 | `noon` → `PM` | `noon` → `PM` | 相同 |
| 编辑器可见 | **可见** | **不可见** | A 更好 |

### 3.5 B 的两套格式引擎内部不一致

> B（微信优化反编译）中存在**两套独立的格式解析引擎**，但它们不是完全一致的实现。

| 对比项 | B-消息引擎 (`FUN_0003bcb0`) | B-编辑器预览引擎 (`CSTimeFormatEditor::updatePreview`) | 一致性 |
|--------|:---:|:---:|:---:|
| 令牌数组 | `NSConstantArray_00121a60` | `NSConstantArray_00121bf8` | **不同数组** |
| 格式化字符串数组 | `NSConstantArray_00121a78` | `NSConstantArray_00121c10` | **不同数组** |
| Regex预处理 | 无（只扫描令牌，直接进入主循环） | **有**：另一次正则扫描，对首个字符做预处理替换（行 88028-88049） | **不一致** |
| {b} 后处理中的 {EEEE} 判断 | 无需额外判断 | **有**：替换 {b} 后额外检查是否为 {EEEE} 或 {a}，若是则重设 locale（行 88185-88197） | **不一致** |
| NSDateFormatter 创建时机 | 每个 token 创建新的 formatter | 先创建统一 formatter（行 88083），后续复用 | **不一致** |

> **结论**：B 的两个格式引擎存在细微差异，可能导致编辑器预览与实际消息显示结果不同。而 A 只有一套格式引擎，编辑器和实际渲染共享同一个 `MessageTimeFormatParser`，**所见即所得**。

### 3.6 边界情况处理对比

| 边界情况 | A 的处理方式 | B 的处理方式 | 谁更好 |
|----------|-------------|-------------|--------|
| 空格式字符串 | `if (!fmt || fmt.length == 0)` → 返回默认格式 `HH:mm:ss` | 正则无匹配 → 返回原始字符串（可能为空） | **A 更好**（有明确的空值保护） |
| 格式只有非特殊令牌 | 整体传给 NSDateFormatter 格式化 | 逐个令牌查找替换，非令牌部分保持原样 | **平手** |
| 格式含未知 `{xxx}` | 正则匹配到 → 剥除花括号 → 传给 NSDateFormatter，若 NSDateFormatter 不识则原样输出 | 遍历 `NSConstantArray_00121a60` → 未找到匹配 → `{xxx}` 保持原样不替换 | **A 更好**（会尝试让 NSDateFormatter 解析；B 直接忽略，花括号内容残留） |
| 创建时间为 nil | `if (!date) date = [NSDate date]` 用当前时间 | 直接使用传入的 createTime（上层保证非空） | **A 更好**（防御性强） |
| 令牌出现多次 | 占位符 `SP0` `SP1` 支持同一特殊令牌多次出现 | `while` 循环支持同一令牌多次替换 | **平手** |
| 混合纯文本与令牌 | `{YYYY}年{MM}月{dd}日` → 正则提取 `YYYY年MM月dd日` → NSDateFormatter 整体处理 | `{YYYY}` 替换为年份 → `年` 保持原样 → `{MM}` 替换... | **A 更好**（NSDateFormatter 整体处理能利用其内置的文本穿插能力） |

### 3.7 格式解析器评分总表

| 评分维度 | A (WeChatPlugin) | B (FUN_0003bcb0) | 说明 |
|----------|:---:|:---:|------|
| 双模式支持 | ★★★★★ | ☆☆☆☆☆ | A 独有 |
| 性能（NSDateFormatter 创建次数） | ★★★★★ | ★★☆☆☆ | A: ≤2次, B: N次 |
| 空值/异常保护 | ★★★★★ | ★★☆☆☆ | A 有空格式、nil date 防护 |
| 代码简洁度 | ★★★★★ | ★★☆☆☆ | A: ~100行清晰ObjC, B: ~350行Goto混杂C |
| 所见即所得 | ★★★★★ | ★★★☆☆ | A 单引擎, B 两引擎有不一致风险 |
| 未知令牌降级 | ★★★★☆ | ★★☆☆☆ | A 会尝试 NSDateFormatter, B 直接忽略 |
| 边界完备性 | ★★★★★ | ★★★☆☆ | A 覆盖更多边界情况 |

---

## 四、格式编辑器 UI 对比

### 4.1 编辑器页面

| 对比项 | WeChatPlugin (A) | 微信优化反编译 (B) | 功能说明 | 谁更好 |
|--------|:---:|:---:|----------|--------|
| 实现类 | `MessageTimeFormatEditorVC` | `CSTimeFormatEditorViewController` | 自定义格式编辑界面 | **平手** |
| 导航栏 | "关闭"（左）、"恢复"+"保存"（右） | 同 | 操作按钮 | **平手** |
| 帮助表格 | 11 个令牌 + 中文说明 | 11 个令牌 + 中文说明（`NSConstantArray_00121bb0` 令牌名 + `NSConstantArray_00121bc8` 说明） | 列出可用令牌供用户参考 | **平手** |
| 表格每行高度 | 25.0px | 25.0px | 行高 | **平手** |
| 表格圆角 | 10.0px | 10.0px | 容器圆角 | **平手** |
| 左列字体 | 14px Semibold，蓝色（`systemBlueColor`） | 14px Semibold，蓝色（同） | 令牌名样式 | **平手** |
| 右列字体 | 14px Regular，灰色（`secondaryLabelColor`） | 14px Regular，灰色（同） | 说明文字样式 | **平手** |
| {b} 令牌 | **在表格中展示** | **不在表格中展示** | `{b}` (AM/PM) 令牌对用户的可见性 | **A 更好** |
| 编辑框 | `UITextView`，15px Regular，80px 高，10px 圆角 | 同 | 格式输入区域 | **平手** |
| 预览框 | `UITextView` 只读，50px 高，10px 圆角 | 同 | 实时预览区域 | **平手** |
| 键盘适配 | 监听 `UIKeyboardWillShow/Hide`，调整 `scrollView.contentInset` | 同（`keyboardWillShow:` selector） | 键盘弹出时滚动视图适配 | **平手** |
| 背景色 | `systemGroupedBackgroundColor` | 同 | 页面背景 | **平手** |
| 默认显示值 | 从 `MessageTimeFormatParser.defaultFormat` 读取（=`HH:mm:ss`） | `{HH}:{mm}:{ss}` | 编辑器初始格式文本 | **B 更好**（花括号格式与编辑器令牌表格一致） |

### 4.2 预览功能

| 对比项 | WeChatPlugin (A) | 微信优化反编译 (B) | 功能说明 | 谁更好 |
|--------|------------------|---------------------|----------|--------|
| 预览触发 | `textViewDidChange:` → `updatePreview` | 同（`textViewDidChange` 回调） | 输入变化时即时预览 | **平手** |
| 预览引擎 | 复用 `MessageTimeFormatParser.previewWithFormat:` | 使用**独立**的格式引擎（行 88083 起，`NSConstantArray_00121bf8` + `NSConstantArray_00121c10`） | 预览用的格式化逻辑 | **A 更好**（复用同一引擎保证一致性；B 两个引擎可能行为不同） |
| 暗色模式感知 | `traitCollection.userInterfaceStyle` 检测 | 需要进一步确认 | 预览时是否检测当前模式 | **平手** |

---

## 五、消息时间标签配置对比

| 配置项 | WeChatPlugin (A) 属性名 | 微信优化反编译 (B) NSUserDefaults key | 功能说明 | 谁更好 |
|--------|-------------------------|---------------------------------------|----------|--------|
| 功能开关 | `showMessageTime` | `messageTime.enabled` | 是否显示消息时间标签 | **平手** |
| 字体大小 | `messageTimeFontSize` | `messageTime.fontSize` | 标签字号，默认 7.0 | **平手** |
| 粗体 | `messageTimeBoldFont` | `messageTime.boldFont` | 是否使用粗体 | **平手** |
| 圆角 | `messageTimeCornerRadius` | `messageTime.cornerRadius` | 标签背景圆角，默认 8.0 | **平手** |
| 位置 | `messageTimePosition` (0-7) | `messageTime.position` (0-7) | 8 个预设位置 | **平手** |
| 水平偏移 | `messageTimeOffsetX` | `messageTime.offsetX` | 微调 X 偏移 | **平手** |
| 垂直偏移 | `messageTimeOffsetY` | `messageTime.offsetY` | 微调 Y 偏移 | **平手** |
| 气泡扩展宽度 | `messageTimeBubbleExtWidth` (默认 38.0) | `messageTime.bubbleExtensionWidth` (默认 38.0) | position=7 时气泡额外宽度 | **平手** |
| 禁用宽度自适应 | `disableLabelWidthAdjustment` | **无** | 禁用标签宽度动态调整 | **A 更好**（独有配置） |

### 5.1 颜色配置

| 配置项 | WeChatPlugin (A) | 微信优化反编译 (B) | 谁更好 |
|--------|------------------|---------------------|--------|
| 颜色存储方式 | **Hex 字符串**（如 `#808080`），通过 `colorFromHex:` 解析 | **NSKeyedArchiver 序列化的 UIColor 对象**（通过 `FUN_000d0b60` 反序列化） | **A 更好**（Hex 字符串跨版本兼容、可读可调试；NSKeyedArchiver 存在类版本兼容问题） |
| 发送者文本色 | `senderTextColorHex` + `senderTextColorDarkHex` | `messageTime.sender.textColor` + `.dark` | **平手** |
| 发送者背景色 | `senderBackgroundColorHex` + `senderBackgroundColorDarkHex` | `messageTime.sender.backgroundColor` + `.dark` | **平手** |
| 接收者文本色 | `receiverTextColorHex` + `receiverTextColorDarkHex` | `messageTime.receiver.textColor` + `.dark` | **平手** |
| 接收者背景色 | `receiverBackgroundColorHex` + `receiverBackgroundColorDarkHex` | `messageTime.receiver.backgroundColor` + `.dark` | **平手** |
| 简化色配置 | `messageTimeTextColor`（不分收发） | **无** | **A 更好** |
| 暗色模式提亮 | `autoDarkColor()` 自动提亮 | 无自动提亮，需手动设 dark 值 | **A 更好** |

---

## 六、核心差异总结

| 差异项 | 谁有 | 谁没有 | 重要性 | 评价 |
|--------|:---:|:---:|:---:|------|
| **双模式格式**（逗号分隔亮/暗） | A | B | 高 | A 可分别给亮暗模式不同格式，如白天用短格式、夜间用完整格式 |
| **暗色自动提亮** | A | B | 中 | A 未设 dark 色值时自动提亮保证可见，B 需要用户手动配置 |
| **{b} 令牌编辑器可见** | A | B（引擎支持但编辑器不展示） | 低 | A 用户可见可用 AM/PM 格式，B 用户不知道此令牌存在 |
| **Hex 颜色存储** | A | B（NSKeyedArchiver） | 中 | A 字符串可读可调试，B 序列化对象有版本兼容风险 |
| **默认格式字符串** | A=`HH:mm:ss` | B=`{HH}:{mm}:{ss}` | 低 | B 默认格式与编辑器令牌风格一致，A 无花括号但 NSDateFormatter 同样能解析 |
| **预览引擎一致性** | A（复用同一引擎） | B（消息引擎和预览引擎是两个实现） | 低 | A 所见即所得更可靠 |
| **统一文字颜色** | A 有 | B 无 | 低 | A 多一个简化配置项 |
| **禁用宽度自适应** | A 有 | B 无 | 低 | A 多一个标签宽度控制选项 |
| 令牌集 | 12 个 | **12 个**（含隐藏的 {b}） | — | **完全相同** |

---

## 七、结论

1. **令牌支持完全相同**：两个项目的格式引擎都支持 12 个令牌（`{YYYY}` `{YY}` `{MM}` `{dd}` `{HH}` `{hh}` `{mm}` `{ss}` `{EE}` `{EEEE}` `{a}` `{b}`），令牌集无差异。

2. **A（WeChatPlugin）在以下方面优于 B**：
   - **解析器架构**：占位符策略（≤2 次 NSDateFormatter 初始化）vs B 的逐个替换策略（N 次），性能更优
   - **双模式格式**：逗号分隔亮/暗两套格式（A 独有）
   - **空值防御**：空格式 → 回退默认值，nil date → 当前时间；B 不处理
   - **Regex 严谨性**：`\{\w+\}` vs B 的 `\{.*?\}`，不会误匹配空 token
   - **单引擎一致性**：编辑器和消息渲染共用同一解析器，所见即所得；B 两套引擎有不一致风险
   - **暗色自动提亮**：未设 dark 色值自动提亮（A 独有）
   - **颜色存储**：Hex 字符串比 NSKeyedArchiver 更可靠
   - **`{b}` 令牌可见**：编辑器中展示了用户可用
   - **额外配置项**：统一文字颜色、禁用宽度自适应

3. **B（微信优化反编译）在以下方面优于 A**：
   - **默认格式更友好**：`{HH}:{mm}:{ss}` 与编辑器令牌表格一致
   - **非特殊令牌花括号处理正确**：B 对所有 12 个令牌的花括号统一处理；A 只处理了 4 个，其余 8 个花括号残留传给 NSDateFormatter 导致显示异常

4. **A 当前存在的 BUG（2.4 节详细分析）**：
   - **BUG 1**：编辑器帮助表格教用户用 `{TOKEN}` 花括号格式，但默认值 `HH:mm:ss` 无花括号，用户困惑 → `EE` 无花括号时走 en_US_POSIX 输出英文
   - **BUG 2**：`rangeOfString:@"{EE}"` 在 `{EEEE}` 中触发前缀匹配，污染破坏 `{EEEE}` 令牌
   - **BUG 3**：`{YYYY}{MM}{dd}{HH}{hh}{mm}{ss}` 八种非特殊令牌的 `{}` 花括号不被剥离，NSDateFormatter 把 `{` `}` 当字面字符 → 输出 `{2025}:{30}` 之类异常

5. **总体评价**：WeChatPlugin 的自定义时间格式功能在架构设计上是对微信优化的**创新改进**（占位符策略、双模式格式、自动提亮），但在实现上存在**花括号格式不统一**的核心缺陷，导致 8/12 的令牌使用花括号输入时无法正常工作，以及 `{EE}`/`{EEEE}` 的处理顺序 BUG。建议优先修复 2.4 节列出的三个 BUG。`,

---

*本文档基于 `/www/wwwroot/ios/WeChatPlugin` 源代码和 `/www/wwwroot/ios/插件/微信优化反编译最新/123456.c` 反编译代码的逐一比对分析生成。*