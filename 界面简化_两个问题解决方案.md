# 界面简化改造 — 两个问题的解决方案

---

## 问题一: 好友数替换误伤所有含数字的文本

### 根因

setText 和 setAttributedText 中好友数匹配路径排在最后，且**没有任何上下文门控**。前面 Nav/通讯录/发现页路径都有 `hasComma` 保护，但好友数路径是无条件兜底，导致任意含数字的文本("5条新消息"等)落进来就被替换。

另外 `_friendsCount` 被同时当作正则和 format 用——用户配置的是 `%d位朋友` 这种格式串，拿去当正则匹配什么都匹配不到，功能实际是瘫痪的。

### 微信优化怎么做的

微信优化的正则 pattern 和 format string 是两个不同的值（Ghidra 在那一块混淆严重无法100%还原，但从功能上推断：
- 正则: 硬编码的 `\d+` 类数字匹配
- 上下文限制: 文本内容本身限定在通讯录底部的好友数展示（如"123个朋友"）

### 修改位置

**两个函数都要改**: `hook_MMUILabel_setText` 和 `hook_MMUILabel_setAttributedText`

### 具体改法

**setText** — 找到当前的好友数匹配代码块，替换为：

```objc
// 好友数格式替换 — 只处理通讯录底部的好友数量
if (_friendsCount && _friendsCount.length > 0) {
    // 门控: 只有通讯录好友数文本才处理，防止误伤其他含数字的文本
    if (![text containsString:@"个朋友"]) {
        ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, text);
        return;
    }
    // 正则硬编码 \d+，仅提取数字部分
    NSRegularExpression *regex = [NSRegularExpression
        regularExpressionWithPattern:@"\\d+" options:0 error:nil];
    if (regex) {
        NSTextCheckingResult *match = [regex firstMatchInString:text
            options:0 range:NSMakeRange(0, text.length)];
        if (match && match.range.location != NSNotFound) {
            NSString *number = [text substringWithRange:match.range];
            // _friendsCount 仅作 format 字符串 (如 @"%d位朋友")
            NSString *formatted = [NSString stringWithFormat:_friendsCount, number];
            if (formatted) {
                ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, formatted);
                return;
            }
        }
    }
}

// 兜底: 原始文本
((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, text);
```

**setAttributedText** — 同样位置做同等改动：

```objc
// 好友数格式替换
if (_friendsCount && _friendsCount.length > 0) {
    // 门控: 只有通讯录好友数文本才处理
    if (![text containsString:@"个朋友"]) {
        ((void (*)(id, SEL, id))_orig_MMUILabel_setAttributedText)(self, _cmd, attrText);
        return;
    }
    NSRegularExpression *regex = [NSRegularExpression
        regularExpressionWithPattern:@"\\d+" options:0 error:nil];
    if (regex) {
        NSTextCheckingResult *match = [regex firstMatchInString:text
            options:0 range:NSMakeRange(0, text.length)];
        if (match && match.range.location != NSNotFound) {
            NSString *number = [text substringWithRange:match.range];
            NSString *formatted = [NSString stringWithFormat:_friendsCount, number];
            if (formatted) {
                NSDictionary *attrs = nil;
                if (attrText.length > 0) {
                    attrs = [attrText attributesAtIndex:0 effectiveRange:NULL];
                }
                NSMutableAttributedString *replaced = [[NSMutableAttributedString alloc]
                    initWithString:formatted attributes:attrs ?: @{}];
                if (replaced) {
                    ((void (*)(id, SEL, id))_orig_MMUILabel_setAttributedText)(self, _cmd, replaced);
                    [replaced release];
                    return;
                }
            }
        }
    }
}

((void (*)(id, SEL, id))_orig_MMUILabel_setAttributedText)(self, _cmd, attrText);
```

### 改动总结

| 改前 | 改后 |
|------|------|
| `_friendsCount` 当正则用 | 正则硬编码 `@"\\d+"` |
| 无上下文门控，任何含数字文本都被替换 | 加 `containsString:@"个朋友"` 门控 |
| `_friendsCount` 当 regex+format | `_friendsCount` 仅作 format |

---

## 问题二: 低版本"卡包" vs 高版本"订单与卡包"跨版本兼容

### 根因

改造后 WCTitle 和 getTitle 都是**精确字典查找**。用户菜单字典配了 `{"卡包": "自定义名"}`，但高版本微信实际标题是"订单与卡包"，精确匹配不到，替换不生效。

旧的 Mio助手 靠 `_altMenuKeys` + `menuNameLookup` 做反向映射("订单与卡包"→"卡包"→查字典)，但改造时整体删除了这个机制。

### 微信优化怎么做的

微信优化在配置加载阶段(FUN_00043e84)做**预处理**。反编译代码中的关键逻辑(L40024-40051):

```
// 从菜单字典中读两个版本的名字
value_old = menuDict["卡包"]     // 低版本标题
value_new = menuDict["订单与卡包"] // 高版本标题

if (只有旧版本配了)      → 自动添加 {"订单与卡包": value_old}
if (只有新版本配了)      → 自动添加 {"卡包": value_new}
if (两个都配了且不包含)   → 自动添加 {"订单与卡包": value_new}
```

本质：**装载时自动扩张字典**，保证两个版本标题都指向同一个替换值。

### 修改位置

`UISimplify_ReloadConfig()` 函数，在 `_menuNames` 赋值后增加预处理。

### 具体改法

在 `UISimplify_ReloadConfig` 中，加载完 `_menuNames` 后：

```objc
// 微信优化 L40024-40051: 菜单名跨版本兼容预处理
// 低版本和高版本菜单标题不同(如"卡包"/"订单与卡包")，
// 用户只需配其中一个，自动补齐另一个。
{
    // 定义需要跨版本兼容的菜单名对: {新版本名, 旧版本名}
    NSArray *versionPairs = @[
        @[@"订单与卡包", @"卡包"],
        // 以后如果有其他跨版本差异的菜单项，在这里追加
    ];
    
    NSMutableDictionary *mutDict = [_menuNames mutableCopy];
    for (NSArray *pair in versionPairs) {
        NSString *newName = pair[0];
        NSString *oldName = pair[1];
        id newVal = [mutDict objectForKey:newName];
        id oldVal = [mutDict objectForKey:oldName];
        
        if (oldVal && !newVal) {
            // 只配了旧版名 → 自动补新版名
            [mutDict setObject:oldVal forKey:newName];
        } else if (!oldVal && newVal) {
            // 只配了新版名 → 自动补旧版名
            [mutDict setObject:newVal forKey:oldName];
        }
    }
    [_menuNames release];
    _menuNames = [mutDict copy];
    [mutDict release];
}
```

### 工作原理示例

| 用户配置 | 预处理后实际生效 |
|---------|---------------|
| `{"卡包": "钱包"}` | `{"卡包": "钱包", "订单与卡包": "钱包"}` |
| `{"订单与卡包": "钱包"}` | `{"订单与卡包": "钱包", "卡包": "钱包"}` |
| `{"卡包": "钱包", "订单与卡包": "订单"}` | 两个都配了 → 保持不变 |

### 扩展

如果以后还有其他跨版本差异的菜单项，只需要在 `versionPairs` 数组中追加一对即可，例如:

```objc
@[@"订单与卡包", @"卡包"],
@[@"新版本名X", @"旧版本名X"],
```

---

## 对比: 和旧 Mio助手 alt key 回退的区别

| | 旧 Mio助手 (_altMenuKeys) | 微信优化 / 本方案 |
|---|---|---|
| 生效时机 | 每次 Hook 调用时动态查找 | **配置加载时一次性预处理** |
| 性能 | 每次都遍历字典 | 加载后零开销 |
| 方向 | 单向: "订单与卡包"→"卡包" | 双向: 配任一方向自动补齐另一方 |
| 扩展性 | 硬编码在数组里 | 同样硬编码在 versionPairs |

两种方式最终效果一致，但预处理方案更干净，也更接近微信优化原实现。