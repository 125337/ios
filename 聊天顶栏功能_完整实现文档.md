# 微信优化 — 聊天顶栏功能 完整实现文档（二次验证版）

> 基于微信优化 1.6.5 反编译 `123456.c` 100% 逐行对齐，二次交叉验证。
> 总开关：**显示聊天头像** (`chat.avatar.display.enabled`)
> 核心类：**CSChatAvatarTitleView** + **CSChatTitleSettingsViewController**

---

## 一、功能简介

在微信聊天界面，用 **自定义的 CSChatAvatarTitleView** 替换导航栏原生 `titleView`，实现：

- 双方头像显示（左侧对方 + 右侧自己）
- 8 种显示模式（头像组合/重叠/单边/昵称）
- 头像尺寸 / 圆角 / 间距可定制
- 分隔符系统（图标 / GIF / 文字）
- 昵称 + 添加好友时长 / 群成员数后缀
- **联系人级别**黑名单
- 头像点击跳转用户信息弹窗 + 触觉反馈
- 后台静默预加载联系人扩展信息

```
┌──────────────────────────────────────────────────────────┐
│                    原生日志状态栏                            │
├────┬──────────────────────────────────────────────────┬───┤
│ ←  │     ┌──────┐  分隔符  ┌──────┐  titleLabel       │ ···│
│    │     │  对方  │ ────→  │  自己  │  (昵称+后缀)     │     │
│    │     │  头像  │        │  头像  │                  │    │
│    │     └──────┘  icon/  └──────┘                   │    │
│    │   leftAvatar  txt/gif  rightAvatar               │    │
├────┴──────────────────────────────────────────────────┴───┤
│              聊天消息列表区域 (BaseMsgContentVC)              │
└──────────────────────────────────────────────────────────┘
```

---

## 二、实例变量与属性 — CSChatAvatarTitleView

> 对齐 `123456.c` L448-L453

| 偏移 | 属性名 (getter/setter) | 类型 | 内存语义 | 说明 |
|------|----------------------|------|---------|------|
| 448 | `leftAvatarView` / `setLeftAvatarView:` | UIImageView | strong | 对方头像 |
| 449 | `rightAvatarView` / `setRightAvatarView:` | UIImageView | strong | 自己的头像 |
| 450 | `separatorView` / `setSeparatorView:` | UIImageView | strong | 分隔符图标视图 |
| 451 | `separatorTextLabel` / `setSeparatorTextLabel:` | UILabel | strong | 分隔符文字标签 |
| 452 | `titleLabel` / `setTitleLabel:` | UILabel | strong | **昵称标签** (非 nameLabel!) |
| 453 | `chatController` / `setChatController:` | BaseMsgContentVC | **weak** | 聊天控制器引用 (非 baseMsgContentVC!) |

> **关键纠正**：昵称标签的 ivar 名是 `_titleLabel` (不是 `_nameLabel`)，聊天控制器是 `_chatController` 且为 **weak** 引用（`_objc_loadWeakRetained` / `_objc_storeWeak` / `_objc_destroyWeak`）。

---

## 三、配置 Key 大全 — 21 个配置项

> 对齐 `123456.c` L414-L447 CFString 常量

### 3.1 开关类

| Key | 类型 | 默认值 | 说明 |
|-----|------|-------|------|
| `chat.avatar.display.enabled` | BOOL | NO | **总开关** |
| `chat.avatar.click.feedback.enabled` | BOOL | NO | 点击头像触觉反馈 (初次安装默认 YES) |
| `chat.show.addtime.enabled` | BOOL | NO | 昵称后显示添加好友时长 |
| `chat.show.group.member.count.enabled` | BOOL | NO | 群聊时显示群成员数量 |

### 3.2 数值类

| Key | 类型 | 默认值 | 说明 |
|-----|------|-------|------|
| `chat.avatar.size` | Float | 30.0 | 头像宽/高 (正方形) |
| `chat.avatar.spacing` | Float | 3.0 | 双头像间距 |
| `chat.avatar.corner.radius` | Float | 1.0 | 圆角系数 (百分比，1.0=100%=半圆) |
| `chat.name.font.size` | Float | 9.0 | 昵称字体大小 |
| `chat.name.position.offset` | Float | 0 | 昵称垂直位移 |
| `chat.name.horizontal.offset` | Float | 0 | 昵称水平位移 |
| `chat.separator.size` | Float | 12.0 | 分隔符尺寸/字体基准 |
| `chat.title.view.width.v2` | Float | 210.0 | 整体 titleView 宽度 |
| `chat.position.offset` | Float | 0 | 整体垂直位移 |
| `chat.horizontal.offset` | Float | 0 | 整体水平位移 |

### 3.3 枚举/格式/数据类

| Key | 类型 | 说明 |
|-----|------|------|
| `chat.display.mode` | Int (0~7) | 显示模式 |
| `chat.separator.icon` | Data | 分隔符 PNG 图标 (NSData) |
| `chat.separator.gif` | Data/Path | 分隔符 GIF 图片 (NSData 或文件路径) |
| `chat.separator.text` | String | 分隔符文字 |
| `chat.avatar.blacklist` | String | 黑名单 (换行 `\n` 分隔) |
| `chat.addtime.suffix.format` | String | 添加时间后缀格式 (如 `%ld天`) |
| `chat.group.member.count.suffix.format` | String | 群成员数后缀格式 |

---

## 四、8 种显示模式 (display.mode)

> 对齐 `123456.c` L55316 `showDisplayModeOptions` + L55640 `setDisplayMode:`

| mode | 标签 | 左头像 | 右头像 | 分隔符 | titleLabel |
|------|------|--------|--------|--------|-----------|
| 0 | "对方头像+昵称" | 显示 | — | — | 显示 |
| 1 | "简单模式" | — | 显示 | — | — |
| 2 | "仅自己头像" | — | 显示 | — | — |
| **3** | **"仅对方头像"** (默认) | 显示 | — | — | — |
| 4 | "默认模式" | 显示 | — | — | — |
| 5 | "左头像+分隔符" | 显示 | — | 显示 | 显示 |
| 6 | "右头像+昵称" | — | 显示 | — | 显示 |
| 7 | "重叠模式" | 显示(100%) | 显示(85%) | — | — |

### 4.1 模式 0 — 对方头像 + 昵称

```
┌──────────────────────────────┐
│   [{对方头像}]   昵称文字      │
└──────────────────────────────┘
```

- 对方头像左对齐
- titleLabel 紧接头像右侧，字体 9pt，`UIFontWeightRegular`

### 4.2 模式 7 — 重叠模式 (叠影效果)

```
┌──────────────────────────────┐
│      ┌──────┐                │
│      │  对方  │   100%尺寸     │
│     ┌┤      ├──┐             │
│     │└──────┘  │             │
│     │  ┌──────┐│             │
│     └──┤  自己  ├┘  85%尺寸    │
│        └──────┘  30%偏移      │
└──────────────────────────────┘
```

- 对方头像 frame: `(0, avatarY, avatarSize, avatarSize)`，圆角 `calculateCornerRadiusForSize:`
- 自己头像 frame: `(avatarSize*0.3, avatarSize*avatarY-avatarSize*0.15, avatarSize*0.85, avatarSize*0.85)`

---

## 五、核心架构：Hook 系统

### 5.1 注册入口 — FUN_00014c14 (L13272)

```objc
void FUN_00014c14(void) {
    Class cls = objc_getClass("BaseMsgContentViewController");
    MSHookMessageEx(cls, @selector(viewDidLoad),      FUN_00014c74, &orig_viewDidLoad);
    MSHookMessageEx(cls, @selector(viewWillAppear:),   FUN_00014ed0, &orig_viewWillAppear);
}
```

### 5.2 Hook ① — viewDidLoad (L13285)

```
FUN_00014c74 (viewDidLoad Hook)
│
├─ Step 1: associatedObject 缓存原始 titleView
│     uintptr_t stored = (uintptr_t)objc_getAssociatedObject(self, KEY);
│     └─ stored == 0 → 首次进入:
│           objc_setAssociatedObject(self, KEY,
│               (uintptr_t)[self.navigationItem.titleView],
│               OBJC_ASSOCIATION_ASSIGN);
│           // ★ 保存原始 titleView 以便关闭开关时恢复
│
├─ Step 2: 总开关
│     └─ [[NSUserDefaults standardUserDefaults]
│           boolForKey:@"chat.avatar.display.enabled"]
│         └─ NO → 调原始 viewDidLoad → return
│
└─ Step 3: 开关=YES → 创建 CSChatAvatarTitleView
      ├─ id contact = [self GetContact];  // objc_msgSend 调用
      ├─ NSString *username = [contact m_nsUsrName];
      ├─ isContactInBlacklist(username)
      │     └─ YES → 调原始 viewDidLoad → return
      │
      ├─ CGFloat width = [d floatForKey:@"chat.title.view.width.v2"] ?: 210.0;
      ├─ CSChatAvatarTitleView *view = [[CSChatAvatarTitleView alloc]
      │       initWithFrame:CGRectMake(0, 0, width, 45)];
      │
      ├─ [view setChatController:self];  // weak setter
      ├─ [[self.navigationItem.titleView] removeFromSuperview];
      ├─ [self.navigationItem setTitleView:view];
      │
      └─ [view layoutSubviews];
```

### 5.3 Hook ② — viewWillAppear: (L13357)

```
FUN_00014ed0 (viewWillAppear: Hook)
│
├─ 调原始 viewWillAppear:
│
├─ Branch 1: 总开关 = NO
│     ├─ id currentTitle = [self.navigationItem titleView];
│     ├─ [currentTitle isKindOfClass:[CSChatAvatarTitleView class]]?
│     │    ├─ YES → 恢复原始 titleView:
│     │    │     uintptr_t saved = objc_getAssociatedObject(self, KEY);
│     │    │     [self.navigationItem setTitleView:(UIView *)saved];
│     │    └─ NO → return
│     └─ return
│
└─ Branch 2: 总开关 = YES
      ├─ 获取 username → isContactInBlacklist
      │    └─ YES → 恢复原始titleView (同 Branch 1) → return
      │
      ├─ currentTitle 是否为 CSChatAvatarTitleView?
      │    ├─ NO → 创建新的 (同 viewDidLoad Step 3)
      │    │     ├─ 读取 width
      │    │     ├─ alloc + initWithFrame: + setChatController:
      │    │     └─ navigationItem.titleView = view
      │    │
      │    └─ 无论是否新建:
      │         ├─ [view updateAvatars]   ← ★ 每次出现强制刷新头像
      │         ├─ [view setNeedsLayout]
      │         ├─ [view layoutIfNeeded]
      │         └─ [view setNeedsDisplay]
      │
      └─ return
```

---

## 六、CSChatAvatarTitleView 实现详解

### 6.1 子视图层次总览

```
CSChatAvatarTitleView : UIView
│
├── leftAvatarView (UIImageView)         ← 对方头像
│   ├── tag = 2
│   ├── contentMode = ScaleAspectFill (1)
│   ├── clipsToBounds = YES
│   └── UITapGestureRecognizer → onLeftAvatarTapped:
│
├── rightAvatarView (UIImageView)        ← 自己头像
│   ├── tag = 2
│   ├── contentMode = ScaleAspectFill (1)
│   ├── clipsToBounds = YES
│   ├── userInteractionEnabled = NO (初始)
│   └── UITapGestureRecognizer → onRightAvatarTapped:
│
├── separatorView (UIImageView)          ← 分隔符图标/GIF
│   ├── contentMode = ScaleAspectFill (1)
│   ├── userInteractionEnabled = YES (仅当有图标/GIF时)
│   └── hidden = YES (初始隐藏)
│
├── separatorTextLabel (UILabel)         ← 分隔符文字
│   ├── textColor = [UIColor grayColor]
│   ├── font = systemFontOfSize:clamp(8, sepSize*0.4, 16) weight:Medium
│   └── hidden = YES (初始隐藏, setHidden in setup)
│
└── titleLabel (UILabel)                 ← 昵称+后缀
    ├── textColor = [UIColor grayColor]
    ├── font = systemFontOfSize:nameFontSize (默认9)
    ├── numberOfLines = 1
    └── hidden = YES (初始隐藏)
```

### 6.2 initWithFrame: (L10924)

```
initWithFrame:(CGRect)frame
├── [super initWithFrame:frame]
├── backgroundColor = [UIColor clearColor] (透明)
├── clipsToBounds = YES
├── [self setupSubviews]
└── return self
```

### 6.3 setupSubviews (L10948)

```objc
- (void)setupSubviews {
    // ─── 5 个子视图的创建 ───

    // 1. leftAvatarView (UIImageView)
    UIImageView *la = [[UIImageView alloc] init];
    [self addSubview:la];
    [self setLeftAvatarView:la];            // strong 属性
    [la setContentMode:1];                  // ScaleAspectFill
    [la setTag:2];
    [la setClipsToBounds:YES];
    [la addGestureRecognizer:[[UITapGestureRecognizer alloc]
        initWithTarget:self action:@selector(onLeftAvatarTapped:)]];

    // 2. rightAvatarView (UIImageView)
    UIImageView *ra = [[UIImageView alloc] init];
    [self addSubview:ra];
    [self setRightAvatarView:ra];           // strong 属性
    [ra setContentMode:1];
    [ra setTag:2];
    [ra setUserInteractionEnabled:NO];      // 初始禁用交互
    [ra setClipsToBounds:YES];
    [ra addGestureRecognizer:[[UITapGestureRecognizer alloc]
        initWithTarget:self action:@selector(onRightAvatarTapped:)]];

    // 3. separatorView (UIImageView)
    UIImageView *sv = [[UIImageView alloc] init];
    [self addSubview:sv];
    [self setSeparatorView:sv];             // strong 属性
    [sv setContentMode:1];
    [sv setUserInteractionEnabled:YES];
    [sv setHidden:YES];

    // 4. separatorTextLabel (UILabel)
    UILabel *st = [[UILabel alloc] init];
    [self addSubview:st];
    [self setSeparatorTextLabel:st];        // strong 属性
    CGFloat sepSize = [d floatForKey:@"chat.separator.size"] ?: 12.0;
    CGFloat fs = MAX(8.0, MIN(sepSize * 0.4, 16.0));  // clamp
    [st setFont:[UIFont systemFontOfSize:fs weight:UIFontWeightMedium]];
    [st setTextColor:[UIColor grayColor]];

    // 5. titleLabel (UILabel) — 昵称标签
    UILabel *tl = [[UILabel alloc] init];
    [self addSubview:tl];
    [self setTitleLabel:tl];                // strong 属性
    CGFloat nameSize = [d floatForKey:@"chat.name.font.size"] ?: 9.0;
    [tl setFont:[UIFont systemFontOfSize:nameSize]];  // ★ 普通字体, 非 Bold
    [tl setTextColor:[UIColor grayColor]];
    [tl setNumberOfLines:1];
    [tl setHidden:YES];
}
```

### 6.4 layoutSubviews (L11130)

> 根据 `displayMode` (0~7)、`hasSeparator`、`hasName` 三个维度，共 8+ 个分支。

**Y 轴高度计算（全局）：**

```objc
CGFloat totalH = self.bounds.size.height;
CGFloat avatarY;

if (displayMode == 1 || displayMode == 2 || displayMode == 3 || displayMode == 7) {
    // 仅头像：垂直居中
    avatarY = (totalH - avatarSize) * 0.5;
} else {
    // 头像 + 分隔 + titleLabel：上下排列后居中
    CGFloat contentH = avatarSize + 1 + 1 + nameFontSize + 1;
    avatarY = (totalH - contentH) * 0.5 + 1;
}
```

**各模式 X 轴布局逻辑：**

| 条件 | 左头像 | 分隔符文字 | 分隔符图标 | 右头像 | titleLabel |
|------|--------|----------|----------|--------|-----------|
| mode=7 (重叠) | `x=0` | — | — | `x=avatarSize*0.3` w: `0.85*avatarSize` | — |
| mode=5 (左头像+分隔+昵称) | 右侧偏移计算 | — | 间距30 | — | 计算宽度 |
| mode=6 (右头像+昵称) | — | — | — | 居中 | 显示 |
| `hasSeparator && hasName` (完整) | `x=centerOff` | `x+avatarSize+halfSpacing` | 紧接文字后 | `x+...` 末尾 | — |
| `!hasSeparator && hasName` | 居中 | — | — | — | — |
| `hasSeparator && !hasName` | 居中间距 | — | 图标居中 | 图标后 | — |
| `!hasSeparator && !hasName` | 居中单头像 | — | — | — | — |

---

## 七、头像更新系统 — updateAvatars (L11673)

```
updateAvatars
│
├─ 1. 通过 chatController (weak) 获取 BaseMsgContentVC
│     └─ nil → return
│
├─ 2. 获取联系人
│     └─ id contact = [[self chatController] GetContact];  // objc_msgSend
│         └─ nil → return
│
├─ 3. 解析信息
│     ├─ NSString *opponentWxid = [contact m_nsUsrName];   // 对方
│     ├─ NSString *nickname     = [contact m_nsNickName];
│     └─ NSString *selfWxid     = [self getSelfWxid];      // 自己
│
├─ 4. 加载头像 (按优先级)
│     ├─ [self loadAvatarWithPriorityForWxid:opponentWxid]
│     │     ├─ ① loadCustomAvatarForWxid:  → Documents/HBWechatHelper/UserHeadImage/{wxid}.gif
│     │     │                               → Documents/HBWechatHelper/UserHeadImage/{wxid}.jpg
│     │     ├─ ② loadWeChatAvatarForWxid:  → 微信原生头像 (MMServiceCenter + CContactMgr)
│     │     └─ ③ [UIImage imageNamed:@"DefaultHead"]  → 默认头像占位
│     │
│     └─ [self loadAvatarWithPriorityForWxid:selfWxid]  (同上)
│
├─ 5. 构建 titleLabel 文本
│     ├─ 群聊 + show.group.member.count.enabled
│     │     └─ [[CContactMgr shared] getGroupMemberCount] → "{count}{suffix格式}"
│     │        如: suffix="%ld人" → "25人"
│     │
│     ├─ C2C + show.addtime.enabled
│     │     └─ [contact m_uiAddTime] → 计算天数 → "{天数}{suffix格式}"
│     │        如: suffix="%ld天" → "365天"
│     │
│     └─ 追加到昵称后，设置到 titleLabel.text
│
└─ 6. dispatch_async(dispatch_get_main_queue(), ^{
        self.leftAvatarView.image = 对方头像UIImage;
        self.rightAvatarView.image = 自己头像UIImage;
        self.titleLabel.text = 昵称文本;
    });
```

### 7.1 loadCustomAvatarForWxid: — 自定义头像优先级 (L12102)

```objc
- (UIImage *)loadCustomAvatarForWxid:(NSString *)wxid {
    NSString *dir = [NSSearchPathForDirectoriesInDomains(
        NSDocumentDirectory, NSUserDomainMask, YES) firstObject];
    // ① 优先 .gif
    NSString *gifPath = [dir stringByAppendingPathComponent:
        [NSString stringWithFormat:@"HBWechatHelper/UserHeadImage/%@.gif", wxid]];
    if ([[NSFileManager defaultManager] fileExistsAtPath:gifPath]) {
        NSData *data = [NSData dataWithContentsOfFile:gifPath];
        return [UIImage imageWithData:data];
    }
    // ② 回退 .jpg
    NSString *jpgPath = [dir stringByAppendingPathComponent:
        [NSString stringWithFormat:@"HBWechatHelper/UserHeadImage/%@.jpg", wxid]];
    if ([[NSFileManager defaultManager] fileExistsAtPath:jpgPath]) {
        NSData *data = [NSData dataWithContentsOfFile:jpgPath];
        return [UIImage imageWithData:data];
    }
    return nil;
}
```

### 7.2 loadWeChatAvatarForWxid: — 微信原生头像 (L12180)

```objc
- (UIImage *)loadWeChatAvatarForWxid:(NSString *)wxid {
    // 获取微信头像管理器
    id serviceCenter = [objc_getClass("MMServiceCenter") defaultCenter];
    id contactMgr = [serviceCenter getService:[objc_getClass("CContactMgr") class]];

    id contact = [contactMgr getContactByName:wxid];
    if (!contact) return nil;

    // 通过微信原生 getter 获取头像
    return [contact m_avatarImage];  // 或类似 getter
}
```

---

## 八、黑名单系统 — isContactInBlacklist (L13157)

```objc
BOOL isContactInBlacklist(NSString *contactID) {
    if (!contactID || contactID.length == 0) return NO;

    NSString *blacklist = [[NSUserDefaults standardUserDefaults]
        stringForKey:@"chat.avatar.blacklist"];
    if (!blacklist || blacklist.length == 0) return NO;

    NSArray *lines = [blacklist componentsSeparatedByString:@"\n"];

    BOOL isGroup = [contactID containsString:@"@chatroom"];

    for (NSString *line in lines) {
        NSString *t = [line stringByTrimmingCharactersInSet:
                        [NSCharacterSet whitespaceCharacterSet]];
        if (t.length == 0) continue;

        // 规则1: "@" → 匹配所有群聊
        if (isGroup && [t isEqualToString:@"@"]) return YES;

        // 规则2: "gh_"前缀 → 匹配所有公众号
        if ([contactID hasPrefix:@"gh_"] && [t hasPrefix:@"gh_"]) return YES;

        // 规则3: 精确匹配
        if ([t isEqualToString:contactID]) return YES;

        // 规则4: 包含匹配 (针对群聊ID)
        if (isGroup && [contactID containsString:t]) return YES;
    }
    return NO;
}
```

### 8.1 匹配规则矩阵

| 黑名单输入 | 匹配条件 | 匹配结果 |
|-----------|---------|---------|
| `@` | `contactID` 含 `@chatroom` | **所有群聊** |
| `gh_` | `contactID` 以 `gh_` 开头 | **所有公众号** |
| `wxid_xxxx` | 完全相等 | 精确匹配 |
| 群聊ID片段 | `contactID` 包含片段 | 部分匹配(群聊) |

---

## 九、分隔符系统 (L12229 + L12356)

### 9.1 加载优先级

```
loadSeparatorIcon (L12229)
├── 读取 chat.separator.icon (NSData)
│     └─ 有 → [UIImage imageWithData:] → setImage: on separatorView
│         └─ separatorView.hidden = NO
│         └─ separatorView.userInteractionEnabled = YES
│
├── 读取 chat.separator.gif (NSData 或文件路径)
│     └─ 有 → GIF 动画
│         └─ separatorView.hidden = NO
│         └─ separatorView.userInteractionEnabled = YES
│
└─ 无 → separatorView.hidden = YES

loadSeparatorText (L12356)
├── 读取 chat.separator.text (NSString)
│     └─ 有 → separatorTextLabel.text = text
│         └─ separatorTextLabel.hidden = NO
│
└─ 无 → separatorTextLabel.hidden = YES
```

### 9.2 分隔符文字字体自适应

```objc
// clamp(8, separatorSize * 0.4, 16)
CGFloat fs = MAX(8.0, MIN(separatorSize * 0.4, 16.0));
[separatorTextLabel setFont:
    [UIFont systemFontOfSize:fs weight:UIFontWeightMedium]];
```

---

## 十、圆角系统 — calculateCornerRadiusForSize: (L12552)

```objc
- (CGFloat)calculateCornerRadiusForSize:(CGFloat)size {
    CGFloat cornerRadius = [[NSUserDefaults standardUserDefaults]
        floatForKey:@"chat.avatar.corner.radius"];
    // ★ 默认值 = 1.0 (而非文档中旧写的 0.1)
    if (cornerRadius == 0) cornerRadius = 1.0;

    // ★ 公式: size * 0.5 * (cornerRadius / 100.0)
    return size * 0.5 * (cornerRadius / 100.0);
}
```

| cornerRadius 配置 | 公式 | 30px 头像的结果 | 效果 |
|-------------------|------|---------------|------|
| 1.0 (默认) | `30 * 0.5 * 0.01` | 0.15 | 近乎无圆角 |
| 50.0 | `30 * 0.5 * 0.5` | 7.5 | 小圆角 |
| 100.0 | `30 * 0.5 * 1.0` | 15.0 | **完美圆形** |
| 200.0 | `30 * 0.5 * 2.0` | 30.0 | 超大圆角 (最大值) |

---

## 十一、昵称宽度计算 — calculateNameWidth (L12575)

```objc
- (CGFloat)calculateNameWidth {
    // ① 获取联系人 (与 updateAvatars 相同逻辑)
    id chatController = [self chatController];  // weak, load 后使用
    id contact = [chatController GetContact];
    if (!contact) return 30.0;

    NSString *nickname = [contact m_nsNickName];

    // ② 构建完整显示文本 (含后缀)
    NSString *fullText = nickname;
    if (isGroup && showMemberCount) {
        fullText = [NSString stringWithFormat:@"%@ %ld%@",
            nickname, memberCount, suffix];
    } else if (isC2C && showAddTime) {
        fullText = [NSString stringWithFormat:@"%@ %ld%@",
            nickname, days, suffix];
    }

    // ③ 用 NSAttributedString 计算渲染宽度
    CGFloat fontSize = [d floatForKey:@"chat.name.font.size"] ?: 9.0;
    UIFont *font = [UIFont systemFontOfSize:fontSize];
    NSDictionary *attrs = @{NSFontAttributeName: font};
    CGFloat textWidth = [fullText sizeWithAttributes:attrs].width;

    // ④ 边界 clamp
    // max(nameTextWidth + 4, min(viewWidth - avatarSize - 20, 30))
    CGFloat minW = self.bounds.size.width - avatarSize - 20;
    if (minW > 30.0) minW = 30.0;
    return MAX(textWidth + 4.0, minW);
}
```

---

## 十二、头像点击交互

### 12.1 playHapticFeedback (L12993)

```objc
- (void)playHapticFeedback {
    if (![[NSUserDefaults standardUserDefaults]
          boolForKey:@"chat.avatar.click.feedback.enabled"]) return;

    UIImpactFeedbackGenerator *generator = [[UIImpactFeedbackGenerator alloc]
        initWithStyle:0];  // UIImpactFeedbackStyleLight
    [generator prepare];
    [generator impactOccurred];
}
```

### 12.2 弹窗类 — CSUserInfoPopoverController

> 对齐 `123456.c` class_t 引用

```
onLeftAvatarTapped: / onRightAvatarTapped:
├── chat.avatar.click.feedback.enabled? → playHapticFeedback
│
├── 获取联系人
│     ├─ 对方: [self.chatController GetContact]
│     └─ 自己: CContactMgr.getSelfContact
│
└── CSUserInfoPopoverController (气泡弹窗)
      ├── [self findViewController]  遍历 responder chain 到 UIViewController
      ├── presentViewController: CSUserInfoPopoverController
      └── 显示联系人详情页 (头像/昵称/微信号/备注等)
```

---

## 十三、后台静默加载 — silentLoadContactExtInfo: (L11926)

```objc
- (void)silentLoadContactExtInfo:(id)contact {
    // ① 获取 ContactInfoViewController (微信原生)
    id infoVC = [[objc_getClass("ContactInfoViewController") alloc] init];
    [infoVC setM_contact:contact];

    // ② 触发 viewDidLoad (加载联系人扩展信息)
    [infoVC view];  // 强制加载视图

    // ③ 延迟 0.8s 后关闭 (给网络请求足够时间)
    dispatch_after(dispatch_time(DISPATCH_TIME_NOW, 0.8 * NSEC_PER_SEC),
                   dispatch_get_main_queue(), ^{
        [infoVC dismissViewControllerAnimated:NO completion:nil];
    });
}
```

> **作用**：打开聊天时在后台预加载联系人的详细信息（如备注名、头像、朋友圈等），
> 避免用户点击头像时等待加载，提升体验流畅度。

---

## 十四、设置页面 — CSChatTitleSettingsViewController

### 14.1 实例变量

| 属性 | 类型 | 说明 |
|------|------|------|
| `sections` | NSMutableArray | sections 数据源 |
| `displayModeItem` | CSSettingItem | 显示模式行 (用于刷新文本) |
| `separatorIconItem` | CSSettingItem | 分隔符图标行 (用于显示预览) |

### 14.2 viewDidLoad (L54166)

```
viewDidLoad
├── [super viewDidLoad]
├── UINavigationBarAppearance 配置 (transparent/backgroundColor)
├── title = "聊天顶栏" (cf_J_Yvhn_)
├── tableView.backgroundColor + separatorStyle
├── registerClass: [CSSettingTableViewCell class]
├── [self setupData]     ← 构建数据源
├── [self loadSeparatorIcon]  ← 加载分隔符预览
└── [self loadBlacklistData]  ← 读取黑名单
```

### 14.3 设置页 Section 排布（动态行数）

```
总开关 ON 时显示的完整列表：

Section 0: 基础 (始终显示)
├── 显示聊天头像  (UISwitch) → chat.avatar.display.enabled
├── 点击反馈      (UISwitch) → chat.avatar.click.feedback.enabled
│     ★ 首次安装默认 ON (valueForKey==nil → setBool:YES)
├── 添加时间      (UISwitch) → chat.show.addtime.enabled
└── 群成员数      (UISwitch) → chat.show.group.member.count.enabled

Section 1: 布局 (仅开关ON时显示)
├── 显示模式      → showDisplayModeOptions (8选项弹窗)
├── 头像尺寸      → UIAlertController textField → chat.avatar.size
├── 头像间距      → UIAlertController textField → chat.avatar.spacing
├── 头像圆角      → UIAlertController textField → chat.avatar.corner.radius
├── 视图宽度      → UIAlertController textField → chat.title.view.width.v2
├── 昵称字号      → UIAlertController textField → chat.name.font.size
├── 昵称垂直偏移  → UIAlertController textField → chat.name.position.offset
├── 昵称水平偏移  → UIAlertController textField → chat.name.horizontal.offset
└── 整体水平偏移  → UIAlertController textField → chat.horizontal.offset

Section 2: 分隔符 (仅开关ON时显示)
├── 分隔符图标    → [self selectSeparatorIcon]  (UIImagePickerController)
├── 分隔符GIF     → [self selectSeparatorGIF]   (文件选择器)
├── 分隔符文字    → UIAlertController textField → chat.separator.text
└── 分隔符尺寸    → UIAlertController textField → chat.separator.size

Section 3: 昵称增强 (仅开关ON时显示)
├── 添加时间后缀  → UIAlertController textField → chat.addtime.suffix.format
├── 群成员数后缀  → UIAlertController textField → chat.group.member.count.suffix.format
├── 头像黑名单    → [self editBlacklist]  (多行文本编辑器)
└── 清空分隔符    → [self clearAllSeparators]
```

### 14.4 显示模式选择弹窗

```
showDisplayModeOptions (L55316)
├── UIAlertController (actionSheet 风格)
│   ├── ✓ 仅对方头像 (mode=3, 默认)
│   ├──   仅自己头像 (mode=2)
│   ├──   对方头像+昵称 (mode=0)
│   ├──   默认模式 (mode=4)
│   ├──   简单模式 (mode=1)
│   ├──   左头像+分隔符 (mode=5)
│   ├──   右头像+昵称 (mode=6)
│   ├──   重叠模式 (mode=7)
│   └──   取消
│
└─ 选择回调:
      [d setInteger:mode forKey:@"chat.display.mode"];
      [d synchronize];
      displayModeItem.detailText = 模式名称;
      [tableView reloadData];
```

### 14.5 displayModeItem 详情文本更新 (L55640)

```objc
- (void)setDisplayMode:(NSInteger)mode {
    NSArray *labels = @[
        @"对方头像+昵称",    // 0
        @"简单模式",         // 1
        @"仅自己头像",       // 2
        @"仅对方头像",       // 3
        @"默认模式",         // 4
        @"左头像+分隔符",    // 5
        @"右头像+昵称",      // 6
        @"重叠模式"          // 7
    ];
    self.displayModeItem.detailText = labels[mode];
}
```

---

## 十五、数据流全链路 (端到端)

```
┌───────────────────────────────────────────────────┐
│ ① 用户: CSChatTitleSettingsVC → 打开开关            │
│    设置 mode=2, size=40, separator.text="♥"        │
└────────────────────┬──────────────────────────────┘
                     │ NSUserDefaults
                     ▼
┌───────────────────────────────────────────────────┐
│ ② 存储: chat.avatar.display.enabled=YES            │
│         chat.avatar.size=40.0                      │
│         chat.display.mode=2                        │
│         chat.separator.text="♥"                    │
└────────────────────┬──────────────────────────────┘
                     │ 切换聊天 / 后台恢复
                     ▼
┌───────────────────────────────────────────────────┐
│ ③ viewWillAppear: Hook (FUN_00014ed0)              │
│    → enabled=YES, 非黑名单                          │
│    → currentTitleView == CSChatAvatarTitleView?     │
│       NO → 创建: alloc+initWithFrame(210,45)        │
│            setChatController:self (weak)            │
│            navigationItem.titleView = view          │
│    → [view updateAvatars]                          │
│    → [view layoutSubviews]                         │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ ④ layoutSubviews (mode=2: 完整布局)                │
│    leftAvatarView:  x=calc, y=avatarY, 40x40       │
│    separatorTextLabel: "♥" 居中                    │
│    separatorView: 预留30x30 图标位                  │
│    rightAvatarView: x+间距, y=avatarY, 40x40       │
│    titleLabel: 昵称计算宽度，隐藏                   │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ ⑤ updateAvatars                                  │
│    opponentWxid → HBWechatHelper/UserHeadImage/xxx.jpg  │
│    selfWxid     → WeChat native CContactMgr       │
│    dispatch_async(main) { left.image, right.image }│
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌───────────────────────────────────────────────────┐
│ ⑥ 渲染结果（导航栏中显示）：                        │
│   ┌──────┐  ♥  ┌──────┐                           │
│   │对方   │ ──→ │自己   │                           │
│   │头像   │     │头像   │                           │
│   └──────┘     └──────┘                           │
└───────────────────────────────────────────────────┘
```

---

## 十六、完整函数/方法索引 (123456.c)

### 16.1 CSChatAvatarTitleView

| 行号 | 函数/方法 | 说明 |
|------|----------|------|
| L404 | `class_t` | 类定义，6 个 ivars |
| L448-L453 | instance variables | `leftAvatarView`, `rightAvatarView`, `separatorView`, `separatorTextLabel`, **`titleLabel`**, **`chatController`** |
| L10924 | `initWithFrame:` | 初始化 + setupSubviews |
| L10948 | `setupSubviews` | 创建 5 个子视图 |
| L11130 | `layoutSubviews` | 8 模式布局 |
| L11673 | `updateAvatars` | 加载头像 + 昵称构建 |
| L11926 | `silentLoadContactExtInfo:` | 后台静默加载 |
| L11974 | `getSelfWxid` | 获取自己微信ID |
| L12063 | `loadAvatarWithPriorityForWxid:` | 头像加载优先级 |
| L12102 | `loadCustomAvatarForWxid:` | 自定义头像 (gif/jpg) |
| L12180 | `loadWeChatAvatarForWxid:` | 微信原生头像 |
| L12229 | `loadSeparatorIcon` | 加载分隔符图标 |
| L12356 | `loadSeparatorText` | 加载分隔符文字 |
| L12425 | `updateFontSizes` | 更新字体 → setNeedsLayout |
| L12504 | `applyPositionOffset` | 整体位移 |
| L12552 | `calculateCornerRadiusForSize:` | `size*0.5*(r/100)` |
| L12575 | `calculateNameWidth` | 昵称宽度计算 |
| L12780 | `onLeftAvatarTapped:` | 左头像点击 |
| L12809 | `onRightAvatarTapped:` | 右头像点击 |
| L12993 | `playHapticFeedback` | UIImpactFeedbackGenerator |
| L13011 | `leftAvatarView` getter | 返回 `_leftAvatarView` |
| L13020 | `setLeftAvatarView:` | 设置 `_leftAvatarView` |
| L13031 | `rightAvatarView` getter | 返回 `_rightAvatarView` |
| L13040 | `setRightAvatarView:` | 设置 `_rightAvatarView` |
| L13051 | `separatorView` getter | 返回 `_separatorView` |
| L13060 | `setSeparatorView:` | 设置 `_separatorView` |
| L13071 | `separatorTextLabel` getter | 返回 `_separatorTextLabel` |
| L13080 | `setSeparatorTextLabel:` | 设置 `_separatorTextLabel` |
| L13091 | `titleLabel` getter | 返回 `_titleLabel` |
| L13100 | `setTitleLabel:` | 设置 `_titleLabel` |
| L13113 | `chatController` getter | **weak** getter |
| L13124 | `setChatController:` | **weak** setter |
| L13139 | `_cxx_destruct` | ARC 析构 (release 所有 strong) |

### 16.2 Hook 函数（全局）

| 行号 | 函数 | 说明 |
|------|------|------|
| L13157 | `isContactInBlacklist` | 黑名单匹配 |
| L13272 | `FUN_00014c14` | Hook 注册 |
| L13285 | `FUN_00014c74` | viewDidLoad Hook |
| L13357 | `FUN_00014ed0` | viewWillAppear: Hook |

### 16.3 CSChatTitleSettingsViewController

| 行号 | 函数/方法 | 说明 |
|------|----------|------|
| L2110 | `class_t` | 类定义 |
| L54166 | `viewDidLoad` | 初始化 |
| L54227 | `loadSeparatorIcon` | 加载分隔符预览 |
| L54322 | `setupData` | 构建数据源 |
| L54959 | `numberOfSectionsInTableView:` | sections.count |
| L54977 | `tableView:numberOfRowsInSection:` | section[x].count |
| L55003 | `tableView:cellForRowAtIndexPath:` | cell 创建 |
| L55316 | `showDisplayModeOptions` | 8 选项弹窗 |
| L55640 | `setDisplayMode:` | mode 写入 + 刷新 |
| L58176 | `sections` getter | 返回 `_sections` |
| L58186 | `setSections:` | 设置 `_sections` |
| L58197 | `separatorIconItem` getter | 返回 `_separatorIconItem` |
| L58207 | `setSeparatorIconItem:` | 设置 `_separatorIconItem` |
| L58218 | `displayModeItem` getter | 返回 `_displayModeItem` |
| L58228 | `setDisplayModeItem:` | 设置 `_displayModeItem` |

---

## 十七、文档准确性纠正清单

| 位置 | 旧描述 | 纠正后 |
|------|--------|--------|
| 昵称标签 ivar | `_nameLabel` | **`_titleLabel`** (L452) |
| 昵称标签 getter | `nameLabel` | **`titleLabel`** (L13091) |
| 聊天控制器 ivar | `_baseMsgContentVC` (strong) | **`_chatController`** (weak, `_objc_storeWeak`, L453) |
| 圆角默认值 | `0` / `10%` | **`1.0`** → `size * 0.5 * 0.01` (L12552) |
| 圆角公式 | `直接返回radius值` | **`size * 0.5 * (radius / 100.0)`** (L12552) |
| calculateNameWidth | "计算宽度" | **昵称+后缀 → NSAttributedString → sizeWithAttributes → max(textW+4, clamp)** (L12575) |
| loadCustomAvatar: | 未记录路径 | **`Documents/HBWechatHelper/UserHeadImage/{wxid}.gif` → `.jpg` 回退** (L12102) |
| loadWeChatAvatar: | 未记录实现 | **MMServiceCenter → CContactMgr → getContactByName:** (L12180) |
| silentLoadContactExtInfo: | 遗漏 | **ContactInfoViewController 后台创建，0.8s 后关闭** (L11926) |
| playHapticFeedback | 遗漏 | **UIImpactFeedbackGenerator style 0 (Light)** (L12993) |
| separatorTextLabel | 遗漏初始状态 | **hidden = YES (setupSubviews 中)** (L10948) |
| setters 全部遗漏 | 未记录 | **6属性 × 2 (getter/setter) + _cxx_destruct** (L13011-L13139) |
| CSUserInfoPopoverController | 遗漏 | **头像点击弹窗类** (L12780-L12809 引用) |