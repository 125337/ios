# 资料卡圆角解耦改造步骤

> **目标**：将资料卡圆角从资料卡背景功能中解耦，归入列表圆角功能；同时确保只开卡片背景时不会给所有 Cell 加圆角
> **效果**：
> - 关闭卡片背景 → 资料卡仍然有圆角（来自 MMUIButton 层）
> - 只开卡片背景 → 只有资料卡有圆角，其他 Cell 不受影响

---

## 1. 问题一：关掉卡片背景，资料卡无圆角

### 根因

[MMUIButton Hook L201-203](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L201-L203)：

```objc
if (!needsCardBg && needsCorner) {
    return;    // ← 只开圆角不开背景图时直接返回，APPLY_CORNER 永远走不到
}
```

### 修复

**Step 1**：删除这 3 行 early return

**Step 2**：用 `if (needsFullCardBg)` 包裹卡片背景专属操作（clearColor / FIX-WHITE / bgImageView），让它们只在开卡片背景时执行。APPLY_CORNER 始终执行。

详见下文第3节完整步骤。

---

## 2. 问题二（新）：只开卡片背景，所有 Cell 都被加圆角

### 根因

**Cell Hook 的入口条件太宽，且圆角代码没有二次检查。**

[Cell Hook L639](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L639) 入口判断：

```objc
if (!config.listCornerRadiusEnabled && !config.cardBgEnabled) {
    return;   // ← 两个都关才返回！
}
// 只要开了任意一个就进入 ↓
```

然后 [L734-777](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L734-L777) 的圆角代码**无条件执行**：

```objc
NSInteger cornerRadius = (NSInteger)config.listCellCornerRadius;
// ... indexPath / tableView 查找 ...
wp_applyStandardCorner(...)  // 或 wp_applyCornerForContacts(...)
// ↑ 没有 if (listCornerRadiusEnabled) 保护！
```

### 执行流分析

| 配置 | Cell Hook 入口 | 圆角代码执行？ | 结果 |
|:----:|:--------------:|:-------------:|:-----:|
| 圆角 ON + 卡片 ON | ✅ 进入 | ✅ 执行 | 所有 Cell 有圆角 ✅ |
| 圆角 ON + 卡片 OFF | ✅ 进入 | ✅ 执行 | 所有 Cell 有圆角 ✅ |
| **圆角 OFF + 卡片 ON** | **✅ 进入**（因为卡开了） | **❌ 也执行了!** | **所有 Cell 被加圆角 ❌** |
| 圆角 OFF + 卡片 OFF | ❌ 返回 | 不执行 | 原始样式 ✅ |

**问题本质**：Cell Hook 把"需要进入"和"需要加圆角"混为一谈了。`cardBgEnabled == YES` 应该只允许进入做透明化处理，不应该触发全量 Cell 的圆角逻辑。

### 影响范围

当只开卡片背景时，以下页面的 **所有 Cell** 都会被加圆角：
- 聊天列表
- 联系人列表
- 发现页
- "我"页面普通 Cell（支付、收藏等）

这些都不应该有圆角——只有资料卡本身（通过 MMUIButton Hook 的 APPLY_CORNER）应该有。

---

## 3. 完整实施步骤（两个问题一起修）

### Step 1：MMUIButton Hook — 删除 early return

文件：[ListCornerRadiusHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m)
位置：`replaced_MMUIButton_layoutSubviews` 方法，[L201-203](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L201-L203)

```objc
// 删除这三行:
// if (!needsCardBg && needsCorner) {
//     return;
// }
```

### Step 2：MMUIButton Hook — 包裹卡片背景专属操作

位置：L274（`// ── 背景图分支 ──`）之前 加 `if (needsFullCardBg) {`

位置：L498（异步加载块结束 `}` 之后）之前 加 `} // end needsFullCardBg`

```objc
BOOL needsFullCardBg = needsCardBg;

if (needsFullCardBg) {
    // ── 背景图分支 ──
    ((UIView *)self).backgroundColor = [UIColor clearColor];
    // FIX-WHITE 遍历...
    // bgImageView 创建...
    // 异步加载图片...
}
// APPLY_CORNER 及之后的代码保持不变（始终执行）
```

### ★ Step 3：Cell Hook — 圆角代码加 listCornerRadiusEnabled 守卫 ★

文件同上，`replaced_MMUITableViewCell_layoutSubviews` 方法
位置：[L734](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L734) 圆角代码段

**当前代码（有问题）：**

```objc
// L734: 无条件执行!
NSInteger cornerRadius = (NSInteger)config.listCellCornerRadius;
if (cornerRadius == 0) cornerRadius = 18;
// ... indexPath / tableView 查找 ...
// ... wp_applyStandardCorner 或 wp_applyCornerForContacts ...
```

**改为：**

```objc
// ★ 只有开启列表圆角时才在 Cell 层设置圆角 ★
if (config.listCornerRadiusEnabled) {
    NSInteger cornerRadius = (NSInteger)config.listCellCornerRadius;
    if (cornerRadius == 0) cornerRadius = 18;

    // ... indexPath / tableView 查找（现有代码不变）...

    if (isContacts) {
        [ListCornerRadiusHook wp_applyCornerForContacts:cellView ...];
    } else {
        [ListCornerRadiusHook wp_applyStandardCorner:cellView ...];
    }
}
// listCornerRadiusEnabled == YES 时：所有 Cell 有圆角 ✅
// listCornerRadiusEnabled == NO 时：Cell 层不做圆角 ✅
// （资料卡的圆角由 MMUIButton Hook 的 APPLY_CORNER 负责）
```

**具体操作**：在 `NSInteger cornerRadius = ...` 这行（约 L734）之前加一行 `if (config.listCornerRadiusEnabled) {`，在 `wp_applyStandardCorner` / `wp_applyCornerForCalls` 调用块的闭合 `}` 之后加对应的 `}`。

注意：indexPath / tableView 查找代码（L740-752）也要包进去，因为它们只为圆角服务。

---

## 4. 改造后的完整执行流

### Cell Hook

```
进入
  │
  ├─ !corner && !cardBg? → return        // 都关才退出
  ├─ VC / skipCheck 过滤
  │
  ├─ margin 代码                         // listCornerRadiusEnabled 时执行
  ├─ orig()
  ├─ bgColor 设置                        // listCornerRadiusEnabled 时执行
  │
  ├─ ★ if (listCornerRadiusEnabled) ★    // ★ 新增守卫!
  │   ├── indexPath / tableView 查找
  │   └── wp_applyStandardCorner         // 只给需要圆角的 Cell 加
  │
  └─ needsCardBgTransparency?
      YES → 透明化 + return              // 仅资料卡 Cell
      NO  → masksToBounds=YES            // 普通 Cell
```

### MMUIButton Hook

```
进入
  │
  ├─ !corner && !cardBg? → orig + return
  ├─ orig()
  ├─ （无 early return）
  │
  ├─ VC / HeadImage / height 过滤（4关）
  │
  ├─ if (needsFullCardBg)               // 即 cardBgEnabled
  │   ├── clearColor
  │   ├── FIX-WHITE
  │   └── bgImageView + 异步加载
  │
  └─ APPLY_CORNER                        // 姭终执行
      ├── wp_applyProfileCardCorner     // 圆角 + 裁剪 + 背景/边框
      └── masksToBounds
```

---

## 5. 验证清单

| # | 测试场景 | Cell 层圆角 | MMUIButton 层圆角 | 背景图 |
|:-:|:---------|:----------:|:-----------------:|:-----:|
| 1 | 圆角 ON + 卡片 ON | ✅ 全部 Cell | ✅ 资料卡 | ✅ |
| 2 | 圆角 ON + 卡片 **OFF** | ✅ 全部 Cell | ✅ 资料卡 | — |
| 3 | **圆角 OFF + 卡片 ON** | **❌ 不加** | **✅ 仅资料卡** | **✅** |
| 4 | 圆角 OFF + 卡片 OFF | ❌ 不加 | ❌ 不加 | — |
| 5 | "我"页面普通 Cell（场景3） | ❌ 方形 ✅ | — | — |
| 6 | 聊天列表（场景3） | ❌ 原始样式 ✅ | — | — |

---

## 6. 回滚方案

共改动 ~15 行：
- MMUIButton Hook：删 3 行 + 加 2 行大括号 + 1 行标志变量
- Cell Hook：加 1 行 `if (config.listCornerRadiusEnabled)` { + 1 行对应 `}`
