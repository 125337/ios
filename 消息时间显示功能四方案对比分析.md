# 消息时间显示功能四方案对比分析

> 分析日期：2026-05-13
> 本文仅对比**消息时间显示**这一个功能。
>
> | 插件 | 大小 | 分析方式 |
> |------|------|---------|
> | **WeChatPlugin**（本项目） | 源码 | 100%确定 |
> | **微信助手 3.9-5** | ~12MB dylib | `strings` 二进制分析 |
> | **微信优化 1.6.5** | ~1.3MB dylib | `strings` 二进制分析 |
> | **锤子助手 2.4.1** | ~14MB dylib | `strings` 二进制分析 |
>
> 通过 WeChat 8.0.60 主二进制（459MB arm64）验证了实际属性名。

---

## 一、Hook 点对比

| Hook 点 | WeChatPlugin | 微信助手 | 微信优化 | 锤子助手 |
|---------|:-----------:|:--------:|:--------:|:--------:|
| `layoutSubviews` | ✅（标签更新）| ✅（轻量，仅更新frame） | ✅ | ✅ |
| `layoutContentView` | ❌ | ❌ | ✅ | ❌ |
| `layoutInternal`（WeChat私有）| ❌ | ❌ | ✅ | ❌ |
| `prepareForReuse` | ✅（清理）| ❌ | ✅ | ❌ |
| `tableView:willDisplayCell:` | ✅ | ✅ | ✅ | ✅ |
| `collectionView:willDisplayCell:` | ✅ | ✅ | ❌ | ❌ |
| `viewDidLayoutSubviews` | ❌ | ❌ | ✅ | ❌ |
| `viewWillLayoutSubviews` | ❌ | ❌ | ❌ | ✅ |
| 防护总层数 | **3层** | **2层** | **5层** | **2层** |

**微信优化**防护最多，`layoutInternal` 是其独有的 WeChat 私有方法 hook，在气泡创建完成后触发，获取气泡 bgImageView 的最佳时机。

---

## 二、头像获取方式

| 插件 | 方式 | 说明 |
|------|------|------|
| **WeChatPlugin** | `headImgView` → `avatarView` → `headImg` → selectors → subviews | 多路 fallback，兼容性最好 |
| **微信助手** | `headImg`（`T@"UIImageView",&,N,V_headImg`）+ `getHeadImageView` | 旧版正式属性 |
| **微信优化** | `leftAvatarView`/`rightAvatarView` 正式属性 + `createAvatarImageView` | 正式属性+自定义创建 |
| **锤子助手** | `headImg` 作为 `NSString`（头像 URL，非 UIImageView） | 不依赖头像视图 |

### WeChat 8.0.60 实际头像属性名

通过分析 WeChat 8.0.60 可执行文件的 ObjC 元数据段，确认：

```objc
T@"MMHeadImageView",&,N,V_headImgView    // 聊天头像（继承 UIImageView）
T@"MMWebImageView",&,N,V_headImgView     // 网络加载头像
T@"UIImageView",&,N,V_headImgView        // 标准 UIImageView
T@"WCFinderHeadImageView",&,N,V_headImgView // 视频号头像

// 另存为 ivar 引用（非属性声明）
m_headImg      // 头像数据
m_headImgView  // 头像视图（同 headImgView）
```

**头像属性名称演变**：
- 旧版：`headImg`（`UIImageView` 属性）
- **8.0.60：`headImgView`**（`MMHeadImageView` 属性）

---

## 三、气泡获取方式

| 插件 | 方式 | 依赖气泡？| 说明 |
|------|------|:--------:|------|
| **WeChatPlugin** | `bubbleView` selector → `bgImageView` KVC → subviews | ✅ | 8种位置需要气泡 frame |
| **微信助手** | 无气泡获取 | ❌ | 仅基础定位（头像下方），不需要气泡 |
| **微信优化** | `getBgImageView` + `setFrameForBgImageView:` + `layoutInternal` 时机 | ✅ | 需要气泡（扩展+圆角）|
| **锤子助手** | 无气泡获取 | ❌ | 简单时间模式，不需要气泡 |

### WeChat 8.0.60 实际气泡属性名

```objc
T@"UIImageView",&,N,V_bgImageView       // 气泡背景图（属性声明）
-[CommonMessageCellView setFrameForBgImageView:]  // 设置气泡frame的方法
```

**气泡属性名称演变**：
- 旧版：`m_bgImageView`
- **8.0.60：`bgImageView`**

### 为何难以找到气泡/头像

WeChat 8.0.60 的 Cell 视图结构：

```
ChatTableViewCell
  └── UITableViewCellContentView (contentView)
        └── TextMessageCellView (cellView，0个子视图，自行绘制内容)
```

`TextMessageCellView` 有 0 个子视图——它内部使用 CoreAnimation layers 绘制，`bgImageView` 是懒加载的，在 `layoutInternal` 时才创建。`willDisplayCell` 触发时气泡还没创建，所以 KVC 返回 nil。

---

## 四、配置项完整对比

| 配置项 | WeChatPlugin | 微信助手 | 微信优化 | 锤子助手 |
|--------|:-----------:|:--------:|:--------:|:--------:|
| **开关** | `showMessageTime` | `mIsMessageTime` | `messageTime.enabled` | ❓ |
| **时间格式** | 自定义占位符 | `mMsgDateFormat` | `messageTime.customFormat` | `timeFormat` |
| **字体大小** | `messageTimeFontSize` | `mMsgDateFontSize` | `messageTime.fontSize` | ❓ |
| **粗体** | `messageTimeBoldFont` | `mMsgDateBoldFont` | `messageTime.boldFont` | ❓ |
| **文字颜色** | 统一色 | 统一色 | **发送/接收×深浅=4色** | ❓ |
| **背景色** | ❌ | ❌ | **发送/接收×深浅=4色** | ❓ |
| **圆角** | ❌ | ❌ | ✅`cornerRadius` | ❓ |
| **位置** | 0~7（8种） | `mMsgDateShowPosition` | `position` | `timeStyle` |
| **X偏移** | ✅ | ❌ | ✅ | ❓ |
| **Y偏移** | ✅ | ❌ | ✅ | ❓ |
| **气泡扩展** | ✅ | ❌ | ✅ | ❓ |
| **隐藏时间线** | ✅ | `mIsHiddenMessageTime` | ✅ | ❓ |
| **联系人后缀** | ✅ | ❌ | ✅ | ❓ |
| **配置总数** | **11项** | **6项** | **17项** | ~2项 |

> ❓ = 无法从 strings 确定

---

## 五、时序问题分析

### 问题现象
进入聊天时时间标签不显示，需要上下滚动后才出现。

### 根因
`willDisplayCell` 触发时气泡 `bgImageView` 还没创建（懒加载），但 `layoutSubviews` 我们之前改成了空实现（微信助手方案），导致标签创建后无法获取正确气泡 frame。

### 各方案处理

| 插件 | 处理方法 | 原理 |
|------|---------|------|
| **微信助手** | 无此问题 | layoutSubviews 只更新 frame，不找气泡。定位不依赖气泡，所以没有问题 |
| **微信优化** | 多层防护 | hook `layoutInternal`（气泡创建后才触发），保证获取 bgImageView 时机正确 |
| **锤子助手** | 无此问题 | 定位简单，不依赖气泡/头像视图 |
| **WeChatPlugin** | **双阶段**（当前方案） | 1. `willDisplayCell` → 用 cellView frame 创建标签（立刻显示）<br>2. `layoutSubviews` → 气泡已创建，更新精确位置 |

---

## 六、发送者检测

| 方法 | WeChatPlugin | 微信助手 | 微信优化 | 锤子助手 |
|------|:-----------:|:--------:|:--------:|:--------:|
| `isSenderFromMsgWrap:` | ✅ | ✅ | ✅ | ✅ |
| `isSender`（BOOL属性） | ❌ | ❌ | ✅ | ❌ |
| 气泡位置兜底 | ✅ | ❌ | ❌ | ❌ |

---

## 七、代码量/复杂度

| 插件 | 消息时间功能代码 | 特征 |
|------|:--------------:|------|
| **WeChatPlugin** | ~850行 | 单文件，功能最丰富（8位置+偏移+占位符+隐藏时间线+联系人后缀）|
| **微信助手** | 3个类 | 配置/工具/UI分离，架构规范但功能基础 |
| **微信优化** | 设置页+配置项 | 样式最丰富（配色+圆角），不是独立模块 |
| **锤子助手** | ~几十行 | 最简单的时间格式/样式切换 |

---

## 八、综合排名（消息时间功能）

| 排名 | 插件 | 优势 | 短板 |
|:---:|------|------|------|
| 🥇 | **微信优化 1.6.5** | 配色最丰富（12个UIColor）、圆角、5层防护、`applyPositionOffset` 独立方法 | Hook `layoutInternal` 依赖私有API，版本兼容性差 |
| 🥈 | **WeChatPlugin** | 位置最多（8种）、占位符体系、隐藏时间线彻底、双阶段兼容性好 | 无设置页、格式不持久化（Bug #1） |
| 🥉 | **微信助手 3.9-5** | 架构规范、配置持久化完善 | 功能最基础 |
| 4 | **锤子助手 2.4.1** | 最简单，定位直接 | 功能最少，仅格式/样式切换 |

### WeChatPlugin 改进清单

**已修复**：
1. ✅ `willDisplayCell` Hook（借鉴微信助手）
2. ✅ `collectionView:willDisplayCell:` Hook（借鉴微信助手）
3. ✅ `prepareForReuse` 清理（防重复标签）
4. ✅ 头像属性优先 `headImgView`（WeChat 8.0.60 实际属性名）
5. ✅ 气泡属性修正为 `bgImageView`（非 `m_bgImageView`）
6. ✅ 双阶段方案（willDisplayCell 创建 + layoutSubviews 更新位置）
7. ✅ `isSenderFromMsgWrap:` 支持 cellView 优先（微信助手方案）

**待修复**：
1. 🔴 Bug #1：`_messageTimeFormat` 硬编码，不持久化
2. 🟡 Bug #2：`{b}` 占位符只匹配精确 12:00/00:00
3. 🟢 `dateFormat` 每次设置（应缓存）
4. 🟢 视图每次 layout 都遍历（应缓存）
