# MioPlugin · 列表圆角功能 — 实现步骤文档

> **分析来源**: [列表圆角功能实现分析.md](file:///www/wwwroot/ios/列表圆角功能实现分析.md)（微信优化反编译分析）  
> **分析日期**: 2026-05-31  
> **项目**: MioPlugin (`/www/wwwroot/ios/MioPlugin`)

---

## 零、当前完成度总览

| 组件 | 文件位置 | 状态 | 说明 |
|------|----------|:---:|------|
| **配置属性声明** | [PluginConfig.h](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.h#L149-L160) | ✅ 已完成 | 12 个属性已声明 |
| **设置页面 UI** | [SettingListCornerRadiusController.m](file:///www/wwwroot/ios/MioPlugin/Settings/Controllers/SettingListCornerRadiusController.m) | ✅ 已完成 | 完整的交互式设置页面 |
| **设置入口注册** | [WPUIVC.m](file:///www/wwwroot/ios/MioPlugin/Modules/SettingEntry/WPUIVC.m) | ✅ 已完成 | 已链入导航 |
| **边框工具类** | [WPBorderLayer.h](file:///www/wwwroot/ios/MioPlugin/Settings/Common/WPBorderLayer.h) / [WPBorderLayer.m](file:///www/wwwroot/ios/MioPlugin/Settings/Common/WPBorderLayer.m) | ✅ 已完成 | CAShapeLayer 边框 + 暗黑模式 |
| **配置加载/持久化** | [PluginConfig.m](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.m) | ❌ 未完成 | loadDefaults / save 均缺少列表圆角属性 |
| **运行时 Hook** | 无 | ❌ 未完成 | 需要新建 `ListCornerRadiusHook.m` |
| **Hook 注册入口** | 无 | ❌ 未完成 | 需要在初始化流程中添加 |

---

## 一、实现步骤总览

```
Step 1 ──→ 补全 PluginConfig.m
           ├─ 1a: loadDefaults 添加 12 个属性的读取
           └─ 1b: save 添加 12 个属性的写入

Step 2 ──→ 创建 ListCornerRadiusHook
           ├─ 2a: 新建 Modules/ListCornerRadius/ 目录
           ├─ 2b: 创建 ListCornerRadiusHook.h
           └─ 2c: 创建 ListCornerRadiusHook.m (核心 Hook 实现)

Step 3 ──→ 注册 Hook
           └─ 在项目初始化入口调用 initListCornerRadiusHook()

Step 4 ──→ 编译测试
           └─ 验证设置页 ↔ Hook 数据流通
```

---

## 二、Step 1 — 补全 PluginConfig.m

### 2a: 在 `loadDefaults` 末尾（`[d synchronize];` 之后，`}` 之前）添加

**文件**: [PluginConfig.m](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.m)  
**位置**: 约 L579 (`[d synchronize];` 之后，`}` 之前)

```objc
    // ========== 列表圆角配置 ==========
    _listCornerRadiusEnabled = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ListCornerRadiusEnabled"]];
    _listSearchCornerRadius = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ListSearchCornerRadius"]];
    _listHideRightQRCode = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ListHideRightQRCode"]];
    _listCellBorder = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ListCellBorder"]];

    CGFloat cr = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ListCellCornerRadius"]];
    _listCellCornerRadius = (cr > 0) ? cr : 18.0;

    CGFloat lm = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ListCellMargin"]];
    _listCellMargin = (lm > 0) ? lm : 9.0;

    CGFloat ps = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ListPinnedSessionTopSpacing"]];
    _listPinnedSessionTopSpacing = (ps > 0) ? ps : 15.0;

    CGFloat ns = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ListNormalSessionSpacing"]];
    _listNormalSessionSpacing = (ns > 0) ? ns : 15.0;

    NSString *clb = [d stringForKey:[kPluginPrefix stringByAppendingString:@"ListCellLightBgColor"]];
    _listCellLightBgColor = [clb copy];

    NSString *cdb = [d stringForKey:[kPluginPrefix stringByAppendingString:@"ListCellDarkBgColor"]];
    _listCellDarkBgColor = [cdb copy];

    NSString *clb2 = [d stringForKey:[kPluginPrefix stringByAppendingString:@"ListCardLightBgColor"]];
    _listCardLightBgColor = [clb2 copy];

    NSString *cdb2 = [d stringForKey:[kPluginPrefix stringByAppendingString:@"ListCardDarkBgColor"]];
    _listCardDarkBgColor = [cdb2 copy];
```

### 2b: 在 `save` 方法末尾（`[d synchronize];` 之前）添加

**文件**: [PluginConfig.m](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.m)  
**位置**: 约 L578（`if (_placeholderTextColorHex)...` 之后，`[d synchronize];` 之前）

```objc
    // ========== 列表圆角配置 ==========
    [d setBool:_listCornerRadiusEnabled forKey:[kPluginPrefix stringByAppendingString:@"ListCornerRadiusEnabled"]];
    [d setBool:_listSearchCornerRadius forKey:[kPluginPrefix stringByAppendingString:@"ListSearchCornerRadius"]];
    [d setBool:_listHideRightQRCode forKey:[kPluginPrefix stringByAppendingString:@"ListHideRightQRCode"]];
    [d setBool:_listCellBorder forKey:[kPluginPrefix stringByAppendingString:@"ListCellBorder"]];
    [d setFloat:_listCellCornerRadius forKey:[kPluginPrefix stringByAppendingString:@"ListCellCornerRadius"]];
    [d setFloat:_listCellMargin forKey:[kPluginPrefix stringByAppendingString:@"ListCellMargin"]];
    [d setFloat:_listPinnedSessionTopSpacing forKey:[kPluginPrefix stringByAppendingString:@"ListPinnedSessionTopSpacing"]];
    [d setFloat:_listNormalSessionSpacing forKey:[kPluginPrefix stringByAppendingString:@"ListNormalSessionSpacing"]];
    if (_listCellLightBgColor) [d setObject:_listCellLightBgColor forKey:[kPluginPrefix stringByAppendingString:@"ListCellLightBgColor"]];
    if (_listCellDarkBgColor) [d setObject:_listCellDarkBgColor forKey:[kPluginPrefix stringByAppendingString:@"ListCellDarkBgColor"]];
    if (_listCardLightBgColor) [d setObject:_listCardLightBgColor forKey:[kPluginPrefix stringByAppendingString:@"ListCardLightBgColor"]];
    if (_listCardDarkBgColor) [d setObject:_listCardDarkBgColor forKey:[kPluginPrefix stringByAppendingString:@"ListCardDarkBgColor"]];
```

### ✅ Step 1 验证

配置键名对照（注意 UI 中使用的 key 值）：

| PluginConfig 属性 | UI 中的 key | NSUserDefaults 实际 Key |
|---|------|------|
| `listCornerRadiusEnabled` | `@"listCornerRadiusEnabled"` | `Mio_ListCornerRadiusEnabled` |
| `listSearchCornerRadius` | `@"listSearchCornerRadius"` | `Mio_ListSearchCornerRadius` |
| `listHideRightQRCode` | `@"listHideRightQRCode"` | `Mio_ListHideRightQRCode` |
| `listCellBorder` | `@"listCellBorder"` | `Mio_ListCellBorder` |
| `listCellCornerRadius` | `@"listCellCornerRadius"` | `Mio_ListCellCornerRadius` |
| `listCellMargin` | `@"listCellMargin"` | `Mio_ListCellMargin` |
| `listPinnedSessionTopSpacing` | `@"listPinnedSessionTopSpacing"` | `Mio_ListPinnedSessionTopSpacing` |
| `listNormalSessionSpacing` | `@"listNormalSessionSpacing"` | `Mio_ListNormalSessionSpacing` |
| `listCellLightBgColor` | `@"listCellLightBgColor"` | `Mio_ListCellLightBgColor` |
| `listCellDarkBgColor` | `@"listCellDarkBgColor"` | `Mio_ListCellDarkBgColor` |
| `listCardLightBgColor` | `@"listCardLightBgColor"` | `Mio_ListCardLightBgColor` |
| `listCardDarkBgColor` | `@"listCardDarkBgColor"` | `Mio_ListCardDarkBgColor` |

> **注意**：`SettingListCornerRadiusController.m` 使用 `addInputRowInGroup:title:key:value:hint:` 和 `addSubSwitchRowInGroup:title:key:isOn:` 等方法，这些方法内部自动拼接 `kPluginPrefix`（`Mio_`）前缀来读写 NSUserDefaults，最终键名格式为 `Mio_<key>`。

---

## 三、Step 2 — 创建运行时 Hook 模块

### 3.1 创建目录和文件

```bash
mkdir -p /www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius
```

需要创建两个文件：

- [ListCornerRadiusHook.h](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.h)
- [ListCornerRadiusHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m)

### 3.2 ListCornerRadiusHook.h

```objc
#import <Foundation/Foundation.h>

@interface ListCornerRadiusHook : NSObject

+ (void)initListCornerRadiusHook;

@end
```

### 3.3 ListCornerRadiusHook.m — 完整实现

**核心设计思路**：

```
MMTableViewCell::layoutSubviews (Hook 点)
    │
    ├── ① 调用原始 layoutSubviews
    ├── ② 检查 PluginConfig.listCornerRadiusEnabled
    │       └── NO → 直接 return
    ├── ③ 查找父级 TableView → 获取 indexPath
    ├── ④ 确定 Cell 位置 (首行 / 中间行 / 末行 / 单行)
    ├── ⑤ 修改 frame (水平边距 + 会话间距)
    ├── ⑥ 设置 layer.cornerRadius + maskedCorners (按位置差异化)
    ├── ⑦ 设置 layer.backgroundColor (自定义颜色)
    ├── ⑧ 应用 WPBorderLayer 边框
    ├── ⑨ (可选) 隐藏二维码按钮
    └── ⑩ (可选) 搜索框圆角
```

完整代码：

```objc
#import "ListCornerRadiusHook.h"
#import "../../Config/PluginConfig.h"
#import "../../Settings/Common/WPBorderLayer.h"
#import "../../Core/LogManager.h"
#import <substrate.h>
#import <objc/runtime.h>
#import <objc/message.h>

// ============================================================
// 原始 IMP 存储
// ============================================================
static IMP _orig_MMTableViewCell_layoutSubviews = NULL;

// ============================================================
// 关联对象 Key
// ============================================================
static const void *kCornerRadiusAppliedKey = &kCornerRadiusAppliedKey;

// ============================================================
// 工具函数：查找父级 UIViewController
// ============================================================
static UIViewController *findParentViewController(UIView *view) {
    UIResponder *responder = view;
    while (responder) {
        if ([responder isKindOfClass:[UIViewController class]]) {
            return (UIViewController *)responder;
        }
        responder = [responder nextResponder];
    }
    return nil;
}

// ============================================================
// 工具函数：查找父级 UITableView
// ============================================================
static UITableView *findParentTableView(UIView *view) {
    UIView *superview = view.superview;
    while (superview) {
        if ([superview isKindOfClass:[UITableView class]]) {
            return (UITableView *)superview;
        }
        superview = superview.superview;
    }
    return nil;
}

// ============================================================
// 工具函数：从 Hex 字符串创建 UIColor
// ============================================================
static UIColor *colorFromHex(NSString *hexString, UIColor *defaultColor) {
    if (!hexString || hexString.length == 0) return defaultColor;

    NSString *clean = [hexString stringByReplacingOccurrencesOfString:@"#" withString:@""];
    if (clean.length != 6 && clean.length != 8) return defaultColor;

    unsigned int rgb = 0;
    NSScanner *scanner = [NSScanner scannerWithString:clean];
    [scanner scanHexInt:&rgb];

    CGFloat r = ((rgb >> 16) & 0xFF) / 255.0;
    CGFloat g = ((rgb >> 8) & 0xFF) / 255.0;
    CGFloat b = (rgb & 0xFF) / 255.0;
    CGFloat a = clean.length == 8 ? ((rgb >> 24) & 0xFF) / 255.0 : 1.0;

    return [UIColor colorWithRed:r green:g blue:b alpha:a];
}

// ============================================================
// 工具函数：判断当前是否为深色模式
// ============================================================
static BOOL isDarkMode(void) {
    if (@available(iOS 13.0, *)) {
        if ([UITraitCollection currentTraitCollection].userInterfaceStyle == UIUserInterfaceStyleDark) {
            return YES;
        }
    }
    return NO;
}

// ============================================================
// 工具函数：获取 Cell 在 Section 中的位置类型
// 返回值：
//   0 = 唯一 Cell (首行也是末行)
//   1 = 首行 (非末行)
//   2 = 中间行
//   3 = 末行 (非首行)
//   -1 = 无法获取
// ============================================================
static NSInteger cellPositionInSection(UITableViewCell *cell, UITableView *tableView) {
    if (!tableView) return -1;

    @try {
        NSIndexPath *indexPath = [tableView indexPathForCell:cell];
        if (!indexPath) return -1;

        NSInteger row = indexPath.row;
        NSInteger section = indexPath.section;
        NSInteger totalRows = [tableView numberOfRowsInSection:section];

        if (totalRows == 1) return 0;       // 唯一 Cell
        if (row == 0) return 1;             // 首行
        if (row == totalRows - 1) return 3;  // 末行
        return 2;                            // 中间行
    } @catch (NSException *e) {
        return -1;
    }
}

// ============================================================
// 核心 Hook 函数：MMTableViewCell::layoutSubviews
// ============================================================
static void replaced_MMTableViewCell_layoutSubviews(id self, SEL _cmd) {
    // ★ 先调用原始实现
    if (_orig_MMTableViewCell_layoutSubviews) {
        ((void (*)(id, SEL))_orig_MMTableViewCell_layoutSubviews)(self, _cmd);
    }

    // ★ 检查开关
    PluginConfig *config = [PluginConfig shared];
    if (!config.listCornerRadiusEnabled) return;

    // ★ 防重入标记
    if (objc_getAssociatedObject(self, kCornerRadiusAppliedKey)) return;
    objc_setAssociatedObject(self, kCornerRadiusAppliedKey,
                             @(YES), OBJC_ASSOCIATION_RETAIN_NONATOMIC);

    @try {
        UITableViewCell *cell = (UITableViewCell *)self;

        // ---- 0. 获取 TableView 和位置 ----
        UITableView *tableView = findParentTableView(cell);
        NSInteger position = cellPositionInSection(cell, tableView);
        if (position < 0) return;

        // ---- 1. 读取配置 ----
        CGFloat cornerRadius = config.listCellCornerRadius;  // 默认 18
        CGFloat margin       = config.listCellMargin;         // 默认 9
        CGFloat pinnedSpace  = config.listPinnedSessionTopSpacing;  // 默认 15
        CGFloat normalSpace  = config.listNormalSessionSpacing;     // 默认 15

        // ---- 2. 修改 Frame (水平边距) ----
        CGRect frame = cell.frame;
        frame.origin.x += margin;
        frame.size.width -= (margin * 2);

        // ---- 3. 修改 Frame (会话间距) ----
        // 判断 Cell 是否为置顶会话: 通过 cell 的背景色或 tag 区分
        // 简化：通过 indexPath.row 判断置顶区域
        NSIndexPath *indexPath = [tableView indexPathForCell:cell];
        BOOL isPinned = NO;
        if (@available(iOS 16.0, *)) {
            // iOS 16+ 无直接 isPinned API，用 position 推测
            isPinned = (position <= 2);
        }
        CGFloat spacing = isPinned ? pinnedSpace : normalSpace;
        frame.size.height -= spacing;

        cell.frame = frame;

        // ---- 4. 设置圆角 (按位置差异化) ----
        cell.layer.masksToBounds = YES;
        cell.layer.cornerRadius = cornerRadius;

        switch (position) {
            case 0: // 唯一 Cell → 四角全圆
                cell.layer.maskedCorners = kCALayerMinXMinYCorner
                                         | kCALayerMaxXMinYCorner
                                         | kCALayerMinXMaxYCorner
                                         | kCALayerMaxXMaxYCorner;
                break;
            case 1: // 首行 → 仅顶部两角
                cell.layer.maskedCorners = kCALayerMinXMinYCorner
                                         | kCALayerMaxXMinYCorner;
                break;
            case 2: // 中间行 → 无圆角
                cell.layer.cornerRadius = 0;
                cell.layer.maskedCorners = 0;
                break;
            case 3: // 末行 → 仅底部两角
                cell.layer.maskedCorners = kCALayerMinXMaxYCorner
                                         | kCALayerMaxXMaxYCorner;
                break;
        }

        // ---- 5. 设置 Cell 背景色 ----
        BOOL dark = isDarkMode();
        NSString *bgHex = dark ? config.listCellDarkBgColor : config.listCellLightBgColor;
        UIColor *defaultBg = dark
            ? [UIColor colorWithRed:0.125 green:0.125 blue:0.125 alpha:1.0]
            : [UIColor whiteColor];
        cell.backgroundColor = colorFromHex(bgHex, defaultBg);

        // ---- 6. 应用 WPBorderLayer 边框 ----
        if (config.listCellBorder) {
            // 清理旧边框层
            NSMutableArray *borderLayers = [cell wp_borderLayers];
            for (WPBorderLayer *layer in [borderLayers copy]) {
                [layer removeFromSuperlayer];
            }
            [borderLayers removeAllObjects];

            // 边框颜色
            UIColor *borderLightColor = colorFromHex(config.listCellLightBgColor,
                [UIColor colorWithRed:0.9 green:0.9 blue:0.9 alpha:1.0]);
            UIColor *borderDarkColor = colorFromHex(config.listCellDarkBgColor,
                [UIColor colorWithRed:0.25 green:0.25 blue:0.25 alpha:1.0]);

            // 按位置创建差异化边框层
            WPBorderSide sides = WPBorderSideNone;
            UIRectCorner roundedCorners = 0;

            switch (position) {
                case 0: // 唯一 Cell → 四边
                    sides = WPBorderSideAll;
                    roundedCorners = UIRectCornerAllCorners;
                    break;
                case 1: // 首行 → 上 + 左 + 右
                    sides = WPBorderSideTop | WPBorderSideLeft | WPBorderSideRight;
                    roundedCorners = UIRectCornerTopLeft | UIRectCornerTopRight;
                    break;
                case 2: // 中间行 → 左 + 右
                    sides = WPBorderSideLeft | WPBorderSideRight;
                    roundedCorners = 0;
                    break;
                case 3: // 末行 → 左 + 右 + 下
                    sides = WPBorderSideLeft | WPBorderSideRight | WPBorderSideBottom;
                    roundedCorners = UIRectCornerBottomLeft | UIRectCornerBottomRight;
                    break;
            }

            WPBorderLayer *borderLayer = [[WPBorderLayer alloc] init];
            borderLayer.sides = sides;
            borderLayer.borderWidth = 1.0;
            borderLayer.borderColorLight = borderLightColor;
            borderLayer.borderColorDark = borderDarkColor;
            borderLayer.cornerRadius = cornerRadius;
            borderLayer.roundedCorners = roundedCorners;
            borderLayer.leftInset = 0;
            borderLayer.hasBorder = YES;
            borderLayer.fillColor = [UIColor clearColor].CGColor;
            borderLayer.lineWidth = 1.0;
            borderLayer.strokeColor = dark ? borderDarkColor.CGColor : borderLightColor.CGColor;
            borderLayer.lineJoin = kCALineJoinRound;
            borderLayer.masksToBounds = NO;

            [borderLayer updatePathInRect:cell.bounds];
            [cell.layer addSublayer:borderLayer];
            [borderLayers addObject:borderLayer];
        }

        // ---- 7. 隐藏右侧二维码按钮 ----
        if (config.listHideRightQRCode) {
            // 查找 Cell 内二维码相关视图并隐藏
            for (UIView *subview in cell.subviews) {
                NSString *className = NSStringFromClass([subview class]);
                if ([className containsString:@"QRCode"] ||
                    [className containsString:@"Qrcode"] ||
                    [className containsString:@"QR"]) {
                    subview.hidden = YES;
                    subview.alpha = 0;
                }
            }
        }

        // ---- 8. 应用暗黑模式外观 ----
        [cell wp_updateBorderAppearanceForDarkMode:dark];

    } @catch (NSException *e) {
        WPLog(@"ListCornerRadius", @"[ERROR] layoutSubviews hook: %@", e);
    } @finally {
        objc_setAssociatedObject(self, kCornerRadiusAppliedKey,
                                 nil, OBJC_ASSOCIATION_RETAIN_NONATOMIC);
    }
}

// ============================================================
// 搜索框圆角 Hook
// ============================================================
static IMP _orig_UIView_layoutSubviews_forSearch = NULL;

static void replaced_UIView_layoutSubviews_forSearch(id self, SEL _cmd) {
    if (_orig_UIView_layoutSubviews_forSearch) {
        ((void (*)(id, SEL))_orig_UIView_layoutSubviews_forSearch)(self, _cmd);
    }

    PluginConfig *config = [PluginConfig shared];
    if (!config.listCornerRadiusEnabled || !config.listSearchCornerRadius) return;

    @try {
        UIView *view = (UIView *)self;
        UIViewController *vc = findParentViewController(view);
        if (!vc) return;

        NSString *className = NSStringFromClass([vc class]);

        // 微信搜索框页面
        if ([className isEqualToString:@"WCSearchController"] ||
            [className isEqualToString:@"FTSHomeViewController"]) {

            // 判断 view 尺寸是否为搜索框尺寸
            CGSize size = view.bounds.size;
            if (size.width > 200 && size.height > 30 && size.height < 60) {
                view.layer.cornerRadius = config.listCellCornerRadius;
                view.layer.masksToBounds = YES;
            }
        }
    } @catch (NSException *e) {
        // 静默
    }
}

// ============================================================
// 初始化入口
// ============================================================
@implementation ListCornerRadiusHook

+ (void)initListCornerRadiusHook {
    WPLog(@"ListCornerRadius", @"[INIT] Initializing ListCornerRadius hook...");

    // ---- Hook ①: MMTableViewCell::layoutSubviews ----
    Class MMTableViewCellClass = objc_getClass("MMTableViewCell");
    if (MMTableViewCellClass) {
        MSHookMessageEx(
            MMTableViewCellClass,
            @selector(layoutSubviews),
            (IMP)replaced_MMTableViewCell_layoutSubviews,
            &_orig_MMTableViewCell_layoutSubviews
        );
        WPLog(@"ListCornerRadius", @"[OK] Hook: MMTableViewCell::layoutSubviews");
    } else {
        WPLog(@"ListCornerRadius", @"[WARN] MMTableViewCell class not found!");
    }

    // ---- Hook ②: UIView::layoutSubviews (搜索框圆角) ----
    MSHookMessageEx(
        [UIView class],
        @selector(layoutSubviews),
        (IMP)replaced_UIView_layoutSubviews_forSearch,
        &_orig_UIView_layoutSubviews_forSearch
    );
    WPLog(@"ListCornerRadius", @"[OK] Hook: UIView::layoutSubviews (search box)");

    WPLog(@"ListCornerRadius", @"[INIT] ListCornerRadius hook initialized.");
}

@end
```

### 3.4 实现要点说明

| 要点 | 说明 |
|------|------|
| **Hook 目标** | `MMTableViewCell::layoutSubviews` — 微信自定义的 TableViewCell 基类 |
| **防重入** | 使用 `objc_setAssociatedObject` 标记，`@finally` 块中清理 |
| **位置判定** | 通过 `[tableView indexPathForCell:]` + `numberOfRowsInSection:` 精确判断 |
| **maskedCorners** | 唯一 Cell → 0xF(四角); 首行 → 0x3(顶角); 末行 → 0xC(底角); 中间 → 0x0 |
| **圆角关闭** | CornerRadius 设为 0 的 row，同时 `maskedCorners = 0` |
| **边框** | 复用已有的 [WPBorderLayer](file:///www/wwwroot/ios/MioPlugin/Settings/Common/WPBorderLayer.m) 工具类 |
| **搜索框** | 通过 `findParentViewController` 识别 `WCSearchController` / `FTSHomeViewController`，对特定尺寸的 View 应用圆角 |
| **错误处理** | 所有代码包裹在 `@try/@catch` 中，防止 Crash |
| **日志** | 使用项目统一的 `WPLog` 宏 |

---

## 四、Step 3 — 注册 Hook

需要在项目的全局初始化入口调用 `initListCornerRadiusHook`。

### 找到入口文件

根据现有项目结构（`substrate.h` 引入 + 其他 Hook 模块模式），找到 `%ctor` 或 `+load` 入口文件，添加：

```objc
#import "../Modules/ListCornerRadius/ListCornerRadiusHook.h"

// 在 %ctor 或 +load 中添加：
[ListCornerRadiusHook initListCornerRadiusHook];
```

### 常见入口位置（需要确认你的 Build Phase）

通常以下位置之一：

1. **统一入口类** —— 如 `MioPluginInit.m` 或 `PluginEntry.m`（如果有）
2. **主 Tweak.xm** —— 如果使用 Logos
3. **某个 +load 方法** —— 如查看哪个文件最先执行 Hook

---

## 五、Step 4 — 编译测试

### 5.1 编译验证清单

| 检查项 | 说明 |
|--------|------|
| ✅ ListCornerRadiusHook.h/.m 已加入 Build Phase | 确认文件被编译 |
| ✅ substrate.h 路径正确 | `#import <substrate.h>` |
| ✅ WPBorderLayer.h 路径正确 | `#import "../../Settings/Common/WPBorderLayer.h"` |
| ✅ PluginConfig 代码补全 | loadDefaults + save 均已添加 12 个属性 |
| ✅ LogManager.h 路径正确 | `#import "../../Core/LogManager.h"` |
| ✅ Objective-C ARC 设置 | 项目支持 ARC |
| ✅ iOS 版本 | 部署目标 >= iOS 8.0（maskedCorners 需要 iOS 11+） |

### 5.2 实机测试步骤

| 步骤 | 操作 | 预期结果 |
|:---:|------|------|
| 1 | 打开 MioPlugin 设置 → 列表圆角 | 看到完整的设置页面 |
| 2 | 开启 "圆角开关" | 子设置项展开显示 |
| 3 | 设置圆角半径 = 20，边距 = 12 | 输入框显示对应数值 |
| 4 | 退出设置，去微信消息列表 | Cell 显示为卡片式：圆角 20 + 边距 12 |
| 5 | 去发现页 | Cell 同样显示圆角卡片效果 |
| 6 | 去聊天页 | Cell 同样显示圆角效果 |
| 7 | 关闭开关，重启微信 | Cell 恢复原生样式 |
| 8 | 开启边框开关 | Cell 边缘出现边框线 |
| 9 | 修改 Cell 背景色 | Cell 背景变为自定义颜色 |
| 10 | 深色模式切换 | Cell 边框和背景自动适配 |

### 5.3 调试技巧

```objc
// 在 replaced_MMTableViewCell_layoutSubviews 中临时添加
WPLog(@"ListCornerRadius", @"[DEBUG] position=%ld radius=%.0f margin=%.0f frame=%@",
      (long)position, cornerRadius, margin, NSStringFromCGRect(cell.frame));
```

---

## 六、关键 NSUserDefaults Key 速查表

| Config 属性 | NSUserDefaults Key |
|------|------|
| `listCornerRadiusEnabled` | `Mio_ListCornerRadiusEnabled` |
| `listSearchCornerRadius` | `Mio_ListSearchCornerRadius` |
| `listHideRightQRCode` | `Mio_ListHideRightQRCode` |
| `listCellBorder` | `Mio_ListCellBorder` |
| `listCellCornerRadius` | `Mio_ListCellCornerRadius` |
| `listCellMargin` | `Mio_ListCellMargin` |
| `listPinnedSessionTopSpacing` | `Mio_ListPinnedSessionTopSpacing` |
| `listNormalSessionSpacing` | `Mio_ListNormalSessionSpacing` |
| `listCellLightBgColor` | `Mio_ListCellLightBgColor` |
| `listCellDarkBgColor` | `Mio_ListCellDarkBgColor` |
| `listCardLightBgColor` | `Mio_ListCardLightBgColor` |
| `listCardDarkBgColor` | `Mio_ListCardDarkBgColor` |

---

## 七、文件变更汇总

| 操作 | 文件 | 说明 |
|:---:|------|------|
| 🔧 修改 | `Config/PluginConfig.m` | loadDefaults + save 各添加 12 行 |
| ✨ 新增 | `Modules/ListCornerRadius/ListCornerRadiusHook.h` | Hook 入口头文件 |
| ✨ 新增 | `Modules/ListCornerRadius/ListCornerRadiusHook.m` | 核心 Hook 实现 (~280 行) |
| 🔧 修改 | 项目入口文件 (Tweak.xm 或 init 文件) | 添加 `[ListCornerRadiusHook initListCornerRadiusHook]` |

**无需修改的文件**（已完成）：

| 文件 | 原因 |
|------|------|
| `Config/PluginConfig.h` | 12 个属性声明已完成 |
| `Settings/Controllers/SettingListCornerRadiusController.m` | 设置页面 UI 已完成 |
| `Modules/SettingEntry/WPUIVC.m` | 已注册设置入口 |
| `Settings/Common/WPBorderLayer.h/.m` | 边框工具类已完成 |

---

## 八、参考分析文档

| 文档 | 路径 |
|------|------|
| Cell 列表圆角深度分析 | [列表圆角功能实现分析.md](file:///www/wwwroot/ios/列表圆角功能实现分析.md) |
| CSUserInfoPopoverController 对比 | [Popover对比分析_MioPlugin_vs_微信优化.md](file:///www/wwwroot/ios/MioPlugin/Modules/ChatTopBar/Popover对比分析_MioPlugin_vs_微信优化.md) |
| CSUserInfoPopoverController 深度分析 | [CSUserInfoPopoverController深度分析.md](file:///www/wwwroot/ios/插件/微信优化反编译最新/CSUserInfoPopoverController深度分析.md) |