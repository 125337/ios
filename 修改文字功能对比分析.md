# 修改文字功能对比分析：锤子助手 vs 我们的插件 (JokerHook)

## 概述

两个插件都提供了在微信聊天中长按文本消息后修改文字内容的功能。本文档从触发方式、修改原理、数据持久性、适用范围等多个维度进行深度对比分析。

---

## 一、触达入口对比

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用说明 | 谁更好 |
|---|---|---|---|---|
| 主要入口 | Hook `TextMessageCellView.operationMenuItems`，注入 `onTextJoker` 菜单项 | Hook `BaseMsgContentViewController.willShowMenuController:inMsgWrap:` 记录消息 + `MMScrollActionSheet.showInView:` 添加"修改文字"按钮 + `MMRichTextCoverView.showMenuWithItems:` 创建 `MMMenuItem` | 决定用户如何触发修改功能 | **锤子助手更好**：单一入口更简洁，直接 hook 菜单数据源方法注入菜单项，代码更干净，冲突风险更低。我们的插件 hook 了3个方法做同一件事，存在冗余和时序风险 |
| 菜单样式 | 使用 `MMMenuItem` 自定义菜单项，带图标 | 1) 在 `MMScrollActionSheet` 用 `NSDictionary@{@"title"}` 的方式添加按钮；2) 在 `MMRichTextCoverView` 通过 `MMMenuItem` 方式添加 | 菜单项在微信中的显示方式和兼容性 | **锤子助手更好**：统一使用 `MMMenuItem` 标准类加图标，与微信原生菜单风格一致。我们的插件混用了 NSDictionary 和 MMMenuItem 两种方式，兼容性有隐患 |
| 消息类型过滤 | 在 `operationMenuItems` 回调中通过 `WeChatTweakSettings` 开关控制 | 在 `willShowMenuController` 中检查 `m_uiMessageType == 1`（仅文本消息） | 限制哪些消息类型可以修改 | **锤子助手更好**：消息类型过滤由菜单方法本身控制（TextMessageCellView 本身只处理文本消息），逻辑更自然。我们手动过滤 msgType==1 虽然安全但多了一层判断 |

---

## 二、弹出编辑框对比

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用说明 | 谁更好 |
|---|---|---|---|---|
| 弹框组件 | 使用微信原生 `WCUIAlertView`，带 `tag=99999`，展示为 `UIAlertControllerStyleAlert` 风格 | 使用自定义的 `WPAlert` 组件 | 编辑框的UI质量和兼容性 | **锤子助手更好**：使用微信内置的 WCUIAlertView，与微信UI完全一致，不会有视觉割裂感。我们的 WPAlert 是自定义组件，可能在暗黑模式、字体缩放等方面存在适配问题 |
| 预填内容 | 获取当前 cellView 的 `m_nsContent` 并通过 `tipsVc.tipsTextView.text` 获取当前文本填入 | 通过 `[msgWrap performSelector:@selector(m_nsContent)]` 获取当前内容填入 | 编辑时是否显示原文 | **持平**：两者都正确获取并预填当前消息内容，体验一致 |
| 输入方式 | 通过 `WCUIAlertView` 的 `tipsVc.tipsTextView.text` 属性获取输入（带文本输入框的Alert） | 通过 `WPAlert showInput` 方法弹出带输入框的提示 | 用户输入体验 | **锤子助手更好**：WCUIAlertView 是微信内部经过大量测试的组件，输入体验更稳定。我们的 WPAlert 自定义实现可能存在键盘遮挡、输入限制等问题 |

---

## 三、核心修改逻辑对比（最关键差异）

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用说明 | 谁更好 |
|---|---|---|---|---|
| **修改 msgWrap 数据层** | **是** — 通过 `setM_nsContent:` 将新文本写入 `CMessageWrap` 的 `m_nsContent` 属性 | **否** — 完全不修改 `msgWrap` 的数据 | 决定修改是否保存到底层消息数据 | **锤子助手更好（关键）**：锤子助手写入了 `m_nsContent`，修改是持久化的（除非消息被重新加载覆盖）。我们的插件只改了显示层，页面刷新、重新进入聊天后修改会丢失 |
| **修改 RichTextView 显示层** | **是** — 通过 `setText:` 更新 `RichTextView` 的文本内容，并调 `setNeedsDisplay` 刷新 | **是** — 通过 `setContent:` + `setText:` 双重调用更新 `RichTextView`，并调用 `setNeedsLayout` + `layoutIfNeeded` + `setNeedsDisplay` | 决定修改是否即时在屏幕上显示 | **持平**：两者都正确更新了 RichTextView 并刷新了显示。我们多调用了 layout 系列方法，理论上布局刷新更完整，但也更耗时 |
| **修改策略** | **双层修改**：先写 `m_nsContent`（数据层持久化），再更新 `RichTextView`（显示层即时生效） | **单层修改**：仅修改 `RichTextView` 显示层，不碰数据层 | 修改的范围和持久性 | **锤子助手更好**：双层修改保证了数据一致性。我们的单层修改只改变视觉效果，是"自欺欺人"式的修改——对方看到的仍然是原文，且自己退出重进后修改丢失 |
| **获取 cellView 方式** | 通过 `cellView.m_delegate → m_messageWrap` 链获取 | 通过 `class_getInstanceVariable` 遍历 `m_richTextView`、`m_messageWrap`、`m_viewModel` 等多个 ivar | 获取 UI 组件的方式 | **锤子助手更好**：使用属性链 `m_delegate → m_messageWrap` 访问，是微信标准的数据访问方式。我们通过 runtime ivar 遍历找 `m_richTextView` 的方式更"暴力"，虽然也能工作但在微信版本升级时更容易因内部结构变化而失效 |

---

## 四、CellView 生命周期管理

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用说明 | 谁更好 |
|---|---|---|---|---|
| 实例保存 | 不需要保存全局引用，每次从方法参数中直接获取 cellView | 使用 3 个全局静态变量：`g_currentMsgWrap`、`g_currentCellView`、`g_currentChatViewController` | cellView 引用的管理方式 | **锤子助手更好**：无全局状态，线程安全，不存在过期引用问题。我们的全局变量存在状态不一致风险（如快速切换聊天时可能拿到旧引用） |
| 清理机制 | 无需管理（无全局状态） | 在 `viewWillDisappear:` 中将所有全局变量置 nil | 防止内存泄漏和引用错误 | **锤子助手更好**：根本不需要清理，架构天然避免了状态管理问题 |
| 防错处理 | 通过 `operationMenuItems` 方法本身保证 cellView 是当前选中的消息 | 在 `showMenuWithItems:` 中如果发现 `g_currentMsgWrap` 为 nil，尝试从 delegate 重新获取（遍历 `m_messageWrap`、`m_viewModel` 等路径） | 异常情况下的容错能力 | **我们的更好**：设计了 fallback 机制，当直接引用丢失时尝试从 delegate 重新获取。锤子助手依赖方法参数传入，不会遇到这种情况 |

---

## 五、支持的场景范围

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用说明 | 谁更好 |
|---|---|---|---|---|
| 文本消息 | 支持 | 支持 | 普通文本消息修改 | **持平** |
| **转账消息** | **支持**（`onTransferJoker` — 修改转账消息的备注/文字内容） | **不支持** | 转账消息的文字修改 | **锤子助手更好**：额外支持转账消息修改 |
| 钱包余额隐藏 | 支持（`WCPayWalletEntryHeaderView.updateBalanceEntryView` hook 隐藏余额） | 不支持 | 钱包相关功能 | **锤子助手更好**：附带钱包功能 |
| 群聊 vs 单聊 | 均支持（`operationMenuItems` 对所有 `TextMessageCellView` 生效） | 均支持（`BaseMsgContentViewController` 覆盖所有聊天场景） | 不同聊天场景的覆盖 | **持平** |

---

## 六、代码架构与可维护性

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用说明 | 谁更好 |
|---|---|---|---|---|
| 代码组织形式 | 功能分散在多个函数中，与 TTS 语音、转账等其他功能混在一起（同一个安装函数 `FUN_0076f450` 中混合了文字修改 + 转账修改 + 钱包隐藏） | 功能集中在单一文件 `JokerHook.m` 中，有清晰的 install 入口和各种 hook 逻辑的分离 | 代码可读性和维护性 | **我们的更好**：功能独立封装在 `JokerHook` 模块中，职责单一清晰，便于维护和调试。锤子助手代码耦合度高 |
| 日志系统 | 无明显日志（反编译代码中未发现专门日志） | 完善的 `jokerLog` 日志系统，记录到 `Documents/WeChatPlugin_Logs/joker.log`，包含时间戳 | 调试和问题排查 | **我们的更好**：详细的日志记录便于用户反馈问题时排查 |
| 开关控制 | 通过 `WeChatTweakSettings.enableJoker` 属性 + 设置界面 `EnableJoker` 开关 | 通过 `PluginConfig.enableJoker` + `SettingGeneralFunctionController` 设置开关 | 功能是否可启用/禁用 | **持平**：两者都有设置开关和启用提示 |
| 版本兼容处理 | 有微信版本号检查（`CFBundleShortVersionString >= 8.0.29`），不满足版本不安装 hook | 无版本号检查 | 微信升级时的兼容性 | **锤子助手更好**：版本检查防止在不适配的新版微信上crash |

---

## 七、综合评分

| 评估维度 | 锤子助手得分 | 我们的插件得分 | 评价 |
|---|---|---|---|
| 数据持久性 | 10/10 | 3/10 | 锤子助手修改写入数据层，持久化；我们仅改显示 |
| 代码简洁性 | 7/10 | 8/10 | 我们模块化更好但有多余hook |
| UI兼容性 | 9/10 | 7/10 | 锤子用原生组件，我们用自定义 |
| 功能扩展性 | 9/10 | 6/10 | 锤子额外支持转账和钱包 |
| 可维护性 | 6/10 | 9/10 | 我们代码结构更清晰，有日志 |
| 健壮性 | 8/10 | 7/10 | 锤子有版本检查，我们有fallback |
| 线程安全 | 9/10 | 6/10 | 锤子无全局状态，我们有全局变量风险 |

---

## 八、结论与改进建议

### 核心差异总结

**锤子助手修改文字最大的优势在于：它的修改是"真修改"——同时修改了数据层（`m_nsContent`）和显示层（`RichTextView`），修改后数据被保存到消息对象中，具有持久性。**

**我们的插件修改文字最大的问题是：仅修改了显示层，没有写回数据层。这意味着：**
1. 对方看到的仍然是原文
2. 退出聊天重进后修改丢失
3. 本质上只是"视觉欺骗"

### 改进建议

1. **最关键**：在 `modifyMsgContent` 中增加对 `msgWrap` 的 `setM_nsContent:` 调用，将新内容写入消息数据层
2. 简化入口：可以去掉 `MMScrollActionSheet` 的 hook，只保留 `MMRichTextCoverView.showMenuWithItems:` 一个入口（这是微信新版的主要菜单方式）
3. 添加微信版本检查，防止新版本接口变更导致的 crash
4. 考虑使用微信原生 `WCUIAlertView` 替代自定义 `WPAlert`，提升UI一致性
5. 减少全局变量的使用，改为从方法参数中获取必要对象