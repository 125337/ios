# 修改文字功能对比分析：锤子助手 vs 我们的插件 (JokerHook)

## 重要前提

**两个插件都是客户端本地"假修改"**——都不写入微信本地数据库，对方看到的始终是原文，退出聊天重新进入后修改均会丢失。因为两者都无法触及服务端，修改仅存在于当前内存中的 `CMessageWrap` 对象或 `RichTextView` 视图层。

---

## 一、触发入口对比

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) — 已照抄 | 作用 | 谁更好 |
|---|---|---|---|---|
| Hook 目标类 | `TextMessageCellView` | `TextMessageCellView` | 在哪类对象上注入修改入口 | **持平**：现在完全一致，都 hook 在 Cell 层 |
| hook 方式 | `class_addMethod("onTextJoker")` + hook `operationMenuItems` | `class_addMethod("onTextJoker")` + hook `operationMenuItems` | 菜单注入方式 | **持平**：完全照抄锤子助手 |
| 消息类型匹配 | 天然匹配 — hook 在 `TextMessageCellView` 上 | 天然匹配 — hook 在 `TextMessageCellView` 上 | 只对文本消息生效 | **持平** |
| 菜单项构造 | `MMMenuItem` + `initWithTitle:iconName:target:actionName:` | `MMMenuItem` + `initWithTitle:iconName:target:actionName:` (fallback: `initWithTitle:action:`) | 菜单项标准性 | **持平**：核心一致，我们多了一个 fallback 路径 |
| 全局变量 | 无 | **无（已去掉）** | 状态管理 | **持平**：现在都无全局变量 |

---

## 二、编辑弹窗对比

| 对比维度 | 锤子助手 | 我们的插件 — 已照抄 | 作用 | 谁更好 |
|---|---|---|---|---|
| 弹窗组件 | 微信原生 `WCUIAlertView`（tag=99999） | `WCUIAlertView`（tag=99999），fallback: `UIAlertController` | UI一致性和稳定性 | **持平**：主路径照抄 WCUIAlertView，我们多了 UIAlertController fallback 提高兼容性 |
| 弹窗展示方式 | `[alert show]` — 直接加到 window 视图层级 | `[alert show]` — 直接加到 window 视图层级 | 绕过 UIViewController presentation 系统 | **持平**：完全照抄 |
| 弹窗不秒关的关键 | `show` 方法不经过 `presentViewController:`，微信内部视图管理检测不到 | `show` 方法不经过 `presentViewController:` | 避免被微信自动 dismiss | **持平** |
| 获取原文 | `cellView → m_viewModel → m_messageWrap → m_nsContent` | `cellView → m_viewModel → m_messageWrap → m_nsContent` | 预填原文 | **持平**：完全照抄 |
| 获取输入文本 | `tipsVc.tipsTextView.text` | `tipsVc.tipsTextView.text`（fallback: `tipsVc.tipsTextField.text`） | 获取用户输入 | **持平** |
| ARC block 处理 | MRC 编译，`objc_msgSend` 裸调无 block 生命周期问题 | ARC 编译，通过 `@interface WCUIAlertView` 声明 + 标准 ObjC 调用，ARC 自动 `_Block_copy` | 防止 block 在栈上被释放导致 crash | **我们的更好**：ARC 环境下安全，MRC 无此需要 |
| cancelAction target | `cellView`（调用方对象） | `cellView`（通过 `showInputAlert:target:` 传入） | WCUIAlertView 需要有效 target | **持平** |

---

## 三、核心修改逻辑对比

| 对比维度 | 锤子助手 | 我们的插件 — 已照抄 | 作用 | 谁更好 |
|---|---|---|---|---|
| 写入 msgWrap 数据层 | `[msgWrap setM_nsContent:newText]` | `[msgWrap setM_nsContent:newText]` | 保证会话内滚动不丢失 | **持平**：完全照抄 |
| 写入 RichTextView 显示层 | `[richTextView setText:newText]` + `setNeedsDisplay` | `[richTextView setText:newText]` + `setNeedsDisplay` | 即时显示效果 | **持平** |
| 写入数据库 | 否 | 否 | 持久化 | **持平** |
| 修改策略 | 双层（先 msgWrap 后 RichTextView） | 双层（先 msgWrap 后 RichTextView） | 覆盖范围 | **持平** |
| 获取依赖链 | 属性链：`cellView.m_viewModel.m_messageWrap` | 属性链：`cellView.m_viewModel.m_messageWrap`（多路径 fallback: KVC / runtime ivar） | 获取组件的健壮性 | **持平**：主路径一致，我们多 fallback |

---

## 四、同一会话内滚动稳定性

| 对比维度 | 锤子助手 | 我们的插件 — 已照抄 | 作用 | 谁更好 |
|---|---|---|---|---|
| Cell 复用场景 | msgWrap 已修改 → 不丢失 | msgWrap 已修改 → 不丢失 | 滚动后修改保留 | **持平** |
| 消息列表刷新 | msgWrap 已修改（内存） | msgWrap 已修改（内存） | 摘要显示 | **持平** |

---

## 五、会话生命周期管理

| 对比维度 | 锤子助手 | 我们的插件 — 已照抄 | 作用 | 谁更好 |
|---|---|---|---|---|
| 全局状态 | 无 | **无（已去掉全部全局变量）** | 线程安全 | **持平** |
| 方法注入 | `class_addMethod` on `TextMessageCellView` | `class_addMethod` + `method_setImplementation` 兜底 on `TextMessageCellView` | 避免引用错误 + 热重载安全 | **我们的更好**：多了兜底机制 |
| 清理机制 | 无需（无状态） | 无需（无状态） | — | **持平** |

---

## 六、功能覆盖范围

| 对比维度 | 锤子助手 | 我们的插件 — 已照抄 | 作用 | 谁更好 |
|---|---|---|---|---|
| 普通文本消息 | 支持 | 支持 | — | **持平** |
| 转账消息修改 | 支持（`onTransferJoker` on `WCPayTransferMessageCellView`） | **支持（已照抄）** | 转账消息文字修改 | **持平** |
| 钱包余额隐藏 | 支持（`WCPayWalletEntryHeaderView.updateBalanceEntryView` hook + 长按手势） | **支持（已照抄）** | 钱包余额长按隐藏 | **持平**：核心逻辑一致，我们多了重复手势检查 |
| 红包名称修改 | 支持（`WeChatTweakRedEnvelopParam.setNickName_`） | **支持（已照抄）** | 红包昵称自定义 | **持平**：锤子助手有完整 ParamPool 管理，我们轻量化单例存储 |
| 群聊/单聊 | 均支持 | 均支持 | — | **持平** |

---

## 七、代码质量

| 对比维度 | 锤子助手 | 我们的插件 — 现状 | 作用 | 谁更好 |
|---|---|---|---|---|
| 模块化 | 低 — 功能混在同一个安装函数 | 高 — 独立 `JokerHook` 模块，每个功能有注释分隔 | 代码可维护性 | **我们的更好** |
| WeChatAlertHelper 封装 | 无（WCUIAlertView 内联在每个调用点） | **有** — `Core/WeChatAlertHelper` 统一封装，提供 `showInputAlert:target:` / `showTipAlert:` API | WCUIAlertView 复用性 | **我们的更好**：一次封装，全局复用 |
| method 注册兜底 | `class_addMethod` 无返回值检查 | `class_addMethod` + `method_setImplementation` 兜底，热重载保证最新实现 | 热重载可靠性 | **我们的更好** |
| 日志系统 | 无 | 完善的 `jokerLog` + 独立 `wechat_alert.log` 日志系统 | 调试能力 | **我们的更好** |
| 版本检查 | `CFBundleShortVersionString >= 8.0.29` | **已添加版本检查** | 兼容性 | **持平** |
| 设置开关 | `WeChatTweakSettings.enableJoker` | `PluginConfig.enableJoker` | 功能开关 | **持平** |
| Fallback 机制 | 无 | 有（MMMenuItem 双初始化路径、WCUIAlertView fallback UIAlertController、RichTextView 双获取路径、class_addMethod → method_setImplementation） | 兼容不同微信版本和运行环境 | **我们的更好** |

---

## 八、架构差异总结 — 已照抄完毕

| 方面 | 锤子助手 | 我们的插件 |
|---|---|---|
| 入口 hook | `TextMessageCellView.operationMenuItems` + `class_addMethod` | `TextMessageCellView.operationMenuItems` + `class_addMethod` + `method_setImplementation` 兜底 ✅ |
| 修改层级 | 双层：msgWrap + RichTextView | 双层：msgWrap + RichTextView ✅ |
| 会话内稳定性 | 滚动不丢失 | 滚动不丢失 ✅ |
| 退出重进 | 失效（不写DB） | 失效（不写DB） |
| 功能扩展 | 文本 + 转账 + 红包 + 钱包 | 文本 + 转账 + 红包 + 钱包 ✅ |
| 状态管理 | 无状态 | 无状态 ✅ |
| 编辑弹窗 | WCUIAlertView.show | WCUIAlertView.show（+ WeChatAlertHelper 封装）✅ |
| msgWrap 获取路径 | `m_viewModel.m_messageWrap` | `m_viewModel.m_messageWrap`（多路径 fallback）✅ |

---

## 九、改造完成清单

- [x] ~~在 `modifyMsgContent` 增加 `setM_nsContent:`~~ → 已完成
- [x] **架构重构**：从 `BaseMsgContentViewController/MMRichTextCoverView/MMScrollActionSheet` 三入口 → 改为 `TextMessageCellView.operationMenuItems` + `class_addMethod("onTextJoker")` 单入口
- [x] **去掉全部全局变量**：删除 `g_currentMsgWrap`、`g_currentCellView`、`g_currentChatViewController`
- [x] **添加版本检查**：`CFBundleShortVersionString >= 8.0.29`
- [x] **转账消息修改**：`class_addMethod("onTransferJoker")` on `WCPayTransferMessageCellView`
- [x] **钱包余额隐藏**：hook `WCPayWalletEntryHeaderView.updateBalanceEntryView` + 长按手势
- [x] **红包名称修改**：hook 红包发送流程 `setNickName:`
- [x] **编辑弹窗**：改为 `WCUIAlertView.show` 主路径（绕过 `presentViewController:` 避免微信秒关），fallback `UIAlertController`
- [x] **启用提示迁移**：从 `JokerHook install` 中移除，改为在 `SettingGeneralFunctionController.switchChanged:` 中开启开关时用 `WCUIAlertView` 弹窗（仅开关从关→开时触发，不会每次启动微信都弹）
- [x] **WeChatAlertHelper 封装**：`Core/WeChatAlertHelper` 统一封装 WCUIAlertView，`showInputAlert:target:` / `showTipAlert:` 全局复用，内置 `@interface WCUIAlertView` 声明解决 ARC block 生命周期问题
- [x] **弹窗秒关修复**：`UIAlertController.presentViewController:` 被微信视图管理秒关 → 改用 `WCUIAlertView.show` 直接加到 window 层级
- [x] **class_addMethod 兜底**：`class_addMethod` 返回 NO 时用 `method_setImplementation` 强制替换，热重载保证最新实现生效
- [x] **msgWrap 获取路径确定**：`cellView → m_viewModel → m_messageWrap`（微信 8.0.60 确认有效）
- [x] **cancelAction target 修复**：`WeChatAlertHelper.showInputAlert` 增加 `target:` 参数，传 `cellView`（WCUIAlertView 需要有效 target 才能正常显示）
- [x] **文档持续更新**：`修改文字功能对比分析.md` 随改造同步更新