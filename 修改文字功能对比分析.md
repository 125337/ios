# 修改文字功能对比分析：锤子助手 vs 我们的插件 (JokerHook)

## 重要前提

**两个插件都是客户端本地"假修改"**——都不写入微信本地数据库，对方看到的始终是原文，退出聊天重新进入后修改均会丢失。因为两者都无法触及服务端，修改仅存在于当前内存中的 `CMessageWrap` 对象或 `RichTextView` 视图层。

---

## 一、触发入口对比

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用 | 谁更好 |
|---|---|---|---|---|
| Hook 目标类 | `TextMessageCellView` + `WCPayTransferMessageCellView` | `BaseMsgContentViewController` + `MMScrollActionSheet` + `MMRichTextCoverView` | 在哪类对象上注入修改入口 | **锤子助手更好**：直接在消息 Cell 的菜单数据源方法上 hook，天然绑定了当前消息，不需要全局变量传递上下文 |
| hook 数量 | **2个** hook：`operationMenuItems`（文本消息 + 转账消息各1个） | **5个** hook：`willShowMenuController`、`showInView`、`showInView:isConstraintByParent:`、`showSelectMenu`、`showMenuWithItems:` | 代码复杂度和潜在冲突 | **锤子助手更好**：hook 少、逻辑简单。我们 hook 了3个方法做同一件事（往菜单加按钮），存在时序和重复问题 |
| 消息类型匹配 | 天然匹配 — hook 在 `TextMessageCellView` 上，只对文本消息生效 | 手动检查 `m_uiMessageType == 1` | 能否正确过滤非文本消息 | **锤子助手更好**：由 hook 目标类自身保证只处理文本消息。我们手动判断 msgType，多一层 |
| 菜单项构造 | 统一使用 `MMMenuItem`，带图标 `cf_expression` | NSDictionary `@{@"title"}` (ScrollActionSheet) + `MMMenuItem` (CoverView)，混用两种方式 | 菜单项与微信原生风格的兼容性 | **锤子助手更好**：统一标准。我们混用 NSDictionary 和 MMMenuItem，兼容性有隐患 |

---

## 二、编辑弹窗对比

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用 | 谁更好 |
|---|---|---|---|---|
| 弹窗组件 | 微信原生 `WCUIAlertView`（tag=99999），带文本输入框 | 自定义 `WPAlert` | UI 一致性和适配 | **锤子助手更好**：WCUIAlertView 是微信内部组件，暗黑模式、字体缩放等完全跟微信一致 |
| 获取原文 | `cellView → m_delegate → m_messageWrap → m_nsContent`，为空时 fallback 到 `m_nsTitle` | `[msgWrap performSelector:@selector(m_nsContent)]` | 编辑时预填原文 | **锤子助手更好**：有 `m_nsContent` → `m_nsTitle` 的 fallback 链 |
| 获取输入文本 | `[alertVC valueForKeyPath:@"tipsVc.tipsTextView.text"]` | WPAlert handler 直接传入 | 获取用户输入 | **持平** |
| 取消编辑 | 原生 UIAlertAction 取消按钮 | WPAlert 取消按钮 | 取消操作 | **持平** |

---

## 三、核心修改逻辑对比

| 对比维度 | 锤子助手 (chuizizhushou 2.4.1) | 我们的插件 (JokerHook) | 作用 | 谁更好 |
|---|---|---|---|---|
| 写入 msgWrap 数据层 | **是** — `[msgWrap setM_nsContent:newText]`（仅内存） | **否** — 完全不碰 msgWrap | 修改是否影响消息数据对象 | **锤子助手更好**：写入 msgWrap 后，Cell 复用时从 msgWrap 读取到的是修改后的内容，同一聊天会话内滚动不会丢失修改。我们只改 RichTextView 显示层，Cell 复用时可能丢失 |
| 写入 RichTextView 显示层 | **是** — `[richTextView setText:newText]` + 刷新 | **是** — `setContent:` + `setText:` + `setNeedsLayout` + `layoutIfNeeded` + `setNeedsDisplay` | 即时显示效果 | **持平**：都能正确更新显示。我们多调了 layout 系列方法，刷新更彻底 |
| 写入数据库 | **否** | **否** | 修改是否持久化 | **持平**：两者都不写 DB |
| 修改策略 | **双层**（msgWrap + RichTextView） | **单层**（仅 RichTextView） | 修改覆盖范围 | **锤子助手更好**：双层保证了同一会话内的稳定性。如果微信在滚动时重新从 msgWrap 渲染 Cell（而非复用视图），锤子助手的修改仍然生效，我们的会丢失 |
| 获取依赖链 | 属性链：`cellView.m_delegate.m_messageWrap` | Runtime ivar 遍历：`m_richTextView`、`m_messageWrap`、`m_viewModel` 多重 fallback | 获取 UI 组件的健壮性 | **锤子助手更好**：使用标准属性访问，是微信内部 API 的标准方式。我们 runtime ivar 方式更"暴力"，微信版本升级容易失效 |

---

## 四、同一会话内滚动稳定性（关键差异）

| 对比维度 | 锤子助手 | 我们的插件 | 作用 | 谁更好 |
|---|---|---|---|---|
| Cell 复用场景 | msgWrap 已修改，Cell 重新配置时读取的是修改后的内容 → **不丢失** | 仅 RichTextView 被改过，Cell 复用时从 msgWrap 读取原文重新渲染 → **丢失** | 滚动后修改是否保留 | **锤子助手更好** |
| 消息列表刷新 | msgWrap 已修改（内存），刷新摘要时可能显示修改内容 | msgWrap 未修改，刷新时显示原文 | 列表摘要显示 | **锤子助手更好** |

---

## 五、会话生命周期管理

| 对比维度 | 锤子助手 | 我们的插件 | 作用 | 谁更好 |
|---|---|---|---|---|
| 全局状态 | 无全局变量 | 3个静态变量：`g_currentMsgWrap`、`g_currentCellView`、`g_currentChatViewController` | 线程安全和内存管理 | **锤子助手更好**：无状态，每次从方法参数获取 cellView，不存在过期引用风险 |
| 清理机制 | 无需（无状态） | `viewWillDisappear:` 中全部置 nil | 防止引用错误 | **锤子助手更好**：架构天然避免状态管理问题 |
| 异常容错 | 无 fallback | `showMenuWithItems:` 中若 msgWrap 为 nil，遍历 `m_messageWrap`、`m_viewModel` 重新获取 | 异常恢复 | **我们的更好**：有 fallback 机制 |

---

## 六、功能覆盖范围

| 对比维度 | 锤子助手 | 我们的插件 | 作用 | 谁更好 |
|---|---|---|---|---|
| 普通文本消息 | 支持 | 支持 | — | **持平** |
| 转账消息修改 | 支持（`onTransferJoker`，修改金额/备注） | 不支持 | 额外场景 | **锤子助手更好** |
| 钱包余额隐藏 | 支持（`WCPayWalletEntryHeaderView`） | 不支持 | 附带功能 | **锤子助手更好** |
| 红包名称修改 | 支持（`WeChatTweakRedEnvelopParam.setNickName:`） | 不支持 | 额外场景 | **锤子助手更好** |
| 群聊/单聊 | 均支持 | 均支持 | — | **持平** |

---

## 七、代码质量

| 对比维度 | 锤子助手 | 我们的插件 | 作用 | 谁更好 |
|---|---|---|---|---|
| 模块化 | 低 — 文字修改、TTS 语音、转账修改、输入框占位符等多个功能混在同一个安装函数中（`FUN_0076f450` 中还有 `onTextJoker`、`onTransferJoker`、`TimeoutNumber`、`WCPayWalletEntryHeaderView` 等） | 高 — 独立 `JokerHook` 模块，职责单一，`install` 入口清晰 | 代码可维护性 | **我们的更好** |
| 日志系统 | 无（反编译代码中未发现日志） | 完善的 `jokerLog` 系统，写入 `Documents/WeChatPlugin_Logs/joker.log`，带时间戳 | 调试能力 | **我们的更好** |
| 版本检查 | 有 `CFBundleShortVersionString >= 8.0.29` 检查 | 无 | 微信版本升级兼容 | **锤子助手更好** |
| 设置开关 | `WeChatTweakSettings.enableJoker` + 设置界面 | `PluginConfig.enableJoker` + `SettingGeneralFunctionController` | 功能开关 | **持平** |
| 启用提示 | 弹 WCUIAlertView 提示 | 弹 WPAlert 提示"已为你启用了修改文字功能，长按聊天记录即可修改" | 用户体验 | **持平** |

---

## 八、架构差异总结

| 方面 | 锤子助手 | 我们的插件 |
|---|---|---|
| 入口 hook | Cell 层 (`TextMessageCellView.operationMenuItems`) | Controller 层 + View 层（3条路径） |
| 修改层级 | 双层：msgWrap(内存) + RichTextView | 单层：仅 RichTextView |
| 会话内稳定性 | 滚动不丢失 | 滚动可能丢失 |
| 退出重进 | 失效（不写DB） | 失效（不写DB） |
| 功能扩展 | 文本 + 转账 + 红包 + 钱包 | 仅文本 |
| 代码结构 | 功能耦合高 | 模块清晰 |
| 状态管理 | 无状态（优秀） | 全局变量（有风险） |
| 调试支持 | 无日志 | 有完整日志 |

---

## 九、改进建议

1. **最优先**：在 `modifyMsgContent` 中增加 `[msgWrap setValue:newContent forKey:@"m_nsContent"]`，使修改写入 msgWrap 内存层，保证同一聊天会话内滚动不丢失
2. 简化入口：去掉 `MMScrollActionSheet` 的两个 hook（`showInView:` / `showInView:isConstraintByParent:`），只保留 `MMRichTextCoverView.showMenuWithItems:` 一个路径
3. 去掉全局变量 `g_currentMsgWrap`、`g_currentCellView`，改为在 `showMenuWithItems:` 中直接从 delegate 获取
4. 添加微信版本号检查
5. 考虑扩展转账消息修改（`WCPayTransferMessageCellView.operationMenuItems`）