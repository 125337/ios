# 界面简化功能 — 完整实现总结

> 基于微信优化 1.6.5 反编译（`FUN_00044244` ~ `FUN_000454d0`）100% 对齐。

---

## 一、配置体系

### 1.1 配置Key

| Key | 类型 | 用途 |
|-----|------|------|
| `SimplifyEnabled` | BOOL | 总开关 |
| `Simplify_MenuNames` | NSDictionary | 设置页/发现页菜单名映射 |
| `Simplify_Tab_Names` | NSDictionary | 底部Tab栏名称映射 |
| `Simplify_MainTitle` | NSString | 顶部主标题替换（导航栏"微信"） |
| `Simplify_ContactsTitle` | NSString | 通讯录页标题替换 |
| `Simplify_DiscoverTitle` | NSString | 发现页标题替换 |
| `Simplify_FriendsCount` | NSString | 通讯录底部好友数替换文本（如 "999个朋友"） |

### 1.2 配置加载流程（`UISimplify_ReloadConfig`）

1. 释放所有旧值（MRC release）
2. 读取 `SimplifyEnabled`，nil 时默认 NO
3. 读取 `Simplify_MenuNames` 字典，nil 时设为空字典 `@{}`
4. **跨版本菜单名兼容预处理**：`"订单与卡包"` ↔ `"卡包"` 自动互填，用户只需配其中一个
5. 读取 `Simplify_Tab_Names` 字典
6. 读取4个单值配置：`Simplify_MainTitle`、`Simplify_ContactsTitle`、`Simplify_DiscoverTitle`、`Simplify_FriendsCount`

### 1.3 热更新

通过 `CFNotificationCenterGetDarwinNotifyCenter()` 监听 `com.wechat.tweak.settings_changed`，通知到达时自动重新加载全部配置。

---

## 二、Hook 总览 (共9个)

```
策略A：字典查表替换（6个）
   Hook #1: WCTableViewCellLeftConfig.title
   Hook #2: MMTabbarItem.initWithTitle:normalImage:selectedImage:
   Hook #3: MMTabbarItem.setNormalTitle:
   Hook #4: MMTabBarItemView.setTitle:
   Hook #5: MMTabBarController.setTabBarItemTitle:forIndex:
   Hook #6: MMTableViewInfo.getTitle:

策略B：上下文匹配（3个）
   Hook #7: MMUILabel.setText:
   Hook #8: MMUILabel.setAttributedText:
   Hook #9: MFTitleView.updateTitleView:title:
```

---

## 三、策略A — 字典查表替换

### 原理

直接 `_tabNames[原文] ?? 原文` 或 `_menuNames[原文] ?? 原文`，O(1) 查表。

### Hook #1：设置页菜单标题

- **目标类**: `WCTableViewCellLeftConfig`
- **方法**: `- title`
- **数据源**: `_menuNames` 字典
- **直接调用原IMP获取原文** → 查字典 → 有则替换，无则原样返回

### Hook #2~#5：底部Tab栏（4个Hook）

公用辅助函数 `replaceTabTitle:`：
```
_tabNames[原文] ?? 原文
```

| Hook | 目标类 | 方法 | 说明 |
|------|--------|------|------|
| #2 | MMTabbarItem | `initWithTitle:normalImage:selectedImage:` | Tab初始化时替换 |
| #3 | MMTabbarItem | `setNormalTitle:` | Tab标题赋值时替换 |
| #4 | MMTabBarItemView | `setTitle:` | Tab视图标题赋值时替换 |
| #5 | MMTabBarController | `setTabBarItemTitle:forIndex:` | TabBar控制器设置标题时替换 |

> 覆盖Tab栏标题的所有赋值路径，确保 "微信"/"通讯录"/"发现"/"我" 都能被替换。

### Hook #6：设置页列表标题

- **目标类**: `MMTableViewInfo`
- **方法**: `- getTitle:`
- **数据源**: `_menuNames` 字典
- **特殊门控**: 每次调用都重新读 `NSUserDefaults` 查开关 + 只处理**含 `\n`** 的标题

---

## 四、策略B — 上下文匹配

### 4.1 通用规则（Hook #7、#8、#9 共用）

所有上下文匹配都基于对**待替换文本**的判断。处理优先级：

```
1. Guard: 开关关闭 / 文本为空 → 直接调用原IMP
2. Guard: 文本以 "[" 开头（badge/红点数） → 不处理
3. 主标题 (mainTitle)：逗号门控 → responder chain 兜底 → 替换
4. 通讯录 (contactsTitle)：逗号 OR 文本含 "通讯录" → 替换
5. 发现页 (discoverTitle)：逗号 OR 文本含 "发现" → 替换
6. 好友数 (friendsCount)：门控 "个朋友" → 正则 \\d+ → stringByReplacingCharactersInRange → 替换
7. 兜底：调用原IMP（不做任何替换）
```

### 4.2 主标题替换（mainTitle）详解

```
if 文本包含 ",":
    → 直接替换（高置信度，逗号是联系人列表的标志）
else:
    → 遍历 responder chain，找类名含 "NavigationBar" 的响应者
    → 找到 → 替换（确认当前UILabel在导航栏中）
    → 找不到 → 不替换
```

> **NavigationBar 检测用子串匹配**（`rangeOfString:`），而非前缀匹配（`hasPrefix:`）。
> 因为 UIKit 导航栏类名是 `UINavigationBar`，以 `"UI"` 开头，`hasPrefix:@"NavigationBar"` 无法匹配。

### 4.3 通讯录/发现页 — 双重匹配

微信优化原始设计中 `contactsTitle` 和 `discoverTitle` 各自使用独立的匹配模式。当前实现使用**逗号 OR 文本匹配**作为兼容方案：

- 通讯录：`hasComma || containsString:@"通讯录"`
- 发现页：`hasComma || containsString:@"发现"`

逗号匹配覆盖联系人列表中的导航栏标题（如 "xxx, yyy, zzz"），文本匹配覆盖独立页面标题。

### 4.4 好友数替换

```
门控: 文本必须包含 "个朋友"（只处理通讯录底部）
正则: \\d+ （匹配任意连续数字）
替换: stringByReplacingCharactersInRange:withString:_friendsCount
```

- 门控"个朋友"确保只替换通讯录底部的好友数量文本
- 用 `stringByReplacingCharactersInRange:withString:` 保持原文本结构，只替换数字部分

---

## 五、三个策略B Hook 的差异

### Hook #7 — `setText:`

处理 `NSString`，直接调用原IMP传入替换后的字符串。

### Hook #8 — `setAttributedText:`

处理 `NSAttributedString`，替换时需**保留原富文本属性**：

```objc
// 提取原文本的属性字典
NSDictionary *attrs = [attrText attributesAtIndex:0 effectiveRange:NULL];
// 用新文本+原属性创建 NSMutableAttributedString
NSMutableAttributedString *replaced = [[NSMutableAttributedString alloc]
    initWithString:newTitle attributes:attrs ?: @{}];
```

### Hook #9 — `MFTitleView.updateTitleView:title:`

处理顶部导航栏标题视图。微信优化在此 Hook 中**不遍历 responder chain**（`FUN_000454d0` 中无 `nextResponder` 调用）。当前实现加了 responder chain 兜底以支持"微信"顶部标题替换。

---

## 六、特殊处理

### 6.1 Badge 保护

所有 Hook 检查 `hasPrefix:@"["]`，跳过红点/未读数 badge（如 `"[3]联系人"`），防止误替换。

### 6.2 跨版本菜单名兼容

`"卡包"`（低版本微信）和 `"订单与卡包"`（高版本微信）是同一功能的不同名称。配置加载时自动补齐：

```
用户配了 "卡包" → 新名    → 自动补 "订单与卡包" → 新名
用户配了 "订单与卡包" → 新名 → 自动补 "卡包" → 新名
```

用户只需在设置中填一个 key，无需关心微信版本差异。

### 6.3 MRC 内存管理

整个模块 MRC 编译。每个 `release` 都精确配对：
- `reloadConfig` 先 `release` 旧值再 `retain` 新值
- `setAttributedText:` 中创建的 `NSMutableAttributedString` 用完后立即 `release`
- `menuNames` 跨版本补齐时，`mutableCopy` 用完 `release`

---

## 七、文件结构

| 文件 | 作用 |
|------|------|
| `UISimplifyHook.h` | 对外接口：`+install`、`+reloadConfig` |
| `UISimplifyHook.m` | 全部实现：配置、9个Hook、安装流程 |

`install` 方法顺序：
1. `UISimplify_ReloadConfig()` 加载配置
2. `UISimplify_RegisterHotReload()` 注册热更新
3. 按 Hook #1~#9 顺序安装到目标类