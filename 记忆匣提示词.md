# 记忆匣提示词 — MioPlugin 语音包功能调试（接力文档）

> 用途：在新会话窗口中向我发送此文件内容（或让助手读取本文件），即可恢复语音包功能调试的全部脉络。
> 项目：`c:\Users\20546\Desktop\ios_cc4BX`（MioPlugin 微信 dylib 插件，语音包模块）
> 更新时间：2026-09-19 深夜（对应 dylib 构建 run 2035）

---

## 一、任务目标

MioPlugin（微信 iOS 增强插件，产物 `dist\Mio_arm64-1.dylib`，用户用"全能签"注入）的**语音包功能**：用户长按聊天加号 → 选择语音包文件（silk 格式）→ 发送到当前聊天。**卡点：消息永远是"本地假消息"——UI 显示已发送/发送中、本地方有声音，但对方收不到**。且近几轮引入"一点发送就闪退"的新问题（已定位）。

用户约束：
- **不依赖 WCRefine**（另一个常驻插件，有自己的语音包功能且实测能发出）——我们要独立实现
- 用户有**越狱设备**，已配好 Frida（USB 连接，iPhone 上跑 com.tencent.qy.xin 微信）
- 交流用中文；dylib 构建走 `scripts\build_local.ps1`（git push → GitHub Actions → 下载产物，全自动）

---

## 二、已实锤的核心机制（宝贵情报，勿重复探索）

### 1. 微信语音消息的两条真实管线
- **录音管线**（用户按住说话，100% 能发出）：
  - `AddMsg:MsgWrap:` 是在**录音开始时**调用的（不是结束！）——localID=0 入库、dl=1、XML voicelength="0" 占位、扩展 VoiceTime=0/EndFlag=0/dtVoice=nil、m_nsMsgSource=@""（非 nil）
  - 音频文件由微信**边录边写**到 `Audio/{md5(chatName)}/{localID}.aud`（FileTL 时间线实锤：t0.5s=1149B → t4s=7072B 停止）
  - **松手后**微信把消息标记为"完成"（log37 抓到三连 UpdateVoiceMessage：①fixTime版(dl=1,vt=0) → ②普通(dl=9,时长回填,VTime=毫秒) → ③普通(st=2,dl=1)）→ 上传管线启动
- **转发管线**（WCRefine 语音包实测，Frida 追踪完整抓到）：
  ```
  ① 暂存: AddLocalMsg(2参) 入库 (dl=0 fw=1 扩展End=1/Fwd=1 dtVoice=数据) → 内部自动 SaveMesVoice×2（第2次自动翻转 EndFlag→0、_voiceUrl=""/_aesKey=""、m_nsLastDisplayContent 出现）
  ② 正式: AddLocalMsg(2参) 入库 (dl=9 fw=0 扩展End=0/Fwd=0 dtVoice 仍在 _m_nsBizCliMsgId="")
  ③ 显式 SaveMesVoice(正式)
  ④ 正式 wrap: st 1→2、dl 9→1
  ⑤ UpdateVoiceMessage:MsgWrap: 提交
  → UploadVoiceCDNMgr 轮询拾取 → 上传发出
  ```
- **上传体系**：`UploadVoiceCDNMgr`（内存版）/ `MMNewUploadVoiceMgr`（DB 版）两个管理器，**靠定时器每 2 秒跑 TimerCheckUpload → readyUploadItems 轮询队列**——没有显式注册调用，谁被正确标记谁被传
- **AddNewPart:LocalID:n64SvrID:Offset:Len:VoiceTime:CreateTime:EndFlag:CancelFlag:VoiceFormat:ForwardFlag:msgSource:**（12 参）= 上传队列登记方法（SaveMesVoice 内部调用）；**ResendVoiceMsg:MsgWrap:**（2 参）= 启动/重试触发（**在 UploadVoiceCDNMgr 类上**，不在 CMessageMgr！）
- **CMessageWrap 没有 m_nsVoicePath ivar**（147 ivar 全量确认），路径是派生的：`getPathOfAudio` → `Audio/{md5(chatName)}/{localID}.aud`

### 2. dylib 复刻现状（run 2027/2028/2032 的组合，UI 层面全部正确）
- 五步序列已按 WCRefine 原样复刻进 `VoicePackStore.m 的 sendVoiceAtRelPath`（暂存 fw=1/dl=0/EndFlag=1 + 正式 fw=0/dl=9/EndFlag=0 + SaveMesVoice + st=2/dl=1 + UpdateVoiceMessage 提交 + ⑥a AddNewPart(part=文件路径推测) + ⑥b ResendVoiceMsg(活实例)）
- 效果：UI 显示已发送、本地有声（dl=9）、文件落盘成功、**但对方收不到**
- **残留问题**：⑥b ResendVoiceMsg 调用后微信闪退（崩点见下）

### 3. 闪退真相（Frida Process.setExceptionHandler 捕获，决定性证据）
```
异常: access-violation, read @ 0x4（对 nil 对象读偏移 0x4）
异常地址: 0x19764ac0c（两次崩溃完全相同=同一点）
堆栈（新→旧）:
  WeChat+0x491e3d4          ← 方法实现内部读 0x4 崩
  libobjc (objc_msgSend)
  Mio_arm64+0x5bda4/0x5bce4 ← 我们的两个函数（run 2035/2033 偏移，随构建漂移）
  Mio_arm64+0x5bbf8/0x5bb38
  UIKitCore+0xfe8de8        ← UI 布局链（语音气泡 cell 渲染！）
```
- **崩溃不在 ResendVoiceMsg 本身，而在 UIKit 布局回调链上的某个 Mio hook**（语音气泡渲染时触发）
- **头号嫌疑：UIPurifyHook.m 的 `hook_YYAsyncImg_layoutSubviews`**（YYAsyncImageView 气泡背景 hook，内部 performSelector getBgImageView → WeChat 方法读 nil 成员崩）；次嫌疑 VoiceMessageCellView layoutSubviews hook（KVC 安全，可能性低）
- **触发条件**：st=2/dl=1 状态的死消息气泡渲染（以前卡的消息是 st=1，渲染路径不同所以以前不崩）
- **log43 的闪退**是另一个独立原因：run 2029 的 AddNewPart 12 参 hook 里 object_getClass(part) 解引用非对象参数 → EXC_BAD_ACCESS（已修复为纯指针打印、后彻底移除）
- **log47/48 的参数行丢失**：AddNewPart hook 12 参签名与真实不符导致 MSHook 转发即崩，WPLog 都没执行到（已彻底移除该 hook）

---

## 三、当前状态与下一步

### 当前产物状态
- **run 2035 已构建**（commit 64b127e）= 干净模式：**所有语音取证 hook（18 个）受 `NSUserDefaults MioPlugin_Voice_ForensicsHooks` 开关控制，默认全关**；功能路径只保留五步+⑥a AddNewPart(part=路径推测)+⑥b ResendVoiceMsg(活实例)
- 用户报告：干净模式下**发语音包还是闪退** → 证实崩溃在功能路径或 UI hook（非取证 hook）
- dylib 里已内置：TimerCheckUpload hook 捕获活的 UploadVoiceCDNMgr 实例（`MioGetUploadVoiceCDNMgr()`，log44 实锤它不是 MMServiceCenter 注册 service，getService 拿不到）

### 下一步计划（按优先级）
1. **零构建验证**（用户操作）：MioPlugin 设置 → UI 净化 → **关掉"聊天气泡背景"**（hideBubbleBackground 开关）→ 重启微信 → 发语音包测试。不崩 = 确认崩溃源是 YYAsyncImageView hook，dylib 里修掉它
2. 若还崩：依次关 UI 净化的其它开关（隐藏语音红点等）二分定位
3. 定位后修复 dylib → 验证发送 → 若 ResendVoiceMsg 触发后上传仍不启动，需反汇编 UploadVoiceCDNMgr 的 ResendVoiceMsg/getOneUploadItemFromDB 内部逻辑（本地有完整反汇编基础设施）
4. **DB 清理**：用户微信 DB 里堆积了大量卡 status=2/1 的死语音消息（2712/2713/2730/2732/2737...），微信每次启动会自动重发它们（启动 3 秒内 ResendVoiceMsg(2730) 就是），可能干扰测试
5. 成功后：清理暂存气泡（只留正式消息）、更新项目记忆、清理临时文件

### 备用情报
- 防撤回通知的"真发"参考（RevokeHandler.m:305-353）：type=1 文本 + 只设 m_nsContent/m_nsToUsr/m_nsFromUsr + AddMsg:MsgWrap: → 对方真收到（文本走 CGI 直发，无上传环节）

---

## 四、Frida 基础设施（已搭好，全在 scripts\frida\）

- **runner.py**：attach 模式 runner。动态解析 qy.xin 的 PID（frida-ps -Uai）→ `frida -U <PID> -l <脚本>`，stdin=PIPE 永不关闭（防 REPL 退出），cmd.txt 命令通道（写命令进文件→REPL 执行），日志 → trace.log。用法：`python runner.py <脚本路径>`，需微信已打开
- **wc_crash.js**：★崩溃捕获（当前在用）。`Process.setExceptionHandler` 打印异常类型/地址/双调用栈（FUZZY+ACCURATE，模块+偏移格式）；加 AddNewPart/ResendVoiceMsg/startSend 的 onEnter 监控。**崩溃时 console.log 会完整 flush 到 trace.log**（已验证）
- **voice_test.js**：分步发送测试（sendStep1 入库+写文件 / st1 读结果 / grab 堆扫描实例 / trySend ResendVoiceMsg / st1/t2 读结果），全部走主队列调度（防 JS 线程崩溃）
- **wc_hook2.js**：UploadVoiceCDNMgr 全方法追踪（AddNewPart/ResendVoiceMsg/startSend/uploadVoiceToCDN/loadDataFromAudioFile/handleSendVoiceSuccess 等，纯指针打印）
- **wcrefine_trace.js**：WCRefine 638 方法 + CMessageMgr 关键方法 + 上传类追踪（曾因此卡顿，现已轻量化）
- **sym_lookup.py**：Mach-O 符号表解析（崩溃偏移→导出符号范围）
- **disasm_crash.py**：capstone 反汇编崩溃点（本地已装 capstone 5.0.7）
- **已知坑**（重要！）：Frida 17 的 API 改名 `Module.getExportByName(null,x)` → `Module.getGlobalExportByName(x)`；REPL 模式 ObjC 桥是全局注入（不要 require）；裸 create_script 没有 ObjC 桥（runtime='v8' 也不行）；cmd.txt 必须无 BOM（`[System.IO.File]::WriteAllText(..., UTF8Encoding($false))`）；Frida 的 JS 线程直调微信内部代码会崩（重量 ObjC 调用必须 ObjC.schedule(mainQueue)）；stdout 重定向到文件是全缓冲（崩溃时才 flush）；spawn 模式在 Windows CLI 上会崩（用 attach）；-n WeChat 有歧义（多开分身），必须按 PID 附加
- **hook 编写铁律**（血泪教训）：未知类型参数禁止 object_getClass/%@/[arg class] 解引用（EXC_BAD_ACCESS 非 NSException，@try 拦不住）——只用 %p；整型参数按 unsigned long 接收（arm64 零扩展安全）；hook 转发签名必须与真实一致（12 参签名不符 = MSHook 转发即崩）

---

## 五、环境与工具链

- dylib 构建：`powershell -ExecutionPolicy Bypass -File scripts\build_local.ps1 -Message "..."`（自动 commit+push+Actions+下载到 dist\）
- Frida 17.18.0（PC 端 pip frida-tools；越狱机 Sileo 源 build.frida.re）
- 越狱机：iPhone（id=00008130-0005050822F3803A），USB 连接，微信 bundle id = **com.tencent.qy.xin**（注意还有 com.tencent.xin / com.tencent.wx 两个分身未运行）
- 用户手机里常驻插件：黄白助手2.1-5、微信助手3.9-5、锤子助手2.6.9、WCRefine2.1-2、MikotoHelper、微信优化1.6.5、WCPulse（共同 hook AddMsg 链，Mio 的 hook 与它们链式共存）
- 语音包目录（与 WCRefine 共用）：`Documents/Mio助手/语音包/`（内含 WCRefine 的 .wcr_voicepack_bookmarks.plist / .wcr_voicepack_duration.plist）
- 音频格式：silk，发送 wire = 0x02 前缀 + silk 流

---

## 六、debug 关键日志文件（桌面）

- MioPlugin(31)~(48).log：各轮 dylib 运行日志（[Send] ①~⑥ 是发送序列，[Upload] 是上传管理器，[真实流程] 是 hook 捕获）
- scripts\frida\trace.log：Frida 会话日志（崩溃栈在 [CRASH] 块）

---

## 七、给新会话助手的开场建议

1. 先读本文件全文（即本提示词）
2. 确认用户 dylib 当前版本（问是否已装 run 2035）
3. 第一个动作：让用户在设置里关掉 UI 净化的"聊天气泡背景"→ 重启微信 → 发语音包 → 看是否还闪退（零成本验证崩溃源假设）
4. 崩溃修掉后再验证上传（对方收到与否），不要在闪退状态下验证上传（结果不可信）
