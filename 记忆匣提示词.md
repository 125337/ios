# 记忆匣提示词 — MioPlugin 语音包功能调试（接力文档）

> 用途：在新会话窗口中向我发送此文件内容（或让助手读取本文件），即可恢复语音包功能调试的全部脉络。
> 项目：`c:\Users\20546\Desktop\ios_cc4BX`（MioPlugin 微信 dylib 插件，语音包模块）
> 更新时间：2026-09-20（对应 dylib 构建 **run 2036**，已下载待测试）

---

## 一、任务目标

MioPlugin（微信 iOS 增强插件，产物 `dist\Mio_arm64-1.dylib`，用户用"全能签"注入）的**语音包功能**：长按聊天加号 → 选语音包文件（silk）→ 发到当前聊天。**历史卡点：消息是"本地假消息"（UI 已发送/本地方有声，对方收不到）+ 一发就闪退**。

用户约束：
- **不依赖 WCRefine**（常驻插件，有自己的语音包功能且实测能发出）——独立实现
- 用户有**越狱设备**，Frida 已配好（USB，微信 bundle id = com.tencent.qy.xin）
- 中文交流；dylib 构建 = `scripts\build_local.ps1`（git push → GitHub Actions → 下载 dist\）

---

## 二、★最新重大突破：拿到了 WCRefine 的反编译源码（WCR反编译\ 目录）

用户提供了 **WCRefine 的反编译产物**（按方法名拆分的 .c 文件，GHidra 风格）。**权威实现在 `WCR反编译\FUN_008dee38.c`**（14.7KB，= `_WCRefineSendVoiceDataToChat`，即语音包发送的完整实现）。调用链：
```
WCRefineVoicePackPickerViewController → WCRefineVoicePackSender sendItems/sendItem:toChatName:completion:
→ (dispatch_async 全局队列) → WCRefineLinkMediaSender sendVoiceAtPath:toUsr:
→ _WCRefineVoiceDataFromMediaPath(读文件) → _WCRefineSendVoiceDataToChat → FUN_008dee38
```

### ★FUN_008dee38 的正确实现（逐行翻译，推翻之前所有轮次的字段猜测）：
```objc
// 参数: voiceData(NSData), voiceTimeMs(uint), toUsr(NSString), path(NSString可空), wxid(NSString)
1. wrap = [[CMessageWrap alloc] initWithMsgType:nsFromUsr:](34, wxid)   // 2参 init（守卫 instancesRespondToSelector），fallback initWithMsgType:(34)
2. [wrap setM_uiMessageType: 34]
3. [wrap setM_nsFromUsr: wxid]
4. [wrap setM_nsToUsr: toUsr]
5. [wrap setM_uiCreateTime: MMNewSessionMgr GenSendMsgTimeWithChatName:(toUsr)]  // fallback GenSendMsgTime
6. [wrap setM_uiStatus: 1]
7. [wrap setM_uiDownloadStatus: 9]                    // ★dl=9
8. [wrap setM_bForward: 1]                            // ★★bForward=1（不是0！）
9. [wrap setM_uiVoiceTime: voiceTimeMs]               // ★毫秒
10. [wrap setM_uiVoiceFormat: 4]
11. [wrap setM_uiVoiceEndFlag: 1]                     // ★1
12. [wrap setM_uiVoiceForwardFlag: 1]                 // ★1
13. [wrap setM_dtVoice: voiceData]                    // ★★wrap 级官方 setter！（内部自动处理扩展）
14. [wrap setM_nsContent: "<msg><voicemsg voicelength=\"%u\" voiceformat=\"4\" forwardflag=\"0\" /></msg>"]
15. setM_uiVoiceCancelFlag: 0（NSSelectorFromString 动态调用）
16. objc_setAssociatedObject ×4（voiceData/path/voiceTime/format——照抄）
17. msgMgr = FUN_008f3348(getClass("CMessageMgr"))     // = MMServiceCenter.getService 多级 fallback
    audioSender = FUN_008f3348(getClass("AudioSender")) // ★★"AudioSender" 类！ResendVoiceMsg 在它上面！
    守卫: msgMgr.respondsToSelector(AddLocalMsg:MsgWrap:) && audioSender.respondsToSelector(ResendVoiceMsg:MsgWrap:)，任一失败 return NO
18. [msgMgr AddLocalMsg:MsgWrap: toUsr, wrap]          // 入库（无暂存！单消息！）
19. 路径三级 fallback:
    a. [CUtility GetPathOfMesAudio:LocalID:DocPath: toUsr, wrap.localID, [CUtility GetDocPath]]  // ★CUtility 类方法（守卫）
    b. [msgMgr getAudioFileName:LocalID: toUsr, wrap.localID]
    c. [wrap getVoicePath]
    全失败 return NO
20. createDirectoryAtPath(路径去尾)
21. [voiceData writeToFile:path atomically:YES]        // ★数据写文件（m_dtVoice → 盘）
22. 文件存在检查（失败 return NO）
23. FUN_008f38f4(wrap, sel(setM_nsVoicePath:), path)    // setM_nsVoicePath（守卫 respondsToSelector 后 msgSend）
24. [msgMgr SaveMesVoice:MsgWrap: toUsr, wrap]          // ★SaveMesVoice 只调 1 次（守卫）
25. [audioSender ResendVoiceMsg:MsgWrap: toUsr, wrap]   // ★★上传触发（AudioSender 实例，MMServiceCenter 标准 service）
26. return YES
```
- **没有**：暂存消息、UpdateVoiceMessage、AddNewPart 显式调用、m_nsMsgSource、m_bNew=0、m_uiImgStatus、手动挂 CExtendInfoOfVoiceMsg
- **重要认知修正**：`setM_dtVoice:/setM_uiVoiceEndFlag:/setM_uiVoiceForwardFlag:/setM_uiVoiceTime:` 是 **CMessageWrap 的官方语音 setter**（内部自动创建/关联扩展对象）——之前手动构造 CExtendInfoOfVoiceMsg 挂载是错误姿势，很可能是历轮失败的根源之一
- **log43 的"UploadVoiceCDNMgr ResendVoiceMsg(2730)"是微信启动自动重发旧死消息**，不是 WCRefine 的调用（WCRefine 用 AudioSender 类）
- **log42 追踪到的"暂存+正式双消息"是 WCRefine 的另一条路径**（sendItems 复数版），FUN_008dee38（单消息）才是 sendItem 单发的权威实现
- **flashback**：`CMessageMgr ResendVoiceMsg:MsgWrap:` 不存在（0/0），`UploadVoiceCDNMgr ResendVoiceMsg` 是微信启动重发用的；正确触发 = **AudioSender 类的 ResendVoiceMsg:MsgWrap:**（MMServiceCenter 标准 service 获取）
- 辅助函数：FUN_008f3348 = 通用 service 获取（MMContext.currentContext.getService → serviceCenter.getService → MMServiceCenter.defaultCenter.getService 多级 fallback）；FUN_008f38f4 = 守卫式动态 setter 调用；FUN_008f2f84 = createTime 生成

---

## 三、闪退真相（Frida Process.setExceptionHandler 捕获，决定性证据）

```
异常: access-violation, read @ 0x4（对 nil 对象读偏移 0x4）
异常地址: 0x19764ac0c（两次崩溃完全相同=同一点）
堆栈（新→旧）:
  WeChat+0x491e3d4          ← 方法实现内部读 0x4 崩
  libobjc (objc_msgSend)
  Mio_arm64+0x5bda4/0x5bce4 ← 我们的两个函数（偏移随构建漂移，同一对函数）
  Mio_arm64+0x5bbf8/0x5bb38
  UIKitCore+0xfe8de8        ← UI 布局链（气泡 cell 渲染！）
```
- **崩溃不在上传调用链，而在 UIKit 布局回调链上的某个 Mio hook**（气泡渲染时触发）
- **头号嫌疑：UIPurifyHook.m 的 `hook_YYAsyncImg_layoutSubviews`**（YYAsyncImageView 气泡背景 hook，performSelector getBgImageView → 微信方法读 nil 成员崩）——**零构建验证：设置 → UI 净化 → 关"聊天气泡背景"→ 重启微信 → 测试**
- 次嫌疑：VoiceMessageCellView layoutSubviews hook（KVC 有 @try，可能性低）
- **触发条件**：st=2/dl=1 状态的死消息气泡渲染（以前卡的消息是 st=1，渲染路径不同）
- **hook 编写铁律**（血泪）：未知类型参数禁止 object_getClass/%@/[arg class] 解引用（EXC_BAD_ACCESS 非 NSException，@try 拦不住）——只用 %p；12 参签名不符 = MSHook 转发即崩（AddNewPart hook 教训）

---

## 四、当前状态与下一步

### 产物状态
- **run 2036 已构建并下载**（commit a6019ef1，dist\Mio_arm64-1.dylib 831984B）= 按 FUN_008dee38 逐行复刻：单消息（dl=9/fw=1/EndFlag=1/Fwd=1/VoiceTime=ms/setM_dtVoice 官方 setter）→ AddLocalMsg(2参) → CUtility 路径 → 写文件 → setM_nsVoicePath → SaveMesVoice×1 → **AudioSender ResendVoiceMsg**
- **待用户测试**（装 dylib 发语音包）
- 保留：TimerCheckUpload hook 捕活 UploadVoiceCDNMgr 实例（`MioGetUploadVoiceCDNMgr()`，备用）；MioAttachVoiceExtension（wrap 不响应 setM_dtVoice: 时的 fallback）
- run 2035 起：全部取证 hook 受 `NSUserDefaults MioPlugin_Voice_ForensicsHooks` 开关控制（默认关=干净模式）

### 测试观察点（日志关键行）
- `setM_dtVoice: 官方setter已用`（确认 wrap 响应官方 setter）vs `手动扩展挂载`（fallback 生效）
- `CUtility路径` / `getAudioFileName路径` / `getVoicePath路径`（哪级 fallback 命中）
- `AudioSender ResendVoiceMsg 已调` vs `AudioSender 不可用`
- 对方收到与否

### 若失败的后备方向
1. 若闪退复现：继续 UI 净化开关二分（隐藏语音红点等）
2. 若 AudioSender 拿不到：检查类名（objc_getClass("AudioSender") 可能需要完整名——反编译里就是这个字符串）
3. 若上传仍不启动：反编译目录里还有 FUN_008c1298.c/FUN_008c38d0.c（引用了上传管理器）可深挖；AudioSender 的方法表可枚举
4. **DB 清理**：堆积的死语音消息（2712/2713/2730/2732/2737...）每次启动被自动重发（启动 3 秒内 ResendVoiceMsg(2730)），干扰测试
5. 成功后：更新项目记忆、清理临时文件（scripts\frida\ 的调试脚本、apply_2036.py、桌面 MioPlugin(*).log）

---

## 五、Frida 基础设施（scripts\frida\，已搭好）

- **runner.py**：动态解析 qy.xin PID（frida-ps -Uai）→ `frida -U <PID> -l <脚本>`，stdin=PIPE 防退出，cmd.txt 命令通道，日志 → trace.log。用法：`python runner.py <脚本路径>`（微信需已打开）
- **wc_crash.js**：★崩溃捕获（Process.setExceptionHandler 打异常类型/地址/双调用栈 + AddNewPart/ResendVoiceMsg/startSend onEnter 监控）。崩溃时 console.log 完整 flush（已验证）
- **voice_test.js**：分步发送测试（sendStep1/st1/grab/trySend/t2），主队列调度版
- **wc_hook2.js**：UploadVoiceCDNMgr 全方法追踪；**wc_hook.js**：用户写的 MMNewUploadVoiceMgr hook（证实 WCRefine 不走它）
- **wcrefine_trace.js**：WCRefine 638 方法追踪（轻量版：只打方法名，打参数会崩）
- **sym_lookup.py**：Mach-O 符号表解析；**disasm_crash.py**：capstone 反汇编（capstone 5.0.7 已装）
- **已知坑**：Frida 17 `Module.getExportByName(null,x)` → `Module.getGlobalExportByName(x)`；REPL 的 ObjC 桥是全局注入（不要 require）；cmd.txt 必须无 BOM（`[System.IO.File]::WriteAllText(..., UTF8Encoding($false))`）；Frida JS 线程直调微信代码会崩（必须 ObjC.schedule(mainQueue)）；stdout 重定向是全缓冲（崩溃时 flush）；spawn 模式 Windows CLI 会崩（用 attach）；多开分身导致 -n WeChat 歧义（按 PID 附加）；`Process.setExceptionHandler` 可抓 native 崩溃堆栈（access-violation 时 details.type/address/memory/context 可用，return false 放行崩溃）
- **hook 铁律**：见"闪退真相"末尾

---

## 六、环境与工具链

- dylib 构建：`scripts\build_local.ps1`（commit+push+Actions+下载）。**若卡在 push**：分步手动——git push（MinGit `C:\Users\20546\.local\share\MinGit\cmd\git.exe`）→ token（`scripts\.gh_token`）调 API 轮询 Actions run → 下载 artifact zip → Expand-Archive 到 dist\
- 仓库：github.com/125337/ios（private），master 分支
- 越狱机 iPhone（id=00008130-0005050822F3803A）USB；常驻插件：黄白助手2.1-5、微信助手3.9-5、锤子助手2.6.9、WCRefine2.1-2、MikotoHelper、微信优化1.6.5、WCPulse
- 语音包目录（与 WCRefine 共用）：`Documents/Mio助手/语音包/`；音频 wire = 0x02 前缀 + silk 流
- **WCR反编译\ 目录**：WCRefine 的反编译 .c 文件（按方法名一文件）——语音发送相关：FUN_008dee38.c（★核心）、sendVoiceAtPath_toUsr_.c、sendItem_toChatName_completion_.c、_WCRefineSendVoiceDataToChat.c、FUN_008f3348.c（service 获取）、FUN_008f2f84.c（createTime）

---

## 七、debug 日志索引（桌面）

- MioPlugin(31)~(48).log：各轮 dylib 日志（[Send] ①~⑥ 发送序列、[Upload] 上传管理器、[真实流程] hook 捕获、[CRASH] Frida 崩溃栈在 scripts\frida\trace.log）
- MioPlugin(44) 实锤：getService 拿不到 UploadVoiceCDNMgr；MioPlugin(45) 崩溃栈 read@0x4

---

## 八、给新会话助手的开场建议

1. 读本文件全文
2. 问用户 run 2036 dylib 测试结果（对方收到？闪退？）
3. 若成功：清理暂存残留、更新项目记忆、收尾
4. 若闪退：按"闪退真相"节的 UI 净化开关二分（YYAsyncImageView 嫌疑）
5. 若上传不动：用 WCR反编译\ 目录对照（FUNC 链都在里面），必要时反汇编 AudioSender 的 ResendVoiceMsg
