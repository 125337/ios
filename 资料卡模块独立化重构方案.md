# 资料卡模块独立化重构 — 详细实施步骤

> **唯一方案**：保持单一 Hook 入口（不 double-hook），内部分发到两个独立类
> **目标**：ListCornerRadius 只管列表圆角，ProfileCardBg 只管资料卡背景

---

## 1. 最终文件结构

```
MioPlugin/Modules/
├── ListCornerRadius/
│   ├── ListCornerRadiusHook.h          ← 不变
│   └── ListCornerRadiusHook.m          ← 瘦身为 ~500 行（薄分发层 + 列表圆角逻辑）
│
└── ProfileCardBg/                      ← ★ 新建目录
    ├── ProfileCardBgHook.h             ← ★ 新建
    └── ProfileCardBgHook.m             ← ★ 新建（~600 行，资料卡全部逻辑）
```

---

## 2. 代码归属划分总览

### 从 ListCornerRadiusHook.m 搬到 ProfileCardBgHook.m 的内容

| 行号范围 | 内容 | 方法 |
|:--------:|:-----|:-----|
| L177-621 | `replaced_MMUIButton_layoutSubviews` 全部 | → `handleButtonLayout:` |
| L667-668 | `isProfileCardCell` / `needsCardBgTransparency` 判断 | → `handleCellLayout:` 内部 |
| L788-825 | Cell 透明化代码块 | → `handleCellLayout:` 内部 |
| L1154-1190 | `wp_applyProfileCardCorner` | → `ProfileCardBgHook` 的类方法 |
| L1192-1216 | `wp_hideQRButtonInCell` + `wp_hideQRButtonInSubviews` | → `ProfileCardBgHook` 的类方法 |
| L1300-1412 | `wp_loadBackgroundImageSync` | → `ProfileCardBgHook` 的类方法 |
| L1414-1439 | `wp_cardBackgroundDirectory` | → `ProfileCardBgHook` 的类方法 |
| L1441-1537 | `wp_loadBackgroundImageForImageView` | → `ProfileCardBgHook` 的类方法 |
| L1137-1152 | `wp_isProfileCard:` + `wp_findMMHeadImageViewInSubviews:` | → `ProfileCardBgHook` 的类方法 |

### ListCornerRadiusHook.m 保留的内容

| 行号范围 | 内容 |
|:--------:|:-----|
| L1-11 | #import / static 变量声明（去掉 `_orig_MMUIButton_layoutSubviews` 相关的引用调整）|
| L12-21 | `findParentViewController` 函数 |
| L23-77 | `@interface` 声明（去掉资料卡相关的方法声明）|
| L79-86 | `wp_cellDefaultBgColor` |
| L88-157 | `shouldSkipCorner` |
| L159-175 | `replaced_WCSearchBar_layoutSubviews`（不变）|
| **重写** | `replaced_MMUITableViewCell_layoutSubviews`（瘦身后）|
| **重写** | `replaced_MMUIButton_layoutSubviews`（精简为 orig + 调用 ProfileCardBg）|
| L831-887 | `@implementation ListCornerRadiusHook` / `initListCornerRadiusHook`（Hook 注册不变）|
| L889-1120 | `wp_applyStandardCorner` / `wp_applyCornerForContacts` / `wp_applyBorderAndBg` 等（列表圆角全部保留）|
| L1122-1124 | `applyBorderToView:` |
| L1126-1135 | `wp_findFoldViewInSubviews:` |
| L1218-1281 | `wp_buildUnifiedBorderLayer:` |
| L1283-1298 | `wp_isCurrentDarkMode` |

---

## 3. Step-by-Step 实施步骤

### Step 1：创建新文件 — ProfileCardBgHook.h

**新建文件**：`MioPlugin/Modules/ProfileCardBg/ProfileCardBgHook.h`

```objc
#import <Foundation/Foundation.h>
#import <UIKit/UIKit.h>

@interface ProfileCardBgHook : NSObject

// 入口方法（由 ListCornerRadiusHook 的薄分发层调用）
+ (void)handleCellLayout:(UITableViewCell *)cell;
+ (void)handleButtonLayout:(UIView *)button;

// 资料卡识别
+ (BOOL)isProfileCard:(UIView *)cell;
+ (BOOL)findMMHeadImageViewInSubviews:(NSArray<UIView *> *)subviews;

// 资料卡圆角
+ (void)applyProfileCardCorner:(UIView *)cell
                    cornerRadius:(NSInteger)radius
                         isDark:(BOOL)isDark;

// QR 码隐藏
+ (void)hideQRButtonInCell:(UIView *)cell;
+ (void)hideQRButtonInSubviews:(NSArray<UIView *> *)subviews;

// 图片加载
+ (UIImage *)loadBackgroundImageSync:(BOOL)isDark;
+ (void)loadBackgroundImageForImageView:(UIImageView *)imageView isDark:(BOOL)isDark;
+ (NSString *)cardBackgroundDirectory;

@end
```

**注意**：方法名去掉了 `wp_` 前缀（因为不再和列表圆角混在同一个类里了），但保持参数签名不变。

---

### Step 2：创建新文件 — ProfileCardBgHook.m（核心搬迁）

**新建文件**：`MioPlugin/Modules/ProfileCardBg/ProfileCardBgHook.m`

这个文件的内容 = 从 ListCornerRadiusHook.m **原样搬出**的资料卡相关代码。下面按顺序列出每一块从哪里搬、搬到哪。

#### 2.1 文件头

```objc
#import "ProfileCardBgHook.h"
#import "../../Config/PluginConfig.h"
#import "../../Core/LogManager.h"
#import <objc/runtime.h>
#import <objc/message.h>
```

#### 2.2 搬入：`isProfileCard:` + `findMMHeadImageViewInSubviews:`

**来源**：[ListCornerRadiusHook.m L1137-L1152](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L1137-L1152)

**改动**：
- `[ListCornerRadiusHook wp_isProfileCard:]` → `[ProfileCardBgHook isProfileCard:]`
- `[self wp_findMMHeadImageViewInSubviews:]` → `[ProfileCardBgHook findMMHeadImageViewInSubviews:]`

#### 2.3 搬入：`applyProfileCardCorner:`

**来源**：[L1154-L1190](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L1154-L1190)

**改动**：
- `[ListCornerRadiusHook wp_applyProfileCardCorner:...]` → `[ProfileCardBgHook applyProfileCardCorner:...]`
- 方法体内的 `[ListCornerRadiusHook ...]` 引用改为 `[ProfileCardBgHook ...]`（如果有）

#### 2.4 搬入：`hideQRButtonInCell:` + `hideQRButtonInSubviews:`

**来源**：[L1192-L1216](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L1192-L1216)

**改动**：同上，去掉 `wp_` 前缀，`[self xxx]` 改为 `[ProfileCardBgHook xxx]`。

#### 2.5 搬入：`loadBackgroundImageSync:`

**来源**：[L1300-L1412](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L1300-L1412)

**改动**：
- `[ListCornerRadiusHook wp_loadBackgroundImageSync:]` → `[ProfileCardBgHook loadBackgroundImageSync:]`
- **L1308**：`[ListCornerRadiusHook wp_cardBackgroundDirectory]` → `[ProfileCardBgHook cardBackgroundDirectory]`

#### 2.6 搬入：`cardBackgroundDirectory`

**来源**：[L1414-L1439](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L1414-L1439)

**改动**：去掉 `wp_` 前缀。

#### 2.7 搬入：`loadBackgroundImageForImageView:isDark:`

**来源**：[L1441-L1537](file:///wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L1441-L1537)

**改动**：
- **L1448**：`[[ListCornerRadiusHook wp_cardBackgroundDirectory] copy]` → `[[ProfileCardBgHook cardBackgroundDirectory] copy]`

#### 2.8 ★ 核心：`handleButtonLayout:` （从 MMUIButton Hook 搬入）

**来源**：[L177-L621](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L177-L621) 中 `replaced_MMUIButton_layoutSubviews` 的业务逻辑部分

**完整方法如下**（注意与原 Hook 的区别：不需要调 orig，不需要注册 hook，只做纯业务逻辑）：

```objc
+ (void)handleButtonLayout:(UIView *)button {
    PluginConfig *config = [PluginConfig shared];

    // ★ 极速拒绝链 ★
    if (!config.cardBgEnabled && !config.listCornerRadiusEnabled) return;

    // 第2关：VC 类型
    UIViewController *vc = nil;
    UIResponder *responder = button.nextResponder;
    while (responder) {
        if ([responder isKindOfClass:[UIViewController class]]) {
            vc = (UIViewController *)responder;
            break;
        }
        responder = [responder nextResponder];
    }
    if (!vc || ![NSStringFromClass([vc class]) isEqualToString:@"MoreViewController"]) return;

    // 第3关：MMHeadImageView 存在
    BOOL foundHead = NO;
    for (UIView *sub in button.subviews) {
        if ([sub isKindOfClass:NSClassFromString(@"MMHeadImageView")]) {
            foundHead = YES; break;
        }
    }
    if (!foundHead) return;

    // 第4关：高度过滤
    CGFloat selfHeight = button.frame.size.height;
    if (selfHeight <= 50.0) return;

    BOOL isDark = NO;
    if (@available(iOS 13.0, *)) {
        isDark = (vc.traitCollection.userInterfaceStyle == UIUserInterfaceStyleDark);
    }

    BOOL needsFullCardBg = config.cardBgEnabled;

    // ══════════════════════════════════════════
    // 卡片背景专属操作（只在 cardBgEnabled 时执行）
    // ══════════════════════════════════════════
    if (needsFullCardBg) {

        // ── HideCard 分支 ──
        if (config.cardBgHidden) {
            button.backgroundColor = [UIColor clearColor];
            button.layer.masksToBounds = NO;
            button.layer.cornerRadius = 0;
            button.layer.borderWidth = 0;

            Ivar bgIvar = class_getInstanceVariable([button class], "m_bgImageView");
            if (bgIvar) {
                id bgImgView = object_getIvar(button, bgIvar);
                if (bgImgView && [bgImgView isKindOfClass:[UIImageView class]]) {
                    [(UIImageView *)bgImgView setImage:nil];
                    [(UIImageView *)bgImgView setBackgroundColor:[UIColor clearColor]];
                    [(UIImageView *)bgImgView setHidden:YES];
                }
                object_setIvar(button, bgIvar, nil);
            }
            for (UIView *sub in button.subviews) { sub.hidden = YES; }

            if (config.listHideRightQRCode) {
                [ProfileCardBgHook hideQRButtonInCell:button];
            }
            return;
        }

        // ── 背景图分支 ──
        button.backgroundColor = [UIColor clearColor];

        // 清除微信原生 m_bgImageView
        Ivar bgIvar = class_getInstanceVariable([button class], "m_bgImageView");
        if (bgIvar) {
            id bgImgView = object_getIvar(button, bgIvar);
            if (bgImgView && [bgImgView isKindOfClass:[UIImageView class]]) {
                [(UIImageView *)bgImgView setImage:nil];
                [(UIImageView *)bgImgView setBackgroundColor:[UIColor clearColor]];
                [(UIImageView *)bgImgView setHidden:YES];
            }
            object_setIvar(button, bgIvar, nil);
        }

        // FIX-WHITE: 隐藏白色视图（原 L298-356 整块搬入）
        {
            static const NSInteger kMioBgImageTag_local = 999902;
            for (NSInteger i = button.subviews.count - 1; i >= 0; i--) {
                UIView *sub = button.subviews[i];
                if (sub.tag == kMioBgImageTag_local) continue;
                if ([sub isKindOfClass:NSClassFromString(@"MMHeadImageView")]) continue;
                if ([sub isKindOfClass:[UILabel class]]) continue;
                if ([NSStringFromClass([sub class]) isEqualToString:@"MMCPLabel"]) continue;
                if ([NSStringFromClass([sub class]) isEqualToString:@"MMUILabel"]) continue;
                if ([NSStringFromClass([sub class]) isEqualToString:@"TextStatePublishEntryButton"]) continue;
                if ([NSStringFromClass([sub class]) isEqualToString:@"TextStateFriendTopicButton"]) continue;
                if ([sub isKindOfClass:[UIImageView class]] && sub.tag != 0) continue;

                UIColor *subBg = sub.backgroundColor;
                BOOL shouldHide = NO;
                if (subBg) {
                    NSString *bgClassName = NSStringFromClass([subBg class]);
                    if ([bgClassName containsString:@"DynamicProvider"] ||
                        [bgClassName containsString:@"UIDynamic"]) {
                        shouldHide = YES;
                    } else if ([subBg isEqual:[UIColor whiteColor]]) {
                        shouldHide = YES;
                    } else {
                        CGFloat r=0,g=0,b=0,a=0;
                        if ([subBg getRed:&r green:&g blue:&b alpha:&a]) {
                            if (r>0.95 && g>0.95 && b>0.95 && a>0.95) shouldHide=YES;
                        }
                    }
                }
                if ([NSStringFromClass([sub class]) isEqualToString:@"UIView"] && subBg) {
                    shouldHide = YES;
                }
                if (shouldHide) sub.hidden = YES;
            }
        }

        // bgImageView 创建/去重/加载（原 L358-501 整块搬入）
        // 注意：[ListCornerRadiusHook wp_loadBackgroundImageSync:]
        //       改为 [ProfileCardBgHook loadBackgroundImageSync:]
        static const NSInteger kMioBgImageTag = 999902;
        static const void *kMioBgLoadedKey = &kMioBgLoadedKey;

        UIImageView *existingBgImg = nil;
        BOOL alreadyLoaded = [objc_getAssociatedObject(button, kMioBgLoadedKey) boolValue];
        for (UIView *sub in button.subviews) {
            if (sub.tag == kMioBgImageTag && [sub isKindOfClass:[UIImageView class]]) {
                existingBgImg = (UIImageView *)sub; break;
            }
        }

        // 分支A：已存在且已加载 → 更新 frame
        if (existingBgImg != nil && alreadyLoaded) {
            UIView *parentCell = nil;
            UIView *p = button.superview;
            if (p) parentCell = p.superview;
            CGRect cellBounds = parentCell ? parentCell.bounds : button.bounds;
            CGFloat imgW = cellBounds.size.width;
            CGFloat imgH = cellBounds.size.height;
            CGFloat offsetX = isDark ? config.cardBgDarkOffsetX : config.cardBgLightOffsetX;
            CGFloat offsetY = isDark ? config.cardBgDarkOffsetY : config.cardBgLightOffsetY;
            existingBgImg.frame = CGRectMake(offsetX, offsetY, imgW, imgH);

            NSInteger layerPos = isDark ? config.cardBgDarkLayer : config.cardBgLightLayer;
            if (layerPos == 1) [button bringSubviewToFront:existingBgImg];

            goto APPLY_CORNER;
        }

        // 分支B：不存在或未加载 → 创建
        if (existingBgImg != nil) [existingBgImg removeFromSuperview];

        UIImageView *btnBgImg = [[UIImageView alloc] init];
        btnBgImg.tag = kMioBgImageTag;
        btnBgImg.clipsToBounds = YES;
        btnBgImg.userInteractionEnabled = NO;

        NSInteger fillMode = config.cardBgFillMode;
        switch (fillMode) {
            case 1: btnBgImg.contentMode = UIViewContentModeScaleAspectFit; break;
            case 2: btnBgImg.contentMode = UIViewContentModeScaleAspectFill; break;
            default: btnBgImg.contentMode = UIViewContentModeScaleToFill; break;
        }
        [button insertSubview:btnBgImg atIndex:0];

        UIView *parentCell = nil;
        UIView *p = button.superview;
        if (p) parentCell = p.superview;
        CGRect cellBounds = parentCell ? parentCell.bounds : button.bounds;
        CGFloat imgW = cellBounds.size.width;
        CGFloat imgH = cellBounds.size.height;
        CGFloat offsetX = isDark ? config.cardBgDarkOffsetX : config.cardBgLightOffsetX;
        CGFloat offsetY = isDark ? config.cardBgDarkOffsetY : config.cardBgLightOffsetY;
        btnBgImg.frame = CGRectMake(offsetX, offsetY, imgW, imgH);

        objc_setAssociatedObject(button, kMioBgLoadedKey, @NO,
                                 OBJC_ASSOCIATION_RETAIN_NONATOMIC);

        __weak UIImageView *weakBgImg = btnBgImg;
        __weak UIView *weakSelf = button;
        BOOL capturedIsDark = isDark;

        dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), ^{
            __strong UIImageView *strongBgImg = weakBgImg;
            __strong UIView *strongSelf = weakSelf;
            if (!strongBgImg || !strongSelf) return;

            UIImage *resultImage = [ProfileCardBgHook loadBackgroundImageSync:capturedIsDark];

            dispatch_async(dispatch_get_main_queue(), ^{
                __strong UIImageView *finalImg = weakBgImg;
                __strong UIView *finalSelf = weakSelf;
                if (!finalImg || !finalSelf) return;

                if (resultImage) {
                    finalImg.image = resultImage;
                    finalImg.alpha = 1.0;
                    finalImg.hidden = NO;
                    objc_setAssociatedObject(finalSelf, kMioBgLoadedKey, @YES,
                                             OBJC_ASSOCIATION_RETAIN_NONATOMIC);
                } else {
                    BOOL dark = NO;
                    if (@available(iOS 13.0, *)) {
                        UIViewController *vCtrl = nil;
                        UIResponder *resp = finalSelf.nextResponder;
                        while (resp) {
                            if ([resp isKindOfClass:[UIViewController class]]) {
                                vCtrl = (UIViewController *)resp; break;
                            }
                            resp = resp.nextResponder;
                        }
                        if (vCtrl) dark = (vCtrl.traitCollection.userInterfaceStyle == UIUserInterfaceStyleDark);
                    }
                    PluginConfig *cfg = [PluginConfig shared];
                    UIColor *cardBg = [cfg colorFromHex:dark
                        ? cfg.listCardDarkBgColor : cfg.listCardLightBgColor];
                    if (cardBg) finalSelf.backgroundColor = cardBg;
                    objc_setAssociatedObject(finalSelf, kMioBgLoadedKey, @YES,
                                             OBJC_ASSOCIATION_RETAIN_NONATOMIC);
                }
            });
        });

    } // end needsFullCardBg

APPLY_CORNER:
    // ── 圆角 + 边框 + QR码隐藏（始终执行）──
    {
        NSInteger radius = (NSInteger)config.listCellCornerRadius;
        if (radius == 0) radius = 18;

        [ProfileCardBgHook applyProfileCardCorner:button
                                     cornerRadius:radius
                                          isDark:isDark];

        if (config.listHideRightQRCode) {
            [ProfileCardBgHook hideQRButtonInCell:button];
        }

        BOOL skipMasksToBounds = (config.cardBgFillMode == 3);
        button.layer.masksToBounds = skipMasksToBounds ? NO : YES;
    }
}
```

**关键变化说明**：
- 不需要 `static void replaced_...` —— 这是普通类方法 `+ (void)handleButtonLayout:`
- 不需要调用 `orig` —— orig 已经在 ListCornerRadiusHook 的薄分发层中调过了
- 不需要第1关的功能开关判断 —— 由薄分发层决定是否调用此方法
- `[ListCornerRadiusHook wp_xxx]` → `[ProfileCardBgHook xxx]`（所有内部调用）

#### 2.9 ★ 核心：`handleCellLayout:` （Cell 透明化）

```objc
+ (void)handleCellLayout:(UITableViewCell *)cell {
    PluginConfig *config = [PluginConfig shared];

    if (!config.cardBgEnabled) return;

    NSString *className = nil;
    UIViewController *vc = nil;
    UIResponder *responder = cell.nextResponder;
    while (responder) {
        if ([responder isKindOfClass:[UIViewController class]]) {
            vc = (UIViewController *)responder;
            className = NSStringFromClass([vc class]);
            break;
        }
        responder = [responder nextResponder];
    }
    if (!vc) return;

    BOOL isMoreVC = [className isEqualToString:@"MoreViewController"];
    if (!isMoreVC) return;

    BOOL isProfileCard = [ProfileCardBgHook isProfileCard:cell];
    if (!isProfileCard) return;

    // ★ 到这里的一定是 MoreVC + cardBgEnabled + 资料卡 Cell ★
    // 不需要三条件判断了——能进到这里的全是目标 Cell

    UIView *cellView = cell;

    WPLog(@"CardBg-Diag", @"[CELL-TRANSPARENCY] Before: cellBg=%@, cellMasks=%d, cellBorder=%.1f",
          cellView.backgroundColor, cellView.layer.masksToBounds, cellView.layer.borderWidth);

    cellView.backgroundColor = [UIColor clearColor];
    cellView.layer.borderWidth = 0;
    cellView.layer.masksToBounds = NO;

    UIView *cv = cell.contentView;
    if (cv) {
        cv.backgroundColor = [UIColor clearColor];
        cv.layer.masksToBounds = NO;
    }

    if ([cellView respondsToSelector:@selector(backgroundView)]) {
        UIView *bgv = [cellView backgroundView];
        if (bgv) {
            bgv.backgroundColor = [UIColor clearColor];
            bgv.hidden = YES;
        }
    }
    if ([cellView respondsToSelector:@selector(selectedBackgroundView)]) {
        UIView *sbgv = [cellView selectedBackgroundView];
        if (sbgv) sbgv.backgroundColor = [UIColor clearColor];
    }

    WPLog(@"CardBg-Diag", @"[CELL-TRANSPARENCY] After: cellBg=%@, cellMasks=%d",
          cellView.backgroundColor, cellView.layer.masksToBounds);

    // 注意：不设 masksToBounds=YES，由 MMUIButton 层负责裁剪
}
```

**关键变化说明**：
- 入口就检查 `!cardBgEnabled? return` —— 干净利落
- 不需要 `isMoreVC && cardBgEnabled && isProfileCardCell` 三条件 —— 前两步已经过滤了
- 不需要 margin / bgColor / corner —— 这些是列表圆角的事

#### 2.10 @implementation 收尾

```objc
@implementation ProfileCardBgHook
// 上面 2.2 ~ 2.9 所有方法都在这里
@end
```

---

### Step 3：改造 ListCornerRadiusHook.m 为薄分发层

#### 3.1 修改 `@interface` 声明（L23-77）

**删除以下方法声明**（已搬到 ProfileCardBgHook）：

```objc
// 删除这些行:
+ (void)wp_isProfileCard:(UIView *)cell;
+ (void)wp_findMMHeadImageViewInSubviews:(NSArray<UIView *> *)subviews;
+ (void)wp_applyProfileCardCorner:(UIView *)cell cornerRadius:(NSInteger)radius isDark:(BOOL)isDark;
+ (void)wp_hideQRButtonInCell:(UIView *)cell;
+ (void)wp_hideQRButtonInSubviews:(NSArray<UIView *> *)subviews;
+ (UIImage *)wp_loadBackgroundImageSync:(BOOL)isDark;
+ (void)wp_loadBackgroundImageForImageView:(UIImageView *)imageView isDark:(BOOL)isDark;
+ (NSString *)wp_cardBackgroundDirectory;
```

**新增 import**：

```objc
// 在文件顶部 #import 区域添加:
#import "../ProfileCardBg/ProfileCardBgHook.h"
```

#### 3.2 重写 `replaced_MMUIButton_layoutSubviews`（当前 L177-621）

**替换为**：

```objc
static void replaced_MMUIButton_layoutSubviews(id self, SEL _cmd) {
    // 先让微信完成原始布局
    if (_orig_MMUIButton_layoutSubviews) {
        ((void (*)(id, SEL))_orig_MMUIButton_layoutSubviews)(self, _cmd);
    }

    // ★ 分发到资料卡模块 ★
    [ProfileCardBgHook handleButtonLayout:(UIView *)self];

    // 列表圆角不需要在 MMUIButton 层做任何事
}
```

**整个 L177-621（~450行）替换为上面 10 行。**

#### 3.3 重写 `replaced_MMUITableViewCell_layoutSubviews`（当前 L623-829）

**替换为**：

```objc
static void replaced_MMUITableViewCell_layoutSubviews(id self, SEL _cmd) {
    // 懒注册 MMUIButton hook（如果还没注册的话）
    if (!_orig_MMUIButton_layoutSubviews) {
        Class MMUIButtonClass = objc_getClass("MMUIButton");
        if (MMUIButtonClass) {
            MSHookMessageEx(
                MMUIButtonClass,
                @selector(layoutSubviews),
                (IMP)replaced_MMUIButton_layoutSubviews,
                &_orig_MMUIButton_layoutSubviews
            );
        }
    }

    PluginConfig *config = [PluginConfig shared];

    // ★ 列表圆角入口守卫：只看自己的开关 ★
    if (!config.listCornerRadiusEnabled) {
        // 列表圆角关闭 → 但仍需调用 orig + 让资料卡处理透明化
        if (_orig_MMTableViewCell_layoutSubviews) {
            _orig_MMUITableViewCell_layoutSubviews(self, _cmd);
        }
        [ProfileCardBgHook handleCellLayout:(UITableViewCell *)self];
        return;
    }

    UIViewController *vc = findParentViewController((UIView *)self);
    if (!vc) {
        if (_orig_MMTableViewCell_layoutSubviews) {
            ((void (*)(id, SEL))_orig_MMUITableViewCell_layoutSubviews)(self, _cmd);
        }
        return;
    }
    NSString *className = NSStringFromClass([vc class]);

    if (shouldSkipCorner(vc)) {
        if (_orig_MMUITableViewCell_layoutSubviews) {
            ((void (*)(id, SEL))_orig_MMUITableViewCell_layoutSubviews)(self, _cmd);
        }
        return;
    }

    UIView *cellView = (UIView *)self;

    // ★ margin 代码（只在 listCornerRadiusEnabled 时执行，进入此处说明已开启）★
    CGFloat margin = config.listCellMargin;
    if (margin > 0) {
        CGFloat currentX = cellView.frame.origin.x;
        UIView *superview = cellView.superview;
        CGFloat superX = superview ? superview.frame.origin.x : 0;
        CGFloat targetX = (margin > superX) ? margin - superX : 0;
        CGFloat containerW = superview ? superview.bounds.size.width
                                       : [UIScreen mainScreen].bounds.size.width;
        CGFloat targetW = containerW - 2.0 * margin;
        CGFloat currentW = cellView.frame.size.width;
        if (currentX != targetX || fabs(currentW - targetW) > 0.5) {
            CGRect f = cellView.frame;
            f.origin.x = targetX;
            f.size.width = targetW;
            cellView.frame = f;
        }
    }

    // ★ orig ★
    if (_orig_MMTableViewCell_layoutSubviews) {
        _orig_MMUITableViewCell_layoutSubviews(self, _cmd);
    }

    // ★ bgColor 设置 ★
    static NSSet *bgColorSkipList = nil;
    static dispatch_once_t onceBgToken;
    dispatch_once(&onceBgToken, ^{
        bgColorSkipList = [NSSet setWithObjects:
            @"WCTimeLineViewController", @"WCAccountLoginUsersViewController",
            @"SessionSelectController", @"WCListViewController",
            @"BrandNotificationListViewController", @"BrandNewSessionViewController",
            @"BaseMsgContentViewController", @"BraceletRankProfileViewController",
            @"BraceletRankViewController", @"WCRedEnvelopesRedEnvelopesDetailViewController",
            @"MsgRecordDetailViewController", @"ChatRoomInfoViewController",
            @"ContactInfoViewController", @"AddFriendEntryViewController",
            @"AddContactToChatRoomViewController", @"SayHelloViewController",
            @"FTSHomeViewController", @"MMFinderPivotLiveViewController",
            @"WCSearchController", nil];
    });
    if (![bgColorSkipList containsObject:className]) {
        BOOL isDark = NO;
        if (@available(iOS 13.0, *)) {
            isDark = (vc.traitCollection.userInterfaceStyle == UIUserInterfaceStyleDark);
        }
        UIColor *customBg = [config colorFromHex:isDark
            ? config.listCellDarkBgColor : config.listCellLightBgColor];
        ((UIView *)self).backgroundColor = customBg ?: wp_cellDefaultBgColor(isDark);
    }

    // ★ corner 圆角设置（不需要 if 守卫！进入这里一定是因为 listCornerRadiusEnabled==YES）★
    NSInteger cornerRadius = (NSInteger)config.listCellCornerRadius;
    if (cornerRadius == 0) cornerRadius = 18;

    BOOL isContacts = [className isEqualToString:@"ContactsViewController"];
    BOOL isFTSHome = [className isEqualToString:@"FTSHomeViewController"];

    UIView *parent = cellView.superview;
    UITableView *tableView = nil;
    while (parent) {
        if ([parent isKindOfClass:[UITableView class]]) {
            tableView = (UITableView *)parent; break;
        }
        parent = parent.superview;
    }
    if (!tableView) return;

    NSIndexPath *indexPath = [tableView indexPathForCell:(UITableViewCell *)self];
    if (!indexPath) return;

    NSInteger section = indexPath.section;
    NSInteger row = indexPath.row;
    NSInteger totalRows = [tableView numberOfRowsInSection:section];

    if (isContacts) {
        [ListCornerRadiusHook wp_applyCornerForContacts:cellView
                                             tableView:tableView
                                             indexPath:indexPath
                                               section:section
                                                   row:row
                                                 total:totalRows
                                          cornerRadius:cornerRadius
                                              isFTSHome:isFTSHome];
    } else {
        [ListCornerRadiusHook wp_applyStandardCorner:cellView
                                          tableView:tableView
                                          indexPath:indexPath
                                            section:section
                                                row:row
                                              total:totalRows
                                       cornerRadius:cornerRadius
                                          isFTSHome:isFTSHome
                                          className:className];
    }

    // ★ 分发到资料卡模块（透明化处理）★
    [ProfileCardBgHook handleCellLayout:(UITableViewCell *)self];

    // ★ 非 MoreVC 资料卡 Cell 的正常收尾 ★
    cellView.layer.masksToBounds = YES;
}
```

**关键变化**：
- 删除了 `isProfileCardCell` / `needsCardBgTransparency` 变量
- 删除了内联的透明化代码块（L788-825）→ 替换为 `[ProfileCardBgHook handleCellLayout:]` 一行调用
- **删除了 `!listCornerRadiusEnabled && !cardBgEnabled` 的联合入口判断** → 改为各自独立
- **圆角代码不再需要 `if (listCornerRadiusEnabled)` 守卫**
- margin 代码也不需要额外守卫（进入分支时已确认 listCornerRadiusEnabled==YES）

#### 3.4 删除已搬迁的方法实现

从 `@implementation ListCornerRadiusHook` 中**删除**以下方法的实现（已搬到 ProfileCardBgHook.m）：

| 方法 | 当前位置 |
|:----:|:--------:|
| `wp_isProfileCard:` | L1137 |
| `wp_findMMHeadImageViewInSubviews:` | L1141 |
| `wp_applyProfileCardCorner:` | L1154 |
| `wp_hideQRButtonInCell:` | L1192 |
| `wp_hideQRButtonInSubviews:` | L1205 |
| `wp_loadBackgroundImageSync:` | L1300 |
| `wp_cardBackgroundDirectory` | L1414 |
| `wp_loadBackgroundImageForImageView:isDark:` | L1441 |

#### 3.5 `initListCornerRadiusHook` 保持不变

[L833-887](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m#L833-L887) 的 Hook 注册逻辑完全不动。Hook 仍然注册在这个类里，只是方法体变成了薄分发层。

---

## 4. 改造前后对比

### MMUIButton Hook

| | 改造前 | 改造后 |
|:--:|:------|:------:|
| 代码量 | ~450 行 (L177-621) | **8 行** |
| 职责 | 列表圆角判断 + 资料卡全部逻辑 | **只调 orig + 分发给 ProfileCardBg** |
| 需要了解 cardBgEnabled? | **是** | **否** |
| 需要了解 listCornerRadiusEnabled? | **是** | **否** |

### Cell Hook

| | 改造前 | 改造后 |
|:--:|:------|:------:|
| 代码量 | ~200 行 (L623-829) | ~120 行（去掉透明化内联代码）|
| 入口条件 | `!corner && !cardBg`（耦合） | **各管各的**（corner 在外层，cardBg 在 ProfileCardBg 内部）|
| 圆角守卫 | 需要 `if (listCornerRadiusEnabled)` | **不需要** |
| 透明化 | 内联 40 行三条件判断 | **一行**: `[ProfileCardBgHook handleCellLayout:self]` |
| isProfileCardCell 判断 | 有 | **无**（交给 ProfileCardBg）|

---

## 5. 执行清单

按以下顺序操作，每步完成后编译验证：

- [ ] **Step 1**：创建 `ProfileCardBg/ProfileCardBgHook.h`
- [ ] **Step 2**：创建 `ProfileCardBg/ProfileCardBgHook.m`（2.1 ~ 2.10 全部内容）
- [ ] **Step 3.1**：修改 `ListCornerRadiusHook.h` 的 `@interface`（删除搬迁的方法声明）
- [ ] **Step 3.2**：替换 `replaced_MMUIButton_layoutSubviews` 为薄分发版（8行）
- [ ] **Step 3.3**：替换 `replaced_MMUITableViewCell_layoutSubviews` 为新版
- [ ] **Step 3.4**：删除 `@implementation` 中已搬迁的 8 个方法
- [ ] **Step 3.5**：确认 `initListCornerRadiusHook` 不变
- [ ] **编译**：确认无语法错误
- [ ] **测试场景 1**：全开（圆角 ON + 卡片 ON）
- [ ] **测试场景 2**：只开圆角
- [ ] **测试场景 3**：只开卡片背景（**重点验证其他 Cell 不被加圆角**）
- [ ] **测试场景 4**：全关

---

## 6. 回滚方案

如果重构后有问题：
1. 用 git 回退 `ListCornerRadiusHook.m` 和 `ListCornerRadiusHook.h` 到上一版本
2. 删除 `ProfileCardBg/` 目录
3. 一切恢复原状

因为不是修改现有文件而是**新建独立文件 + 瘦身旧文件**，回滚非常干净。
