# 附件布局功能 — 开发操作步骤

## 当前状态

| 组件 | 状态 | 文件 |
|------|:----:|------|
| UI 设置页面 | 已完成 | `Modules/SettingEntry/WPUIAttachmentLayoutVC.m` |
| 导航跳转 | 已完成 | `Modules/SettingEntry/WPUIVC.m` → `openAttachLayout:` |
| Hook 实现 | **未开始** | 需要创建 `Modules/Layout/UIAttachLayoutHook.m` |
| 模块注册 | **未开始** | 需要修改 `Registry/FeatureModuleRegistry.m` |

---

## 步骤一：创建 Hook 实现文件

### 1.1 新建头文件

**文件**: `Modules/Layout/UIAttachLayoutHook.h`

```objc
#import <Foundation/Foundation.h>

@interface UIAttachLayoutHook : NSObject
+ (void)install;
@end
```

### 1.2 新建实现文件

**文件**: `Modules/Layout/UIAttachLayoutHook.m`

**对齐依据**: 微信优化 `FUN_00005a14` (Hook安装) + `FUN_00005a9c` (layoutSubviews) + `FUN_00005d88` (numberOfCols) + `FUN_00005e70` (numberOfRows)

```objc
//
//  UIAttachLayoutHook.m — 附件布局优化
//  精确对齐微信优化 123456.c:
//    FUN_00005a14 — Hook 安装
//    FUN_00005a9c — SelectAttachmentView.layoutSubviews
//    FUN_00005d88 — SelectAttachmentViewController.numberOfCols
//    FUN_00005e70 — SelectAttachmentViewController.numberOfRows
//

#import "UIAttachLayoutHook.h"
#import <Foundation/Foundation.h>
#import <UIKit/UIKit.h>
#import <objc/runtime.h>
#import <substrate.h>

// ═══════════════════════════════════════════════════════
// 配置键
// ═══════════════════════════════════════════════════════
static NSString *const kEnabledKey = @"AttachLayoutEnabled";
static NSString *const kColumnsKey = @"AttachLayout_Columns";
static NSString *const kRowsKey    = @"AttachLayout_Rows";

// ═══════════════════════════════════════════════════════
// 读取配置
// ═══════════════════════════════════════════════════════
static inline BOOL attachLayoutEnabled(void) {
    return [[NSUserDefaults standardUserDefaults] boolForKey:kEnabledKey];
}

static inline NSInteger attachLayoutColumns(void) {
    // 微信优化默认值 = 5 (123456.c:5153)
    NSString *val = [[NSUserDefaults standardUserDefaults] stringForKey:kColumnsKey];
    if (!val || val.length == 0) return 5;
    return [val integerValue];
}

static inline NSInteger attachLayoutRows(void) {
    // 微信优化默认值 = 2 (123456.c:5186)
    NSString *val = [[NSUserDefaults standardUserDefaults] stringForKey:kRowsKey];
    if (!val || val.length == 0) return 2;
    return [val integerValue];
}

// ═══════════════════════════════════════════════════════
// 原始 IMP 存储 (对齐 DAT_0013a8c0/c8/d0)
// ═══════════════════════════════════════════════════════
static IMP __orig_SelectAttachmentView_layoutSubviews = NULL;
static IMP __orig_SelectAttachmentViewController_numberOfCols = NULL;
static IMP __orig_SelectAttachmentViewController_numberOfRows = NULL;

// ═══════════════════════════════════════════════════════
// Hook ①: SelectAttachmentView.layoutSubviews
//   对齐 FUN_00005a9c (123456.c:5000-5124)
//   当 rows > 2 时动态调整 ScrollView 以支持垂直滚动
// ═══════════════════════════════════════════════════════
static void hook_SelectAttachmentView_layoutSubviews(id self, SEL _cmd) {
    // Step 0: 调用原始 layoutSubviews
    ((void (*)(id, SEL))__orig_SelectAttachmentView_layoutSubviews)(self, _cmd);

    // Step 1: 检查功能是否开启
    if (!attachLayoutEnabled()) return;

    // Step 2: 读取行数，仅当 > 2 时才需要滚动
    NSInteger rows = attachLayoutRows();
    if (rows <= 2) return;

    // Step 3: 通过 ivar 获取 _scrollView
    Ivar ivar = class_getInstanceVariable([self class], "_scrollView");
    if (!ivar) return;
    UIScrollView *scrollView = object_getIvar(self, ivar);
    if (!scrollView) return;

    // Step 4: 遍历 ScrollView 的所有子视图，找到 UIButton 的最大 maxY
    CGFloat maxY = 0.0;
    for (UIView *subview in scrollView.subviews) {
        if ([subview isKindOfClass:[UIButton class]]) {
            CGFloat buttonMaxY = CGRectGetMaxY(subview.frame);
            if (buttonMaxY > maxY) {
                maxY = buttonMaxY;
            }
        }
    }

    // Step 5: 如果内容超出当前视图高度，调整 ScrollView
    CGFloat scrollHeight = scrollView.frame.size.height;
    if (scrollHeight < maxY) {
        // contentSize.height = maxY + 25 (底部留白)
        CGFloat contentWidth = scrollView.bounds.size.width;
        scrollView.contentSize = CGSizeMake(contentWidth, maxY + 25.0);

        // 启用垂直滚动 (对齐微信优化: setScrollEnabled:YES)
        scrollView.scrollEnabled = YES;

        // 显示垂直滚动条 (对齐微信优化: setShowsVerticalScrollIndicator:YES)
        scrollView.showsVerticalScrollIndicator = YES;

        // 隐藏水平滚动条 (对齐微信优化: setShowsHorizontalScrollIndicator:NO)
        scrollView.showsHorizontalScrollIndicator = NO;

        // 启用弹性回弹 (对齐微信优化: setBounces:YES)
        scrollView.bounces = YES;
    }
}

// ═══════════════════════════════════════════════════════
// Hook ②: SelectAttachmentViewController.numberOfCols
//   对齐 FUN_00005d88 (123456.c:5128-5157)
//   默认返回 5 列 (微信优化默认)
// ═══════════════════════════════════════════════════════
static long hook_numberOfCols(id self, SEL _cmd) {
    if (!attachLayoutEnabled()) {
        return ((long (*)(id, SEL))__orig_SelectAttachmentViewController_numberOfCols)(self, _cmd);
    }
    return attachLayoutColumns();
}

// ═══════════════════════════════════════════════════════
// Hook ③: SelectAttachmentViewController.numberOfRows
//   对齐 FUN_00005e70 (123456.c:5161-5190)
//   默认返回 2 行 (微信优化默认)
// ═══════════════════════════════════════════════════════
static long hook_numberOfRows(id self, SEL _cmd) {
    if (!attachLayoutEnabled()) {
        return ((long (*)(id, SEL))__orig_SelectAttachmentViewController_numberOfRows)(self, _cmd);
    }
    return attachLayoutRows();
}

// ═══════════════════════════════════════════════════════
// +install — Hook 安装 (对齐 FUN_00005a14)
// ═══════════════════════════════════════════════════════
@implementation UIAttachLayoutHook

+ (void)install {
    Class cls;

    // ① SelectAttachmentView.layoutSubviews
    cls = objc_getClass("SelectAttachmentView");
    if (cls) {
        MSHookMessageEx(cls, sel_registerName("layoutSubviews"),
                        (IMP)hook_SelectAttachmentView_layoutSubviews,
                        (IMP *)&__orig_SelectAttachmentView_layoutSubviews);
    }

    // ② SelectAttachmentViewController.numberOfCols
    cls = objc_getClass("SelectAttachmentViewController");
    if (cls) {
        MSHookMessageEx(cls, sel_registerName("numberOfCols"),
                        (IMP)hook_numberOfCols,
                        (IMP *)&__orig_SelectAttachmentViewController_numberOfCols);
    }

    // ③ SelectAttachmentViewController.numberOfRows
    cls = objc_getClass("SelectAttachmentViewController");
    if (cls) {
        MSHookMessageEx(cls, sel_registerName("numberOfRows"),
                        (IMP)hook_numberOfRows,
                        (IMP *)&__orig_SelectAttachmentViewController_numberOfRows);
    }
}

@end
```

### 1.3 关键对齐检查清单

| 微信优化原始 | Mio助手实现 | 对齐状态 |
|-------------|------------|:------:|
| `FUN_00005a14`: MSHookMessageEx × 3 | `+install` 3次 MSHookMessageEx | ✅ |
| `FUN_00005a9c`: 先调原始 layoutSubviews | `((void(*))orig)(self, _cmd)` 在函数首行 | ✅ |
| `FUN_00005a9c`: `boolForKey:enabled` 检查 | `attachLayoutEnabled()` | ✅ |
| `FUN_00005a9c`: `rows > 2` 才操作 | `if (rows <= 2) return` | ✅ |
| `FUN_00005a9c`: ivar `_scrollView` 直接访问 | `class_getInstanceVariable` + `object_getIvar` | ✅ |
| `FUN_00005a9c`: 遍历 UIButton 子视图 | `for (...subviews) { isKindOfClass:[UIButton class] }` | ✅ |
| `FUN_00005a9c`: `contentSize = (width, maxY + 25)` | `CGSizeMake(width, maxY + 25.0)` | ✅ |
| `FUN_00005a9c`: `setScrollEnabled:YES` | `scrollView.scrollEnabled = YES` | ✅ |
| `FUN_00005a9c`: `setVerticalScrollIndicator:YES` | `scrollView.showsVerticalScrollIndicator = YES` | ✅ |
| `FUN_00005a9c`: `setHorizontalScrollIndicator:NO` | `scrollView.showsHorizontalScrollIndicator = NO` | ✅ |
| `FUN_00005a9c`: `setBounces:YES` | `scrollView.bounces = YES` | ✅ |
| `FUN_00005d88`: default cols = 5 | `attachLayoutColumns()` → `return 5` | ✅ |
| `FUN_00005e70`: default rows = 2 | `attachLayoutRows()` → `return 2` | ✅ |

---

## 步骤二：注册 Hook 到模块系统

### 2.1 修改 FeatureModuleRegistry.m

**文件**: `Registry/FeatureModuleRegistry.m`

**① 添加 import**（在文件顶部 import 区域末尾添加）:

```objc
#import "../Modules/Layout/UIAttachLayoutHook.h"  // 新增
```

**② 修改 "layout" 模块的 `hookInstallerClasses`**（约第 59 行）:

改前:
```objc
hookInstallerClasses:@[[TintHook class], [UIPurifyHook class],
                        [UISimplifyHook class], [HideAvatarHook class]]
```

改后:
```objc
hookInstallerClasses:@[[TintHook class], [UIPurifyHook class],
                        [UISimplifyHook class], [HideAvatarHook class],
                        [UIAttachLayoutHook class]]  // 新增
```

### 2.2 完整修改后的模块注册

```objc
[FeatureModuleDescriptor moduleWithIdentifier:@"layout"
                                        title:@"界面布局"
                                     subtitle:@"外观颜色、深色模式"
                                 sectionTitle:@"通用"
                              controllerClass:[SettingLayoutFunctionController class]
                         hookInstallerClasses:@[
                             [TintHook class],
                             [UIPurifyHook class],
                             [UISimplifyHook class],
                             [HideAvatarHook class],
                             [UIAttachLayoutHook class]  // ← 新增
                         ]],
```

---

## 步骤三：构建配置校验

### 3.1 确认编译文件

确保以下文件已被添加到 Xcode 项目的编译源中：

```
Modules/Layout/UIAttachLayoutHook.m  ← 新增
```

（头文件 `UIAttachLayoutHook.h` 不需要加入 Compile Sources）

### 3.2 配置清单

| 配置项 | NSUserDefaults Key | 类型 | 默认值 | UI 控件 |
|--------|-------------------|------|--------|---------|
| 总开关 | `AttachLayoutEnabled` | BOOL | NO | UISwitch |
| 每行列数 | `AttachLayout_Columns` | NSString | `@"5"` | EditableRow |
| 显示行数 | `AttachLayout_Rows` | NSString | `@"2"` | EditableRow |

### 3.3 运行时验证流程

```
1. 启动微信 → 进入插件设置 → 找到「界面布局」
2. 点击「附件布局优化」进入 WPUIAttachmentLayoutVC
3. 开启总开关
4. 设置「每行列数」= 4
5. 设置「显示行数」= 3
6. 进入聊天界面 → 点击「+」按钮
7. 验证：附件面板为 4列×3行，共12个按钮可垂直滚动
```

---

## 步骤四：功能验证

### 4.1 正常模式（默认配置）

| 操作 | 预期结果 |
|------|----------|
| 总开关 OFF | 附件面板与微信原始完全一致（4列2行，不分页） |
| 总开关 ON，行数列数均未设置 | 5列2行，10个按钮平铺（微信优化默认值） |

### 4.2 自定义模式

| columns | rows | 预期结果 |
|---------|------|----------|
| 5 | 1 | 单行显示5个按钮，无滚动 |
| 4 | 3 | 4列3行，12个按钮，垂直滚动 |
| 3 | 5 | 3列5行，15个按钮，垂直滚动 |
| 6 | 2 | 6列2行，12个按钮平铺 |

### 4.3 边界测试

| 测试场景 | 预期结果 |
|----------|----------|
| columns = 0 | 所有按钮挤在一列（极限情况） |
| rows = 100 | 垂直滚动 + contentSize 正确计算 + 底部留白 25pt |
| 关闭开关后立即打开附件面板 | 恢复微信原始4列×2行 |
| 暗黑模式下 | ScrollView 的表现不影响暗黑模式渲染 |

---

## 已完成的代码（无需修改）

### WPUIAttachmentLayoutVC.m

已实现功能：
- 总开关 UISwitch（绑定 `AttachLayoutEnabled`）
- 「每行列数」可编辑行（绑定 `AttachLayout_Columns`）
- 「显示行数」可编辑行（绑定 `AttachLayout_Rows`）
- 开关关闭时只显示开关卡片
- 开关切换时自动 rebuild UI
- `WPUIAttachLayoutVCHelper` 动态类创建

### WPUIVC.m

已实现导航注册：
- Nav 列表项: `@"附件布局", @"openAttachLayout:"`
- 跳转方法: `- (void)openAttachLayout:(id)sender`
- 通过 `WPUIAttachLayoutVCHelper.makeVC` 创建 ViewController

---

## 总结

| 步骤 | 操作 | 文件 |
|------|------|------|
| 1 | 创建 `UIAttachLayoutHook.h` | 新建 |
| 2 | 创建 `UIAttachLayoutHook.m` | 新建 |
| 3 | 在 `FeatureModuleRegistry.m` 添加 import | 修改 1 行 |
| 4 | 在 `FeatureModuleRegistry.m` 添加 Hook 类到 layout 模块 | 修改 1 行 |
| 5 | 将 `UIAttachLayoutHook.m` 加入 Xcode Compile Sources | Xcode 操作 |
| 6 | 构建 → 部署 → 验证 | — |

共需新建 2 个文件，修改 1 个文件的 2 行代码。

---

*文档时间: 2026-05-26*