# 资料卡高度与边距解耦改造方案

> 专注目标：只拆高度（方案 H）和边距（方案 M），不改其他任何逻辑

---

## 1. 当前耦合情况

### 1.1 handleButtonLayout: 执行顺序

```
  ① 守卫: cardBgBeautifyEnabled (否则直接 return)
  ② VC 类型检查: MoreViewController
  ③ MMHeadImageView 检查
  ④ 高度过滤: selfHeight <= 50 跳过
  ⑤ hasMaterial 分支: 创建/管理自定义背景图
  ⑥ handleCardHiddenInButton: 隐藏/显示逻辑
  ⑦ FIX-WHITE: 隐藏白色背景视图
 ─────────────────────────────────────────────
  ⑧ 方案 H: 高度扩展        ←── 改 button.frame.size.height
  ⑨ 方案 M: 左右边距        ←── 改 button.frame.origin.x / width
 ─────────────────────────────────────────────
  ⑩ DO_CORNER: 圆角 + 边框 + QR码隐藏
```

**耦合点**：⑧ 和 ⑨ 在同一个方法中连续执行。每次 `button.frame = xxx` 都可能触发 `layoutSubviews` 递归，当前靠浮点阈值守卫 `fabs(diff) > 0.5` 防止无限循环。

### 1.2 各自的特征

| 维度 | 方案 H（高度） | 方案 M（边距） |
|------|--------------|--------------|
| 修改属性 | `frame.size.height` | `frame.origin.x` / `frame.size.width` |
| 守卫条件 | `frame.height < targetH` | `fabs(currentX-targetX) > 0.5` |
| 父视图检查 | 需要 TextStateProfileTableView | 不需要 |
| 副作用 | 可能触发 layoutSubviews | 可能触发 layoutSubviews |
| label sizeToFit | 无 | 有 |

---

## 2. 改造方案

### 2.1 策略

**方法级别拆分**：不新增 Hook、不改配置项、不新建文件。只在 `ProfileCardBgHook.m` 中：

1. 将方案 H 提取为 `+ (void)handleHeightAdjustment:(UIView *)button`
2. 将方案 M 提取为 `+ (void)handleMarginAdjustment:(UIView *)button`
3. `handleButtonLayout:` 中原来位置替换为两行调用

### 2.2 改动 1：提取 handleHeightAdjustment:

```objc
/// 独立处理资料卡高度扩展（不修改边距）
+ (void)handleHeightAdjustment:(UIView *)button {
    PluginConfig *config = [PluginConfig shared];
    CGFloat targetH = config.cardBgHeight;
    if (targetH <= 0) return;

    // ★ 当前高度已达标则跳过（防递归守卫）
    if (button.frame.size.height >= targetH) return;

    // ── 父视图链检查 ──
    UIView *tableView = button.superview;
    if (!tableView || ![NSStringFromClass([tableView class]) isEqualToString:@"TextStateProfileTableView"]) {
        WPLog(@"CardBg-Diag", @"[HEIGHT-SKIP] superview is %@, not TextStateProfileTableView",
              tableView ? NSStringFromClass([tableView class]) : @"nil");
        return;
    }
    UIView *container = tableView.superview;
    if (!container || ![container isKindOfClass:NSClassFromString(@"MMUIButton")]) {
        WPLog(@"CardBg-Diag", @"[HEIGHT-SKIP] tableView.superview is %@, not MMUIButton",
              container ? NSStringFromClass([container class]) : @"nil");
        return;
    }

    // ── 只改 height ──
    CGRect bf = button.frame;
    CGFloat oldH = bf.size.height;
    bf.size.height = targetH;
    button.frame = bf;

    WPLog(@"CardBg-Diag", @"[HEIGHT-SET] %.0f→%.0f, container=(%.0f,%.0f,%.0f,%.0f)",
          oldH, targetH,
          container.frame.origin.x, container.frame.origin.y,
          container.frame.size.width, container.frame.size.height);
}
```

**与原代码的差异**：
- 删掉了 `goto DO_CORNER`（调用方自然继续执行后续逻辑）
- VC 类型和头像检查由调用方 `handleButtonLayout:` 负责，本方法不再重复
- 防递归守卫：`button.frame.size.height >= targetH`

### 2.3 改动 2：提取 handleMarginAdjustment:

```objc
/// 独立处理资料卡左右边距（不修改高度）
+ (void)handleMarginAdjustment:(UIView *)button {
    PluginConfig *config = [PluginConfig shared];

    CGFloat margin = config.cardBgCornerUseGlobal
        ? config.listCellMargin
        : (config.cardBgCornerMargin > 0 ? config.cardBgCornerMargin : 9.0);

    WPLog(@"CardBg-Diag", @"[MARGIN] useGlobal=%d, margin=%.1f, cardBgCornerMargin=%.1f, listCellMargin=%.1f",
          config.cardBgCornerUseGlobal, margin,
          config.cardBgCornerMargin, config.listCellMargin);

    if (margin <= 0) {
        WPLog(@"CardBg-Diag", @"[MARGIN] SKIP: margin=0 or disabled");
        return;
    }

    CGFloat currentX = button.frame.origin.x;
    CGFloat currentW = button.frame.size.width;
    UIView *superview = button.superview;
    CGFloat containerW = superview ? superview.bounds.size.width
                                   : [UIScreen mainScreen].bounds.size.width;
    CGFloat targetX = margin;
    CGFloat targetW = containerW - margin * 2;

    WPLog(@"CardBg-Diag", @"[MARGIN] check: current=(%.1f,%.1f) target=(%.1f,%.1f) containerW=%.1f deviation=(%.1f,%.1f)",
          currentX, currentW, targetX, targetW, containerW,
          fabs(currentX - targetX), fabs(currentW - targetW));

    // ★ 浮点比较防递归（只在这一个地方守卫）
    if (fabs(currentX - targetX) > 0.5 || fabs(currentW - targetW) > 0.5) {
        CGRect bf = button.frame;
        bf.origin.x = targetX;
        bf.size.width = targetW;
        button.frame = bf;

        WPLog(@"CardBg-Diag", @"[MARGIN] APPLIED: frame=(%.1f,%.1f,%.1f,%.1f)",
              bf.origin.x, bf.origin.y, bf.size.width, bf.size.height);

        // ── Label sizeToFit ──
        for (UIView *sub in button.subviews) {
            if ([sub isKindOfClass:[UILabel class]]) {
                UILabel *label = (UILabel *)sub;
                if (label.text.length > 0) {
                    [label sizeToFit];
                }
            }
        }
    } else {
        WPLog(@"CardBg-Diag", @"[MARGIN] SKIP: already matches target");
    }
}
```

**与原代码的差异**：
- 所有日志输出保留
- label sizeToFit 从手动块移入本方法
- 防递归守卫：`fabs(diff) > 0.5`

### 2.4 改动 3：handleButtonLayout: 中的替换

**原代码（第634-712行）**：
```objc
    // ── 方案 H：仅扩充 button 高度（不含边距） ──
    {
        CGFloat targetH = config.cardBgHeight;
        if (targetH > 0 && button.frame.size.height < targetH) {
            // 向上找 TextStateProfileTableView
            UIView *tableView = button.superview;
            if (!tableView || ![NSStringFromClass([tableView class]) isEqualToString:@"TextStateProfileTableView"]) {
                WPLog(@"CardBg-Diag", @"[HEIGHT-SKIP] superview is %@, not TextStateProfileTableView",
                      tableView ? NSStringFromClass([tableView class]) : @"nil");
                goto DO_CORNER;
            }

            // 继续向上找 MMUIButton 容器
            UIView *container = tableView.superview;
            if (!container || ![container isKindOfClass:NSClassFromString(@"MMUIButton")]) {
                WPLog(@"CardBg-Diag", @"[HEIGHT-SKIP] tableView.superview is %@, not MMUIButton",
                      container ? NSStringFromClass([container class]) : @"nil");
                goto DO_CORNER;
            }

            CGRect bf = button.frame;
            CGFloat oldH = bf.size.height;
            bf.size.height = targetH;
            button.frame = bf;

            WPLog(@"CardBg-Diag", @"[HEIGHT-SET] %.0f→%.0f, container=(%.0f,%.0f,%.0f,%.0f)",
                  oldH, targetH,
                  container.frame.origin.x, container.frame.origin.y,
                  container.frame.size.width, container.frame.size.height);
        }
    }

    // ── 方案 M：左右边距（独立于高度） ──
    {
        CGFloat margin = config.cardBgCornerUseGlobal
            ? config.listCellMargin
            : (config.cardBgCornerMargin > 0 ? config.cardBgCornerMargin : 9.0);
        WPLog(@"CardBg-Diag", @"[MARGIN] useGlobal=%d, margin=%.1f, cardBgCornerMargin=%.1f, listCellMargin=%.1f",
              config.cardBgCornerUseGlobal, margin,
              config.cardBgCornerMargin, config.listCellMargin);
        if (margin > 0) {
            CGFloat currentX = button.frame.origin.x;
            CGFloat currentW = button.frame.size.width;
            UIView *superview = button.superview;
            CGFloat containerW = superview ? superview.bounds.size.width
                                           : [UIScreen mainScreen].bounds.size.width;
            CGFloat targetX = margin;
            CGFloat targetW = containerW - margin * 2;

            WPLog(@"CardBg-Diag", @"[MARGIN] check: current=(%.1f,%.1f) target=(%.1f,%.1f) containerW=%.1f deviation=(%.1f,%.1f)",
                  currentX, currentW, targetX, targetW, containerW,
                  fabs(currentX - targetX), fabs(currentW - targetW));

            if (fabs(currentX - targetX) > 0.5 || fabs(currentW - targetW) > 0.5) {
                CGRect bf = button.frame;
                bf.origin.x = targetX;
                bf.size.width = targetW;
                button.frame = bf;

                WPLog(@"CardBg-Diag", @"[MARGIN] APPLIED: frame=(%.1f,%.1f,%.1f,%.1f)",
                      bf.origin.x, bf.origin.y, bf.size.width, bf.size.height);

                // ★ sizeToFit Labels ★
                for (UIView *sub in button.subviews) {
                    if ([sub isKindOfClass:[UILabel class]]) {
                        UILabel *label = (UILabel *)sub;
                        if (label.text && label.text.length > 0) {
                            [label sizeToFit];
                        }
                    }
                }
            } else {
                WPLog(@"CardBg-Diag", @"[MARGIN] SKIP: already matches target");
            }
        } else {
            WPLog(@"CardBg-Diag", @"[MARGIN] SKIP: margin=0 or disabled");
        }
    }

DO_CORNER:
```

**替换为**：
```objc
    // ── 方案 H：高度扩展（独立方法） ──
    [ProfileCardBgHook handleHeightAdjustment:button];

    // ── 方案 M：左右边距（独立方法） ──
    [ProfileCardBgHook handleMarginAdjustment:button];
```

**删掉**：
- 整个 `{ ... }` 高度块（约30行）
- 整个 `{ ... }` 边距块（约47行）
- `DO_CORNER:` 标签（不再需要 goto）

---

## 3. 改动汇总

| # | 位置 | 操作 | 行数变化 |
|---|------|------|---------|
| 1 | 新增方法 | `handleHeightAdjustment:` | +28 行 |
| 2 | 新增方法 | `handleMarginAdjustment:` | +45 行 |
| 3 | 替换原位置 | 两行调用替代 ~80 行内联代码 | -80 行 |
| | **合计** | 仅改 ProfileCardBgHook.m | **净减 ~7 行** |

### 不涉及
- ❌ 不改 PluginConfig.h / .m
- ❌ 不改 ListCornerRadiusHook.m
- ❌ 不改 ProfileCardBgHook.h（新增方法不对外暴露，仅在 @implementation 内部）
- ❌ 不改 m_bgImageView 清理逻辑
- ❌ 不改圆角、隐藏、FIX-WHITE 逻辑

---

## 4. 改造后 handleButtonLayout: 结构

```objc
+ (void)handleButtonLayout:(UIView *)button {
    // ★ 极速拒绝链 ★
    if (!config.cardBgBeautifyEnabled) return;

    // VC 类型检查
    // MMHeadImageView 检查
    // 高度过滤

    // needsNewCardBg 块（素材 + 隐藏 + FIX-WHITE）
    if (needsNewCardBg) {
        if (hasMaterial) { /* 背景图生命周期 */ }
        [self handleCardHiddenInButton:...];
        if (isHidden) { /* FIX-WHITE */ }
    }

    // ── 高度扩展（独立方法） ──
    [ProfileCardBgHook handleHeightAdjustment:button];

    // ── 左右边距（独立方法） ──
    [ProfileCardBgHook handleMarginAdjustment:button];

    // ── 圆角 + QR码 ──
    if (config.cardBgCornerEnabled) { [self applyProfileCardCorner:...]; }
    if (config.listHideRightQRCode) { [self hideQRButtonInCell:...]; }
}
```

---

## 5. 验证要点

| 检查项 | 预期 |
|--------|------|
| 开启资料卡片美化 + 圆角，进入资料卡 | 边距正常、高度正常、圆角正常 |
| 关闭所有功能，进入资料卡 | 原生资料卡完整显示 |
| 反复快速切换开关 | 不卡死、不闪退、用户名不显示"微信用户" |
| 仅开启高度扩展（cardBgHeight > 0） | 高度变高、边距不变 |
| 仅开启边距（cardBgCornerMargin > 0） | 左右缩进、高度不变 |