# 删除「信息卡片高度」功能 — 详细操作文档

日期：2026-06-06

## 为什么要删除

手动修改 `button.frame.size.height` 会导致 button 越出 cell，覆盖下方的间距区域。WCRefine 没有这个功能，所以不存在这个问题。删除此功能可从根本上消除该 bug。

## 删除内容总览

| # | 文件 | 删除内容 | 行号 |
|---|------|---------|------|
| 1 | `ProfileCardBgHook.h` | `handleHeightAdjustment:` 方法声明 | L30 |
| 2 | `ProfileCardBgHook.m` | 入口调用 `handleHeightAdjustment` | L608 |
| 3 | `ProfileCardBgHook.m` | 整个 `handleHeightAdjustment:` 方法实现 | L613-L647 |
| 4 | `PluginConfig.h` | `cardBgHeight` 属性 | L194 |
| 5 | `PluginConfig.m` | `cardBgHeight` 读取代码 | L483 |
| 6 | `PluginConfig.m` | `cardBgHeight` 保存代码 | L718 |
| 7 | `SettingCardBackgroundController.m` | UI 控件（信息卡片高度输入行） | L53-L61 |
| 8 | 文档 | 已有的修复文档可删除 | - |

---

## 第 1 步：删除 `.h` 声明

**文件**：[ProfileCardBgHook.h](file:///www/wwwroot/ios/MioPlugin/Modules/ProfileCardBg/ProfileCardBgHook.h#L30)

删除第 30 行：

```objc
+ (void)handleHeightAdjustment:(UIView *)button;  // ← 删除这一整行
```

---

## 第 2 步：删除 `.m` 入口调用

**文件**：[ProfileCardBgHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/ProfileCardBg/ProfileCardBgHook.m#L607-L608)

删除第 608 行：

```objc
    [ProfileCardBgHook handleHeightAdjustment:button];  // ← 删除这一整行
```

改后 L607-L610 变为：

```objc
    // 独立功能
    [ProfileCardBgHook handleMarginAdjustment:button];
    [ProfileCardBgHook handleCornerAndQR:button isDark:isDark];
}
```

---

## 第 3 步：删除 `.m` 整个方法实现

**文件**：[ProfileCardBgHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/ProfileCardBg/ProfileCardBgHook.m#L613-L647)

删除第 613-647 行（整个 `handleHeightAdjustment:` 方法）：

```objc
#pragma mark - 方案 H：高度扩展

+ (void)handleHeightAdjustment:(UIView *)button {
    PluginConfig *config = [PluginConfig shared];
    CGFloat targetH = config.cardBgHeight;
    if (targetH <= 0) return;

    // ★ 当前高度已达标则跳过（防递归守卫）
    if (button.frame.size.height >= targetH) return;

    // ── 父视图链检查 ──
    UIView *tableView = button.superview;
    if (!tableView || ![NSStringFromClass([tableView class]) isEqualToString:@"TextStateProfileTableView"]) {
        WPLog(@"CardBg-Diag", @"[HEIGHT-SKIP] superview is %@, not TextStateProfileTableView",
              tableView ? NSStringFromClass([tableView class]) : @"nil");
        return;
    }
    UIView *container = tableView.superview;
    if (!container || ![container isKindOfClass:NSClassFromString(@"MMUIButton")]) {
        WPLog(@"CardBg-Diag", @"[HEIGHT-SKIP] tableView.superview is %@, not MMUIButton",
              container ? NSStringFromClass([container class]) : @"nil");
        return;
    }

    // ── 只改 height ──
    CGRect bf = button.frame;
    CGFloat oldH = bf.size.height;
    bf.size.height = targetH;
    button.frame = bf;

    WPLog(@"CardBg-Diag", @"[HEIGHT-SET] %.0f→%.0f, container=(%.0f,%.0f,%.0f,%.0f)",
          oldH, targetH,
          container.frame.origin.x, container.frame.origin.y,
          container.frame.size.width, container.frame.size.height);
}
```

**注意**：删除后检查 `#pragma mark` 的顺序是否需要调整。保留 `#pragma mark - 方案 M：左右边距` 不变。

---

## 第 4 步：删除 `PluginConfig.h` 属性

**文件**：[PluginConfig.h](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.h#L194)

删除第 194 行：

```objc
@property (nonatomic, assign) CGFloat cardBgHeight;  // ← 删除这一整行
```

---

## 第 5 步：删除 `PluginConfig.m` 读取代码

**文件**：[PluginConfig.m](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.m#L483)

删除第 483 行：

```objc
    _cardBgHeight = [d floatForKey:[kPluginPrefix stringByAppendingString:@"CardBgHeight"]];  // ← 删除
```

保持在 `_cardBgListSpacing = ...` 和 `_cardBgImagePath = ...` 之间的连贯性。

---

## 第 6 步：删除 `PluginConfig.m` 保存代码

**文件**：[PluginConfig.m](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.m#L718)

删除第 718 行：

```objc
    [d setFloat:_cardBgHeight forKey:[kPluginPrefix stringByAppendingString:@"CardBgHeight"]];  // ← 删除
```

---

## 第 7 步：删除设置页面的 UI 控件

**文件**：[SettingCardBackgroundController.m](file:///www/wwwroot/ios/MioPlugin/Settings/Controllers/SettingCardBackgroundController.m#L53-L61)

删除第 53-61 行：

```objc
            CGFloat h = cfg.cardBgHeight > 0 ? cfg.cardBgHeight : 144.0;
            *e2y = [self addInputRowInGroup:e2
                                      title:@"信息卡片高度"
                                        key:@"cardBgHeight"
                                      value:[NSString stringWithFormat:@"%.0f", h]
                                       hint:@"144"
                                 alertTitle:@"设置信息卡片高度"
                               alertMessage:@"请输入高度值(144-500像素)"
                                         cy:*e2y width:w];
            *e2y = [self addSeparatorInGroup:e2 cy:*e2y width:w];
```

删除后，第 64 行的 `CGFloat sp = ...` 变成紧接在第 52 行 `subBuilder:` 之后，**第 62 行的 `addSeparatorInGroup` 也要一并删除**，否则会出现多余的 separater。

---

## 第 8 步：删除相关文档（可选）

以下文档内容已过时，不再需要：

```bash
rm "/www/wwwroot/ios/文档/WCRefine圆角功能分析/高度设置导致间距消失分析与修复.md"
```

---

## 验证清单

编译后回归测试以下场景：

| # | 开关组合 | 预期结果 |
|---|---------|---------|
| 1 | 只开总开关 | 资料卡无变化（无高度改变、无边距、无圆角） |
| 2 | 总开关 + 背景素材 | 背景图正常显示，间距正常 |
| 3 | 总开关 + 圆角 | 圆角+边距生效，间距正常 |
| 4 | 总开关 + 隐藏 | 卡片被隐藏，间距正常 |
| 5 | 全功能组合 | 全部生效，间距正常 |

**重点验证**：以前设过 `cardBgHeight` 的用户，升级后资料卡恢复原生高度，间距正常显示。