# 删除「媒体视图圆角 + 禁用标签宽度调整」功能 清理方案

> 完全移除 `listMediaCornerEnabled` 和 `listDisableLabelWidthAdjustment` 两个功能的全部代码。

---

## 改动概述

两个功能都只在 `_hooked_MMUIButton_layoutSubviews` 这一个 Hook 中使用，删除后该 Hook 无任何功能残留，可整体移除。

共涉及 **4 个文件，全部删除操作**。

---

## 改动明细

### 改动 1：ListCornerRadiusHook.m

**文件**：[ListCornerRadiusHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/ListCornerRadius/ListCornerRadiusHook.m)

#### 1a）删除 Hook 实现函数（L379-L408）

```objc
// 删：整个函数
// ★★★ [WPAuxiliaryHooks] MMUIButton list media corner ★★★
static void (*_orig_MMUIButton_layoutSubviews)(id, SEL);
static void _hooked_MMUIButton_layoutSubviews(id self, SEL _cmd) {
    _orig_MMUIButton_layoutSubviews(self, _cmd);

    PluginConfig *config = [PluginConfig shared];
    if (!config.globalCornerRadiusEnabled) return;

    UIViewController *vc = findParentViewController((UIView *)self);
    if (!vc) return;
    if (![NSStringFromClass([vc class]) isEqualToString:@"NewMainFrameViewController"]) return;

    if (config.listMediaCornerEnabled) {
        UIView *view = (UIView *)self;
        NSInteger radius = (NSInteger)config.listCellCornerRadius;
        if (radius == 0) radius = 18;
        view.layer.cornerRadius = radius;
        view.layer.masksToBounds = YES;
    }

    if (config.listDisableLabelWidthAdjustment) {
        UIView *view = (UIView *)self;
        for (UIView *subview in view.subviews) {
            if ([subview isKindOfClass:[UILabel class]]) {
                UILabel *label = (UILabel *)subview;
                [label sizeToFit];
            }
        }
    }
}
```

#### 1b）删除 Hook 注册代码（L541-L546）

```objc
// 删：MMUIButton Hook 注册
// ★ MMUIButton Hook — 仅保留列表媒体圆角
Class c4 = objc_getClass("MMUIButton");
if (c4) {
    MSHookMessageEx(c4, @selector(layoutSubviews),
        (IMP)_hooked_MMUIButton_layoutSubviews, (IMP *)&_orig_MMUIButton_layoutSubviews);
}
```

---

### 改动 2：SettingListCornerRadiusController.m

**文件**：[SettingListCornerRadiusController.m](file:///www/wwwroot/ios/MioPlugin/Settings/Controllers/SettingListCornerRadiusController.m)

#### 2a）删除「媒体视图圆角」两段重复 UI（L84-L96）

```objc
// 删：L84-89（第1个）
pcy = [self addSubSwitchRowInGroup:paramGroup
                             title:@"媒体视图圆角"
                               key:@"listMediaCornerEnabled"
                              isOn:cfg.listMediaCornerEnabled
                                cy:pcy width:w];
pcy = [self addSeparatorInGroup:paramGroup cy:pcy width:w];

// 删：L91-96（第2个，完全重复）
pcy = [self addSubSwitchRowInGroup:paramGroup
                             title:@"媒体视图圆角"
                               key:@"listMediaCornerEnabled"
                              isOn:cfg.listMediaCornerEnabled
                                cy:pcy width:w];
pcy = [self addSeparatorInGroup:paramGroup cy:pcy width:w];
```

#### 2b）删除「禁用标签宽度调整」UI（L98-L103）

```objc
// 删：L98-103
pcy = [self addSubSwitchRowInGroup:paramGroup
                             title:@"禁用标签宽度调整"
                               key:@"listDisableLabelWidthAdjustment"
                              isOn:cfg.listDisableLabelWidthAdjustment
                                cy:pcy width:w];
pcy = [self addSeparatorInGroup:paramGroup cy:pcy width:w];
```

#### 2c）从 switchChanged 中移除对应的 key（L227-L228）

```objc
// 改前（L221-230）：
if ([key isEqualToString:@"globalCornerRadiusEnabled"]
    || [key isEqualToString:@"globalCornerMyPageEnabled"]
    || [key isEqualToString:@"globalCornerContactsPageEnabled"]
    || [key isEqualToString:@"globalCornerDiscoverPageEnabled"]
    || [key isEqualToString:@"listSearchCornerRadius"]
    || [key isEqualToString:@"listCellBorder"]
    || [key isEqualToString:@"listDisableLabelWidthAdjustment"]    // ← 删
    || [key isEqualToString:@"listMediaCornerEnabled"]             // ← 删
    || [key isEqualToString:@"listProfileCardBorderEnabled"]) {
    [MioRestartHelper showRestartAlertFromVC:self];
}

// 改后：
if ([key isEqualToString:@"globalCornerRadiusEnabled"]
    || [key isEqualToString:@"globalCornerMyPageEnabled"]
    || [key isEqualToString:@"globalCornerContactsPageEnabled"]
    || [key isEqualToString:@"globalCornerDiscoverPageEnabled"]
    || [key isEqualToString:@"listSearchCornerRadius"]
    || [key isEqualToString:@"listCellBorder"]
    || [key isEqualToString:@"listProfileCardBorderEnabled"]) {
    [MioRestartHelper showRestartAlertFromVC:self];
}
```

---

### 改动 3：PluginConfig.h — 删除属性声明

**文件**：[PluginConfig.h](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.h)

删除 L174-L175：

```objc
// 删：
@property (nonatomic, assign) BOOL listDisableLabelWidthAdjustment;
@property (nonatomic, assign) BOOL listMediaCornerEnabled;
```

---

### 改动 4：PluginConfig.m — 删除读写

**文件**：[PluginConfig.m](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.m)

#### 4a）删除 load（L461-L462）

```objc
// 删：
_listDisableLabelWidthAdjustment = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ListDisableLabelWidthAdjustment"]];
_listMediaCornerEnabled = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ListMediaCornerEnabled"]];
```

#### 4b）删除 save（L713-L714）

```objc
// 删：
[d setBool:_listDisableLabelWidthAdjustment forKey:[kPluginPrefix stringByAppendingString:@"ListDisableLabelWidthAdjustment"]];
[d setBool:_listMediaCornerEnabled forKey:[kPluginPrefix stringByAppendingString:@"ListMediaCornerEnabled"]];
```

---

## 涉及文件一览

| 文件 | 改动内容 | 改动类型 |
|------|---------|---------|
| `ListCornerRadiusHook.m` | 删除整个 `_hooked_MMUIButton_layoutSubviews` 函数 + 对应的 Hook 注册 | 删除 |
| `SettingListCornerRadiusController.m` | 删除 2 个 UI 开关行（含1个重复） + `switchChanged` 中 2 个 key | 删除 |
| `PluginConfig.h` | 删除 2 个 `BOOL` 属性 | 删除 |
| `PluginConfig.m` | 删除 load 2 行 + save 2 行 | 删除 |

共 **4 个文件，删 2 个功能**。

---

## 安全校验

删除后需确认：

1. **全局搜 `listMediaCornerEnabled` 和 `listDisableLabelWidthAdjustment`** — 确保无其他引用遗漏
2. **全局搜 `_hooked_MMUIButton_layoutSubviews` 和 `_orig_MMUIButton_layoutSubviews`** — 确认无遗漏引用
3. **全局搜 `MMUIButton` 在 ListCornerRadiusHook.m 中** — 确认 Hook 注册已移除
4. **编译测试** — 不报 `undeclared identifier` 或 `unused variable` 即可

> 注意：`ListCornerRadiusHook.m` 中还有一个 `replaced_MMUIButton_layoutSubviews`（L36-42）属于 `ProfileCardBgHook`，不要误删。两者同名但在不同文件中，各自独立。