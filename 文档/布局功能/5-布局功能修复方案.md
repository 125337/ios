# 布局功能修复方案 v4 —— 基于实际日志的完整重写

## 背景

基于 `MioPlugin(7).log` 的实际日志证据，之前所有文档中对锤子助手 `FUN_00753950` 的分析是**错误**的。微信实际调用的属性名、规则集、返回值类型与我们的假设严重不符。

**日志铁证**：
- 微信实际属性名：`titleview_fontsize`、`common_font_small_size`、`tool_view_fontSize` 等
- 微信实际规则集：`#navigation_bar`、`#common_default` 等（以 `#` 开头）
- 微信实际返回值类型：`__NSArrayM`（NSMutableArray），如 `@[17]`、`@[17, @"dynamic"]`
- 我们的 Hook 条件（`allLevel` + `_font_set`）**从未命中任何一次调用**

---

## 一、需要修改的文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `FontLayoutHook.m` | **整体重写** | Hook 的核心逻辑全面改为基于数组操作 |
| `FontLayoutConfig.h` | 无需修改 | 属性定义保持不变 |
| `FontLayoutConfig.m` | 无需修改 | ConfigManager 读写路径保持不变 |
| `WPUILayoutSettingsVC.m` | 小改 | 保持与前面一致的范围和日志 |
| `MioModuleRegistry.m` | 无需修改 | 注册逻辑保持不变 |

---

## 二、FontLayoutHook.m 完整重写

### 2.1 整体设计

```
┌─ hook_getValueOfProperty:inRuleSet: ──────────────────────────────┐
│                                                                   │
│  1. 调原方法获取原始值 (originalResult)                             │
│                                                                   │
│  2. 检查布局开关                                                   │
│     ├─ 都关闭 → 直接返回 originalResult                            │
│     └─ 任一开启 → 继续                                            │
│                                                                   │
│  3. 检查是否可拦截                                                 │
│     ├─ 必须是 NSArray（所有字体属性返回数组）                        │
│     ├─ 必须有至少1个元素                                            │
│     └─ 第一个元素必须是数字(NSNumber)或可转数字的(NSString)          │
│                                                                   │
│  4. 确定目标字号                                                   │
│     ├─ 全局布局开启 → globalFontSize                               │
│     ├─ 仅对话布局开启 → chatFontSize                               │
│     └─ 都开启 → globalFontSize（可配置优先级）                       │
│                                                                   │
│  5. 按同类型创建新值替换第一个元素                                   │
│     ├─ 原为 NSNumber → @((NSInteger)targetSize)                    │
│     ├─ 原为 NSString → [NSString stringWithFormat:@"%.0f", targetSize] │
│     └─ 保留后续元素不变（如 @"dynamic"）                             │
│                                                                   │
│  6. 返回修改后的数组                                                │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

### 2.2 完整代码

```objc
// FontLayoutHook.m —— 修复版 v4（基于日志证据）

// 修改说明：
//   1. 不再检查具体属性名（allLevel/webLevel/chatLevel）
//   2. 不再检查 _font_set 规则集
//   3. 改为检查返回值是否为 NSArray + 第一个元素是否为数值
//   4. 保持数组结构，只修改第一个元素（字号）
//   5. 保持元素类型（NSNumber→NSNumber, NSString→NSString）

#import "FontLayoutHook.h"
#import "FontLayoutConfig.h"
#import "../../Core/LogManager.h"
#import <substrate.h>
#import <objc/runtime.h>

#pragma mark - 常量

// 字号范围（与 UI 侧保持一致）
static const CGFloat kMinFontSize = 10.0f;
static const CGFloat kMaxFontSize = 16.0f;

// 白名单规则集（只修改这些规则集中的字号）
// 基于日志 v7 中实际出现的所有规则集
// 以 # 前缀开头，可通过持续观察补充
static NSSet *s_fontRuleSets(void) {
    static NSSet *sets = nil;
    static dispatch_once_t onceToken;
    dispatch_once(&onceToken, ^{
        sets = [NSSet setWithObjects:
            @"#navigation_bar",         // 导航栏
            @"#common_default",         // 通用默认
            @"#input_tool_view_tool",   // 输入工具栏
            @"#widget_tipsbar_base",    // Tipsbar 组件
            @"#brand_timeline_view",    // 朋友圈品牌视图
            nil];
    });
    return sets;
}

#pragma mark - 辅助函数

/// 判断是否需要拦截此返回值
/// 规则：必须是可变数组 + 第一个元素是数值（NSNumber 或数字字符串）
/// @param originalResult 原方法返回值
/// @param property 属性名（用于日志）
/// @param ruleSet 规则集（用于日志）
/// @param outSize 输出：原始字号值
/// @param outElementType 输出：第一个元素的类型（NSNumber/NSString）
/// @return YES=需要拦截
static BOOL shouldIntercept(id originalResult,
                            NSString *property,
                            NSString *ruleSet,
                            CGFloat *outSize,
                            NSString **outElementType) {
    // ── 检查是否为数组 ──
    if (![originalResult isKindOfClass:[NSArray class]]) {
        return NO;
    }
    
    NSArray *arr = (NSArray *)originalResult;
    if (arr.count == 0) {
        return NO;
    }
    
    // ── 可选的白名单检查 ──
    // 如果规则集在白名单中，直接通过
    BOOL inWhitelist = [s_fontRuleSets() containsObject:ruleSet];
    if (!inWhitelist) {
        // 如果不在白名单中，用宽松条件检查属性名是否含 "font"
        NSString *lowerProperty = [property lowercaseString];
        if (![lowerProperty containsString:@"font"]) {
            return NO;  // 属性名不含 font，跳过
        }
    }
    
    // ── 检查第一个元素是否为数值 ──
    id firstObj = arr[0];
    
    if ([firstObj isKindOfClass:[NSNumber class]]) {
        *outSize = [firstObj floatValue];
        *outElementType = @"NSNumber";
        return YES;
    }
    
    if ([firstObj isKindOfClass:[NSString class]]) {
        NSString *str = (NSString *)firstObj;
        // 尝试解析为浮点数
        NSScanner *scanner = [NSScanner scannerWithString:str];
        float val;
        if ([scanner scanFloat:&val] && [scanner isAtEnd]) {
            *outSize = (CGFloat)val;
            *outElementType = @"NSString";
            return YES;
        }
    }
    
    return NO;
}

/// 创建新的第一个元素值（保持原类型）
/// @param originalElement 原第一个元素
/// @param newSize 目标字号
/// @return 同类型的新值
static id createNewValueForElement(id originalElement, CGFloat newSize) {
    if ([originalElement isKindOfClass:[NSNumber class]]) {
        // NSNumber → NSNumber
        return @((NSInteger)newSize);
    } else if ([originalElement isKindOfClass:[NSString class]]) {
        // NSString → NSString
        return [NSString stringWithFormat:@"%.0f", newSize];
    }
    // 未知类型 → 返回原值（不做修改）
    return originalElement;
}

#pragma mark - Hook 1: MMThemeManager.getValueOfProperty:inRuleSet:

static id (*orig_getValueOfProperty_inRuleSet)(id, SEL, NSString *, NSString *);

static id hook_getValueOfProperty_inRuleSet(id self, SEL _cmd,
                                             NSString *property,
                                             NSString *ruleSet) {
    // ★ 第一步：先调原方法获取原始值 ★
    id originalResult = orig_getValueOfProperty_inRuleSet(self, _cmd, property, ruleSet);
    
    FontLayoutConfig *config = [FontLayoutConfig shared];
    BOOL globalOn = config.globalLayoutEnabled;
    BOOL chatOn   = config.chatLayoutEnabled;
    
    // ── 两个布局都未开启 → 直接返回 ──
    if (!globalOn && !chatOn) {
        return originalResult;
    }
    
    // ── 检查是否需要拦截 ──
    CGFloat originalSize = 0;
    NSString *elementType = nil;
    
    if (!shouldIntercept(originalResult, property, ruleSet,
                         &originalSize, &elementType)) {
        // 不需要拦截 → 返回原始值
        return originalResult;
    }
    
    // ── 确定目标字号 ──
    CGFloat targetSize = kMaxFontSize; // 默认 16
    
    if (globalOn && chatOn) {
        // 两个都开启 → 全局优先（可根据需求改为对话优先）
        targetSize = config.globalFontSize;
    } else if (globalOn) {
        // 仅全局
        targetSize = config.globalFontSize;
    } else if (chatOn) {
        // 仅对话
        targetSize = config.chatFontSize;
    }
    
    // ── 范围验证 ──
    if (targetSize < kMinFontSize || targetSize > kMaxFontSize) {
        WPLog(@"FontLayout",
              @"[SKIP] fontSize %.0f out of range [%.0f, %.0f]",
              targetSize, kMinFontSize, kMaxFontSize);
        return originalResult;
    }
    
    // ── 执行修改：保留数组结构，只改第一个元素 ──
    NSMutableArray *modified = [(NSArray *)originalResult mutableCopy];
    id newElement = createNewValueForElement(modified[0], targetSize);
    modified[0] = newElement;
    
    // ── 日志 ──
    WPLog(@"FontLayout",
          @"[MODIFY] %@ in %@ : %.0f → %.0f (type=%@, count=%lu)",
          property, ruleSet,
          originalSize, targetSize,
          elementType, (unsigned long)modified.count);
    
    return modified;
}

#pragma mark - Hook 2: CLocalInfo.m_uiGlobalFontLevel

static unsigned int (*orig_m_uiGlobalFontLevel)(id, SEL);

static unsigned int hook_m_uiGlobalFontLevel(id self, SEL _cmd) {
    FontLayoutConfig *config = [FontLayoutConfig shared];

    if (config.globalLayoutEnabled || config.chatLayoutEnabled) {
        // 大字模式激活，触发微信内置大字号体系
        // （我们的 Hook-1 会在此基础上进一步精确控制具体字号）
        return 1;
    }

    return orig_m_uiGlobalFontLevel(self, _cmd);
}

#pragma mark - 安装入口

@implementation FontLayoutHook

+ (void)install {
    WPLog(@"FontLayout", @"=== FontLayoutHook v4 Install Start ===");

    // ── 诊断：检查类和方法的可用性 ──
    Class mmThemeManager = objc_getClass("MMThemeManager");
    Class clocalInfo = objc_getClass("CLocalInfo");

    SEL selGetValue = @selector(getValueOfProperty:inRuleSet:);
    SEL selFontLevel = @selector(m_uiGlobalFontLevel);

    Method mGetValue = mmThemeManager
        ? class_getInstanceMethod(mmThemeManager, selGetValue) : NULL;
    Method mFontLevel = clocalInfo
        ? class_getInstanceMethod(clocalInfo, selFontLevel) : NULL;

    WPLog(@"FontLayout", @"MMThemeManager: %@",
          mmThemeManager ? @"EXISTS" : @"NIL");
    WPLog(@"FontLayout", @"getValueOfProperty:inRuleSet: method: %@",
          mGetValue ? @"EXISTS" : @"NIL");
    WPLog(@"FontLayout", @"CLocalInfo: %@",
          clocalInfo ? @"EXISTS" : @"NIL");
    WPLog(@"FontLayout", @"m_uiGlobalFontLevel method: %@",
          mFontLevel ? @"EXISTS" : @"NIL");

    // ── 诊断：打印当前的配置值 ──
    FontLayoutConfig *cfg = [FontLayoutConfig shared];
    WPLog(@"FontLayout",
          @"Config: globalOn=%d globalSize=%.0f chatOn=%d chatSize=%.0f",
          cfg.globalLayoutEnabled, cfg.globalFontSize,
          cfg.chatLayoutEnabled, cfg.chatFontSize);

    // ── 诊断：打印白名单 ⚠️ 仅供调试，后续可以移除 ⚠️ ──
    WPLog(@"FontLayout", @"FontRuleSet whitelist: %@",
          [[s_fontRuleSets() allObjects] componentsJoinedByString:@", "]);

    // ── Hook 安装 ──
    if (mmThemeManager && mGetValue) {
        MSHookMessageEx(mmThemeManager, selGetValue,
                        (IMP)hook_getValueOfProperty_inRuleSet,
                        (IMP *)&orig_getValueOfProperty_inRuleSet);
        WPLog(@"FontLayout",
              @"[+] MMThemeManager.getValueOfProperty:inRuleSet: HOOKED");
    } else {
        WPLog(@"FontLayout",
              @"[-] MMThemeManager HOOK SKIPPED (class=%@ method=%@)",
              mmThemeManager ? @"OK" : @"NIL",
              mGetValue ? @"OK" : @"NIL");
    }

    if (clocalInfo && mFontLevel) {
        MSHookMessageEx(clocalInfo, selFontLevel,
                        (IMP)hook_m_uiGlobalFontLevel,
                        (IMP *)&orig_m_uiGlobalFontLevel);
        WPLog(@"FontLayout",
              @"[+] CLocalInfo.m_uiGlobalFontLevel HOOKED");
    } else {
        WPLog(@"FontLayout",
              @"[-] CLocalInfo HOOK SKIPPED (class=%@ method=%@)",
              clocalInfo ? @"OK" : @"NIL",
              mFontLevel ? @"OK" : @"NIL");
    }

    WPLog(@"FontLayout", @"=== FontLayoutHook v4 Install Complete ===");
}

@end
```

### 2.3 关键设计决策说明

#### 决策1：不匹配具体属性名 → 改为检查数组结构

```
旧方案：if ([property isEqualToString:@"allLevel"]) → 失败（微信不用 allLevel）
新方案：if ([originalResult isKindOfClass:[NSArray class]]) → 成功（微信全返回数组）
```

理由：从日志看，所有字体属性的返回值都是 `__NSArrayM`。检查数组比检查具体属性名更可靠，且不受微信版本变化影响。

#### 决策2：使用规则集白名单 + 属性名宽松匹配

白名单 `s_fontRuleSets()` 包含日志中确认的所有规则集。不在白名单中的，检查属性名是否包含 `font` 子串（不区分大小写）。

理由：防止意外修改非字体属性（虽然目前所有 MMTM 调用都是字体相关，但有备无患）。

#### 决策3：大字模式 + 精确修改双管齐下

```
CLocalInfo.m_uiGlobalFontLevel → 1 → 微信进入大字模式
MMThemeManager.getValueOfProperty:inRuleSet: → 修改返回值 → 精确控制字号
```

理由：大字模式让微信启用大字号配置，我们的 Hook 再进一步精确调整到用户指定的值。两者协同工作。

#### 决策4：全局/对话布局共用同一个 Hook 逻辑

日志中没有发现单独的"对话"规则集，所以全局和对话布局共用同一个 Hook，区别仅在于使用的目标字号不同：
- 仅全局开启 → 用 `globalFontSize`
- 仅对话开启 → 用 `chatFontSize`
- 都开启 → 用 `globalFontSize`（可调整）

如果未来需要区分全局和对话的影响范围，可以在 `shouldIntercept` 中根据规则集做细分（如对话布局只修改 `#common_default` 中的属性）。

---

## 三、WPUILayoutSettingsVC.m 更新

### 3.1 保持范围一致

已在 v3 中更新 `kMaxFontSize = 16.0`，与 Hook 中的 `kMaxFontSize` 一致，无需额外修改。

### 3.2 可选的增强日志

在 WPUILayoutSettingsVC.m 的界面元素中，可以添加实时反馈：

```objc
// 在切换开关或输入值时输出保存状态
// 添加在 switchChanged: 方法中
WPLog(@"FontLayout",
      @"[UI] Layout settings saved: globalOn=%d globalSize=%.0f chatOn=%d chatSize=%.0f",
      cfg.globalLayoutEnabled, cfg.globalFontSize,
      cfg.chatLayoutEnabled, cfg.chatFontSize);
```

（已在 v3 中添加，无需重复操作）

---

## 四、完整的预期行为

### 4.1 正常流程

```
1. 用户进入布局设置页面
2. 开启"全局布局"开关 → switchChanged → ConfigManager.saveAll
3. 输入 14 → onEditRowTap → ConfigManager.saveAll
4. 弹出重启弹窗 → 用户确认 → 杀死微信
5. 微信重启 → dylib 加载 → MioModuleRegistry.registerAll
                       → FontLayoutHook.install
                       → ConfigManager.loadAll (读取 globalSize=14)
6. 微信渲染 UI → CLocalInfo.m_uiGlobalFontLevel → 返回 1 (大字模式)
7. 微信渲染 UI → MMThemeManager.getValueOfProperty:inRuleSet:
       → property=titleview_fontsize, ruleSet=#navigation_bar
       → 原方法返回 @[17]
       → shouldIntercept 检查通过 (NSArray + "#navigation_bar" 在白名单中)
       → 修改为 @[14]
       → 返回 @[14]
8. 微信用 14pt 渲染导航栏标题
```

### 4.2 修改范围

基于 `s_fontRuleSets()` 白名单，以下 UI 元素的字号会被修改：

| UI 元素 | 属性 | 规则集 | 默认值 | 修改后 |
|---------|------|--------|-------|--------|
| 导航栏标题 | `titleview_fontsize` | `#navigation_bar` | 17 → 14 | ✅ |
| 通用小字号文本 | `common_font_small_size` | `#common_default` | 12 → 14 | ✅ |
| 输入工具栏 | `tool_view_fontSize` | `#input_tool_view_tool` | 17 → 14 | ✅ |
| Tipsbar 文本 | `text_font` | `#widget_tipsbar_base` | 14 → 14 | ✅ (没变化) |
| 朋友圈底部文本 | `footer_nomore_msg_font` | `#brand_timeline_view` | 12 → 14 | ✅ |

### 4.3 预期的日志输出

```
=== FontLayoutHook v4 Install Start ===
MMThemeManager: EXISTS
getValueOfProperty:inRuleSet: method: EXISTS
CLocalInfo: EXISTS
m_uiGlobalFontLevel method: EXISTS
Config: globalOn=1 globalSize=14 chatOn=1 chatSize=14
FontRuleSet whitelist: #navigation_bar, #common_default, #input_tool_view_tool, #widget_tipsbar_base, #brand_timeline_view
[+] MMThemeManager.getValueOfProperty:inRuleSet: HOOKED
[+] CLocalInfo.m_uiGlobalFontLevel HOOKED
=== FontLayoutHook v4 Install Complete ===

[CLocalInfo] m_uiGlobalFontLevel -> 1 (大字模式, global=1 chat=1)
[MMTM] property=titleview_fontsize ruleSet=#navigation_bar -> orig=@[17]
[MODIFY] titleview_fontsize in #navigation_bar : 17.0 → 14.0 (type=NSNumber, count=1)
[MMTM] property=common_font_small_size ruleSet=#common_default -> orig=@[12]
[MODIFY] common_font_small_size in #common_default : 12.0 → 14.0 (type=NSNumber, count=1)
```

---

## 五、可能的问题与兜底方案

### 5.1 白名单不完整

如果微信在渲染某个 UI 元素时使用了不在白名单中的规则集，该元素的字号不会被修改。

**兜底**：白名单可以扩展。在第一个版本的实现中，如果日志中出现新的规则集，将其加入 `s_fontRuleSets()` 即可。也可以暂时注释掉白名单检查，让所有含 `font` 的属性都通过。

### 5.2 数组第一个元素不是数值

如果某个属性返回的 NSArray 第一个元素不是数值（比如是 UIFont 对象、UIColor 对象等），`shouldIntercept` 会返回 NO，不会修改。

**兜底**：这种情况在日志中未出现，但未来如果遇到，该属性会被跳过。

### 5.3 大字模式与修改值冲突

如果 `m_uiGlobalFontLevel = 1` 时，微信在某些地方硬编码了字号而不走 `MMThemeManager`，这些地方不会被修改。

**兜底**：大字模式 + MMTM Hook 已覆盖绝大多数场景。如果某个 UI 元素仍不受影响，需要单独 Hook 该元素的布局方法（如 `layoutSubviews`）。

### 5.4 多个版本覆盖

如果微信更新版本后：
- 规则集命名变化 → 更新白名单
- 返回值类型变化（不再是 NSArray）→ 修改 `shouldIntercept` 逻辑
- MMTM 方法签名变化 → 更新 Hook 签名

---

## 六、实施步骤

### 步骤1：替换 FontLayoutHook.m

用上面 2.2 节的完整代码替换现有的 `FontLayoutHook.m`。

### 步骤2：编译部署

```bash
# 在项目目录下
make clean && make
# 部署到手机（根据你的部署方式）
# 通常是 scp 或通过越狱市场
```

### 步骤3：设置并重启

1. 进入微信 → 插件设置 → 布局设置
2. 开启"全局布局"
3. 输入 14
4. 确认弹出"杀进程重启"
5. 微信重启

### 步骤4：验证日志

检查 `MioPlugin.log`，应看到：
- `[MODIFY] xxx in yyy : 17.0 → 14.0` → 确认 Hook 生效
- 多次出现 `[MODIFY]` → 确认覆盖了多个 UI 元素

### 步骤5：观察界面

- 导航栏标题字号是否变小
- 对话框/列表中的字号是否变化
- 如果字号变小太多（14 < 之前的大字模式 17），效果应该是可见的

---

## 七、与旧方案的对比

| 维度 | 旧方案（v1-v3） | 新方案（v4） |
|------|---------------|-------------|
| 属性匹配 | `allLevel`/`webLevel`/`chatLevel` | 无硬编码属性名，检查数组结构 |
| 规则集匹配 | `hasSuffix:@"_font_set"` | `#` 前缀白名单 |
| 返回值类型 | `NSString` / `NSNumber` | `NSArray`（与原方法一致） |
| 修改方式 | 直接返回新值 | 保留数组结构，只改第一个元素 |
| 元素类型 | 固定 `NSString` | 保留原类型（NSNumber/NSString） |
| 大字模式 | 返回 1 | 返回 1（不变） |
| 是否需要原方法 | 需要（已修复） | 需要 |
| 白名单保护 | 无 | 有（防止误改） |

---

## 八、附录：关键日志行解读

```
[MMTM] property=titleview_fontsize ruleSet=#navigation_bar origType=__NSArrayM origValue=(
    17
)
```

这条日志说明：
- 微信查询了 `MMThemeManager` 的属性 `titleview_fontsize`，规则集 `#navigation_bar`
- 原方法返回了 `__NSArrayM`（Mutable Array）
- 数组内容为 `@[17]`（导航栏标题字号为 17pt）
- **我们的 Hook 应在此时拦截，将 17 修改为用户设置的 14**

```
[CLocalInfo] m_uiGlobalFontLevel -> 1 (大字模式, global=1 chat=1)
```

这条日志说明：
- `CLocalInfo.m_uiGlobalFontLevel` 被微信调用
- 我们的 Hook 返回了 1（大字模式）
- 微信的文字体系切换到大字号配置（17/12/14 等）
- 这为我们的 MMTM Hook 提供了基础（微信在大字模式下更依赖 MMTM 返回值）

```
Config: globalOn=1 globalSize=14 chatOn=1 chatSize=14
```

这条日志说明：
- ConfigManager 正确读取了用户设置的 14 并赋值给了 FontLayoutConfig
- 数据持久化路径正常
- 问题不在数据层，在 Hook 匹配层