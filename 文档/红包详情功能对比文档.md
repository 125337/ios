# 红包详情功能对比文档

## WeChatPlugin（最终版） vs Mikoto 反编译插件

---

## 一、总体概述

**WeChatPlugin 和 Mikoto 核心逻辑一致**：在 `viewDidLoad` 中从 VC ivar 读取 `m_oWCRedEnvelopesDetailInfo`，创建浮动按钮，点击弹出详情。

**关键差异**：WeChatPlugin 只 hook `WCRedEnvelopesRedEnvelopesDetailViewController`（不 hook StoryVC），避免了 StoryVC/DetailVC 层级干扰导致的按钮消失 bug（经 log 85/86 验证）。

---

## 二、Hook 覆盖范围

| Hook 目标 | WeChatPlugin | Mikoto | 说明 |
|---|---|---|---|
| `CMessageMgr.onNewSyncAddMessage:` | ✅ | ✅ | 新消息通知 |
| `CMessageMgr.addMessageLibWithWrap:` | ✅ | ✅ | 消息入库回调 |
| `CMessageMgr.onNewSyncNotAddDBMessage:` | ✅ | ✅ | 不入库消息 |
| `CMessageMgr.AddMsg:MsgWrap:` | ✅ | ✅ | 手动添加消息 |
| `CMessageMgr.AsyncOnAddMsg:MsgWrap:` | ✅ | ✅ | 异步添加消息 |
| `WCRedEnvelopesLogicMgr.OnWCToHongbaoCommonResponse:Request:` | ✅ | ✅ | 红包响应拦截(2参数) |
| `WCRedEnvelopesLogicMgr.OnWCToHongbaoCommonResponse:Request:WithType:` | ✅ | ✅ | 红包响应拦截(3参数) |
| `WCRedEnvelopesRedEnvelopesDetailViewController.viewDidLoad` | ✅ | ✅ | 详情页注入按钮 |
| `WCRedEnvelopesStoryViewController.viewDidLoad` | ❌ 不 hook | ✅ | 已移除（原因见第六章） |

---

## 三、详情按钮实现逐项对比

### 3.1 数据来源

| 对比维度 | WeChatPlugin | Mikoto |
|---|---|---|
| 数据来源 | `self.m_data.m_oWCRedEnvelopesDetailInfo` (KVC) | 同左 |
| Fallback | `self.m_oWCRedEnvelopesDetailInfo` | 同左 |
| 数据传递 | `objc_setAssociatedObject(btn, "detailInfo", ...)` | 同左 |
| 并发安全 | 每次 viewDidLoad 重新读 ivar，无全局变量 | 同左 |

### 3.2 按钮样式参数

所有参数与 Mikoto 完全一致：

| 参数 | 值 | Mikoto 对应 |
|---|---|---|
| tag | 99992 | 0x18698 |
| frame | `{viewW-50, viewH/2-22, 44, 44}` | 同公式 |
| backgroundColor | black alpha 0.6 | 0x3fe3333333333333 |
| cornerRadius | 22 | 22 |
| font | systemFontOfSize:12 | 12.0 |
| title | "详情" | "详情" |
| titleColor | white | white |
| autoresizingMask | 0x29 (LeftMargin\|TopMargin\|BottomMargin) | 0x29 |

### 3.3 弹窗内容

| 维度 | WeChatPlugin | Mikoto |
|---|---|---|
| 标题 | "红包详情" | 同左 |
| 字段 | 总金额/已领/剩余/总个数/已领/剩余 | 同左 |
| 金额转换 | 分÷100, %.2f | 同左 |
| 弹窗方式 | WeChatAlertHelper.showTipAlert | 同左 |
| 按钮 | "好的" | 同左 |

---

## 四、WeChatPlugin 独有改进

| 改进 | 说明 |
|---|---|
| bounds 零值保护 | viewDidLoad 时 bounds 可能为 CGRectZero，用屏幕尺寸兜底 |
| bringSubviewToFront | 确保按钮不被其他视图遮挡 |
| 只 hook DetailVC | 不 hook StoryVC，避免视图层级干扰（见第六章） |

---

## 五、代码健康度

| 维度 | WeChatPlugin | Mikoto |
|---|---|---|
| 死代码 | 已全部清理 | 无 |
| 全局变量 | 无 | 无 |
| 重试机制 | 无 | 无 |
| 可维护性 | ObjC 源码 | C 反编译代码 |

---

## 六、按钮不显示问题分析与解决

### 6.1 根因

微信红包详情页的视图层级：

```
DetailVC.view
  └── StoryVC.view (子 VC，播放红包动画)
```

Mikoto 两个 VC 都 hook `viewDidLoad` 的后果：

```
1. StoryVC.viewDidLoad  → 创建按钮在 StoryVC.view 上
2. DetailVC.viewDidLoad → viewWithTag:99992 递归搜到 StoryVC 的按钮 → SKIP
3. 红包动画结束，StoryVC 被移除
4. 按钮随 StoryVC.view 一起消失 ❌
5. viewDidLoad 只执行一次，DetailVC 不会再创建按钮
```

**日志验证**（redenvelop(85).log）：

```
Line 25: enter StoryVC  → 按钮创建成功
Line 41: enter DetailVC → step4 SKIP: button already exists (搜到了StoryVC的按钮)
→ 最终按钮消失
```

### 6.2 解决方案（方案B）

**只 hook DetailVC.viewDidLoad，不 hook StoryVC。**

```
DetailVC.viewDidLoad → 创建按钮在 DetailVC.view 上
StoryVC 加载 → 无按钮逻辑
StoryVC 被移除 → 按钮不受影响（在 DetailVC.view 上）
```

**日志验证**（redenvelop(86).log）—— 只有一条 DetailVC 记录，按钮创建成功，用户确认可见。

### 6.3 为什么不用 viewWillAppear

viewWillAppear 也能解决但存在不必要的重复调用（每次页面出现都执行），方案B 用 viewDidLoad 更干净——只执行一次，按钮就固定在 DetailVC.view 上。

---

## 七、最终结论

WeChatPlugin 与 Mikoto 在红包详情按钮的核心逻辑上完全对齐（数据源、按钮样式、弹窗内容），同时通过只 hook DetailVC 避免了 StoryVC 干扰问题。

| 对比项 | 结论 |
|---|---|
| 数据来源 | 一致（VC ivar 直接读取） |
| 按钮样式 | 一致（所有参数逐项对齐） |
| 弹窗内容 | 一致 |
| 可靠性 | **WeChatPlugin 更优**（无 StoryVC 干扰） |
| 代码简洁度 | **WeChatPlugin 更优**（少一个 hook） |