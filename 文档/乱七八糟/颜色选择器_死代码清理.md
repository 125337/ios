# 自定义颜色选择器 · 死代码清理文档

> 扫描文件：7 个 (WPHsvColorPickerController, WPHueSlider, WPSaturationBrightnessView, WPRGBAControl, WPHexInputView, WPColorPaletteView, WPColorUtil)
> 共发现 **8 处**死代码，分布见下表

---

## 一、死代码总览

| # | 文件 | 死代码 | 类型 | 建议动作 |
|---|------|--------|------|---------|
| 1 | `WPHueSlider.m` | `needsRebuildGradient` 属性 | 未使用的私有属性 | 删除声明 |
| 2 | `WPHueSlider.m` | `setIndicatorPositionAnimated:` | 外部未调用的公开方法 | 降级为私有或删除 |
| 3 | `WPSaturationBrightnessView.m` | `needsRebuild` 属性 | 未使用的私有属性 | 删除声明 |
| 4 | `WPSaturationBrightnessView.m` | `setIndicatorPositionAnimated:` | 外部未调用的公开方法 | 降级为私有或删除 |
| 5 | `WPRGBAControl.m` | `RGBColor` 宏 | 未使用的宏 | 删除 |
| 6 | `WPRGBAControl.m` | `suppressingCallback` | 永远为 NO 的条件分支 | 删除逻辑或用起来 |
| 7 | `WPColorPaletteView.m` | `kSectionGap` 常量 | 未使用的静态常量 | 删除 |
| 8 | `WPColorPaletteView.m` | `sectionLabels` 属性 | 只写不读 | 删除 |

---

## 二、逐一分析

### 1. WPHueSlider.m — `needsRebuildGradient`

```objc
// line 6 — 声明
@property (nonatomic, assign) BOOL needsRebuildGradient;
```

- 从未被任何地方 `set` 或 `read`
- `rebuildGradientIfNeeded` 用的是 `CGRectEqualToRect` 判断，根本不依赖这个 flag
- **删除**，不影响任何逻辑

**删除操作**：删掉 line 6

---

### 2. WPHueSlider.m — `setIndicatorPositionAnimated:`

```objc
// .h line 14 — 公开声明
- (void)setIndicatorPositionAnimated:(BOOL)animated;

// .m line 104-106 — 实现
- (void)setIndicatorPositionAnimated:(BOOL)animated {
    [self updateIndicatorPositionAnimated:animated];
}
```

- `.h` 中声明为公开方法，但外部从未调用
- VC 通过 `self.hueSlider.hue = xxx` 内部触发 `updateIndicatorPositionAnimated:`
- **论证**：没有实际用处，只是 `updateIndicatorPositionAnimated:` 的包装

**两种处理方式**（二选一）：
- **方案 A（推荐）**：从 `.h` 中删除声明，`.m` 中方法也删除（直接调私有的 `updateIndicatorPositionAnimated:`）
- **方案 B**：保留，用于未来外部控制指示器动画

---

### 3. WPSaturationBrightnessView.m — `needsRebuild`

```objc
// line 5 — 声明
@property (nonatomic, assign) BOOL needsRebuild;
```

- 从未被任何地方 `set` 或 `read`
- `layoutSubviews` 直接无条件调 `rebuildGradient`，不用这个 flag
- **删除**，不影响任何逻辑

**删除操作**：删掉 line 5

---

### 4. WPSaturationBrightnessView.m — `setIndicatorPositionAnimated:`

```objc
// .h line 20 — 公开声明
- (void)setIndicatorPositionAnimated:(BOOL)animated;

// .m line 131-133 — 实现
- (void)setIndicatorPositionAnimated:(BOOL)animated {
    [self updateIndicatorPositionAnimated:animated];
}
```

- 和 #2 完全相同的模式：公开暴露但无人调用
- **处理方式**：同 #2，从 `.h` 删除声明，`.m` 中删除包装方法

---

### 5. WPRGBAControl.m — `RGBColor` 宏

```objc
// line 4
#define RGBColor(r, g, b) [UIColor colorWithRed:r/255.0 green:g/255.0 blue:b/255.0 alpha:1]
```

- 整个文件内检索，没有一处使用此宏
- **删除**，无任何影响

**删除操作**：删掉 line 3-4（注释 + 宏）

---

### 6. WPRGBAControl.m — `suppressingCallback`

```objc
// line 15 — 声明
@property (nonatomic, assign) BOOL suppressingCallback;

// line 157 — 唯一读取处
- (void)notifyCallback {
    if (self.suppressingCallback) return;  // ← 永不满足，因为从未设过 YES
    ...
}
```

- `suppressingCallback` 只有声明和读取，**从不写入**
- 读取处的 `if (self.suppressingCallback) return;` 是死代码路径

**两种处理方式**（二选一）：
- **方案 A（推荐）**：删掉 `suppressingCallback` 属性声明，同时删掉 `notifyCallback` 中 `if (self.suppressingCallback) return;` 这一行——既然从未用到，说明不需要
- **方案 B**：把抑制功能用起来，在 `setRed:green:blue:alpha:animated:` 中设 `suppressingCallback = YES` 再调 `syncUI` 防止死循环

---

### 7. WPColorPaletteView.m — `kSectionGap`

```objc
// line 7
static CGFloat const kSectionGap  = 12;
```

- 声明在此，但全程未引用
- 所有间距硬编码在约束中（0 或 8），不用这个常量
- **删除**，不影响任何逻辑

**删除操作**：删掉 line 7

---

### 8. WPColorPaletteView.m — `sectionLabels`

```objc
// line 15 — 声明
@property (nonatomic, strong) NSMutableArray<UILabel *> *sectionLabels;

// 初始化:
//   line 27 — initWithFrame 中赋空数组
//   line 39 — initWithCoder 中赋空数组
//   line 93 — rebuildAll 中 removeAllObjects

// line 142 — 唯一写入处
[self.sectionLabels addObject:label];
```

- `sectionLabels` 被创建、添加元素，但**从未被读取**
- 标签视图 `label` 已通过 `[row addSubview:label]` 添加到视图层级，不需要另外维护数组
- **删除**，不影响任何功能

**删除操作**：删掉 line 15（属性声明）, line 27/39（初始化赋值）, line 93（`removeAllObjects`）, line 142（`addObject:`）

---

## 三、Cleanup 核对表

### 改动汇总

| 文件 | 删除 | 行数范围 | 合计 |
|------|------|---------|------|
| `WPHueSlider.m` | `needsRebuildGradient` 属性 | line 6 | −1 |
| `WPHueSlider.h` | `setIndicatorPositionAnimated:` 声明 | line 14 | −1 |
| `WPHueSlider.m` | `setIndicatorPositionAnimated:` 实现 + 注释 | lines 103-106 | −4 |
| `WPSaturationBrightnessView.m` | `needsRebuild` 属性 | line 5 | −1 |
| `WPSaturationBrightnessView.h` | `setIndicatorPositionAnimated:` 声明 | line 20 | −1 |
| `WPSaturationBrightnessView.m` | `setIndicatorPositionAnimated:` 实现 + 注释 | lines 130-133 | −4 |
| `WPRGBAControl.m` | `RGBColor` 宏 + 注释 | lines 3-4 | −2 |
| `WPRGBAControl.m` | `suppressingCallback` 属性声明 | line 15 | −1 |
| `WPRGBAControl.m` | `notifyCallback` 中的死条件 | line 157 | −1 |
| `WPColorPaletteView.m` | `kSectionGap` 常量 | line 7 | −1 |
| `WPColorPaletteView.m` | `sectionLabels` 属性声明 | line 15 | −1 |
| `WPColorPaletteView.m` | `sectionLabels` 初始化 x2 | lines 27, 39 | −2 |
| `WPColorPaletteView.m` | `sectionLabels removeAllObjects` | line 93 | −1 |
| `WPColorPaletteView.m` | `sectionLabels addObject:` | line 142 | −1 |
| **总计** | **−21 行** | | |

### 验证步骤

清理后按以下顺序检查：

```
1. 编译 → 无 warning
2. WPHueSlider 色相条拖动正常
3. WPSaturationBrightnessView 面板渐变 + 拖动正常
4. WPRGBAControl 滑动/输入双向同步正常
5. WPColorPaletteView 预设色/莫兰迪/历史色块显示和点击正常
6. WPColorPaletteView 确认后历史色保存正常
```

---

## 四、删除后各文件最终行数（预估）

| 文件 | 改造前 | 删除后 |
|------|--------|--------|
| `WPHueSlider.h` | 18 行 | 17 行 |
| `WPHueSlider.m` | 121 行 | 116 行 |
| `WPSaturationBrightnessView.h` | 24 行 | 23 行 |
| `WPSaturationBrightnessView.m` | 148 行 | 143 行 |
| `WPRGBAControl.m` | 199 行 | 195 行 |
| `WPColorPaletteView.m` | 220 行 | 213 行 |
| 其他文件 | 不变 | — |