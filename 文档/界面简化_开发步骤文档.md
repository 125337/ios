# 界面简化 — 开发步骤文档

> 基于 `微信优化反编译最新/123456.c` 逆向分析，UI 已实现 (`WPUISimplifyVC.m`)，本文档聚焦 Hook 实现和数据持久化。

---

## 开发状态总览

| 步骤 | 内容 | 状态 | 涉及文件 |
|------|------|------|----------|
| 1 | 配置属性定义 | 待开发 | `PluginConfig.h`, `PluginConfig.m`, `Constants.h` |
| 2 | Hook 模块创建 (9个Hook) | 待开发 | `UISimplifyHook.h`, `UISimplifyHook.m` |
| 3 | 设置页 UI | ✅ 已完成 | `WPUISimplifyVC.m` |
| 4 | 导航入口注册 | ✅ 已完成 | `WPUIVC.m`, `FeatureModuleRegistry.m` |

---

## 现有 UI 结构 (已完成)

文件: `WeChatPlugin/Modules/SettingEntry/WPUISimplifyVC.m`

### 总开关

- 存储键: `@"SimplifyEnabled"` (BOOL, NSUserDefaults)
- 控制: `SimplifySwitchTarget` 响应 UISwitch toggle，控制下方 4 个 section 的显示/隐藏 + scroll contentSize 切换

```objc
// 现有代码 (L11-13):
- (void)toggleSwitch:(UISwitch *)sw {
    NSArray *sections = objc_getAssociatedObject(sw, "sections");
    for (UIView *v in sections) { v.hidden = !sw.on; }
    [[NSUserDefaults standardUserDefaults] setBool:sw.on forKey:@"SimplifyEnabled"];
    // ... scroll resize ...
}
```

### 4 个配置 Section

| Section | 标题 | 条目 | 默认值 (第2列) |
|---------|------|------|----------------|
| 1 | 顶部标签自定义 | 微信标题 | `@"微信"` |
| | | 通讯录标题 | `@"通讯录"` |
| | | 发现标题 | `@"发现"` |
| 2 | 特殊自定义 | 通讯录底部好友 | `@"好友"` |
| 3 | 我的页面菜单名称自定义 | 服务/支付与服务 | `@"服务"` |
| | | 收藏 | `@"收藏"` |
| | | 朋友圈 | `@"朋友圈"` |
| | | 卡片/订单与卡包 | `@"卡包"` |
| | | 表情 | `@"表情"` |
| | | 设置 | `@"设置"` |
| | | 插件 | `@"插件"` |
| 4 | 底部标签自定义 | 微信 | `@"微信"` |
| | | 通讯录 | `@"通讯录"` |
| | | 发现 | `@"发现"` |
| | | 我 | `@"我"` |

### 取值方式

`WPAddEditableRow(card, y, w, title, value, handler)` 在每行创建左右两个 UILabel:
- 左侧: `title` 文本 (配置项名称)
- 右侧: `value` 文本 (当前值)

点击该行 → `onEditRowTap:` → 弹出 UIAlertController，alert.title = `title`，可编辑右侧 `valueLabel.text`。

---

## 需要补充: 数据持久化

当前 `onEditRowTap:` 只更新了 Label 文本，**没有持久化到 NSUserDefaults**。需要为每条数据增加独立的存储键。

### NSUserDefaults 键名定义

```objc
// 在 Constants.h 中添加（或直接在 UI 文件中定义）:
#define kSimplify_MainTitle     @"Simplify_MainTitle"       // 微信标题
#define kSimplify_ContactsTitle @"Simplify_ContactsTitle"    // 通讯录标题
#define kSimplify_DiscoverTitle @"Simplify_DiscoverTitle"    // 发现标题
#define kSimplify_FriendsCount  @"Simplify_FriendsCount"     // 通讯录底部好友
#define kSimplify_Menu_Names    @"Simplify_MenuNames"        // 菜单名字典
#define kSimplify_Tab_Names     @"Simplify_TabNames"         // Tab名字典
```

### 持久化逻辑: 修改 `onEditRowTap:`

```objc
// WPUISimplifyVC.m 中 onEditRowTap: 的"确定"按钮回调增加持久化:

NSString *nsKey = objc_getAssociatedObject(sender, "editNSKey");  // 新增: 绑定存储键
if (nsKey && weakLabel) {
    NSUserDefaults *d = [NSUserDefaults standardUserDefaults];
    [d setObject:newText forKey:nsKey];
    [d synchronize];
}
```

### NSUserDefaults 数据组织

**字典类型的键** (`Simplify_MenuNames`, `Simplify_Tab_Names`) 以默认值(即微信原始名称)为 key，用户输入为 value:

```
Simplify_MenuNames = {
    "服务" : "支付",       // 微信原文"服务" → 用户输入"支付"
    "收藏" : "收藏夹",
    "朋友圈" : "动态",
    "卡包" : "卡券",
    "表情" : "表情包",
    "设置" : "选项",
    "插件" : "扩展",
}
Simplify_Tab_Names = {
    "微信" : "聊天",
    "通讯录" : "联系人",
    "发现" : "探索",
    "我" : "我的",
}
```

**字符串类型的键** (`Simplify_MainTitle` 等) 直接存储用户输入的字符串。

### 修改 `WPAddEditableRow` 调用

在 `WPUISimplifyViewDidLoad` 中，每处 `WPAddEditableRow` 调用后，需要把对应的 NSUserDefaults key 绑定到该行的 `sender` (UIButton) 上:

```objc
// 示例: 修改"微信标题"行
UIButton *row = WPAddEditableRow(topBarCard, tby, w, @"微信标题", @"微信", handler);
objc_setAssociatedObject(row, "editNSKey", @"Simplify_MainTitle", OBJC_ASSOCIATION_COPY_NONATOMIC);
```

---

## 步骤2: Hook 模块 — UISimplifyHook (待开发)

共 **9 个 Hook**，分 2 种策略（不含钱包余额）：

### 文件结构

```
WeChatPlugin/Modules/Layout/UISimplifyHook.h
WeChatPlugin/Modules/Layout/UISimplifyHook.m
```

```
UISimplifyHook.m:
├── 原始 IMP 指针声明区 (9个)
├── 配置读取 (直接从 NSUserDefaults 读)
├── 策略A: 字典查找替换 (6个Hook)
│   ├── WCTableViewCellLeftConfig.title         → 菜单名
│   ├── MMTabbarItem.initWithTitle:...           → Tab初始化
│   ├── MMTabbarItem.setNormalTitle:             → Tab标题
│   ├── MMTabBarItemView.setTitle:               → Tab视图
│   ├── MMTabBarController.setTabBarItemTitle:.. → Tab控制器
│   └── MMTableViewInfo.getTitle:               → Cell标题+换行处理
├── 策略B: 全局标签拦截+上下文匹配 (3个Hook)
│   ├── MMUILabel.setText: ⭐ 4层决策树
│   ├── MMUILabel.setAttributedText: (保留属性版)
│   └── MFTitleView.updateTitleView:title:
└── + (void)install  (9个 MSHookMessageEx)
```

### UISimplifyHook.h

```objc
#import <Foundation/Foundation.h>

@interface UISimplifyHook : NSObject
+ (void)install;
@end
```

### 配置读取辅助

```objc
// 直接从 NSUserDefaults 读取, 对应反编译 DAT_0013a400 全局变量
static inline BOOL SimplifyEnabled(void) {
    return [[NSUserDefaults standardUserDefaults] boolForKey:@"SimplifyEnabled"];
}

// 菜单名字典: 微信原文→用户自定义名
static inline NSDictionary *MenuNames(void) {
    return [[NSUserDefaults standardUserDefaults] dictionaryForKey:@"Simplify_MenuNames"] ?: @{};
}

// Tab名字典: 微信原文→用户自定义名
static inline NSDictionary *TabNames(void) {
    return [[NSUserDefaults standardUserDefaults] dictionaryForKey:@"Simplify_Tab_Names"] ?: @{};
}

// 单个字符串读取
static inline NSString *StrForKey(NSString *key) {
    return [[NSUserDefaults standardUserDefaults] stringForKey:key];
}
#define MainTitle()     StrForKey(@"Simplify_MainTitle")
#define ContactsTitle() StrForKey(@"Simplify_ContactsTitle")
#define DiscoverTitle() StrForKey(@"Simplify_DiscoverTitle")
#define FriendsCount()  StrForKey(@"Simplify_FriendsCount")
```

### 策略A: WCTableViewCellLeftConfig.title (参照 FUN_00044490)

```objc
static IMP _orig_WCTableViewCellLeftConfig_title = NULL;

static id hook_WCTableViewCellLeftConfig_title(id self, SEL _cmd) {
    id orig = ((id (*)(id, SEL))_orig_WCTableViewCellLeftConfig_title)(self, _cmd);
    if (!SimplifyEnabled() || !orig) return orig;

    id repl = MenuNames()[orig];
    if (repl) return repl;

    // 处理 "服务\n支付" 复合标题 → 取 \n 后部分查字典
    NSRange nl = [orig rangeOfString:@"\n"];
    if (nl.location != NSNotFound) {
        NSString *suffix = [orig substringFromIndex:nl.location + 1];
        id subRepl = MenuNames()[suffix];
        if (subRepl) return subRepl;
    }
    return orig;
}
```

### 策略A: 4个底部Tab Hook (参照 FUN_00044544/698/764/830)

```objc
// 共享替换核心
static NSString *replaceTabTitle(NSString *title) {
    if (!SimplifyEnabled() || !title) return title;
    id repl = TabNames()[title];
    return repl ?: title;
}

// Hook 1: MMTabbarItem.initWithTitle:normalImage:selectedImage:
static IMP _orig_MMTabbarItem_init = NULL;
static id hook_MMTabbarItem_init(id self, SEL _cmd, NSString *title, id img, id selImg) {
    return ((id (*)(id, SEL, id, id, id))_orig_MMTabbarItem_init)(
        self, _cmd, replaceTabTitle(title), img, selImg);
}

// Hook 2: MMTabbarItem.setNormalTitle:
static IMP _orig_MMTabbarItem_setNormalTitle = NULL;
static void hook_MMTabbarItem_setNormalTitle(id self, SEL _cmd, NSString *title) {
    ((void (*)(id, SEL, id))_orig_MMTabbarItem_setNormalTitle)(self, _cmd, replaceTabTitle(title));
}

// Hook 3: MMTabBarItemView.setTitle:
static IMP _orig_MMTabBarItemView_setTitle = NULL;
static void hook_MMTabBarItemView_setTitle(id self, SEL _cmd, NSString *title) {
    ((void (*)(id, SEL, id))_orig_MMTabBarItemView_setTitle)(self, _cmd, replaceTabTitle(title));
}

// Hook 4: MMTabBarController.setTabBarItemTitle:forIndex:
static IMP _orig_MMTabBarController_setTitle = NULL;
static void hook_MMTabBarController_setTitle(id self, SEL _cmd, NSString *title, NSUInteger idx) {
    ((void (*)(id, SEL, id, NSUInteger))_orig_MMTabBarController_setTitle)(
        self, _cmd, replaceTabTitle(title), idx);
}
```

### 策略A: MMTableViewInfo.getTitle: (参照 FUN_00044910)

```objc
static IMP _orig_MMTableViewInfo_getTitle = NULL;

static id hook_MMTableViewInfo_getTitle(id self, SEL _cmd) {
    id orig = ((id (*)(id, SEL))_orig_MMTableViewInfo_getTitle)(self, _cmd);
    if (!SimplifyEnabled() || !orig) return orig;

    id repl = MenuNames()[orig];
    if (repl) return repl;

    // "服务\n支付" → 取 "支付" 查字典
    NSRange nl = [orig rangeOfString:@"\n"];
    if (nl.location != NSNotFound) {
        NSString *suffix = [orig substringFromIndex:nl.location + 1];
        id subRepl = MenuNames()[suffix];
        if (subRepl) return subRepl;
    }
    return orig;
}
```

### 策略B — MMUILabel.setText: (参照 FUN_00044a80, L40388) ⭐核心

```objc
static IMP _orig_MMUILabel_setText = NULL;

static void hook_MMUILabel_setText(id self, SEL _cmd, NSString *text) {
    // 守卫1: nil 或未启用
    if (!text || !SimplifyEnabled()) {
        ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, text);
        return;
    }

    // 守卫2: "[" 开头 = badge数字, 白名单跳过
    if ([text hasPrefix:@"["]) {
        ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, text);
        return;
    }

    // 匹配层1: NavigationBar → 微信/通讯录/发现标题
    id responder = self;
    while ((responder = [responder nextResponder])) {
        NSString *className = NSStringFromClass([responder class]);
        if ([className containsString:@"NavigationBar"]) {
            // 区分三种页面
            NSString *title = MainTitle();
            if (title.length == 0) title = ContactsTitle();
            if (title.length == 0) title = DiscoverTitle();
            if (title.length > 0) {
                ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, title);
                return;
            }
            break;
        }
    }

    // 匹配层2: Contacts → contactsReplacement
    NSString *ct = ContactsTitle();
    if (ct.length > 0) {
        responder = self;
        while ((responder = [responder nextResponder])) {
            if ([NSStringFromClass([responder class]) containsString:@"Contact"]) {
                if ([text hasPrefix:@"通讯录"] || [text isEqualToString:@"通讯录"]) {
                    ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, ct);
                    return;
                }
            }
        }
    }

    // 匹配层3: Discover → discoverReplacement
    NSString *dt = DiscoverTitle();
    if (dt.length > 0) {
        if ([text hasPrefix:@"发现"] || [text isEqualToString:@"发现"]) {
            ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, dt);
            return;
        }
    }

    // 匹配层4: 好友数格式替换
    NSString *fc = FriendsCount();
    if (fc.length > 0) {
        NSRegularExpression *regex = [NSRegularExpression
            regularExpressionWithPattern:@"\\d+" options:0 error:nil];
        NSTextCheckingResult *match = [regex firstMatchInString:text
            options:0 range:NSMakeRange(0, text.length)];
        if (match && [text containsString:@"位"] && [text containsString:@"联系人"]) {
            NSString *number = [text substringWithRange:match.range];
            NSString *formatted = [NSString stringWithFormat:fc, number];
            ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, formatted);
            return;
        }
    }

    // 默认
    ((void (*)(id, SEL, id))_orig_MMUILabel_setText)(self, _cmd, text);
}
```

### 策略B — MMUILabel.setAttributedText: (参照 FUN_00044ecc)

与 `setText:` **相同的匹配决策树**，替换时保留富文本属性：

```objc
static IMP _orig_MMUILabel_setAttributedText = NULL;

static NSAttributedString *replacedAttrStr(NSAttributedString *orig, NSString *newText) {
    NSDictionary *attrs = (orig.length > 0)
        ? [orig attributesAtIndex:0 effectiveRange:NULL]
        : nil;
    return [[NSAttributedString alloc] initWithString:newText ?: @""
                                           attributes:attrs ?: @{}];
}

static void hook_MMUILabel_setAttributedText(id self, SEL _cmd, NSAttributedString *attrText) {
    if (!attrText || !SimplifyEnabled()) {
        ((void (*)(id, SEL, id))_orig_MMUILabel_setAttributedText)(self, _cmd, attrText);
        return;
    }
    NSString *text = attrText.string;
    if ([text hasPrefix:@"["]) {
        ((void (*)(id, SEL, id))_orig_MMUILabel_setAttributedText)(self, _cmd, attrText);
        return;
    }
    // ... 同 setText 的4层决策树，每处将 setText 替换为 setAttributedText + replacedAttrStr ...
    ((void (*)(id, SEL, id))_orig_MMUILabel_setAttributedText)(self, _cmd, attrText);
}
```

### 策略B — MFTitleView.updateTitleView:title: (参照 FUN_000454d0)

```objc
static IMP _orig_MFTitleView_updateTitle = NULL;

static void hook_MFTitleView_updateTitle(id self, SEL _cmd, id titleView, NSString *title) {
    if (!title || !SimplifyEnabled()) {
        ((void (*)(id, SEL, id, id))_orig_MFTitleView_updateTitle)(self, _cmd, titleView, title);
        return;
    }
    // ... 同 setText 的3层标题决策树 (不含好友数)，匹配成功时替换 title 参数 ...
    ((void (*)(id, SEL, id, id))_orig_MFTitleView_updateTitle)(self, _cmd, titleView, title);
}
```

### install 方法

```objc
+ (void)install {
    WPLog(@"UISimplify", @"UISimplifyHook install start");

    // ===== 策略A: 字典查找替换 (6个) =====
    MSHookMessageEx(objc_getClass("WCTableViewCellLeftConfig"), @selector(title),
        (IMP)hook_WCTableViewCellLeftConfig_title,
        (IMP *)&_orig_WCTableViewCellLeftConfig_title);

    Class tabItem = objc_getClass("MMTabbarItem");
    MSHookMessageEx(tabItem, @selector(initWithTitle:normalImage:selectedImage:),
        (IMP)hook_MMTabbarItem_init, (IMP *)&_orig_MMTabbarItem_init);
    MSHookMessageEx(tabItem, @selector(setNormalTitle:),
        (IMP)hook_MMTabbarItem_setNormalTitle, (IMP *)&_orig_MMTabbarItem_setNormalTitle);

    MSHookMessageEx(objc_getClass("MMTabBarItemView"), @selector(setTitle:),
        (IMP)hook_MMTabBarItemView_setTitle, (IMP *)&_orig_MMTabBarItemView_setTitle);
    MSHookMessageEx(objc_getClass("MMTabBarController"), @selector(setTabBarItemTitle:forIndex:),
        (IMP)hook_MMTabBarController_setTitle, (IMP *)&_orig_MMTabBarController_setTitle);
    MSHookMessageEx(objc_getClass("MMTableViewInfo"), @selector(getTitle:),
        (IMP)hook_MMTableViewInfo_getTitle, (IMP *)&_orig_MMTableViewInfo_getTitle);

    // ===== 策略B: 全局标签拦截 (3个) =====
    MSHookMessageEx(objc_getClass("MMUILabel"), @selector(setText:),
        (IMP)hook_MMUILabel_setText, (IMP *)&_orig_MMUILabel_setText);
    MSHookMessageEx(objc_getClass("MMUILabel"), @selector(setAttributedText:),
        (IMP)hook_MMUILabel_setAttributedText, (IMP *)&_orig_MMUILabel_setAttributedText);
    MSHookMessageEx(objc_getClass("MFTitleView"), @selector(updateTitleView:title:),
        (IMP)hook_MFTitleView_updateTitle, (IMP *)&_orig_MFTitleView_updateTitle);

    WPLog(@"UISimplify", @"UISimplifyHook install complete (9 hooks, 2 strategies)");
}
```

---

## 步骤3: 设置页 UI (✅ 已完成)

文件: `WeChatPlugin/Modules/SettingEntry/WPUISimplifyVC.m`

当前页面结构:

```
┌─────────────────────────────────────┐
│ 界面名称简化                 [Switch]│ ← SimplifyEnabled
├─────────────────────────────────────┤
│ 顶部标签自定义                      │
│  微信标题      "微信"    → 点击弹框 │ ← Simplify_MainTitle
│  通讯录标题    "通讯录"  → 点击弹框 │ ← Simplify_ContactsTitle
│  发现标题      "发现"    → 点击弹框 │ ← Simplify_DiscoverTitle
├─────────────────────────────────────┤
│ 特殊自定义                          │
│  通讯录底部好友 "好友"   → 点击弹框 │ ← Simplify_FriendsCount
├─────────────────────────────────────┤
│ 我的页面菜单名称自定义 (7项)        │
│  服务/支付与服务 "服务" → 点击弹框  │ ← Simplify_MenuNames["服务"]
│  收藏          "收藏"   → 点击弹框  │ ← Simplify_MenuNames["收藏"]
│  朋友圈        "朋友圈" → 点击弹框  │ ← Simplify_MenuNames["朋友圈"]
│  卡片/订单与卡包 "卡包" → 点击弹框  │ ← Simplify_MenuNames["卡包"]
│  表情          "表情"   → 点击弹框  │ ← Simplify_MenuNames["表情"]
│  设置          "设置"   → 点击弹框  │ ← Simplify_MenuNames["设置"]
│  插件          "插件"   → 点击弹框  │ ← Simplify_MenuNames["插件"]
├─────────────────────────────────────┤
│ 底部标签自定义 (4项)                │
│  微信   "微信"   → 点击弹框         │ ← Simplify_Tab_Names["微信"]
│  通讯录 "通讯录" → 点击弹框         │ ← Simplify_Tab_Names["通讯录"]
│  发现   "发现"   → 点击弹框         │ ← Simplify_Tab_Names["发现"]
│  我     "我"     → 点击弹框         │ ← Simplify_Tab_Names["我"]
└─────────────────────────────────────┘
  "修改后将在下次启动时生效"
```

---

## 步骤4: 模块注册与导航 (✅ 已完成)

### 4.1 FeatureModuleRegistry.m

```objc
#import "../Modules/Layout/UISimplifyHook.h"

// 在 layout 模块的 hookInstallerClasses 中添加:
@[[TintHook class], [UIPurifyHook class], [UISimplifyHook class]]
```

### 4.2 WPUIVC.m — 导航入口 (L10-96)

已实现:

```objc
@[@"界面简化", @"openUISimplify:"],                        // items 数组 L34
- (void)openUISimplify:(id)sender { ... }                  // 导航方法 L79-96
```

### 4.3 WPUISimplifyVC.m — Helper 类 (L239-263)

已实现 `WPUISimplifyVCHelper`，动态创建 `WPUISimplifyVC` 子类。

---

## 验证清单

| 验证项 | 操作 |
|--------|------|
| 总开关 | 打开/关闭 switch → 检查下方配置区显示/隐藏 |
| 总开关功能 | 关闭 switch → Tab/菜单恢复原样 |
| 底部Tab替换 | 修改"微信"→"聊天" → 微信重启后底部Tab显示"聊天" |
| 菜单名替换 | 修改"服务"→"钱包" → "我的"页面显示"钱包" |
| 微信标题 | 修改→自定义文字 → 微信首页导航栏标题变更 |
| 通讯录标题 | 修改→自定义文字 → 通讯录页导航栏标题变更 |
| 发现标题 | 修改→自定义文字 → 发现页导航栏标题变更 |
| 好友数格式 | 输入 `%@个人` → 通讯录底部"X个人" |
| badge数字保护 | 未读消息角标仍正常显示 |
| 与界面净化共存 | 两个功能独立控制，互不影响 |
| 弹框编辑 | 点击行 → 弹出 UIAlertController → 输入 → 确定 → 右侧值更新 |

---

## 数据流总图

```
┌─────────────────────────────────┐
│  WPUISimplifyVC (编辑)          │
│  onEditRowTap: → UIAlertController│
│  "确定" → valueLabel.text = newText│
│          → NSUserDefaults setObject:│
└───────────┬─────────────────────┘
            │ NSUserDefaults
            │ SimplifyEnabled
            │ Simplify_MainTitle
            │ Simplify_ContactsTitle
            │ Simplify_DiscoverTitle
            │ Simplify_FriendsCount
            │ Simplify_MenuNames (dict)
            │ Simplify_Tab_Names (dict)
            ▼
┌─────────────────────────────────┐
│  UISimplifyHook                 │
│  +install 安装 9个 Hook         │
│  直接从 NSUserDefaults 读取     │
│  策略A (6个): 字典查表替换      │
│  策略B (3个): 上下文匹配替换    │
└───────────┬─────────────────────┘
            │
     ┌──────┼──────┐
     ▼      ▼      ▼
  微信Tab 微信菜单 微信标题
  名称替换 名称替换 名称替换
```

## 附录: 反编译对照速查表

| 反编译函数 | 行号 | 项目对应 |
|-----------|------|----------|
| `FUN_00043e44` | L39964 | 启动加载 (直接读 NSUserDefaults) |
| `FUN_00043e84` | L39977 | 配置加载器 (直接读 NSUserDefaults) |
| `FUN_00044244` | L40131 | `UISimplifyHook.+install` |
| `FUN_00044490` | L40165 | `hook_WCTableViewCellLeftConfig_title` |
| `FUN_00044544` | L40200 | `hook_MMTabbarItem_init` |
| `FUN_00044698` | L40244 | `hook_MMTabbarItem_setNormalTitle` |
| `FUN_00044764` | L40273 | `hook_MMTabBarItemView_setTitle` |
| `FUN_00044830` | L40302 | `hook_MMTabBarController_setTitle` |
| `FUN_00044910` | L40331 | `hook_MMTableViewInfo_getTitle` |
| `FUN_00044a80` | L40388 | `hook_MMUILabel_setText` ⭐ |
| `FUN_00044ecc` | L40526 | `hook_MMUILabel_setAttributedText` |
| `FUN_000454d0` | L40696 | `hook_MFTitleView_updateTitle` |
| ~~FUN_00045788~46010~~ | ~~L40800~~ | ~~已移除: 钱包余额 x6~~ |