# 箭码/二维码识别代码最终精简方案

> 本文档基于 `ProfileCardBgHook.m` 第 97-124 行的实际代码现况编写

---

## 当前代码现况（第 99-115 行）

```objc
+ (BOOL)isArrowQRView:(UIView *)view {
    // 箭头：UIImageView + 帧 + 右侧位置（排除 TextState 按钮）
    if ([view isKindOfClass:[UIImageView class]]) {
        CGFloat w = view.frame.size.width;
        CGFloat h = view.frame.size.height;
        CGFloat x = view.frame.origin.x;
        CGFloat parentW = view.superview.bounds.size.width;
        // ★ parentW - 25
        if (w >= 10 && w <= 18 && h >= 8 && h <= 40 && x >= parentW - 25) {
            return YES;
        }
    }
    // 二维码：MMUIButton 精准匹配（容器内唯一 MMUIButton）
    if ([view isKindOfClass:NSClassFromString(@"MMUIButton")]) {
        return YES;  // ← 无位置条件
    }
    return NO;
}
```

## 当前问题

| 策略 | 当前状态 | 结果 |
|---|---|---|
| 帧启发式 `parentW - 25` | 太紧 | ❌ 箭头没被隐藏 |
| MMUIButton 无位置 | 连带隐藏状态按钮 | ❌ 二维码隐藏了，状态按钮也被隐藏了 |

## 改动步骤

### 改动一：帧启发式从 `parentW - 25` 恢复为 `parentW - 40`

**第107行：**
```objc
// 旧（太紧，箭头不命中）
if (w >= 10 && w <= 18 && h >= 8 && h <= 40 && x >= parentW - 25) {

// 新（恢复原值，箭头命中）
if (w >= 10 && w <= 18 && h >= 8 && h <= 40 && x >= parentW - 40) {
```

### 改动二：MMUIButton 加位置条件排除状态按钮

**第111-113行改为：**
```objc
    // 二维码：MMUIButton + 右侧位置（排除 TextState 状态按钮）
    if ([view isKindOfClass:NSClassFromString(@"MMUIButton")]) {
        CGFloat x = view.frame.origin.x;
        CGFloat parentW = view.superview.bounds.size.width;
        if (x >= parentW - 60) {
            return YES;
        }
    }
```

### 改动三：更新注释

**第106行注释改为：**
```objc
        // 箭头：UIImageView + 帧（WCRefine FUN_00337128）
```

---

## 改后完整代码

```objc
#pragma mark - 箭码/二维码识别

+ (BOOL)isArrowQRView:(UIView *)view {
    // 箭头：UIImageView + 帧（WCRefine FUN_00337128）
    if ([view isKindOfClass:[UIImageView class]]) {
        CGFloat w = view.frame.size.width;
        CGFloat h = view.frame.size.height;
        CGFloat x = view.frame.origin.x;
        CGFloat parentW = view.superview.bounds.size.width;
        if (w >= 10 && w <= 18 && h >= 8 && h <= 40 && x >= parentW - 40) {
            return YES;
        }
    }
    // 二维码：MMUIButton + 右侧位置（排除 TextState 状态按钮）
    if ([view isKindOfClass:NSClassFromString(@"MMUIButton")]) {
        CGFloat x = view.frame.origin.x;
        CGFloat parentW = view.superview.bounds.size.width;
        if (x >= parentW - 60) {
            return YES;
        }
    }
    return NO;
}

+ (void)hideArrowQRInCell:(UIView *)cell {
    for (UIView *sub in cell.subviews) {
        if ([self isArrowQRView:sub]) {
            sub.hidden = YES;
        }
    }
}
```

---

## 各策略分工

| 策略 | 位置条件 | 命中目标 | 排除依据 |
|---|---|---|---|
| 帧启发式 | `x >= parentW - 40` | **箭头** | 状态按钮不是 UIImageView，不会误伤 |
| MMUIButton | `x >= parentW - 60` | **二维码** | 状态按钮 x 在 `parentW - 60` 左侧，排除 |

---

## 验证清单

| 检查项 | 预期 |
|---|---|
| 开启「隐藏箭码」 | 箭头隐藏 ✅ 二维码隐藏 ✅ 状态按钮正常显示 ✅ |
| 关闭「隐藏箭码」 | 箭头显示、二维码显示 |
| 同时开启卡片隐藏 | 全部隐藏 |
| 开启素材 + 隐藏箭码 | 素材正常，箭码隐藏 |