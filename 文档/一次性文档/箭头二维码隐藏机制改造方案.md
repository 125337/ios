# 箭头/二维码隐藏 — 识别 + 遍历改造步骤

> 前置条件：配置迁移 `listHideRightQRCode` → `myPageHideArrow`、UI 迁移到"我的页面美化"节段、功能独立不再受总开关保护——这些**均已就绪**，本改造不涉及。

---

## 第一步：新增 3 个识别方法（ProfileCardBgHook.m）

### 1.1 新增方法 A — accessibilityLabel 匹配

在 `#pragma mark - QR 码隐藏` 节段上方，插入以下 3 个方法。注意：目前该节段已有 `hideQRButtonInCell:` 和 `hideQRButtonInSubviews:`，**先不删它们**，新方法加在前面。

**操作：** 找到 L97 的 `#pragma mark - QR 码隐藏`，将光标移到这一行的上一行，粘贴：

```objc
#pragma mark - 箭码/二维码识别（单层遍历，参考 WCRefine）

+ (BOOL)isArrowQRByAccessibilityLabel:(UIView *)view {
    NSString *label = view.accessibilityLabel;
    if (!label) return NO;
    // WCRefine FUN_00336e4c 反编译确认的三次 isEqualToString 对比
    return [label isEqualToString:@"More_CardInfo"] ||
           [label isEqualToString:@"•"] ||
           [label isEqualToString:@"• N"];
}

+ (BOOL)isArrowQRByFrameHeuristic:(UIView *)view {
    if (![view isKindOfClass:[UIImageView class]]) return NO;
    CGFloat w = view.frame.size.width;
    CGFloat h = view.frame.size.height;
    CGFloat x = view.frame.origin.x;
    CGFloat parentW = view.superview.bounds.size.width;
    // WCRefine FUN_00337128 反编译确认的帧判断
    return (w >= 10 && w <= 18) &&
           (h >= 8 && h <= 40) &&
           (x >= parentW - 40);
}

+ (BOOL)isArrowQRView:(UIView *)view {
    return [self isArrowQRByAccessibilityLabel:view] ||
           [self isArrowQRByFrameHeuristic:view];
}

```

### 1.2 新增主入口方法

在 `isArrowQRView:` 方法下方（紧接其后），粘贴新主入口方法：

```objc
+ (void)hideArrowQRInCell:(UIView *)cell {
    for (UIView *sub in cell.subviews) {  // 单层遍历，不递归
        if ([self isArrowQRView:sub]) {
            sub.hidden = YES;
        }
    }
}
```

### 1.3 删除旧方法

找到并删除以下两个方法：

| 方法 | 行号范围 |
|:-----|:---------|
| `hideQRButtonInCell:` | L99-L110（约 12 行） |
| `hideQRButtonInSubviews:` | L112-L123（约 12 行） |

> 删完后 `#pragma mark - 箭码/二维码识别` 节段只有 4 个新方法，旧代码完全清除。

---

## 第二步：更新 3 处调用方法名（ProfileCardBgHook.m）

将调用 `hideQRButtonInCell:` 的 3 个地方**全部改为** `hideArrowQRInCell:`：

| 位置 | 行号（删旧方法后的新行号） | 改前 | 改后 |
|:-----|:--------------------------|:------|:------|
| `handleArrowQRHiding:` | 约 L700-705 | `[self hideQRButtonInCell:button]` | `[self hideArrowQRInCell:button]` |
| `handleHiddenPath` 无素材 | 约 L726-732 | `[ProfileCardBgHook hideQRButtonInCell:button]` | `[ProfileCardBgHook hideArrowQRInCell:button]` |
| `handleHiddenPath` 有素材 | 约 L750-756 | `[ProfileCardBgHook hideQRButtonInCell:button]` | `[ProfileCardBgHook hideArrowQRInCell:button]` |

> 注意 `handleArrowQRHiding:` 内用 `self` 调用（同一个类），`handleHiddenPath` 内用 `ProfileCardBgHook` 类名调用。

---

## 第三步：更新头文件声明（ProfileCardBgHook.h）

| 操作 | 当前 | 改为 |
|:-----|:------|:------|
| **删除** L41 | `+ (void)hideQRButtonInCell:(UIView *)cell;` | 删掉整行 |
| **删除** L42 | `+ (void)hideQRButtonInSubviews:(NSArray<UIView *> *)subviews;` | 删掉整行 |
| **新增**（放在 L42 原位置） | — | `+ (void)hideArrowQRInCell:(UIView *)cell;` |

---

## 第四步：编译验证

```bash
# 1. 编译检查
xcodebuild -project MioPlugin.xcodeproj -target MioPlugin

# 2. 全局搜索确认零残留
grep -r "hideQRButtonInCell" /www/wwwroot/ios/MioPlugin/
grep -r "hideQRButtonInSubviews" /www/wwwroot/ios/MioPlugin/

# 两个都要返回 0 条结果
```

如果编译通过且搜索无残留，改造完成。

---

## 改前改后完整代码对比

### 改前（L97-L123）

```objc
#pragma mark - QR 码隐藏

+ (void)hideQRButtonInCell:(UIView *)cell {
    for (UIView *sub in cell.subviews) {
        NSString *cn = NSStringFromClass([sub class]);
        if ([cn containsString:@"Button"]) {
            CGFloat x = sub.frame.origin.x;
            if (x > sub.superview.bounds.size.width * 0.7) {
                sub.hidden = YES;
            }
        }
        [self hideQRButtonInSubviews:sub.subviews];
    }
}

+ (void)hideQRButtonInSubviews:(NSArray<UIView *> *)subviews {
    for (UIView *sub in subviews) {
        NSString *cn = NSStringFromClass([sub class]);
        if ([cn containsString:@"Button"]) {
            CGFloat x = sub.frame.origin.x;
            if (x > sub.superview.bounds.size.width * 0.7) {
                sub.hidden = YES;
            }
        }
        [self hideQRButtonInSubviews:sub.subviews];
    }
}
```

### 改后（L97-L135）

```objc
#pragma mark - 箭码/二维码识别（单层遍历，参考 WCRefine）

+ (BOOL)isArrowQRByAccessibilityLabel:(UIView *)view {
    NSString *label = view.accessibilityLabel;
    if (!label) return NO;
    // WCRefine FUN_00336e4c 反编译确认的三次 isEqualToString 对比
    return [label isEqualToString:@"More_CardInfo"] ||
           [label isEqualToString:@"•"] ||
           [label isEqualToString:@"• N"];
}

+ (BOOL)isArrowQRByFrameHeuristic:(UIView *)view {
    if (![view isKindOfClass:[UIImageView class]]) return NO;
    CGFloat w = view.frame.size.width;
    CGFloat h = view.frame.size.height;
    CGFloat x = view.frame.origin.x;
    CGFloat parentW = view.superview.bounds.size.width;
    // WCRefine FUN_00337128 反编译确认的帧判断
    return (w >= 10 && w <= 18) &&
           (h >= 8 && h <= 40) &&
           (x >= parentW - 40);
}

+ (BOOL)isArrowQRView:(UIView *)view {
    return [self isArrowQRByAccessibilityLabel:view] ||
           [self isArrowQRByFrameHeuristic:view];
}

+ (void)hideArrowQRInCell:(UIView *)cell {
    for (UIView *sub in cell.subviews) {  // 单层遍历，不递归
        if ([self isArrowQRView:sub]) {
            sub.hidden = YES;
        }
    }
}
```

---

## 验证清单

| # | 验证项 | 预期 |
|:-:|:-------|:------|
| 1 | 编译通过 | 无 error / warning |
| 2 | 全局 `hideQRButtonInCell` 残留 | 0 条 |
| 3 | 全局 `hideQRButtonInSubviews` 残留 | 0 条 |
| 4 | 箭头码隐藏（我的页面） | 开关 ON → 隐藏，OFF → 显示 |
| 5 | 二维码隐藏（我的页面） | 开关 ON → 隐藏，OFF → 显示 |
| 6 | 总开关 OFF + 隐藏箭码 ON | 依然生效（独立功能已验证） |
| 7 | 资料卡隐藏态 + 无素材 | 箭码/二维码同样受控 |
| 8 | 资料卡隐藏态 + 有素材 | 箭码/二维码同样受控 |
| 9 | 其他右侧按钮 | 未被误隐藏（双策略范围严格） |
| 10 | 多次开关切换 | 隐藏/显示行为一致 |