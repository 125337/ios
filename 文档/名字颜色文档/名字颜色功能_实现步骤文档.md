# 名字颜色功能 — 实现步骤文档

> **本文档已根据 `实现分析文档.md` 重写。**
> 旧版文档的错误：Hook 了 `updateStatus`/`layoutContentView` 修改 `NSAttributedString`，那是**消息时间颜色**的实现方式。正确的做法是 Hook 昵称标签的初始化方法，直接修改 `UILabel.textColor`。
>
> 项目路径：`/www/wwwroot/ios/MioPlugin/`

---

## 目录

1. [当前状态总览](#一当前状态总览)
2. [核心概念：旧方案 vs 新方案](#二核心概念旧方案-vs-新方案)
3. [唯一实现步骤：重写 TintHook.m](#三唯一实现步骤重写-tinthookm)
4. [TintHook.m 完整代码](#四-tinthookm-完整代码)
5. [验证清单](#五验证清单)
6. [文件变更清单](#六文件变更清单)

---

## 一、当前状态总览

### 1.1 已完成部分（无需操作）

| 文件 | 路径 | 说明 |
|------|------|------|
| `PluginConfig.h/.m` | `MioPlugin/Config/` | 已有 `chatNameColorHex`/`chatNameColorDarkHex`/`enableChatNameColor`/`chatNameFontSize`/`enableChatNameSize` 等完整属性及持久化 |
| `SettingNameColorController.m` | `MioPlugin/Settings/Controllers/` | 已有完整 UI：聊天界面设置卡片 + 朋友圈设置卡片，含颜色选择器、字体大小 |
| `WPUIVC.m` | `MioPlugin/Modules/SettingEntry/` | 已有导航入口（tag:101 → `SettingNameColorController`） |
| `FeatureModuleRegistry.m` | `MioPlugin/Registry/` | 已 import `TintHook`，已在 `layout` 模块注册 |
| `SettingCategoryController.m` | `MioPlugin/Settings/Common/` | `ColorPairMap` 已有 `chatNameColorHex↔chatNameColorDarkHex`、`momentsNameColorHex↔momentsNameColorDarkHex` |
| `TintHook.h` | `MioPlugin/Modules/Layout/` | 已有 `+install` 声明 |

### 1.2 唯一缺失部分

| 文件 | 路径 | 状态 |
|------|------|------|
| `TintHook.m` | `MioPlugin/Modules/Layout/TintHook.m` | ❌ 仅有空实现，**需要替换为正确的昵称颜色 Hook 代码** |

---

## 二、核心概念：旧方案 vs 新方案

### 旧方案（错误，已废弃）

来自旧 `实现文档.md`，实际是**消息时间颜色**：

```
Hook 点:    SystemMessageCellView / TextMessageCellView / CommonMessageCellView / BaseMessageCellView
Hook 方法:  updateStatus / layoutContentView
修改目标:   RichTextView 的 NSMutableAttributedString → NSForegroundColorAttributeName
适用范围:   ⚠️ 仅处理撤回消息（containsString:@"撤回"）
颜色来源:   [config nameColor] → nameColorHex/darkNameColorHex（与聊天昵称颜色无关）
```

**问题**：修改的是撤回消息的富文本显示颜色，不是联系人昵称本身的颜色。

### 新方案（正确）

来自 `实现分析文档.md`，真正的**昵称颜色**：

```
Hook 点:     CommonMessageCellView              WCTimeLineCellView
Hook 方法:   initChatRoomNameLabel               initNickLabel
修改目标:    m_chatRoomNameLabel.textColor        nickLabel.textColor
            m_chatRoomNameLabel.font             nickLabel.font
适用范围:    所有聊天消息的发送者昵称              所有朋友圈动态的发布者昵称
颜色来源:    chatNameColorHex/darkHex             momentsNameColorHex/darkHex
            (由 enableChatNameColor 开关控制)      (由 enableMomentsNameColor 开关控制)
```

**关键差异**：
- 旧方案修改 `NSAttributedString` → 新方案直接修改 `UILabel.textColor`
- 旧方案只处理撤回消息 → 新方案覆盖所有聊天消息和朋友圈的昵称
- 旧方案使用 `nameColorHex`（全局）→ 新方案使用 `chatNameColorHex`/`momentsNameColorHex`（独立配置）
- 旧方案不支持字体大小 → 新方案支持字号自定义

---

## 三、唯一实现步骤：重写 TintHook.m

### 操作文件

`/www/wwwroot/ios/MioPlugin/Modules/Layout/TintHook.m`

### 说明

只需将 `TintHook.m` 的当前空实现替换为完整实现。涉及的其他文件均已就绪。

### 当前内容

```objc
@implementation TintHook
+ (void)install {
    WPLog(@"Tint", @"TintHook install SKIPPED — 已删除所有 Hook 实现");
}
@end
```

### 替换为完整实现（见第四章）

### 核心逻辑详解

#### 3.1 install 入口 — 注册两个 Hook

```objc
+ (void)install {
    // Hook 1: 聊天消息 Cell 的昵称标签初始化
    Class commonMsgCell = objc_getClass("CommonMessageCellView");
    MSHookMessageEx(commonMsgCell,
                    @selector(initChatRoomNameLabel),
                    (IMP)hook_InitChatRoomNameLabel,
                    (IMP *)&orig_InitChatRoomNameLabel);

    // Hook 2: 朋友圈 Cell 的昵称标签初始化
    Class timeLineCell = objc_getClass("WCTimeLineCellView");
    MSHookMessageEx(timeLineCell,
                    @selector(initNickLabel),
                    (IMP)hook_InitNickLabel,
                    (IMP *)&orig_InitNickLabel);
}
```

#### 3.2 Hook: 聊天界面昵称 — hook_InitChatRoomNameLabel

**执行流程：**

```
CommonMessageCellView 初始化昵称标签
    │  微信调用 initChatRoomNameLabel
    ▼
hook_InitChatRoomNameLabel 拦截
    │
    ├─ 1. 调用原始方法创建 UILabel
    │     label = orig_InitChatRoomNameLabel(self, cmd)
    │
    ├─ 2. 通过 ivar 获取 m_chatRoomNameLabel
    │     Ivar ivar = class_getInstanceVariable(cls, "m_chatRoomNameLabel")
    │     UILabel *nameLabel = object_getIvar(self, ivar)
    │
    ├─ 3. 检查是否启用开关
    │     if ([PluginConfig shared].enableChatNameColor)
    │
    ├─ 4. 读取浅色/深色颜色 hex
    │     lightHex = config.chatNameColorHex  (默认 #808080)
    │     darkHex  = config.chatNameColorDarkHex  (默认 #BFBFBF)
    │
    ├─ 5. 创建动态 UIColor
    │     [UIColor colorWithDynamicProvider:^UIColor *(UITraitCollection *trait) {
    │         return trait.userInterfaceStyle == UIUserInterfaceStyleDark
    │             ? [config colorFromHex:darkHex]
    │             : [config colorFromHex:lightHex];
    │     }]
    │
    ├─ 6. 应用到 UILabel
    │     [nameLabel setTextColor:dynamicColor]
    │
    └─ 7. 如果字号开关启用，设置字号
          if (config.enableChatNameSize)
              nameLabel.font = [UIFont systemFontOfSize:config.chatNameFontSize]
```

#### 3.3 Hook: 朋友圈昵称 — hook_InitNickLabel

**执行流程：**

```
WCTimeLineCellView 初始化昵称标签
    │  微信调用 initNickLabel
    ▼
hook_InitNickLabel 拦截
    │
    ├─ 1. 调用原始方法创建 UILabel
    │     label = orig_InitNickLabel(self, cmd)
    │
    ├─ 2. 通过 valueForKey 获取 nickLabel
    │     UILabel *nickLabel = [self valueForKey:@"nickLabel"]
    │
    ├─ 3. 检查是否启用开关
    │     if ([PluginConfig shared].enableMomentsNameColor)
    │
    ├─ 4. 读取浅色/深色颜色 hex
    │     lightHex = config.momentsNameColorHex  (默认 #808080)
    │     darkHex  = config.momentsNameColorDarkHex  (默认 #BFBFBF)
    │
    ├─ 5. 创建动态 UIColor
    │     [UIColor colorWithDynamicProvider:^UIColor *(UITraitCollection *trait) {
    │         return trait.userInterfaceStyle == UIUserInterfaceStyleDark
    │             ? [config colorFromHex:darkHex]
    │             : [config colorFromHex:lightHex];
    │     }]
    │
    ├─ 6. 应用到 UILabel
    │     [nickLabel setTextColor:dynamicColor]
    │
    └─ 7. 如果字号开关启用，设置字号
          if (config.enableMomentsNameSize)
              nickLabel.font = [UIFont systemFontOfSize:config.momentsNameFontSize]
```

#### 3.4 动态颜色机制（核心亮点）

使用 `[UIColor colorWithDynamicProvider:]` 创建动态颜色：

```objc
UIColor *dynamicColor = [UIColor colorWithDynamicProvider:^UIColor *(UITraitCollection *trait) {
    if (trait.userInterfaceStyle == UIUserInterfaceStyleDark) {
        return [config colorFromHex:darkHex];    // 深色模式颜色
    }
    return [config colorFromHex:lightHex];       // 浅色模式颜色
}];
```

**优势**：
- 系统在浅色/深色模式切换时，UILabel 自动更新颜色
- 不需要监听通知、不需要额外刷新代码
- 零性能开销——只在颜色实际使用时才调用 provider block

#### 3.5 为什么使用 MSHookMessageEx？

项目中的其他 Hook（如 `ProfileCardBgHook`、`ListCornerRadiusHook`）统一使用 `MSHookMessageEx`，保持一致性。且 `initChatRoomNameLabel` 是微信的实例方法，`MSHookMessageEx` 是标准的 method swizzling 方式。

#### 3.6 为什么用 ivar 访问而非属性？

`CommonMessageCellView` 的 `m_chatRoomNameLabel` 是实例变量（ivar），微信可能未为其声明公开属性。通过 `class_getInstanceVariable` + `object_getIvar` 是最可靠的访问方式。

---

## 四、TintHook.m 完整代码

以下是 `/www/wwwroot/ios/MioPlugin/Modules/Layout/TintHook.m` 的完整实现：

```objc
#import "TintHook.h"
#import "../../Config/PluginConfig.h"
#import "../../Core/LogManager.h"
#import <objc/runtime.h>
#import <objc/message.h>
#import <substrate.h>

// ============================================================
// 聊天界面昵称颜色 Hook
// Hook CommonMessageCellView.initChatRoomNameLabel
// 修改 m_chatRoomNameLabel.textColor + font
// ============================================================
static IMP orig_InitChatRoomNameLabel = nil;

static UILabel *hook_InitChatRoomNameLabel(id self, SEL cmd) {
    // 第1步：调用原始方法，获取 UILabel
    UILabel *label = ((UILabel *(*)(id, SEL))orig_InitChatRoomNameLabel)(self, cmd);

    @try {
        // 第2步：通过 ivar 获取 m_chatRoomNameLabel
        Ivar ivar = class_getInstanceVariable(object_getClass(self), "m_chatRoomNameLabel");
        if (!ivar) return label;

        UILabel *nameLabel = object_getIvar(self, ivar);
        if (!nameLabel) return label;

        PluginConfig *config = [PluginConfig shared];

        // 第3步：检查聊天昵称颜色开关
        if (config.enableChatNameColor) {
            // 读取浅色/深色 hex 值
            NSString *lightHex = config.chatNameColorHex ?: @"#808080";
            NSString *darkHex = config.chatNameColorDarkHex ?: @"#BFBFBF";

            // 第4步：创建动态颜色（自动响应浅色/深色模式切换）
            UIColor *dynamicColor = [UIColor colorWithDynamicProvider:^UIColor *(UITraitCollection *trait) {
                if (trait.userInterfaceStyle == UIUserInterfaceStyleDark) {
                    return [config colorFromHex:darkHex]
                        ?: [UIColor colorWithRed:0.75 green:0.75 blue:0.75 alpha:1.0];
                }
                return [config colorFromHex:lightHex]
                    ?: [UIColor colorWithRed:0.5 green:0.5 blue:0.5 alpha:1.0];
            }];

            // 第5步：应用颜色
            [nameLabel setTextColor:dynamicColor];
        }

        // 第6步：聊天昵称字号设置
        if (config.enableChatNameSize) {
            CGFloat fontSize = config.chatNameFontSize;
            if (fontSize <= 0) fontSize = 16.0;
            nameLabel.font = [UIFont systemFontOfSize:fontSize];
        }

    } @catch (NSException *e) {
        WPLog(@"Tint", @"hook_InitChatRoomNameLabel 异常: %@", e.reason);
    }

    return label;
}

// ============================================================
// 朋友圈昵称颜色 Hook
// Hook WCTimeLineCellView.initNickLabel
// 修改 nickLabel.textColor + font
// ============================================================
static IMP orig_InitNickLabel = nil;

static UILabel *hook_InitNickLabel(id self, SEL cmd) {
    // 第1步：调用原始方法，获取 UILabel
    UILabel *label = ((UILabel *(*)(id, SEL))orig_InitNickLabel)(self, cmd);

    @try {
        // 第2步：通过 valueForKey 获取 nickLabel
        UILabel *nickLabel = nil;
        @try {
            nickLabel = [self valueForKey:@"nickLabel"];
        } @catch (NSException *e) {
            return label;
        }
        if (!nickLabel) return label;

        PluginConfig *config = [PluginConfig shared];

        // 第3步：检查朋友圈昵称颜色开关
        if (config.enableMomentsNameColor) {
            // 读取浅色/深色 hex 值
            NSString *lightHex = config.momentsNameColorHex ?: @"#808080";
            NSString *darkHex = config.momentsNameColorDarkHex ?: @"#BFBFBF";

            // 第4步：创建动态颜色（自动响应浅色/深色模式切换）
            UIColor *dynamicColor = [UIColor colorWithDynamicProvider:^UIColor *(UITraitCollection *trait) {
                if (trait.userInterfaceStyle == UIUserInterfaceStyleDark) {
                    return [config colorFromHex:darkHex]
                        ?: [UIColor colorWithRed:0.75 green:0.75 blue:0.75 alpha:1.0];
                }
                return [config colorFromHex:lightHex]
                    ?: [UIColor colorWithRed:0.5 green:0.5 blue:0.5 alpha:1.0];
            }];

            // 第5步：应用颜色
            [nickLabel setTextColor:dynamicColor];
        }

        // 第6步：朋友圈昵称字号设置
        if (config.enableMomentsNameSize) {
            CGFloat fontSize = config.momentsNameFontSize;
            if (fontSize <= 0) fontSize = 16.0;
            nickLabel.font = [UIFont systemFontOfSize:fontSize];
        }

    } @catch (NSException *e) {
        WPLog(@"Tint", @"hook_InitNickLabel 异常: %@", e.reason);
    }

    return label;
}

// ============================================================
// TintHook 主入口
// ============================================================
@implementation TintHook

+ (void)install {
    WPLog(@"Tint", @"TintHook install — 开始注册名字颜色 Hook");

    Class commonMessageCellView = objc_getClass("CommonMessageCellView");
    if (commonMessageCellView) {
        MSHookMessageEx(
            commonMessageCellView,
            @selector(initChatRoomNameLabel),
            (IMP)hook_InitChatRoomNameLabel,
            (IMP *)&orig_InitChatRoomNameLabel
        );
        WPLog(@"Tint", @"  ✓ Hook CommonMessageCellView.initChatRoomNameLabel");
    } else {
        WPLog(@"Tint", @"  ✗ CommonMessageCellView 不存在，跳过");
    }

    Class timeLineCellView = objc_getClass("WCTimeLineCellView");
    if (timeLineCellView) {
        MSHookMessageEx(
            timeLineCellView,
            @selector(initNickLabel),
            (IMP)hook_InitNickLabel,
            (IMP *)&orig_InitNickLabel
        );
        WPLog(@"Tint", @"  ✓ Hook WCTimeLineCellView.initNickLabel");
    } else {
        WPLog(@"Tint", @"  ✗ WCTimeLineCellView 不存在，跳过");
    }

    WPLog(@"Tint", @"TintHook install — 完成注册");
}

@end
```

### 代码要点说明

| 要点 | 说明 |
|------|------|
| `MSHookMessageEx` | 与项目其他 Hook 保持一致（ProfileCardBgHook、ListCornerRadiusHook 统一使用） |
| `class_getInstanceVariable` + `object_getIvar` | 获取 `m_chatRoomNameLabel` 实例变量，绕过可能的属性限制 |
| `valueForKey:` 获取 `nickLabel` | 朋友圈昵称标签可通过 KVC 安全访问 |
| `colorWithDynamicProvider:` | 创建动态颜色，系统在浅色/深色切换时自动更新 UILabel |
| 颜色 fallback | `[config colorFromHex:]` 返回 nil 时使用硬编码兜底色 |
| 开关控制 | `enableChatNameColor`/`enableMomentsNameColor` 控制颜色是否生效 |
| `@try/@catch` | 异常静默捕获，确保不干扰微信正常运行 |

### 数据流转图

```
用户操作设置页
    │
    ├─ 选择聊天昵称颜色 → chatNameColorHex/chatNameColorDarkHex
    ├─ 选择朋友圈昵称颜色 → momentsNameColorHex/momentsNameColorDarkHex
    ├─ 设置字体大小 → chatNameFontSize/momentsNameFontSize
    │
    ▼  保存到 NSUserDefaults (PluginConfig.save)

微信创建消息 Cell / 朋友圈 Cell
    │
    ├─ CommonMessageCellView.initChatRoomNameLabel (HOOK)
    │   └─ 读取 PluginConfig → enableChatNameColor?
    │       ├─ YES → colorWithDynamicProvider → [nameLabel setTextColor:]
    │       └─ enableChatNameSize? → nameLabel.font = ...
    │
    └─ WCTimeLineCellView.initNickLabel (HOOK)
        └─ 读取 PluginConfig → enableMomentsNameColor?
            ├─ YES → colorWithDynamicProvider → [nickLabel setTextColor:]
            └─ enableMomentsNameSize? → nickLabel.font = ...
```

---

## 五、验证清单

### 5.1 编译验证

- [ ] Xcode 编译无错误
- [ ] 无 `TintHook.m` 相关的 warning
- [ ] `substrate.h` 头文件引用正确

### 5.2 功能验证

| 测试场景 | 预期结果 | 操作步骤 |
|---------|---------|---------|
| 聊天昵称颜色 | 所有聊天消息的发送者昵称变为设置的颜色 | 设置页 → 修改聊天名字颜色 → 发消息/看聊天记录 |
| 朋友圈昵称颜色 | 朋友圈动态的发布者昵称变为设置的颜色 | 设置页 → 修改朋友圈名字颜色 → 刷新朋友圈 |
| 深色模式 | 切换到深色模式后昵称颜色变为对应的深色值 | 系统设置切换深色模式 → 观察聊天/朋友圈昵称 |
| 开关控制关闭 | 关闭开关后昵称恢复微信原生颜色 | 设置页关闭对应开关 → 观察昵称 |
| 字体大小 | 昵称字号变为设置的大小 | 设置页修改字体大小 → 观察昵称 |
| 重启保持 | 重启微信后设置保留 | 杀掉微信进程 → 重新打开 → 验证 |
| 浅色/深色切换 | 不重启微信直接切换模式，颜色自动变化 | 控制中心切换深色模式 → 观察昵称颜色自动更新 |

### 5.3 边界情况

| 场景 | 预期行为 |
|------|---------|
| `CommonMessageCellView` 不存在 | `objc_getClass` 返回 nil，跳过 Hook，不影响其他功能 |
| `WCTimeLineCellView` 不存在 | 同上了，跳过 Hook |
| `m_chatRoomNameLabel` ivar 不存在 | `class_getInstanceVariable` 返回 NULL，不修改 label |
| `nickLabel` 不存在 | `valueForKey:` 抛出异常被 `@try` 捕获，不修改 label |
| 颜色 hex 值为空 | `colorFromHex:` 返回 nil，fallback 到灰色的 `UIColor` |
| 字体大小为 0 | 使用默认值 16.0 |

---

## 六、文件变更清单

### 修改文件（仅1个）

| 文件 | 变更内容 | 行数 |
|------|---------|------|
| `MioPlugin/Modules/Layout/TintHook.m` | 空实现 → 完整昵称颜色 Hook（两个 Hook 点：聊天 + 朋友圈） | 约 140 行 |

### 无需修改的文件（均已就绪）

| 文件 | 路径 |
|------|------|
| `PluginConfig.h` | `MioPlugin/Config/PluginConfig.h` |
| `PluginConfig.m` | `MioPlugin/Config/PluginConfig.m` |
| `TintHook.h` | `MioPlugin/Modules/Layout/TintHook.h` |
| `SettingNameColorController.m` | `MioPlugin/Settings/Controllers/SettingNameColorController.m` |
| `WPUIVC.m` | `MioPlugin/Modules/SettingEntry/WPUIVC.m` |
| `FeatureModuleRegistry.m` | `MioPlugin/Registry/FeatureModuleRegistry.m` |
| `SettingCategoryController.m` | `MioPlugin/Settings/Common/SettingCategoryController.m` |