# 名字颜色功能 — 删除文档

> 本文档详细说明如何彻底删除"名字颜色"功能。
> 基于当前项目路径：`/www/wwwroot/ios/MioPlugin/`
> **注意：本文档只指导删除，不改代码。**

---

## 目录

1. [删除范围速览](#一删除范围速览)
2. [Step 1: 删除 4 个完整文件](#二step-1-删除-4-个完整文件)
3. [Step 2: 修改 PluginConfig.h](#三step-2-修改-plugconfigh)
4. [Step 3: 修改 PluginConfig.m](#四step-3-修改-plugconfigm)
5. [Step 4: 修改 Constants.h](#五step-4-修改-constantsh)
6. [Step 5: 修改 FeatureModuleRegistry.m](#六step-5-修改-featuremoduleregistrym)
7. [Step 6: 修改 WPUIVC.m（导航入口）](#七step-6-修改-wpuivcm导航入口)
8. [Step 7: 修改 SettingCategoryController.m（颜色配对映射）](#八step-7-修改-settingcategorycontrollerm颜色配对映射)
9. [Step 8: 修改 SettingController.m（架构面板）](#九step-8-修改-settingcontrollerm架构面板)
10. [附：完整 diff 对照](#十附完整-diff-对照)

---

## 一、删除范围速览

### 需删除的文件（4个）

| # | 文件路径 | 说明 |
|---|---------|------|
| 1 | `MioPlugin/Modules/Layout/TintHook.h` | Hook 头文件 |
| 2 | `MioPlugin/Modules/Layout/TintHook.m` | Hook 实现（含聊天 + 朋友圈昵称颜色代码） |
| 3 | `MioPlugin/Settings/Controllers/SettingNameColorController.h` | 设置页头文件 |
| 4 | `MioPlugin/Settings/Controllers/SettingNameColorController.m` | 设置页 UI（卡片布局、颜色选择器、字体输入） |

### 需修改的文件（6个）

| # | 文件路径 | 修改内容 |
|---|---------|---------|
| 5 | `MioPlugin/Config/PluginConfig.h` | 删除名字颜色相关属性声明和方法声明 |
| 6 | `MioPlugin/Config/PluginConfig.m` | 删除名字颜色相关初始化、持久化、颜色计算方法 |
| 7 | `MioPlugin/Config/Constants.h` | 删除名字颜色默认常量 |
| 8 | `MioPlugin/Registry/FeatureModuleRegistry.m` | 删除 TintHook 的 import 和注册 |
| 9 | `MioPlugin/Modules/SettingEntry/WPUIVC.m` | 删除"名字颜色"菜单项和导航跳转 |
| 10 | `MioPlugin/Settings/Common/SettingCategoryController.m` | 删除 ColorPairMap 中名字颜色配对 |
| 11 | `MioPlugin/Settings/Controllers/SettingController.m` | 删除架构面板中的 TintHook 记录 |

---

## 二、Step 1: 删除 4 个完整文件

直接删除以下文件（无其他文件引用）：

### 文件1: TintHook.h

**路径：** `MioPlugin/Modules/Layout/TintHook.h`

**内容：**
```objc
#import <Foundation/Foundation.h>

@interface TintHook : NSObject
+ (void)install;
@end
```

**操作：** 直接删除文件。

### 文件2: TintHook.m

**路径：** `MioPlugin/Modules/Layout/TintHook.m`

**内容：** 约 157 行，包含：
- `hook_InitChatRoomNameLabel` — Hook 聊天昵称标签初始化
- `hook_InitNickLabel` — Hook 朋友圈昵称标签初始化
- `+install` — 注册两个 Hook 点

**操作：** 直接删除文件。

### 文件3: SettingNameColorController.h

**路径：** `MioPlugin/Settings/Controllers/SettingNameColorController.h`

**内容：**
```objc
#import "../Common/SettingCategoryController.h"

@interface SettingNameColorController : SettingCategoryController
@end
```

**操作：** 直接删除文件。

### 文件4: SettingNameColorController.m

**路径：** `MioPlugin/Settings/Controllers/SettingNameColorController.m`

**内容：** 约 163 行，包含：
- `keyForTag` / `fontSizeForKey` — 字体大小输入辅助函数
- `onNumericRowTap:` — 字体大小弹窗
- `buildUI` — 构建聊天 + 朋友圈两个设置卡片
- 使用 `chatNameColorHex`/`chatNameColorDarkHex`/`enableChatNameColor`/`chatNameFontSize`/`enableChatNameSize`/`momentsNameColorHex`/`momentsNameColorDarkHex`/`enableMomentsNameColor`/`momentsNameFontSize`/`enableMomentsNameSize`

**操作：** 直接删除文件。

---

## 三、Step 2: 修改 PluginConfig.h

**路径：** `MioPlugin/Config/PluginConfig.h`

### 2.1 删除 `customColorsEnabled` 属性

**删除行：**

```objc
@property (nonatomic, assign) BOOL customColorsEnabled;
```

（位于第 20 行附近）

### 2.2 删除名字颜色属性区块

**删除区域：** 第 111-138 行

```objc
#pragma mark - 名字颜色设置
@property (nonatomic, assign) BOOL enableChatNameColor;      // 启用聊天界面名称颜色
@property (nonatomic, assign) BOOL enableChatNameSize;       // 启用聊天界面名称大小
@property (nonatomic, assign) CGFloat chatNameFontSize;      // 聊天名称字体大小
@property (nonatomic, assign) BOOL enableMomentsNameColor;   // 启用朋友圈名称颜色
@property (nonatomic, assign) BOOL enableMomentsNameSize;    // 启用朋友圈名称大小
@property (nonatomic, assign) CGFloat momentsNameFontSize;   // 朋友圈名称字体大小
```

以及：

```objc
// 名字颜色设置（聊天界面昵称颜色，与消息时间颜色独立）
@property (nonatomic, copy) NSString *chatNameColorHex;
@property (nonatomic, copy) NSString *chatNameColorDarkHex;
@property (nonatomic, copy) NSString *momentsNameColorHex;
@property (nonatomic, copy) NSString *momentsNameColorDarkHex;
@property (nonatomic, copy) NSString *nameColorHex;
@property (nonatomic, copy) NSString *timeColorHex;
@property (nonatomic, copy) NSString *contentColorHex;
@property (nonatomic, copy) NSString *darkNameColorHex;
@property (nonatomic, copy) NSString *darkTimeColorHex;
@property (nonatomic, copy) NSString *darkContentColorHex;
```

### 2.3 删除方法声明

**删除行：** 第 213-215 行

```objc
- (UIColor *)nameColor;
- (UIColor *)timeColor;
- (UIColor *)contentColor;
```

### 2.4 删除后效果

删除后，`#pragma mark - 好友添加时间后缀` 区块直接跟在 `#pragma mark - 通知撤回者` 之后，代码紧凑不断裂。

---

## 四、Step 3: 修改 PluginConfig.m

**路径：** `MioPlugin/Config/PluginConfig.m`

### 3.1 init 方法 — 删除颜色初始化

**删除位置：** 第 21-26 行

```objc
_nameColorHex = [kDefaultNameColor copy];
_timeColorHex = [kDefaultTimeColor copy];
_contentColorHex = [kDefaultContentColor copy];
_darkNameColorHex = [kDefaultDarkNameColor copy];
_darkTimeColorHex = [kDefaultDarkTimeColor copy];
_darkContentColorHex = [kDefaultDarkContentColor copy];
```

### 3.2 loadDefaults — 删除读取

**删除以下内容：**

| 行号 | 代码 |
|------|------|
| 67 | `_customColorsEnabled = [d boolForKey:[kPluginPrefix stringByAppendingString:@"CustomColorsEnabled"]];` |
| 148-180 | 所有 `NameColorHex`、`TimeColorHex`、`ContentColorHex`、`DarkNameColorHex`、`DarkTimeColorHex`、`DarkContentColorHex`、`ChatNameColorHex`、`ChatNameColorDarkHex`、`MomentsNameColorHex`、`MomentsNameColorDarkHex` 的读取代码 |
| 289-296 | `enableChatNameColor`、`enableChatNameSize`、`chatNameFontSize`、`enableMomentsNameColor`、`enableMomentsNameSize`、`momentsNameFontSize` 的读取代码 |

具体要删除的代码段：

```objc
// 第 67 行
_customColorsEnabled = [d boolForKey:[kPluginPrefix stringByAppendingString:@"CustomColorsEnabled"]];

// 第 148-180 行
v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"NameColorHex"]];
if (v.length > 0) {
    _nameColorHex = [v copy];
}
v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"TimeColorHex"]];
if (v.length > 0) {
    _timeColorHex = [v copy];
}
v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"ContentColorHex"]];
if (v.length > 0) {
    _contentColorHex = [v copy];
}

v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"ChatNameColorHex"]];
_chatNameColorHex = v.length > 0 ? [v copy] : @"#808080";
v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"ChatNameColorDarkHex"]];
_chatNameColorDarkHex = v.length > 0 ? [v copy] : @"#BFBFBF";
v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"MomentsNameColorHex"]];
_momentsNameColorHex = v.length > 0 ? [v copy] : @"#808080";
v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"MomentsNameColorDarkHex"]];
_momentsNameColorDarkHex = v.length > 0 ? [v copy] : @"#BFBFBF";

v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"DarkNameColorHex"]];
if (v.length > 0) {
    _darkNameColorHex = [v copy];
}
v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"DarkTimeColorHex"]];
if (v.length > 0) {
    _darkTimeColorHex = [v copy];
}
v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"DarkContentColorHex"]];
if (v.length > 0) {
    _darkContentColorHex = [v copy];
}

// 第 289-296 行
_enableChatNameColor = [d boolForKey:[kPluginPrefix stringByAppendingString:@"EnableChatNameColor"]];
_enableChatNameSize = [d boolForKey:[kPluginPrefix stringByAppendingString:@"EnableChatNameSize"]];
_chatNameFontSize = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatNameFontSize"]];
if (_chatNameFontSize == 0) _chatNameFontSize = 16.0;
_enableMomentsNameColor = [d boolForKey:[kPluginPrefix stringByAppendingString:@"EnableMomentsNameColor"]];
_enableMomentsNameSize = [d boolForKey:[kPluginPrefix stringByAppendingString:@"EnableMomentsNameSize"]];
_momentsNameFontSize = [d floatForKey:[kPluginPrefix stringByAppendingString:@"MomentsNameFontSize"]];
if (_momentsNameFontSize == 0) _momentsNameFontSize = 16.0;
```

### 3.3 save — 删除写入

**删除以下内容：**

| 行号 | 代码 |
|------|------|
| 519 | `[d setBool:_customColorsEnabled forKey:...];` |
| 568-577 | 所有颜色 hex 的写入 |
| 623-628 | 所有开关和字体大小的写入 |

具体代码：

```objc
// 第 519 行
[d setBool:_customColorsEnabled forKey:[kPluginPrefix stringByAppendingString:@"CustomColorsEnabled"]];

// 第 568-577 行
[d setObject:_nameColorHex forKey:[kPluginPrefix stringByAppendingString:@"NameColorHex"]];
[d setObject:_timeColorHex forKey:[kPluginPrefix stringByAppendingString:@"TimeColorHex"]];
[d setObject:_contentColorHex forKey:[kPluginPrefix stringByAppendingString:@"ContentColorHex"]];
[d setObject:_darkNameColorHex forKey:[kPluginPrefix stringByAppendingString:@"DarkNameColorHex"]];
[d setObject:_darkTimeColorHex forKey:[kPluginPrefix stringByAppendingString:@"DarkTimeColorHex"]];
[d setObject:_darkContentColorHex forKey:[kPluginPrefix stringByAppendingString:@"DarkContentColorHex"]];
if (_chatNameColorHex) [d setObject:_chatNameColorHex forKey:[kPluginPrefix stringByAppendingString:@"ChatNameColorHex"]];
if (_chatNameColorDarkHex) [d setObject:_chatNameColorDarkHex forKey:[kPluginPrefix stringByAppendingString:@"ChatNameColorDarkHex"]];
if (_momentsNameColorHex) [d setObject:_momentsNameColorHex forKey:[kPluginPrefix stringByAppendingString:@"MomentsNameColorHex"]];
if (_momentsNameColorDarkHex) [d setObject:_momentsNameColorDarkHex forKey:[kPluginPrefix stringByAppendingString:@"MomentsNameColorDarkHex"]];

// 第 623-628 行
[d setBool:_enableChatNameColor forKey:[kPluginPrefix stringByAppendingString:@"EnableChatNameColor"]];
[d setBool:_enableChatNameSize forKey:[kPluginPrefix stringByAppendingString:@"EnableChatNameSize"]];
[d setFloat:_chatNameFontSize forKey:[kPluginPrefix stringByAppendingString:@"ChatNameFontSize"]];
[d setBool:_enableMomentsNameColor forKey:[kPluginPrefix stringByAppendingString:@"EnableMomentsNameColor"]];
[d setBool:_enableMomentsNameSize forKey:[kPluginPrefix stringByAppendingString:@"EnableMomentsNameSize"]];
[d setFloat:_momentsNameFontSize forKey:[kPluginPrefix stringByAppendingString:@"MomentsNameFontSize"]];
```

### 3.4 删除 nameColor / timeColor / contentColor 方法

**删除位置：** 第 805-827 行

```objc
- (UIColor *)nameColor {
    if (!_customColorsEnabled) {
        return [UIColor colorWithRed:250.0/255.0 green:81.0/255.0 blue:81.0/255.0 alpha:1.0];
    }
    return [self colorFromHex:[self isDarkMode] ? _darkNameColorHex : _nameColorHex]
        ?: [UIColor colorWithRed:250.0/255.0 green:81.0/255.0 blue:81.0/255.0 alpha:1.0];
}

- (UIColor *)timeColor {
    if (!_customColorsEnabled) {
        return [UIColor colorWithRed:250.0/255.0 green:81.0/255.0 blue:81.0/255.0 alpha:1.0];
    }
    return [self colorFromHex:[self isDarkMode] ? _darkTimeColorHex : _timeColorHex]
        ?: [UIColor colorWithRed:250.0/255.0 green:81.0/255.0 blue:81.0/255.0 alpha:1.0];
}

- (UIColor *)contentColor {
    if (!_customColorsEnabled) {
        return [UIColor colorWithRed:153.0/255.0 green:153.0/255.0 blue:153.0/255.0 alpha:1.0];
    }
    return [self colorFromHex:[self isDarkMode] ? _darkContentColorHex : _contentColorHex]
        ?: [UIColor colorWithRed:153.0/255.0 green:153.0/255.0 blue:153.0/255.0 alpha:1.0];
}
```

---

## 五、Step 4: 修改 Constants.h

**路径：** `MioPlugin/Config/Constants.h`

### 删除内容

删除第 20-25 行的颜色常量：

```objc
static NSString *const kDefaultNameColor = @"#FA5151";
static NSString *const kDefaultTimeColor = @"#FA5151";
static NSString *const kDefaultContentColor = @"#999999";
static NSString *const kDefaultDarkNameColor = @"#FF6B6B";
static NSString *const kDefaultDarkTimeColor = @"#FF6B6B";
static NSString *const kDefaultDarkContentColor = @"#BBBBBB";
```

### 删除后效果

`kDefaultRevokeTemplate` 之后直接到 `#endif`，文件干净。

---

## 六、Step 5: 修改 FeatureModuleRegistry.m

**路径：** `MioPlugin/Registry/FeatureModuleRegistry.m`

### 5.1 删除 import

**删除第 4 行：**

```objc
#import "../Modules/Layout/TintHook.h"
```

### 5.2 删除 TintHook 类引用

**删除第 73 行中 `TintHook class`：**

```objc
// 修改前：
hookInstallerClasses:@[[TintHook class], [UIPurifyHook class], [HideAvatarHook class], ...]
// 修改后：
hookInstallerClasses:@[[UIPurifyHook class], [HideAvatarHook class], ...]
```

---

## 七、Step 6: 修改 WPUIVC.m（导航入口）

**路径：** `MioPlugin/Modules/SettingEntry/WPUIVC.m`

### 6.1 删除 import

**删除第 4 行：**

```objc
#import "../../Settings/Controllers/SettingNameColorController.h"
```

### 6.2 删除菜单项

**删除第 33 行数组元素：**

```objc
// 修改前：
NSArray *items = @[
    @[@"聊天顶栏", @(100)],
    @[@"名字颜色", @(101)],   // ← 删除这一行
    @[@"附件布局", @(102)],
    ...

// 修改后：
NSArray *items = @[
    @[@"聊天顶栏", @(100)],
    @[@"附件布局", @(102)],
    ...
```

### 6.3 删除导航跳转

**删除第 69-71 行的 case:**

```objc
case 101:
    subVC = [[SettingNameColorController alloc] init];
    break;
```

删除后，`case 100` 的 break 之后直接接 `case 102`。

---

## 八、Step 7: 修改 SettingCategoryController.m（颜色配对映射）

**路径：** `MioPlugin/Settings/Common/SettingCategoryController.m`

### 删除 ColorPairMap 中名字颜色配对

**删除第 652-654 行：**

```objc
// SettingNameColorController
@"chatNameColorHex": @"chatNameColorDarkHex",
@"momentsNameColorHex": @"momentsNameColorDarkHex",
```

---

## 九、Step 8: 修改 SettingController.m（架构面板）

**路径：** `MioPlugin/Settings/Controllers/SettingController.m`

### 删除 TintHook 记录行

**删除第 114 行的 `@"TintHook", @"染色渲染",`：**

```objc
// 修改前：
NSArray *modules = @[
    @"RevokeHook", @"防撤回",
    @"TintHook", @"染色渲染",   // ← 删除这一行
    @"ClearUnreadHook", @"未读清零",
    ...

// 修改后：
NSArray *modules = @[
    @"RevokeHook", @"防撤回",
    @"ClearUnreadHook", @"未读清零",
    ...
```

---

## 十、附：完整 diff 对照

### 10.1 文件删除清单

| 操作 | 文件 | 行数 |
|------|------|------|
| 删除 | `MioPlugin/Modules/Layout/TintHook.h` | 5 行 |
| 删除 | `MioPlugin/Modules/Layout/TintHook.m` | 157 行 |
| 删除 | `MioPlugin/Settings/Controllers/SettingNameColorController.h` | 3 行 |
| 删除 | `MioPlugin/Settings/Controllers/SettingNameColorController.m` | 163 行 |

### 10.2 代码删除清单

| 文件 | 删除行数 | 删除位置 |
|------|---------|---------|
| `PluginConfig.h` | 约 30 行 | 属性声明 + 方法声明 |
| `PluginConfig.m` | 约 70 行 | init + loadDefaults + save + 3 个颜色方法 |
| `Constants.h` | 6 行 | 颜色常量 |
| `FeatureModuleRegistry.m` | 1 行 + 修改 1 行 | import 删除 + 数组元素删除 |
| `WPUIVC.m` | 4 行 | import 删除 + menu 项删除 + case 删除 |
| `SettingCategoryController.m` | 3 行 | ColorPairMap 配对删除 |
| `SettingController.m` | 2 行 | 数组元素删除 |

### 10.3 总计

| 类别 | 数量 |
|------|------|
| 删除文件 | 4 个 |
| 修改文件 | 6 个 |
| 删除代码行 | 约 440 行 |

### 10.4 NSUserDefaults 残留键（无需处理）

以下键在用户设备上会有残留数据，但由于删除后代码不再读取，**不会影响功能或报错**。如需彻底清理，可以在 `[PluginConfig save]` 中增加移除逻辑：

```
Mio_CustomColorsEnabled
Mio_NameColorHex
Mio_TimeColorHex
Mio_ContentColorHex
Mio_DarkNameColorHex
Mio_DarkTimeColorHex
Mio_DarkContentColorHex
Mio_ChatNameColorHex
Mio_ChatNameColorDarkHex
Mio_MomentsNameColorHex
Mio_MomentsNameColorDarkHex
Mio_EnableChatNameColor
Mio_EnableChatNameSize
Mio_ChatNameFontSize
Mio_EnableMomentsNameColor
Mio_EnableMomentsNameSize
Mio_MomentsNameFontSize
```

---

### 执行顺序

```
Step 1: 删除 4 个文件
  TintHook.h / TintHook.m / SettingNameColorController.h / SettingNameColorController.m

Step 2-4: 清理配置层
  PluginConfig.h → 删除属性 + 方法声明
  PluginConfig.m → 删除 init/load/save 代码 + 3 个颜色方法
  Constants.h    → 删除 6 个颜色常量

Step 5-9: 清理引用
  FeatureModuleRegistry.m     → 删除 import + 数组元素
  WPUIVC.m                   → 删除 import + menu + case
  SettingCategoryController.m → 删除配对映射
  SettingController.m         → 删除架构面板记录
```

**操作完成后，编译验证无错误即删除成功。**