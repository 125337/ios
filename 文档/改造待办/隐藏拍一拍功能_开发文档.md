# 隐藏拍一拍提示 — 开发文档

> 基于项目当前状态编写 | 专注拍一拍，无关功能不混入
> 核验日期：2026-06-11 | 代码已验证实现

---

## 一、功能描述

隐藏聊天界面中"xx拍了拍xx"的系统提示消息。拍一拍消息完全不显示，不留空白占位。

### 功能范围

| 项目 | 内容 |
|------|------|
| 功能 | 隐藏拍一拍提示 |
| 配置 Key | `hidePatHint` |
| NSUserDefaults Key | `WCP_UIPurify_hidePatHint` |
| 设置页面 | 界面净化 → 消息显示 → 隐藏拍一拍提示 |
| 当前状态 | **代码已实现** |

---

## 二、项目现况

### 2.1 已有组件一览

| 组件 | 文件 | 当前状态 |
|------|------|:--------:|
| 配置定义 | [UIPurifyConfig.h](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyConfig.h) | ✅ `@property (nonatomic, assign) BOOL hidePatHint;` |
| 配置持久化 | [UIPurifyConfig.m](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyConfig.m) | ✅ `[ConfigDescriptor boolItem:@"hidePatHint" default:@(NO)],` |
| UI 开关 | [WPUIPurifyVC.m](file:///www/wwwroot/ios/MioPlugin/Modules/SettingEntry/WPUIPurifyVC.m) | ✅ L32: `@[@"隐藏拍一拍提示", @"hidePatHint", @(config.hidePatHint)],` |
| Hook 注册 | [FeatureModuleRegistry.m](file:///www/wwwroot/ios/MioPlugin/Registry/FeatureModuleRegistry.m) | ✅ L70: `@[[UIPurifyHook class], ...]` |
| Hook 逻辑 | [UIPurifyHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyHook.m) | ✅ L127-L149: 拍一拍 Hook 已安装 |

### 2.2 配置层（UIPurifyConfig）

```objc
// UIPurifyConfig.h L8
@property (nonatomic, assign) BOOL hidePatHint;

// UIPurifyConfig.m L23
[ConfigDescriptor boolItem:@"hidePatHint" default:@(NO)],
```

- 继承 `ConfigModule` 协议，通过 `modulePrefix` 生成 Key：`UIPurify_hidePatHint`
- 最终持久化 Key：`WCP_UIPurify_hidePatHint`
- ConfigManager 统一加载/保存，不需要手动操作 NSUserDefaults

### 2.3 UI 层（WPUIPurifyVC）

```objc
// WPUIPurifyVC.m L32
@[@"隐藏拍一拍提示", @"hidePatHint", @(config.hidePatHint)],
```

- 位于"界面净化"页面的"消息显示"分组
- 使用 `addSwitchRowInGroup:title:desc:key:isOn:cy:width:` 工厂方法
- 切换开关时自动触发 `UIPurifyConfig` 的对应属性变更

### 2.4 Hook 层（UIPurifyHook）

`UIPurifyHook` 已注册到 `FeatureModuleRegistry` 的 `layout` 模块中，通过 `HookRegistry installAllHooks` 自动调用 `+install` 方法。

---

## 三、微信目标类

拍一拍消息在微信中的类层次：

```
BaseMessageCellView           ← 微信原生基类
  └── SystemMessageCellView   ← 系统消息 Cell（撤回提示归属此类）
        └── AppPatMessageCellView  ← ★ 拍一拍 Cell（目标）

BaseMessageViewModel          ← 微信原生 ViewModel 基类
  └── SystemMessageViewModel  ← 系统消息 ViewModel
        └── AppPatMessageViewModel ← ★ 拍一拍 ViewModel（目标）
```

### 目标类与 Hook 方法

| 方法 | 所属类 | 功能开启时行为 | 作用 |
|------|--------|--------------|------|
| `initWithViewModel:` | `AppPatMessageCellView` | setHidden:YES + setFrame: | 创建时立即隐藏，同步内部布局 |
| `layoutInternal` | `AppPatMessageCellView` | 直接 return（跳过） | 阻止布局计算 |
| `canBeReused` | `AppPatMessageCellView` | 返回 YES | 标记 Cell 可复用 |
| `shouldLayoutIfNeeded` | `AppPatMessageCellView` | 返回 NO | 告知 TableView 不需要重新布局 |
| `measure:` | `AppPatMessageViewModel` | 返回 CGSizeZero | 返回零高度，占位完全消失 |

---

## 四、核心技术点：purifySafeHook

### 4.1 为什么要用 purifySafeHook

`AppPatMessageCellView` 继承自 `SystemMessageCellView`。直接使用 `MSHookMessageEx` 时：

- 如果 `AppPatMessageCellView` **未覆写**目标方法（微信版本不同，覆写情况不同）
- `MSHookMessageEx` 会在继承链上查找，找到的是**父类的方法表**
- Hook 会打到父类（`SystemMessageCellView`）上，影响所有系统消息 Cell
- 如果将来再实现 `hideRevokeHint`（Hook `SystemMessageCellView`），就会交叉污染

### 4.2 purifySafeHook 实现

```objc
/// 检测子类是否覆写了该方法
static BOOL purifyIsOverridden(Class cls, SEL sel) {
    Class superCls = class_getSuperclass(cls);
    if (!superCls) return YES;
    IMP own   = class_getMethodImplementation(cls, sel);
    IMP parent = class_getMethodImplementation(superCls, sel);
    return (own != parent);
}

/// 安全 Hook：子类未覆写时先 class_addMethod 创建独立副本
static void purifySafeHook(Class cls, SEL sel, IMP replacement, IMP *orig) {
    if (purifyIsOverridden(cls, sel)) {
        // 子类已覆写 → 直接 MSHookMessageEx
        MSHookMessageEx(cls, sel, replacement, orig);
        return;
    }
    // 未覆写 → class_addMethod 创建独立方法副本 → 再 Hook
    Method parentMethod = class_getInstanceMethod(class_getSuperclass(cls), sel);
    IMP parentIMP = method_getImplementation(parentMethod);
    const char *types = method_getTypeEncoding(parentMethod);
    class_addMethod(cls, sel, parentIMP, types);
    MSHookMessageEx(cls, sel, replacement, orig);
}
```

**流程：**
1. `purifyIsOverridden` 检测子类是否覆写了目标方法
2. 已覆写 → 直接 `MSHookMessageEx`（子类方法表已有独立条目）
3. 未覆写 → `class_addMethod` 在子类方法表创建独立副本（指向父类原始 IMP）
   → 再 `MSHookMessageEx` Hook 这个副本

`_orig` 始终指向目标类**自身的原始实现**，不会指向其他类的 Hook。

### 4.3 当前使用情况

| 目标类 | 策略 | 5 个方法全部使用 `purifySafeHook` |
|--------|:----:|:-------------------------------:|
| `AppPatMessageCellView` | `purifySafeHook` | ✅ |
| `AppPatMessageViewModel` | `purifySafeHook` | ✅ |

---

## 五、Hook 实现结构

### 5.1 _orig 指针（5 个）

```objc
static IMP _orig_AppPatCell_initWithViewModel = NULL;
static IMP _orig_AppPatCell_layoutInternal = NULL;
static IMP _orig_AppPatCell_canBeReused = NULL;
static IMP _orig_AppPatCell_shouldLayoutIfNeeded = NULL;
static IMP _orig_AppPatVM_measure = NULL;
```

### 5.2 Hook 函数（5 个）

```objc
// ① initWithViewModel: — 创建时隐藏
static id hook_AppPatCell_initWithViewModel(id self, SEL _cmd, id viewModel) {
    id result = ((id (*)(id, SEL, id))_orig_AppPatCell_initWithViewModel)(self, _cmd, viewModel);
    if (result && [UIPurifyConfig shared].hidePatHint) {
        [result setHidden:YES];
        [result setFrame:[result frame]];  // setHidden 后同步内部布局状态
    }
    return result;
}

// ② layoutInternal — 跳过布局
static void hook_AppPatCell_layoutInternal(id self, SEL _cmd) {
    if ([UIPurifyConfig shared].hidePatHint) return;
    ((void (*)(id, SEL))_orig_AppPatCell_layoutInternal)(self, _cmd);
}

// ③ canBeReused — 可复用
static BOOL hook_AppPatCell_canBeReused(id self, SEL _cmd) {
    if ([UIPurifyConfig shared].hidePatHint) return YES;
    return ((BOOL (*)(id, SEL))_orig_AppPatCell_canBeReused)(self, _cmd);
}

// ④ shouldLayoutIfNeeded — 不需要布局
static BOOL hook_AppPatCell_shouldLayoutIfNeeded(id self, SEL _cmd) {
    if ([UIPurifyConfig shared].hidePatHint) return NO;
    return ((BOOL (*)(id, SEL))_orig_AppPatCell_shouldLayoutIfNeeded)(self, _cmd);
}

// ⑤ measure: — 返回零高度
static CGSize hook_AppPatVM_measure(id self, SEL _cmd, CGSize size) {
    if ([UIPurifyConfig shared].hidePatHint) return CGSizeZero;
    return ((CGSize (*)(id, SEL, CGSize))_orig_AppPatVM_measure)(self, _cmd, size);
}
```

### 5.3 Install 逻辑

```objc
+ (void)install {
    // ... 已有 hideSeparatorLine 的 Hook ...

    // ── 拍一拍 ──
    Class appPatCellClass = objc_getClass("AppPatMessageCellView");
    Class appPatVMClass = objc_getClass("AppPatMessageViewModel");
    if (appPatCellClass && appPatVMClass) {
        purifySafeHook(appPatCellClass, @selector(initWithViewModel:),
                       (IMP)hook_AppPatCell_initWithViewModel,
                       &_orig_AppPatCell_initWithViewModel);
        purifySafeHook(appPatCellClass, @selector(layoutInternal),
                       (IMP)hook_AppPatCell_layoutInternal,
                       &_orig_AppPatCell_layoutInternal);
        purifySafeHook(appPatCellClass, @selector(canBeReused),
                       (IMP)hook_AppPatCell_canBeReused,
                       &_orig_AppPatCell_canBeReused);
        purifySafeHook(appPatCellClass, @selector(shouldLayoutIfNeeded),
                       (IMP)hook_AppPatCell_shouldLayoutIfNeeded,
                       &_orig_AppPatCell_shouldLayoutIfNeeded);
        purifySafeHook(appPatVMClass, @selector(measure:),
                       (IMP)hook_AppPatVM_measure,
                       &_orig_AppPatVM_measure);
        WPLog(@"UIPurify", @"[+] AppPatMessageCellView/ViewModel hooked (5 methods, purifySafeHook)");
    } else {
        WPLog(@"UIPurify", @"[-] AppPatMessageCellView/ViewModel not found");
    }
}
```

---

## 六、独立性分析

### 6.1 项目中无冲突代码

| 扫描维度 | 结果 |
|---------|:----:|
| 其他模块 Hook `AppPatMessageCellView`？ | ❌ 不存在 |
| 其他模块 Hook `AppPatMessageViewModel`？ | ❌ 不存在 |
| 其他模块 Hook `SystemMessageCellView`？ | ❌ 不存在 |
| 其他模块 Hook `setFrame:`？ | ❌ 不存在 |
| AppPat 的 `layoutSubviews` 被其他模块 Hook？ | ❌ 不存在 |

### 6.2 与防撤回（RevokeHandler）的关系

| 模块 | Hook 目标 | 作用层级 | 功能 |
|------|----------|:--------:|------|
| RevokeHandler | `CMessageMgr.onNewSyncNotAddDBMessage:` | 消息接收层 | 阻止消息被撤回 |
| 拍一拍隐藏 | `AppPatMessageCellView` + `AppPatMessageViewModel` | UI 渲染层 | 隐藏拍一拍消息 |

**完全正交，互不干扰。** 一个管消息要不要被撤回，一个管已存在的消息显不显示。

### 6.3 与 hideSeparatorLine 的关系

两个功能在同一文件（`UIPurifyHook.m`）由同一个 `+install` 安装，但：
- Hook 的类完全不同（`UITableView`/`WCColor` vs `AppPatMessageCellView`）
- 配置独立（`hideSeparatorLine` vs `hidePatHint`）
- **功能之间完全独立，互不影响**

---

## 七、验证方法

| 验证项 | 操作 | 预期结果 |
|--------|------|---------|
| 开关生效 | 开启 → 发一条拍一拍 | 拍一拍不显示，无空白占位 |
| 开关动态切换 | 开启→发拍一拍→关闭→再发拍一拍 | 开启时不显示，关闭后正常显示 |
| 不影响其他消息 | 开启拍一拍隐藏，不发拍一拍 | 所有其他消息正常显示 |
| 不影响其他净化功能 | 只开启拍一拍隐藏 | 分隔线、撤回提示等不受影响 |
| 快速开关+滑动 | 快速开关 + 快速滑动聊天列表 | 无 crash，无布局错乱 |

---

## 八、实现记录

| 步骤 | 内容 | 文件 | 行数 |
|:----:|------|------|:----:|
| 1 | 新增 `purifySafeHook` + `purifyIsOverridden` 工具函数 | `UIPurifyHook.m` | 23 行 |
| 2 | 新增 5 个 `_orig` 指针 | `UIPurifyHook.m` | 5 行 |
| 3 | 新增 5 个拍一拍 Hook 函数 | `UIPurifyHook.m` | 33 行 |
| 4 | 在 `+install` 新增拍一拍 Hook 安装逻辑 | `UIPurifyHook.m` | 23 行 |
| **合计** | **全部在 `UIPurifyHook.m` 一个文件内** | — | **~85 行** |

---

## 九、参考

- [UIPurifyHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyHook.m) — 完整实现
- [UIPurifyConfig.h](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyConfig.h) — 配置定义
- [UIPurifyConfig.m](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyConfig.m) — 配置持久化
- [WPUIPurifyVC.m](file:///www/wwwroot/ios/MioPlugin/Modules/SettingEntry/WPUIPurifyVC.m) — UI 开关
- [FeatureModuleRegistry.m](file:///www/wwwroot/ios/MioPlugin/Registry/FeatureModuleRegistry.m) — Hook 注册