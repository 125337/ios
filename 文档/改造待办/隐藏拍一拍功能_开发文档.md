# 隐藏拍一拍提示 — 功能开发文档

> 基于项目当前代码状态编写 | 核验日期：2026-06-11
> **不修改代码，仅作为开发指导**

---

## 一、功能概述

隐藏聊天界面中的"拍一拍"系统提示消息（如"xx拍了拍xx"），让拍一拍消息完全不显示，不留空白占位。

### 关联功能

| 功能 | 配置 Key | 当前状态 |
|------|----------|:--------:|
| 隐藏拍一拍提示 | `hidePatHint` | **未实现 Hook**（仅有配置+UI） |
| 隐藏撤回提示 | `hideRevokeHint` | **未实现 Hook**（仅有配置+UI） |

> 本文档专注于**隐藏拍一拍提示**的实现。隐藏撤回提示可参照类似方案实现。

---

## 二、当前项目现况

### 2.1 已有代码（无需重复创建）

**配置层：** [UIPurifyConfig.h](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyConfig.h) / [UIPurifyConfig.m](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyConfig.m)

```objc
// UIPurifyConfig.h L8
@property (nonatomic, assign) BOOL hidePatHint;
```

```objc
// UIPurifyConfig.m L23 — 已通过 ConfigModule 注册
[ConfigDescriptor boolItem:@"hidePatHint" default:@(NO)],
```

- 模块前缀：`UIPurify_` → 持久化 Key 为 `WCP_UIPurify_hidePatHint`
- 已通过 `ConfigManager` 统一加载/保存

**UI 层：** [WPUIPurifyVC.m](file:///www/wwwroot/ios/MioPlugin/Modules/SettingEntry/WPUIPurifyVC.m)

```objc
// WPUIPurifyVC.m L32 — 已有开关
@[@"隐藏拍一拍提示", @"hidePatHint", @(config.hidePatHint)],
```

- 开关位于"界面净化"页面的"消息显示"分组
- 使用统一的 `addSwitchRowInGroup:title:desc:key:isOn:cy:width:` 工厂方法

**Hook 注册：** [FeatureModuleRegistry.m](file:///www/wwwroot/ios/MioPlugin/Registry/FeatureModuleRegistry.m)

```objc
// L72 — UIPurifyHook 已注册到 HookRegistry
hookInstallerClasses:@[[UIPurifyHook class], ...]
```

- `+[UIPurifyHook install]` 在 `HookRegistry installAllHooks` 时自动调用
- 当前 `install` 方法（[UIPurifyHook.m L36-61](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyHook.m#L36-L61)）只实现了 `hideSeparatorLine` 的 Hook

### 2.2 需要补充的内容

| 组件 | 当前状态 | 需补充 |
|------|:--------:|--------|
| `UIPurifyHook.m` 中的拍一拍 Hook | ❌ 未实现 | 新增 5 个 C 函数 Hook + install 逻辑 |
| `UIPurifyHook.m` 中的 purifySafeHook 工具函数 | ❌ 未实现 | 新增 `purifyIsOverridden` + `purifySafeHook` |

---

## 三、微信目标类分析

拍一拍消息在微信中的 Cell/ViewModel 类层次：

```
BaseMessageCellView                    ← 微信原生基类
  └── SystemMessageCellView            ← 系统消息 Cell（撤回提示归属此类）
        └── AppPatMessageCellView      ← 拍一拍 Cell

BaseMessageViewModel                   ← 微信原生 ViewModel 基类
  └── SystemMessageViewModel           ← 系统消息 ViewModel
        └── AppPatMessageViewModel     ← 拍一拍 ViewModel
```

需要 Hook 的目标类及方法（每个类 5 个方法）：

| 方法 | 所属类 | 功能开启时的行为 | 作用 |
|------|--------|----------------|------|
| `initWithViewModel:` | `AppPatMessageCellView` | setHidden:YES + setFrame: | 创建时立即隐藏，触发父类布局 |
| `layoutInternal` | `AppPatMessageCellView` | 直接 return（跳过） | 阻止布局计算 |
| `canBeReused` | `AppPatMessageCellView` | 返回 YES | 标记 Cell 可复用 |
| `shouldLayoutIfNeeded` | `AppPatMessageCellView` | 返回 NO | 告知 TableView 不需要重新布局 |
| `measure:` | `AppPatMessageViewModel` | 返回 CGSizeZero | 返回零高度，占位完全消失 |

---

## 四、核心难题：父/子类 Hook 交叉污染

### 4.1 问题本质

当对父子类同时 Hook 同一方法时（如 `SystemMessageCellView` 和 `AppPatMessageCellView` 都 Hook `initWithViewModel:`），子类的 `_orig` 可能指向父类的 Hook（而非原始 IMP），形成：

```
AppPat_hook → SystemMessage_hook → 原始 IMP  ← ❌ 坏链
AppPat_hook → AppPat_原始 IMP                ← ✅ 好链
```

### 4.2 触发条件

`MSHookMessageEx(cls, sel, hook, &orig)` 的内部查找逻辑：
1. `class_getInstanceMethod(cls, sel)` 在当前类方法表中查找
2. 如果**未找到**（子类未覆写该方法，继承自父类）→ 沿继承链向上遍历
3. 如果父类已被 Hook → 返回父类的 Hook IMP
4. `_orig` 被赋值为父类的 Hook → 交叉污染

### 4.3 解决方案：`purifySafeHook`

核心思路：在 Hook 前确保子类方法表中有**独立的方法副本**，避免 `_orig` 指向父类 Hook。

```objc
/// 检测子类是否覆写了该方法
static BOOL purifyIsOverridden(Class cls, SEL sel) {
    Class superCls = class_getSuperclass(cls);
    if (!superCls) return YES;
    IMP own   = class_getMethodImplementation(cls, sel);
    IMP parent = class_getMethodImplementation(superCls, sel);
    return (own != parent);
}

/// 安全 Hook：子类未覆写时先 class_addMethod 创建独立副本
static void purifySafeHook(Class cls, SEL sel, IMP replacement, IMP *orig) {
    if (purifyIsOverridden(cls, sel)) {
        // 子类覆写了 → 直接 MSHookMessageEx
        MSHookMessageEx(cls, sel, replacement, orig);
        return;
    }
    // 子类未覆写 → class_addMethod 创建独立副本 → 再 Hook
    Method parentMethod = class_getInstanceMethod(class_getSuperclass(cls), sel);
    IMP parentIMP = method_getImplementation(parentMethod);
    const char *types = method_getTypeEncoding(parentMethod);
    class_addMethod(cls, sel, parentIMP, types);
    MSHookMessageEx(cls, sel, replacement, orig);
}
```

**关键原理：** `class_addMethod` 在子类方法表中创建独立条目（指向父类原始 IMP），`MSHookMessageEx` 只修改这个本地副本，父类方法表完全不受影响。`_orig` 永远指向父类原始 IMP，不会指向父类的 Hook。

### 4.4 适用范围

| 目标类 | 策略 | 原因 |
|--------|:----:|------|
| `AppPatMessageCellView` | **purifySafeHook** | 继承自 SystemMessageCellView，需要隔离 |
| `AppPatMessageViewModel` | **purifySafeHook** | 继承自 SystemMessageViewModel，需要隔离 |

---

## 五、Hook 实现方案

### 5.1 文件修改范围

| 文件 | 操作 | 说明 |
|------|:----:|------|
| [UIPurifyHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyHook.m) | **修改** | 新增 hook 函数 + `purifySafeHook` 工具 + install 中添加 Hook |

无需修改的文件：
- `UIPurifyConfig.h/m` — 配置已就绪
- `WPUIPurifyVC.m` — UI 已就绪
- `FeatureModuleRegistry.m` — 注册已就绪
- `UIPurifyHook.h` — 公开接口不变（仍只有 `+install`）

### 5.2 在 `UIPurifyHook.m` 中新增的内容

#### 5.2.1 `purifySafeHook` 工具函数

在 `@implementation UIPurifyHook` 上方（或内部）新增两个静态函数：

```objc
#pragma mark - purifySafeHook 工具

/// 检测子类是否覆写了该方法
static BOOL purifyIsOverridden(Class cls, SEL sel) {
    Class superCls = class_getSuperclass(cls);
    if (!superCls) return YES;
    IMP own   = class_getMethodImplementation(cls, sel);
    IMP parent = class_getMethodImplementation(superCls, sel);
    return (own != parent);
}

/// 安全 Hook 函数：避免父子类 Hook 交叉污染
static void purifySafeHook(Class cls, SEL sel, IMP replacement, IMP *orig) {
    if (purifyIsOverridden(cls, sel)) {
        MSHookMessageEx(cls, sel, replacement, orig);
        return;
    }
    Method parentMethod = class_getInstanceMethod(class_getSuperclass(cls), sel);
    IMP parentIMP = method_getImplementation(parentMethod);
    const char *types = method_getTypeEncoding(parentMethod);
    class_addMethod(cls, sel, parentIMP, types);
    MSHookMessageEx(cls, sel, replacement, orig);
}
```

#### 5.2.2 拍一拍 Hook 函数（5 个）

```objc
#pragma mark - 拍一拍 Hook 函数

/// initWithViewModel: — 创建时隐藏
static id hook_AppPatCell_initWithViewModel(id self, SEL _cmd, id viewModel) {
    id result = ((id (*)(id, SEL, id))_orig_AppPatCell_initWithViewModel)(self, _cmd, viewModel);
    if (result && [UIPurifyConfig shared].hidePatHint) {
        [result setHidden:YES];
        [result setFrame:[result frame]];  // 触发 setNeedsLayout
    }
    return result;
}

/// layoutInternal — 跳过布局
static void hook_AppPatCell_layoutInternal(id self, SEL _cmd) {
    if ([UIPurifyConfig shared].hidePatHint) return;
    ((void (*)(id, SEL))_orig_AppPatCell_layoutInternal)(self, _cmd);
}

/// canBeReused — 可复用
static BOOL hook_AppPatCell_canBeReused(id self, SEL _cmd) {
    if ([UIPurifyConfig shared].hidePatHint) return YES;
    return ((BOOL (*)(id, SEL))_orig_AppPatCell_canBeReused)(self, _cmd);
}

/// shouldLayoutIfNeeded — 不需要布局
static BOOL hook_AppPatCell_shouldLayoutIfNeeded(id self, SEL _cmd) {
    if ([UIPurifyConfig shared].hidePatHint) return NO;
    return ((BOOL (*)(id, SEL))_orig_AppPatCell_shouldLayoutIfNeeded)(self, _cmd);
}

/// measure: — 返回零高度
static CGSize hook_AppPatVM_measure(id self, SEL _cmd, CGSize size) {
    if ([UIPurifyConfig shared].hidePatHint) return CGSizeZero;
    return ((CGSize (*)(id, SEL, CGSize))_orig_AppPatVM_measure)(self, _cmd, size);
}
```

#### 5.2.3 对应的 `_orig` 指针声明

```objc
// ★ 拍一拍 _orig 指针
static IMP _orig_AppPatCell_initWithViewModel = NULL;
static IMP _orig_AppPatCell_layoutInternal = NULL;
static IMP _orig_AppPatCell_canBeReused = NULL;
static IMP _orig_AppPatCell_shouldLayoutIfNeeded = NULL;
static IMP _orig_AppPatVM_measure = NULL;
```

#### 5.2.4 在 `+install` 中安装拍一拍 Hook

```objc
// ── 拍一拍 ──
Class appPatCellClass = objc_getClass("AppPatMessageCellView");
Class appPatVMClass = objc_getClass("AppPatMessageViewModel");
if (appPatCellClass && appPatVMClass) {
    purifySafeHook(appPatCellClass, @selector(initWithViewModel:),
                   (IMP)hook_AppPatCell_initWithViewModel,
                   &_orig_AppPatCell_initWithViewModel);
    purifySafeHook(appPatCellClass, @selector(layoutInternal),
                   (IMP)hook_AppPatCell_layoutInternal,
                   &_orig_AppPatCell_layoutInternal);
    purifySafeHook(appPatCellClass, @selector(canBeReused),
                   (IMP)hook_AppPatCell_canBeReused,
                   &_orig_AppPatCell_canBeReused);
    purifySafeHook(appPatCellClass, @selector(shouldLayoutIfNeeded),
                   (IMP)hook_AppPatCell_shouldLayoutIfNeeded,
                   &_orig_AppPatCell_shouldLayoutIfNeeded);
    purifySafeHook(appPatVMClass, @selector(measure:),
                   (IMP)hook_AppPatVM_measure,
                   &_orig_AppPatVM_measure);
    WPLog(@"UIPurify", @"[+] AppPatMessageCellView/ViewModel hooked (5 methods, purifySafeHook)");
} else {
    WPLog(@"UIPurify", @"[-] AppPatMessageCellView/ViewModel not found");
}
```

---

## 六、独立性影响分析（代码级交叉核验）

> 本节对拍一拍 Hook 方案做全项目范围的冲突扫描，验证其是否足够独立。

### 6.1 冲突扫描结果总览

对整个 `MioPlugin/` 项目进行了以下维度的扫描：

| 扫描维度 | 扫描方法 | 结果 |
|---------|---------|:----:|
| 有无人 Hook `AppPatMessageCellView` | 搜索 `objc_getClass("AppPatMessageCellView")` 或类名字符串 | ✅ **无冲突** — 该 WeChat 类当前无人 Hook |
| 有无人 Hook `AppPatMessageViewModel` | 同上 | ✅ **无冲突** — 该 WeChat 类当前无人 Hook |
| 有无人 Hook `SystemMessageCellView` | 搜索 | ✅ **无冲突** — 该 WeChat 类当前无人 Hook |
| 有无人 Hook `SystemMessageViewModel` | 搜索 | ✅ **无冲突** — 该 WeChat 类当前无人 Hook |
| 有无人 Hook `BaseMessageCellView` | 搜索 | ✅ **无冲突** |
| 有无人 Hook `setFrame:` | 搜索 `MSHookMessageEx` + `setFrame` | ✅ **无冲突** — 项目中未 Hook 任何类的 `setFrame:` |
| 有无人 Hook `AppPatMessageCellView.layoutSubviews` | 搜索所有 layoutSubviews hook | ✅ **无冲突** — 只有其他不相关的类被 Hook |
| `purifySafeHook` 的 `class_getSuperclass` 安全 | 分析 | ✅ **安全** — 如 superclass 为 nil，`purifyIsOverridden` 返回 YES，走 MSHookMessageEx 路径 |
| `objc_getClass` 返回 nil 时的处理 | 分析 | ✅ **安全** — install 代码有 `if (cls && cls2)` 保护，不存在则跳过 |

### 6.2 当前项目中所有 Hook 清单（确认无重叠）

| 模块 | 被 Hook 的类 | Hook 的方法 | 与拍一拍 Hook 的重叠风险 |
|------|-------------|-------------|:----------------------:|
| **UIPurifyHook**（已有） | `UITableView` | `separatorColor`, `separatorStyle` | 🟢 完全无关 |
| **UIPurifyHook**（已有） | `WCColor`（元类） | `seperatorColor`（类方法） | 🟢 完全无关 |
| **ListCornerRadiusHook** | `MMTableViewCell` | `layoutSubviews` | 🟢 不同类 |
| **ListCornerRadiusHook** | `MFWebMMBtn`, `MFBannerBtn`, `FoldView` | `layoutSubviews` | 🟢 不同类 |
| **ListCornerRadiusHook** | `UIView`, `WCSearchBar` | `layoutSubviews` | 🟢 不同类 |
| **ListCornerRadiusHook** | `SelectAttachmentView` | `layoutSubviews` | 🟢 不同类 |
| **ProfileCardBgHook** | `MMUIButton` | `layoutSubviews` | 🟢 不同类 |
| **PlaceholderTextHook** | `MMGrowTextView` | `layoutSubviews` | 🟢 不同类 |
| **MessageTimeHook** | `ChatTimeCellView` | `layoutSubviews` | 🟢 不同类 |
| **UIAttachLayoutHook** | `SelectAttachmentView` | `layoutSubviews` | 🟢 不同类 |
| **ChatTopBarHook** | `UIViewController`, `UINavigationController` | `viewDidLoad`, `pushViewController:` | 🟢 完全无关 |
| **RevokeHook** | `CMessageMgr` | `onNewSyncNotAddDBMessage:` | 🟢 完全无关 |
| 拍一拍（新增） | **`AppPatMessageCellView`** | 5 个方法 | 🟢 **独占** |

> 结论：项目中没有其他代码与拍一拍的目标类或方法重叠，**新增 Hook 完全独立**。

### 6.3 `setFrame:` 递归风险分析

拍一拍 Hook 中的 `[result setFrame:[result frame]]` 是来自微信优化反编译的标准模式，用于隐藏后强制同步内部布局状态。

递归风险排查：

| 风险来源 | 分析 | 结论 |
|---------|------|:----:|
| 其他模块 Hook 了 `setFrame:` | **无** — 项目内没有任何模块 Hook `setFrame:` | 🟢 无风险 |
| `setFrame:` 触发 `layoutSubviews` 后被其他 Hook 拦截 | `layoutSubviews` 的 Hook 目标类列表（见 6.2）中不包含 `AppPatMessageCellView` 或其父类 | 🟢 无风险 |
| `setFrame:` 设置相同 frame 触发递归 | UIKit 在 frame 值未变化时不触发 `layoutSubviews`，`[result frame]` → `setFrame` 设置相同值 | 🟢 无风险 |
| WeChat 内部重写 `setFrame:` 导致循环 | Hook 的是 `initWithViewModel:`，不是 `setFrame:`，WeChat 内部 `setFrame:` 最多触发 `layoutSubviews` | 🟢 低风险 |

### 6.4 与 RevokeHandler（防撤回）的交互验证

| 维度 | RevokeHandler（防撤回） | hidePatHint（隐藏拍一拍） |
|------|----------------------|------------------------|
| **Hook 目标** | `CMessageMgr.onNewSyncNotAddDBMessage:` | `AppPatMessageCellView` × 4 + `AppPatMessageViewModel` × 1 |
| **作用层级** | 消息接收层（阻止撤回指令执行） | UI 渲染层（隐藏已创建的消息 Cell） |
| **Hook 方法** | 消息处理 | Cell 生命周期 + ViewModel 布局 |
| **数据流** | 输入→不被删除 | 输出→不显示 |

```
消息流：
  [微信服务器] → RevokeHook (拦截撤回指令) → AppPatMessageCellView → hidePatHint (隐藏)
                      ↑ 防撤回                   ↑ 拍一拍隐藏
                     完全不同的 Hook 目标         完全不同的 Hook 目标
```

**结论：完全正交，互不影响。**

### 6.5 微信版本兼容性风险

| 风险 | 等级 | 说明 |
|------|:----:|------|
| `AppPatMessageCellView` 类名变化 | 🟡 中 | 微信版本更新可能改名。`objc_getClass` 返回 nil 时 Hook 不会安装，功能静默失效，**不会 crash** |
| `AppPatMessageViewModel` 类名变化 | 🟡 中 | 同上 |
| 目标方法签名变化 | 🟡 低 | 5 个方法都是 WeChat 原生方法，且方法名（`initWithViewModel:`、`layoutInternal` 等）属于 WeChat 内部稳定的模式 |
| `purifySafeHook` 中 types 错误 | 🟢 低 | `method_getTypeEncoding` 获取父类的类型编码，与子类相同，类型匹配 |

### 6.6 核验结论

| 核验项 | 结论 |
|--------|:----:|
| **功能独立性** | ✅ **完全独立** — 不依赖任何其他模块的配置或钩子 |
| **无冲突** | ✅ **无冲突** — 项目中无其他代码 Hook 相同或相关的 WeChat 类 |
| **无递归** | ✅ **无递归风险** — 无其他模块 Hook `setFrame:`，`layoutSubviews` Hook 目标类不重叠 |
| **无崩溃** | ✅ **nil 安全** — 运行时类不存在时静默跳过，不会 crash |
| **顺序无关** | ✅ **安装顺序无关** — 通过 `HookRegistry` 统一注册，即使 install 被多次调用也有去重保护（HookRegistry.m L21-23） |
| **最小改动** | ✅ **单文件改动** — 只修改 `UIPurifyHook.m`，新增约 85 行代码 |

> **最终判断：该方案足够独立，实现后不会影响项目中任何已有功能。**

---

## 七、验证方法

1. **开关测试：**
   - 进入"界面净化"页面 → 开启"隐藏拍一拍提示"
   - 回到聊天 → 让好友发一条拍一拍消息
   - 预期：拍一拍消息完全不显示，不留空白占位

2. **开关动态生效测试：**
   - 开启状态下发一条拍一拍 → 不显示
   - 关闭开关 → 再发一条拍一拍 → 正常显示
   - 注意：已隐藏的拍一拍消息不会因为关闭开关而重新出现（Cell 已被复用）

3. **独立生效测试：**
   - 只开启"隐藏拍一拍提示"，不开启其他净化功能
   - 预期：只有拍一拍消息被隐藏，分割线、撤回提示等不受影响

4. **无闪退测试：**
   - 快速开关 → 快速滑动聊天列表
   - 预期：无 crash、无布局错乱

---

## 八、实现步骤总结

| 步骤 | 操作 | 涉及文件 | 行数估算 |
|:----:|------|----------|:--------:|
| 1 | 新增 `purifySafeHook`/`purifyIsOverridden` 工具函数 | `UIPurifyHook.m` | ~20 行 |
| 2 | 新增 5 个拍一拍 Hook C 函数 + 5 个 `_orig` 指针 | `UIPurifyHook.m` | ~40 行 |
| 3 | 在 `+install` 中新增拍一拍 Hook 安装逻辑 | `UIPurifyHook.m` | ~25 行 |
| 4 | 编译验证 | — | — |

**总新增代码量约 85 行**（全部在 `UIPurifyHook.m` 一个文件中）。

---

## 九、参考文档

- [撤回拍一拍隐藏_完整实现文档.md](file:///www/wwwroot/ios/文档/乱七八糟/撤回拍一拍隐藏_完整实现文档.md) — 包含 purifySafeHook 的设计原理
- [UIPurifyHook.m](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyHook.m) — 当前 Hook 实现（仅分隔线）
- [UIPurifyConfig.h](file:///www/wwwroot/ios/MioPlugin/Modules/Layout/UIPurifyConfig.h) — 配置定义
- [WPUIPurifyVC.m](file:///www/wwwroot/ios/MioPlugin/Modules/SettingEntry/WPUIPurifyVC.m) — UI 开关