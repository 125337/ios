# 快速置顶/备注/免打扰 — 开发步骤文档

> **创建日期**: 2026-06-01  
> **目标项目**: MioPlugin  
> **参考实现**: 微信助手 (wxzs 3.9-5)

---

## 目录

1. [前置条件 — 已完成的准备工作](#一前置条件--已完成的准备工作)
2. [Step 1: 创建 QuickActionsHook 模块文件](#step-1-创建-quickactionshook-模块文件)
3. [Step 2: 实现 Hook 核心逻辑](#step-2-实现-hook-核心逻辑)
    - [2.1 Hook 会话 Cell 的右滑手势配置](#21-hook-会话-cell-的右滑手势配置)
    - [2.2 实现三个操作按钮](#22-实现三个操作按钮)
        - [免打扰按钮](#221-免打扰按钮)
        - [备注按钮](#222-备注按钮)
        - [置顶按钮](#223-置顶按钮)
    - [2.3 完整 install 方法](#23-完整-install-方法)
4. [Step 3: 注册模块到 FeatureModuleRegistry](#step-3-注册模块到-featuremoduleregistry)
5. [Step 4: 编译与运行时验证](#step-4-编译与运行时验证)
6. [附录: 关键微信 API 参考](#附录-关键微信-api-参考)
7. [预期效果](#预期效果)

---

## 一、前置条件 — 已完成的准备工作

以下两项已经实现，无需修改代码：

### 1.1 UI 开关

文件: [SettingGeneralFunctionController.m](file:///www/wwwroot/ios/MioPlugin/Settings/Controllers/SettingGeneralFunctionController.m#L127-L133)

```objc
cy = [self addSwitchRowInGroup:group
                          title:@"快速置顶/备注/免打扰"
                           desc:nil
                            key:@"quickActionsEnabled"
                           isOn:config.quickActionsEnabled
                             cy:cy
                          width:w];
```

- 位置: `通用功能` 页面，位于"启用退群检测"和"显示消息时间"之间
- 用户操作: 打开/关闭此开关即可启用/禁用快捷操作功能

### 1.2 配置属性

文件: [PluginConfig.h](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.h#L20)

```objc
@property (nonatomic, assign) BOOL quickActionsEnabled;
```

- `PluginConfig` 是单例，通过 `[PluginConfig shared].quickActionsEnabled` 读写
- 开关状态自动持久化到 `NSUserDefaults`

---

## Step 1: 创建 QuickActionsHook 模块文件

### 1.1 新建目录和文件

在 MioPlugin 项目下创建以下两个文件:

```
MioPlugin/Modules/QuickActions/
├── QuickActionsHook.h
└── QuickActionsHook.m
```

### 1.2 QuickActionsHook.h

参照现有模块（如 [ClearUnreadHook.h](file:///www/wwwroot/ios/MioPlugin/Modules/Unread/ClearUnreadHook.h)）的格式：

```objc
#import <Foundation/Foundation.h>

@interface QuickActionsHook : NSObject
+ (void)install;
@end
```

### 1.3 QuickActionsHook.m — 导入头文件

```objc
#import "QuickActionsHook.h"
#import "../../Config/PluginConfig.h"
#import "../../Core/LogManager.h"
#import "../../Core/ServiceHelper.h"
#import <objc/runtime.h>
#import <objc/message.h>
#import <UIKit/UIKit.h>
#import <substrate.h>
```

---

## Step 2: 实现 Hook 核心逻辑

### 2.1 Hook 会话 Cell 的左滑手势配置

**目标**: 拦截微信会话列表 Cell 的 **leading swipe（从左往右滑）** 配置方法，在微信原生按钮（「标为已读」「不显示」「删除」）的前面插入我们自己的三个按钮。

> **微信滑动方向说明**（基于反编译代码分析确认）:
> - **从左往右滑**（leading swipe）→ 微信原生显示「标为已读」「不显示」「删除」
> - **从右往左滑**（trailing swipe）→ 微信无原生按钮（或版本不同表现不同）
> - 本功能 Hook 的是 **从左往右滑** 这一侧，与微信助手保持一致

**Hook 目标分析**（来自微信助手反编译代码 [FUN_007f61c4](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L654623)):

```c
// FUN_007f61c4 是微信助手替换的 swipe 配置方法
// 签名: (id self, SEL _cmd, UITableView *tableView, NSIndexPath *indexPath)
// 内部逻辑:
//   1. self (param_1) 通过 valueForKey: 获取会话管理器
//      例: [self valueForKey:@"m_sessionMgr"] 或类似属性
//   2. 调用 [sessionMgr getSessionInfoAtIndexPath:indexPath] 获取会话对象
//   3. 通过 [contactMgr getContactByName:userName] 获取 CContact
//   4. 检查 isContactSessionTop / isChatStatusNotifyOpen 确定按钮文字
//   5. 构建 3 个 UIContextualAction + 原生 actions → UISwipeActionsConfiguration
```

**需要 Hook 的类和方法**:

微信会话列表由 `NewMainFrameViewController`（或 `MMMainFrameViewController`）管理，该类实现了 `UITableViewDelegate` 协议。iOS 11+ 通过以下方法提供 leading swipe：

```objc
- (UISwipeActionsConfiguration *)tableView:(UITableView *)tableView
    leadingSwipeActionsConfigurationForRowAtIndexPath:(NSIndexPath *)indexPath;
```

**Hook 策略**: 使用 `MSHookMessageEx` Hook 该方法，在原生返回的 `UISwipeActionsConfiguration` 基础上，将我们的三个按钮 `[免打扰] [备注] [置顶]` 插入到 actions 数组末尾（视觉上左侧）。

**具体实施逻辑**（伪代码）:

```objc
static IMP orig_leadingSwipeActionsConfig = NULL;

static id replaced_leadingSwipeActionsConfig(id self, SEL _cmd, 
    UITableView *tableView, NSIndexPath *indexPath) {
    
    // 1. 先调用原始方法获取微信原生的滑动配置
    id origConfig = ((id (*)(id, SEL, id, id))orig_leadingSwipeActionsConfig)(self, _cmd, tableView, indexPath);
    
    // 2. 检查功能开关
    if (![PluginConfig shared].quickActionsEnabled) {
        return origConfig;
    }
    
    // 3. 获取当前 Cell 对应的会话信息
    //    参考反编译代码: [self valueForKey:@"m_sessionMgr"] → getSessionInfoAtIndexPath:
    id sessionInfo = /* 从 tableView 获取对应 indexPath 的会话 */;
    NSString *userName = /* 从会话获取 userName */;
    id contact = WXGetContactForWxid(userName);
    if (!contact) return origConfig;
    
    // 4. 查询当前状态（用于动态按钮文字）
    BOOL isTop = /* [contact isContactSessionTop] */;
    BOOL isMuted = /* [contact isChatStatusNotifyOpen] */;
    
    // 5. 构建三个 UIContextualAction
    UIContextualAction *muteAction = [UIContextualAction contextualActionWithStyle:UIContextualActionStyleNormal
        title:(isMuted ? @"取消免打扰" : @"免打扰")
        handler:^(UIContextualAction *action, UIView *view, 
                  void (^completion)(BOOL finished)) {
            // 免打扰逻辑 → 见 2.2.1
            completion(YES);
        }];
    muteAction.backgroundColor = [UIColor grayColor];
    
    UIContextualAction *remarkAction = [UIContextualAction contextualActionWithStyle:UIContextualActionStyleNormal
        title:@"备注"
        handler:^(UIContextualAction *action, UIView *view, 
                  void (^completion)(BOOL finished)) {
            // 备注逻辑 → 见 2.2.2
            completion(YES);
        }];
    remarkAction.backgroundColor = [UIColor orangeColor];
    
    UIContextualAction *pinAction = [UIContextualAction contextualActionWithStyle:UIContextualActionStyleNormal
        title:(isTop ? @"取消置顶" : @"置顶")
        handler:^(UIContextualAction *action, UIView *view, 
                  void (^completion)(BOOL finished)) {
            // 置顶逻辑 → 见 2.2.3
            completion(YES);
        }];
    pinAction.backgroundColor = [UIColor redColor];
    
    // 6. 合并: 我们的3个按钮放在最左侧 + 微信原生按钮
    NSArray *ourActions = @[muteAction, remarkAction, pinAction];
    NSArray *origActions = /* origConfig.actions */;
    NSArray *merged = [ourActions arrayByAddingObjectsFromArray:origActions];
    
    // 7. 返回新配置
    return [UISwipeActionsConfiguration configurationWithActions:merged];
}
```

#### 关键点 — 如何找到正确的 Hook 目标

基于微信助手反编译代码分析，Hook 的核心流程如下：

1. **Hook 目标类**: `NewMainFrameViewController`（微信主界面控制器）
   - 该类通过 `valueForKey:` 持有会话管理器属性
   - 实现了 `getSessionInfoAtIndexPath:` / `logicGetSessionAtIndexPath:` 方法

2. **Hook 目标方法**: `tableView:leadingSwipeActionsConfigurationForRowAtIndexPath:`
   - 微信原生在此返回 [标为已读, 不显示, 删除] 三个按钮
   - 我们在原生数组前面插入 [免打扰, 备注, 置顶]

3. **获取会话信息的方式**（参考反编译代码 [654771](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L654771)):
   ```objc
   // 方式 A: self 直接有 getSessionInfoAtIndexPath:
   [self getSessionInfoAtIndexPath:indexPath];
   
   // 方式 B: 通过 valueForKey 获取 m_sessionMgr
   id sessionMgr = [self valueForKey:@"m_sessionMgr"];
   [sessionMgr getSessionInfoAtIndexPath:indexPath];
   ```

> **注意**: 实际属性名需要在运行时通过 LLDB `po [self valueForKey:...]` 或 `class_copyIvarList` 确认。反编译代码中的 key 字符串是 XOR 混淆的，无法直接读取。

**推荐调试步骤**:
1. 运行时 Hook `NewMainFrameViewController` 的 `viewDidLoad` 或 `viewDidAppear`
2. 打印 `[self valueForKey:@"m_sessionMgr"]` 等可能的 key
3. 用 `class_copyIvarList` 列出所有 ivar，找到持有一组会话的 ivar 名
4. 确认后 Hook `leadingSwipeActionsConfigurationForRowAtIndexPath:`

#### 获取会话 Cell 对应 Contact 的方法

```objc
// 从 Cell 获取 m_sessinInfo / m_contact / m_nsUserName
id sessionInfo = WXContactValueForKey(sessionCell, @"m_sessionInfo");
// 或通过 SessionCell 的 m_contact 属性
NSString *userName = WXContactValueForKey(sessionInfo, @"m_nsUserName");
id contact = WXGetContactForWxid(userName);
```

注意: 实际属性名需要在运行时通过 `class_copyIvarList` 或 LLDB 调试确认，可能为 `m_sessinInfo`（微信内部拼写）或 `m_sessionInfo`。

---

### 2.2 实现三个操作按钮

#### 2.2.1 免打扰按钮

**按钮文字**: 根据 `isChatStatusNotifyOpen` 动态显示 "免打扰"（通知当前开启）或 "取消免打扰"（通知当前关闭）

**背景颜色**: 灰色 (`[UIColor grayColor]`)

**执行逻辑**:

```objc
// 1. 获取当前免打扰状态
BOOL isMuted = [contact respondsToSelector:NSSelectorFromString(@"isChatStatusNotifyOpen")]
    ? ((BOOL (*)(id, SEL))objc_msgSend)(contact, NSSelectorFromString(@"isChatStatusNotifyOpen"))
    : 1;

// 2. 翻转状态
BOOL newStatus = !isMuted;

// 3. 调用微信原生 API
id contactMgr = WXGetService(objc_getClass("CContactMgr"));
if (contactMgr) {
    SEL changeNotifySel = NSSelectorFromString(@"ChangeNotifyStatus:withStatus:sync:");
    if ([contactMgr respondsToSelector:changeNotifySel]) {
        ((void (*)(id, SEL, id, BOOL, BOOL))objc_msgSend)(contactMgr, changeNotifySel, contact, newStatus, YES);
    }
}

// 4. 同步本地状态
SEL setNotifySel = NSSelectorFromString(@"setChatStatusNotifyOpen:");
if ([contact respondsToSelector:setNotifySel]) {
    ((void (*)(id, SEL, BOOL))objc_msgSend)(contact, setNotifySel, newStatus);
}

// 5. Toast 提示
NSString *tip = newStatus ? @"已开启免打扰" : @"已取消免打扰";
WPLog(@"QuickActions", @"[INFO] mute: %@ -> %@", userName, tip);
```

**错误处理**: 如果微信 API 调用失败，不显示 Toast，静默失败。

#### 2.2.2 备注按钮

**按钮文字**: 固定 "备注"

**背景颜色**: 橙色 (`[UIColor orangeColor]`)

**执行逻辑**: 直接打开微信原生 ContactInfoViewController 并进入编辑模式：

```objc
// 1. 完成滑动动画（completion(YES) 接收来自 block 的 completion 参数）

// 2. 获取当前最上层的 ViewController
UIViewController *topVC = /* 获取当前 topViewController */;

// 3. 创建 ContactInfoViewController
Class contactInfoVCClass = objc_getClass("ContactInfoViewController");
if (!contactInfoVCClass) return;

id contactInfoVC = [contactInfoVCClass alloc];
((void (*)(id, SEL))objc_msgSend)(contactInfoVC, @selector(init));

// 4. 传入联系人
SEL setContactSel = NSSelectorFromString(@"setM_contact:");
if ([contactInfoVC respondsToSelector:setContactSel]) {
    ((void (*)(id, SEL, id))objc_msgSend)(contactInfoVC, setContactSel, contact);
}

// 5. 设置编辑状态（关键：直接进入编辑模式）
SEL setNeedEditSel = NSSelectorFromString(@"setNeedEditState:");
if ([contactInfoVC respondsToSelector:setNeedEditSel]) {
    ((void (*)(id, SEL, BOOL))objc_msgSend)(contactInfoVC, setNeedEditSel, YES);
}

// 6. Push 到 NavigationController
UINavigationController *nav = [[UINavigationController alloc] initWithRootViewController:contactInfoVC];
[(UIViewController *)topVC presentViewController:nav animated:YES completion:nil];
```

**获取 topViewController 的方法**:

```objc
static UIViewController *getTopViewController(void) {
    UIWindow *keyWindow = nil;
    if (@available(iOS 13.0, *)) {
        for (UIWindowScene *scene in [UIApplication sharedApplication].connectedScenes) {
            if (scene.activationState == UISceneActivationStateForegroundActive) {
                keyWindow = scene.windows.firstObject;
                break;
            }
        }
    }
    if (!keyWindow) {
        keyWindow = [UIApplication sharedApplication].keyWindow;
    }
    UIViewController *topVC = keyWindow.rootViewController;
    while (topVC.presentedViewController) {
        topVC = topVC.presentedViewController;
    }
    return topVC;
}
```

**备选方案**: 如果 `ContactInfoViewController` 在微信新版本中改名，使用字符串动态获取:
```objc
Class cls = NSClassFromString(@"ContactInfoViewController");
```

#### 2.2.3 置顶按钮

**按钮文字**: 根据 `isContactSessionTop` 动态显示 "置顶"（当前未置顶）或 "取消置顶"（当前已置顶）

**背景颜色**: 红色 (`[UIColor redColor]`)

**执行逻辑**:

```objc
// 1. 获取当前置顶状态
BOOL isTop = [contact respondsToSelector:NSSelectorFromString(@"isContactSessionTop")]
    ? ((BOOL (*)(id, SEL))objc_msgSend)(contact, NSSelectorFromString(@"isContactSessionTop"))
    : NO;

// 2. 翻转状态
BOOL newTop = !isTop;

// 3. 调用微信原生 API
id contactMgr = WXGetService(objc_getClass("CContactMgr"));
if (contactMgr) {
    SEL setTopSel = NSSelectorFromString(@"setContact:sessionTop:sync:");
    if ([contactMgr respondsToSelector:setTopSel]) {
        ((void (*)(id, SEL, id, BOOL, BOOL))objc_msgSend)(contactMgr, setTopSel, contact, newTop, YES);
    }
}

// 4. 直接设置 bIsTop 标记（确保生效）
SEL setBIsTopSel = NSSelectorFromString(@"setM_bIsTop:");
if ([contact respondsToSelector:setBIsTopSel]) {
    ((void (*)(id, SEL, BOOL))objc_msgSend)(contact, setBIsTopSel, newTop);
}

// 5. Toast 提示
NSString *tip = newTop ? @"已置顶" : @"已取消置顶";
WPLog(@"QuickActions", @"[INFO] pin: %@ -> %@", userName, tip);

// 6. 触觉反馈
UIImpactFeedbackGenerator *feedback = [[UIImpactFeedbackGenerator alloc] initWithStyle:UIImpactFeedbackStyleLight];
[feedback impactOccurred];
```

**Toast 显示方法**（简化版，可直接使用微信内部方法或自定义）:

```objc
// 方案 A: 使用微信内部 Toast
id contactMgr = WXGetService(objc_getClass("CContactMgr"));
SEL toastSel = newTop ? NSSelectorFromString(@"TopSessionByName:") 
                       : NSSelectorFromString(@"UntopSessionByName:");
((void (*)(id, SEL, id))objc_msgSend)(contactMgr, toastSel, userName);

// 方案 B: 自定义标签在 window 上显示 2 秒
static void showToast(NSString *text) {
    dispatch_async(dispatch_get_main_queue(), ^{
        UIWindow *window = [UIApplication sharedApplication].keyWindow;
        UILabel *label = [[UILabel alloc] init];
        label.text = text;
        label.textAlignment = NSTextAlignmentCenter;
        label.font = [UIFont systemFontOfSize:14];
        label.textColor = [UIColor whiteColor];
        label.backgroundColor = [[UIColor blackColor] colorWithAlphaComponent:0.7];
        label.layer.cornerRadius = 8;
        label.clipsToBounds = YES;
        [label sizeToFit];
        CGFloat w = label.frame.size.width + 30;
        CGFloat h = label.frame.size.height + 16;
        CGFloat x = (window.bounds.size.width - w) / 2;
        CGFloat y = window.bounds.size.height - 200;
        label.frame = CGRectMake(x, y, w, h);
        [window addSubview:label];
        dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(2.0 * NSEC_PER_SEC)), dispatch_get_main_queue(), ^{
            [label removeFromSuperview];
        });
    });
}
```

### 2.3 完整 install 方法

```objc
@implementation QuickActionsHook

+ (void)install {
    WPLog(@"QuickActions", @"[INFO] install start");
    
    // Hook 会话列表的 leading swipe 配置
    // Hook 目标: NewMainFrameViewController 或 MMMainFrameViewController
    // Hook 方法: tableView:leadingSwipeActionsConfigurationForRowAtIndexPath:
    
    Class targetClass = objc_getClass("NewMainFrameViewController");
    if (targetClass) {
        SEL targetSel = @selector(tableView:leadingSwipeActionsConfigurationForRowAtIndexPath:);
        if ([targetClass instancesRespondToSelector:targetSel]) {
            MSHookMessageEx(targetClass, targetSel, 
                (IMP)replaced_leadingSwipeActionsConfig, 
                &orig_leadingSwipeActionsConfig);
            WPLog(@"QuickActions", @"[INFO] leading swipe hooked on NewMainFrameViewController");
        }
    }
    
    // 如果 NewMainFrameViewController 未找到，尝试 MMMainFrameViewController
    if (!targetClass) {
        targetClass = objc_getClass("MMMainFrameViewController");
        if (targetClass) {
            SEL targetSel = @selector(tableView:leadingSwipeActionsConfigurationForRowAtIndexPath:);
            if ([targetClass instancesRespondToSelector:targetSel]) {
                MSHookMessageEx(targetClass, targetSel,
                    (IMP)replaced_leadingSwipeActionsConfig,
                    &orig_leadingSwipeActionsConfig);
                WPLog(@"QuickActions", @"[INFO] leading swipe hooked on MMMainFrameViewController");
            }
        }
    }
    
    WPLog(@"QuickActions", @"[INFO] install complete");
}

@end
```

#### 运行时确定 Hook 目标的调试方法

在 `install` 方法的头部添加以下调试代码，输出到日志以便确认:

```objc
// 打印 NewMainFrameViewController 中与 swipe 相关的方法
Class cls = objc_getClass("NewMainFrameViewController");
unsigned int methodCount = 0;
Method *methods = class_copyMethodList(cls, &methodCount);
for (unsigned int i = 0; i < methodCount; i++) {
    NSString *name = NSStringFromSelector(method_getName(methods[i]));
    if ([name containsString:@"swipe"] || [name containsString:@"leading"] 
        || [name containsString:@"SessionInfo"] || [name containsString:@"getSession"]) {
        WPLog(@"QuickActions", @"[DEBUG] Found method: %@", name);
    }
}
free(methods);

// 打印 ivar 列表，找到会话管理器属性名
unsigned int ivarCount = 0;
Ivar *ivars = class_copyIvarList(cls, &ivarCount);
for (unsigned int i = 0; i < ivarCount; i++) {
    NSString *name = [NSString stringWithUTF8String:ivar_getName(ivars[i])];
    if ([name containsString:@"session"] || [name containsString:@"Session"]) {
        WPLog(@"QuickActions", @"[DEBUG] Found ivar: %@", name);
    }
}
free(ivars);
```

---

## Step 3: 注册模块到 FeatureModuleRegistry

### 3.1 修改文件

文件: [FeatureModuleRegistry.m](file:///www/wwwroot/ios/MioPlugin/Registry/FeatureModuleRegistry.m)

#### 3.1.1 添加 import（在文件顶部）

```objc
#import "../Modules/QuickActions/QuickActionsHook.h"
```

#### 3.1.2 将 Hook 类注册到 `通用功能` 模块

在 `general` 模块的 `hookInstallerClasses` 数组末尾添加 `[QuickActionsHook class]`:

修改前（第 52 行附近）:

```objc
hookInstallerClasses:@[[RevokeHook class], [ClearUnreadHook class], 
    [JokerHook class], [GroupExitHook class], [MessageTimeHook class]]
```

修改后:

```objc
hookInstallerClasses:@[[RevokeHook class], [ClearUnreadHook class], 
    [JokerHook class], [GroupExitHook class], [MessageTimeHook class], 
    [QuickActionsHook class]]
```

---

## Step 4: 编译与运行时验证

### 4.1 编译

执行编译命令（根据项目具体配置）:

```bash
# 如果使用 make 编译
cd /www/wwwroot/ios && make

# 或使用 xcodebuild
cd /www/wwwroot/ios && xcodebuild -project MioPlugin.xcodeproj ...
```

### 4.2 运行时验证清单

| 验证项 | 操作 | 预期结果 |
|--------|------|----------|
| 1. 开关生效 | 在通用功能页开关"快速置顶/备注/免打扰" | 开关状态正确保存和读取 |
| 2. 按钮出现 | 开启开关后，在会话列表从左往右滑任意 Cell | 出现 [免打扰] [备注] [置顶] 三个按钮，位于微信原生按钮（标为已读/不显示/删除）的左侧 |
| 3. 按钮消失 | 关闭开关后，从左往右滑任意 Cell | 只显示微信原生按钮（「标为已读」「不显示」「删除」） |
| 4. 免打扰按钮 | 点击"免打扰"按钮 | 该会话开启免打扰，按钮文字变为"取消免打扰"，Toast 提示"已开启免打扰" |
| 5. 取消免打扰 | 对已免打扰的会话点击"取消免打扰" | 取消免打扰，Toast 提示"已取消免打扰" |
| 6. 备注按钮 | 点击"备注"按钮 | 弹出微信原生联系人资料页并直接进入备注编辑状态 |
| 7. 置顶按钮 | 点击"置顶"按钮 | 该会话被置顶，按钮文字变为"取消置顶"，Toast 提示"已置顶"，触发触觉反馈 |
| 8. 取消置顶 | 对已置顶的会话点击"取消置顶" | 取消置顶，Toast 提示"已取消置顶" |
| 9. 按钮顺序 | 右滑任意 Cell | 从左到右: 免打扰(灰) → 备注(橙) → 置顶(红) → 微信原生按钮 |
| 10. 触觉反馈 | 点击任意快捷按钮 | 感受到轻微震动反馈 |

### 4.3 日志检查

运行时在 Xcode Console 或设备日志中搜索 `[QuickActions]`:

```
[MioPlugin] [QuickActions] [INFO] install start
[MioPlugin] [QuickActions] [INFO] trailing swipe hooked on NewMainFrameViewController
[MioPlugin] [QuickActions] [INFO] install complete
[MioPlugin] [QuickActions] [INFO] mute: wxid_xxx -> 已开启免打扰
[MioPlugin] [QuickActions] [INFO] pin: wxid_yyy -> 已置顶
```

### 4.4 常见问题排查

| 问题 | 可能原因 | 排查方法 |
|------|----------|----------|
| 开关开启后右滑无反应 | Hook 目标类/方法名不正确 | 运行调试代码打印方法列表，确认实际的类名和方法签名 |
| 按钮出现但点击无反应 | 微信 API 方法名变化 | 检查 `isContactSessionTop` / `setContact:sessionTop:sync:` 等 selector 在当前版本是否存在 |
| 获取不到 contact 对象 | Cell 内部属性名变化 | 打印 Cell 的所有 ivar，确认存储 contact 的属性名 |
| 编译报错 | 缺少头文件导入 | 确认 `substrate.h` 和 `ServiceHelper.h` 路径正确 |

---

## 五、关键文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| [QuickActionsHook.h](#) | **新建** | 头文件，声明 `+ (void)install` |
| [QuickActionsHook.m](#) | **新建** | 实现文件，包含 Hook 和三个按钮逻辑 |
| [FeatureModuleRegistry.m](file:///www/wwwroot/ios/MioPlugin/Registry/FeatureModuleRegistry.m) | **修改** | 添加 import 和 hookInstallerClasses 注册 |
| [PluginConfig.h](file:///www/wwwroot/ios/MioPlugin/Config/PluginConfig.h) | **无改动** | `quickActionsEnabled` 已存在 |
| [SettingGeneralFunctionController.m](file:///www/wwwroot/ios/MioPlugin/Settings/Controllers/SettingGeneralFunctionController.m) | **无改动** | UI 开关已存在 |

---

## 六、附录: 关键微信 API 参考

| API | 类 | 作用 | 来源 |
|-----|-----|------|------|
| `isChatStatusNotifyOpen` | CContact | 判断会话免打扰状态 | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L655287) |
| `setChatStatusNotifyOpen:` | CContact | 设置会话免打扰 | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L655810) |
| `ChangeNotifyStatus:withStatus:sync:` | CContactMgr | 修改通知状态 | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L655805) |
| `isContactSessionTop` | CContact | 判断会话置顶状态 | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L654826) |
| `setContact:sessionTop:sync:` | CContactMgr | 设置会话置顶 | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L656388) |
| `setM_bIsTop:` | CContact | 设置置顶标记 | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L432918) |
| `getContactByUserName:` | CContactMgr | 通过 wxid 获取联系人 | [ServiceHelper.h](file:///www/wwwroot/ios/MioPlugin/Core/ServiceHelper.h#L13) |
| `setM_contact:` | ContactInfoViewController | 设置联系人数据 | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L655911) |
| `setNeedEditState:` | ContactInfoViewController | 进入编辑模式 | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L655914) |
| `TopSessionByName:` | CContactMgr | 置顶 Toast | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L656400) |
| `UntopSessionByName:` | CContactMgr | 取消置顶 Toast | [wxzs](file:///www/wwwroot/ios/插件/微信助手反编译/wxzs%203.9-5.dylib.c#L656403) |

---

## 七、预期效果

```
关闭状态 (quickActionsEnabled = NO):
  从左往右滑 Cell → 只显示微信原生的「标为已读」「不显示」「删除」

开启状态 (quickActionsEnabled = YES):
  从左往右滑 Cell → 显示六个按钮:

  ┌─────────────────────────────────────────────────────────────────────┐
  │ [免打扰] │ [备注] │ [置顶] │ [标为已读] │ [不显示] │ [删除]          │
  │  (灰色)  │ (橙色) │ (红色) │        ...微信原生按钮...              │
  └─────────────────────────────────────────────────────────────────────┘
    ← 左侧 (leading edge)                               右侧 (trailing) →

  点击免打扰 → 切换该会话的免打扰状态，Toast 提示
  点击备注   → 弹出联系人资料页，直接进入备注编辑模式
  点击置顶   → 切换该会话的置顶状态，Toast + 触觉反馈
```