# 微信助手 3.9-5 dylib — 快速置顶/备注/免打扰功能深度分析

> 分析目标：`微信助手 3.9-5(1).dylib`
> 文件格式：Mach-O 64-bit arm64 dynamically linked shared library
> 文件大小：~12.2 MB
> 插件原名：MiYou（觅友）
> 加载路径：`/Library/MobileSubstrate/DynamicLibraries/MiYou.dylib`

---

## 一、插件整体架构概览

### 1.1 核心类体系

插件以 `MiYou` 为命名前缀，构建了完整的 MVC 架构：

| 类名 | 职责 |
|------|------|
| `MiYouConfig` | 全局配置中心，管理所有功能开关与参数（持久化至 NSUserDefaults） |
| `MiYouSettingViewController` | 主设置页面控制器，聚合所有功能 Section |
| `MiYouToolActionsViewController` | 工具快捷操作控制器 |
| `MiYouViewController` | 插件主入口控制器 |
| `MiYouMessageAction` | 消息操作处理类 |
| `MiYouMessageToolBar` | 聊天消息工具栏 |
| `MiYouNotificationViewController` | 通知/免打扰管理控制器 |
| `MiYouPriUtil` | 权限/隐私工具类 |
| `MiYouFloatingView` | 悬浮窗视图 |
| `MiYouToastView` / `MiYouHudView` | 提示视图 |
| `MGSessionBoxViewController` | 会话列表增强控制器 |
| `MYRoomTool` | 群聊工具类 |
| `GroupTool` | 群组操作工具类 |
| `CheckHelper` | 好友检测辅助类 |

### 1.2 Hook 机制（2026-05-11 深度逆向结果）

插件使用 `method_setImplementation` 进行 Objective-C 方法替换（Native Method Swizzling），不使用 Logos/libsubstrate。核心 Hook 点：

**一级 Hook：`UITableView.setDataSource:`**
- 在 `setDataSource:` 回调中动态注入 5 个方法到 dataSource 类上：
  - `tableView:canEditRowAtIndexPath:` — 允许行编辑
  - `tableView:editingStyleForRowAtIndexPath:` — 返回编辑样式
  - `tableView:leadingSwipeActionsConfigurationForRowAtIndexPath:` — 右滑操作
  - `tableView:trailingSwipeActionsConfigurationForRowAtIndexPath:` — 左滑操作
  - `tableView:editActionsForRowAtIndexPath:` — iOS 8-10 兼容

**二级 Hook：`NewMainFrameViewController.viewDidLoad` / `viewWillAppear:`**
- `viewDidLoad` — 确保 tableView.panGestureRecognizer 启用
- `viewWillAppear:` — 启用会话手势，确保 panGesture 不被其他手势拦截

**关键数据结构：**
- `FakeMainFrameCellData` — 微信内部 cell 数据模型，包含 `m_sessionInfo`
- `MMSessionInfo` — 会话信息，包含 `m_nsUserName`
- `getCellData:` → FakeMainFrameCellData → `m_sessionInfo` / `m_cellData` → `m_nsUserName`
- `getSessionInfoAtIndexPath:` → sessionInfo → `m_nsUserName`
- `WCTableViewManager` — 微信内部 tableView 管理器

**HEXA_STRING 关键方法签名（从 dylib 提取）：**
```
@"UISwipeActionsConfiguration"32@0:8@"UITableView"16@"NSIndexPath"24
v32@?0@"FakeMainFrameCellData"8Q16^B24
@ T@"NSMutableArray",&,N,V_filterSessionList
@ T@"NSMutableArray",&,N,V_sessionDataList
@ T@"WCTableViewManager",&,N,V_tableViewManager
```

### 1.3 配置持久化

配置通过 `NSUserDefaults` 持久化，键名为 `WeChatMiYouSeting`，`MiYouConfig` 类持有 `userDefaults` 属性：

```
T@"NSUserDefaults",&,N,V_userDefaults
```

---

## 二、快速置顶功能深度分析

### 2.1 功能概述

快速置顶功能允许用户在会话列表中通过**滑动手势**快速将某个会话置顶或取消置顶，无需进入聊天设置页面操作。

### 2.2 涉及的核心属性

#### MiYouConfig 中的置顶相关属性

| 属性名 | 类型 | 说明 |
|--------|------|------|
| `isChatBoxTop` | `BOOL` (TB,N,V_isChatBoxTop) | 聊天盒子置顶开关 |
| `isBrandTop` | `BOOL` (TB,N,V_isBrandTop) | 品牌号置顶开关 |
| `isBrandSessionHolderTop` | `BOOL` (TB,N,V_isBrandSessionHolderTop) | 品牌会话置顶开关 |
| `isHelperTop` | `BOOL` (TB,N,V_isHelperTop) | 辅助置顶开关 |
| `chatBoxTopInList` | `NSInteger` (Tq,N,V_chatBoxTopInList) | 聊天盒子在列表中的置顶位置 |
| `mIsSessionGesture` | `BOOL` (TB,N,V_mIsSessionGesture) | 会话手势总开关 |

#### 微信内部类的置顶属性（被 Hook/调用）

| 属性名 | 所属类 | 说明 |
|--------|--------|------|
| `m_bIsTop` | 微信会话模型 | 当前会话是否置顶 |
| `m_topSessions` | 微信会话管理器 | 置顶会话列表 |

### 2.3 涉及的核心方法

#### 微信原生 API（被调用）

| 方法签名 | 说明 |
|----------|------|
| `setContact:sessionTop:sync:` | 设置联系人会话置顶状态（核心 API） |
| `TopSessionByName:` | 按名称置顶会话 |
| `UntopSessionByName:` | 按名称取消置顶会话 |
| `isContactSessionTop` | 查询联系人会话是否置顶 |
| `canFoldTopSession` | 查询是否可折叠置顶会话 |
| `topSessions` | 获取置顶会话列表 |
| `setM_bIsTop:` | 设置置顶标志 |
| `setTopSessions:` | 设置置顶会话列表 |

#### 插件设置方法

| 方法签名 | 说明 |
|----------|------|
| `settingChatBoxTopEnable:` | 设置聊天盒子置顶启用 |
| `settingBrandTopEnable:` | 设置品牌号置顶启用 |
| `settingBrandSessionTop:` | 设置品牌会话置顶 |
| `settingIsHelperTop:` | 设置辅助置顶 |
| `settingSessionGesture:` | 设置会话手势开关 |

### 2.4 实现流程

```
┌─────────────────────────────────────────────────────────┐
│                    插件加载阶段                           │
├─────────────────────────────────────────────────────────┤
│  1. __attribute__((constructor)) 入口函数执行            │
│  2. Hook NewMainFrameViewController:                    │
│     - viewDidLoad → 注入初始化代码                       │
│     - viewWillAppear: → 启用会话手势                     │
│     - canEditRowAtIndexPath: → 返回 YES                 │
│     - 注入 trailingSwipeActionsConfiguration             │
│     - 注入 leadingSwipeActionsConfiguration              │
│  3. 从 NSUserDefaults 读取 MiYouConfig 配置             │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  用户交互阶段                             │
├─────────────────────────────────────────────────────────┤
│  1. 用户在会话列表左滑/右滑某个会话                       │
│  2. 触发 trailingSwipeActionsConfiguration               │
│  3. 显示"置顶/取消置顶"操作按钮                           │
│  4. 用户点击按钮                                         │
│  5. UIContextualAction handler 执行                      │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  置顶执行阶段                             │
├─────────────────────────────────────────────────────────┤
│  1. 获取当前会话的 FakeMainFrameCellData                 │
│  2. 从 CellData 提取联系人/会话信息                      │
│  3. 调用微信内部 API:                                    │
│     - 置顶: TopSessionByName: / setContact:sessionTop:sync: │
│     - 取消: UntopSessionByName:                          │
│  4. 更新 m_bIsTop 属性                                   │
│  5. 刷新会话列表: updateMainSessionList                  │
└─────────────────────────────────────────────────────────┘
```

### 2.5 关键技术点

1. **UISwipeActionsConfiguration 注入**：通过 Hook `trailingSwipeActionsConfigurationForRowAtIndexPath:` 方法，在微信原生滑动菜单基础上追加自定义操作按钮，使用 `UIContextualAction` 创建置顶/取消置顶按钮。

2. **canEditRowAtIndexPath 拦截**：必须 Hook 此方法返回 YES，否则滑动操作不会触发。从日志可见，此方法被频繁调用以判断每行是否可编辑。

3. **FakeMainFrameCellData 解析**：微信使用 `FakeMainFrameCellData` 类封装会话列表中每个会话的数据，插件通过 `getCellData:` 方法获取对应 indexPath 的数据，从中提取会话标识符。

4. **微信内部 API 调用**：
   - `setContact:sessionTop:sync:` — 最核心的置顶 API，参数依次为联系人对象、是否置顶(BOOL)、是否同步到服务器(BOOL)
   - `TopSessionByName:` / `UntopSessionByName:` — 按用户名置顶/取消置顶
   - `addIMBehaviorContactOp:contactOpType:` — 记录操作行为

---

## 三、快速备注功能深度分析

### 3.1 功能概述

快速备注功能允许用户在会话列表中通过滑动手势快速修改联系人备注名，无需进入联系人详情页。

### 3.2 涉及的核心属性

#### 微信联系人模型属性（被读取/修改）

| 属性名 | 类型 | 说明 |
|--------|------|------|
| `m_nsRemark` | `NSString *` | 联系人备注名 |
| `m_nsNickName` | `NSString *` | 联系人昵称 |
| `m_nsAliasName` | `NSString *` | 联系人微信号 |

#### MiYouConfig 中的备注相关属性

| 属性名 | 类型 | 说明 |
|--------|------|------|
| `mIsShowUserName` | `BOOL` (TB,N,V_mIsShowUserName) | 是否显示用户名 |
| `m_arrContactNameList` | `NSMutableArray *` | 联系人名称列表 |

### 3.3 涉及的核心方法

| 方法签名 | 说明 |
|----------|------|
| `settingShowUserNameEnable:` | 设置显示用户名开关 |
| `settingFriendName` | 设置好友名称显示方式 |
| `updateContact` | 更新联系人信息（微信内部 API） |
| `setM_nsNickName:` | 设置联系人昵称 |
| `createSessionWithUserName:nickName:showRedDot:readAsRedDot:` | 创建带昵称的会话 |

### 3.4 实现流程

```
┌─────────────────────────────────────────────────────────┐
│                  用户交互阶段                             │
├─────────────────────────────────────────────────────────┤
│  1. 用户在会话列表滑动某个联系人会话                       │
│  2. 触发 SwipeActionsConfiguration                      │
│  3. 显示"备注"操作按钮                                   │
│  4. 用户点击"备注"按钮                                   │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  备注编辑阶段                             │
├─────────────────────────────────────────────────────────┤
│  1. 弹出 UIAlertController（带文本输入框）                │
│     - alertControllerTitle:message:text:block:           │
│  2. 预填充当前备注名（m_nsRemark）                        │
│  3. 用户输入新备注名                                     │
│  4. 点击确认                                            │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  备注提交阶段                             │
├─────────────────────────────────────────────────────────┤
│  1. 获取 CContactMgr（联系人管理器）                      │
│     - 通过 conontact / contactMgr 属性                   │
│  2. 获取目标联系人对象                                   │
│  3. 修改联系人 m_nsRemark 属性                           │
│  4. 调用 updateContact 提交到微信服务器                   │
│  5. 刷新会话列表显示                                     │
└─────────────────────────────────────────────────────────┘
```

### 3.5 关键技术点

1. **UIAlertController 文本输入**：插件使用 `alertControllerTitle:message:text:block:` 封装方法，创建带 `UITextField` 的 `UIAlertControllerStyleAlert` 弹窗，预填充当前备注名。

2. **CContactMgr 获取**：微信的联系人管理器 `CContactMgr` 是核心服务，插件通过 Hook 获取其引用：
   ```
   T@"CContactMgr",&,N,V_conontact
   T@"CContactMgr",&,N,V_contactMgr
   ```

3. **联系人数据模型**：微信使用 `CContact` 类表示联系人，包含 `m_nsRemark`（备注）、`m_nsNickName`（昵称）、`m_nsAliasName`（微信号）等属性。插件直接修改 `m_nsRemark` 后调用 `updateContact` 同步到服务器。

4. **显示名称切换**：`settingShowUserNameEnable:` 和 `settingFriendName` 控制会话列表中显示备注名还是昵称，通过 Hook 会话 Cell 的显示逻辑实现。

---

## 四、免打扰功能深度分析

### 4.1 功能概述

免打扰功能允许用户对指定联系人或群组设置消息免打扰，支持自定义免打扰时段、免打扰提示消息，以及在会话列表中快速切换免打扰状态。

### 4.2 涉及的核心属性

#### MiYouConfig 中的免打扰相关属性

| 属性名 | 类型 | 说明 |
|--------|------|------|
| `mIsDonotdisturbState` | `BOOL` (TB,N,V_mIsDonotdisturbState) | 免打扰总开关 |
| `mIsAddDonotdisturbMenu` | `BOOL` (TB,N,V_mIsAddDonotdisturbMenu) | 是否添加免打扰菜单项 |
| `mDonotdisturbList` | `NSMutableArray *` (T@"NSMutableArray",&,N,V_mDonotdisturbList) | 免打扰联系人列表 |
| `mDonotdisturbMsg` | `NSString *` (T@"NSString",&,N,V_mDonotdisturbMsg) | 免打扰提示消息内容 |
| `mDonotdisturbWorkingTime` | `NSString *` (T@"NSString",&,N,V_mDonotdisturbWorkingTime) | 免打扰工作时段 |

#### 通知相关属性

| 属性名 | 类型 | 说明 |
|--------|------|------|
| `localNotification` | `UILocalNotification *` | 本地通知对象 |
| `mIsOpenNotice` | `BOOL` | 通知开关 |
| `mIsAutoNotifyOpen` | `BOOL` | 自动通知开关 |
| `mIsShockNotice` | `BOOL` | 震动通知开关 |
| `mIsFakeNotice` | `BOOL` | 伪装通知开关 |
| `mIsShowNewMessage` | `BOOL` | 显示新消息开关 |
| `mFakeNoticeTitle` | `NSString *` | 伪装通知标题 |
| `mFakeNoticeBody` | `NSString *` | 伪装通知内容 |
| `mNoticeNum` | `NSInteger` | 通知数量 |

### 4.3 涉及的核心类和方法

#### MiYouNotificationViewController

免打扰管理界面控制器，负责：
- 显示免打扰联系人列表
- 添加/移除免打扰联系人
- 配置免打扰时段
- 配置免打扰提示消息

#### 核心方法

| 方法签名 | 说明 |
|----------|------|
| `settingDonotdisturbState:` | 设置免打扰总开关 |
| `settingAddDonotdisturbMenu:` | 设置是否添加免打扰菜单 |
| `settingDonotdisturbMsg` | 设置免打扰提示消息 |
| `showDonotdisturbList` | 显示免打扰列表 |
| `addDonotdisturbSection` | 添加免打扰设置 Section |
| `addNotificationSection` | 添加通知设置 Section |
| `showLocalNotificationWithInfo:` | 显示本地通知（带信息） |
| `showLocalNotificationWithMsg:repeatVibration:` | 显示本地通知（带消息和重复震动） |
| `scheduleLocalNotification:` | 调度本地通知 |
| `cancelLocalNotification:` | 取消本地通知 |

### 4.4 实现流程

```
┌─────────────────────────────────────────────────────────┐
│                  免打扰配置阶段                           │
├─────────────────────────────────────────────────────────┤
│  1. 用户进入 MiYouSettingViewController                 │
│  2. addDonotdisturbSection 添加免打扰设置区域             │
│     - 免打扰总开关 (mIsDonotdisturbState)                │
│     - 添加免打扰菜单 (mIsAddDonotdisturbMenu)            │
│     - 免打扰提示消息 (mDonotdisturbMsg)                   │
│     - 免打扰时段 (mDonotdisturbWorkingTime)               │
│  3. addNotificationSection 添加通知设置区域               │
│     - 通知开关 (mIsOpenNotice)                           │
│     - 震动通知 (mIsShockNotice)                          │
│     - 伪装通知 (mIsFakeNotice)                           │
│  4. 配置保存至 NSUserDefaults (WeChatMiYouSeting)        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  免打扰列表管理                           │
├─────────────────────────────────────────────────────────┤
│  1. showDonotdisturbList 展示免打扰列表                   │
│  2. mDonotdisturbList 存储免打扰联系人 userName 列表     │
│  3. 用户可添加/移除免打扰联系人                           │
│  4. 支持从会话列表滑动快速添加到免打扰列表                 │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  消息拦截阶段                             │
├─────────────────────────────────────────────────────────┤
│  1. Hook 微信消息接收回调                                │
│  2. 检查 mIsDonotdisturbState 是否开启                   │
│  3. 检查消息发送者是否在 mDonotdisturbList 中             │
│  4. 检查当前时间是否在 mDonotdisturbWorkingTime 时段内    │
│  5. 若满足免打扰条件:                                     │
│     a. 抑制通知显示                                      │
│     b. 抑制声音/震动提醒                                 │
│     c. 可选: 显示伪装通知 (mFakeNoticeTitle/Body)        │
│     d. 可选: 自动回复 (mDonotdisturbMsg)                 │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  通知处理阶段                             │
├─────────────────────────────────────────────────────────┤
│  正常通知路径:                                           │
│    scheduleLocalNotification: → 系统推送通知              │
│    showLocalNotificationWithMsg:repeatVibration:         │
│                                                          │
│  免打扰通知路径:                                          │
│    cancelLocalNotification: → 取消系统通知               │
│    若开启伪装通知:                                        │
│      → 使用 mFakeNoticeTitle/mFakeNoticeBody 替代        │
│      → 通过 UILocalNotification 发送伪装内容             │
└─────────────────────────────────────────────────────────┘
```

### 4.5 关键技术点

1. **消息接收 Hook**：插件 Hook 微信的消息接收回调（`CMessageMgr` 相关方法），在消息到达时检查免打扰条件。

2. **免打扰时段控制**：`mDonotdisturbWorkingTime` 存储时段字符串（如 "22:00-08:00"），插件在消息到达时解析时段并与当前时间比较，决定是否进入免打扰模式。

3. **通知拦截与替换**：
   - 通过 `cancelLocalNotification:` 取消微信原始通知
   - 通过 `CFNotificationCenterAddObserver` / `CFNotificationCenterGetLocalCenter` 监听系统通知事件
   - 若开启伪装通知，使用 `UILocalNotification` 发送自定义内容，播放 `UILocalNotificationDefaultSoundName` 或静音

4. **自动回复集成**：免打扰模式下可配置 `mDonotdisturbMsg` 作为自动回复消息，当收到免打扰联系人的消息时自动发送。

5. **会话列表快速操作**：当 `mIsAddDonotdisturbMenu` 开启时，在会话列表的滑动菜单中添加"免打扰"选项，用户可快速将联系人加入/移出免打扰列表。

---

## 五、三大功能的交互关系

```
┌──────────────────────────────────────────────────────────────────┐
│                    MiYouSettingViewController                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │addSessionBox │  │addChatBox    │  │addDonotdisturb       │  │
│  │Section       │  │Section       │  │Section               │  │
│  │              │  │              │  │                      │  │
│  │·会话手势开关  │  │·聊天盒子置顶 │  │·免打扰总开关         │  │
│  │·置顶相关设置  │  │·品牌号置顶   │  │·免打扰菜单           │  │
│  └──────┬───────┘  │·辅助置顶     │  │·免打扰消息           │  │
│         │          └──────┬───────┘  │·免打扰时段           │  │
│         │                 │          └──────────┬───────────┘  │
│         │                 │                     │               │
└─────────┼─────────────────┼─────────────────────┼───────────────┘
          │                 │                     │
          ▼                 ▼                     ▼
┌──────────────────────────────────────────────────────────────────┐
│              NewMainFrameViewController (Hooked)                  │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  trailingSwipeActionsConfigurationForRowAtIndexPath:        │ │
│  │                                                             │ │
│  │  ┌────────┐  ┌────────┐  ┌──────────┐                     │ │
│  │  │ 置顶   │  │ 备注   │  │ 免打扰   │   ← 滑动操作按钮     │ │
│  │  └───┬────┘  └───┬────┘  └────┬─────┘                     │ │
│  └──────┼───────────┼────────────┼────────────────────────────┘ │
│         │           │            │                               │
│         ▼           ▼            ▼                               │
│  ┌────────────┐ ┌────────────┐ ┌────────────────┐              │
│  │TopSession  │ │UIAlert     │ │mDonotdisturb   │              │
│  │ByName:     │ │Controller  │ │List 添加/移除   │              │
│  │UntopSession│ │(文本输入)  │ │                │              │
│  │ByName:     │ │            │ │                │              │
│  └────────────┘ └────────────┘ └────────────────┘              │
│         │           │            │                               │
│         ▼           ▼            ▼                               │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              updateMainSessionList                         │ │
│  │              刷新会话列表显示                                │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

---

## 六、数据流与持久化

### 6.1 配置存储结构

所有功能配置通过 `MiYouConfig` 类统一管理，持久化到 `NSUserDefaults`：

```
WeChatMiYouSeting = {
    // 置顶相关
    isChatBoxTop = NO;
    isBrandTop = NO;
    isBrandSessionHolderTop = NO;
    isHelperTop = NO;
    chatBoxTopInList = 0;
    mIsSessionGesture = YES;

    // 备注相关
    mIsShowUserName = NO;

    // 免打扰相关
    mIsDonotdisturbState = NO;
    mIsAddDonotdisturbMenu = NO;
    mDonotdisturbList = (...);
    mDonotdisturbMsg = "";
    mDonotdisturbWorkingTime = "";

    // 通知相关
    mIsOpenNotice = NO;
    mIsAutoNotifyOpen = NO;
    mIsShockNotice = NO;
    mIsFakeNotice = NO;
    mIsShowNewMessage = NO;
    mFakeNoticeTitle = "";
    mFakeNoticeBody = "";
    mNoticeNum = 0;

    // ... 其他 100+ 配置项
};
```

### 6.2 微信内部 API 调用链

```
插件方法                     微信内部类/方法
─────────                    ──────────────
置顶操作  ──────────────→   CContactMgr
                             ├── setContact:sessionTop:sync:
                             ├── TopSessionByName:
                             ├── UntopSessionByName:
                             └── addIMBehaviorContactOp:contactOpType:

备注操作  ──────────────→   CContact
                             ├── m_nsRemark (属性修改)
                             └── updateContact

免打扰操作 ─────────────→   CMessageMgr (消息拦截)
                             └── 消息接收回调 Hook

通知控制  ──────────────→   UILocalNotification
                             ├── scheduleLocalNotification:
                             └── cancelLocalNotification:

会话刷新  ──────────────→   NewMainFrameViewController
                             └── updateMainSessionList
```

---

## 七、安全与稳定性考量

### 7.1 Hook 安全性

- 使用 `method_setImplementation` 而非 `method_exchangeImplementations`，避免递归调用风险
- 日志记录 Hook 状态（如 `[setDataSource] already hooked: NewMainFrameViewController`），防止重复 Hook

### 7.2 数据安全

- 配置通过 `NSUserDefaults` 明文存储，键名 `WeChatMiYouSeting`
- 免打扰列表 `mDonotdisturbList` 存储联系人 userName，不存储敏感信息
- 隐藏功能密码 `mHidePassword` 以 `NSString` 明文存储

### 7.3 兼容性

- 插件依赖微信内部类名（`CContactMgr`、`CContact`、`NewMainFrameViewController` 等），微信版本更新可能导致类名变更
- `FakeMainFrameCellData` 是微信私有类，版本更新后数据结构可能变化

---

## 八、总结

| 功能 | 实现方式 | 核心 Hook 点 | 核心 API |
|------|----------|-------------|----------|
| 快速置顶 | UISwipeActionsConfiguration 注入 | NewMainFrameViewController | `setContact:sessionTop:sync:` / `TopSessionByName:` / `UntopSessionByName:` |
| 快速备注 | UIAlertController + CContact 属性修改 | NewMainFrameViewController | `updateContact` / `m_nsRemark` |
| 免打扰 | 消息接收 Hook + 通知拦截 | CMessageMgr 回调 | `UILocalNotification` / `cancelLocalNotification:` |

三大功能共享同一套 Hook 基础设施（会话列表滑动操作），通过 `MiYouConfig` 统一管理配置，通过 `MiYouSettingViewController` 提供设置界面，形成完整的功能闭环。

---

## 九、WeChatPlugin 项目实现指南

> 目标项目：`/www/wwwroot/ios/WeChatPlugin`
> 项目名称：MioHelper
> 构建系统：Theos
> 目标架构：arm64 / iOS 14.0+

### 9.1 项目现有架构

```
WeChatPlugin/
├── Tweak.m                          ← 入口（constructor → HookRegistry.installAllHooks）
├── Makefile                         ← Theos 构建配置
├── WeChatPlugin.plist               ← CydiaSubstrate 过滤器（com.tencent.xin）
├── Config/
│   ├── PluginConfig.h/m             ← 全局配置（单例，NSUserDefaults 持久化）
│   └── Constants.h                  ← 常量定义（kPluginPrefix = "WCP_"）
├── Core/
│   ├── HookEngine.h/m               ← Method Swizzle 工具类
│   └── WPAlert.h/m                  ← 弹窗工具类
├── Registry/
│   ├── HookRegistry.h/m             ← Hook 安装调度器
│   ├── FeatureModule.h              ← 模块描述符
│   ├── FeatureModuleRegistry.h/m    ← 模块注册中心
│   └── FeatureRegistry.h            ← 功能注册
├── Modules/
│   ├── SessionBox/
│   │   ├── WPSessionBoxHook.h/m     ← 会话列表 Hook（setDataSource + leadingSwipe）
│   │   └── WPSessionBoxController.h/m ← 会话盒子控制器（trailingSwipe + 业务逻辑）
│   ├── Revoke/                      ← 防撤回
│   ├── RedEnvelop/                  ← 自动抢红包
│   ├── Layout/                      ← 界面布局
│   ├── Unread/                      ← 未读管理
│   ├── AutoTransfer/                ← 自动确认转账
│   ├── SettingEntry/                ← 设置入口
│   ├── Joker/                       ← 彩蛋
│   ├── GroupExit/                   ← 退群监控
│   └── MessageTime/                 ← 消息时间
└── Settings/
    ├── Common/
    │   ├── SettingCategoryController.h/m ← 设置页基类
    │   └── WPBorderLayer.h/m            ← 边框装饰
    └── Controllers/
        ├── SettingSessionActionController.h/m ← 会话增强设置页
        └── ...其他设置页
```

### 9.2 已实现功能 vs 缺失功能（2026-05-11 第3轮更新 — 深度逆向后）

| 功能 | 状态 | 说明 |
|------|------|------|
| 快速置顶 - 滑动菜单 | ✅ 已完成 | WPSessionBoxHook 统一注入 leading + trailing |
| 快速置顶 - TopSessionByName / UntopSessionByName | ✅ 已完成 | |
| 快速置顶 - setContact:sessionTop:sync: | ✅ 已完成 | WPSessionBoxController |
| 快速置顶 - canEditRowAtIndexPath Hook | ✅ 已修复 | 去掉 section > 0 错误判断 |
| 快速置顶 - editingStyleForRowAtIndexPath Hook | ✅ 新增 | 返回 Delete 样式，iOS 双重检查机制必需 |
| 快速置顶 - userName 多路径获取 | ✅ 已修复 | 4条路径: getSessionInfo → getCellData/FakeMainFrameCellData → logicGetSession → sessionInfoForIndexPath |
| 快速置顶 - viewWillAppear Hook | ✅ 已恢复 | 确保 tableView.panGestureRecognizer 启用 |
| 快速置顶 - 置顶折叠 | ⚠️ 部分完成 | 配置项存在，实际折叠逻辑未实现 |
| 快速备注 - 滑动菜单触发 | ✅ 已完成 | |
| 快速备注 - UIAlertController | ✅ 已完成 | |
| 快速备注 - setM_nsRemark + modifyDataItem | ✅ 已完成 | |
| 快速备注 - 修改后刷新会话列表 | ✅ 已修复 | |
| 快速免打扰 - 滑动菜单触发 | ✅ 已完成 | |
| 快速免打扰 - ChangeNotifyStatus | ✅ 已完成 | |
| 快速免打扰 - muteContactList 管理 | ✅ 已完成 | |
| 会话盒子 - CollectionView | ✅ 已完成 | WPSessionBoxController |
| Hook 机制统一 | ✅ 已完成 | setDataSource 注入 5个方法 + viewWillAppear 启用手势 |

### 9.3 最终修改记录（精简方案 — 只做 leadingSwipe + canEdit + viewWillAppear）

**核心原则**：微信原生右→左滑（trailing）显示"标记已读/删除"，微信助手左→右滑（leading）显示"置顶/备注/免打扰"。**两者方向不同，互不冲突。** 我们只 Hook leading 方向，完全不碰 trailing。

#### ✅ 修复 1：canEditRowAtIndexPath — 去掉 section > 0 Bug
所有行都返回 YES，让 iOS 能识别滑动手势。

#### ✅ 修复 2：userName 获取 — 4 条后备路径
1. `getSessionInfoAtIndexPath:` → `m_nsUserName`
2. `getCellData:` → `FakeMainFrameCellData` → `m_sessionInfo` → `m_nsUserName`
3. `getCellData:` → `FakeMainFrameCellData` → `m_cellData` → `m_nsUserName`
4. `logicGetSessionAtIndexPath:` / `sessionInfoForIndexPath:` → `m_nsUserName`

#### ✅ 修复 3：leadingSwipe 空配置优化
当微信原始 leading swipe 返回空配置（0 actions）时，不合并，直接创建全新的干净配置，`performsFirstActionWithFullSwipe = NO`。

#### ✅ 修复 4：viewWillAppear — 确保 panGesture 启用
`tableView.panGestureRecognizer.enabled = YES`。

#### ❌ 已移除：trailingSwipe + editingStyle Hook
这两个 Hook 会干扰微信原生右→左滑动的"标记已读/删除"按钮，已完全移除。

### 9.4 当前完整 Hook 架构

```
WPSessionBoxHook.m install
├── 1. Hook UITableView.setDataSource:
│       └── replaced_setDataSource → 注入到 dataSource 类:
│           ├── leadingSwipeActionsConfigurationForRowAtIndexPath:  (左→右滑，我们的自定义菜单)
│           └── canEditRowAtIndexPath:                              (允许滑动)
│
└── 2. Hook NewMainFrameViewController.viewWillAppear:
        └── tableView.panGestureRecognizer.enabled = YES

完全不碰 trailing (微信原生: 标记已读/删除)
完全不碰 editingStyle
```

### 9.5 项目 vs MiYou 架构最终对比

| 接入点 | 说明 | 状态 |
|--------|------|------|
| `UITableView.setDataSource:` | 注入 leadingSwipe + canEditRow | ✅ |
| `leadingSwipeActionsConfigurationForRowAtIndexPath:` | 左→右滑：显示置顶/备注/免打扰 | ✅ |
| `canEditRowAtIndexPath:` | 所有行返回 YES | ✅ |
| `viewWillAppear:` | panGesture 启用 | ✅ |
| `trailingSwipeActionsConfigurationForRowAtIndexPath:` | 不 Hook，保留微信原生 | ❌ 故意跳过 |
| `editingStyleForRowAtIndexPath:` | 不 Hook，保留微信原生 | ❌ 故意跳过 |
| `FakeMainFrameCellData` / `getCellData:` 路径 | userName 多路径获取 | ✅ |
| `MMSessionInfo` → `m_nsUserName` | 会话信息提取 | ✅ |
| `PluginConfig` / NSUserDefaults | 配置持久化 | ✅ |
