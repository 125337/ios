# 微信助手 3.9-5 dylib — 快速置顶/备注/免打扰功能深度分析

> 分析目标：`微信助手 3.9-5(1).dylib`
> 文件格式：Mach-O 64-bit arm64 dynamically linked shared library
> 文件大小：~12.2 MB
> 插件原名：MiYou（觅友）
> 加载路径：`/Library/MobileSubstrate/DynamicLibraries/MiYou.dylib`

---

## 一、插件整体架构概览

### 1.1 核心类体系

插件以 `MiYou` 为命名前缀，构建了完整的 MVC 架构：

| 类名 | 职责 |
|------|------|
| `MiYouConfig` | 全局配置中心，管理所有功能开关与参数（持久化至 NSUserDefaults） |
| `MiYouSettingViewController` | 主设置页面控制器，聚合所有功能 Section |
| `MiYouToolActionsViewController` | 工具快捷操作控制器 |
| `MiYouViewController` | 插件主入口控制器 |
| `MiYouMessageAction` | 消息操作处理类 |
| `MiYouMessageToolBar` | 聊天消息工具栏 |
| `MiYouNotificationViewController` | 通知/免打扰管理控制器 |
| `MiYouPriUtil` | 权限/隐私工具类 |
| `MiYouFloatingView` | 悬浮窗视图 |
| `MiYouToastView` / `MiYouHudView` | 提示视图 |
| `MGSessionBoxViewController` | 会话列表增强控制器 |
| `MYRoomTool` | 群聊工具类 |
| `GroupTool` | 群组操作工具类 |
| `CheckHelper` | 好友检测辅助类 |

### 1.2 Hook 机制（2026-05-11 深度逆向结果）

插件使用 `method_setImplementation` 进行 Objective-C 方法替换（Native Method Swizzling），不使用 Logos/libsubstrate。

---

## 二、MiYou 完整 Hook 列表（从二进制逆向得出）

> 通过 `strings` 命令对 MiYou dylib 进行深度逆向，提取出所有与 swipe/session 相关的 hook 点。

### 2.1 核心手势 Hook（关键发现）

| # | Hook 方法 | 作用 | 重要性 |
|---|-----------|------|--------|
| **1** | **`translationInView:`** | **4x 放大 X 轴位移** | ⭐⭐⭐⭐⭐ 核心！让系统以为手指滑了更远 |
| **2** | **`velocityInView:`** | **4x 放大 X 轴速度** | ⭐⭐⭐⭐⭐ 核心！帮助系统快速确认 swipe 意图 |
| **3** | `setDelaysTouchesBegan:` | 强制 NO，阻止触摸延迟 | ⭐⭐⭐⭐ |
| **4** | `gestureRecognizerShouldBegin:` | 对 swipe 手势返回 YES | ⭐⭐⭐⭐ |
| **5** | `addGestureRecognizer:` | 截获 `_UISwipeActionPanGestureRecognizer` | ⭐⭐⭐⭐ |

### 2.2 UITableView/UIScrollView 属性 Hook

| # | Hook 方法 | 作用 |
|---|-----------|------|
| 6 | `setAllowsMultipleSelection:` | 强制 NO |
| 7 | `setDirectionalLockEnabled:` | 强制 NO，关闭方向锁定 |
| 8 | `setDataSource:` | 注入 leadingSwipe/canEditRow/editingStyle |
| 9 | `setDelegate:` | 注入 swipe 方法 |
| 10 | `setEditing:animated:` | 控制编辑模式 |
| 11 | `setAllowsEditing:` | 强制允许编辑 |

### 2.3 微信自定义属性 Hook

| # | Hook 方法 | 作用 |
|---|-----------|------|
| 12 | `setNeedEditState:` | 微信自定义编辑状态标志 |
| 13 | `setForbidDisplayMenuWithGestures:` | 解除手势菜单禁止 |
| 14 | `setMultiplexEnabled:` | 关闭多路复用 |
| 15 | `setIsAddGesture:` | 添加手势标志 |
| 16 | `setMIsSessionGesture:` | 会话手势标志 |
| 17 | `setM_bInteractivePopEnabled:` | 关闭交互式返回弹出 |
| 18 | `settingSessionGesture:` | 设置会话手势 |

### 2.4 UISwipeActionsConfiguration Hook

| # | Hook 方法 | 作用 |
|---|-----------|------|
| 19 | `setPerformsFirstActionWithFullSwipe:` | 强制 NO，防止全滑误操作 |

### 2.5 业务逻辑 Hook

| # | Hook 方法 | 作用 |
|---|-----------|------|
| 20 | `leadingSwipeActionsConfigurationForRowAtIndexPath:` | 返回置顶/备注/免打扰按钮 |
| 21 | `canEditRowAtIndexPath:` | 返回 YES |
| 22 | `editingStyleForRowAtIndexPath:` | 返回 Delete 样式 |

### 2.6 MiYou 没有的 Hook（之前错误添加）

| 方法 | 原因 |
|------|------|
| ~~`gestureRecognizer:shouldRecognizeSimultaneouslyWithGestureRecognizer:`~~ | MiYou 二进制中没有此 hook，返回 YES 反而让 pan 和 swipe 冲突 |
| ~~`gestureRecognizer:shouldRequireFailureOfGestureRecognizer:`~~ | MiYou 没有 |
| ~~`gestureRecognizer:shouldBeRequiredToFailByGestureRecognizer:`~~ | MiYou 没有 |

---

## 三、关键技术发现

### 3.1 translationInView 放大 — MiYou 的核心策略

**这是 MiYou 能灵敏响应的根本原因！**

```objc
// MiYou 的做法（从二进制逆向得出）
static CGPoint sb_amplifiedTranslationInView(id self, SEL _cmd, UIView *view){
    CGPoint pt = [self translationInView:view];  // 原始位移
    pt.x *= 4.0;  // X 轴放大 4 倍
    return pt;
}
```

**原理**：iOS 的 `_UISwipeActionPanGestureRecognizer` 有内部距离阈值（约 30-50px）。用户手指滑动 15px，系统看到 60px，轻松跨过阈值。

**之前的错误尝试**：
- KVC 修改 `_hysteresis`、`_minimumTranslation` 等私有属性 → 不可靠
- `gestureRecognizerShouldBegin:` 返回 YES → 只绕过 delegate 检查，不影响内部阈值

### 3.2 setDelaysTouchesBegan 全局 Hook

MiYou hook 了 `UIGestureRecognizer.setDelaysTouchesBegan:`，强制所有手势的 `delaysTouchesBegan = NO`。这确保触摸事件立即传递，不被延迟。

### 3.3 KVO 子类问题

微信的 delegate/dataSource 实际类是 `NSKVONotifying_NewMainFrameViewController`（KVO 动态子类），必须在 `setDataSource:/setDelegate:` 回调中用 `object_getClass(dg)` 获取真实类。

### 3.4 install 多次调用导致闪退

日志显示 `install` 被调用 3 次。每次都重置 `g_origIMPs`，第二次 install 时把**自己的 hook IMP 当作原始 IMP 保存**，形成无限递归 → 栈溢出闪退。

**修复**：添加 `static BOOL g_installed = NO;` 防护。

---

## 四、调试历程（v9 → v14）

| 版本 | 策略 | 结果 | 问题 |
|------|------|------|------|
| v9 | `gestureRecognizerShouldBegin:` 覆盖 | 滑动有概率触发 | 阈值问题未解决 |
| v10 | KVC 修改手势内部属性 | 无改善 | 属性名猜错，KVC 不可靠 |
| v11 | `translationInView:` 4x 放大 | 未测试 | 未部署 |
| v12 | 完整 MiYou hook 覆盖 | 未测试 | 未部署 |
| v13 | 添加 `shouldRecognizeSimultaneously` | 闪退 | install 多次调用 + 错误的 hook |
| **v14** | 修复闪退 + 移除错误 hook | 待测试 | 当前版本 |

---

## 五、WeChatPlugin 实现状态

### 5.1 已实现

| 功能 | 状态 | 文件 |
|------|------|------|
| `translationInView:` 4x 放大 | ✅ | WPSessionBoxHook.m |
| `velocityInView:` 4x 放大 | ✅ | WPSessionBoxHook.m |
| `setDelaysTouchesBegan:` hook | ✅ | WPSessionBoxHook.m |
| `gestureRecognizerShouldBegin:` hook | ✅ | WPSessionBoxHook.m |
| `addGestureRecognizer:` hook | ✅ | WPSessionBoxHook.m |
| `setDataSource:/setDelegate:` hook | ✅ | WPSessionBoxHook.m |
| `setAllowsMultipleSelection:` hook | ✅ | WPSessionBoxHook.m |
| `setDirectionalLockEnabled:` hook | ✅ | WPSessionBoxHook.m |
| `setPerformsFirstActionWithFullSwipe:` hook | ✅ | WPSessionBoxHook.m |
| `setNeedEditState:` hook | ✅ | WPSessionBoxHook.m |
| `setForbidDisplayMenuWithGestures:` hook | ✅ | WPSessionBoxHook.m |
| `setMultiplexEnabled:` hook | ✅ | WPSessionBoxHook.m |
| `setIsAddGesture:` hook | ✅ | WPSessionBoxHook.m |
| `setMIsSessionGesture:` hook | ✅ | WPSessionBoxHook.m |
| `setM_bInteractivePopEnabled:` hook | ✅ | WPSessionBoxHook.m |
| `leadingSwipeActions` 注入 | ✅ | WPSessionBoxHook.m |
| `canEditRow` 注入 | ✅ | WPSessionBoxHook.m |
| `editingStyle` 注入 | ✅ | WPSessionBoxHook.m |
| install 多次调用防护 | ✅ | WPSessionBoxHook.m |
| 置顶/取消置顶业务逻辑 | ✅ | WPSessionBoxHook.m |
| 备注修改业务逻辑 | ✅ | WPSessionBoxHook.m |
| 免打扰切换业务逻辑 | ✅ | WPSessionBoxHook.m |

### 5.2 待验证

- 从左往右滑动是否能灵敏触发（v14 待测试）

---

## 六、数据流与持久化

### 6.1 配置存储结构

所有功能配置通过 `PluginConfig` 类统一管理，持久化到 `NSUserDefaults`：

```
PluginConfig = {
    quickPinEnabled = YES;      // 快速置顶开关
    quickRemarkEnabled = YES;   // 快速备注开关
    quickMuteEnabled = YES;     // 快速免打扰开关
};
```

### 6.2 微信内部 API 调用链

```
插件方法                     微信内部类/方法
─────────                    ──────────────
置顶操作  ──────────────→   CContactMgr
                             ├── TopSessionByName:
                             ├── UntopSessionByName:
                             └── isContactSessionTop

备注操作  ──────────────→   CContact
                             ├── m_nsRemark (属性修改)
                             └── modifyDataItem:notify:

免打扰操作 ─────────────→   CContactMgr
                             └── ChangeNotifyStatus:withStatus:sync:

会话刷新  ──────────────→   SessionMgr
                             └── updateMainSessionList / resortSessions
```

---

## 七、总结

| 功能 | 实现方式 | 核心 Hook 点 | 核心 API |
|------|----------|-------------|----------|
| 快速置顶 | UISwipeActionsConfiguration 注入 | leadingSwipeActions | `TopSessionByName:` / `UntopSessionByName:` |
| 快速备注 | UIAlertController + CContact 属性修改 | leadingSwipeActions | `modifyDataItem:notify:` |
| 快速免打扰 | ChangeNotifyStatus 调用 | leadingSwipeActions | `ChangeNotifyStatus:withStatus:sync:` |

**关键发现**：MiYou 的灵敏响应来自 `translationInView:` 4x 放大，而非修改手势内部阈值。这是逆向分析得出的最重要结论。

---

## 八、版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1 | 2026-05-11 | 初始分析 |
| v2 | 2026-05-11 | 添加 WeChatPlugin 实现指南 |
| v3 | 2026-05-11 | 深度逆向 MiYou hook 列表 |
| v4 | 2026-05-11 | 添加 translationInView 核心发现、调试历程、v14 状态 |