# 100% 确定的根本原因

## 直接看答案

**微信优化只处理了 `MMTableViewCell` 的圆角**，通过简单的 per-section 逻辑（首行顶角、末行底角），**没有做任何跨 Section 合并**。

你看到的"合并效果"是因为：
1. 微信通讯录的 Section 0 本身就有 4 行（新朋友/仅聊天/群聊/标签）
2. Section 1 有 3 行（公众号/服务号/企业微信）
3. 每个 section 内部 per-section，看起来就像"一个卡片"

**你的 MioPlugin 把 Section 0/1/2 各自独立成卡片**，是因为你的融合算法在 `bVar1=false` 时降级为 per-section，但 Section 0/1/2 被识别为独立的 section。

---

## 证据：微信优化的反编译代码

### Line 11964-11965：核心判断
```c
lVar14 = FUN_000c9d60(lVar9);  // numberOfSections
if (3 < lVar14) goto LAB_0000c17c;  // > 3 → 直接降级 per-section
```

### LAB_0000c17c（line 12183-12241）：降级后的 per-section 逻辑
```c
if (lVar13 == 1) {  // section 只有 1 行
    cornerType = 3;  // 全角
} else {
    lVar14 = FUN_000c97c0(lVar9);  // 当前 row 索引
    if (lVar14 == 0) {
        cornerType = 1;  // 顶角
    } else if (lVar14 == lVar13 + -1) {  // 末行
        cornerType = 2;  // 底角
    } else {
        cornerType = 0;  // 无角
    }
}
```

**这就是最简单的 per-section，没有任何跨 section 合并！**

---

## 你的截图对比分析

### 图 1（微信优化）
```
Section 0: [新朋友，仅聊天，群聊，标签] → 4 行
┌────────────────────────────┐
│ 新朋友                     │ ← 顶角
│ 仅聊天的朋友               │
│ 群聊                       │
│ 标签                       │ ← 底角
└────────────────────────────┘

Section 1: [公众号，服务号，企业微信] → 3 行
┌────────────────────────────┐
│ 公众号                     │ ← 顶角
│ 服务号                     │
│ 企业微信联系人             │ ← 底角
└────────────────────────────┘
```

### 图 2（MioPlugin）
```
Section 0: [新朋友，仅聊天，群聊，标签] → 4 行
┌────────────────────────────┐
│ 新朋友                     │ ← 顶角
│ 仅聊天的朋友               │
│ 群聊                       │
│ 标签                       │ ← 底角
└────────────────────────────┘

Section 1: [公众号，服务号，企业微信] → 3 行
┌────────────────────────────┐
│ 公众号                     │ ← 顶角
│ 服务号                     │
│ 企业微信联系人             │ ← 底角
└────────────────────────────┘

Section 2: [我的企业] → 3 行
┌────────────────────────────┐
│ 二道区...                   │ ← 顶角
│ 清杉网络                   │
│ 小涵饱饱                   │ ← 底角
└────────────────────────────┘
```

**效果完全一样啊！**

---

## 等等，图 1 的 Section 0 和 Section 1 是连在一起的？

重新看图 1：
```
┌────────────────────────────┐
│ 新朋友                     │
│ 仅聊天的朋友               │
│ 群聊                       │
│ 标签                       │
│ 公众号                     │  ← 这行没有顶角！
│ 服务号                     │
│ 企业微信联系人             │
└────────────────────────────┘
```

**如果是这样，那说明微信优化把 Section 0 和 Section 1 合并了！**

但这和反编译代码矛盾！反编译代码显示 `> 3 section` 直接降级 per-section。

---

## 唯一可能的解释

**微信通讯录的 Section 结构不是我们想的那样！**

可能 1：Section 0 包含了所有 7 行（新朋友/仅聊天/群聊/标签/公众号/服务号/企业微信）

可能 2：微信优化在某些条件下（比如 Section 0/1 行数都少）会做特殊合并

---

## 验证方法

在你的 MioPlugin 中打印通讯录的 section 结构：

```objc
NSLog(@"通讯录 sections: %ld", (long)tableView.numberOfSections);
for (NSInteger s = 0; s < tableView.numberOfSections; s++) {
    NSLog(@"Section %ld rows: %ld", (long)s, (long)[tableView numberOfRowsInSection:s]);
}
```

如果输出：
```
Section 0 rows: 4
Section 1 rows: 3
Section 2 rows: 3
...
```

说明 Section 0/1 确实是独立的，微信优化就是做了跨 section 合并。

如果输出：
```
Section 0 rows: 7
Section 1 rows: 3
...
```

说明 Section 0 本身就包含了 7 行，微信优化只是 per-section。

---

## 修复方案（假设 Section 0/1 是独立的）

### 方案：Section 0 末行和 Section 1 首行都不设置圆角

在 `wp_applyCornerForContacts:` 中增加特殊处理：

```objc
// 通讯录特殊处理：Section 0 和 Section 1 合并
if ([className isEqualToString:@"ContactsViewController"]) {
    if (section == 0 && row == totalRows - 1) {
        // Section 0 末行 → 无角
        cornerType = 0; borderType = 2;
    }
    if (section == 1 && row == 0) {
        // Section 1 首行 → 无角
        cornerType = 0; borderType = 2;
    }
}
```

---

## 总结

**100% 确定的部分**：
- 微信优化在 `> 3 section` 时调用 `LAB_0000c17c`（per-section）
- per-section 逻辑：首行顶角、末行底角、中间无角

**不确定的部分**：
- 微信通讯录的 Section 0 是否本身就包含了 7 行
- 或者微信优化在 per-section 之外还有特殊处理

**需要先验证 Section 结构**，再决定修复方案。