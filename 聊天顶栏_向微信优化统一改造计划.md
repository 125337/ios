# 聊天顶栏 — 向微信优化统一改造计划

> 基于《聊天顶栏_微信优化对比分析文档.md》的差异清单，分步骤将我方实现向微信优化对齐
> 文档状态：仅规划，不涉及代码改动

---

## 改造总览

| 阶段 | 内容 | 涉及文件 | 优先级 |
|:---:|------|------|:---:|
| 1 | 默认数值统一 | `PluginConfig.m` | 🔴 高 |
| 2 | 新增 `silentLoadContactExtInfo` | `MioChatAvatarTitleView.m` | 🔴 高 |
| 3 | 设置页补充后缀格式UI | `SettingChatTopBarController.m` | 🔴 高 |
| 4 | Hook 恢复逻辑完善 | `ChatTopBarHook.m` | 🟡 中 |
| 5 | Mode 7 / Mode 6 布局修正 | `MioChatAvatarTitleView.m` | 🟡 中 |
| 6 | contact 属性与辅助方法对齐 | `MioChatAvatarTitleView.m` | 🟡 中 |
| 7 | 分隔符加载 dispatch_async 化 | `MioChatAvatarTitleView.m` | 🟢 低 |
| 8 | 弹窗增强 | `CSContactInfoPopoverController.m` | 🟢 低 |
| 9 | 分隔符存储格式改为文件路径 | `MioChatAvatarTitleView.m` + 设置页 | 🟢 低 |
| 10 | chatController 改为 weak | `MioChatAvatarTitleView.h` | 🟢 低 |

---

## 阶段一：默认数值统一

**目标**：所有配置项默认值与微信优化保持一致

**涉及文件**：`WeChatPlugin/Config/PluginConfig.m`

**改动清单**：

| 属性 | 当前默认值 | 改为 | 微信优化 key |
|------|-----------|------|-------------|
| `chatAvatarSize` | `34` | `30` | `avatar_size` |
| `chatAvatarSpacing` | `4` | `3` | `avatar_spacing` |
| `chatSeparatorSize` | `16` | `30` | `separator_size` |
| `chatNicknameFontSize` | `12` | `9` | `name_font_size` |
| `chatTitleViewWidth` | `200` | `210` | `title_view_width_v2` |
| `chatAvatarCornerRadius` | — | `100`（满圆） | `avatar_corner_radius` |

**注意事项**：
- 仅修改默认值，已存在的用户配置保持不变（`registerDefaults` 中修改）
- `chatSeparatorSize` 从 16 改为 30 后，分隔符文字字号会自动从 6.4→8pt 变为 12pt，与微信一致
- 该修改无需版本迁移，新用户直接使用新默认值

---

## 阶段二：新增 `silentLoadContactExtInfo` 延迟加载联系人信息

**目标**：当 `addTime==0` 时，通过静默创建 `ContactInfoViewController` 触发微信底层加载扩展信息，0.8s 后重试 `updateAvatars`

**涉及文件**：`WeChatPlugin/Modules/ChatTopBar/MioChatAvatarTitleView.m`

**改动步骤**：

### Step 2.1：在 `MioChatAvatarTitleView` 中新增方法

```objc
// 新增方法：静默加载联系人扩展信息
- (void)silentLoadContactExtInfo:(id)contact {
    if (!contact) return;

    // 1. 获取 ContactInfoViewController 类
    Class contactInfoVCClass = objc_getClass("ContactInfoViewController");
    if (!contactInfoVCClass) return;

    // 2. 创建实例
    id vc = [[contactInfoVCClass alloc] init];
    if (!vc) return;

    // 3. 设置 m_contact（触发微信加载扩展信息）
    SEL setContactSel = NSSelectorFromString(@"setM_contact:");
    if ([vc respondsToSelector:setContactSel]) {
        ((void (*)(id, SEL, id))objc_msgSend)(vc, setContactSel, contact);
    }

    // 4. 调用 viewDidLoad（触发微信底层加载逻辑）
    ((void (*)(id, SEL))objc_msgSend)(vc, @selector(viewDidLoad));

    // 5. 0.8秒后重试 updateAvatars
    dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(0.8 * NSEC_PER_SEC)),
                   dispatch_get_main_queue(), ^{
        [self updateAvatars];
    });
}
```

### Step 2.2：修改 `updateAvatars` 中添加时间的处理分支

**修改前**（当前代码）：
```objc
if (addTime > 0) {
    // 计算天数、显示
}
```

**修改后**：
```objc
if (addTime > 0) {
    // 计算天数、显示
} else {
    // addTime == 0：触发静默加载
    [self silentLoadContactExtInfo:contact];
}
```

**注意事项**：
- `ContactInfoViewController` 是微信私有类，需要通过 `objc_getClass` 获取
- `viewDidLoad` 方法调用会触发微信底层网络请求，必须在主线程调用
- 0.8s 延时是为了给微信足够时间完成网络请求和数据处理
- 如果 `silentLoadContactExtInfo` 中的类或方法不存在，静默失败即可，不影响主流程

---

## 阶段三：设置页补充后缀格式自定义 UI

**目标**：在设置页中增加"添加时间后缀格式"和"群成员数后缀格式"两个输入项

**涉及文件**：`WeChatPlugin/Settings/Controllers/SettingChatTopBarController.m`

**改动步骤**：

### Step 3.1：在 Card 1（功能开关区域）新增两个 UI 项

```
现有 Card 1:
  总开关 (Switch)
  头像点击反馈 (Switch)
  显示添加时间 (Switch)
  显示群聊人数 (Switch)
  显示模式 (分段选择)
  分隔符号 (按钮)
  管理黑名单 (按钮)
```

新增后：
```
  ...
  显示添加时间 (Switch)
  └─ 添加时间后缀格式 (TextField)  ← 新增，在"显示添加时间"下方
  显示群聊人数 (Switch)
  └─ 群成员数后缀格式 (TextField)  ← 新增，在"显示群聊人数"下方
  ...
```

### Step 3.2：后缀格式 TextField 的具体实现要求

```objc
// 添加时间后缀格式输入框
UITextField *addTimeSuffixField;
addTimeSuffixField.placeholder = @"%ld天";  // 占位提示
addTimeSuffixField.text = config.chatAddTimeSuffixFormat;

// 群成员数后缀格式输入框
UITextField *groupCountSuffixField;
groupCountSuffixField.placeholder = @"%u人"; // 占位提示
groupCountSuffixField.text = config.chatGroupMemberCountSuffix;
```

**保存逻辑**：
- 监听 `UIControlEventEditingChanged` 或 `textFieldDidEndEditing:`
- 将输入值写入 `config.chatAddTimeSuffixFormat` / `config.chatGroupMemberCountSuffix`
- 空字符串时恢复为 nil（使用默认格式）

**注意事项**：
- 两个 TextField 在对应的 Switch 关闭时建议灰色不可编辑，或用小字提示"开启开关后生效"
- 不需要在 PluginConfig 中新增属性（已经存在）
- 输入框建议使用 `UIKeyboardTypeDefault`，无特殊限制
- 注意已有的 layoutSubviews 需要在新增两个控件后重新计算 Card 高度

---

## 阶段四：Hook 恢复逻辑完善

**目标**：`viewWillAppear` 中增加功能关闭或黑名单匹配时恢复原始 titleView 的逻辑

**涉及文件**：`WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m`

**改动步骤**：

### Step 4.1：修改 `hook_viewWillAppear` 方法

**修改前**（当前代码逻辑）：
```
1. 检查 titleView 是否为 MioChatAvatarTitleView
2. 是 → 更新分隔符+布局+updateAvatars
3. 否 → 跳过
```

**修改后**：
```
1. 读取 config.showChatAvatar
2. 若 enabled=NO:
   a. 检查当前 titleView 是否为 MioChatAvatarTitleView
   b. 是 → 从 associatedObject 取出原始 titleView → 恢复设置
   c. 否 → 什么都不做
   d. return

3. 若 enabled=YES:
   a. 获取 contact → 获取 username
   b. 检查黑名单 isContactInBlacklist
   c. 若在黑名单中:
      - 检查当前 titleView 是否为 MioChatAvatarTitleView
      - 是 → 恢复原始 titleView
      - return
   d. 若不在黑名单中:
      - 检查当前 titleView 是否为 MioChatAvatarTitleView
      - 否 → 创建新的 MioChatAvatarTitleView → 设置为 titleView
      - 是 → loadSeparatorIcon + loadSeparatorText + setNeedsLayout + updateAvatars + setNeedsDisplay
```

### Step 4.2：从 associatedObject 取出原始 titleView 的方法

```objc
// 获取保存的原始 titleView
UIView *originalTitleView = objc_getAssociatedObject(baseMsgVC, kOriginalTitleViewKey);

// 恢复原始 titleView
if (originalTitleView) {
    baseMsgVC.navigationItem.titleView = originalTitleView;
}
```

**注意事项**：
- `kOriginalTitleViewKey` 在 `viewDidLoad` hook 中保存，`viewWillAppear` 中取出
- 恢复原始 titleView 后，MioChatAvatarTitleView 会被释放
- 如果已是原始 titleView，不需要重复恢复（加类型判断避免死循环）
- `isContactInBlacklist` 逻辑需与微信优化对齐（参见阶段六的黑名单逻辑）

---

## 阶段五：Mode 7 / Mode 6 布局修正

**目标**：重叠模式偏移量和Mode 6文字对齐与微信优化一致

**涉及文件**：`WeChatPlugin/Modules/ChatTopBar/MioChatAvatarTitleView.m`

### Step 5.1：Mode 7 右头像 x 偏移修改

**修改位置**：`layoutSubviews` 方法中 Mode 7 分支

```objc
// 修改前（L129）:
CGFloat offset = avatarSize * 0.3;

// 修改后:
CGFloat offset = avatarSize * 0.7;
```

### Step 5.2：Mode 7 右头像 y 偏移修改

```objc
// 修改前（L130）:
self.rightAvatarView.frame = CGRectMake(offset, avatarY + avatarSize - smallSize + (avatarSize * 0.15), smallSize, smallSize);

// 修改后:
self.rightAvatarView.frame = CGRectMake(offset, avatarY + (avatarSize - smallSize), smallSize, smallSize);
```

### Step 5.3：Mode 7 右头像 clipsToBounds 设置

```objc
// 微信优化的 rightAvatarView 设置 clipsToBounds=NO
// 建议在 setupSubviews 中改为:
[ra setClipsToBounds:NO];

// 或在 layoutSubviews Mode 7 分支中临时设置:
self.rightAvatarView.clipsToBounds = NO;
```

### Step 5.4：Mode 6 文字对齐修改

**修改位置**：`layoutSubviews` 方法中 Mode 6 分支

```objc
// 在 Mode 6 分支中增加:
self.titleLabel.textAlignment = NSTextAlignmentRight;
```

### Step 5.5：Mode 1 显示分隔符/分隔文字

**修改位置**：`layoutSubviews` 方法中 Mode 1 分支

```objc
// 修改前：Mode 1 隐藏 separatorView 和 separatorTextLabel
// 修改后：Mode 1 显示 separatorView 和 separatorTextLabel（用于展示群人数/添加时间）
self.separatorView.hidden = NO;
self.separatorTextLabel.hidden = !config.chatSeparatorText.length;
```

**注意事项**：
- Mode 7 偏移量改为 0.7 后，重叠程度更大，需要确认视觉效果
- Mode 7 右头像 clipsToBounds=NO 可能影响其他 Mode 的显示（其他 Mode 右头像也可能溢出）
- Mode 1 显示分隔符后，需要重新计算 layout（单头像 + 分隔文字/分隔符图标 居中布局）

---

## 阶段六：contact 属性与辅助方法对齐

**目标**：`addTime` 属性名、`chatController` 引用方式、寻找 ViewController 方式与微信优化对齐

**涉及文件**：`MioChatAvatarTitleView.h`、`MioChatAvatarTitleView.m`

### Step 6.1：chatController 改为 weak 引用

**修改文件**：`MioChatAvatarTitleView.h`

```objc
// 修改前:
@property (nonatomic, assign) id chatController;

// 修改后:
@property (nonatomic, weak) id chatController;
```

### Step 6.2：验证 addTime 属性名

**修改文件**：`MioChatAvatarTitleView.m`，`updateAvatars` 方法中

```objc
// 当前代码:
if ([contact respondsToSelector:@selector(m_uiAddTime)]) {
    addTime = (unsigned int)((unsigned int (*)(id, SEL))objc_msgSend)(contact, @selector(m_uiAddTime));
}

// 需要验证微信是否有 m_uiAddCreateTime 属性
// 如果有，使用 m_uiAddCreateTime；否则保持 m_uiAddTime
// 验证方法：运行时检查
if ([contact respondsToSelector:NSSelectorFromString(@"m_uiAddCreateTime")]) {
    addTime = (unsigned int)((unsigned int (*)(id, SEL))objc_msgSend)(contact, NSSelectorFromString(@"m_uiAddCreateTime"));
} else if ([contact respondsToSelector:@selector(m_uiAddTime)]) {
    addTime = (unsigned int)((unsigned int (*)(id, SEL))objc_msgSend)(contact, @selector(m_uiAddTime));
}
```

### Step 6.3：增加 `findViewController` 方法

```objc
// 新增方法：从 view 向上遍历找到所属 UIViewController
- (UIViewController *)findViewController {
    UIResponder *responder = self;
    while (responder) {
        if ([responder isKindOfClass:[UIViewController class]]) {
            return (UIViewController *)responder;
        }
        responder = [responder nextResponder];
    }
    return nil;
}
```

### Step 6.4：修改 `presentUserInfoPopoverWithContact:` 使用 `findViewController`

```objc
// 修改前:
UIViewController *presentingVC = (UIViewController *)self.chatController;

// 修改后:
UIViewController *presentingVC = [self findViewController];
if (!presentingVC) {
    presentingVC = (UIViewController *)self.chatController;
}
```

**注意事项**：
- `chatController` 改为 `weak` 需要检查所有引用处是否有 `nil` 处理
- `findViewController` 使用 `nextResponder` 链式查找，比直接用 chatController 更健壮
- 如果找不到 ViewController，回退到 chatController 作为 fallback

---

## 阶段七：分隔符加载 dispatch_async 化

**目标**：所有分隔符加载通过 `dispatch_async` 回到主线程执行，防止跨线程 UI 更新问题

**涉及文件**：`WeChatPlugin/Modules/ChatTopBar/MioChatAvatarTitleView.m`

### Step 7.1：修改 `loadSeparatorIcon`

```objc
// 修改前：直接在主线程同步加载
// 修改后：包裹在 dispatch_async 中

- (BOOL)loadSeparatorIcon {
    NSData *iconData = [[NSUserDefaults standardUserDefaults]
        dataForKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorIcon"]];
    if (!iconData) return NO;

    dispatch_async(dispatch_get_main_queue(), ^{
        UIImage *icon = [UIImage imageWithData:iconData];
        self.separatorView.image = icon;
        self.separatorView.hidden = NO;
        [self.separatorView sizeToFit];
    });
    return YES;
}
```

### Step 7.2：修改 `loadSeparatorText`

```objc
// 同理包裹
- (void)loadSeparatorText {
    PluginConfig *config = [PluginConfig shared];
    if (config.chatSeparatorText.length > 0) {
        dispatch_async(dispatch_get_main_queue(), ^{
            self.separatorTextLabel.text = config.chatSeparatorText;
            self.separatorTextLabel.hidden = NO;
            [self.separatorTextLabel sizeToFit];
        });
    }
}
```

### Step 7.3：修改 `loadSeparatorGIF`

```objc
// 同理包裹，且 GIF 解析改用 createAnimatedImageFromGIFData
- (BOOL)loadSeparatorGIF {
    NSData *gifData = [[NSUserDefaults standardUserDefaults]
        dataForKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorGIF"]];
    if (!gifData) return NO;

    dispatch_async(dispatch_get_main_queue(), ^{
        UIImage *gifImage = [self createAnimatedImageFromGIFData:gifData];
        if (gifImage) {
            self.separatorView.image = gifImage;
            self.separatorView.hidden = NO;
            [self.separatorView sizeToFit];
        }
    });
    return YES;
}
```

**注意事项**：
- 如果当前已在主线程，`dispatch_async(main_queue)` 中的 block 会在当前 runloop 结束时执行
- 如果在主线程直接调用这些方法，`dispatch_async` 不会产生明显的延迟
- 如果从后台线程调用（如网络回调），确保 UI 更新在主线程

---

## 阶段八：弹窗增强

**目标**：弹窗与微信优化对齐

**涉及文件**：`WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m`

### Step 8.1：`permittedArrowDirections` 改为全部方向

```objc
// 修改前:
popPC.permittedArrowDirections = UIPopoverArrowDirectionUp;

// 修改后:
popPC.permittedArrowDirections = UIPopoverArrowDirectionAny;
// 或直接使用微信的 0xf (UIPopoverArrowDirectionUp | Down | Left | Right)
```

### Step 8.2：弹窗内增加触觉反馈

```objc
// 在 tableView:didSelectRowAtIndexPath: 中增加
- (void)tableView:(UITableView *)tableView didSelectRowAtIndexPath:(NSIndexPath *)indexPath {
    [tableView deselectRowAtIndexPath:indexPath animated:YES];

    // 新增：触觉反馈
    UIImpactFeedbackGenerator *feedback = [[UIImpactFeedbackGenerator alloc] initWithStyle:UIImpactFeedbackStyleLight];
    [feedback impactOccurred];

    // 原有逻辑...
}
```

### Step 8.3：增加复制成功提示

```objc
// 新增方法
- (void)showCopySuccessToast {
    UILabel *toast = [[UILabel alloc] init];
    toast.text = @"已复制";
    toast.textAlignment = NSTextAlignmentCenter;
    toast.backgroundColor = [[UIColor blackColor] colorWithAlphaComponent:0.7];
    toast.textColor = [UIColor whiteColor];
    toast.font = [UIFont systemFontOfSize:14];
    toast.layer.cornerRadius = 6;
    toast.clipsToBounds = YES;

    [toast sizeToFit];
    CGFloat w = toast.frame.size.width + 20;
    CGFloat h = toast.frame.size.height + 10;
    toast.frame = CGRectMake((self.view.bounds.size.width - w) * 0.5,
                              self.view.bounds.size.height * 0.8,
                              w, h);

    [self.view addSubview:toast];

    dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(1.0 * NSEC_PER_SEC)),
                   dispatch_get_main_queue(), ^{
        [UIView animateWithDuration:0.3 animations:^{
            toast.alpha = 0;
        } completion:^(BOOL finished) {
            [toast removeFromSuperview];
        }];
    });
}
```

### Step 8.4（可选）：增加群聊/公众号专用 Section

```
// 如果有时间和需求，可增加:
- (void)addGroupInfoSection_   // 群聊信息 section
- (void)addOfficialAccountInfoSection_  // 公众号信息 section
```

**注意事项**：
- 改用 `UIPopoverArrowDirectionAny` 后需要注意 popover 可能出现在屏幕之外
- 触觉反馈可复用已有的 `playHapticFeedback` 方法
- 复制成功提示使用简洁的 Toast 样式

---

## 阶段九：分隔符存储格式改为文件路径

**目标**：分隔符图标/GIF 存储方式从 NSData 改为文件路径

**涉及文件**：`MioChatAvatarTitleView.m`、`SettingChatTopBarController.m`

### Step 9.1：保存时存文件路径而非 NSData

**修改文件**：设置页中分隔符选择回调

```objc
// 修改前:
NSData *data = [NSData dataWithContentsOfFile:pickedPath];
[[NSUserDefaults standardUserDefaults] setObject:data forKey:...];

// 修改后:（直接存路径）
[[NSUserDefaults standardUserDefaults] setObject:pickedPath forKey:...];
```

### Step 9.2：加载时从文件路径加载

```objc
// 修改后:
- (BOOL)loadSeparatorIcon {
    NSString *iconPath = [[NSUserDefaults standardUserDefaults]
        stringForKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorIcon"]];
    if (!iconPath.length) return NO;

    dispatch_async(dispatch_get_main_queue(), ^{
        NSData *data = [NSData dataWithContentsOfFile:iconPath];
        if (data) {
            UIImage *icon = [UIImage imageWithData:data];
            self.separatorView.image = icon;
            self.separatorView.hidden = NO;
            [self.separatorView sizeToFit];
        }
    });
    return YES;
}
```

### Step 9.3：loadSeparatorIcon 内合并 GIF 检查

```objc
// 微信优化在 loadSeparatorIcon 同方法内先检查 GIF，再检查静态图片
- (BOOL)loadSeparatorIcon {
    // 1. 先尝试 GIF
    NSString *gifPath = [[NSUserDefaults standardUserDefaults]
        stringForKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorGIF"]];
    if (gifPath.length && [[NSFileManager defaultManager] fileExistsAtPath:gifPath]) {
        dispatch_async(dispatch_get_main_queue(), ^{
            NSData *data = [NSData dataWithContentsOfFile:gifPath];
            UIImage *gifImage = [self createAnimatedImageFromGIFData:data];
            if (gifImage) {
                self.separatorView.image = gifImage;
                self.separatorView.hidden = NO;
                [self.separatorView sizeToFit];
            }
        });
        return YES;
    }

    // 2. 再尝试静态图片
    NSString *iconPath = [[NSUserDefaults standardUserDefaults]
        stringForKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorIcon"]];
    if (iconPath.length && [[NSFileManager defaultManager] fileExistsAtPath:iconPath]) {
        dispatch_async(dispatch_get_main_queue(), ^{
            NSData *data = [NSData dataWithContentsOfFile:iconPath];
            UIImage *icon = [UIImage imageWithData:data];
            self.separatorView.image = icon;
            self.separatorView.hidden = NO;
            [self.separatorView sizeToFit];
        });
        return YES;
    }

    return NO;
}
```

**注意事项**：
- 需要同时修改设置页中的分隔符选择保存逻辑
- 存储路径前需要确保文件持久存在（如果用临时路径会失败）
- 如果改为文件路径存储，现有用户的 NSData 数据需要做迁移兼容

---

## 阶段十：chatController 改为 weak（与阶段六联动）

已在阶段六中描述，此处不重复。

---

## 执行建议

### 推荐执行顺序

```
阶段一 (默认值统一)
    ↓
阶段二 (silentLoadContactExtInfo)
    ↓
阶段三 (后缀格式UI)
    ↓
阶段四 (Hook恢复逻辑)  ← 可以与其他阶段并行
    ↓
阶段五 (布局修正)
    ↓
阶段六 (属性对齐)
    ↓
阶段七 (dispatch_async)
    ↓
阶段八 (弹窗增强)
    ↓
阶段九 (存储格式)
    ↓
阶段十 (weak引用)
```

### 测试验证清单

每完成一个阶段，建议验证以下内容：

| 验证项 | 方法 |
|--------|------|
| 默认值生效 | 卸载重装或清除 defaults 后检查设置页默认值 |
| 添加时间显示 | 找一个已添加X天的联系人，确认显示正确 |
| 新联系人添加时间 | 新添加好友，进入聊天页，等待 1-2 秒后确认显示 |
| 后缀格式自定义 | 在设置页输入自定义格式，聊天顶栏显示正确 |
| 功能关闭恢复 | 关闭总开关 → 返回聊天页 → 确认恢复原始顶栏 |
| 黑名单恢复 | 添加某人到黑名单 → 进入聊天页 → 确认恢复原始顶栏 |
| Mode 7 重叠 | 切换到 Mode 7，确认两个头像重叠效果 |
| Mode 6 文字对齐 | 切换到 Mode 6，确认名字靠右显示 |
| 弹窗箭头方向 | 在不同位置点击头像，确认弹窗箭头方向自适应 |
| 弹窗复制提示 | 长按弹窗内的行，确认提示显示 |
| 分隔符 GIF | 设置 GIF 分隔符，确认动画播放 |
| 弱引用释放 | 退出聊天页，确认 MioChatAvatarTitleView 正常释放 |

### 风险评估

| 风险 | 阶段 | 缓解措施 |
|------|:---:|------|
| `silentLoadContactExtInfo` 触发微信异常 | 2 | 使用 try-catch 包裹，失败时静默跳过 |
| 默认值修改影响老用户体验 | 1 | 仅改 `registerDefaults`，已有配置不受影响 |
| Hook 恢复逻辑导致 titleView 闪烁 | 4 | 增加类型判断，避免重复设置 |
| Mode 7 重叠改动视觉效果不佳 | 5 | 提供设置页预览功能 |
| 文件路径迁移导致分隔符丢失 | 9 | 先做数据迁移，兼容旧 NSData 格式 |

---

*文档生成时间：2026-05-27*
*基于：聊天顶栏_微信优化对比分析文档.md*