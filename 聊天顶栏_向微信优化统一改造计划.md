# 聊天顶栏 — 向微信优化统一改造计划

> 基于《聊天顶栏_微信优化对比分析文档.md》的差异清单
> 编译环境：**MRC**
> 微信默认配置：已从微信优化设置页直接确认
> 更新日期：2026-05-27

---

## 微信优化默认配置（权威来源）

| # | 设置项 | 微信默认值 |
|---|--------|-----------|
| 1 | 头像圆角程度 | **100%**（满圆） |
| 2 | 双方头像大小 | **30px** |
| 3 | 分隔符大小 | **30px** |
| 4 | 网名字体大小 | **9px** |
| 5 | 双方头像间距 | **6px** |
| 6 | 整体上下偏移 | **0px** |
| 7 | 整体水平偏移 | **0px** |
| 8 | 网名上下偏移 | **0px** |
| 9 | 网名水平偏移 | **0px** |
| 10 | 视图宽度 | **210px** |

---

## 改造进度总览

| 阶段 | 内容 | 涉及文件 | 状态 |
|:---:|------|------|:---:|
| 1 | 默认数值统一 | `PluginConfig.m` | ⚠️ **需修正2项** |
| 2 | 新增 `silentLoadContactExtInfo` | `MioChatAvatarTitleView.m` | ✅ 已完成 |
| 3 | 设置页补充后缀格式UI | `SettingChatTopBarController.m` | ✅ 已完成 |
| 4 | Hook 恢复逻辑完善 | `ChatTopBarHook.m` | ✅ 已完成 |
| 5 | 部分布局修正 | `MioChatAvatarTitleView.m` | ⚠️ 部分完成 |
| 6 | 属性与辅助方法 | `MioChatAvatarTitleView.m` | ⚠️ 含 bug |
| 7 | 分隔符 dispatch_async | `MioChatAvatarTitleView.m` | ✅ 已完成 |
| 8 | 弹窗增强 | `CSContactInfoPopoverController.m` | ❌ 待做 |
| 9 | 分隔符存储改文件路径 | `MioChatAvatarTitleView.m` + 设置页 | ❌ 待做 |

---

## 阶段一：默认数值统一  ⚠️ 需修正2项

**文件**：`WeChatPlugin/Config/PluginConfig.m`

### 已正确修改

| 属性 | 值 | 状态 |
|------|------|:---:|
| `chatAvatarSize` | 30.0 | ✅ |
| `chatAvatarCornerRadius` | 100.0 | ✅ |
| `chatNicknameFontSize` | 9.0 | ✅ |
| `chatTitleViewWidth` | 210.0 | ✅ |

### 需要修正

| 属性 | 当前值 | 应改为 | 差异 |
|------|--------|--------|------|
| **`chatAvatarSpacing`** | **3.0** | **6.0** | 差3px |
| **`chatSeparatorSize`** | **12.0** | **30.0** | 差18px |

**具体修改**（`PluginConfig.m` 的 `loadDefaults` 或 `registerDefaults` 中）：

```objc
// 修改前
self.chatAvatarSpacing = 3.0;
self.chatSeparatorSize = 12.0;

// 修改后
self.chatAvatarSpacing = 6.0;
self.chatSeparatorSize = 30.0;
```

**影响说明**：
- `chatAvatarSpacing` 3→6：Mode 2/5/6/7 中头像间距增大一倍，与微信视觉效果一致
- `chatSeparatorSize` 12→30：分隔符图标从12x12变为30x30，分隔符文字从 `12*0.4=4.8→8pt(clamped)` 变为 `30*0.4=12pt`，文本更大更清晰

---

## 阶段二：silentLoadContactExtInfo  ✅ 已完成

**文件**：`MioChatAvatarTitleView.m`（L467-491）

```
已验证：createContactVC → setM_contact → viewDidLoad → 0.8s→updateAvatars
MRC：block 中 [vc release] 正确 ✅
```

---

## 阶段三：后缀格式 UI  ✅ 已完成

**文件**：`SettingChatTopBarController.m`

```
已验证：UIAlertController 输入 → PluginConfig 保存 ✅
```

---

## 阶段四：Hook 恢复逻辑  ✅ 已完成

**文件**：`ChatTopBarHook.m`

```
已验证：enabled=NO 恢复、黑名单恢复、重建逻辑 ✅
```

---

## 阶段五：布局修正  ⚠️ 部分完成

**文件**：`MioChatAvatarTitleView.m`

| 项 | 状态 |
|----|:---:|
| `rightAvatarView.clipsToBounds = NO` | ✅ |
| Mode 6 `textAlignment = Right` | ✅ |
| Mode 7 x 偏移 0.7 | ❌ 用户保持 0.3 |
| Mode 1 显示分隔符 | ❌ 用户未改 |

---

## 阶段六：属性与辅助方法  ⚠️ 含 bug

**文件**：`MioChatAvatarTitleView.m`

### 🔴 crash bug

```objc
// MioChatAvatarTitleView.m L634
UIViewController *presentingVC = [self findViewController];
```

`findViewController` 方法**未定义**，点击头像时 **selector not recognized → crash**。

修复方法（在 `MioChatAvatarTitleView.m` 末尾添加）：

```objc
- (UIViewController *)findViewController {
    UIResponder *responder = self;
    while (responder) {
        if ([responder isKindOfClass:[UIViewController class]]) {
            return (UIViewController *)responder;
        }
        responder = [responder nextResponder];
    }
    return (UIViewController *)self.chatController; // fallback
}
```

### ⚠️ addTime 属性名需验证

当前使用 `m_uiAddTime`，微信优化使用 `m_uiAddCreateTime`。需要运行时验证哪个属性存在且有值。

---

## 阶段七：分隔符 dispatch_async  ✅ 已完成

```
已验证：loadSeparatorIcon / loadSeparatorText / loadSeparatorGIF ✅
```

---

## 阶段八：弹窗增强  ❌ 待做

**文件**：`CSContactInfoPopoverController.m`

### 8.1 permittedArrowDirections 改为全方向

```objc
// L629 修改前
popPC.permittedArrowDirections = UIPopoverArrowDirectionUp;
// 修改后
popPC.permittedArrowDirections = UIPopoverArrowDirectionAny;
```

### 8.2 弹窗内触觉反馈

```objc
- (void)tableView:(UITableView *)tableView didSelectRowAtIndexPath:(NSIndexPath *)indexPath {
    [tableView deselectRowAtIndexPath:indexPath animated:YES];
    
    UIImpactFeedbackGenerator *feedback = [[UIImpactFeedbackGenerator alloc]
        initWithStyle:UIImpactFeedbackStyleLight];
    [feedback impactOccurred];
    // 原有逻辑...
}
```

### 8.3 复制成功 Toast

```objc
- (void)showCopySuccessToast {
    UILabel *toast = [[UILabel alloc] init];
    toast.text = @"已复制";
    toast.textAlignment = NSTextAlignmentCenter;
    toast.font = [UIFont systemFontOfSize:14];
    toast.textColor = [UIColor whiteColor];
    toast.backgroundColor = [[UIColor blackColor] colorWithAlphaComponent:0.7];
    toast.layer.cornerRadius = 6;
    toast.clipsToBounds = YES;
    [toast sizeToFit];
    CGFloat w = toast.frame.size.width + 20, h = toast.frame.size.height + 10;
    toast.frame = CGRectMake((self.view.bounds.size.width - w) * 0.5,
                              self.view.bounds.size.height * 0.8, w, h);
    [self.view addSubview:toast];

    dispatch_after(dispatch_time(DISPATCH_TIME_NOW, 1.0 * NSEC_PER_SEC),
                   dispatch_get_main_queue(), ^{
        [UIView animateWithDuration:0.3 animations:^{ toast.alpha = 0; }
                       completion:^(BOOL f) { [toast removeFromSuperview]; }];
    });
}
```

### 8.4（可选）群聊/公众号专用 section

---

## 阶段九：分隔符存储改文件路径  ❌ 待做

**文件**：`MioChatAvatarTitleView.m` + 设置页

```objc
// 保存时存路径
[defaults setObject:filePath forKey:@"ChatSeparatorIcon"];
// 加载时从路径读
NSString *path = [defaults stringForKey:@"ChatSeparatorIcon"];
NSData *data = [NSData dataWithContentsOfFile:path];
```

需要兼容旧 NSData 格式的迁移逻辑。

---

## 当前优先执行清单

| 优先级 | 操作 | 文件 |
|:---:|------|------|
| 🔴 | 添加 `findViewController` 方法 | `MioChatAvatarTitleView.m` |
| 🟡 | `chatAvatarSpacing` 3.0 → **6.0** | `PluginConfig.m` |
| 🟡 | `chatSeparatorSize` 12.0 → **30.0** | `PluginConfig.m` |
| 🟡 | 验证 `m_uiAddCreateTime` vs `m_uiAddTime` | `MioChatAvatarTitleView.m` |
| 🟡 | `permittedArrowDirections` Up→Any | `CSContactInfoPopoverController.m` |
| 🟢 | 弹窗内触觉反馈 + 复制Toast | `CSContactInfoPopoverController.m` |
| 🟢 | 分隔符存文件路径 + 旧格式兼容 | 设置页 + TitleView |

---

### 此前我分析错误的修正记录

| 配置项 | 我之前写的 | 微信实际 | 错因 |
|--------|----------|---------|------|
| 双方头像间距 | **3px** | **6px** | 反编译分析出错 |
| 分隔符大小 | 写对30但后来自疑撤回 | **30px** | 二次复查自我怀疑 |

---

*文档生成时间：2026-05-27*
*基于：聊天顶栏_微信优化对比分析文档.md*
*微信默认值来源：微信优化设置页直接确认*