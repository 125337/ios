# 好友检测功能对比分析

## 一、架构总览

### 1.1 微信优化 1.6.5

```
App 启动
  ↓
FriendDetector 类注册（编译时 @implementation）
  ├── 属性: checkedFriendWxIDs, checkFriendsEnd, friendCheckSem, deletedFriends, lastCheckIndex, totalFriendsCount
  ├── 实例方法: allFriends → MMServiceCenter → CContactMgr → getContactList:contactType:
  ├── 实例方法: checkFriendsWithCompletion: → allFriends + checkSpecificFriends:completion:
  ├── 实例方法: checkSpecificFriends:completion: → 调 WeChat 原生 API
  ├── 实例方法: loadDetectionResults → NSUserDefaults.load
  └── 实例方法: showResultNotification: → 弹窗/通知

FriendDetectResult 类注册（编译时 @implementation）
  ├── 属性: contact (CContact), isDeleted (BOOL), isInvalid (BOOL)
  └── 类方法: infoWithContact:isDeleted:isInvalid:

CSFriendDetectionViewController（UI 页）
  ├── showResultNotification:
  └── 显示检测结果列表

Native WeChat 参与:
  FriendDetector class（原生）→ checkSpecificFriends:completion:
    → agreeDuty 机制（服务端验证好友关系）
  WeChatFriendDetector class（原生备用）
```

### 1.2 我们的实现

```
App 启动
  ↓
MioFriendDetector 类注册（编译时 @implementation）
  ├── 属性: checkedFriendWxIDs, checkFriendsEnd, friendCheckSem
  ├── 实例方法: allFriends → MMServiceCenter → CContactMgr → getContactList:contactType:
  ├── 实例方法: checkSpecificFriends:completion: → getContactInfo: → getContactByName: → CContact 属性检测
  └── 实例方法: checkFriendsWithCompletion: → allFriends + checkSpecificFriends:completion:

MioFriendDetectResult 类注册（编译时 @implementation）
  ├── 属性: contact, isDeleted, isInvalid
  └── 类方法: infoWithContact:isDeleted:isInvalid:

WPFriendDetectionVC（UI 页，runtime 生成）
  ├── 开始检测按钮
  ├── 查看上次结果按钮
  └── UIAlertController 弹窗显示
```

---

## 二、类定义对比

### 2.1 FriendDetector vs MioFriendDetector

| 对比项 | 微信优化 1.6.5 | 我们的实现 | 状态 |
|--------|:-------------:|:---------:|:----:|
| 类名 | `FriendDetector` | `MioFriendDetector` | ✅ 避免冲突 |
| 是否编译 | `@implementation` 编译 | `@implementation` 编译 | ✅ 一致 |
| `allFriends` 方法 | ✅ | ✅ | ✅ 一致 |
| `checkSpecificFriends:completion:` | ✅ 调原生 API | ✅ 本地属性检测 | ⚠️ 检测方式不同 |
| `checkFriendsWithCompletion:` | ✅ | ✅ | ✅ 一致 |
| `checkFriendsWithOptions:completion:` | ✅ | ❌ 未实现 | ❌ 缺失 |
| `checkSpecificFriends:` (无 block) | ✅ | ✅ | ✅ 一致 |
| `checkFriendsEnd` | ✅ BOOL 属性 | ✅ BOOL 属性 | ✅ 一致 |
| `friendCheckSem` | ✅ semaphore 属性 | ✅ semaphore 属性 | ✅ 一致 |
| `checkedFriendWxIDs` | ✅ NSArray 属性 | ✅ NSArray 属性 | ✅ 一致 |
| `deletedFriends` | ✅ NSMutableArray | ❌ 在 startFriendDetection 局部 | ⚠️ |
| `totalFriendsCount` | ✅ int 属性 | ❌ 未实现 | ❌ 缺失 |
| `lastCheckIndex` | ✅ int 属性 | ❌ 未实现 | ❌ 缺失 |
| `loadDetectionResults` | ✅ | ❌ 未实现 | ❌ 缺失 |
| `showResultNotification:` | ✅ 类方法 | ❌ 未实现 | ❌ 缺失 |

### 2.2 FriendDetectResult vs MioFriendDetectResult

| 对比项 | 微信优化 1.6.5 | 我们的实现 | 状态 |
|--------|:-------------:|:---------:|:----:|
| 类名 | `FriendDetectResult` | `MioFriendDetectResult` | ✅ 避免冲突 |
| `contact` 属性 | ✅ | ✅ | ✅ 一致 |
| `isDeleted` 属性 | ✅ | ✅ | ✅ 一致 |
| `isInvalid` 属性 | ✅ | ✅ | ✅ 一致 |
| `infoWithContact:isDeleted:isInvalid:` | ✅ | ✅ | ✅ 一致 |

---

## 三、检测流程对比

### 3.1 微信优化 1.6.5

```
用户点击"开始检测"
  ↓
checkFriendsWithCompletion:（后台线程）
  ↓
allFriends → MMServiceCenter.defaultCenter → CContactMgr.getService:
  → getContactList:contactType: → 2940 contacts → 过滤出 2688 好友
  ↓
checkSpecificFriends:completion:（后台线程）
  ↓
对每个 WX ID:
  1. 找原生 FriendDetector / WeChatFriendDetector 类
  2. 调 checkSpecificFriends:completion:（类方法）
  3. 原生类内部: agreeDuty 机制 → 服务端验证好友关系
  4. 返回 FriendDetectResult (contact, isDeleted, isInvalid)
  ↓
汇总结果 → 保存 NSUserDefaults → 发送 notification
  ↓
UI 刷新显示
```

**核心检测机制**: 依赖 WeChat 原生 `FriendDetector` 类的 `checkSpecificFriends:completion:` 方法。
该方法调用 WeChat 服务端的 `agreeDuty` 接口，服务端验证好友关系后返回结果。
**如果原生类不存在或方法不存在，检测完全跳过，返回空结果。**

### 3.2 我们的实现

```
用户点击"开始检测"
  ↓
startFriendDetection()（后台线程）
  ↓
MioFriendDetector.allFriends → MMServiceCenter → CContactMgr → getContactList → 2688 好友
  ↓
MioFriendDetector.checkSpecificFriends:completion:
  ↓
对每个 WX ID:
  1. 尝试 getContactInfo:（可能服务端获取）
  2. 回退 getContactByName:（本地缓存）
  3. CContact 属性检测:
     - m_uiVerifyFlag > 0 ? → deleted
     - nickname 为空或等于 wxID ? → deleted
     - 否则 → valid
  ↓
汇总结果 → 保存 NSUserDefaults → 弹窗显示
```

### 3.3 关键差异

| 步骤 | 微信优化 1.6.5 | 我们的实现 | 影响 |
|------|:-------------:|:---------:|:----:|
| 好友获取 | `allFriends` 方法 | `allFriends` 方法 | ✅ 一致 |
| WX ID提取 | 过滤 @chatroom/gh_ | 过滤 @chatroom/gh_ | ✅ 一致 |
| 检测方式 | **服务端验证** (`agreeDuty`) | **本地属性检查** (verifyFlag) | ⚠️ 根本差异 |
| 检测类 | WeChat 原生 `FriendDetector` | 无（本地`MioFriendDetector`） | ⚠️ |
| 网络请求 | 有（服务端验证） | 无（纯本地） | ⚠️ |
| 检测准确性 | **高**（服务端真实数据） | **低**（本地缓存可能滞后） | ❌ |
| 兼容性 | 依赖 WeChat 版本 | 不依赖 WeChat 版本 | ✅ |
| 稳定性 | 有 crash 风险（方法签名不匹配） | 稳定 | ✅ |

---

## 四、属性/ivar 完整列表

### 4.1 微信优化 1.6.5 的 FriendDetector 属性

| 属性名 | 类型 | 说明 |
|--------|------|------|
| `checkedFriendWxIDs` | `NSArray` (copy, nonatomic) | 已检测的好友 WX ID 列表 |
| `checkFriendsEnd` | `BOOL` | 检测是否完成 |
| `friendCheckSem` | `dispatch_semaphore_t` | 同步信号量 |
| `deletedFriends` | `NSMutableArray` (strong, nonatomic) | 累计的删除好友列表 |
| `lastCheckIndex` | `int` | 上次检测到第几个（进度跟踪） |
| `totalFriendsCount` | `int` | 总好友数 |

### 4.2 我们的 MioFriendDetector 属性

| 属性名 | 类型 | 说明 | 状态 |
|--------|------|------|:----:|
| `checkedFriendWxIDs` | `NSArray` (strong, nonatomic) | 已检测的好友 WX ID 列表 | ✅ |
| `checkFriendsEnd` | `BOOL` | 检测是否完成 | ✅ |
| `friendCheckSem` | `NSObject` (strong, nonatomic) | 同步信号量 | ✅ |
| `deletedFriends` | ❌ 缺失 | ❌ 没有此属性 | ❌ |
| `lastCheckIndex` | ❌ 缺失 | ❌ 没有此属性 | ❌ |
| `totalFriendsCount` | ❌ 缺失 | ❌ 没有此属性 | ❌ |

---

## 五、方法调用链对比

### 5.1 微信优化 1.6.5

```
checkFriendsWithCompletion:
  ├── allFriends ─────────────────────────────→ MMServiceCenter → CContactMgr.getService:
  │                                                  └── getContactList:contactType: (0,8 → 0,0 → 1,0)
  │                                                  └── 过滤 @chatroom / gh_
  └── checkSpecificFriends:completion: ──────→ FriendDetector 类（原生 WeChat）
                                                     ├── checkSpecificFriends:completion:（类方法）
                                                     │     └── agreeDuty 服务端验证
                                                     └── 返回 FriendDetectResult[]
  └── 处理结果 → 保存 NSUserDefaults → notification

checkSpecificFriends:（无 block）──→ checkSpecificFriends:completion: + nil

checkFriendsWithOptions:completion:──→ checkSpecificFriends:completion: + 选项参数

loadDetectionResults──→ NSUserDefaults → 恢复 checkedFriendWxIDs / deletedFriends / lastCheckIndex

showResultNotification:──→ CSFriendDetectionViewController.showResultNotification:
```

### 5.2 我们的实现

```
checkFriendsWithCompletion:
  ├── allFriends ─────────────────────────────→ MMServiceCenter → CContactMgr.getService:
  │                                                  └── getContactList:contactType: (1,0 → 0,0 → 0,8)
  │                                                  └── 过滤 @chatroom / gh_
  └── checkSpecificFriends:completion: ──────→ 本地 MioFriendDetector
                                                     ├── getContactInfo:（服务端，可能无效）
                                                     ├── getContactByName:（本地缓存）
                                                     ├── m_uiVerifyFlag > 0 ?
                                                     ├── nickname 异常？
                                                     └── 返回 MioFriendDetectResult[]
  └── 处理结果 → 保存 NSUserDefaults

checkSpecificFriends:（无 block）──→ checkSpecificFriends:completion: + nil
```

---

## 六、检测准确性分析

### 6.1 微信优化 1.6.5

**原理**：调用 WeChat 原生 `FriendDetector` 类，该类内部使用 `agreeDuty` 机制与服务端通信。

`agreeDuty` 是 WeChat 的"好友关系协议"机制：
- 当 A 添加 B 为好友时，双方签署 `agreeDuty`（同意协议）
- 当 B 删除 A 时，B 单方面解除协议
- 服务端可以查询协议的签署状态 → 准确判断好友关系是否仍存在

**准确性**：✅ 高（服务端真实数据）
**延迟**：秒级（网络请求）
**兼容性**：⚠️ 只在部分 WeChat 版本中有原生 FriendDetector 类

### 6.2 我们的实现

**原理**：读取 CContact 的本地属性

检测指标：
1. `CContact.m_uiVerifyFlag` — 永远为 0（代表"已验证"），被删除后不更新
2. `CContact.m_nsNickName` — 被删除后微信可能不更新本地昵称
3. `CContact.m_uiStatus` — 永远为 0，被删除后不更新

**实际效果**（来自 log 10）：
```
wxID=wxid_xxx nick=嘴硬 verifyFlag=0 status=0 → valid（实际可能已被删）
wxID=wxid_xxx nick=睿睿. verifyFlag=0 status=0 → valid（实际可能已被删）
... 2688 个全部 verifyFlag=0 ...
Detection returned 2688 results
Deleted friends: 0
```

**结论**：❌ 无法检测出被删除的好友。WeChat 联系人的本地缓存数据（`m_uiVerifyFlag`、`m_uiStatus`、`m_nsNickName`）在好友删除你后不会立即更新，需要服务端同步后才能反映真实状态。

---

## 七、结论

| 对比项 | 微信优化 1.6.5 | 我们的实现 |
|--------|:-------------:|:---------:|
| 类结构复刻 | — | ✅ 基本一致 |
| 好友获取 | ✅ | ✅ |
| 检测方式 | 服务端 `agreeDuty` | 本地属性读取 |
| 准确性 | ✅ 高 | ❌ 无法检出 |
| 网络依赖 | ✅ 需要 | ❌ 不需要（但无效） |
| WeChat 版本依赖 | ⚠️ 依赖原生 FriendDetector | ✅ 不依赖 |
| Crash 风险 | ⚠️ 原生方法签名不匹配时 | ✅ 无 |
| UI 展示 | CSFriendDetectionViewController | 弹窗 |

**根因**：好友关系状态存储在 WeChat 服务端，本地 CContact 缓存只在登录/同步时刷新。
`m_uiVerifyFlag` 在本地永远是 0（已通过验证），即使对方删除了你也不会变。

---

## 八、深度分析：微信优化插件的检测机制

### 8.1 二进制分析结论

对微信优化 1.6.5 的二进制进行深度逆向分析后，得出其检测机制的全貌：

```
checkSpecificFriends:completion: (FriendDetector 类方法)
  │
  ├── Step 1: 保留参数
  │   ├── param_3 = wxIDs (NSArray)
  │   └── param_4 = completion block
  │
  ├── Step 2: 调 FUN_000c2ae0(self)
  │   │
  │   ├── 先调 self.allFriends → 获取所有好友
  │   │   └── allFriends → MMServiceCenter → CContactMgr → getContactList
  │   │
  │   ├── 加载引用表 (0x137f48 处) → 这是 ObjC tagged pointer 表
  │   │   包含 FriendDetector 内部方法的选择器引用
  │   │
  │   └── 调 FUN_000c5080 → FUN_000c8420 → FUN_000cff80
  │       │
  │       ├── FUN_000c8420: 加载 CFString "agreeDuty"
  │       │   "agreeDuty" 是 WeChat 的"好友关系协议"机制
  │       │   → A添加B为好友时，服务端双方签署 agreeDuty
  │       │   → B删除A时，B单方面解除协议
  │       │   → 查询 agreeDuty 状态 = 服务端验证好友关系
  │       │
  │       └── FUN_000cff80: 执行实际的服务端请求
  │           通过 MMServiceCenter 获取网络服务
  │           发送请求到 WeChat 服务器
  │           等待响应 → 解析 isDeleted/isInvalid
  │
  ├── Step 3: 等待结果
  │   └── dispatch_semaphore 同步
  │
  └── Step 4: 创建 FriendDetectResult[]
      └── infoWithContact:isDeleted:isInvalid:
```

### 8.2 核心检测链

```
FriendDetector.checkSpecificFriends:completion:
  → [self allFriends] → CContactMgr → 联系人列表
  → 调 WeChat 服务端 agreeDuty 接口
  → 服务端返回每条好友关系的协议状态
  → 已解除 agreeDuty = 被删除
  → 正常 = 好友关系存在
```

### 8.3 关键发现

| 发现 | 说明 |
|------|------|
| 检测需要服务端验证 | 本地数据不可靠，必须调 WeChat 服务器 |
| 使用 `agreeDuty` 机制 | WeChat 内部的好友关系协议系统 |
| FriendDetector 类是 WeChat 原生类 | 不是微信优化插件创建的，它是 WeChat SDK 的一部分 |
| 引用表使用 tagged pointer | 0x137f48 处是 ObjC tagged pointer 引用表，存储选择器引用 |
| 方法链深 | 从 checkSpecificFriends → 4层函数调用 → 最终到服务端请求 |

### 8.4 我们的困境

| 方案 | 结果 | 原因 |
|------|:----:|------|
| 调 WeChat FriendDetector 类方法 | ❌ Crash | 方法签名不匹配或内部 API 变更 |
| 调 WeChat FriendDetector 实例方法 | ❌ Crash | 同上 |
| 本地 CContact 属性检测 | ❌ 0 deleted | `m_uiVerifyFlag` 永远为 0 |
| 本地 getContactByName: | ❌ 0 deleted | 返回的是缓存数据 |
| 跳过检测只用本地 | ❌ 0 deleted | 根本问题 |

**结论**：好友检测必须依赖 WeChat 服务端的 `agreeDuty` 接口。
WeChat 原生的 `FriendDetector` 类曾提供此接口，但在当前 WeChat 版本中
（该类的存在不稳定、方法签名不兼容），无法安全调用。
微信优化插件之所以能工作，是因为它编译时绑定了特定 WeChat SDK 版本，
而该版本的 `FriendDetector` 接口与它匹配。

---

## 九、新策略：直接绑定底层网络服务（替代 FriendDetector）

### 9.1 思路转变

之前一直在调 `FriendDetector` 的方法，但这些方法是**微信优化插件的 `@implementation` 加上的**。
没有微信优化时那些方法不存在。

**正确的思路**：直接绑定 WeChat 底层的 `agreeDuty` 网络服务，跳过 FriendDetector 类。

```
微信优化插件:  FriendDetector (category) → agreeDuty → 网络服务 → 服务端
我们的新方案:  MMServiceCenter → 网络服务 → agreeDuty → 服务端
                      ↑ 直接绑定，跳过中间层
```

### 9.2 绑定方案

| 策略 | 方法 | 说明 |
|------|------|------|
| **C: 运行时绑定** | `scanAllServices()` + `findAndCallAgreeDutyService()` | 首次启动时扫描所有 MMServiceCenter 服务，列出方法名含 agree/duty/verify/check 的服务。尝试直接调用 |
| **D: Hook 网络层** | `installNetworkHook()` | Hook CNetworkMgr 的请求发送方法，拦截 agreeDuty 请求/响应 |
| **E: Hook 同步** | `installContactSyncHook()` | Hook CContactMgr 的联系人同步回调，捕获联系人状态变更 |
| **B: CContactMgr 同步** | `getContactInfo:` 批量获取 | 调 CContactMgr 服务端获取接口，触发联系人同步 |

### 9.3 运行时服务扫描（全自动探索）

首次启动时，扫描以下服务的所有方法并过滤关键词：

```
CNetworkMgr, CRequestMgr, CContactMgr, ContactVerifyMgr,
CSyncMgr, CContactCacheMgr, ContactTagMgr, ContactRemarkMgr,
ContactBlockMgr, ...
```

同时扫描全部注册类中名称含 `agree`/`Agree`/`Duty` 的相关类。

日志输出示例：
```
[Scan] === CNetworkMgr (0x1234) ===
[Scan]     [0] sendRequest:withDelegate:  type=v@:@@
[Scan]     [1] SendRequest:Completion:    type=v@:@?
[Scan] === CContactMgr (0x5678) ===
[Scan]     [3] getContactInfo:           type=@@:@
[Scan]     [7] syncContact:              type=v@:@
[Scan] === 扫描所有 agreeDuty 相关类 ===
[Scan]    类: MyAgreeDutyCheckerClass // 如果存在的话
```

这些日志能告诉我们 WeChat 到底提供了什么服务，以及哪个服务能处理 agreeDuty。

### 9.4 执行流程

```
startFriendDetection()
  ├── allFriends() → CContactMgr.getContactList → 好友列表
  ├── runBoundDetection(wxIDs) ← 核心绑定函数
  │     ├── dispatch_once: scanAllServices() [探索]
  │     ├── dispatch_once: installNetworkHook() [Hook]
  │     ├── dispatch_once: installContactSyncHook() [Hook]
  │     ├── findAndCallAgreeDutyService() [绑定]
  │     │     ├── 遍历候选服务类(CNetworkMgr, CContactMgr...)
  │     │     ├── 遍历候选方法名(checkAgreeDutyForContact:, queryRelation:...)
  │     │     ├── 找到可用方法 → 调用 → 记录结果
  │     │     └── 未找到 → 继续
  │     └── CContactMgr.getContactInfo: 批量同步 [回退]
  │           └── getContactInfo 返回 CContact → 好友存在
  │           └── getContactInfo 返回 nil → 可能被删
  ├── tryLocalDetection(wxIDs) [最终回退]
  │     └── getContactByName + verifyFlag/nickname 检查
  └── saveResults(results) [保存]
```

### 9.5 预期结果

| 情形 | 预期 |
|------|------|
| WeChat 有 agreeDuty 网络服务 | ✅ 可调通服务端验证，准确检测 |
| 网络服务存在但方法名不匹配 | ⚠️ bind 失败，走 CContactMgr 同步 |
| 所有绑定都失败 | ⚠️ 回退到本地属性检测（准确性低） |
| **扫描日志有收获** | ✅ **下次迭代可根据日志找到正确方法签名** |
