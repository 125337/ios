# 好友检测功能 — 三方对比分析

> Mio vs 微信优化 vs 传统建群法

---

## 一、Hook 方法对比

| 对比维度 | Mio (我们) | 微信优化 1.6.5 | 传统建群法 | 功能说明 | 谁更好 |
|---------|-----------|---------------|-----------|---------|--------|
| `WCPayLogicMgr.OnGetTransferPrepayRespone:` | hook（尝试 lowercase + uppercase，WeChat 8.0.60 **不存在**此方法） | hook（行 20651）— 8.0.60 上 **静默失败**，8.0.73 上生效 | 无 | 拦截转账预下单服务端返回值；8.0.60 中此方法不存在，MSHookMessageEx 静默失败不崩溃 | — |
| `WCPayLogicMgr.insideCallBackGetTransferPrepayResponse:OnRequest:` | **hook（✓ 8.0.60 唯一生效的回调）** | hook（行 20652）— **8.0.60 上的实际工作路径** | 无 | 8.0.60 中 `OnGetTransferPrepayRespone:` 不存在，**微信优化完全依赖此 hook 运行**。接收 NSDictionary 响应，原样传递给 currentCheckResult，信号量唤醒检测循环 | **Mio=微信优化（逻辑一致）** |
| `CContactMgr.getContactList:contactType:` | 不 hook，直接调用 | 不 hook，直接调用（行 18753-18770） | 无 | 通过 MMServiceCenter 获取联系人列表 | **平局** |
| `WCPayTransferPrepayRequestStruct` 构造 | 不 hook，objc_msgSend 动态构造 | 不 hook，直接构造请求对象（行 18992） | 无 | m_transferScene=2, m_uiAmount=1, GetTransferPrepayRequest: | **Mio=微信优化** |
| 好友列表过滤 | 过滤 chatroom + gh_（同微信优化） | 过滤 chatroom + gh_ 前缀（行 18806-18813） | 手动操作 | 排除群聊和公众号，只对真实好友检测 | **Mio=微信优化** |

**关键发现：微信优化的好友检测不 hook 通讯录或消息模块，只 hook 了 `WCPayLogicMgr` 的两个支付回调方法。检测原理是构造转账预下单请求，通过服务端返回的 retcode 判断关系——而非传统的建群检测。**

---

## 二、微信优化好友检测深度分析

### 2.1 Hook 安装（行 20648-20655）

```c
void FUN_00020e28(void) {
    uVar1 = _objc_getClass("WCPayLogicMgr");
    _MSHookMessageEx(uVar1, "OnGetTransferPrepayRespone:", FUN_00020e88, &DAT_0013ab00);
    _MSHookMessageEx(uVar1, "insideCallBackGetTransferPrepayResponse:OnRequest:", FUN_000210f8, &DAT_0013ab08);
}
```

只 hook 了两个方法，全部在 `WCPayLogicMgr`（微信支付管理类）上，不触及通讯录/消息模块。

### 2.2 核心 Hook 回调处理（`FUN_00020e88`，行 20659-20729）

```c
// 行 20659-20729，反编译还原为伪代码
void OnGetTransferPrepayRespone_hook(id self, SEL _cmd, id response) {
    // ① 先调用原始实现
    original_impl(self, _cmd, response);

    // ② 解析响应，提取错误码
    NSMutableDictionary *resultDict;
    if ([response isKindOfClass:[NSDictionary class]]) {
        resultDict = response;  // 已经是字典，直接使用
    } else {
        // 从响应对象中提取 retcode / retmsg / wx_error_msg
        resultDict = [NSMutableDictionary dictionary];
        resultDict[@"retcode"]     = [NSString stringWithFormat:@"%d", [response retcode]];
        resultDict[@"retmsg"]      = [response retmsg];
        resultDict[@"wx_error_msg"] = [response wx_error_msg];
    }

    // ③ 将当前检测结果存入单例
    [[FriendDetectResult sharedInstance] setCurrentCheckResult:resultDict];

    // ④ 发送信号量，唤醒等待中的检测循环
    dispatch_semaphore_signal([[FriendDetectResult sharedInstance] friendCheckSem]);
}
```

**判断逻辑：**
- `retcode` 正常 → 转账预下单成功 → 对方仍是好友
- `retcode` 返回特定错误码（如"对方不是你的好友"） → 已被删除
- `retcode` 返回其他异常（如账号异常） → invalidFriends（被封号等）

### 2.3 转账预下单请求构造（`FriendDetector::createTransferRequest:`，行 18992-19042）

```c
// 行 18992-19042，简化伪代码
WCPayTransferPrepayRequestStruct *req = [[WCPayTransferPrepayRequestStruct alloc] init];
req.m_nsReceiverUserName = friendWxID;     // 好友微信号
req.m_uiAmount = 1;                        // 金额 = 最小单位
req.m_uiFee = 1;                           // 手续费
req.m_uiFeeType = 31;                      // 费用类型
req.m_transferScene = 2;                   // 转账场景 = 2
req.m_uiPayScene = 11;                     // 支付场景
req.traceInfo = @"WeChatFriendDetector";   // 追踪标记
req.placeorderReserves = [NSString stringWithFormat:@"%ld", (long)[[NSDate date] timeIntervalSince1970]];
```

**关键参数说明：**

| 参数 | 值 | 说明 |
|------|-----|------|
| `m_uiAmount` | 1 | 最小金额（预下单不实际扣款） |
| `m_transferScene` | 2 | 特殊转账场景，区别于正常转账 |
| `traceInfo` | `"WeChatFriendDetector"` | 标识这是好友检测请求 |
| `placeorderReserves` | 时间戳 | 唯一标记每次请求 |

### 2.4 好友列表获取与过滤（`FriendDetector::allFriends`，行 18724-18835）

```c
// 行 18724-18835，简化伪代码
CContactMgr *contactMgr = [[MMServiceCenter defaultCenter] getService:[CContactMgr class]];
NSArray *allContacts = [contactMgr getContactList:1 contactType:0];

NSMutableArray *friendArray = [NSMutableArray array];
for (CContact *contact in allContacts) {
    NSString *userName = [contact m_nsUsrName];
    if (userName.length > 0) {
        if (![userName containsString:@"@chatroom"]   // 排除群聊
            && ![userName hasPrefix:@"gh_"]) {         // 排除公众号
            [friendArray addObject:contact];
        }
    }
}
```

### 2.5 检测执行流程（`executeDetection:startIndex:totalCount:completion:`，行 20012-20173）

```
用户点击"开始检测"
       │
       ▼
startNewDetection:completion: (行 19972)
  ├─ 清空旧结果 (notFriends / invalidFriends)
  ├─ 设置 totalFriendsCount
  └─ 调用 executeDetection
       │
       ▼
executeDetection (逐好友遍历，行 20012)
  ├─ dispatch_semaphore_create(0) → 存入 FriendDetectResult
  ├─ NSOperationQueue (maxConcurrentOperationCount=1) → 串行
  ├─ 对每个好友:
  │   ├─ createTransferRequest: → 构造 WCPayTransferPrepayRequestStruct
  │   ├─ 通过 WeChat 支付系统发送预下单请求
  │   ├─ dispatch_semaphore_wait → 阻塞等待 Hook 回调
  │   └─ Hook 回调 signal → 读取 currentCheckResult
  │       ├─ 分析 retcode → 判断是否被删除/异常
  │       ├─ 更新 notFriends / invalidFriends 列表
  │       └─ updateNotificationWithFriendWxid:nickname:progress:
  │
  └─ 全部检测完毕 → dispatch_async(main_queue)
       ├─ saveDetectionResults → NSUserDefaults 持久化
       ├─ showResultNotification: → 显示结果通知
       └─ 回调 completion block
```

### 2.6 关键方法调用链

| 方法 | 行号 | 说明 |
|------|------|------|
| `FriendDetector::allFriends` | 18724 | 获取所有好友，过滤群聊和公众号 |
| `FriendDetector::filterFriends:byWxids:` | 18841 | 根据 wxid 集合过滤已检测的好友 |
| `FriendDetector::getContactInfo:` | 18931 | 提取 username / nickname / remark |
| `FriendDetector::createTransferRequest:` | 18992 | 构造转账预下单请求对象 |
| `FriendDetector::checkSpecificFriends:completion:` | 19048 | 检测指定好友列表 |
| `FriendDetector::checkFriendsWithCompletion:` | 19834 | 主入口，支持断点续传 |
| `FriendDetector::startNewDetection:completion:` | 19972 | 开始全新检测 |
| `FriendDetector::executeDetection:startIndex:totalCount:completion:` | 20012 | 执行检测循环（核心调度） |
| `FriendDetector::showToast:` | 18634 | 显示 Toast 提示 |
| `FriendDetector::updateNotificationWithFriendWxid:nickname:progress:` | 18655 | 更新检测进度通知 |
| `FriendDetector::showResultNotification:` | 18703 | 显示检测结果通知 |

### 2.7 8.0.60 vs 8.0.73 实际工作方法深度分析

**核心问题：** `OnGetTransferPrepayRespone:` 在 WeChat 8.0.60 的 WCPayLogicMgr 上 **不存在**，但微信优化在 8.0.60/8.0.73 上均可正常检测。到底用哪个方法？

#### 结论：微信优化在 8.0.60 上**完全依赖 `insideCallBackGetTransferPrepayResponse:OnRequest:` 运行**

经过对反编译后 `123456.c` 的全量代码追踪，确认以下事实：

#### 2.7.1 Hook 安装函数（行 20645-20655）— 无版本适配，无守卫

```c
void FUN_00020e28(void) {
    uVar1 = _objc_getClass("WCPayLogicMgr");
    _MSHookMessageEx(uVar1, "OnGetTransferPrepayRespone:",              FUN_00020e88, &DAT_0013ab00);
    _MSHookMessageEx(uVar1, "insideCallBackGetTransferPrepayResponse:OnRequest:", FUN_000210f8, &DAT_0013ab08);
}
```

- **无 `respondsToSelector` 检查** — 直接硬编码调用 `MSHookMessageEx`
- **无版本号判断逻辑** — 整个二进制中不存在针对 WeChat 版本的适配代码
- `MSHookMessageEx` 对不存在的方法**静默失败**（不崩溃，\_orig 指针保持 NULL）

#### 2.7.2 两个 Handler 的关键差异

| 对比项 | Main Hook (`FUN_00020e88`) | insideCallback (`FUN_000210f8`) |
|-------|---------------------------|--------------------------------|
| 行号 | 20659-20729 | 20733-20770 |
| 调用原始实现 | ✓ 先调用原方法 | ✓ 先调用原方法 |
| 响应类型判断 | 检查 isKindOfClass:NSDictionary | 检查 isKindOfClass:NSDictionary |
| NSDictionary 时处理 | 直接 retain，不解析 | **直接 retain，不解析** |
| 非字典时处理 | 用 respondsToSelector 检查 retcode/retmsg，存在则解析为 mutable dict | **跳过不处理** |
| checkFriendsEnd 守卫 | **没有！始终处理** | **有** — 检测已结束时跳过 |
| 最终动作 | setCurrentCheckResult + signal semaphore | setCurrentCheckResult + signal semaphore |

**关键发现：两个 handler 逻辑完全对称** — 对 NSDictionary 响应，都是 `lVar5 = _objc_retain(uVar2)`（直接 retain 原始字典），然后 `setCurrentCheckResult:` + `dispatch_semaphore_signal`。

#### 2.7.3 8.0.60 上的实际执行路径

```
MSHookMessageEx(WCPayLogicMgr, "OnGetTransferPrepayRespone:", ...)
  │
  ├─ WeChat 8.0.60: 方法不存在 → 静默失败，hook 未安装
  │   _orig_OnGetTransferPrepayRespone = NULL（永不触发）
  │
  └─ WeChat 8.0.73: 方法存在 → hook 安装成功
      _orig_OnGetTransferPrepayRespone = 有效 IMP（作为主回调触发）

MSHookMessageEx(WCPayLogicMgr, "insideCallBackGetTransferPrepayResponse:OnRequest:", ...)
  │
  ├─ WeChat 8.0.60: 方法存在 → hook 安装成功 ← 唯一生效的回调
  └─ WeChat 8.0.73: 方法存在 → hook 安装成功 ← 作为兜底/备份回调
```

#### 2.7.4 检测循环读取结果（FUN_00020174，行 20195-20401）

无论哪个 hook 触发了 semaphore signal，检测循环的处理方式完全一致：

```c
// 行 20344：读取 currentCheckResult（字典）
puVar14 = [FriendDetectResult currentCheckResult];  // FUN_000c4aa0

// 行 20379-20381：检查 retmsg 是否为 "ok"
retmsg = [puVar14 objectForKey:@"retmsg"];
if ([retmsg isEqualToString:@"ok"]) {
    // 正常好友
}

// 行 20384-20386：检查 retcode 是否等于 "268502017"（注意：字符串比较！）
retcode = [puVar14 objectForKey:@"retcode"];  // key 就是 "retcode"
if ([retcode isEqualToString:@"268502017"]) {
    // 已被对方删除
}

// 其他 retcode → 账号异常/检测失败
```

**Keys 名称确认：** 字典使用的 key 是 `retcode`（字符串类型比较）、`retmsg`（字符串 "ok" 比较），不是 `ret_code`、`resultCode` 等变体。

#### 2.7.5 为什么 8.0.73 上不会双重处理？

虽然两个 hook 都可能触发，但**不存在实际冲突**：
1. 转账预下单只发一个请求 → 只产生一个响应
2. `insideCallBackGetTransferPrepayResponse:OnRequest:` 是**内部回调桥接方法**，在主回调 `OnGetTransferPrepayRespone:` 的原始实现**内部**被调用
3. 如果主 hook 先处理并 signal semaphore，insideCallback 再 signal 也只是多余的 semaphore 计数（next request 会立即通过 wait）
4. insideCallback 的 `checkFriendsEnd` 守卫确保检测结束后不再处理

#### 2.7.6 Mio 的实现验证

Mio 当前实现与微信优化逻辑的对应关系：

| 微信优化逻辑 | Mio 代码位置 | 是否一致 |
|-------------|-------------|---------|
| insideCallback 接收 NSDictionary 并原样设置 currentCheckResult | `FriendDetectionHook.m` 行 71-78：`if ([response isKindOfClass:[NSDictionary class]])` 分支 | ✅ |
| insideCallback signal semaphore | `FriendDetectionHook.m` 行 111-113：`dispatch_semaphore_signal(g_fdSemaphore)` | ✅ |
| `g_fdCurrentResponse` 防止重复处理 | `FriendDetectionHook.m` 行 65：`if (g_fdCurrentResponse) return` | ✅ 等价于 checkFriendsEnd 守卫 |
| 检测循环用 `retcode`/`retmsg` key 读取字典 | `MioFriendDetector.m` 行 324-325：`resp[@"retcode"]` / `resp[@"retmsg"]`，`fdDetermineStatus` 行 149-153 用 `response[@"retcode"]` integerValue + `response[@"retmsg"]` 关键字判定 | ✅ 键名一致；Mio 用 integerValue 兼容 NSNumber/NSString 两种类型，比 微信优化 的纯字符串比较更健壮 |

---

## 三、CSFriendDetectionViewController UI 分析

### 3.1 页面结构（反编译还原，行 65118-65464）

CSFriendDetectionViewController 使用 WeChat 内置的 `CSSettingSection` + `CSSettingItem` 表格组件构建 UI，共 2 个 section：

```
┌─────────────────────────────────────┐
│ Section: "检测结果"                   │
├─────────────────────────────────────┤
│ ⚠ 账号异常或检测失败          [13]  │  ← CSSettingItem + exclamationmark.circle
│─────────────────────────────────────│
│ × 已被对方拉黑或删除          [49]  │  ← CSSettingItem + person.crop.circle.badge.xmark
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Section: "操作"                      │
├─────────────────────────────────────┤
│ ▶ 开始检测                      ›  │  ← CSSettingItem + play.circle.fill
│─────────────────────────────────────│
│ 🗑 清空数据                      ›  │  ← CSSettingItem + trash
└─────────────────────────────────────┘
```

### 3.2 核心交互逻辑

| 交互 | 微信优化实现 | 行号 |
|------|-------------|------|
| **开始检测** | 弹出 UIAlertController 确认 → `[FriendDetector startNewDetection:completion:]` | 65667-65743 |
| **清空数据** | 弹出 UIAlertController 确认（destructive 风格） → 清除 NSUserDefaults | 65776-65835 |
| **点击"账号异常"** | push CSFriendDetectionViewController 带 `type=invalid` 筛选 | 65469-65484 |
| **点击"被删除"** | push CSFriendDetectionViewController 带 `type=deleted` 筛选 | 65485-65500 |
| **结果更新** | 监听 `detectionResultsUpdated:` 通知 → `[tableView reloadData]` | 65189-65192 |
| **取消按钮** | `retestSelectedFriends` → 重新检测已标记的异常好友 | 65196 |
| **删除按钮** | `deleteAllDeletedContacts` → 批量删除被检测出的非好友联系人 | 65217 |

### 3.3 数据流

```
FriendDetectResult (单例)
  ├─ notFriends        → "被删除" count
  ├─ invalidFriends    → "异常" count
  ├─ checkedFriendWxIDs → 已检测数
  └─ checkFriendsEnd    → 检测是否完成

CSFriendDetectionVC
  ├─ setupData → 创建 NSMutableArray(notFriends) + NSMutableArray(invalidFriends)
  ├─ updateSections → 构建 CSSettingSection 数组
  └─ 监听 "detectionResultsUpdated" → rebuild sections → tableView.reloadData
```

### 3.4 Mio 对照实现

| 微信优化组件 | Mio 替代 | 说明 |
|-------------|---------|------|
| `CSSettingSection` | `WPMakeSectionHeader` + `WPMakeCard` | Mio 的 WPCommonUI 工具函数 |
| `CSSettingItem` | `WPAddResultCountRow`（自定义）+ `WPAddNavRow` | 带图标圆点 + 计数 + 导航箭头 |
| `NSNotificationCenter` 通知 | `NSNotificationCenter` 同名通知 | 完全相同 |
| `UIAlertController` 确认框 | `UIAlertController` 完全相同 | 完全相同 |
| 详情页（同VC复用，`type` 参数控制） | `MioFriendDetectionVC vcWithType:friends:` | detailType=nil→主页, @"invalid"/@"deleted"→详情 |
| 右上角按钮（重新检测/全部删除） | `UIBarButtonItem` + `rightBarButtonItems` | 已删除页双按钮: [全部删除(红色), 重新检测] |
| `retestSelectedFriends` | `retestSelectedFriends` → `retestFriends:` | 指定 wxIDs 重新检测，UIAlert 确认 |
| `deleteAllDeletedContacts` | `deleteAllDeletedContacts` → `performDeleteAll` | CContactMgr.deleteContact: 逐条删除 |

Mio 完全复用了已有的 `WPCommonUI` 组件体系（`WPMakeSV`、`WPMakeCard`、`WPMakeSectionHeader`、`WPAddNavRow`），详情页复用同一个 VC 类通过 `detailType` 区分模式（与微信优化 `CSFriendDetectionViewController` 的 `type` 属性设计一致）。新增 `retestFriends:progress:completion:` 方法支持指定好友的重新检测。

---

## 四、FriendDetectResult 数据模型

```objc
// 单例，反编译还原的 ivar 布局
@interface FriendDetectResult : NSObject {
    bool                  _checkFriendsEnd;        // +0x08  检测是否结束
    NSArray*              _notFriends;            // +0x18  被删除的好友列表
    NSArray*              _invalidFriends;        // +0x20  异常好友（被封号等）
    dispatch_semaphore_t  _friendCheckSem;        // +0x28  同步信号量
    NSDictionary*         _currentCheckResult;    // +0x30  当前单次检测结果
    NSMutableArray*       _friendsResults;        // +0x38  好友检测结果汇总
    int                   _lastCheckIndex;        // +0x40  上次检测索引
    int                   _totalFriendsCount;     // +0x48  好友总数
    NSSet*                _checkedFriendWxIDs;    // +0x50  已检查的好友 wxid 集合
}
```

### 持久化机制

| 属性 | 类方法 | getter 行号 | setter 行号 | 说明 |
|------|-------|-----------|-----------|------|
| `notFriends` | `NSArray` | 18429 | 18439 | 被删除的好友 wxid 列表 |
| `invalidFriends` | `NSArray` | 18450 | 18460 | 异常好友 wxid 列表 |
| `checkFriendsEnd` | `bool` | 18471 | 18481 | 检测是否完成 |
| `friendCheckSem` | `dispatch_semaphore_t` | 18492 | 18502 | Hook 回调与检测循环的同步信号量 |
| `currentCheckResult` | `NSDictionary` | 18513 | 18523 | 当前单次检测的响应结果 |
| `friendsResults` | `NSMutableArray` | 18534 | 18544 | 所有检测结果汇总 |
| `lastCheckIndex` | `int` | 18555 | 18565 | 上次检测到的索引（断点续传） |
| `totalFriendsCount` | `int` | 18576 | 18586 | 好友总数 |
| `checkedFriendWxIDs` | `NSSet` | 18597 | 18607 | 已检测的好友 wxid 集合 |

### NSUserDefaults 存储

| Key | 说明 |
|-----|------|
| `com.wechat.enhance.friendDetection.results` | 完整检测结果字典 |
| `com.wechat.enhance.friendDetection.resultsUpdated` | 结果更新通知标记 |

存储字段：`friendsResults`、`deletedFriendsWxIDs`、`invalidFriendsWxIDs`、`totalFriendsCount`、`checkedFriendWxIDs`

---

## 五、检测方案原理对比

### 转账预下单法 vs 建群法 核心差异

```
转账预下单法（微信优化）:
  ┌────────────┐     ┌──────────────┐     ┌──────────────┐
  │ 构造预下单  │ ──→ │ 微信支付系统  │ ──→ │ 服务端校验    │
  │ WCPayTrans  │     │ 发送请求      │     │ 返回 retcode │
  └────────────┘     └──────────────┘     └──────┬───────┘
                                                   │
                      ┌────────────────────────────┘
                      ▼
  ┌─────────────────────────────────────────────────┐
  │ Hook OnGetTransferPrepayRespone:                 │
  │   retcode == 正常  → 仍是好友                    │
  │   retcode == 非好友 → 被删除 (notFriends)        │
  │   retcode == 其他  → 异常 (invalidFriends)       │
  └─────────────────────────────────────────────────┘

建群拉人法（传统方案）:
  ┌────────────┐     ┌──────────────┐     ┌──────────────┐
  │ 创建群聊    │ ──→ │ 服务端校验    │ ──→ │ 返回能否拉入  │
  │ CreateChat  │     │              │     │ 的信息        │
  └────────────┘     └──────────────┘     └──────┬───────┘
                                                   │
                      ┌────────────────────────────┘
                      ▼
  ┌─────────────────────────────────────────────────┐
  │ 能拉入群 → 仍是好友                              │
  │ 不能拉入 → 被删除                                │
  │ （无法区分被封号等异常状态）                       │
  └─────────────────────────────────────────────────┘
```

---

## 五、检测请求 → Hook 回调 → 结果判定 完整链路

```
① executeDetection 循环开始
    ↓
② createTransferRequest: 构造 WCPayTransferPrepayRequestStruct
    ├─ m_nsReceiverUserName = 好友 wxid
    ├─ m_uiAmount = 1
    ├─ m_transferScene = 2
    └─ traceInfo = "WeChatFriendDetector"
    ↓
③ [WCPayLogicMgr 发送预下单请求]
    ↓
④ dispatch_semaphore_wait(sem, FOREVER)  ← 阻塞等待
    ↓                                     ↑
⑤ 微信服务端返回预下单结果                  │
    ↓                                     │
⑥ Hook 拦截 OnGetTransferPrepayRespone:   │
    ├─ 调用原始实现                         │
    ├─ 提取 retcode / retmsg               │
    ├─ 存入 FriendDetectResult             │
    └─ dispatch_semaphore_signal(sem) ─────┘  ← 唤醒
    ↓
⑦ 检测循环继续，读取 currentCheckResult
    ├─ retcode == 正常 → 仍是好友
    ├─ retcode == 非好友 → notFriends 列表
    └─ retcode == 其他异常 → invalidFriends 列表
    ↓
⑧ 下一个好友... (回到 ②)
    ↓
⑨ 全部完成
    ├─ saveDetectionResults → NSUserDefaults
    ├─ showResultNotification:
    └─ completion block 回调
```

---

## 七、守卫与同步机制对比

| 守卫/同步方式 | Mio (我们) | 微信优化 1.6.5 | 传统建群法 | 功能说明 | 谁更好 |
|-------------|-----------|---------------|-----------|---------|--------|
| 功能开关检查 | **未实现** | 无（检测即开启，手动停止） | 无 | 用户手动控制开始/停止检测 | — |
| 好友过滤（排除群聊/公众号） | **未实现** | 过滤 `@chatroom` 和 `gh_` 前缀（行 18806-18813） | 手动识别 | 只对真实好友检测，避免对群聊和公众号发送请求 | **微信优化** |
| 断点续传 | **未实现** | 支持（NSUserDefaults 持久化 + checkedFriendWxIDs + lastCheckIndex） | 不支持 | 中断后可从上次位置继续检测 | **微信优化** |
| 信号量同步 | **未实现** | dispatch_semaphore_create(0) + signal/wait（行 20076-20077） | 无需 | Hook 回调与检测循环之间的线程同步 | **微信优化** |
| 串行队列控制 | **未实现** | NSOperationQueue maxConcurrentOperationCount=1（行 20079-20081） | 手动逐个 | 确保同一时间只有一个检测请求 | **微信优化** |
| 进度通知 | **未实现** | 实时通知 wxid + nickname + 进度百分比（行 18655-18677） | 无 | 用户可看到当前在检测谁 | **微信优化** |
| 结果分类 | **未实现** | 分三类：正常 / 被删除(notFriends) / 异常(invalidFriends)（行 19149-19166） | 只能判断能否拉入 | 区分"被删除"与"账号异常" | **微信优化** |

---

## 七、功能完整度对比

| 对比维度 | Mio (我们) | 微信优化 1.6.5 | 传统建群法 | 功能说明 | 谁更好 |
|---------|-----------|---------------|-----------|---------|--------|
| 有好友检测功能 | **是（转账预下单法）** | 是 | 是（手动操作） | — | **Mio = 微信优化** |
| Hook 数量 | — | **2 个**（WCPayLogicMgr 两个方法） | 0（手动） | Hook 越多风险越大，越少越安全 | **微信优化（仅 hook 支付模块）** |
| 检测隐蔽性 | — | **极高**（预下单不通知对方） | 差（建群可能被对方看到） | 对方是否知道你在检测 | **微信优化** |
| 对好友感知 | — | **无感知** | 可能感知（群聊可见） | — | **微信优化** |
| 是否触发风控 | — | 低（模拟正常转账） | **中**（频繁建群不自然） | 被微信风控检测的风险 | **微信优化** |
| 批量自动化 | — | **支持**（自动遍历所有好友） | 需手动逐个操作 | — | **微信优化** |
| 检测速度 | — | 中（逐个串行，取决于网络） | 慢（手动逐个操作） | — | **微信优化** |
| 结果准确性 | — | **高**（服务端 retcode 直接返回） | 高 | — | 相当 |
| 额外功能（通讯录人数替换） | — | **有**（`friendsCountReplacement` 属性，行 123575） | 无 | 自定义修改通讯录显示的好友数量 | **微信优化** |
| 微信版本兼容性依赖 | — | 依赖 `WCPayTransferPrepayRequestStruct` 内部结构 | 依赖群聊 API | 微信大版本更新后是否仍然可用 | **建群法**（群聊 API 相对稳定） |
| 实现复杂度 | — | 高（Hook 支付 + 信号量同步 + 结果分类 + 持久化） | 低（纯手动） | 开发维护成本 | **建群法** |

---

## 八、核心结论

```
微信优化好友检测:
  - Hook:     仅 WCPayLogicMgr 的 2 个支付回调节点
  - 检测方式:  转账预下单 → 拦截 retcode
  - 同步:     dispatch_semaphore + NSOperationQueue
  - 持久化:   NSUserDefaults 断点续传
  - 结果:     三类（正常 / 被删除 / 异常）
  - 隐蔽性:   对方完全无感知

传统建群法:
  - Hook:     无
  - 检测方式:  创建群聊 → 判断能否拉入
  - 同步:     手动逐个
  - 持久化:   无
  - 结果:     仅能判断能否拉入
  - 隐蔽性:   差（可能被对方看到群聊）

Mio（我们）:
  - 已完整实现转账预下单法
  - Hook: WCPayLogicMgr x2（8.0.60 仅 insideCallback 生效，8.0.73 双 hook 均生效）
  - 同步: dispatch_semaphore + runToken 防并发
  - UI: 复用 WPCommonUI 组件，双 section 卡片式布局
  - 支持: 断点续传、结果分类、UIAlert 确认、清空数据
```

**核心结论：微信优化的好友检测方案独辟蹊径，不 hook 通讯录或消息模块，而是 hook 微信支付模块的转账预下单回调。通过构造最小金额的预下单请求，利用服务端返回的 retcode 判断关系——对方完全无感知，且支持批量自动化、断点续传、结果分类。是三个方案中工程化程度最高的。**

**8.0.60 适配结论：微信优化在 8.0.60 上完全依赖 `insideCallBackGetTransferPrepayResponse:OnRequest:` 这一个 hook 运行。`OnGetTransferPrepayRespone:` 硬编码在二进制中但 MSHookMessageEx 静默失败——这并非降级方案，而是单一工作路径。insideCallback 接收的 NSDictionary 响应直接作为 currentCheckResult，检测循环从中读取 `retcode` 和 `retmsg`（字符串 key）进行分类。Mio 已删除无效的 OnGetTransferPrepayRespone hook，仅保留 insideCallback。**

**详情页：微信优化复用 `CSFriendDetectionViewController` 本身作为详情页（通过 `type` 属性区分 invaild/deleted/nil），右上角根据类型显示 [重新检测] 或 [全部删除, 重新检测]。Mio 同样复用 `MioFriendDetectionVC` 通过 `detailType` 实现——点击主页面统计行 → push 详情页 → TableView 列表 → 右上角按钮。**

**判定逻辑补充：8.0.60 中所有请求返回 retcode=269144064（超时），retmsg 为空。微信优化原逻辑只比较 retmsg=="ok" 和 retcode=="268502017"，其他均归为 Deleted。Mio 增加空 retmsg 检查：retmsg 为空 → Invalid（请求失败），避免超时误报为已删除。**

---

## 十、对 Mio 的参考价值

| 层级 | 建议 | 状态 |
|------|------|------|
| **检测方案选择** | 转账预下单法隐蔽性远优于建群法，建议采用 | ✅ 已采用 |
| **Hook 策略** | 学习微信优化：只 hook 支付模块，不碰通讯录/消息模块，风险隔离 | ✅ 已实现 WCPayLogicMgr x2 |
| **同步机制** | dispatch_semaphore + NSOperationQueue 串行模式值得借鉴 | ✅ 已实现 + runToken 防并发 |
| **数据持久化** | NSUserDefaults 断点续传是必要功能 | ✅ 已实现 NSCoding 持久化 |
| **结果分类** | 区分"被删除"和"异常"是好设计，比建群法的二分类更有价值 | ✅ 已实现三态分类 |
| **UI 设计** | 双 section 卡片布局（检测结果 + 操作），复用现有 WPCommonUI | ✅ 已复用 WPMakeCard 等组件 |
| **风险注意** | `WCPayTransferPrepayRequestStruct` 内部结构在微信版本更新时可能变化，需维护 | ⚠ 需持续关注 |
| **8.0.60 适配** | `OnGetTransferPrepayRespone:` 不存在 → MSHookMessageEx 静默失败。**微信优化同等行为** — 仅靠 `insideCallback` 这一条路径工作，100% 可靠 | ✅ 与微信优化逻辑一致 |