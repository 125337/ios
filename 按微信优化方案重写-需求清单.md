# 按微信优化方案重写消息时间功能 — 需求清单

> 分析日期：2026-05-13
> 目标：参照 `微信优化 1.6.5.dylib` 的方式彻底重写 WeChatPlugin 的消息时间显示功能

---

## 一、已知信息（可以直接用）

### 配置项（17个 keys，全部确定）

```
messageTime.enabled              // 开关
messageTime.customFormat          // 时间格式
messageTime.fontSize              // 字体大小
messageTime.boldFont              // 粗体
messageTime.position              // 显示位置
messageTime.offsetX               // X偏移
messageTime.offsetY               // Y偏移
messageTime.bubbleExtensionWidth  // 气泡扩展宽度
messageTime.cornerRadius          // 圆角
messageTime.sender.textColor / .dark         // 发送者文字色
messageTime.sender.backgroundColor / .dark   // 发送者背景色
messageTime.receiver.textColor / .dark       // 接收者文字色
messageTime.receiver.backgroundColor / .dark // 接收者背景色
DisableLabelWidthAdjustment       // 禁用标签宽度自适应
```

### UIColor 属性（12个，类型编码确认）

```
T@"UIColor",&,N,V_senderColor / V_senderColorDark
T@"UIColor",&,N,V_senderTextColor / V_senderTextColorDark
T@"UIColor",&,N,V_senderBackgroundColor / V_senderBackgroundColorDark
T@"UIColor",&,N,V_receiverColor / V_receiverColorDark
T@"UIColor",&,N,V_receiverTextColor / V_receiverTextColorDark
T@"UIColor",&,N,V_receiverBackgroundColor / V_receiverBackgroundColorDark
```

### Hook 点（5个，方法名确认）

```
layoutSubviews, layoutContentView, prepareForReuse
tableView:willDisplayCell:forRowAtIndexPath:
viewDidLayoutSubviews
```

### 时间标签管理

```
initTimeLabel, m_timeLabel, messageTime
applyPositionOffset  (独立偏移方法)
```

### WeChat 8.0.60 实际属性名（Ghidra 反编译确认）

| 属性名 | 类型 | 状态 | 证据来源 |
|--------|------|:----:|---------|
| **`m_bgImageView`** | `YYAsyncImageView*` | **✅ 确认** | Ghidra: `CommonMessageCellView` 完整方法列表 |
| **`m_headImageView`** | `MMHeadImageView*` | **✅ 确认** | Ghidra: `CommonMessageCellView` 完整方法列表 |
| **`getBgImageView`** | 方法→id | **✅ 确认** | Ghidra: `getBgImageView` |
| **`getHeadImageView`** | 方法→id | **✅ 确认** | Ghidra: `getHeadImageView` |
| **`initBgImageView`** | 方法 | **✅ 确认** | Ghidra: `initBgImageView`（懒加载，在 `layoutInternal` 中调用） |
| **`initHeadImageView`** | 方法 | **✅ 确认** | Ghidra: `initHeadImageView` |
| **`setFrameForBgImageView:`** | 方法 | **✅ 确认** | Ghidra: `setFrameForBgImageView:` |
| **`layoutInternal`** | 方法 | **✅ 确认** | Ghidra: `layoutInternal`（CommonMessageCellView 方法） |
| **`layoutContentView`** | 方法 | **✅ 确认** | Ghidra: `layoutContentView` |
| **`leftAvatarView`** | `UIImageView` | **✅ 确认** | 之前已确认 |
| **`rightAvatarView`** | `UIImageView` | **✅ 确认** | 之前已确认 |
| **`m_bubbleBorderFrame`** | `CGRect` | **✅ 确认** | Ghidra: `m_bubbleBorderFrame` |
| **`m_contentView`** | UIView | **✅ 确认** | 之前已确认 |
| **`m_messageWrap`** | CMessageWrap | **✅ 确认** | 之前已确认 |
| **`viewModel`** | ViewModel | **✅ 确认** | 之前已确认 |

**布局调用链（确认）**：
```
layoutSubviews → layoutContentView → layoutInternal → initBgImageView (懒加载创建气泡)
```

---

## 二、缺失信息（经 Ghidra 验证，分色标注）

> 通过 Ghidra 反编译 `weixinyouhua1.6.5.dylib` 逐条验证：
> - ✅ **Ghidra 可确认**
> - ⚠️ **strings 推测，未反编译验证**
> - ❌ **已核实为错误，从文档中移除**

### ✅ Ghidra 反编译确认

| # | 信息 | 结果 |
|---|------|------|
| 1 | 设置页类名 | `CSMessageTimeSettingsViewController`（48 个方法），确认 |
| 2 | `position` 类型 | **整数**（`long long`），不是字符串。`setMessageTimePosition:` 参数类型确认 |
| 3 | 颜色选择器 | 使用 `UIColorPickerViewController`，有 `showColorPicker:` 等方法 |
| 4 | 设置项方法 | `showPositionOptions`, `showFontSizeInputAlert`, `showCornerRadiusInputAlert`, `showOffsetInputAlertForKey:title:`, `showTimeFormatEditor` 等 |
| 5 | 气泡属性 | `m_bgImageView`（`YYAsyncImageView*`），`getBgImageView` 返回此 ivar |
| 6 | 头像属性 | `m_headImageView`（`MMHeadImageView*`），`getHeadImageView` 返回此 ivar |
| 7 | WeChat 布局方法链 | `layoutSubviews → layoutContentView → layoutInternal → initBgImageView` |

### ⚠️ strings 推测（未反编译验证，仅供参考）

| # | 信息 | 推测 |
|---|------|------|
| 8 | 时间格式化 | 使用 `NSDateFormatter` + `setDateFormat:`（`_OBJC_CLASS_$_NSDateFormatter` 存在） |
| 9 | 时间格式串 | `HH:mm:ss`, `yyyy-MM-dd`, `yyyy-MM-dd HH:mm:ss`（strings 存在） |
| 10 | `getBgImageView` 使用 | WeChat 自身方法，微信优化可能在 hook 中调用 |
| 11 | 配置 key（17个） | `com.wechat.enhance.messageTime.*` 系列 key（strings 确认） |
| 12 | 设置页相关类 | `CSSettingItem`, `CSSettingSection`, `CSTimeFormatEditorViewController`, `CSBubbleStyleSettingsViewController` 等 |

### ❌ 已移除的错误信息

以下原文档条目经 Ghidra 验证为错误或不准确，已删除：

| 原条目 | 错误原因 |
|-------|---------|
| `layoutInternal` hook 内部逻辑 | `layoutInternal` 在 WeChat 中存在但**未确认微信优化 hook 了它** |
| `viewDidLayoutSubviews` hook | Ghidra 搜到的 `viewDidLayoutSubviews` 在设置页类中，**与消息时间无关** |
| `applyPositionOffset` 伪代码（4 位置 + offset 算法） | 实际 `applyPositionOffset` 在 `CSChatAvatarTitleView` 中，**与消息时间无关** |
| position 为 4 个字符串值 | `setMessageTimePosition:` 参数是 `long long` → position 是数字 |
| 标签加到 contentView | `contentView`/`addSubview:` 无特异性，不是证据 |
| Hook 目标类表 | 只是 WeChat 方法名存在，未确认微信优化实际 hook 了哪些 |

## 二-B、dylib 中确认的配置键（strings 分析，可靠）

```
com.wechat.enhance.messageTime.enabled
com.wechat.enhance.messageTime.fontSize
com.wechat.enhance.messageTime.boldFont
com.wechat.enhance.messageTime.customFormat
com.wechat.enhance.messageTime.position
com.wechat.enhance.messageTime.offsetX
com.wechat.enhance.messageTime.offsetY
com.wechat.enhance.messageTime.sender.textColor
com.wechat.enhance.messageTime.sender.textColor.dark
com.wechat.enhance.messageTime.sender.backgroundColor
com.wechat.enhance.messageTime.sender.backgroundColor.dark
com.wechat.enhance.messageTime.receiver.textColor
com.wechat.enhance.messageTime.receiver.textColor.dark
com.wechat.enhance.messageTime.receiver.backgroundColor
com.wechat.enhance.messageTime.receiver.backgroundColor.dark
com.wechat.enhance.messageTime.cornerRadius
com.wechat.enhance.messageTime.bubbleExtensionWidth
```

---

## 三、Ghidra 项目现状

已有 Ghidra 项目文件（`/www/wwwroot/ios/插件/jeiya/wechat.gpr`），**已在 Ghidra GUI 中成功打开**，已加载 `weixinyouhua1.6.5.dylib`。

**关键发现**：核心消息时间逻辑（创建/更新标签的函数）在 dylib 中被混淆或使用了动态调用方式，无法通过 Ghidra 的反编译直接定位。可以确认的信息：

| 确认方式 | 信息 |
|---------|------|
| ✅ Ghidra 类定义 | `CommonMessageCellView` 的属性和布局方法链 |
| ✅ Ghidra 类列表 | `CSMessageTimeSettingsViewController` 48 个方法 |
| ✅ strings | 17 个配置 key、`NSDateFormatter` 使用、格式字符串 |
| ❌ 无法确认 | dylib hook 了哪些 WeChat 方法、核心标签创建逻辑的内部实现 |

### 需要的工具

| 工具 | 用途 | 状态 |
|------|------|:----:|
| **Ghidra GUI**（已打开） | 反编译分析微信优化 dylib | ✅ 可用 |
| **证书签名（非越狱）** | 运行时调试测试 | ✅ 可用 |
| **Theos** | 编译 | ✅ 已有 |

---

## 四、代码修正方向（基于 Ghidra 最终确认）

根据 `CommonMessageCellView` 的 Ghidra 完整分析方法列表，当前 `MessageTimeHook.m` 中的属性名需要修正：

| 当前代码 | 实际属性名 | 实际类型 | 需要修正 |
|---------|-----------|---------|:-------:|
| `valueForKey:@"bgImageView"` | **`m_bgImageView`** | `YYAsyncImageView*` | ✅ |
| `valueForKey:@"headImgView"` | **`m_headImageView`** | `MMHeadImageView*` | ✅ |
| subviews 搜索 UIImageView | 气泡类型 `YYAsyncImageView`，头像类型 `MMHeadImageView` | 都不是 UIImageView | ✅ |
| `bubbleView` selector | 非属性，不存在 | - | ✅（移除，直接用 `m_bgImageView`） |

**修正方案**：将 `getBubbleView()` 和 `getAvatarView()` 的 KVC key 从 `bgImageView`/`headImgView` 改为 `m_bgImageView`/`m_headImageView`，`isKindOfClass` 判断改为响应正确的类型。

---

## 五、已完成

以下功能已参照微信优化方案完成实现（2026-05-13）：

| 功能 | 状态 | 说明 |
|-----|:----:|------|
| Bug #1: `messageTimeFormat` 不持久化 | ✅ 已修复 | `PluginConfig.m` 中 `loadDefaults` 和 `save` 现在正确读写 `MessageTimeFormat` key |
| 发送者/接收者独立文字色（含深色） | ✅ 已实现 | 4个 hex 属性：`senderTextColorHex/Dark` + `receiverTextColorHex/Dark` |
| 发送者/接收者独立背景色（含深色）| ✅ 已实现 | 4个 hex 属性：`senderBackgroundColorHex/Dark` + `receiverBackgroundColorHex/Dark` |
| `cornerRadius` 圆角 | ✅ 已实现 | `messageTimeCornerRadius`，CGFloat，0=无圆角 |
| `DisableLabelWidthAdjustment` | ✅ 已实现 | 禁用标签宽度自适应，保持系统默认行为 |
| 调试日志开关 | ✅ 已优化 | `[DBG]` 日志仅在 `debugLogging` 开启时记录 |

### 代码改动摘要

| 文件 | 改动 |
|------|------|
| `PluginConfig.h` | 新增 8 个色彩 hex 属性 + `messageTimeCornerRadius` + `disableLabelWidthAdjustment` |
| `PluginConfig.m` | 加载/保存新增属性的 NSUserDefaults 读写 |
| `MessageTimeHook.m` | 使用发送者/接收者独立配色替代单一颜色；支持圆角；支持宽度自适应开关；DBG日志按开关过滤 |

---

## 六、推荐做法

### 仅推方案：基于已知信息实现（不依赖未验证的推测）

由于 dylib 核心逻辑无法通过 Ghidra 反编译直接定位，以下方案仅基于**已确认的信息**：

| 依据 | 内容 | 可做决定 |
|------|------|---------|
| ✅ Ghidra 类定义 | `m_bgImageView`(YYAsyncImageView\*), `m_headImageView`(MMHeadImageView\*) | **修正 KVC key** |
| ✅ Ghidra 类定义 | WeChat 的布局方法链 `layoutSubviews→layoutContentView→layoutInternal→initBgImageView` | **保留现有 Hook 策略**（willDisplayCell + layoutSubviews + prepareForReuse，足够覆盖所有时机） |
| ✅ Ghidra + strings | `CSMessageTimeSettingsViewController` 设置页 UI（颜色选择器、输入框式设置项） | **设置页照做** |
| ✅ strings | 17 个配置 key、NSDateFormatter | **配置与格式化照做** |
| ❌ 仅推测，不采用 | dylib hook 了哪些 WeChat 方法、`applyPositionOffset` 算法、4 种 position 字符串值 | **不盲从，用自己已验证的方案** |

### 具体实施步骤

1. **修正属性名**（已确认 ✅）：`bgImageView`→`m_bgImageView`，`headImgView`→`m_headImageView`
   - 类型 `YYAsyncImageView`/`MMHeadImageView` 不继承 `UIImageView`，不能用 `isKindOfClass` 判断
   - 直接 `valueForKey:` + `[view respondsToSelector:@selector(image)]` 或移类型检查

2. **保留现有 Hook 策略**（已验证可用 🔄）：willDisplayCell（创建）+ layoutSubviews（更新位置）+ prepareForReuse（清理）

3. **已实现的改进**（无需再改 ✅）：
   - 发送者/接收者独立配色（8 色）
   - 圆角支持（`messageTimeCornerRadius`）
   - `DisableLabelWidthAdjustment` 配置
   - Bug #1 修复（格式持久化）

4. **后续可做**（低优）：
   - 设置页（仿 `CSMessageTimeSettingsViewController` 风格）
   - `applyPositionOffset` 抽取为独立方法（代码整理，位置算法用现有的 0-7 整数版）
