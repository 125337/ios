# 微信优化插件 1.6.5 - 消息时间显示功能深度分析

## 一、插件基本信息

| 属性 | 值 |
|------|-----|
| 文件名 | 微信优化1.6.5.dylib |
| 文件类型 | Mach-O 64-bit arm64 dynamically linked shared library |
| 架构 | arm64 |
| 安装标识 | @rpath/WeChatEnhance.dylib |
| 版本号 | 1.6-5 |
| Hook框架 | CydiaSubstrate (`MSHookMessageEx`) |
| 设置控制器 | CSPluginMainViewController |

### 依赖框架

```
/usr/lib/libobjc.A.dylib
/System/Library/Frameworks/Foundation.framework/Foundation
/System/Library/Frameworks/CoreFoundation.framework/CoreFoundation
/System/Library/Frameworks/UIKit.framework/UIKit
/System/Library/Frameworks/LocalAuthentication.framework/LocalAuthentication
/usr/lib/libc++.1.dylib
/usr/lib/libSystem.B.dylib
/System/Library/Frameworks/AVFAudio.framework/AVFAudio
/System/Library/Frameworks/CoreGraphics.framework/CoreGraphics
/System/Library/Frameworks/CoreLocation.framework/CoreLocation
/System/Library/Frameworks/CoreText.framework/CoreText
/System/Library/Frameworks/ImageIO.framework/ImageIO
/System/Library/Frameworks/MapKit.framework/MapKit
/System/Library/Frameworks/PhotosUI.framework/PhotosUI
/System/Library/Frameworks/QuartzCore.framework/QuartzCore
/System/Library/Frameworks/SafariServices.framework/SafariServices
/System/Library/Frameworks/UserNotifications.framework/UserNotifications
@rpath/CydiaSubstrate.framework/CydiaSubstrate
```

---

## 二、消息时间显示功能架构总览

该插件的消息时间显示功能由 **三个子功能模块** 组成，它们协同工作：

```
┌─────────────────────────────────────────────────────────┐
│              消息时间显示功能架构                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌───────────────────┐  ┌────────────────────────────┐ │
│  │ 模块1: 消息气泡时间 │  │ 模块2: 隐藏聊天时间分隔线   │ │
│  │ (MessageTime)     │  │ (HideChatTime)             │ │
│  │                   │  │                            │ │
│  │ 在每条消息气泡上    │  │ 隐藏聊天界面中的            │ │
│  │ 显示精确时间标签    │  │ 时间分隔线                  │ │
│  └───────────────────┘  └────────────────────────────┘ │
│                                                         │
│  ┌───────────────────┐                                 │
│  │ 模块3: 添加时间后缀 │                                 │
│  │ (AddTimeSuffix)   │                                 │
│  │                   │                                 │
│  │ 在会话列表中显示    │                                 │
│  │ 好友添加时间        │                                 │
│  └───────────────────┘                                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 三、模块1: 消息气泡时间标签 (MessageTime) - 核心功能深度分析

### 3.1 功能描述

在每条聊天消息气泡上添加一个精确的时间标签，显示该消息的发送/接收时间。支持自定义格式、位置、颜色、字体等。

### 3.2 配置键 (NSUserDefaults)

| 配置键 | 类型 | 功能说明 |
|--------|------|----------|
| `com.wechat.enhance.messageTime.enabled` | BOOL | 是否启用消息时间标签 |
| `com.wechat.enhance.messageTime.fontSize` | CGFloat | 时间标签字体大小 |
| `com.wechat.enhance.messageTime.boldFont` | BOOL | 是否使用粗体 |
| `com.wechat.enhance.messageTime.customFormat` | NSString | 自定义时间格式模板 |
| `com.wechat.enhance.messageTime.position` | NSInteger | 时间标签位置 (枚举值) |
| `com.wechat.enhance.messageTime.offsetX` | CGFloat | X轴偏移量 |
| `com.wechat.enhance.messageTime.offsetY` | CGFloat | Y轴偏移量 |
| `com.wechat.enhance.messageTime.sender.textColor` | NSString(HEX) | 发送方文字颜色 |
| `com.wechat.enhance.messageTime.sender.textColor.dark` | NSString(HEX) | 发送方文字颜色(深色模式) |
| `com.wechat.enhance.messageTime.sender.backgroundColor` | NSString(HEX) | 发送方背景颜色 |
| `com.wechat.enhance.messageTime.sender.backgroundColor.dark` | NSString(HEX) | 发送方背景颜色(深色模式) |
| `com.wechat.enhance.messageTime.receiver.textColor` | NSString(HEX) | 接收方文字颜色 |
| `com.wechat.enhance.messageTime.receiver.textColor.dark` | NSString(HEX) | 接收方文字颜色(深色模式) |
| `com.wechat.enhance.messageTime.receiver.backgroundColor` | NSString(HEX) | 接收方背景颜色 |
| `com.wechat.enhance.messageTime.receiver.backgroundColor.dark` | NSString(HEX) | 接收方背景颜色(深色模式) |
| `com.wechat.enhance.messageTime.cornerRadius` | CGFloat | 时间标签圆角半径 |
| `com.wechat.enhance.messageTime.bubbleExtensionWidth` | CGFloat | 气泡扩展宽度 |

### 3.3 时间格式模板系统

插件实现了一套自定义的时间格式模板系统，使用花括号 `{}` 包裹的占位符：

#### 默认格式
```
{HH}:{mm}:{ss}
```
输出示例: `14:30:25`

#### 支持的占位符

| 占位符 | 含义 | 示例值 |
|--------|------|--------|
| `{yyyy}` | 四位年份 | 2026 |
| `{yy}` | 两位年份 | 26 |
| `{MM}` | 月份(补零) | 01-12 |
| `{dd}` | 日期(补零) | 01-31 |
| `{HH}` | 24小时制(补零) | 00-23 |
| `{hh}` | 12小时制(补零) | 01-12 |
| `{mm}` | 分钟(补零) | 00-59 |
| `{ss}` | 秒(补零) | 00-59 |
| `{EEEE}` | 星期全称 | Monday |
| `{EE}` | 星期缩写 | Mon |
| `{a}` | AM/PM标记 | AM/PM |
| `{b}` | 中文时段标记 | noon/midnight |

#### 格式化实现逻辑

```
1. 从 NSUserDefaults 读取 customFormat，若为空则使用默认格式 "{HH}:{mm}:{ss}"
2. 将自定义占位符替换为 NSDateFormatter 的标准格式:
   {yyyy} → yyyy
   {yy} → yy
   {MM} → MM
   {dd} → dd
   {HH} → HH
   {hh} → hh
   {mm} → mm
   {ss} → ss
   {EEEE} → EEEE
   {EE} → EE
   {a} → a
   {b} → 特殊处理(noon/midnight)
3. 使用 NSDateFormatter 进行格式化
4. Locale 设置:
   - 默认使用 en_US_POSIX 进行基础格式化
   - 对于中文时段标记使用 zh_Hans_CN
```

### 3.4 Hook目标类和方法

#### 被Hook的微信原生类

| 类名 | Hook方式 | 说明 |
|------|----------|------|
| `ChatTableViewCell` | MSHookMessageEx | 聊天消息单元格，添加时间标签 |
| `ChatTimeCellView` | MSHookMessageEx | 聊天时间分隔线视图，控制隐藏 |
| `ChatTimeViewModel` | MSHookMessageEx | 聊天时间分隔线数据模型 |

#### 插件自定义类

| 类名 | 父类 | 说明 |
|------|------|------|
| `CSMessageTimeSettingsViewController` | UITableViewController | 消息时间设置页面 |
| `CSTimeFormatEditorViewController` | UIViewController | 时间格式编辑器 |

### 3.5 核心方法分析

#### `initTimeLabel` - 初始化时间标签

```
功能: 在 ChatTableViewCell 中创建并初始化 UILabel 作为时间标签
实现推测:
  1. 创建 UILabel 实例
  2. 设置默认字体大小 (从 messageTime.fontSize 读取)
  3. 设置默认颜色
  4. 设置圆角 (从 messageTime.cornerRadius 读取)
  5. 设置背景色
  6. 将标签添加为 subview
  7. 保存到 m_timeLabel 实例变量
```

#### `setMessageTimePosition:` - 设置时间标签位置

```
功能: 根据 position 枚举值设置时间标签在气泡上的位置
可能的枚举值:
  - 顶部左/右
  - 底部左/右
  - 气泡内部顶部
  - 气泡内部底部
  - 气泡外部顶部
  - 气泡外部底部
实现推测:
  1. 读取 messageTime.position 配置
  2. 根据 position 值计算 label 的 frame
  3. 应用 offsetX/offsetY 微调
  4. 更新 label 的约束/frame
```

#### `showTimeFormatEditor` / `showTimeFormatInputAlert` - 时间格式编辑

```
功能: 显示时间格式编辑界面/弹窗
实现推测:
  1. 创建 UIAlertController 或自定义 ViewController
  2. 显示当前格式和可用占位符列表
  3. 允许用户输入自定义格式
  4. 保存到 messageTime.customFormat
```

### 3.6 时间数据获取流程

```
┌──────────────────────────────────────────────────────────────┐
│                    时间数据获取流程                            │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  1. ChatTableViewCell 渲染消息                                │
│     │                                                        │
│     ▼                                                        │
│  2. 从消息数据模型获取时间戳                                    │
│     │  - CMessageWrap.m_uiCreateTime (消息创建时间)            │
│     │  - 通过 viewModel → parentModel → subViewModels 访问    │
│     ▼                                                        │
│  3. 时间戳转换                                                │
│     │  - m_uiCreateTime 是 Unix 时间戳 (UInt32)               │
│     │  - 使用 dateWithTimeIntervalSince1970: 转换为 NSDate     │
│     ▼                                                        │
│  4. 格式化时间                                                │
│     │  - 读取 customFormat 配置                               │
│     │  - 替换占位符为 NSDateFormatter 格式                     │
│     │  - 使用 NSDateFormatter 格式化                          │
│     ▼                                                        │
│  5. 显示时间标签                                              │
│     │  - 判断消息方向 (发送/接收)                               │
│     │  - 应用对应颜色方案                                      │
│     │  - 设置位置和偏移                                        │
│     ▼                                                        │
│  6. 更新气泡布局                                              │
│        - 根据 bubbleExtensionWidth 调整气泡宽度               │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 3.7 关键实例变量

| 变量名 | 类型 | 所属类 | 说明 |
|--------|------|--------|------|
| `m_timeLabel` | UILabel* | ChatTableViewCell (Category) | 时间标签视图 |
| `m_uiCreateTime` | UInt32 | CMessageWrap | 消息创建时间戳 |
| `initialFormat` | NSString* | CSTimeFormatEditorViewController | 初始格式字符串 |
| `addTimeSuffixFormatItem` | CSSettingItem* | CSMessageTimeSettingsViewController | 添加时间后缀格式设置项 |

---

## 四、模块2: 隐藏聊天时间分隔线 (HideChatTime)

### 4.1 功能描述

隐藏微信聊天界面中的时间分隔线（如"上午 10:30"、"昨天"、"星期一"等），使聊天界面更加简洁。

### 4.2 配置键

| 配置键 | 类型 | 功能说明 |
|--------|------|----------|
| `com.wechat.tweak.hide_chat_time_enabled` | BOOL | 是否隐藏聊天时间分隔线 |

### 4.3 Hook实现分析

```
Hook目标: ChatTimeCellView / ChatTimeViewModel
Hook方式: MSHookMessageEx

实现推测:
  方案A - 隐藏 Cell:
    Hook ChatTimeCellView 的 layoutSubviews 或 didMoveToWindow
    设置 self.hidden = YES 或 self.frame = CGRectZero

  方案B - 修改 ViewModel:
    Hook ChatTimeViewModel 的高度计算方法
    返回高度为0，使 Cell 不显示

  方案C - 修改数据源:
    Hook 消息列表的数据源方法
    过滤掉 ChatTimeViewModel 类型的数据
```

### 4.4 相关的隐藏功能

| 配置键 | 说明 |
|--------|------|
| `com.wechat.tweak.hide_chat_time_enabled` | 隐藏聊天时间 |
| `com.wechat.tweak.hide_revoke_message_enabled` | 隐藏撤回消息提示 |
| `com.wechat.tweak.hide_pat_message_enabled` | 隐藏拍一拍消息 |
| `com.wechat.tweak.hide_voice_hint_enabled` | 隐藏语音提示 |
| `com.wechat.tweak.transparent_bubble_enabled` | 透明气泡 |
| `com.wechat.tweak.disable_dictation_enabled` | 禁用听写 |

---

## 五、模块3: 添加时间后缀 (AddTimeSuffix)

### 5.1 功能描述

在会话列表中，在好友昵称后显示该好友的添加时间。

### 5.2 配置键

| 配置键 | 类型 | 功能说明 |
|--------|------|----------|
| `com.wechat.enhance.chat.show.addtime.enabled` | BOOL | 是否显示添加时间 |
| `com.wechat.enhance.chat.addtime.suffix.format` | NSString | 添加时间后缀格式 |

### 5.3 数据来源

```
时间数据来源: CContact 中的 m_uiAddCreateTime
格式化方式: 使用配置的 suffix.format 进行格式化
显示位置: 会话列表中好友昵称后面
格式示例: "张三 (2026-01-15)"
```

### 5.4 关键方法

| 方法名 | 说明 |
|--------|------|
| `showAddTimeSuffixFormatInputDialog` | 显示添加时间格式编辑弹窗 |
| `setAddTimeSuffixFormatItem:` | 设置添加时间格式项 |
| `addTimeSuffixFormatItem` | 获取添加时间格式项 |

---

## 六、设置界面结构

### 6.1 CSMessageTimeSettingsViewController

```
CSMessageTimeSettingsViewController (UITableViewController)
├── 启用开关 (messageTime.enabled)
├── 字体大小 (messageTime.fontSize)
├── 粗体 (messageTime.boldFont)
├── 自定义格式 (messageTime.customFormat)
│   └── showTimeFormatEditor / showTimeFormatInputAlert
├── 位置 (messageTime.position)
│   └── setMessageTimePosition:
├── X偏移 (messageTime.offsetX)
├── Y偏移 (messageTime.offsetY)
├── 发送方文字颜色 (messageTime.sender.textColor)
├── 发送方背景颜色 (messageTime.sender.backgroundColor)
├── 接收方文字颜色 (messageTime.receiver.textColor)
├── 接收方背景颜色 (messageTime.receiver.backgroundColor)
├── 圆角半径 (messageTime.cornerRadius)
└── 气泡扩展宽度 (messageTime.bubbleExtensionWidth)
```

### 6.2 CSTimeFormatEditorViewController

```
CSTimeFormatEditorViewController (UIViewController)
├── 格式输入框
├── 占位符说明
│   ├── {yyyy} - 四位年份
│   ├── {yy} - 两位年份
│   ├── {MM} - 月份
│   ├── {dd} - 日期
│   ├── {HH} - 24小时
│   ├── {hh} - 12小时
│   ├── {mm} - 分钟
│   ├── {ss} - 秒
│   ├── {EEEE} - 星期全称
│   ├── {EE} - 星期缩写
│   ├── {a} - AM/PM
│   └── {b} - 时段(noon/midnight)
├── 实时预览
└── 保存/取消按钮
```

---

## 七、完整方法列表 (消息时间相关)

### 7.1 核心方法

| 方法选择器 | 类型 | 说明 |
|------------|------|------|
| `initTimeLabel` | 实例方法 | 初始化时间标签 |
| `m_timeLabel` | 属性 | 时间标签视图 |
| `setMessageTimePosition:` | 实例方法 | 设置时间标签位置 |
| `initialFormat` | 属性 | 初始格式 |
| `setInitialFormat:` | 属性设置 | 设置初始格式 |
| `createTime` | 实例方法 | 创建时间相关设置 |
| `m_uiCreateTime` | 属性 | 消息创建时间戳 |
| `setM_uiCreateTime:` | 属性设置 | 设置消息创建时间戳 |
| `calculateNextUpdateTime` | 实例方法 | 计算下次更新时间 |

### 7.2 设置界面方法

| 方法选择器 | 说明 |
|------------|------|
| `showTimeFormatEditor` | 显示时间格式编辑器 |
| `showTimeFormatInputAlert` | 显示时间格式输入弹窗 |
| `showAddTimeSuffixFormatInputDialog` | 显示添加时间后缀格式弹窗 |
| `addTimeSuffixFormatItem` | 添加时间后缀格式设置项 |
| `setAddTimeSuffixFormatItem:` | 设置添加时间后缀格式项 |
| `_addTimeSuffixFormatItem` | 添加时间后缀格式项(ivar) |

### 7.3 时间线相关方法

| 方法选择器 | 说明 |
|------------|------|
| `createTimelineBorderSettings` | 创建时间线边框设置 |
| `loadTimelineBorderColor` | 加载时间线边框颜色 |
| `showTimelineBorderColorPicker` | 显示时间线边框颜色选择器 |
| `isEditingTimeLineSetting` | 是否正在编辑时间线设置 |
| `setIsEditingTimeLineSetting:` | 设置编辑时间线状态 |

---

## 八、改造步骤

### 8.1 改造方向一: 修改时间格式系统

**目标**: 增加更多时间格式占位符或修改默认格式

**步骤**:

1. **定位格式化代码**
   - 在二进制中搜索 `{HH}:{mm}:{ss}` 字符串 (偏移 0xe5f57)
   - 搜索 `{yyyy}` 等占位符定义区域 (偏移 0xe5f9f 附近)
   - 搜索 `en_US_POSIX` 和 `zh_Hans_CN` locale 标识

2. **修改占位符替换逻辑**
   - 找到占位符替换的代码段 (ARM64 汇编)
   - 添加新的占位符支持 (如 `{M}` 不补零月份、`{d}` 不补零日期)
   - 需要修改 __cstring 段中的占位符定义字符串
   - 需要修改 __text 段中的替换逻辑代码

3. **修改默认格式**
   - 将 `{HH}:{mm}:{ss}` 替换为新格式 (注意长度需一致或调整偏移)
   - 如需更长格式字符串，需要使用 inline hook 技术跳转到新代码区

4. **具体操作** (使用 Theos/Logos):
   ```objc
   // 在 Tweak.x 中添加新的格式化方法
   %hook ChatTableViewCell
   - (void)initTimeLabel {
       %orig;
       // 修改默认格式
       if (![[NSUserDefaults standardUserDefaults] objectForKey:@"com.wechat.enhance.messageTime.customFormat"]) {
           [[NSUserDefaults standardUserDefaults] setObject:@"{yyyy}-{MM}-{dd} {HH}:{mm}:{ss}" 
                                                     forKey:@"com.wechat.enhance.messageTime.customFormat"];
       }
   }
   %end
   ```

### 8.2 改造方向二: 修改时间标签位置和样式

**目标**: 自定义时间标签的显示位置、大小、动画等

**步骤**:

1. **Hook initTimeLabel 方法**
   ```objc
   %hook ChatTableViewCell
   - (void)initTimeLabel {
       %orig;
       // 获取时间标签
       UILabel *timeLabel = [self valueForKey:@"m_timeLabel"];
       if (timeLabel) {
           // 修改样式
           timeLabel.font = [UIFont systemFontOfSize:10 weight:UIFontWeightMedium];
           timeLabel.layer.cornerRadius = 4;
           timeLabel.layer.masksToBounds = YES;
           // 添加阴影
           timeLabel.layer.shadowColor = [UIColor blackColor].CGColor;
           timeLabel.layer.shadowOffset = CGSizeMake(0, 1);
           timeLabel.layer.shadowOpacity = 0.3;
       }
   }
   %end
   ```

2. **Hook layoutSubviews 控制位置**
   ```objc
   %hook ChatTableViewCell
   - (void)layoutSubviews {
       %orig;
       UILabel *timeLabel = [self valueForKey:@"m_timeLabel"];
       if (timeLabel && !timeLabel.hidden) {
           // 自定义位置逻辑
           // 例如: 将时间标签放在气泡右上角
           CGRect bubbleFrame = /* 获取气泡frame */;
           timeLabel.frame = CGRectMake(bubbleFrame.origin.x + bubbleFrame.size.width - 60,
                                        bubbleFrame.origin.y - 15,
                                        55, 14);
       }
   }
   %end
   ```

### 8.3 改造方向三: 增加相对时间显示

**目标**: 显示"刚刚"、"3分钟前"、"1小时前"等相对时间

**步骤**:

1. **创建相对时间格式化工具类**
   ```objc
   @interface CSRelativeTimeFormatter : NSObject
   + (NSString *)relativeTimeStringFromDate:(NSDate *)date;
   @end

   @implementation CSRelativeTimeFormatter
   + (NSString *)relativeTimeStringFromDate:(NSDate *)date {
       NSTimeInterval interval = [[NSDate date] timeIntervalSinceDate:date];
       
       if (interval < 60) {
           return @"刚刚";
       } else if (interval < 3600) {
           return [NSString stringWithFormat:@"%ld分钟前", (long)(interval / 60)];
       } else if (interval < 86400) {
           return [NSString stringWithFormat:@"%ld小时前", (long)(interval / 3600)];
       } else if (interval < 604800) {
           return [NSString stringWithFormat:@"%ld天前", (long)(interval / 86400)];
       } else {
           NSDateFormatter *formatter = [[NSDateFormatter alloc] init];
           formatter.dateFormat = @"yyyy-MM-dd HH:mm";
           return [formatter stringFromDate:date];
       }
   }
   @end
   ```

2. **Hook 时间标签更新逻辑**
   ```objc
   %hook ChatTableViewCell
   - (void)updateTimeLabel {
       // 获取消息时间
       unsigned int createTime = [[self valueForKeyPath:@"viewModel.parentModel.m_uiCreateTime"] unsignedIntValue];
       NSDate *messageDate = [NSDate dateWithTimeIntervalSince1970:createTime];
       
       // 使用相对时间
       NSString *timeString = [CSRelativeTimeFormatter relativeTimeStringFromDate:messageDate];
       
       UILabel *timeLabel = [self valueForKey:@"m_timeLabel"];
       timeLabel.text = timeString;
   }
   %end
   ```

3. **添加定时刷新机制**
   ```objc
   // 在 BaseMsgContentViewController 中添加定时器
   %hook BaseMsgContentViewController
   - (void)viewDidAppear:(BOOL)animated {
       %orig;
       // 每30秒刷新一次时间标签
       [NSTimer scheduledTimerWithTimeInterval:30
                                        target:self
                                      selector:@selector(refreshTimeLabels)
                                      userInfo:nil
                                       repeats:YES];
   }
   
   - (void)refreshTimeLabels {
       // 遍历可见 cell，更新时间标签
       UITableView *tableView = /* 获取 tableView */;
       for (ChatTableViewCell *cell in tableView.visibleCells) {
           [cell updateTimeLabel];
       }
   }
   %end
   ```

### 8.4 改造方向四: 修改隐藏时间分隔线逻辑

**目标**: 不完全隐藏时间分隔线，而是显示精确时间

**步骤**:

1. **Hook ChatTimeCellView 替代隐藏逻辑**
   ```objc
   %hook ChatTimeCellView
   - (void)layoutSubviews {
       %orig;
       
       BOOL hideChatTime = [[NSUserDefaults standardUserDefaults] boolForKey:@"com.wechat.tweak.hide_chat_time_enabled"];
       
       if (hideChatTime) {
           // 不隐藏，而是修改显示内容为精确时间
           // 找到时间标签
           for (UIView *subview in self.subviews) {
               if ([subview isKindOfClass:[UILabel class]]) {
                   UILabel *label = (UILabel *)subview;
                   // 获取原始时间数据
                   // 修改为精确时间格式
                   label.text = [self preciseTimeString];
                   label.font = [UIFont systemFontOfSize:10];
                   label.textColor = [UIColor grayColor];
               }
           }
       }
   }
   
   - (NSString *)preciseTimeString {
       // 从 viewModel 获取精确时间戳
       // 返回格式化后的精确时间
       return @"14:30:25"; // 示例
   }
   %end
   ```

2. **Hook ChatTimeViewModel 修改高度**
   ```objc
   %hook ChatTimeViewModel
   - (CGFloat)cellHeight {
       BOOL hideChatTime = [[NSUserDefaults standardUserDefaults] boolForKey:@"com.wechat.tweak.hide_chat_time_enabled"];
       if (hideChatTime) {
           // 返回较小高度而非0，保留精确时间显示
           return 20.0;
       }
       return %orig;
   }
   %end
   ```

### 8.5 改造方向五: 增加消息时间分组显示

**目标**: 按时间间隔对消息进行分组，在分组边界显示时间

**步骤**:

1. **实现时间分组逻辑**
   ```objc
   @interface CSMessageTimeGrouper : NSObject
   @property (nonatomic, assign) NSTimeInterval groupInterval; // 分组间隔(秒)
   - (BOOL)shouldShowTimeForMessage:(CMessageWrap *)message 
                        previousMessage:(CMessageWrap *)previousMessage;
   @end

   @implementation CSMessageTimeGrouper
   - (BOOL)shouldShowTimeForMessage:(CMessageWrap *)message 
                        previousMessage:(CMessageWrap *)previousMessage {
       if (!previousMessage) return YES;
       
       NSTimeInterval interval = message.m_uiCreateTime - previousMessage.m_uiCreateTime;
       return interval >= self.groupInterval;
   }
   %end
   ```

2. **Hook 消息列表数据源**
   ```objc
   %hook CommonMessageViewModel
   - (NSArray *)subViewModels {
       NSArray *orig = %orig;
       
       BOOL messageTimeEnabled = [[NSUserDefaults standardUserDefaults] boolForKey:@"com.wechat.enhance.messageTime.enabled"];
       if (!messageTimeEnabled) return orig;
       
       NSTimeInterval groupInterval = 300; // 5分钟
       NSMutableArray *result = [NSMutableArray array];
       CMessageWrap *previousMessage = nil;
       
       for (id viewModel in orig) {
           if ([viewModel isKindOfClass:NSClassFromString(@"ChatTimeViewModel")]) {
               // 保留原有时间分隔线
               [result addObject:viewModel];
               previousMessage = nil;
               continue;
           }
           
           CMessageWrap *currentMessage = /* 从 viewModel 获取 */;
           
           if ([self shouldShowTimeForMessage:currentMessage previousMessage:previousMessage]) {
               // 插入新的时间分隔线
               ChatTimeViewModel *timeViewModel = /* 创建时间 ViewModel */;
               [result addObject:timeViewModel];
           }
           
           [result addObject:viewModel];
           previousMessage = currentMessage;
       }
       
       return result;
   }
   %end
   ```

### 8.6 改造方向六: 直接修改二进制 (高级)

**目标**: 不使用 Theos，直接修改 dylib 二进制文件

**步骤**:

1. **修改默认时间格式字符串**
   ```
   原始: {HH}:{mm}:{ss} (偏移 0xe5f57, 14字节)
   修改为: {HH}:{mm} (10字节, 剩余用 \x00 填充)
   或修改为: {MM}-{dd} {HH}:{mm} (19字节, 需要空间)
   ```

2. **修改配置键默认值**
   - 找到 `com.wechat.enhance.messageTime.customFormat` 对应的 CFString
   - 修改其默认值指针指向新的格式字符串

3. **修改占位符定义**
   - 在偏移 0xe5f9f 附近找到占位符列表
   - 可以添加新占位符 (需要同时修改替换逻辑代码)

4. **使用 hex 编辑器修改**
   ```bash
   # 备份原始文件
   cp 微信优化1.6.5.dylib 微信优化1.6.5.dylib.bak
   
   # 修改默认格式 (将 {HH}:{mm}:{ss} 改为 {HH}:{mm})
   # 原始 hex: 7b 48 48 7d 3a 7b 6d 6d 7d 3a 7b 73 73 7d
   # 修改为:   7b 48 48 7d 3a 7b 6d 6d 7d 00 00 00 00 00
   printf '\x7b\x48\x48\x7d\x3a\x7b\x6d\x6d\x7d\x00\x00\x00\x00\x00' | \
     dd of=微信优化1.6.5.dylib bs=1 seek=$((0xe5f57)) conv=notrunc
   ```

5. **重签名**
   ```bash
   # 修改后需要重新签名
   ldid -S 微信优化1.6.5.dylib
   ```

---

## 九、关键偏移地址速查表

| 内容 | 文件偏移 | 说明 |
|------|----------|------|
| `{HH}:{mm}:{ss}` | 0xe5f57 | 默认时间格式模板 |
| `{yyyy}` 等占位符 | 0xe5f9f | 格式占位符定义区域 |
| `en_US_POSIX` | 0xe5fbf | 基础格式化 Locale |
| `zh_Hans_CN` | 0xe5fcb | 中文 Locale |
| `noon` / `midnight` | 0xe5fd5 | 时段标记 |
| `ChatTableViewCell` | 0xe6050 | Hook 目标类名 |
| `messageTime.enabled` | 0xe5c97 | 启用开关配置键 |
| `messageTime.fontSize` | 0xe5cbf | 字体大小配置键 |
| `messageTime.boldFont` | 0xe5ce7 | 粗体配置键 |
| `messageTime.customFormat` | 0xe5d13 | 自定义格式配置键 |
| `messageTime.position` | 0xe5d3b | 位置配置键 |
| `messageTime.offsetX` | 0xe5d62 | X偏移配置键 |
| `messageTime.offsetY` | 0xe5d89 | Y偏移配置键 |
| `messageTime.sender.textColor` | 0xe5db9 | 发送方文字颜色键 |
| `messageTime.sender.backgroundColor` | 0xe5dee | 发送方背景颜色键 |
| `messageTime.receiver.textColor` | 0xe5e24 | 接收方文字颜色键 |
| `messageTime.receiver.backgroundColor` | 0xe5e5f | 接收方背景颜色键 |
| `messageTime.cornerRadius` | 0xe5e91 | 圆角配置键 |
| `messageTime.bubbleExtensionWidth` | 0xe602f | 气泡扩展宽度键 |
| `hide_chat_time_enabled` | 0xe4a5c | 隐藏时间分隔线键 |
| `ChatTimeCellView` | 0xe4a72 | 时间分隔线视图类名 |
| `ChatTimeViewModel` | 0xe4a83 | 时间分隔线模型类名 |
| `m_uiAddCreateTime` | 0xe3d0d | 添加时间属性 |
| `m_uiCreateTime` | 0xf3e0a | 消息创建时间属性 |
| `m_timeLabel` | 0xf3dfe | 时间标签属性 |
| `initTimeLabel` | 0xf2ad0 | 初始化时间标签方法名 |
| `CSMessageTimeSettingsViewController` | 0xe943e | 设置控制器类名 |
| `CSTimeFormatEditorViewController` | - | 格式编辑器类名 |

---

## 十、Mach-O 段布局

| 段名 | 节名 | VMA | 大小 | 说明 |
|------|------|-----|------|------|
| __TEXT | __text | 0x4000 | 0xBDD60 | 代码段 |
| __TEXT | __stubs | 0xC1D60 | 0x5AC | 桩代码 |
| __TEXT | __objc_stubs | 0xC2320 | 0xEFA0 | ObjC桩代码 |
| __TEXT | __objc_methlist | 0xD1390 | 0x6910 | 方法列表 |
| __TEXT | __cstring | 0xE27C8 | 0x6F09 | C字符串 |
| __TEXT | __objc_methname | 0xE96D1 | 0x12648 | 方法名 |
| __TEXT | __ustring | 0xFBFE0 | 0x76FA | UTF-16字符串 |
| __TEXT | __objc_classname | 0x1036DA | 0x93F | 类名 |
| __TEXT | __objc_methtype | 0x104019 | 0x1FBE | 方法类型 |
| __DATA_CONST | __cfstring | 0x10E468 | 0x12A40 | CFString常量 |
| __DATA_CONST | __objc_classlist | 0x120EA8 | 0x228 | 类列表 |
| __DATA | __objc_const | 0x124000 | 0xF7F0 | ObjC常量 |
| __DATA | __objc_selrefs | 0x1337F0 | 0x4718 | 选择器引用 |
| __DATA | __objc_classrefs | 0x137F08 | 0x448 | 类引用 |
| __DATA | __objc_ivar | 0x138518 | 0x6F8 | 实例变量 |
| __DATA | __objc_data | 0x138C10 | 0x1590 | ObjC数据 |

---

## 十一、注意事项

1. **二进制修改风险**: 直接修改 dylib 二进制文件风险极高，任何偏移错误都可能导致崩溃。建议使用 Theos/Logos 编写独立 tweak。

2. **ARM64 汇编**: 修改代码段需要理解 ARM64 指令集，指令长度固定为 4 字节。

3. **配置键命名规范**: 插件使用 `com.wechat.enhance.*` 和 `com.wechat.tweak.*` 两种前缀，enhance 通常表示增强功能，tweak 表示调整/修改功能。

4. **深色模式支持**: 所有颜色配置都提供了 `.dark` 后缀的深色模式变体，改造时需同时处理。

5. **CydiaSubstrate 依赖**: 插件依赖 CydiaSubstrate 框架的 `MSHookMessageEx` 函数进行方法 Hook，改造时需确保该框架可用。

6. **代码签名**: 修改 dylib 后需要重新签名才能在 iOS 上加载。

7. **版本兼容性**: 该插件针对特定版本的微信，微信更新后类名和方法可能变化，需要重新分析适配。
