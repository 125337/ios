# 微信优化 1.6.5 — 自定义时间格式 深度分析

> 基于反编译文件 `123456.c` 和原始 dylib `微信优化1.6.5.dylib` 的 strings 提取

---

## 一、架构概览

自定义时间格式涉及两个 ViewController 和一个格式化引擎：

| 类 | 作用 |
|----|------|
| `CSMessageTimeSettingsViewController` | 消息时间设置主页，含"自定义格式"入口 |
| `CSTimeFormatEditorViewController` | 格式编辑器完整 UI，格式预览 + 输入 |
| `FUN_000354d0`（格式化引擎） | 在 `cellForRow`/`updateNodeStatus` 中调用，解析格式字符串 |

---

## 二、UI 结构 — CSTimeFormatEditorViewController

### 2.1 页面布局（由上到下）

```
┌─────────────────────────────────────────┐
│  导航栏                                    │
│  ← 关闭         恢复默认    保存 →         │
├─────────────────────────────────────────┤
│  [格式说明表格]                            │
│  ┌──────┬──────────────────────────────┐ │
│  │{yyyy}│  年份 (2026)                  │ │  ← 左侧：粗体 UILabel
│  │{MM}  │  月份 (01-12)                 │ │  ← 右侧：常规 UILabel
│  │{dd}  │  日期 (01-31)                 │ │
│  │{HH}  │  小时 24小时制 (00-23)         │ │
│  │{mm}  │  分钟 (00-59)                 │ │
│  │{ss}  │  秒 (00-59)                   │ │
│  └──────┴──────────────────────────────┘ │
│                                          │
│  自定义格式                               │  ← section header
│  ┌──────────────────────────────────────┐│
│  │年  月  日  时  分  秒     → 预览文字   ││  ← 预览标签行
│  │2026 05 19 14 30 00    → 2026-05-19   ││
│  └──────────────────────────────────────┘│
│                                          │
│  输入格式                                 │  ← section header
│  ┌──────────────────────────────────────┐│
│  │ {HH}:{mm}:{ss}                       ││  ← UITextView 编辑区
│  └──────────────────────────────────────┘│
│                                          │
│  预览                                     │  ← section header
│  ┌──────────────────────────────────────┐│
│  │ 14:30:00                             ││  ← 实时预览 UITextView（只读）
│  └──────────────────────────────────────┘│
└─────────────────────────────────────────┘
```

### 2.2 导航栏按钮

| 位置 | 标题 | 行为 |
|------|------|------|
| 左侧 | 关闭 | `dismiss` — 返回，不保存 |
| 右侧 | 恢复默认 | `restoreDefault` — 重置为 `{HH}:{mm}:{ss}` |
| 右侧 | 保存 | `saveAndDismiss` — 保存到 NSUserDefaults 并返回 |

### 2.3 编辑区特性

- **键盘监听**：注册了 `UIKeyboardWillShow`/`UIKeyboardWillHide` 通知
- **实时预览**：`textViewDidChange:` 触发 `updatePreview`，输入即见
- **滚动适配**：键盘弹起时自动调整 contentInset
- **默认格式**：`{HH}:{mm}:{ss}`（仅时分秒）

---

## 三、自定义格式引擎

### 3.1 存储键

```
NSUserDefaults key: com.wechat.enhance.messageTime.customFormat
```

写入时若为 nil 则初始化默认值 `{HH}:{mm}:{ss}`。

### 3.2 格式字符串解析流程

```
用户输入: "{yyyy}-{MM}-{dd} {HH}:{mm}:{ss}"
           ↓
1. stringByReplacingOccurrencesOfString:@"\n" withString:@""  (去换行)
           ↓
2. NSRegularExpression pattern: ","
   分割为多个格式（支持亮暗双模）
   若 userFormat = "fmt1,fmt2"
      → lightFormat = "fmt1", darkFormat = "fmt2"
   若 userFormat = "fmt"
      → 亮暗共用 "fmt"
           ↓
3. 遍历 NSRegularExpression 匹配 {token}
   对每个 {token} 做令牌替换
           ↓
4. 令牌替换 → 标准 NSDateFormatter format string
           ↓
5. 特殊令牌处理 → locale-aware 格式化 + 字符串替换
           ↓
6. 最终输出：formatString + locale-aware replacements
```

### 3.3 令牌映射表

#### 标准令牌（直接映射到 NSDateFormatter format specifier）

| 令牌 | NSDateFormatter | 说明 | 示例输出 |
|------|-----------------|------|---------|
| `{yyyy}` | `yyyy` | 4 位年份 | 2026 |
| `{MM}` | `MM` | 2 位月份 | 05 |
| `{dd}` | `dd` | 2 位日期 | 19 |
| `{HH}` | `HH` | 24 小时制 | 14 |
| `{mm}` | `mm` | 分钟 | 30 |
| `{ss}` | `ss` | 秒 | 00 |

#### 特殊令牌（自定义 locale 处理）

| 令牌 | NSDateFormatter | locale | 后处理 | 输出 |
|------|-----------------|--------|--------|------|
| `{EE}` | `E` | `zh_Hans_CN` | replace 上午→上午, 下午→下午 | 周一 / 上午 |
| `{EEEE}` | `EEEE` | `zh_Hans_CN` | 无 | 星期一 |
| `{a}` | `a` | `en_US_POSIX` | 无 | AM / PM |
| `{b}` | `B` | `en_US_POSIX` | replace noon→PM, midnight→AM | AM / PM |

> **注意**：`{EE}` 实际上映射的是 NSDateFormatter 的 `E`（一周中第几天，中文返回"周一"~"周日"），但代码中做了 `replace "上午" with "下午"` 的替换。这可能是因为 `E` 在特定条件下也会返回上午/下午，因此后处理将其标准化为"下午"。实际上经过 `zh_Hans_CN` locale 的 `E` 格式输出为周缩写。

### 3.4 标准令牌在 updateNodeStatus 中的应用

在 `FUN_0003a06c`（updateNodeStatus）和 `FUN_0003c790`（cellForRow 全局队列）中，时间格式化使用的是同一个引擎。流程：

```
1. 读取 customFormat string
2. 若为 nil → 默认 "HH:mm:ss"
3. 去掉换行符
4. 按 , 分割 → [lightFmt, darkFmt]
5. 根据当前暗色模式选择 format
6. 正则匹配 {token}
7. 每次匹配：
   - substring[1..length-2] = token名称(如"HH")
   - 取出 {token} 前后括号之间的部分
   - 若长度 < 2 → 替换为 ""
   - 若长度 == 2 → 取 1 个字符作为 NSDateFormatter format
   - 若长度 >= 3 → 取 2 个字符作为 NSDateFormatter format
8. 拼接替换后的字符串
9. 遍历特殊令牌数组，替换：
   - {EE}  → zh_Hans_CN locale + E format
   - {EEEE} → zh_Hans_CN locale + EEEE format  
   - {a}   → en_US_POSIX locale + a format
   - {b}   → en_US_POSIX locale + a format + noon/midnight 替换
10. 输出最终格式化字符串
```

### 3.5 双格式模式（亮/暗）

格式字符串中使用 **逗号 `,`** 分隔亮色和暗色模式：

```
"{HH}:{mm}:{ss},{HH}:{mm}:{ss}"
  ↑ 亮色模式          ↑ 暗色模式
```

- 若只有一段 → 亮暗共用
- 若有逗号 且 mode=1(暗) → 使用第二段
- 若有逗号 且 mode=0(亮) → 使用第一段
- 若有逗号 且 mode=2 → 使用第一段（默认亮）

---

## 四、格式编辑器 UI 完整 Strings

### 4.1 从 dylib 提取的 strings

| 字符串 | 用途 |
|--------|------|
| `{yyyy}` | 年份令牌 |
| `{MM}` | 月份令牌 |
| `{dd}` | 日期令牌 |
| `{HH}` | 小时令牌 |
| `{mm}` | 分钟令牌 |
| `{ss}` | 秒令牌 |
| `{EE}` | 中文周/上午下午令牌 |
| `{EEEE}` | 中文完整星期令牌 |
| `AM/PM` | {a}/{b} 令牌说明 |
| `{HH}:{mm}:{ss}` | 默认格式/编辑器默认值 |
| `yyyy-MM-dd` | 完整日期格式 |
| `yyyy-MM-dd HH:mm:ss` | 完整日期时间格式 |
| `zh_Hans_CN` | 中文 locale |
| `en_US_POSIX` | 英文 POSIX locale |
| `EEEE` | NSDateFormatter 星期格式 |
| `yyyy` | NSDateFormatter 年份格式 |

### 4.2 说明表格的推断内容

由于 dylib strings 中没有提取到完整的中文字符串（可能以 UTF-16 存储），以下为根据代码结构推断的说明文本：

| 令牌 | 说明 1（左列 粗体） | 说明 2（右列） | 预览示例 |
|------|-------------------|---------------|---------|
| `{yyyy}` | yyyy | 年份 (2026) | 2026 |
| `{MM}` | MM | 月份 (01-12) | 05 |
| `{dd}` | dd | 日期 (01-31) | 19 |
| `{HH}` | HH | 小时 24小时制 (00-23) | 14 |
| `{mm}` | mm | 分钟 (00-59) | 30 |
| `{ss}` | ss | 秒 (00-59) | 00 |

### 4.3 特殊令牌表格的推断内容

| 令牌 | NSDateFormatter | locale | 说明 |
|------|-----------------|--------|------|
| `{EE}` | E | zh_Hans_CN | 中文星期缩写 / 上午下午 |
| `{EEEE}` | EEEE | zh_Hans_CN | 完整中文星期 |
| `{a}` | a | en_US_POSIX | AM/PM |
| `{b}` | a | en_US_POSIX | 上午/下午（英文后处理） |

---

## 五、CSSettingItem 入口配置

在 `CSMessageTimeSettingsViewController::setupData` 中，自定义格式入口：

```objc
// 伪代码还原
CSSettingItem *item = [[CSSettingItem alloc] init];
item.title = @"自定义格式";       // cf__IN<h_
item.icon  = @"text.alignleft";   // cf_text_alignleft
item.detail = customFormatString ?: @"{HH}:{mm}:{ss}";
item.selectionBlock = ^{
    CSTimeFormatEditorViewController *editor = [[CSTimeFormatEditorViewController alloc] init];
    editor.initialFormat = customFormatString;
    editor.saveBlock = ^(NSString *newFormat) {
        [defaults setObject:newFormat forKey:@"com.wechat.enhance.messageTime.customFormat"];
    };
    [self.navigationController pushViewController:editor animated:YES];
};
```

---

## 六、代码关键函数索引

| 函数地址 | 函数名 / 推断用途 |
|----------|------------------|
| `0x354d0` | 时间格式解析引擎（cellForRow/updateNodeStatus 共用） |
| `0x87222` | `CSTimeFormatEditorViewController::viewDidLoad` |
| `0x87940` | `CSTimeFormatEditorViewController::updatePreview` |
| `0x88231` | `CSTimeFormatEditorViewController::restoreDefault` |
| `0x88251` | `CSTimeFormatEditorViewController::saveAndDismiss` |
| `0x88286` | `CSTimeFormatEditorViewController::dismiss` |
| `0x88396` | `CSMessageTimeSettingsViewController::viewDidLoad` |
| `0x88558` | `CSMessageTimeSettingsViewController::setupData` |
| `0x4812` | `FUN_00005568` — 构建日期组件字典（`{yyyy: ..., MM: ..., ...}`） |

---

## 七、与本插件对比

| 特性 | 微信优化 1.6.5 | 本插件（当前） |
|------|---------------|--------------|
| 自定义格式支持 | ✅ 完整 UI + 6 标准令牌 + 4 特殊令牌 | ❌ 仅 `HH:mm:ss` |
| 双格式（亮/暗） | ✅ 逗号分隔 | ❌ |
| 实时预览 | ✅ updatePreview | ❌ |
| 恢复默认 | ✅ `{HH}:{mm}:{ss}` | 无 UI |
| locale 感知 | ✅ zh_Hans_CN + en_US_POSIX | ❌ |
| 键盘适配 | ✅ contentInset 调整 | N/A |
| 格式编辑器 UI | ✅ 完整表格 + 编辑区 + 预览区 | ❌ |
| 令牌说明 | ✅ 6 行说明 + 预览 | ❌ |
| NSUserDefaults key | `com.wechat.enhance.messageTime.customFormat` | `messageTimeFormat` (仅值) |

---

## 八、实现建议

若要复刻微信优化的完整时间格式功能，需要：

### P0（核心）
1. **格式解析引擎** — 正则匹配 `{token}`，映射到 NSDateFormatter specifiers
2. **自定义格式字段** — NSUserDefaults 存储完整格式字符串
3. **特殊令牌** — `{EE}`/`{EEEE}`/`{a}`/`{b}` 的 locale-aware 处理

### P1（UI）
4. **格式编辑器 VC** — UITextView + 实时预览
5. **令牌说明表** — 帮助用户理解可用令牌
6. **双格式** — 逗号分隔亮暗双模式

### P2（体验）
7. **恢复默认按钮**
8. **键盘适配**
9. **保存/取消导航**