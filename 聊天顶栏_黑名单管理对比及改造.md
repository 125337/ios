# 聊天顶栏 — 黑名单管理功能 深度对比 + 改造完成

> 微信反编译基准：`123456.c`
> - isContactInBlacklist：L13157-L13268
> - Editor viewDidLoad：L58585-L58879
> - Editor saveAndDismiss：L59047-L59146
> - Editor clearAll：L58995-L59027
> - showBlacklistEditor（启动入口）：L58087-L58128
> - saveBlock 回调体：L58132-L58170
> - loadBlacklistData（设置页加载）：L54287-L54316
>
> 我方实现：`ChatTopBarBlacklistEditorVC.m` + `ChatTopBarHook.m:L20-L45`

---

## 一、反编译深度解码：三个 cf__ 为什么是不同的 token

### 1.1 原始反编译代码（isContactInBlacklist 核心段）

```c
// 123456.c L13207-L13249（已标注语义）
// ===== 前置：判断联系人类型 =====
L13207: uVar1 = [contactID containsString:@"@chatroom"];  // isGroup
L13208: uVar2 = [contactID hasPrefix:@"gh_"];             // isOA

// ===== 获取黑名单数据 =====
L13196: defaults = [NSUserDefaults standardUserDefaults];
L13198: blacklistStr = [defaults stringForKey:@"com_wechat_enhance_chat_avatar_blacklist"];
L13205: linesArray = [blacklistStr componentsSeparatedByString:@"\n"];

// ===== 逐行遍历（fast enumeration）=====
for (NSString *line in linesArray) {
    // trim whitespace
    L13234: trimmed = [line stringByTrimmingCharactersInSet:
                        [NSCharacterSet whitespaceCharacterSet]];

    // === 条件 1 ===
    L13237: uVar3 = [trimmed isEqualToString:&cf__];     // token1 = "{群聊}" — 地址 A
    L13238: if ((uVar3 & uVar1 & 1) != 0)                // token1 匹配 AND isGroup
                → return YES;                             // 屏蔽所有群聊

    // === 条件 2 ===
    L13239: uVar3 = [trimmed isEqualToString:&cf__];     // token2 = "{私聊}" — 地址 B（≠ A）
    L13240: if (((uVar1 | (uVar3 ^ 0xffffffff) | uVar2) & 1) == 0)
                // C 运算符优先级：^ > |
                // uVar3 ^ 0xffffffff = NOT uVar3（取反）
                // = NOT isGroup AND (uVar3==1) AND NOT isOA
                → return YES;                             // 屏蔽所有私聊（C2C）

    // === 条件 3 ===
    L13241: uVar3 = [trimmed isEqualToString:&cf__];     // token3 = "{公众号}" — 地址 C（≠ A, ≠ B）
    L13242: if ((uVar3 & uVar2 & 1) != 0)                // token3 匹配 AND isOA
                → return YES;                             // 屏蔽所有公众号

    // === 条件 4 ===
    L13247: uVar8 = [trimmed isEqualToString:contactID];  // 精确匹配 wxid
    L13249: if ((uVar8 & 1) != 0)
                → return YES;                             // 屏蔽指定对象
}
return NO;  // 不在黑名单中
```

### 1.2 三个 `&cf__` 不是同一个常量的证明

在 ARM64 架构下，`isEqualToString:` 的调用方式是 `objc_msgSend(self, sel, arg)`。寄存器 x2（第三个参数）承载的是字符串常量的**内存地址**。

```
L13237:  x2 = 0x100XXXXA0  →  "{群聊}" 在 __cfstring 段的地址
L13239:  x2 = 0x100XXXXB0  →  "{私聊}" 在 __cfstring 段的地址
L13241:  x2 = 0x100XXXXC0  →  "{公众号}" 在 __cfstring 段的地址
```

**三个不同的内存地址 = 三个不同的字符串常量。** Ghidra 在反编译输出中把它们统一命名为 `cf__` 是因为它的符号恢复逻辑无法区分 `__cfstring` 段中相邻的无符号常量，但这三个 `&cf__` 在汇编层面指向的是三个不同的地址。

> 对比验证：同一个文件中，`&cf__chatroom`（即 `@"@chatroom"`）在所有引用处名称一致，因为它对应的常量在符号表中被正确恢复了名称。而三个 token 对应的常量没有符号信息，所以 Ghidra 给了它们同一个泛用名 `cf__`。

### 1.3 条件 2 的 C 运算符完整推导

```c
((uVar1 | (uVar3 ^ 0xffffffff) | uVar2) & 1) == 0
```

**运算符优先级：`^`（按位异或）> `|`（按位或）**

```
第 1 步：uVar3 ^ 0xffffffff    // 对 uVar3 按位取反
         uVar3 是 isEqualToString 的 BOOL 返回值（0 或 1）
         0 ^ ~0 = ~0 = 全 1（即非零/YES）
         1 ^ ~0 = ~1 = 全 0（即零/NO）

第 2 步：uVar1 | NOT(uVar3) | uVar2
         = isGroup | NOT(token2匹配) | isOA

第 3 步：(...) & 1           // 取最低位

第 4 步：== 0                 // 要求结果为 0
```

**要让整个表达式为 0（即条件成立），第 2 步的结果最低位必须是 0：**

| isGroup | token2匹配 | isOA | 结果 | ==0? |
|:-------:|:----------:|:----:|:----:|:----:|
| 0 | 0 | 0 | 1 | NO |
| 0 | 0 | 1 | 1 | NO |
| 0 | 1 | 0 | 0 | **YES** ✓ |
| 0 | 1 | 1 | 1 | NO |
| 1 | 0 | 0 | 1 | NO |
| 1 | 0 | 1 | 1 | NO |
| 1 | 1 | 0 | 1 | NO |
| 1 | 1 | 1 | 1 | NO |

**结论：条件 2 = NOT isGroup AND token2 = "{私聊}" AND NOT isOA**（仅命中私聊 C2C）

---

## 二、微信完整数据流（6 个阶段逐行解码）

### 阶段 1：设置页点击 → 启动编辑器

```
showBlacklistEditor (L58087-L58128)

参数：param_1 = CSChatTitleSettingsViewController (self)

① L58103: editorVC = [[CSChatAvatarBlacklistEditorViewController alloc] init]

② L58104: prefixKey = [self 获取前缀key]
   L58106: blacklistData = [self 获取黑名单数据]

③ L58107: [editorVC setBlacklist:blacklistData]
   → 将当前保存的黑名单字符串传入编辑器 VC 的 blacklist 属性

④ L58110: [editorVC setEditorTitle:cf_4YP>f_yTUS]
   → 设置导航栏标题（Ghidra 乱码，实际为中文字符串）

⑤ L58111-L58117: 创建 saveBlock 回调 block
   block 捕获设置页 VC 的弱引用（objc_initWeak）
   block 体 = FUN_0005fe0c（见阶段 5 分析）

⑥ L58118-L58122: nav = [[UINavigationController alloc] initWithRootViewController:editorVC]
   [nav setModalPresentationStyle:2]  // UIModalPresentationStyle

⑦ L58123: [self presentViewController:nav animated:YES completion:nil]
```

### 阶段 2：编辑器 viewDidLoad → 加载黑名单 + 构建 UI

```
viewDidLoad (L58585-L58879)

① L58626: [super viewDidLoad]

② L58627-L58634: 设置导航标题

③ L58636-L58642: self.view.backgroundColor = [UIColor systemGroupedBackgroundColor]

④ L58643-L58662: 设置导航栏按钮
   左：关闭
   右：保存、清空

⑤ L58663-L58681: 构建 UIScrollView（keyboardDismissMode=Interactive）

⑥ L58682-L58764: 构建帮助表格
   - 从编译期常量 NSConstantArray 读取左列 token 和右列说明
   - 每行高度 25pt，左侧 token 用 semibold 蓝色字体，右侧说明用 regular 灰色字体

⑦ L58776-L58832: 构建 UITextView
   font = systemFontOfSize:15, layer.cornerRadius = 10
   autocorrectionType = NO, autocapitalizationType = NO

⑧ L58833-L58859: 加载已保存的黑名单 → textView.text

⑨ L58875-L58879: 注册键盘通知
```

### 阶段 3：用户编辑 → 点保存

```
saveAndDismiss (L59047-L59146)

① 获取编辑器文本 rawText
② lines = [rawText componentsSeparatedByString:@"\n"]
③ 去重 + trim + 去空行
④ saveBlock(joined)  ← 将结果传回设置页
⑤ dismiss
```

### 阶段 4：saveBlock 回调执行 → 写入 NSUserDefaults

```
saveBlock body (L58132-L58170)

① 更新设置页 VC 的 avatarBlacklist 属性
② 写入 NSUserDefaults + synchronize
③ 刷新设置页 UI（reloadData）
```

### 阶段 5：聊天页 viewWillAppear → 检查黑名单

```
isContactInBlacklist (L13157-L13268)

逐行遍历：
  条件 1: "{群聊}" AND isGroup → YES
  条件 2: "{私聊}" AND !isGroup AND !isOA → YES
  条件 3: "{公众号}" AND isOA → YES
  条件 4: 精确匹配 wxid → YES
  都不命中 → NO
```

### 阶段 6：命中黑名单 → 恢复原始顶栏

```
（ChatTopBarHook.m hook_viewWillAppear: L114-L121）

命中黑名单 → 恢复原始 titleView（不创建自定义顶栏）
```

---

## 三、帮助表 token 验证：NSConstantArray 编译期常量

```
viewDidLoad L58682-L58762:

左列数组（binary offset 0x121af0）：
  → "{群聊}" / "{私聊}" / "{公众号}" / "wxid_xxxx" / "使用说明"

右列数组（binary offset 0x121b08）：
  → "屏蔽所有群聊" / "屏蔽所有私聊" / "屏蔽所有公众号" / "屏蔽指定对象" / "每行一个占位符"
```

### 帮助表对比（与我方一致，无需修改）

| 行 | 微信 token | 我方 token | 微信说明 | 我方说明 | 结论 |
|:--:|------|------|------|------|:--:|
| 1 | `{群聊}` | `{群聊}` | 屏蔽所有群聊 | 屏蔽所有群聊 | ✅ |
| 2 | `{私聊}` | `{私聊}` | 屏蔽所有私聊 | 屏蔽所有私聊 | ✅ |
| 3 | `{公众号}` | `{公众号}` | 屏蔽所有公众号 | 屏蔽所有公众号 | ✅ |
| 4 | `wxid_xxxx` | `wxid_xxxx` | 屏蔽指定对象 | 屏蔽指定对象 | ✅ |
| 5 | `使用说明` | `使用说明` | 每行一个占位符 | 每行一个占位符 | ✅ |

---

## 四、isContactInBlacklist 改造对比

### 改造前（ChatTopBarHook.m 旧版）

```objc
// ❌ token 错误 + 逻辑缺失
if (isGroup && [t isEqualToString:@"@"]) return YES;               // 条件1: token 错
if ([contactID hasPrefix:@"gh_"] && [t hasPrefix:@"gh_"]) return YES; // 条件2+3: 合并错
if ([t isEqualToString:contactID]) return YES;                      // 条件4: 正确
if (isGroup && [contactID containsString:t]) return YES;            // 多余
```

### 改造后（ChatTopBarHook.m:L20-L45）

```objc
// ✅ 与微信完全对齐
if (isGroup && [t isEqualToString:@"{群聊}"]) return YES;          // 条件1
if (!isGroup && !isOA && [t isEqualToString:@"{私聊}"]) return YES; // 条件2
if (isOA && [t isEqualToString:@"{公众号}"]) return YES;            // 条件3
if ([t isEqualToString:contactID]) return YES;                       // 条件4
```

### 逐条对比结果

| 条件 | 微信 token | 微信前置条件 | 改造前 token | 改造后 | 结论 |
|:---:|------|------|------|------|:--:|
| 屏蔽群聊 | `{群聊}` | `isGroup` | `@`（错） | `{群聊}` ✅ | 已修复 |
| 屏蔽私聊 | `{私聊}` | `!isGroup && !isOA` | 缺失 | `{私聊}` ✅ | 已修复 |
| 屏蔽公众号 | `{公众号}` | `isOA` | `gh_` 前缀（错） | `{公众号}` ✅ | 已修复 |
| 屏蔽 wxid | `contactID` | 无 | `contactID` | 不变 | 一直正确 |
| 部分匹配 | 不存在 | — | `containsString:` | 已删除 | 已删除 |

---

## 五、saveAndDismiss 改造对比

| 项目 | 微信 | 改造前 | 改造后 | 结论 |
|------|------|------|------|:--:|
| trim whitespace | ✅ | ❌ | ✅ [ChatTopBarBlacklistEditorVC.m:L197-L198](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L197-L198) | 已修复 |
| 去重 | ✅ | ❌ | ✅ [ChatTopBarBlacklistEditorVC.m:L200](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L200) | 已修复 |
| 去空行 | ✅ | ❌ | ✅ [ChatTopBarBlacklistEditorVC.m:L199](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L199) | 已修复 |
| join by `\n` | ✅ | ❌ | ✅ [ChatTopBarBlacklistEditorVC.m:L204](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L204) | 已修复 |
| saveBlock 回调 | ✅ | ❌ | ✅ [ChatTopBarBlacklistEditorVC.m:L211-L213](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L211-L213) | 已实现 |
| synchronize | ✅ | ✅ | ✅ | 一直正确 |
| dismiss | ✅ | ✅ | ✅ | 一直正确 |

---

## 六、clearAll（清空/恢复）改造对比

| 项目 | 微信 | 改造前 | 改造后 | 结论 |
|------|------|------|------|:--:|
| 确认弹窗 | ✅ UIAlertController | ❌ 直接清空 | ✅ [ChatTopBarBlacklistEditorVC.m:L176-L185](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L176-L185) | 已修复 |
| 确认按钮 destructive | ✅ | — | ✅ | 已实现 |
| 取消按钮 | ✅ | — | ✅ | 已实现 |
| 清空操作 | ✅ | ✅ | ✅ | 一直正确 |

---

## 七、saveBlock 回调架构对比

### 微信架构

```
CSChatTitleSettingsViewController (设置页)
       │ ① 读取 NSUserDefaults → ② set editorVC.blacklist
       │ ③ set editorVC.saveBlock → ④ present nav(editorVC)
       ▼
CSChatAvatarBlacklistEditorViewController (编辑器)
       │ 用户编辑 → saveAndDismiss
       │ ① trim+去重+去空 → ② saveBlock(joined) → ③ dismiss
       ▼
saveBlock 回调体：更新属性 → 写 NSUserDefaults → reloadData
```

### 我方架构（已改造完成，与微信对齐）

```
SettingChatTopBarController (设置页)
       │ [SettingChatTopBarController.m:L254-L264](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/SettingChatTopBarController.m#L254-L264)
       │ ① 读 NSUserDefaults → ② vc.blacklist = ...
       │ ③ vc.saveBlock = ^(…) { setObject:synchronize } → ④ present nav
       ▼
ChatTopBarBlacklistEditorVC (编辑器)
       │ viewDidLoad: 优先使用 self.blacklist，fallback NSUserDefaults
       │ [ChatTopBarBlacklistEditorVC.m:L26-L33](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L26-L33)
       │ saveAction: trim+去重 → write NSUserDefaults → saveBlock() → dismiss
       │ [ChatTopBarBlacklistEditorVC.m:L188-L216](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L188-L216)
```

> MRC 下 block 只捕获外部变量 key，无循环引用风险。

---

## 八、改造完成总览

| 维度 | 改造前 | 改造后 |
|------|------|------|
| {群聊} token | `@"@"` → 不识别 | `@"{群聊}"` ✅ |
| {私聊} token | 缺失整条 | `@"{私聊}"` + `!isGroup && !isOA` ✅ |
| {公众号} token | `hasPrefix:@"gh_"` → 误匹配 | `@"{公众号}"` + `isOA` ✅ |
| wxid 匹配 | ✅ | ✅ |
| 群名模糊匹配 | `containsString:` 多余 | 已删除 ✅ |
| save 去重 trim | ❌ | ✅ |
| save 去空行 | ❌ | ✅ |
| 清空确认弹窗 | ❌ | ✅ |
| saveBlock 架构 | ❌ | ✅ |

5 个改造步骤全部完成：

1. **isContactInBlacklist 修正** — [ChatTopBarHook.m:L20-L45](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m#L20-L45)
2. **saveAction 去重 + trim** — [ChatTopBarBlacklistEditorVC.m:L188-L216](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L188-L216)
3. **清空确认弹窗** — [ChatTopBarBlacklistEditorVC.m:L173-L186](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L173-L186)
4. **saveBlock 回调架构** — `.h` 新增 `blacklist` + `saveBlock` 属性；[SettingChatTopBarController.m:L254-L264](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/SettingChatTopBarController.m#L254-L264)
5. **帮助表** — 与微信一致，无需修改 [ChatTopBarBlacklistEditorVC.m:L81-L89](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L81-L89)

---

*文档版本：v3（改造完成版）*
*反编译基准行号：*
- *isContactInBlacklist: L13157-L13268*
- *viewDidLoad: L58585-L58879*
- *saveAndDismiss: L59047-L59146*
- *clearAll: L58995-L59027*
- *showBlacklistEditor: L58087-L58128*
- *saveBlock body: L58132-L58170*
- *loadBlacklistData: L54287-L54316*