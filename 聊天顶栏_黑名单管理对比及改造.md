# 聊天顶栏 — 黑名单管理功能 深度对比 + 实现步骤

> 微信反编译基准：`123456.c` L13190-L13270（isContactInBlacklist）+ L58919-L59190（编辑器）+ L58095-L58160（启动编辑器）
> 我方实现：`ChatTopBarBlacklistEditorVC.m` + `ChatTopBarHook.m:L20-L39`
> 核心发现：**帮助表 token 是正确的（{群聊}/{私聊}/{公众号}），Hook 里的匹配逻辑 token 写错了（写成了 @ 和 gh_）**

---

## 一、反编译验证：C 运算符优先级是关键

```
L13237:  uVar3 = isEqualToString(line, token1);  // token1 = "{群聊}"
L13238:  if ((uVar3 & uVar1 & 1) != 0)           // token1匹配 AND isGroup

L13239:  uVar3 = isEqualToString(line, token2);  // token2 = "{私聊}"
L13240:  ((uVar1 | (uVar3 ^ 0xffffffff) | uVar2) & 1) == 0
         // ^ 优先级高于 | → uVar3 ^ ~0 → 取反
         // → NOT isGroup AND token2匹配 AND NOT isOA

L13241:  uVar3 = isEqualToString(line, token3);  // token3 = "{公众号}"
L13242:  if ((uVar3 & uVar2 & 1) != 0)           // token3匹配 AND isOA

L13247:  uVar8 = isEqualToString(line, contactID)
L13249:  if ((uVar8 & 1) != 0)                   // 精确匹配 wxid
```

三个比较用的是不同 token 字符串常量，Ghidra 把它们都命名成了 `cf__`，但反编译代码结构证明它们是三个不同的值。

---

## 二、微信黑名单匹配逻辑（精确解码）

```
读 NSUserDefaults key: "com_wechat_enhance_chat_avatar_blacklist"
      ↓
   componentsSeparatedByString:@"\n"
      ↓
   逐行遍历 + trim whitespace
      ↓
   每一行做 4 种检查（|| 短路，命中任一即返回 YES）：
```

| 序号 | 条件 | 命中结果 |
|:---:|------|------|
| 1 | `line == "{群聊}"` AND `contactID containsString:"@chatroom"` | 屏蔽该群聊 |
| 2 | `line == "{私聊}"` AND `NOT isGroup` AND `NOT isOA` | 屏蔽该私聊（C2C） |
| 3 | `line == "{公众号}"` AND `contactID hasPrefix:"gh_"` | 屏蔽该公众号 |
| 4 | `line == contactID`（精确匹配） | 屏蔽指定对象 |

```
如果 4 种都不命中 → 继续下一行
如果所有行都不命中 → 返回 NO（不在黑名单中）
```

---

## 三、对比：你的代码哪里错了

### 你的 `isContactInBlacklist`（ChatTopBarHook.m L20-L39）：

```objc
if (isGroup && [t isEqualToString:@"@"]) return YES;               // ❌ token 错误
if ([contactID hasPrefix:@"gh_"] && [t hasPrefix:@"gh_"]) return YES; // ❌ 逻辑错误
if ([t isEqualToString:contactID]) return YES;                      // ✅ 第4条正确
if (isGroup && [contactID containsString:t]) return YES;            // ❌ 微信没有这行
```

### 应该改成：

```objc
if (isGroup && [t isEqualToString:@"{群聊}"]) return YES;           // ✅ 条件1
if (!isGroup && !isOA && [t isEqualToString:@"{私聊}"]) return YES; // ✅ 条件2
if (isOA && [t isEqualToString:@"{公众号}"]) return YES;            // ✅ 条件3
if ([t isEqualToString:contactID]) return YES;                      // ✅ 条件4
```

---

## 四、逐项对比

| 检查项 | 微信 | 你的代码 | 结论 |
|------|------|------|:---:|
| 屏蔽所有群聊 | 输入 `{群聊}` | 输入 `@` | ❌ token 不同 |
| 屏蔽所有私聊 | 输入 `{私聊}` | 不支持 | ❌ 缺失整整一条 |
| 屏蔽所有公众号 | 输入 `{公众号}` | 前缀 `gh_` 匹配 | ❌ token 不同 |
| 屏蔽指定wxid | 输入完整 wxid | 输入完整 wxid | ✅ |
| 群名部分匹配 | 不存在 | `containsString:t` | ❌ 微信没有 |
| 帮助表 token | `{群聊}/{私聊}/{公众号}` | `{群聊}/{私聊}/{公众号}` | ✅ 帮助表是对的 |

> **帮助表自始至终都是对的**，错的只是 `ChatTopBarHook.m` 里的 `isContactInBlacklist` 匹配逻辑。

---

## 五、为什么"保存不了"

结合上面发现的问题，用户实际行为链：

```
1. 用户看到帮助表 → 输入 {群聊} → 点保存
2. NSUserDefaults 已正确存储 "{群聊}"  ✅
3. 回到聊天页 → viewWillAppear → isContactInBlacklist
4. 检查 `[t isEqualToString:@"@"]` → line 是 "{群聊}" → 不等于 "@" → 不命中 ❌
5. 用户看到黑名单没有生效 → 以为"没保存上"
```

**不是没保存，是保存后用错了 token 去匹配。**

---

## 六、改造步骤

### 步骤 1：修正 `isContactInBlacklist`（ChatTopBarHook.m L20-L39）

**这是唯一的硬 bug 修复。**

```objc
// 改前
static BOOL isContactInBlacklist(NSString *contactID) {
    if (!contactID.length) return NO;
    NSString *blacklist = [[NSUserDefaults standardUserDefaults]
        stringForKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarBlacklist"]];
    if (!blacklist.length) return NO;
    NSArray *lines = [blacklist componentsSeparatedByString:@"\n"];
    BOOL isGroup = [contactID containsString:@"@chatroom"];
    for (NSString *line in lines) {
        NSString *t = [line stringByTrimmingCharactersInSet:
                        [NSCharacterSet whitespaceCharacterSet]];
        if (!t.length) continue;
        if (isGroup && [t isEqualToString:@"@"]) return YES;
        if ([contactID hasPrefix:@"gh_"] && [t hasPrefix:@"gh_"]) return YES;
        if ([t isEqualToString:contactID]) return YES;
        if (isGroup && [contactID containsString:t]) return YES;
    }
    return NO;
}

// 改后
static BOOL isContactInBlacklist(NSString *contactID) {
    if (!contactID.length) return NO;
    NSString *blacklist = [[NSUserDefaults standardUserDefaults]
        stringForKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarBlacklist"]];
    if (!blacklist.length) return NO;
    NSArray *lines = [blacklist componentsSeparatedByString:@"\n"];
    BOOL isGroup = [contactID containsString:@"@chatroom"];
    BOOL isOA = [contactID hasPrefix:@"gh_"];
    for (NSString *line in lines) {
        NSString *t = [line stringByTrimmingCharactersInSet:
                        [NSCharacterSet whitespaceCharacterSet]];
        if (!t.length) continue;

        // 条件1: {群聊} — 屏蔽所有群聊
        if (isGroup && [t isEqualToString:@"{群聊}"]) return YES;
        // 条件2: {私聊} — 屏蔽所有私聊（非群非公众号）
        if (!isGroup && !isOA && [t isEqualToString:@"{私聊}"]) return YES;
        // 条件3: {公众号} — 屏蔽所有公众号
        if (isOA && [t isEqualToString:@"{公众号}"]) return YES;
        // 条件4: 精确匹配 wxid
        if ([t isEqualToString:contactID]) return YES;
    }
    return NO;
}
```

### 步骤 2：saveAction 加去重和 trim（ChatTopBarBlacklistEditorVC.m L174-L180）

对齐微信 `saveAndDismiss`：

```objc
- (void)saveAction {
    [self.editorView resignFirstResponder];

    NSString *rawText = self.editorView.text ?: @"";
    NSArray *rawLines = [rawText componentsSeparatedByString:@"\n"];

    // 去重 + trim（对齐微信 saveAndDismiss）
    NSMutableArray *deduped = [NSMutableArray array];
    for (NSString *line in rawLines) {
        NSString *trimmed = [line stringByTrimmingCharactersInSet:
                              [NSCharacterSet whitespaceCharacterSet]];
        if (!trimmed.length) continue;
        if ([deduped containsObject:trimmed]) continue;
        [deduped addObject:trimmed];
    }

    NSString *cleanText = [deduped componentsJoinedByString:@"\n"];

    NSString *key = [kPluginPrefix stringByAppendingString:@"ChatAvatarBlacklist"];
    [[NSUserDefaults standardUserDefaults] setObject:cleanText forKey:key];
    [[NSUserDefaults standardUserDefaults] synchronize];

    [self dismissViewControllerAnimated:YES completion:nil];
}
```

### 步骤 3：清空加确认弹窗（ChatTopBarBlacklistEditorVC.m L170-L172）

```objc
- (void)restoreAction {
    [self.editorView resignFirstResponder];

    UIAlertController *alert = [UIAlertController alertControllerWithTitle:@"清空黑名单"
                                                                   message:@"确定要清空所有黑名单内容吗？"
                                                            preferredStyle:UIAlertControllerStyleAlert];
    [alert addAction:[UIAlertAction actionWithTitle:@"取消" style:UIAlertActionStyleCancel handler:nil]];
    [alert addAction:[UIAlertAction actionWithTitle:@"清空"
                                              style:UIAlertActionStyleDestructive
                                            handler:^(UIAlertAction *action) {
        self.editorView.text = @"";
    }]];
    [self presentViewController:alert animated:YES completion:nil];
}
```

### 步骤 4（可选）：saveBlock 回调架构

**文件**：`ChatTopBarBlacklistEditorVC.h`

```objc
// 新增属性
@property (nonatomic, copy) NSString *blacklist;
@property (nonatomic, copy) void (^saveBlock)(NSString *blacklist);
```

**文件**：`ChatTopBarBlacklistEditorVC.m` — `saveAction` 增加回调：

```objc
// dismiss 前加：
if (self.saveBlock) {
    self.saveBlock(cleanText);
}
```

**文件**：`SettingChatTopBarController.m` — `onBlacklistTap` 传入回调：

```objc
- (void)onBlacklistTap {
    ChatTopBarBlacklistEditorVC *vc = [[[ChatTopBarBlacklistEditorVC alloc] init] autorelease];
    NSString *key = [kPluginPrefix stringByAppendingString:@"ChatAvatarBlacklist"];
    vc.blacklist = [[NSUserDefaults standardUserDefaults] stringForKey:key];
    vc.saveBlock = ^(NSString *blacklist) {
        [[NSUserDefaults standardUserDefaults] setObject:blacklist forKey:key];
        [[NSUserDefaults standardUserDefaults] synchronize];
    };
    UINavigationController *nav = [[[UINavigationController alloc] initWithRootViewController:vc] autorelease];
    [self presentViewController:nav animated:YES completion:nil];
}
```

> MRC 下 `__weak` 不可用，这里不涉及循环引用（block 只访问外部参数，不捕获 self），无需 __unsafe_unretained。

### 步骤 5：帮助表保持不变

帮助表不需要改 — `{群聊}`、`{私聊}`、`{公众号}` 就是微信的真实 token。

---

## 七、改造前后完整对比

| 维度 | 改前 | 改后 |
|------|------|------|
| {群聊} token | 检查 `@`，不识别 → 不生效 | 检查 `{群聊}` → ✅ |
| {私聊} token | 不存在（缺失功能） | 检查 `{私聊}`，非群非公众号 → ✅ |
| {公众号} token | 前缀 `gh_` 匹配 | 检查 `{公众号}` → ✅ |
| wxid 匹配 | ✅ | ✅ 不变 |
| 去重 + trim | ❌ | ✅ |
| 清空确认 | ❌ 直接清空 | ✅ UIAlertController |

---

## 八、改造后完整流程

```
编辑器中输入：
  {群聊}          ← 屏蔽所有群聊
  {私聊}          ← 屏蔽所有私聊（非群非公众号）
  {公众号}        ← 屏蔽所有公众号
  wxid_abc123     ← 屏蔽指定人

点保存 → 去重 → trim → 存 NSUserDefaults → 回到聊天

viewWillAppear:
  检查黑名单:
    ① line == "{群聊}" AND contact含@chatroom → 命中 ✅
    ② line == "{私聊}" AND !isGroup AND !isOA → 命中 ✅
    ③ line == "{公众号}" AND contact前缀gh_ → 命中 ✅
    ④ line == contactID → 命中 ✅
  命中 → titleView = nil → 恢复微信原始顶栏
```

---

## 九、执行顺序

```
Step 1: 修正 isContactInBlacklist（步骤 1）🔥 最核心
Step 2: saveAction 加去重 trim（步骤 2）
Step 3: 清空加确认弹窗（步骤 3）
Step 4: saveBlock 架构（步骤 4，可选）
```

---

*文档生成时间：2026-05-27*
*基准：123456.c L13190-L13270 + L58919-L59190 + L58095-L58160*
*修正点：C运算符优先级纠正了之前对条件2的解码错误*