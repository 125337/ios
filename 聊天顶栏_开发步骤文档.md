# 聊天顶栏功能 — 开发步骤文档（第二十六次修正版：Mio 独占 Hook 根因）

> **关键突破**：v25 对齐微信优化调用时序后仍闪退。通过枚举 Mio 全部 15 个 Hook 模块与 微信优化 反编译代码逐项对比，找到了 Mio 独占的全局 Hook。
>
> **根因**：Mio 的 **UISimplifyHook Hook 了 `MMUILabel.setText:`/`setAttributedText:`（全局）**，微信优化 **不 Hook 这两个方法**。该 Hook 在每次 TableView 渲染 Cell 时对每个 Label 文本设置执行 Responder Chain 遍历（触碰 NavigationBar 等对象），与 WCRefine + 黄白助手的渲染 Hook 产生链式干扰。
>
> **项目用 MRC 编译**

---

## 一、全部 15 个 Hook 模块 vs 微信优化对比

| Mio 模块 | Hook 目标 | 微信优化有？ | 差异 |
|----------|-----------|------------|------|
| **UISimplifyHook** | **MMUILabel.setText:/setAttributedText:** | **❌ 没有** | **Mio 独占全局 Hook** |
| **UISimplifyHook** | MMTabbarItem.setNormalTitle: 等 | ❌ 没有 | Mio 独占 |
| **UISimplifyHook** | MFTitleView.updateTitleView:title: | ✅ 有 | 相同 |
| **UIPurifyHook** | UIView.layoutSubviews | ✅ 有 | **相同（双 Hook 冲突）** |
| UIPurifyHook | WCTableViewManager.getSeparator | ❌ 没有 | Mio 独占 |
| TintHook | MessageCell.updateStatus/layoutContentView | 待验证 | 消息类 |
| HideAvatarHook | CommonMessageViewModel.isShowHeadImage | 待验证 | 消息类 |
| PlaceholderTextHook | MMGrowTextView.layoutSubviews | 待验证 | 输入区 |
| **ChatTopBarHook** | **BaseMsgContentVC.viewDidLoad/viewWillAppear** | **✅ 有** | **已对齐 v25** |
| RevokeHook / ClearUnreadHook / RedEnvelopHook 等 | 消息处理 | 待验证 | 消息类 |

---

## 二、根因详细说明

### 2.1 MMUILabel Hook 的致命副作用

UISimplifyHook 的 `hook_MMUILabel_setText:` 在每次设置 Label 文本时：
1. 调用原始 setText:
2. 获取文本内容
3. 如果文本包含 "微信" → Responder Chain 遍历（从 label 向上查 nextResponder，查找 NavigationBar）
4. 即使功能禁用，Hook 仍运行（NSUserDefaults 检查后返回）

### 2.2 崩溃触发链

```
CA::Transaction::flush_as_runloop_observer
  → UIView layoutSublayersOfLayer
    → WCRefine 导航栏 hook
      → 黄白助手 rendering hooks
        → WeChat(300575924)
          → NewMainFrameVC TableView._updateVisibleCellsNow
            → Cell 渲染 → MMUILabel.setText: 被调用
              → Mio UISimplifyHook 介入！
                → Responder Chain 遍历 → 触碰 NavigationBar 对象
                  → WCRefine hook 二次被激活
                    → 黄白助手 hook 二次被激活
                      → 微信优化 cellForRowAtIndexPath trampoline
                        → dispatch_async block → 访问已释放对象 → 0x54 SIGSEGV
```

**微信优化不 Hook MMUILabel.setText: → 没有这一步 → 不崩溃。**

### 2.3 UIView.layoutSubviews 双 Hook 冲突（次要）

Mio 和 微信优化 **都** Hook 了 `UIView.layoutSubviews`。每次布局都经过双重 Hook 链，但这不是 Mio 独占差异。

---

## 三、v26 修复内容

### 3.1 禁用 UIPurifyHook.UIView.layoutSubviews（双 Hook 冲突）

[UIPurifyHook.m#L505-L508](file:///www/wwwroot/ios/WeChatPlugin/Modules/Layout/UIPurifyHook.m#L505) — 注释掉 UIView.layoutSubviews 全局 Hook。

### 3.2 禁用 UISimplifyHook.MMUILabel Hook（Mio 独占）

[UISimplifyHook.m#L520-L528](file:///www/wwwroot/ios/WeChatPlugin/Modules/Layout/UISimplifyHook.m#L520) — 注释掉 MMUILabel.setText: / setAttributedText: Hook。

### 3.3 保留的 ChatTopBar 改动（v21-v25 累积）

- pushViewController hook：已删除
- removeFromSuperview：已删除
- layoutIfNeeded → setNeedsLayout
- viewDidLoad 新增 updateAvatars
- viewWillAppear 调用序列与 微信优化 完全一致

---

## 四、如果 v26 仍然崩溃

继续排查 Mio 独占 Hook：
1. UIPurifyHook: WCTableViewManager.getSeparator
2. TintHook: MessageCell.updateStatus/layoutContentView
3. PlaceholderTextHook: MMGrowTextView.layoutSubviews
4. 各消息类 Hook（逐模块禁用测试）

---

## 五、历史版本汇总

| 版本 | 修正内容 | 结果 |
|------|---------|------|
| v9-v20 | 各种尝试 | 仍崩溃 |
| v21-v25 | 对齐 ChatTopBar 调用时序/布局/预加载 | **仍崩溃（根因不在 ChatTopBar）** |
| **v26** | **禁用 Mio 独占全局 Hook（MMUILabel + UIView.layoutSubviews 双 Hook 冲突）** | **待验证** |