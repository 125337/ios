# 聊天顶栏功能 — 开发步骤文档（第二十四次修正版：layoutIfNeeded 强制同步布局根因）

> **本次背景**：v22 修复后（删除 push hook + removeFromSuperview），只加载新版 `Mio_arm64.dylib`（无旧版），仍然闪退。新 IPS `WeChat-2026-05-27-201652(1).ips`。
>
> **核心发现**：`viewWillAppear` 中的 `layoutIfNeeded` 强制同步布局，在视图过渡动画期间触发 CoreAnimation commit → WCRefine → 黄白助手连锁反应。微信优化不调用 `layoutIfNeeded`（只用 `layoutSubviews` + `setNeedsLayout`，推迟到下一 runloop），所以不触发此链。
>
> **项目用 MRC 编译**

---

## 一、最新 Crash Log (2026-05-27 20:16) 精确分析

### 1.1 确认：只有新版 dylib（无旧版）

```
Mio_arm64.dylib  uuid=1e6ede2f(新)  base=0x11f02c000  size=311296  ← 仅此一个
```

### 1.2 完整调用栈

```
F38-29: dyld → WeChat main → RunLoop
F28-25: _UIApplicationFlushCATransaction → _UIUpdateSequenceRun
F24-20: QuartzCore CA::Transaction commit → layer layout
F19:    UIKitCore -[UIView(CALayerDelegate) layoutSublayersOfLayer:]
F18:    WCRefine 1.4.4.dylib                        ← 导航裁剪
F17:    黄白助手 2.0-9-2.dylib  offset=15893041     ← rendering hooks
F16:    WeChat                          offset=300575924
F15:    UIKitCore -[UITableView layoutSubviews]
F14-11: UIKitCore _updateVisibleCellsNow → _notifyWillDisplayCell
F10-8:  WeChat → trampoline → WeChat
F7:     ??? imageIndex=30 (source "A")              ← 微信优化 cellForRowAtIndexPath trampoline
F6-1:   WeChat internal
F0:     libdispatch dispatch_async + 188            ← CRASH SIGSEGV at 0x54
```

### 1.3 三次崩溃对比 — 关键发现

| | 旧 (push hook) | Mid (双dylib) | **New (仅新dylib)** |
|---|---|---|---|
| 崩溃终点 | dispatch_async+188, 0x54 | dispatch_async+188, 0x54 | dispatch_async+188, 0x54 |
| WeChat 触发帧 | **63152452** | **300575924** | **300575924** |
| 黄白助手 offset | **3814201** | **15893041** | **15893041** |
| WCRefine offset | 346416 | 2566888 | 2554940 |
| 触发源 | push hook 干扰导航栏 | layoutIfNeeded 强制同步布局 | **layoutIfNeeded** |

**核心结论**：Mid 和 New 崩溃的触发路径**完全相同**（WeChat=300575924, 黄白助手=15893041），与旧崩溃（push hook 触发）**完全不同**。说明 v22 删除 push hook 后，崩溃转由 `layoutIfNeeded` 触发。

---

## 二、根因分析：layoutIfNeeded 的致命副作用

### 2.1 问题代码（v22 版本）

```objc
// viewWillAppear: L123-125 (v22)
[(MioChatAvatarTitleView *)currentTitle updateAvatars];
[currentTitle setNeedsLayout];
[currentTitle layoutIfNeeded];  // ← 致命：强制同步布局！
[currentTitle setNeedsDisplay];
```

### 2.2 `layoutIfNeeded` 的调用链

```
viewWillAppear (视图过渡动画进行中)
  → updateAvatars (修改 images/text/frames → 标记需要布局)
  → layoutIfNeeded ← 强制同步布局，不等下一 runloop
    → MioChatAvatarTitleView.layoutSubviews (8种模式，大量 frame 计算)
      → 子视图 frame 变更 → 导航栏标记需要重新布局
        → 导航栏 layoutSubviews (仍在 viewWillAppear 调用栈中!)
          → CoreAnimation commit
            → UIView layoutSublayersOfLayer
              → WCRefine WCRefineNavApplyOpenSmallTailPruneIfNeeded
                → 黄白助手 layoutSubviews/setFrame hooks
                  → WeChat 内部状态变更
                    → NewMainFrameVC TableView 更新 cell
                      → 微信优化 cellForRowAtIndexPath trampoline
                        → dispatch_async block 捕获已释放对象
                          → 0x54 SIGSEGV
```

### 2.3 为什么微信优化不崩溃

微信优化在 [123456.c#L13446-L13450](file:///www/wwwroot/ios/插件/微信优化反编译最新/123456.c#L13446) 的 viewWillAppear 中调用的是：

```c
FUN_000c7700(titleView);   // 推测: layoutSubviews (只布局当前 view，不强制父布局)
FUN_000c7720(titleView);   // 推测: 某个内部方法
FUN_000d0c20(titleView);   // 推测: setNeedsLayout (异步，推迟到下一 runloop)
FUN_000d0ba0(titleView);   // updateAvatars
FUN_000c2d20(titleView);   // 推测: setNeedsDisplay
```

**微信优化不调用 `layoutIfNeeded`**。它使用 `setNeedsLayout`（异步，推迟到下一 runloop），此时视图过渡已完成，导航栏处于稳定状态，CoreAnimation commit 不再经过 WCRefine→黄白助手的危险链。

### 2.4 viewDidLoad 中的 layoutSubviews（次要）

```objc
// viewDidLoad L79 (v22):
[view layoutSubviews];  // 微信优化不调这个
```

`setTitleView:` 后导航栏会自行触发布局，不需要手动调用。虽然这个调用本身不太可能触发 CoreAnimation commit（view 还未在屏幕上），但多一次布局意味着多一次风险。

---

## 三、修复方案（v24）— 已实施

### 3.1 修改 ChatTopBarHook.m — 两处改动

**改动 1**：viewDidLoad 删除显式 `layoutSubviews` 调用

```objc
// 修改前:
[[self navigationItem] setTitleView:view];
[view layoutSubviews];            // ← 删除
[view release];

// 修改后:
[[self navigationItem] setTitleView:view];
[view release];
```

**改动 2**：viewWillAppear 删除 `layoutIfNeeded`，只用异步 `setNeedsLayout`

```objc
// 修改前:
[(MioChatAvatarTitleView *)currentTitle updateAvatars];
[currentTitle setNeedsLayout];
[currentTitle layoutIfNeeded];    // ← 删除（强制同步布局）
[currentTitle setNeedsDisplay];

// 修改后:
[(MioChatAvatarTitleView *)currentTitle updateAvatars];
[currentTitle setNeedsLayout];     // 异步，推迟到下一 runloop
[currentTitle setNeedsDisplay];    // 异步
```

### 3.2 修改原理

`setNeedsLayout` 只是标记视图需要布局，实际布局发生在下一 runloop 迭代。此时 `viewWillAppear` 的过渡动画已完成，导航栏处于稳定状态，CoreAnimation commit 是在正常的 RunLoop observer 回调中触发，不会经过 WCRefine→黄白助手的危险链。

### 3.3 最终代码状态

[ChatTopBarHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m) v24 最终版本：
- push hook：已删除
- removeFromSuperview：已删除
- viewDidLoad 显式 layoutSubviews：已删除
- viewWillAppear layoutIfNeeded：改为异步 setNeedsLayout
- 仅保留：viewDidLoad + viewWillAppear hook

---

## 四、与微信优化的最终对齐状态

| 维度 | 微信优化 | Mio v24 | 对齐？ |
|------|---------|---------|--------|
| push hook 存在 | ✅ (页面锁) | ❌ | ✅ 安全 |
| removeFromSuperview | ❌ | ❌ | ✅ 安全 |
| viewDidLoad layoutSubviews | ❌ | ❌ | ✅ 对齐 |
| viewWillAppear layoutIfNeeded | ❌ (用 setNeedsLayout) | ❌ (用 setNeedsLayout) | ✅ 对齐 |
| TitleView 实现 | CSChatAvatarTitleView | MioChatAvatarTitleView | 不同但无害 |
| updateAvatars | MMHeadImageMgr only(推测) | +文件I/O +m_nsNickName | ⚠️ 仍有差异 |

---

## 五、如果 v24 仍然崩溃

下一步排查 updateAvatars 中的主线程 I/O 和 m_nsNickName（黄白助手 hook 点）：

**方案 D**：将 updateAvatars 中的文件 I/O 和头像加载移到后台

[ChatTopBarHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m) viewWillAppear 中改为：
```objc
// 只设置同步数据（nickname, 群人数等）
[(MioChatAvatarTitleView *)currentTitle updateTitleOnly];  
[currentTitle setNeedsLayout];
[currentTitle setNeedsDisplay];

// 异步加载头像
dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, 0), ^{
    [(MioChatAvatarTitleView *)currentTitle updateAvatarImagesAsync];
    dispatch_async(dispatch_get_main_queue(), ^{
        [currentTitle setNeedsLayout];
    });
});
```

---

## 六、历史版本修正汇总

| 版本 | 修正内容 | 结果 |
|------|---------|------|
| v9 | objc_msgSendSuper → [super viewDidLoad] | 修复 MRC crash |
| v10 | iPhone popover 全屏修复 | 修复 |
| v12 | 布局 y 坐标修复 | 修复 |
| v15 | Popover 重写为 UITableView 多 section | 架构对齐 |
| v16 | ASSIGN → RETAIN_NONATOMIC | 修复野指针 |
| v17 | 删除 object_getClass 过滤 | 仍冲突 |
| v18 | 添加 view.window 检查 | 顶栏消失 + 仍闪退 |
| v19 | 类名改名 + UINavigationController hook | 仍闪退 |
| v20 | 检测微信优化 → 共存模式 | **方向错误** |
| v21 | 移除 push hook + removeFromSuperview | **方向正确** |
| v22 | 深度复查 + 双dylib 根因 | **双dylib 仍加载导致误判** |
| v23 | 双dylib 分析 + MioChatAvatarTitleView 差异 | **待验证 → 仍崩（单dylib也不行）** |
| **v24** | **删除 layoutIfNeeded + layoutSubviews，对齐微信优化布局时序** | **待验证** |