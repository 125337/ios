# 聊天顶栏功能 — 开发步骤文档（第七次深度复查修正版）

> 基于 [聊天顶栏功能_完整实现文档.md](file:///www/wwwroot/ios/聊天顶栏功能_完整实现文档.md) 的 123456.c 反编译分析  
> 本次重点：**复查级** — 逐项验证修复 + 深度还原 123456.c 的 haptic MRC 实现  
> **项目用 MRC 编译**，123456.c 也用 MRC

---

## 复查结论：11 项已修复 ✓，1 项待微调

| # | 位置 | 内容 | 状态 |
|---|------|------|------|
| 1 | CSChatAvatarTitleView.m L111-114 | avatarY 公式：mode 1,4 偏上给 name 留空间 | ✓ 正确 |
| 2 | CSChatAvatarTitleView.m L120 | nameY = avatarY + (S-N)/2 - offsetY | ✓ 正确 |
| 3 | CSChatAvatarTitleView.m L138-158 | mode 5：name(左)+avatar(右)，对齐 123456.c | ✓ 正确 |
| 4 | CSChatAvatarTitleView.m L550-556 | applyPositionOffset：仅偏移 titleLabel | ✓ 正确 |
| 5 | CSChatAvatarTitleView.m L478-495 | loadWeChatAvatarForWxid: → MMHeadImageMgr | ✓ 正确 |
| 6 | CSChatAvatarTitleView.m L402-439 | createAnimatedImageFromGIFData | ✓ 正确 |
| 7 | CSChatAvatarTitleView.m L384-398 | getSelfWxid：仅 CContactMgr | ✓ 正确 |
| 8 | CSChatAvatarTitleView.m L441-453 | loadAvatarWithPriorityForWxid: 无 DefaultHead | ✓ 正确 |
| 9 | CSChatAvatarTitleView.m L607-641 | presentUserInfoPopoverWithContact: init+setM_contact fallback | ✓ 正确 |
| 10 | PluginConfig.m L509-533 | save：18 个聊天顶栏属性持久化 | ✓ 正确 |
| 11 | PluginConfig.m L328-381 | loadDefaults：全部属性读取 | ✓ 正确 |
| 12 | PluginConfig.m L329 | avatarTapFeedback 默认值 | ⚠️ 应默认 YES |

---

## 重要纠正：haptic MRC 分析

### 之前的分析（错误）

> "MRC 下 `release` 立即销毁 UIImpactFeedbackGenerator，haptic 引擎来不及触发，应改为 `autorelease`"

### 123456.c 的真实实现（L12993-13005）— 找到了！

```c
// 123456.c L12993: CSChatAvatarTitleView::playHapticFeedback
void CSChatAvatarTitleView::playHapticFeedback(ID param_1, SEL param_2)
{
    auVar2 = _objc_alloc(&_OBJC_CLASS___UIImpactFeedbackGenerator);   // [UIImpactFeedbackGenerator alloc]
    uVar1 = FUN_000c66c0(auVar2._0_8_, auVar2._8_8_, 0);            // initWithStyle:0 (Light)
    FUN_000c8980();                                                    // [generator prepare]
    FUN_000c63a0(uVar1);                                              // [generator impactOccurred]
    _objc_release(uVar1);                                             // [generator release] ★ MRC 直接 release!
    return;
}
```

还原为 ObjC：

```objc
- (void)playHapticFeedback {
    UIImpactFeedbackGenerator *generator = [[UIImpactFeedbackGenerator alloc]
        initWithStyle:UIImpactFeedbackStyleLight];  // style=0 = Light
    [generator prepare];
    [generator impactOccurred];
    [generator release];  // ★ 微信优化也用 release，不是 autorelease！
}
```

### 结论

**微信优化也是 MRC 编译，并且使用 `release` 而非 `autorelease`**。这证明 MRC 下 `alloc + ... + release` 对 UIImpactFeedbackGenerator 是安全的——`impactOccurred` 同步触发 haptic 引擎，在 `release` 前已经完成触觉脉冲调度。

项目中 `release` 或 `autorelease` 都可以正常工作：
- `release`：完全对齐 123456.c，立即清理
- `autorelease`：延时清理，稍多占一会内存但无实质差别（用户当前使用的方案）

### 那"点击没反应"真正的原因是什么？

不是 haptic，而是 `presentUserInfoPopoverWithContact:` 崩溃或静默失败：
1. **旧代码** `initWithContact:` crash → 整个进程 SIGABRT，什么都看不到 → **已修复**
2. **旧代码** WCUserInfoViewController 不存在且无 fallback → 静默返回无反应 → **已修复**（加了 ContactInfoViewController init+setM_contact: fallback）

用户的修复是正确的，haptic 本身没问题。

---

## 唯一剩余差异：avatarTapFeedback 默认值

[PluginConfig.m L329](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m#L329)：

```objc
// 当前代码：key 不存在时返回 NO
_avatarTapFeedback = [d boolForKey:[kPluginPrefix stringByAppendingString:@"AvatarTapFeedback"]];
```

123456.c 的行为（L12792-12799）：NSUserDefaults key 不存在时 **默认触发** haptic：

```c
if (lVar3 == 0 || (uVar1 & 1) != 0) {  // key 不存在 → 触发
    playHapticFeedback(param_1);
    showUserInfoPopoverForOpponent(param_1);
}
```

**修复**：

```objc
// 方案：key 存在时读取，不存在时默认 YES
if ([d objectForKey:[kPluginPrefix stringByAppendingString:@"AvatarTapFeedback"]]) {
    _avatarTapFeedback = [d boolForKey:[kPluginPrefix stringByAppendingString:@"AvatarTapFeedback"]];
} else {
    _avatarTapFeedback = YES;  // 首次启动默认开启，对齐 123456.c
}
```

---

## 完整对比验证表

### Haptic 反馈

| 步骤 | 123456.c (MRC) | 项目原代码 (MRC) | 项目当前 (MRC) | 对齐 |
|------|---------------|------------------|---------------|------|
| alloc | `[UIImpactFeedbackGenerator alloc]` | 同 | 同 (alloc+autorelease) | ✓ |
| initWithStyle: | `initWithStyle:0` (Light) | `initWithStyle:UIImpactFeedbackStyleLight` | 同 | ✓ |
| prepare | `[g prepare]` | 同 | 同 | ✓ |
| impactOccurred | `[g impactOccurred]` | 同 | 同 | ✓ |
| 释放 | `[g release]` | `[g release]`（之前）/ 现在 `autorelease` | 两种都行 | ✓ |
| 触发条件 | key 不存在 → 触发 | key 不存在 → 不触发 ⚠️ | 待修复 | ✗ |

### 布局

| 项目 | 123456.c | 对齐 |
|------|----------|------|
| mode 1,4 avatarY = (H-(S+N+3))/2+1 | ✓ | ✓ |
| mode 0,2,3,5,6,7 avatarY = (H-S)/2 | ✓ | ✓ |
| nameY = avatarY + (S-N)/2 - offsetY | ✓ | ✓ |
| mode 5 name(左) + avatar(右) | ✓ | ✓ |
| offset 仅标题（不偏移 frame） | ✓ | ✓ |
| calculateCornerRadius: size*0.5*(radius/100) | ✓ | ✓ |

### 头像加载

| 项目 | 123456.c | 对齐 |
|------|----------|------|
| loadCustomAvatarForWxid: gif → createAnimatedImageFromGIFData | ✓ | ✓ |
| loadWeChatAvatarForWxid: MMHeadImageMgr.getHeadImage:withCategory: | ✓ | ✓ |
| loadAvatarWithPriorityForWxid: 无 DefaultHead | ✓ | ✓ |
| updateAvatars 统一 DefaultHead 分配 | ✓ | ✓ |
| getSelfWxid: 只 CContactMgr | ✓ | ✓ |

### 持久化

| 项目 | 状态 |
|------|------|
| PluginConfig.save 写入 18 个属性 | ✓ |
| PluginConfig.loadDefaults 读取全部属性 | ✓ |
| avatarTapFeedback 默认值 | ⚠️ |

---

## 最终结论

**12 项差异中 11 项已完全修复对齐 123456.c**。

唯一剩余：`avatarTapFeedback` 默认值应从 NO 改为 YES（对齐 123456.c 在 key 不存在时默认触发反馈的行为）。这是一个一行改动，在 [PluginConfig.m L329](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m#L329) 加 `[d objectForKey:]` 判断即可。