# 聊天顶栏功能 — 开发步骤文档（第四次复查修正版）

> 基于 [聊天顶栏功能_完整实现文档.md](file:///www/wwwroot/ios/聊天顶栏功能_完整实现文档.md) 的 123456.c + [WeChat-2026-05-27-005132(1).ips](file:///www/wwwroot/ios/WeChat-2026-05-27-005132(1).ips) crash 分析  

---

## 前置概览

```
已正确实现 ✓：
  PluginConfig.h              — 全部属性声明
  PluginConfig.m loadDefaults — 全部属性读取
  CSChatAvatarTitleView.m     — setupSubviews / layoutSubviews mode 2 fix / calculateNameWidth / loadSeparatorGIF
  ChatTopBarHook.m            — 两个 Hook + isContactInBlacklist
  FeatureModuleRegistry       — ChatTopBarHook 已注册
  SettingChatTopBarController — UI 完整 + delegate 修复
  ChatTopBarBlacklistEditorVC — 黑名单持久化

仍存在差异 ✗：
  PluginConfig.m save        — 聊天顶栏属性未写入 NSUserDefaults（重启丢失）
  silentLoadContactExtInfo:   — [致命] 调了不存在的 initWithContact: → NSInvalidArgumentException crash
  updateAvatars               — suffix 硬编码，未用可配置后缀格式
  updateFontSizes             — 缺 setNeedsLayout/layoutIfNeeded
  loadSeparatorIcon + GIF    — 两者都写 separatorView.image，GIF 覆盖 Icon
```

---

## 致命 Bug：silentLoadContactExtInfo: 触发 crash

### Crash 信息（来自 WeChat-2026-05-27-005132(1).ips）

```
Bug Type:  309 (NSInvalidArgumentException)
Signal:    SIGABRT (Abort trap: 6)
Exception: -[ContactInfoViewController initWithContact:]: unrecognized selector
                      sent to instance 0x2a9277800
Crash 在:  Mio_arm64-1.dylib (你的项目)
```

### 根因

[项目 L276](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L276)：

```objc
// ★ 错误：ContactInfoViewController 没有 initWithContact: 方法
infoVC = ((id (*)(id, SEL, id))objc_msgSend)(infoVC,
    NSSelectorFromString(@"initWithContact:"), contact);
```

### 微信优化为什么不会 crash？

微信优化的 123456.c L11926 使用的不是 `initWithContact:`，而是 `alloc` + 普通 `init` + `setM_contact:`：

```objc
// 123456.c 正确写法：
id infoVC = [[objc_getClass("ContactInfoViewController") alloc] init];
// ★ 通过 setter 注入 contact，不依赖不存在的 init 方法
((void (*)(id, SEL, id))objc_msgSend)(infoVC,
    NSSelectorFromString(@"setM_contact:"), contact);
[infoVC view];  // 触发加载
dispatch_after(0.8s) { [infoVC dismissViewControllerAnimated:...]; }
```

### 修复（方案：对齐 123456.c）

```objc
- (void)silentLoadContactExtInfo:(id)contact {
    Class infoVCClass = objc_getClass("ContactInfoViewController");
    if (!infoVCClass) return;

    id infoVC = ((id (*)(Class, SEL))objc_msgSend)(infoVCClass, NSSelectorFromString(@"alloc"));
    // ★ 改用普通 init
    infoVC = ((id (*)(id, SEL))objc_msgSend)(infoVC, @selector(init));
    if (!infoVC) return;

    // ★ 用 setter 注入 contact（对齐 123456.c）
    if ([infoVC respondsToSelector:NSSelectorFromString(@"setM_contact:")]) {
        ((void (*)(id, SEL, id))objc_msgSend)(infoVC,
            NSSelectorFromString(@"setM_contact:"), contact);
    }

    ((void (*)(id, SEL))objc_msgSend)(infoVC, @selector(view));

    dispatch_after(dispatch_time(DISPATCH_TIME_NOW, 0.8 * NSEC_PER_SEC),
                   dispatch_get_main_queue(), ^{
        if ([infoVC respondsToSelector:@selector(dismissViewControllerAnimated:completion:)]) {
            ((void (*)(id, SEL, BOOL, id))objc_msgSend)(infoVC,
                @selector(dismissViewControllerAnimated:completion:), NO, nil);
        }
        [infoVC release];
    });
}
```

---

## Bug 2：PluginConfig.m save 缺失全部聊天顶栏属性

> 涉及文件：[PluginConfig.m](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m) L508

loadDefaults 已写入（L327-379 ✓），但 save 方法末尾（L508 之后）没有任何聊天顶栏属性的 `setBool:/setFloat:/setInteger:`。

**后果**：SettingCategoryController 的 switchChanged: 调 KVC → `[config save]` → `synchronize`，但聊天顶栏 key 从未写入 → NSUserDefaults 里没有这些 key → 重启 loadDefaults 读到 nil/0。

在 `[d setBool:_hideOASelfAvatar ...]` 行（L508）之后插入：

```objc
    // ========== 聊天顶栏 ==========
    [d setBool:_showChatAvatar forKey:KEY(@"ShowChatAvatar")];
    [d setBool:_avatarTapFeedback forKey:KEY(@"AvatarTapFeedback")];
    [d setBool:_showAddTime forKey:KEY(@"ShowAddTime")];
    [d setBool:_showGroupMemberCount forKey:KEY(@"ShowGroupMemberCount")];
    [d setInteger:_chatDisplayMode forKey:KEY(@"ChatDisplayMode")];
    [d setFloat:_chatAvatarSize forKey:KEY(@"ChatAvatarSize")];
    [d setFloat:_chatAvatarSpacing forKey:KEY(@"ChatAvatarSpacing")];
    [d setFloat:_chatAvatarCornerRadius forKey:KEY(@"ChatAvatarCornerRadius")];
    [d setFloat:_chatNicknameFontSize forKey:KEY(@"ChatNicknameFontSize")];
    [d setFloat:_chatSeparatorSize forKey:KEY(@"ChatSeparatorSize")];
    [d setFloat:_chatTitleViewWidth forKey:KEY(@"ChatTitleViewWidth")];
    [d setFloat:_chatVerticalOffset forKey:KEY(@"ChatVerticalOffset")];
    [d setFloat:_chatHorizontalOffset forKey:KEY(@"ChatHorizontalOffset")];
    [d setFloat:_chatNicknameOffsetY forKey:KEY(@"ChatNicknameOffsetY")];
    [d setFloat:_chatNicknameOffsetX forKey:KEY(@"ChatNicknameOffsetX")];
    if (_chatSeparatorText) {
        [d setObject:_chatSeparatorText forKey:KEY(@"ChatSeparatorText")];
    }
```

---

## Bug 3：updateAvatars suffix 硬编码 vs 可配置格式

> 涉及文件：[CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) L322, L334

当前硬编码了 `@"(%u人)"` 和 `@"(%ld天)"`，但 PluginConfig.h L105-L106 已声明了 `chatGroupMemberCountSuffix` 和 `chatAddTimeSuffixFormat`。

修复：

```objc
// L322: 替换硬编码
-   titleText = [NSString stringWithFormat:@"%@(%u人)", nickname ?: @"", count];
+   NSString *suffix = config.chatGroupMemberCountSuffix.length > 0
+       ? config.chatGroupMemberCountSuffix : @"%u人";
+   titleText = [NSString stringWithFormat:@"%@%@",
+       nickname ?: @"",
+       [NSString stringWithFormat:suffix, count]];

// L334: 替换硬编码
-   titleText = [NSString stringWithFormat:@"%@(%ld天)", nickname ?: @"", (long)days];
+   NSString *suffix = config.chatAddTimeSuffixFormat.length > 0
+       ? config.chatAddTimeSuffixFormat : @"%ld天";
+   titleText = [NSString stringWithFormat:@"%@%@",
+       nickname ?: @"",
+       [NSString stringWithFormat:suffix, (long)days]];
```

同时需要把 `chatGroupMemberCountSuffix` 和 `chatAddTimeSuffixFormat` 加入 PluginConfig.m 的 loadDefaults/save（与 chatSeparatorText 相同模式）。

---

## Bug 4：loadSeparatorIcon 与 loadSeparatorGIF 冲突

> 涉及文件：[CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) L339-L341

```objc
[self loadSeparatorIcon];   // ← 写 separatorView.image
[self loadSeparatorGIF];    // ← 又写 separatorView.image（覆盖前者）
[self loadSeparatorText];
```

**后果**：用户同时设置了图标和 GIF 时，GIF 总是覆盖图标。应该改为只加载一种：

```objc
// 对齐 123456.c L12229 优先级：Icon > GIF > Text
if (![self loadSeparatorIcon]) {
    if (![self loadSeparatorGIF]) {
        [self loadSeparatorText];
    }
}
```

并把 `loadSeparatorIcon` 和 `loadSeparatorGIF` 改为返回 BOOL 表示是否成功加载。

---

## Bug 5：updateFontSizes 缺 setNeedsLayout

> [CSChatAvatarTitleView.m L476-L482](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L476-L482)

```objc
- (void)updateFontSizes {
    // ... 设置字体 ...
    // ★ 缺少触发重新布局（对齐 123456.c L12425）
    [self setNeedsLayout];
    [self layoutIfNeeded];
}
```

---

## Bug 6：CSChatAvatarTitleView.h chatController 属性 assign 风险

> [CSChatAvatarTitleView.h L7](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.h) — 轻微风险，当前可保持

| 项目 | 微信优化 123456.c |
|------|------------------|
| `assign` (MRC) | `weak` (ARC, `_objc_storeWeak`/`_objc_loadWeakRetained`/`_objc_destroyWeak`) |

MRC 下 `assign` 不会在对象释放时自动置 nil，但在此场景下 VC 释放 → navigationItem 释放 → titleView 释放 → CSChatAvatarTitleView 释放 → 不存在悬垂指针问题。**当前无需修改。**

---

## 完整文件行动清单

| # | 文件 | 操作 | 严重度 |
|---|------|------|--------|
| 1 | [CSChatAvatarTitleView.m #276](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L276) | `initWithContact:` → `init` + `setM_contact:` | **致命** |
| 2 | [PluginConfig.m](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m) save | 追加 16 个属性的 setBool/setFloat/setInteger | **严重** |
| 3 | [CSChatAvatarTitleView.m #322](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L322) | `@"(%u人)"` → `config.chatGroupMemberCountSuffix` | 中 |
| 4 | [CSChatAvatarTitleView.m #334](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L334) | `@"(%ld天)"` → `config.chatAddTimeSuffixFormat` | 中 |
| 5 | [CSChatAvatarTitleView.m #339](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L339) | Icon+GIF 顺序改为优先级加载 | 中 |
| 6 | [CSChatAvatarTitleView.m #482](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L482) | updateFontSizes 追加 setNeedsLayout/layoutIfNeeded | 低 |
| 7 | [PluginConfig.m](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m) loadDefaults/save | 追加 suffix format 两个属性的持久化 | 中 |

---

## Crash 完整链路解释

```
打开微信 → 进入聊天 → BaseMsgContentVC.viewWillAppear:
    → ChatTopBarHook.hook_viewWillAppear (FUN_00014ed0)
    → [CSChatAvatarTitleView updateAvatars]
    → [self silentLoadContactExtInfo:contact]

        ↓ L275: ContactInfoViewControllerClass = objc_getClass("ContactInfoViewController")  ✓
        ↓ L275: infoVC = [Class alloc]  ✓
        ↓ L276: infoVC = [infoVC initWithContact:contact]  ✗
                  ↑ 此方法不存在 ↑
                  ↓ NSInvalidArgumentException
                  ↓ SIGABRT → crash

微信优化不会 crash 的原因：
        ↓ [Class alloc]
        ↓ [infoVC init]                          ← 普通 init
        ↓ [infoVC setM_contact:contact]           ← setter 注入，不依赖特定 init 方法
        ↓ [infoVC view]
        ↓ 0.8s 后 dismiss
```

实测 `.ips` 文件中有 **8 个插件**同时运行（插件收纳/Mio/锤子/黄白/WCRefine/Mikoto/微信助手/微信优化），只有 `Mio_arm64-1.dylib` 触发 crash，因为只有这个插件调了不存在的 `initWithContact:`。