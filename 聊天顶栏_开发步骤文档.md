# 聊天顶栏功能 — 开发步骤文档（第十二次修正版）

> 本次重点：**弹窗布局错乱（数据堆在同一行）+ 弹窗比微信优化大很多** — 深度对比 123456.c 的布局架构  
> **项目用 MRC 编译**

---

## 123456.c 弹窗架构深度分析

### 整体架构：UITableView + CSSettingTableViewCell

```
CSUserInfoPopoverController : UIViewController <UITableViewDataSource, UITableViewDelegate>
│
├── UITableView (无缝隙系统列表体验)
│   │  style: 2 (UITableViewStyleGrouped?)
│   │  rowHeight: 46pt
│   │  contentInset: (8, 8, 8, 8)
│   │  separatorInset: (10, 0, 10, 0)
│   │  estimatedRowHeight: UITableViewAutomaticDimension
│   │  backgroundColor: white
│   │
│   ├── tableHeaderView: createHeaderView()
│   │   └── UIView(白色, height 20pt, 仅做顶部间距)
│   │
│   ├── Section 0 (头像区)
│   │   │  height: 170pt（固定）
│   │   │  复用标识: "AvatarCell"
│   │   │  cell 内包含:
│   │   │    UIImageView(头像 60x60 圆形) + UILabel(昵称)
│   │   │    头像从 contact 加载
│   │   │
│   ├── Section 1 (基本信息)
│   │   │  height: UITableViewAutomaticDimension (~46pt → 内容自适应)
│   │   │  复用标识: "DetailCell"
│   │   │  每行 = CSSettingTableViewCell
│   │   │    ├─ 左侧 label (title): "类型"/"微信"/"备注"/"性别"/"地区"/"签名"
│   │   │    └─ 右侧 value (detail): 动态 KVC 取值
│   │   │  header: "力口我发的 v 发的" (section 标题)
│   │   │
│   ├── Section 2 (群聊额外信息，仅群聊)
│   │   │  群成员数、群公告等
│   │   │
│   ├── Section 2 (公众号额外信息，仅公众号)
│   │   │  认证状态等
│   │   │
│   └── tableFooterView: createFooterView()
│         └── UIView(白色) + 底部复制提示 label
│              font: systemFontOfSize:13
│              textColor: grayColor
│              padding: (16, 16, 0, 16)
```

### 关键尺寸数值

| 参数 | 123456.c 值 | 来源 |
|------|-----------|------|
| **弹窗尺寸** | 272 x 400 | L12934: 浮点 0x4071800000000000=272.0, 0x4079000000000000=400.0 |
| **行高** | 46pt | L116672: 浮点 0x4046=46.0 |
| **头像 cell 高度** | 170pt | L118454-L118456 |
| **内容内边距** | (8, 8, 8, 8) | L116676-L116677 |
| **分割线缩进** | (10, 0, 10, 0) | L116678 |
| **detail 字体** | systemFontOfSize:15 | L118215: 0x402e=15.0 |
| **空值字体颜色** | (0.6, 0.6, 0.6, 1.0) | L118237-L118240 |
| **有值字体颜色** | (0.25, 0.25, 0.25, 1.0) | L118237-L118240 |

### CSSettingTableViewCell 的优势

123456.c 直接使用微信内部类 `CSSettingTableViewCell`（L116691 `registerClass:`），该类自带：
- 左右双 Label 自动布局（title 左 + detail 右）
- 与微信设置页完全一致的视觉风格
- 点击高亮、accessory view 等标准行为
- 自动处理文字截断和换行

---

## 项目当前实现 vs 123456.c 差异分析

### 差异 1（BUG）：所有数据堆在同一行

**位置**：[CSContactInfoPopoverController.m L158-L159](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m#L158-L159)

```objc
// L150-151: label — y 坐标被硬编码为 0！
UILabel *label = [[UILabel alloc] initWithFrame:CGRectMake(20, 0, 60, kPopoverRowH)];
                                                        // ↑ BUG: 应该是 y，不是 0

// L158-159: value — y 坐标也被硬编码为 0！
UILabel *value = [[UILabel alloc] initWithFrame:CGRectMake(100, 0, cardWidth - 120, kPopoverRowH)];
                                                          // ↑ BUG: 应该是 y，不是 0
```

循环中 `y = i * kPopoverRowH` 计算正确（L147），但 label 和 value 的 frame 里没有用 `y`，写死了 `0`。结果 7 行数据全部叠在 Y=0 位置。而分割线（L167）和透明手势层（L178）正确用了 `y`，所以分割线位置是对的，文字却都不在分割线上。

### 差异 2：弹窗尺寸大 30%

| | 123456.c | 项目 |
|---|---|---|
| 宽 | 272pt | **300pt** (+10%) |
| 高 | 400pt | **520pt** (+30%) |
| 行高 | 46pt | **50pt** (+9%) |

123456.c 272x400 是 iPhone 上 popover 的标准紧凑尺寸。项目 300x520 在 iPhone SE/小屏机型上可能会超出安全区域。

### 差异 3：UIScrollView 手动布局 vs UITableView

项目使用 UIScrollView + 手动 CGRect 定位 UILabel，123456.c 用 UITableView + CSSettingTableViewCell：

- **分隔线**：123456.c 用系统 cell 自带分隔线（separatorInset: 10pt 左右缩进），项目手动画 UIView 分隔线（0.5pt 高）
- **滚动**：123456.c 用 UITableView 原生滚动，项目用 UIScrollView 设 contentSize
- **Cell 复用**：123456.c 有 Cell 复用（dequeueReusableCell），数据多时不卡；项目每行都 alloc 新的 UILabel
- **点击选中**：123456.c 用 `tableView:didSelectRowAtIndexPath:` 原生选中效果，项目用手势识别器

### 差异 4：无 Section 分组

123456.c 分 3 个 section：头像区（170pt cell）+ 基本信息区（多行）+ 群聊/公众号额外区。Section 之间用 UITableViewStyleGrouped 的圆角卡片自动分隔。

项目把所有 7 个信息行平铺在一个卡片里，没有分组概念。

### 差异 5：tableHeaderView / tableFooterView

123456.c 用 createHeaderView（20pt 空白）+ createFooterView（提示文字 "点击信息项复制到剪贴板"），与表格自然融合。

项目直接往 UIScrollView 里 add headerView（180pt 卡片）+ infoCard + tipLabel，三块独立视图。

### 差异 6：字体大小

| | 123456.c | 项目 |
|---|---|---|
| 信息行 detail 字体 | 15pt (0x402e) | 13pt |
| 空值颜色 | (0.6, 0.6, 0.6) | (0.56, 0.56, 0.58) |
| 有值颜色 | (0.25, 0.25, 0.25) | 未区分 |

---

## 修复清单

### 修复 1（必须）：修复 y 坐标 BUG

[CSContactInfoPopoverController.m L150-L159](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m#L150-L159)

```objc
// 改前（L150-151）：
UILabel *label = [[UILabel alloc] initWithFrame:CGRectMake(20, 0, 60, kPopoverRowH)];
// 改后：
UILabel *label = [[UILabel alloc] initWithFrame:CGRectMake(20, y, 60, kPopoverRowH)];

// 改前（L158-159）：
UILabel *value = [[UILabel alloc] initWithFrame:CGRectMake(100, 0, cardWidth - 120, kPopoverRowH)];
// 改后：
UILabel *value = [[UILabel alloc] initWithFrame:CGRectMake(100, y, cardWidth - 120, kPopoverRowH)];
```

### 修复 2（推荐）：缩小弹窗尺寸

```objc
// [CSChatAvatarTitleView.m presentUserInfoPopoverWithContact:] 中
// 改前：CGSizeMake(300, 520)
// 改后：CGSizeMake(272, 400)  // 与 123456.c L12934 一致
```

### 修复 3（推荐）：缩小行高

```objc
// [CSContactInfoPopoverController.m L9]
// 改前：static const CGFloat kPopoverRowH = 50.0;
// 改后：static const CGFloat kPopoverRowH = 46.0;  // 与 123456.c L116672 一致
```

### 修复 4（可选）：改用 CSSettingTableViewCell

如果微信版本中有 `CSSettingTableViewCell` 类（123456.c L116691 注册了该类），可改为 UITableView 架构：

```objc
// setupUI 中：
UITableView *tableView = [[UITableView alloc] initWithFrame:self.view.bounds style:2];
tableView.delegate = self;
tableView.dataSource = self;
tableView.rowHeight = 46;
[tableView registerClass:objc_getClass("CSSettingTableViewCell") forCellReuseIdentifier:@"DetailCell"];
self.view = tableView;

// cellForRowAtIndexPath 中：
UITableViewCell *cell = [tableView dequeueReusableCellWithIdentifier:@"DetailCell"];
cell.textLabel.text = @"微信";     // CSSettingTableViewCell 的左侧 label
cell.detailTextLabel.text = wxid;  // CSSettingTableViewCell 的右侧 label
```

> 注意：CSSettingTableViewCell 是微信内部类，不是系统类。如果 `objc_getClass` 返回 nil，保持当前手动布局方案即可。

---

## 先前回顾（已修复项状态）

| # | 内容 | 状态 |
|---|------|------|
| 1-12 | 前 12 项（布局/头像/haptic/配置持久化等）| ✓ |
| 13 | UI 已实现（CSContactInfoPopoverController 全部方法已写）| ✓ |
| 14 | crash fix：L50 `[super viewDidLoad]` | ✓ 已修复 |
| 15-17 | adaptivePresentation / delegate / 协议 | ✓ 已修复 |
| 18 | **修复 y 坐标 BUG：label/value 的 y:0 → y:y** | **P0** |
| 19 | **缩小弹窗尺寸：300x520 → 272x400** | **P0** |
| 20 | **缩小行高：50 → 46** | **P0** |
| 21 | 改用 CSSettingTableViewCell（如果类存在） | P1 可选 |

---

## 修改速查

| 文件 | 行号 | 改什么 |
|------|------|--------|
| `CSContactInfoPopoverController.m` | L9 | `50.0` → `46.0` |
| `CSContactInfoPopoverController.m` | L150 | `CGRectMake(20, 0, 60, ...)` → `CGRectMake(20, y, 60, ...)` |
| `CSContactInfoPopoverController.m` | L158 | `CGRectMake(100, 0, ...)` → `CGRectMake(100, y, ...)` |
| `CSChatAvatarTitleView.m` | popover size | `(300, 520)` → `(272, 400)` |