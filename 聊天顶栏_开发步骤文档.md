# 聊天顶栏功能 — 开发步骤文档（修正版）

> 基于 [聊天顶栏功能_完整实现文档.md](file:///www/wwwroot/ios/聊天顶栏功能_完整实现文档.md) 的 123456.c 反编译分析  
> 对比项目现有 [CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) 等 6 个文件逐项验证  

---

## 前置概览：已实现 vs 差异

```
已正确实现 ✓：
  CSChatAvatarTitleView.m  — setupSubviews / updateAvatars / 头像加载优先级
  ChatTopBarHook.m          — 两个 Hook + isContactInBlacklist
  FeatureModuleRegistry     — ChatTopBarHook 已注册
  SettingChatTopBarController — UI 完整（3 Card, 9 数值, 8 mode, separator text, icon picker delegate）
  ChatTopBarBlacklistEditorVC — 黑名单编辑器（save/load/键盘适配）✓
  PluginConfig.h            — 全部 16 个属性已声明
  SettingCategoryController  — KVC 映射 4 个 BOOL → PluginConfig 属性

存在差异 ✗（需修复）：
  PluginConfig.m            — loadDefaults / save 完全缺失全部聊天顶栏属性
  CSChatAvatarTitleView.m   — mode 2 标签与布局不一致 / applyPositionOffset / calculateNameWidth
  反编译 5 个功能未实现       — silentLoadContactExtInfo / suffix format / GIF / 字体刷新
```

---

## Step 1：PluginConfig.m — 持久化全部 16 个属性 [严重Bug]

> 涉及文件：[PluginConfig.m](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m)

`SettingCategoryController.switchChanged:` 已通过 KVC 正确设置 `config.showChatAvatar = YES` 并调用 `[config save]`，但 `save` 方法内从未把这些属性写入 NSUserDefaults。**重启应用后全部聊天顶栏设置丢失**。

### 1.1 loadDefaults 追加

在 `_hideOASelfAvatar` 行（L323）之后插入：

```objc
    // ========== 聊天顶栏 ==========
    _showChatAvatar = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ShowChatAvatar"]];
    _avatarTapFeedback = [d boolForKey:[kPluginPrefix stringByAppendingString:@"AvatarTapFeedback"]];
    _showAddTime = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ShowAddTime"]];
    _showGroupMemberCount = [d boolForKey:[kPluginPrefix stringByAppendingString:@"ShowGroupMemberCount"]];

    _chatDisplayMode = [d integerForKey:[kPluginPrefix stringByAppendingString:@"ChatDisplayMode"]];
    if (_chatDisplayMode < 0 || _chatDisplayMode > 7) _chatDisplayMode = 3;

    _chatAvatarSize = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarSize"]];
    if (_chatAvatarSize == 0) _chatAvatarSize = 30.0;

    _chatAvatarSpacing = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarSpacing"]];
    if (_chatAvatarSpacing == 0) _chatAvatarSpacing = 3.0;

    _chatAvatarCornerRadius = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarCornerRadius"]];
    if (_chatAvatarCornerRadius == 0) _chatAvatarCornerRadius = 1.0;

    _chatNicknameFontSize = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatNicknameFontSize"]];
    if (_chatNicknameFontSize == 0) _chatNicknameFontSize = 9.0;

    _chatSeparatorSize = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorSize"]];
    if (_chatSeparatorSize == 0) _chatSeparatorSize = 12.0;

    _chatTitleViewWidth = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatTitleViewWidth"]];
    if (_chatTitleViewWidth == 0) _chatTitleViewWidth = 210.0;

    _chatVerticalOffset = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatVerticalOffset"]];
    _chatHorizontalOffset = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatHorizontalOffset"]];
    _chatNicknameOffsetY = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatNicknameOffsetY"]];
    _chatNicknameOffsetX = [d floatForKey:[kPluginPrefix stringByAppendingString:@"ChatNicknameOffsetX"]];

    v = [d stringForKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorText"]];
    if (v.length > 0) {
        [_chatSeparatorText release];
        _chatSeparatorText = [v copy];
    } else {
        _chatSeparatorText = [@"" retain];
    }
```

### 1.2 save 追加

在 `_hideOASelfAvatar` 行（L450）之后插入：

```objc
    // ========== 聊天顶栏 ==========
    [d setBool:_showChatAvatar forKey:[kPluginPrefix stringByAppendingString:@"ShowChatAvatar"]];
    [d setBool:_avatarTapFeedback forKey:[kPluginPrefix stringByAppendingString:@"AvatarTapFeedback"]];
    [d setBool:_showAddTime forKey:[kPluginPrefix stringByAppendingString:@"ShowAddTime"]];
    [d setBool:_showGroupMemberCount forKey:[kPluginPrefix stringByAppendingString:@"ShowGroupMemberCount"]];
    [d setInteger:_chatDisplayMode forKey:[kPluginPrefix stringByAppendingString:@"ChatDisplayMode"]];
    [d setFloat:_chatAvatarSize forKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarSize"]];
    [d setFloat:_chatAvatarSpacing forKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarSpacing"]];
    [d setFloat:_chatAvatarCornerRadius forKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarCornerRadius"]];
    [d setFloat:_chatNicknameFontSize forKey:[kPluginPrefix stringByAppendingString:@"ChatNicknameFontSize"]];
    [d setFloat:_chatSeparatorSize forKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorSize"]];
    [d setFloat:_chatTitleViewWidth forKey:[kPluginPrefix stringByAppendingString:@"ChatTitleViewWidth"]];
    [d setFloat:_chatVerticalOffset forKey:[kPluginPrefix stringByAppendingString:@"ChatVerticalOffset"]];
    [d setFloat:_chatHorizontalOffset forKey:[kPluginPrefix stringByAppendingString:@"ChatHorizontalOffset"]];
    [d setFloat:_chatNicknameOffsetY forKey:[kPluginPrefix stringByAppendingString:@"ChatNicknameOffsetY"]];
    [d setFloat:_chatNicknameOffsetX forKey:[kPluginPrefix stringByAppendingString:@"ChatNicknameOffsetX"]];
    if (_chatSeparatorText) {
        [d setObject:_chatSeparatorText forKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorText"]];
    }
```

---

## Step 2：CSChatAvatarTitleView.m — layoutSubviews mode 2 修复 [明确Bug]

> 涉及文件：[CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) L198

**问题**：mode 2 标签是"显示双方头像"，但 L198 行的 `(mode == 1 || mode == 2 || mode == 3 || mode == 4)` 分支中 mode 2 被归为 `showRight=YES`，只显示右侧头像。

**修复**：mode 2 应从单头像分支中剔除，让它落到默认的双方头像布局：

```objc
// L198: 将 mode 2 从单头像分支中移除
-    if (mode == 1 || mode == 2 || mode == 3 || mode == 4) {
-        BOOL showLeft = (mode == 0 || mode == 3 || mode == 4);
-        BOOL showRight = (mode == 1 || mode == 2);
+    if (mode == 1 || mode == 3 || mode == 4) {
+        BOOL showLeft = (mode == 3 || mode == 4);
+        BOOL showRight = (mode == 1);
```

---

## Step 3：CSChatAvatarTitleView.m — 补充 5 个缺失功能

### 3.1 silentLoadContactExtInfo: — 后台预加载联系人信息

> 对齐 123456.c L11926，防止点击头像时等待加载

在 `CSChatAvatarTitleView.m` 中新增：

```objc
- (void)silentLoadContactExtInfo:(id)contact {
    Class infoVCClass = objc_getClass("ContactInfoViewController");
    if (!infoVCClass) return;

    id infoVC = ((id (*)(Class, SEL))objc_msgSend)(infoVCClass, NSSelectorFromString(@"alloc"));
    infoVC = ((id (*)(id, SEL, id))objc_msgSend)(infoVC, NSSelectorFromString(@"initWithContact:"), contact);
    if (!infoVC) return;

    ((void (*)(id, SEL))objc_msgSend)(infoVC, @selector(view)); // 强制加载

    dispatch_after(dispatch_time(DISPATCH_TIME_NOW, 0.8 * NSEC_PER_SEC),
                   dispatch_get_main_queue(), ^{
        [infoVC dismissViewControllerAnimated:NO completion:nil];
        [infoVC release];
    });
}
```

并在 `updateAvatars` 的 `GetContact` 之后调用：

```objc
- (void)updateAvatars {
    // ... existing code ...
    id contact = ((id (*)(id, SEL))objc_msgSend)(self.chatController, NSSelectorFromString(@"GetContact"));
    if (!contact) return;

    // ★ 新增
    [self silentLoadContactExtInfo:contact];

    // ... rest of existing code ...
}
```

### 3.2 updateAvatars — 添加可配置后缀格式

> 对齐 123456.c chat.addtime.suffix.format / chat.group.member.count.suffix.format

当前后缀硬编码为 `(%u人)` 和 `(%ld天)`。改为从 PluginConfig 读取：

```objc
// 群成员数部分 — 替换当前 L300 行
if (isGroup && config.showGroupMemberCount) {
    // ... 获取 memberCount ...

    // 读取后缀格式（默认 "%u人"）
    NSString *suffix = config.groupMemberCountSuffix ?: @"%u人";
    titleText = [NSString stringWithFormat:@"%@%@",
        nickname ?: @"",
        [NSString stringWithFormat:suffix, count]];
}

// 添加时间部分 — 替换当前 L312 行
if (!isGroup && config.showAddTime) {
    // ... 计算天数 ...

    // 读取后缀格式（默认 "%ld天"）
    NSString *suffix = config.addTimeSuffixFormat ?: @"%ld天";
    titleText = [NSString stringWithFormat:@"%@%@",
        nickname ?: @"",
        [NSString stringWithFormat:suffix, (long)days]];
}
```

需要给 [PluginConfig.h](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.h) 添加两个属性：

```objc
@property (nonatomic, copy) NSString *groupMemberCountSuffix;  // 群成员数后缀格式（默认"%u人"）
@property (nonatomic, copy) NSString *addTimeSuffixFormat;     // 添加时间后缀格式（默认"%ld天"）
```

并在 PluginConfig.m 的 loadDefaults / save / dealloc 中增加对应持久化代码（参照 chatSeparatorText 模式）。

### 3.3 calculateNameWidth — 恢复完整计算逻辑

> 对齐 123456.c L12575，当前简化为 `return self.bounds.size.width`

```objc
- (CGFloat)calculateNameWidth {
    PluginConfig *config = [PluginConfig shared];

    // 获取昵称文本
    NSString *text = self.titleLabel.text ?: @"";
    if (text.length == 0) return 30.0;

    // 用 NSAttributedString 计算实际渲染宽度
    UIFont *font = [UIFont systemFontOfSize:config.chatNicknameFontSize];
    NSDictionary *attrs = @{NSFontAttributeName: font};
    CGFloat textWidth = [text sizeWithAttributes:attrs].width;

    // clamp
    CGFloat minW = self.bounds.size.width - config.chatAvatarSize - 20;
    if (minW > 30.0) minW = 30.0;
    return MAX(textWidth + 4.0, minW);
}
```

### 3.4 loadSeparatorGIF — 分离 GIF 加载

> 对齐 123456.c L12229 GIF 分支

在 `loadSeparatorIcon` 之后新增：

```objc
- (void)loadSeparatorGIF {
    NSData *gifData = [[NSUserDefaults standardUserDefaults]
        dataForKey:[kPluginPrefix stringByAppendingString:@"ChatSeparatorGIF"]];
    if (gifData) {
        UIImage *gifImage = [UIImage imageWithData:gifData];
        self.separatorView.image = gifImage;
        self.separatorView.hidden = NO;
        [self.separatorView sizeToFit];
        self.separatorView.userInteractionEnabled = YES;
    }
}
```

并在 `updateAvatars` 中 `[self loadSeparatorText]` 之后追加 `[self loadSeparatorGIF]`（替代 `loadSeparatorIcon` 中缺失的 GIF 加载）。

### 3.5 updateFontSizes — 补全 setNeedsLayout 链

> 当前 L441-446 已经写好了字体设置代码但缺少触发布局

```objc
- (void)updateFontSizes {
    PluginConfig *config = [PluginConfig shared];
    [self.titleLabel setFont:[UIFont systemFontOfSize:config.chatNicknameFontSize]];

    CGFloat sepFontSize = MAX(8.0, MIN(config.chatSeparatorSize * 0.4, 16.0));
    [self.separatorTextLabel setFont:[UIFont systemFontOfSize:sepFontSize weight:UIFontWeightMedium]];

    // 对齐 123456.c L12425 — 调整字体后触发 relayout
    [self setNeedsLayout];
    [self layoutIfNeeded];
}
```

---

## Step 4：SettingChatTopBarController — 新增 4 个设置项

### 4.1 新增 suffix format 输入字段（Card 1 内）

在 `buildUI` 的 Card 1 `subBuilder` block 中，`showGroupMemberCount` 开关行之后追加：

```objc
*ecy = [self addSeparatorInGroup:expand cy:*ecy width:w];
*ecy = [self addInputRowInGroup:expand title:@"群人数后缀" key:@"GroupMemberCountSuffix"
    value:config.groupMemberCountSuffix ?: @"%u人" hint:@"如: %u人" cy:*ecy width:w];
*ecy = [self addSeparatorInGroup:expand cy:*ecy width:w];
*ecy = [self addInputRowInGroup:expand title:@"添加时间后缀" key:@"AddTimeSuffixFormat"
    value:config.addTimeSuffixFormat ?: @"%ld天" hint:@"如: %ld天" cy:*ecy width:w];
```

### 4.2 配置 categoryKey→property 映射

在 `SettingCategoryController.m` 的 `configPropertyForKey:` 方法 (L58-L96) 中追加：

```objc
@"GroupMemberCountSuffix": @"groupMemberCountSuffix",
@"AddTimeSuffixFormat": @"addTimeSuffixFormat",
```

---

## Step 5：CSChatAvatarTitleView.h — 修正 property 语义 [轻微]

> 涉及文件：[CSChatAvatarTitleView.h](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.h) L7

**当前**：`@property (nonatomic, assign) BaseMsgContentViewController *chatController;`

`assign` 在 MRC 下不会自动置 nil，当 ViewController 被释放时变成野指针。123456.c 使用 `_objc_storeWeak`（ARC weak 等价）。MRC 下正确的做法是在 dealloc 中置 nil 或使用 unsafe_unretained + Hook 生命周期。

**方案（MRC 兼容）**：保持 `assign`，但在 CSChatAvatarTitleView 的 `dealloc` 中不需要特殊处理（View 跟随 VC 释放）。如果要更安全，参考 HideAvatarHook 的做法：

```objc
// 不需要修改代码，但需知晓风险：
// chatController 的生命周期 ≤ CSChatAvatarTitleView 的生命周期（View 在 VC 的 navigationItem 上）
// 所以 VC 释放时 View 也一并释放，assign 在 MRC 下是安全的
```

> **结论**：此项无需修改。

---

## 完整文件行动清单

| Step | 文件 | 操作 | 行数 |
|------|------|------|------|
| 1 | [PluginConfig.m](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m) | loadDefaults 追加 16 个属性 | ~50 行 |
| 1 | [PluginConfig.m](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m) | save 追加 16 个属性 | ~20 行 |
| 2 | [CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) | L198 修复 mode 2 分支 | 2 行 |
| 3.1 | [CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) | 新增 silentLoadContactExtInfo: | ~20 行 |
| 3.2 | [PluginConfig.h](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.h) | 新增 2 个 suffix 属性 | 2 行 |
| 3.2 | [PluginConfig.m](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m) | loadDefaults/save/dealloc 追加 suffix | ~10 行 |
| 3.2 | [CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) | updateAvatars 后缀格式从硬编码改为配置读取 | ~10 行 |
| 3.3 | [CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) | calculateNameWidth 完整实现 | ~15 行 |
| 3.4 | [CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) | 新增 loadSeparatorGIF | ~12 行 |
| 3.5 | [CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) | updateFontSizes 追加 setNeedsLayout 链 | 2 行 |
| 4.1 | [SettingChatTopBarController.m](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/SettingChatTopBarController.m) | buildUI 新增 2 个 suffix 输入行 | ~8 行 |
| 4.2 | [SettingCategoryController.m](file:///www/wwwroot/ios/WeChatPlugin/Settings/Common/SettingCategoryController.m) | configPropertyForKey 追加 2 个映射 | 2 行 |

**总计**：修改 4 个已有文件，新增约 150 行代码。

---

## 差异对照表（全量）

### 严重 Bug（已修复则设置生效）

| 位置 | 问题 | 影响 |
|------|------|------|
| PluginConfig.m loadDefaults | 16 个聊天顶栏属性均未从 NSUserDefaults 读取 | 重启后全部恢复默认值（开关=NO，数值=0） |
| PluginConfig.m save | 16 个聊天顶栏属性均未写入 NSUserDefaults | KVC 设置只在内存中，synchronize 无效 |
| layoutSubviews mode 2 | 标签"显示双方头像"但归入单头像分支 showRight=YES | 用户选 mode 2 只看到右侧单头像 |

### 缺失功能（123456.c 有，项目无）

| 功能 | 123456.c 行号 | 影响 |
|------|-------------|------|
| silentLoadContactExtInfo: | L11926 | 点击头像弹窗有网络加载延迟 |
| addTimeSuffixFormat 配置 | L414 | 添加时间后缀硬编码 `%ld天`，不匹配原版灵活性 |
| groupMemberCountSuffix 配置 | L415 | 群成员数后缀硬编码 `%u人` |
| loadSeparatorGIF | L12229 GIF 分支 | 用户选的 GIF 不显示 |
| calculateNameWidth 完整逻辑 | L12575 | titleLabel 在部分模式下宽度计算不准确 |
| updateFontSizes 后 setNeedsLayout | L12425 末尾 | 字体修改后可能不立即重新布局 |
| Separator GIF 设置页 | 123456.c 设置页中有 | 用户选择 GIF 后无法在 UI 中确认已设置 |

### 与 123456.c 设计差异（可接受）

| 差异 | 项目做法 | 123456.c 做法 |
|------|---------|--------------|
| 显示模式标签 | 自定义中文标签("显示双方头像"等) | 中文标签("仅对方头像"等) |
| 用户信息弹窗方式 | push navigation | present popover (CSUserInfoPopoverController) |
| chatController 属性 | `assign` (MRC) | `weak` (ARC _objc_storeWeak) |
| rightAvatarView interaction | YES | NO (初始禁用) |