# 聊天顶栏功能 — 开发步骤文档（第二十二次修正版：深度复查 + 精确定位差异）

> **本次复查**：对两个 crash log、黄白助手反编译代码（90MB）、微信优化反编译代码（4MB）、Mio 源码进行逐行交叉对比。
>
> **核心结论**：冲突方是 Mio ↔ 黄白助手 + WCRefine 的三方交互。微信优化不冲突的原因是它 **不在 push 动画期间修改 titleView**，且 **不调用 removeFromSuperview**。这两个差异共同导致了我们的崩溃。
>
> **项目用 MRC 编译**

---

## 一、Crash Log 精确复查

### 1.1 crash_2605271729.log（由 LoadControl 捕获）

```
调用栈（底→顶）：
Frame 40: dyld
Frame 39: WeChat main (mmkvMemcpy + 129860)
Frame 38: UIApplicationMain
Frame 37-31: UIKitCore → CoreFoundation RunLoop
Frame 30-21: QuartzCore → UIKitCore 渲染管线
Frame 20: WCRefine 1.4.4.dylib (WCRefineNavApplyOpenSmallTailPruneIfNeeded + 346416)  ← 导航裁剪hook
Frame 19: 黄白助手 2.0-9-2.dylib (GetMusicToVoice + 3814201)                        ← 黄白助手在栈中
Frame 18: WeChat (GetSplashCacheImage + 63152452)
Frame 17-13: UIKitCore UITableView (_updateVisibleCells → _notifyWillDisplayCell)
Frame 12-10: WeChat Flutter + internal
Frame 9:   ??? (trampoline 0x1259DC03C)
Frame 8-3: WeChat internal
Frame 2:   ??? (corrupt frame)
Frame 1-0: libsystem_platform signal handler → LoadControl

culprit=黄白助手 2.0-9-2
信号=SIGSEGV 地址=0x54
```

**关键路径**：CoreAnimation 渲染 → **WCRefine 导航裁剪** → **黄白助手** → WeChat → UITableView 渲染 cell → 中间经过某 trampoline → dispatch_async 块访问 0x54 崩溃。

### 1.2 WeChat-2026-05-27-162645(1).ips

```
帧（底→顶）：
Frame 16-11: UIKitCore UITableView (_updateVisibleCells → _notifyWillDisplayCell)
Frame 10-8: WeChat Flutter + WeChat internal
Frame 7:    imageIndex=29 (source "A", base=0, 无UUID) offset=5015306300 ← Substrate trampoline
Frame 6-1: WeChat internal
Frame 0:    libdispatch dispatch_async + 188 ← CRASH 0x54
```

**关键发现**：Frame 7 的 trampoline 地址 `0x12AF7439C` 落在锤子助手（end 0x12A1B5000）和 HBB（start 0x130728000）之间的 ~111MB 空白区。但结合调用上下文（Frame 6/8 都是 WeChat 代码），以及微信优化正是 hook 了 `NewMainFrameViewController::tableView:cellForRowAtIndexPath:`（验证见下），**这个 trampoline 可以确定为微信优化的 cellForRowAtIndexPath hook trampoline**。

### 1.3 两个 Crash 的关系

| | crash_2605271729.log | IPS |
|---|---|---|
| 触发点 | CoreAnimation → WCRefine → 黄白助手 | UITableView _notifyWillDisplayCell |
| 中间 hook | 微信优化 trampoline (frame 9) | 微信优化 trampoline (frame 7) |
| 崩溃点 | WeChat → dispatch_async | WeChat → dispatch_async |
| 地址 | 0x54 | 0x54 |
| 黄白助手在栈中 | ✅ 直接出现 (frame 19) | ❌ 不在直接栈中 |

**结论**：两个 crash 是同一类问题在不同渲染时机触发。根本链路是：

```
某操作触发导航栏/tableView 布局
→ WCRefine navigation pruning hook 介入
→ 黄白助手渲染路径 hook 介入
→ WeChat 内部
→ 微信优化 cellForRowAtIndexPath hook trampoline
→ WeChat 内部 dispatch_async 块
→ 访问已释放对象的 ivar at 0x54 → CRASH
```

---

## 二、微信优化 vs Mio：逐行交叉对比

### 2.1 Hook 点对比

| Hook 点 | 微信优化 | Mio（我们） |
|---------|---------|-----------|
| `BaseMsgContentVC::viewDidLoad` | ✅ FUN_00014c74 | ✅ hook_viewDidLoad |
| `BaseMsgContentVC::viewWillAppear:` | ✅ FUN_00014ed0 | ✅ hook_viewWillAppear |
| `UINavigationController::pushViewController:animated:` | ✅ FUN_0003dae8（**页面锁，不碰 titleView**） | ✅ hook_pushViewController（**聊天顶栏，修改 titleView**） |
| `NewMainFrameVC::tableView:cellForRowAtIndexPath:` | ✅ L30220（聊天增强） | ❌ 不 hook |

### 2.2 viewDidLoad 逐行对比

| 操作 | 微信优化 (FUN_00014c74) | Mio (ChatTopBarHook.m) |
|------|------------------------|------------------------|
| 调用原始 viewDidLoad | ✅ | ✅ |
| 检查功能开关 | ✅ chat_avatar_display_enabled | ✅ showChatAvatar |
| 检查黑名单 | ✅ | ✅ |
| 获取 contact/username | ✅ | ✅ |
| 创建自定义 TitleView | ✅ CSChatAvatarTitleView | ✅ MioChatAvatarTitleView |
| `removeFromSuperview` | **❌ 不调用** | **✅ L79 调用！** |
| `setTitleView:` | ✅ (L13341) | ✅ (L80) |
| `updateAvatars` | ✅ (L13343) | ❌ (在 viewWillAppear 中) |
| `layoutSubviews` | ❌ (不在 viewDidLoad) | ✅ (L81) |

### 2.3 viewWillAppear: 逐行对比

| 操作 | 微信优化 (FUN_00014ed0) | Mio (ChatTopBarHook.m) |
|------|------------------------|------------------------|
| 调用原始 viewWillAppear | ✅ | ✅ |
| disable 时恢复原始 titleView | ✅ (L13383-13393) | ✅ (L94-101) |
| enable 时 isKindOfClass 检查 | ✅ CSChatAvatarTitleView (L13407) | ✅ MioChatAvatarTitleView (L115) |
| 已存在则跳过创建 | ✅ | ✅ |
| 不存在则创建+setTitleView: | ✅ (L13416-13426) | ✅ (L116-122) |
| updateAvatars | ✅ (L13449) | ✅ (L125) |
| layoutSubviews | ✅ (L13446) | ❌ (不直接调用，用 setNeedsLayout+layoutIfNeeded) |
| setNeedsLayout | ✅ (L13447) | ✅ (L126) |
| layoutIfNeeded | ✅ (L13448) | ✅ (L127) |
| setNeedsDisplay | ✅ (L13450) | ✅ (L128) |
| `removeFromSuperview` | **❌ 不调用** | **❌ viewWillAppear 也不调用** ✅ |

### 2.4 pushViewController:animated: 逐行对比

| 操作 | 微信优化 (FUN_0003dae8) | Mio (ChatTopBarHook.m) |
|------|------------------------|------------------------|
| 用途 | **页面锁（Page Lock）** | **聊天顶栏 titleView 替换** |
| 检查 12 个页面锁设置 | ✅ | ❌ |
| 调用原始 push | ✅ | ✅ |
| 访问 navigationItem | ❌ | ✅ |
| `removeFromSuperview` | ❌ | **✅ L160 调用！** |
| `setTitleView:` | ❌ | ✅ (L161) |

---

## 三、根因分析：三个差异点共同导致崩溃

### 3.1 差异 1（致命）：pushViewController 期间修改 titleView

我们在 push 动画**中间态**修改 navigationItem.titleView：
```
L135-163: hook_pushViewController
  → 调原始 push（动画开始，导航栏进入过渡态）
  → removeFromSuperview 旧 titleView ← 立即触发导航栏布局
  → setTitleView: 新 view ← 再次触发导航栏布局
```

微信优化的 push hook 只做页面锁检查，不碰 UI。这意味着 push 期间：
- 如果只有微信优化：导航栏过渡不受干扰 → 安全
- 如果有我们：导航栏过渡中被两次干扰（removeFromSuperview + setTitleView:）→ 触发额外布局回调

### 3.2 差异 2（重要）：removeFromSuperview 的连锁反应

在 [ChatTopBarHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m#L79) 第 79 行和第 160 行：

```objc
[[self navigationItem].titleView removeFromSuperview];  // L79 (viewDidLoad)
[[viewController navigationItem].titleView removeFromSuperview];  // L160 (push)
```

这个调用的问题：
1. **`setTitleView:` 内部已经正确处理旧视图移除**，不需要手动 removeFromSuperview
2. 手动 removeFromSuperview 会**立即从视图树中撕掉旧 titleView**，触发 UINavigationBar 的 `layoutSubviews`
3. 紧接着的 `setTitleView:` 内部又会处理一次旧视图 → **双重处理**
4. 在 push 动画中间态，导航栏正处于 transition coordinator 管理下，额外的布局回调会破坏其状态

### 3.3 差异 3（环境因素）：三方插件连锁反应

黄白助手 hooks：
- `layoutSubviews` + `setFrame:` 在同一自定义 UIView 子类上（L11769-11790）
- `m_nsNickName` 在 CContact 上（L12592）
- `reloadData`、`collectionView:cellForRowAtIndexPath:` 等 table/collection 方法
- 多处访问 `navigationItem`、设置 `leftBarButtonItem`（L179063-179065）

WCRefine hooks 导航栏布局裁剪（`WCRefineNavApplyOpenSmallTailPruneIfNeeded`）

**这三个插件形成一个脆弱的连锁链：**

```
我们的 push hook 干扰导航栏过渡态
  → 触发了导航栏的意外 layoutSubviews
    → WCRefine 的 navigation pruning hook 被激活（它在监控导航栏布局）
      → WCRefine hook 执行过程中触发黄白助手的 layoutSubviews hook
        → 黄白助手 hook 访问 navigationItem/titleView 状态
          → 状态不一致（因为我们刚干扰过）
            → 黄白助手代码触发 WeChat 内部逻辑
              → 触发 UITableView 渲染（NewMainFrameVC 的会话列表）
                → 微信优化 cellForRowAtIndexPath hook 介入
                  → dispatch_async block 持有了悬空指针
                    → 访问 0x54 → SIGSEGV
```

### 3.4 为什么微信优化不冲突

因为微信优化的 hook 点完全避开了这个连锁链：
1. **push hook 不碰 UI** → 导航栏过渡态不受干扰 → WCRefine 不会被意外激活
2. **不调用 removeFromSuperview** → 不会在 setTitleView: 之前触发额外布局
3. **只在 viewDidLoad/viewWillAppear 中设置 titleView** → 此时导航栏已稳定，布局回调是正常的、预期的

### 3.5 为什么 v18（仅有 viewWillAppear hook）可能也崩溃

如果 v18 是在 viewDidLoad 中加了 `if (!self.view.window) return;` 导致 titleView 从未在 viewDidLoad 中设置，但 viewWillAppear 正常运行——那么崩溃来自 viewWillAppear。

viewWillAppear 虽然不调 removeFromSuperview，但它调用的 `setTitleView:` + `layoutIfNeeded` 仍然会在 viewWillAppear 时机触发布局。此时如果 WCRefine + 黄白助手的 hook 链恰好被激活（例如导航栏还有之前 push 残留的布局任务），仍可能触发相同的崩溃链。

**关键区分**：如果 Mio 不加载，微信优化在 viewWillAppear 中设置 titleView → **也会触发 WCRefine + 黄白助手 hook 链**。但微信优化 + 黄白助手不崩溃。

这说明 MioChatAvatarTitleView 和 CSChatAvatarTitleView 在布局行为上可能有差异，或者微信优化在 viewWillAppear 中的额外保护逻辑（如对 UIView 类型的更严格检查）起到了作用。

### 3.6 关于 v20 分析的修正

v20 分析认为冲突是 Mio ↔ 微信优化的"双重 titleView Ping-pong"。这个分析有两个错误：

1. **微信优化的 push hook 不碰 titleView**（已通过反编译代码证实）
2. 当 Mio 和微信优化都加载时，ViewWillAppear 中的确存在两次 titleView 替换（Mio 先设置 → 微信优化再替换），但**这种替换发生在稳定的生命周期中**，与崩溃发生在 CoreAnimation 渲染链路中不符。

真正的冲突三方是：**我们的 push hook titleView 干扰 + WCRefine 导航钩子 + 黄白助手渲染钩子**。

---

## 四、修复方案（v22）：三步走

### 第一步：移除 pushViewController hook 中的 titleView 操作

[ChatTopBarHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m#L135-L164) 的 `hook_pushViewController` 函数中删除 titleView 相关代码，或直接删除整个 hook。

### 第二步：移除所有 removeFromSuperview 调用

删除 [ChatTopBarHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m#L79) 和 [ChatTopBarHook.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m#L160) 两处。

### 第三步（如果仍崩溃）：检查 MioChatAvatarTitleView 的布局行为

如果前两步后仍崩溃，问题出在 MioChatAvatarTitleView 自身——与微信优化的 CSChatAvatarTitleView 在布局/渲染行为上有差异。需要对比两者的 `layoutSubviews`、`updateAvatars`、`setupSubviews` 实现，排查是否有触发额外渲染路径的逻辑。

---

## 五、完整修改清单（v22 最小改动版）

### ChatTopBarHook.m

```objc
// 删除 hook_pushViewController 函数（L135-164 整个函数）
// 删除 _orig_UINavigationController_push 变量声明（L13）
// 删除 install 中的 UINavigationController hook 注册（L185-192）
// 删除 viewDidLoad 中的 removeFromSuperview 调用（L79，保留 setTitleView:）
// viewWillAppear: 不需要改动（它已经不调 removeFromSuperview）
```

修改后代码结构：
```objc
// Hook: viewDidLoad
static void hook_viewDidLoad(id self, SEL _cmd) {
    ((void (*)(id, SEL))_orig_BaseMsgContentVC_viewDidLoad)(self, _cmd);
    // ... 原有逻辑 ...
    // [[self navigationItem].titleView removeFromSuperview];  ← 删除这行
    [[self navigationItem] setTitleView:view];  // ← 保留
    [view layoutSubviews];
    [view release];
}

// Hook: viewWillAppear: — 不变

// Install — 只注册 viewDidLoad + viewWillAppear:
+ (void)install {
    Class cls = objc_getClass("BaseMsgContentViewController");
    if (cls) {
        MSHookMessageEx(cls, @selector(viewDidLoad), ...);
        MSHookMessageEx(cls, @selector(viewWillAppear:), ...);
    }
    // 不再注册 UINavigationController hook
}
```

---

## 六、与微信优化的完整对齐状态

| 维度 | 微信优化 | Mio v22 | 对齐？ |
|------|---------|---------|--------|
| 在 push 动画中修改 titleView | ❌ | ❌ (移除) | ✅ |
| 调用 removeFromSuperview | ❌ | ❌ (移除) | ✅ |
| viewDidLoad 中 setTitleView: | ✅ | ✅ | ✅ |
| viewWillAppear 中 setTitleView: | ✅ | ✅ | ✅ |
| isKindOfClass 守卫 | ✅ | ✅ | ✅ |
| updateAvatars + layout 调用 | ✅ | ✅ | ✅ |
| 使用 associatedObject 保存原始 titleView | ✅ | ✅ | ✅ |
| 黑名单检查 | ✅ | ✅ | ✅ |

---

## 七、如果 v22 仍然崩溃

则需要深入对比 MioChatAvatarTitleView 和 CSChatAvatarTitleView 的实现差异。重点检查：

1. `setupSubviews` 中子视图的初始化和添加顺序
2. `layoutSubviews` 中的 frame 计算逻辑
3. `updateAvatars` 中的网络/图片加载方式（同步 vs 异步）
4. 手势识别器的 target-action 设置
5. dealloc 中的清理逻辑

---

## 八、历史版本修正汇总

| 版本 | 修正内容 | 结果 |
|------|---------|------|
| v9 | objc_msgSendSuper → [super viewDidLoad] | 修复 MRC crash |
| v10 | iPhone popover 全屏修复 | 修复 |
| v12 | 布局 y 坐标修复 | 修复 |
| v15 | Popover 重写为 UITableView 多 section | 架构对齐 |
| v16 | ASSIGN → RETAIN_NONATOMIC | 修复野指针 |
| v17 | 删除 object_getClass 过滤 | 仍冲突 |
| v18 | 添加 view.window 检查 | 顶栏消失 + 仍闪退 |
| v19 | 类名改名 + UINavigationController hook | 仍闪退 |
| v20 | 检测微信优化 → 共存模式 | **方向错误**（冲突方不是微信优化） |
| v21 | 移除 push hook + removeFromSuperview | **方向正确** |
| **v22** | **复查确认：精简修复方案 + 补充备用排查路径** | **待验证** |

---

## 九、建议

1. **立即**：按 v22 最小改动方案修改代码
2. **测试**：用 Mio + 黄白助手 + WCRefine（不加微信优化）验证是否崩溃
3. **如果仍崩溃**：对比 MioChatAvatarTitleView 和 CSChatAvatarTitleView 的 layoutSubviews/updateAvatars 实现