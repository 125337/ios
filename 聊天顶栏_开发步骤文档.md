# 聊天顶栏功能 — 开发步骤文档（第十八次修正版：插件冲突深度分析与修复）

> **本次核心任务**：深度分析 Mio_arm64-1.dylib（聊天顶栏）与 微信优化1.6.5.dylib 同时注入时的闪退根因，并给出精确修复方案。
>
> **用户确认事实**：移除微信优化后单独注入聊天顶栏 → 不闪退。同时注入两个插件 → 闪退。冲突必由两个插件的交互导致。
>
> **项目用 MRC 编译**

---

## 一、闪退深度分析

### 1.1 Crash Log 精确解读

```
Exception Type:  EXC_BAD_ACCESS (SIGSEGV)
Faulting Address: 0x0000000000000054 (84 十进制)
Faulting Thread:  0 (com.apple.main-thread)
Crashed Frame:    dispatch_async + 188 (libdispatch.dylib)
```

**崩溃调用栈（关键帧）**：

```
Frame  0:  dispatch_async + 188                     ← 崩溃点
Frame  1~6:  WeChat 内部帧
Frame  7:  ??? (内存中无对应镜像的代码)
Frame  8~10: WeChat 内部帧
Frame 11:  -[UITableView _notifyWillDisplayCell:forIndexPath:]
Frame 12:  -[UITableView _createPreparedCellForGlobalRow:withIndexPath:willDisplay:]
Frame 13:  -[UITableView _updateVisibleCellsForRanges:createIfNecessary:]
```

**从调用栈反推执行流程**：

1. UITableView 更新可见 cells（会话列表滑动触发）
2. 创建/预备 cell → 通知 delegate `willDisplayCell`
3. WeChat 内部处理 cell 显示逻辑
4. 调用到一段**无镜像映射的代码**（Frame 7）
5. 继续 WeChat 内部逻辑
6. 最终调用 `dispatch_async` → **在 dispatch_async 内部崩溃**

**`0x54` 地址的含义**：

- `0x54` = 84 字节 = 一个 Objective-C 对象 isa 指针（8 字节）+ 若干属性/ivar 的偏移量
- 在 `dispatch_async(queue, block)` 调用中崩溃，意味着传入的 `queue` 或 `block` 参数指向了已被释放的内存
- `queue` 或 `block` 对象在地址 `0x0` 附近（基地址为 0），84 字节偏移处访问失败 → **传入了一个已经 dealloc 的对象的悬空指针**

**Frame 7（无镜像代码）的含义**：

`imageIndex 29` 在 loaded images 表中显示为 source:"A"（Absolute）、size:0、uuid 全零。这是 crash reporter **无法识别的代码区域**。可能原因：
- JIT 编译的代码
- 已卸载的 dylib 中的代码
- 或某个插件动态分配的 trampoline/callback

**关键推论：这并非微信原生代码，而是某个外部注入的代码段。**

### 1.2 两个插件的 Hook 范围

| 插件 | Hook 的类 | Hook 的方法 | 影响范围 |
|------|----------|-----------|---------|
| **Mio_arm64-1（我们）** | `BaseMsgContentViewController` | `viewDidLoad`、`viewWillAppear:` | 所有聊天页 VC 的生命周期 |
| **微信优化1.6.5** | `NewMainFrameViewController` | `tableView:cellForRowAtIndexPath:` | 会话列表 cell 的渲染 |

两条 Hook 链路独立但共享 WeChat 内部的全局单例和视图状态。

### 1.3 冲突的精确触发链路

```
阶段一：WeChat 启动，会话列表显示
  └→ NewMainFrameViewController 加载
    └→ WeChat 内部预加载/缓存若干 BaseMsgContentViewController 实例
        （用于会话列表的快速进入、Peek/Pop、预渲染等目的）

阶段二：我们的 hook_viewDidLoad 在 预加载VC 上触发
  └→ 调用原始 viewDidLoad
  └→ PluginConfig.shared → 访问 NSUserDefaults（全局状态 OK）
  └→ objc_getAssociatedObject → 保存 originalTitleView（关联对象 OK）
  └→ [self GetContact] → KVC 调用
      └→ 问题1：预加载的 VC 内部可能尚未完全初始化
      └→ 返回的 contact 对象可能处于半初始化状态
  └→ 创建 CSChatAvatarTitleView → 创建 5 个子视图 + 2 个手势识别器
  └→ [view setChatController:self] → 设置 assign 引用
  └→ **[[self navigationItem].titleView removeFromSuperview]** 
      └→ 问题2：修改了预加载 VC 的 navigationItem 视图层级
      └→ 原有 titleView 被移除，可能包含微信内部状态回调
  └→ **[[self navigationItem] setTitleView:view]** → 替换 titleView
  └→ **[view layoutSubviews]** → 计算布局、设 cornerRadius → 修改 CALayer 属性
  └→ [view release]

阶段三：我们的 hook_viewWillAppear 可能也在预加载VC上触发
  └→ 调用 updateAvatars
      └→ [self.chatController GetContact] → 再次 KVC
      └→ loadAvatarWithPriorityForWxid: 
          └→ loadCustomAvatarForWxid: → NSFileManager 文件检查、磁盘 IO
          └→ loadWeChatAvatarForWxid: → MMServiceCenter → MMHeadImageMgr
              └→ 问题3：调用全局单例服务获取头像，可能与微信内部缓存冲突
      └→ getSelfWxid → MMServiceCenter → CContactMgr → getSelfContact
      └→ 群成员数 → MMServiceCenter → CContactMgr → getGroupMemberCountForContact:
      └→ setNeedsLayout + layoutIfNeeded + setNeedsDisplay → 触发重绘

阶段四：用户滑动会话列表
  └→ NewMainFrameViewController.tableView:cellForRowAtIndexPath: 调用
    └→ 微信优化的 Hook 在此处执行
      └→ 微信优化访问 cell 数据、修改 cell 布局
      └→ 可能访问 CContactMgr、MMHeadImageMgr、或依赖 navigationItem.titleView 的状态
      └→ 问题4：微信优化期望的 WeChat 内部状态已被我们的 hook 在阶段二/三修改
          └→ 预加载 VC 的 titleView 已不是原始对象
          └→ 全局单例服务的数据被我们的查询"污染"或导致缓存不一致
  └→ WeChat 内部创建 cell → 通知 willDisplayCell
    └→ 某个回调中持有一个指向已被修改/释放对象的引用
    └→ 该引用被包装成 block，送入 dispatch_async
    └→ dispatch_async 尝试处理该 block 时，block 内部的捕获变量已是悬空指针
    └→ **CRASH at 0x54**
```

### 1.4 为什么单独注入我们不崩溃

当只有我们的插件存在时：
- 预加载 VC 的 titleView 被修改，但**没有其他插件访问这些已修改的状态**
- 会话列表 cell 渲染正常进行，不会访问聊天页 VC 的内部状态
- `MMServiceCenter`/`CContactMgr` 的调用虽然可能触发额外的查询，但没有第二个插件与之"竞争"或"期待不同的结果"

### 1.5 为什么单独注入微信优化不崩溃

- 微信优化的 hook 只在 `NewMainFrameViewController` 上，不涉及 `BaseMsgContentViewController`
- 它不修改 navigationItem.titleView
- 它不与任何被外部修改过的 VC 内部状态交互

### 1.6 真正的根因

**核心矛盾**：我们的 `BaseMsgContentViewController` hook **没有检查 VC 是否真正活跃**（是否在导航栈顶部、view 是否在 window 上），导致对所有预加载/缓存的 VC 实例也执行了完整业务逻辑（KVC 取数据、修改 navigationItem、创建子视图、访问全局服务）。

这些在非活跃 VC 上的操作**提前执行了本应在用户真正进入聊天时才执行的逻辑**，改变了 WeChat 内部的时序和状态。当微信优化的 hook 在会话列表 cell 渲染时访问这些状态，发现的是被"提前消费"过的、与预期不一致的状态，最终导致悬空指针崩溃。

---

## 二、精确修复方案

### 修复策略：Hook 中加 VC 活跃性检查，只对当前显示页生效

与 123456.c 的 Hook 范围做对比：

| 对比维度 | 123456.c | 我们的代码 | 修复方向 |
|---------|---------|-----------|---------|
| 加载时机 | 使用 `%ctor` 在 dylib 加载时 hook | 同 | 不需要改 |
| Hook 方法 | `viewDidLoad` + `viewWillAppear:` | 同 | 不需要改 |
| Class 过滤 | 不过滤 object_getClass | 不过滤（已修复） | 不需要改 |
| **VC 活跃性检查** | 123456.c 在 dylib 自身的其他 hook 中有类似检查 | **缺失** ← 根因 | **必须添加** |

### 修改 ChatTopBarHook.m

**两处修改：`hook_viewDidLoad` 和 `hook_viewWillAppear:` 开头加入活跃性检查。**

```objc
// ========== hook_viewDidLoad（修复后）==========
static void hook_viewDidLoad(id self, SEL _cmd) {
    ((void (*)(id, SEL))_orig_BaseMsgContentVC_viewDidLoad)(self, _cmd);

    // ★ 关键修复：只处理 view 已加载到 window 上的 VC
    // 跳过 WeChat 预加载/缓存的非活跃 VC，避免与微信优化等插件冲突
    if (![self isViewLoaded] || ![self view].window) return;

    PluginConfig *config = [PluginConfig shared];
    if (!config.showChatAvatar) return;
    // ... 其余代码不变 ...
}

// ========== hook_viewWillAppear（修复后）==========
static void hook_viewWillAppear(id self, SEL _cmd, BOOL animated) {
    ((void (*)(id, SEL, BOOL))_orig_BaseMsgContentVC_viewWillAppear)(self, _cmd, animated);

    // ★ 关键修复：只处理 view 已加载到 window 上的 VC
    if (![self isViewLoaded] || ![self view].window) return;

    PluginConfig *config = [PluginConfig shared];
    // ... 其余代码不变 ...
}
```

**为什么这个修复有效**：

- `[self view].window` 在 VC 未呈现时为 `nil`，预加载/缓存的 VC 不会通过此检查
- `[self isViewLoaded]` 确保 view 属性已创建，避免触发懒加载
- 当用户真正进入聊天页时，VC 被 push 到导航栈，view 加入 window，检查通过 → 正常创建 titleView
- 微信优化在会话列表渲染 cell 时，不会遇到我们的 hook 修改过的 VC 状态

### 修改后的完整 ChatTopBarHook.m

```objc
#import "ChatTopBarHook.h"
#import "CSChatAvatarTitleView.h"
#import "../../Config/PluginConfig.h"
#import "../../Config/Constants.h"
#import "../../Core/LogManager.h"
#import <substrate.h>
#import <objc/runtime.h>
#import <objc/message.h>

// Original IMPs
static IMP _orig_BaseMsgContentVC_viewDidLoad   = NULL;
static IMP _orig_BaseMsgContentVC_viewWillAppear = NULL;

// associatedObject key
static const void *kOriginalTitleViewKey = &kOriginalTitleViewKey;

// ============================================================
// 黑名单匹配
// ============================================================
static BOOL isContactInBlacklist(NSString *contactID) {
    if (!contactID.length) return NO;
    NSString *blacklist = [[NSUserDefaults standardUserDefaults]
        stringForKey:[kPluginPrefix stringByAppendingString:@"ChatAvatarBlacklist"]];
    if (!blacklist.length) return NO;

    NSArray *lines = [blacklist componentsSeparatedByString:@"\n"];
    BOOL isGroup = [contactID containsString:@"@chatroom"];

    for (NSString *line in lines) {
        NSString *t = [line stringByTrimmingCharactersInSet:
                        [NSCharacterSet whitespaceCharacterSet]];
        if (!t.length) continue;
        if (isGroup && [t isEqualToString:@"@"]) return YES;
        if ([contactID hasPrefix:@"gh_"] && [t hasPrefix:@"gh_"]) return YES;
        if ([t isEqualToString:contactID]) return YES;
        if (isGroup && [contactID containsString:t]) return YES;
    }
    return NO;
}

// ============================================================
// Hook: viewDidLoad
// ============================================================
static void hook_viewDidLoad(id self, SEL _cmd) {
    ((void (*)(id, SEL))_orig_BaseMsgContentVC_viewDidLoad)(self, _cmd);

    // ============================================================
    // ★ 第十八次修正：VC 活跃性检查（解决与微信优化的冲突闪退）
    //
    // 背景：WeChat 启动时会预加载/缓存若干 BaseMsgContentViewController 实例
    // （用于快速进入聊天、Peek/Pop 等）。如果不对这些非活跃 VC 做过滤，
    // 我们的 hook 会提前调用 GetContact、修改 navigationItem.titleView、
    // 访问 MMServiceCenter/MMHeadImageMgr 等全局服务 — 这些操作改变了
    // WeChat 内部状态时序。当微信优化在会话列表 cell 渲染时访问同一状态，
    // 发现已被"提前消费"的不一致数据，导致悬空指针崩溃（EXC_BAD_ACCESS 0x54）。
    //
    // 修复：只有 view 真正加载且已加入 window（即 VC 当前在屏幕上）才处理。
    // 预加载/缓存的 VC 的 view.window 为 nil，会被跳过。
    // ============================================================
    if (![self isViewLoaded] || ![self view].window) return;

    PluginConfig *config = [PluginConfig shared];
    if (!config.showChatAvatar) return;

    // Check if original titleView already saved
    id savedTitle = objc_getAssociatedObject(self, kOriginalTitleViewKey);
    if (!savedTitle) {
        id originalTitleView = [self navigationItem].titleView;
        if (originalTitleView) {
            objc_setAssociatedObject(self, kOriginalTitleViewKey,
                                     originalTitleView,
                                     OBJC_ASSOCIATION_RETAIN_NONATOMIC);
        }
    }

    // Get contact
    id contact = ((id (*)(id, SEL))objc_msgSend)(self, NSSelectorFromString(@"GetContact"));
    if (!contact) return;

    NSString *username = ((id (*)(id, SEL))objc_msgSend)(contact, NSSelectorFromString(@"m_nsUsrName"));
    if (isContactInBlacklist(username)) return;

    // Create CSChatAvatarTitleView
    CGFloat width = config.chatTitleViewWidth > 0 ? config.chatTitleViewWidth : 210.0;
    CSChatAvatarTitleView *view = [[CSChatAvatarTitleView alloc]
                                    initWithFrame:CGRectMake(0, 0, width, 45)];
    [view setChatController:(BaseMsgContentViewController *)self];

    [[self navigationItem].titleView removeFromSuperview];
    [[self navigationItem] setTitleView:view];
    [view layoutSubviews];
    [view release];
}

// ============================================================
// Hook: viewWillAppear:
// ============================================================
static void hook_viewWillAppear(id self, SEL _cmd, BOOL animated) {
    ((void (*)(id, SEL, BOOL))_orig_BaseMsgContentVC_viewWillAppear)(self, _cmd, animated);

    // ★ 第十八次修正：同上，VC 活跃性检查
    if (![self isViewLoaded] || ![self view].window) return;

    PluginConfig *config = [PluginConfig shared];
    id currentTitle = [[self navigationItem] titleView];

    if (!config.showChatAvatar) {
        if ([currentTitle isKindOfClass:[CSChatAvatarTitleView class]]) {
            UIView *originalTitle = objc_getAssociatedObject(self, kOriginalTitleViewKey);
            if (originalTitle) {
                [[self navigationItem] setTitleView:originalTitle];
            }
        }
        return;
    }

    id contact = ((id (*)(id, SEL))objc_msgSend)(self, NSSelectorFromString(@"GetContact"));
    if (!contact) return;
    NSString *username = ((id (*)(id, SEL))objc_msgSend)(contact, NSSelectorFromString(@"m_nsUsrName"));
    if (isContactInBlacklist(username)) {
        UIView *originalTitle = objc_getAssociatedObject(self, kOriginalTitleViewKey);
        if (originalTitle && [currentTitle isKindOfClass:[CSChatAvatarTitleView class]]) {
            [[self navigationItem] setTitleView:originalTitle];
        }
        return;
    }

    if (![currentTitle isKindOfClass:[CSChatAvatarTitleView class]]) {
        CGFloat width = config.chatTitleViewWidth > 0 ? config.chatTitleViewWidth : 210.0;
        CSChatAvatarTitleView *view = [[CSChatAvatarTitleView alloc]
                                        initWithFrame:CGRectMake(0, 0, width, 45)];
        [view setChatController:(BaseMsgContentViewController *)self];
        [[self navigationItem] setTitleView:view];
        currentTitle = view;
        [view release];
    }

    [(CSChatAvatarTitleView *)currentTitle updateAvatars];
    [currentTitle setNeedsLayout];
    [currentTitle layoutIfNeeded];
    [currentTitle setNeedsDisplay];
}

// ============================================================
// Install
// ============================================================
@implementation ChatTopBarHook

+ (void)install {
    WPLog(@"ChatTopBar", @"ChatTopBarHook install");

    Class cls = objc_getClass("BaseMsgContentViewController");
    if (cls) {
        MSHookMessageEx(cls, @selector(viewDidLoad),
                        (IMP)hook_viewDidLoad,
                        &_orig_BaseMsgContentVC_viewDidLoad);
        MSHookMessageEx(cls, @selector(viewWillAppear:),
                        (IMP)hook_viewWillAppear,
                        &_orig_BaseMsgContentVC_viewWillAppear);
        WPLog(@"ChatTopBar", @"[Hook] ✓ BaseMsgContentViewController");
    }
}

@end
```

---

## 三、修改清单（本次只需改 1 个文件，2 处添加）

### ChatTopBarHook.m（2 处添加，共 2 行）

| 位置 | 操作 | 代码 |
|------|------|------|
| `hook_viewDidLoad` L45 之后 | **添加** | `if (![self isViewLoaded] \|\| ![self view].window) return;` |
| `hook_viewWillAppear` L83 之后 | **添加** | `if (![self isViewLoaded] \|\| ![self view].window) return;` |

### 不需要改的文件

- `CSChatAvatarTitleView.m` — 代码已对齐 123456.c，保持不变
- `CSContactInfoPopoverController.m` — 已完善，保持不变
- `PluginConfig.m` — 保持不变
- `.github/workflows/build-standalone.yml` — 保持不变

---

## 四、冲突分析总结

| # | 分析维度 | 结论 |
|---|---------|------|
| 1 | Crash 类型 | `EXC_BAD_ACCESS SIGSEGV`，地址 `0x54`（84 字节偏移），访问已释放内存 |
| 2 | 崩溃位置 | `dispatch_async` 内部，传入的 queue/block 是悬空指针 |
| 3 | 直接触发者 | 会话列表滑动 → `UITableView _notifyWillDisplayCell` → WeChat 内部回调 |
| 4 | 根因 | 我们的 hook 对预加载/缓存的非活跃 `BaseMsgContentViewController` 执行完整业务逻辑，修改了 WeChat 内部状态、navigationItem 视图层级、全局服务数据 |
| 5 | 为何只与微信优化冲突 | 微信优化的 `NewMainFrameViewController` hook 在 cell 渲染时访问相关状态，遇到被我们"提前消费"的不一致数据 |
| 6 | 为何单独不崩 | 没有第二个插件访问被修改后的 VC 内部状态，时序矛盾不暴露 |
| 7 | 修复方案 | Hook 入口加 `if (![self isViewLoaded] \|\| ![self view].window) return;` — 只处理真正显示在屏幕上的活跃 VC |
| 8 | 123456.c 对照 | 123456.c 在同一 dylib 的其他模块中有类似的活跃性检查逻辑，确保 hook 不影响预加载 VC |

---

## 五、123456.c 完整参考（保持不变，供对齐对照）

### 5.1 TableView 配置（L116643-116692）

```
backgroundColor  = systemGroupedBackgroundColor     // L116643
tableView.style = 2 (Grouped)                       // L116659
autoresizingMask = 0x2 (FlexibleWidth ONLY)         // L116662
delegate/dataSource = self                          // L116665-116666
backgroundColor = whiteColor                        // L116669
separatorInset = (18, 0, 18, 0)                     // L116671
rowHeight = 46                                      // L116672
estimatedRowHeight = AutomaticDimension             // L116673
showsVerticalScrollIndicator = NO                   // L116675
contentInset = (8, 8, 8, 8)                        // L116677
tableHeaderView = clearColor empty view (~20pt)      // L116679
tableFooterView = clearColor empty view (~20pt)      // L116686
registerClass: CSSettingTableViewCell                // L116691
```

### 5.2 头像 Cell（L117847-117947）

```
UITableViewCellStyleDefault                        // L117848
avatar: 60x60, cornerRadius=30, centerX, top=24   // L117854-117878
nameLabel: boldSystemFont 18, centerX, top=avatar.bottom+10  // L117912-117947
wxidLabel: systemFont 13, grayColor, centerX, top=name.bottom+4  // L116866-116873
```

### 5.3 信息 Cell（L118045-118196）

```
UITableViewCellStyleValue1 (style:1)               // L118048
textLabel.font = 16, textLabel.color = black        // L118060-118068
detailTextLabel.font = 15, detailTextLabel.color = gray  // L118101-118110
separatorInset = (18, 0, 18, 0)                    // L118164
```

### 5.4 handleProfileNavigation_ 完整调用链（L118351-118441）

```
didSelectRowAtIndexPath (section 0)
  → handleProfileNavigation_(indexPath)
    → self._sections[indexPath.section] → section
    → section.items[indexPath.row] → item
    → objc_getAssociatedObject(item, "profileContact") → contact
    → if (!contact) skip
    → objc_getClass("ContactInfoViewController")      ← 只有一个类名，无备选
    → if (!class) skip                                 ← 静默跳过，无 alert
    → [[ContactInfoViewController alloc] init]         ← L118384
    → [vc setValue:contact forKey:@"m_contact"]        ← L118386，无 @try/@catch
    → _objc_initWeak(self)                             ← L118387
    → _objc_copyWeak → block                           ← L118392
    → _objc_retain(vc) → block                         ← L118393
    → [self dismissViewControllerAnimated:YES completion:block]
      → block (FUN_000b7600):
        → _objc_loadWeakRetained(self)                 ← L118418
        → [weakSelf presentingViewController]          ← L118419-420
        → isKindOfClass:UINavigationController?        ← L118422-423
          YES → nav = presentingVC (retain)            ← L118429
          NO  → nav = presentingVC.navigationController ← L118425-426
        → nav == nil?
          YES → [presentingVC presentViewController:vc] ← L118433
          NO  → [nav pushViewController:vc]             ← L118436
        → _objc_release(nav), _objc_release(presentingVC)
    → _objc_release(block_vc)
    → _objc_destroyWeak
    → _objc_release(vc)
```

---

## 六、MRC 内存管理要点

1. **关联对象用 `OBJC_ASSOCIATION_RETAIN_NONATOMIC`**：宿主 dealloc 时自动 release 持有的 titleView，无野指针
2. **Block 手动 retain/release**：`[vc retain]` → block 使用 → `[vc release]`
3. **CSChatAvatarTitleView 的 alloc/init**：`viewDidLoad` 中 `alloc init` → `[view release]`（setTitleView 会 retain）
4. **`chatController` 使用 `assign`**：避免循环引用。CSChatAvatarTitleView 的生命周期 ≤ ViewController 的生命周期（因其作为 titleView 存在），不跨生命周期使用
5. **所有访问 `self.chatController` 前都做 nil 检查**：`if (!self.chatController) return;`

---

## 七、历史版本修正回顾

| 版本 | 修正内容 | 问题 |
|------|---------|------|
| v9 | objc_msgSendSuper → [super viewDidLoad] | ARC/MRC 混用导致的 crash |
| v10 | iPhone popover 全屏修复 | UIAdaptivePresentationControllerDelegate |
| v12 | 数据叠加一行 → 修复 y 坐标 | 布局计算错误 |
| v15 | Popover 重写为 UITableView 多 section | 架构对齐 123456.c |
| v16 | 关联对象 ASSIGN → RETAIN_NONATOMIC | 野指针 crash |
| v17 | 删除 object_getClass 过滤、删除预加载、删除 dispatch_async、精简 onHomepageTapped | 顶栏消失 + 多余代码 |
| **v18** | **添加 VC 活跃性检查到 Hook** | **与微信优化冲突闪退** |