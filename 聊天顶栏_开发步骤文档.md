# 聊天顶栏功能 — 开发步骤文档（第六次深度复查修正版）

> 基于 [聊天顶栏功能_完整实现文档.md](file:///www/wwwroot/ios/聊天顶栏功能_完整实现文档.md) 的 123456.c 反编译分析  
> 本次重点：头像点击反馈、布局偏移、黑名单保存无效  
> **项目用 MRC 编译**

---

## 当前状态概览

```
已正确实现 ✓：
  PluginConfig.h              — 全部属性声明
  PluginConfig.m loadDefaults — 全部属性读取
  ChatTopBarHook.m            — 两个 Hook + isContactInBlacklist
  FeatureModuleRegistry       — ChatTopBarHook 已注册
  SettingChatTopBarController — UI 完整
  silentLoadContactExtInfo:   — init+setM_contact:（对齐 123456.c，已修复）
  presentUserInfoPopoverWithContact: — 已删除 initWithContact: crash（已修复）

仍存在差异 ✗：共 9 个差异（0 致命 + 3 严重 + 4 中 + 2 低）
  1. [严重] 头像点击反馈 MRC 生命周期问题 → 点击没反应
  2. [严重] loadWeChatAvatarForWxid: 用错方法 → 头像不加载
  3. [严重] PluginConfig.m save 缺 18 个属性 → 重启丢失
  4. [中]   layoutSubviews 布局偏移（5 处公式错误）
  5. [中]   loadCustomAvatarForWxid: GIF 不带动画
  6. [中]   presentUserInfoPopoverWithContact: → WCUserInfoViewController 可能不存在
  7. [中]   tap 走势传参不一致（已修复）
  8. [低]   getSelfWxid 多余 CSetting 路径
  9. [低]   loadAvatarWithPriorityForWxid: DefaultHead 位置不当
```

---

## 新问题 #1（严重）：头像点击反馈"点击没反应"

> 涉及文件：[CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) L567-631

### 问题 A：UIImpactFeedbackGenerator MRC 生命周期过短（主要根因）

```objc
// 项目当前代码 L626-631
- (void)playHapticFeedback {
    UIImpactFeedbackGenerator *generator = [[UIImpactFeedbackGenerator alloc]
        initWithStyle:UIImpactFeedbackStyleLight];
    [generator prepare];
    [generator impactOccurred];
    [generator release];  // ★ MRC 下立即销毁！CoreHaptics 引擎来不及执行
}
```

**根因分析**：`impactOccurred` 虽是同步调用，但 CoreHaptics 引擎的触觉脉冲是异步调度的。MRC 下 `release` 立即销毁 generator 后，系统可能取消尚未执行的 haptic 脉冲。

**123456.c 的实现**（L134646）：
```c
void FUN_000c8800(undefined8 param_1) {
    objc_msgSend(param_1, "playHapticFeedback");  // 调 self 的方法，不是直接创建 generator
}
```

123456.c 调用的是 `[self playHapticFeedback]`——可能是 UIView 私有方法或 category。不会立即释放 generator。

**修复（MRC 兼容）**：

```objc
// 方案 1：autorelease 替代 release
- (void)playHapticFeedback {
    UIImpactFeedbackGenerator *generator = [[[UIImpactFeedbackGenerator alloc]
        initWithStyle:UIImpactFeedbackStyleLight] autorelease];  // ★
    [generator prepare];
    [generator impactOccurred];
    // autorelease 保证在 runloop 结束时释放
}

// 方案 2：使用 AudioServices（更底层，即时触发，无生命周期问题）
- (void)playHapticFeedback {
    AudioServicesPlaySystemSound(1519);  // Peek feedback
}
```

### 问题 B：123456.c 的反馈触发条件不同

```objc
// 项目代码 L569：仅当 config.avatarTapFeedback == YES 时触发
if (config.avatarTapFeedback) {
    [self playHapticFeedback];
}
```

```c
// 123456.c L12792-12799：NSUserDefaults key 不存在 或 YES 时都触发
uVar1 = boolForKey(@"com.wechat.enhance.chat.avatar.click.feedback.enabled");
lVar3 = stringForKey(...);  // 读取值
if (lVar3 == 0 || (uVar1 & 1) != 0) {  // key 不存在 OR YES → 触发
    playHapticFeedback(param_1);
    showUserInfoPopoverForOpponent(param_1);
}
```

**问题**：首次安装时 NSUserDefaults 没有 `ChatAvatarTapFeedback` key，`config.avatarTapFeedback` 返回 NO（读取不到），不触发反馈。但 123456.c 在 key 不存在时默认触发。

**修复**：loadDefaults 中设默认值 `YES`（对齐 123456.c 默认行为）：

```objc
// PluginConfig.m readDefaults/loadDefaults 中
_avatarTapFeedback = [d getBoolForKey:KEY(@"AvatarTapFeedback") default:YES];
```

### 问题 C：presentUserInfoPopoverWithContact: 静默失败

```objc
// 项目代码 L597-612
- (void)presentUserInfoPopoverWithContact:(id)contact sourceView:(UIView *)sourceView {
    if (!contact) return;

    Class infoVCClass = objc_getClass("WCUserInfoViewController");
    if (infoVCClass) {
        // ... 初始化
    }
    // ★ 如果 WCUserInfoViewController 不存在，静默无返回
}
```

如果微信版本中 `WCUserInfoViewController` 不存在（类名变化），方法静默返回，用户感觉"没反应"。

**123456.c 的做法**：使用 `CSUserInfoPopoverController`（内部自定义类），或用微信原生方式保证弹出。

**修复**：增加 fallback，用微信的 `NewMessageViewController` 直接跳转聊天信息页：

```objc
- (void)presentUserInfoPopoverWithContact:(id)contact sourceView:(UIView *)sourceView {
    if (!contact) return;

    // 方案 A：WCUserInfoViewController
    Class infoVCClass = objc_getClass("WCUserInfoViewController");
    if (infoVCClass) {
        NSString *wxid = ((id (*)(id, SEL))objc_msgSend)(contact, NSSelectorFromString(@"m_nsUsrName"));
        id infoVC = ((id (*)(Class, SEL))objc_msgSend)(infoVCClass, NSSelectorFromString(@"alloc"));
        infoVC = ((id (*)(id, SEL, id, id))objc_msgSend)(infoVC,
            NSSelectorFromString(@"initWithUsrName:contact:"), wxid, contact);
        if (infoVC) {
            UIViewController *parentVC = [self findViewController];
            if (parentVC && parentVC.navigationController) {
                [parentVC.navigationController pushViewController:infoVC animated:YES];
                [infoVC release];
                return;
            }
            [infoVC release];
        }
    }

    // 方案 B：用微信原生联系人详情页（备选方案，兼容性更好）
    Class contactInfoVC = objc_getClass("ContactInfoViewController");
    if (contactInfoVC) {
        id vc = ((id (*)(Class, SEL))objc_msgSend)(contactInfoVC, NSSelectorFromString(@"alloc"));
        vc = ((id (*)(id, SEL))objc_msgSend)(vc, @selector(init));  // ★ 用 init，不是 initWithContact:
        if (vc && [vc respondsToSelector:NSSelectorFromString(@"setM_contact:")]) {
            ((void (*)(id, SEL, id))objc_msgSend)(vc,
                NSSelectorFromString(@"setM_contact:"), contact);
            UIViewController *parentVC = [self findViewController];
            if (parentVC && parentVC.navigationController) {
                [parentVC.navigationController pushViewController:vc animated:YES];
                [vc release];
            }
        }
    }
}
```

---

## 新问题 #2（严重）：layoutSubviews 布局偏移

> 涉及文件：[CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m) L94-268

### 偏移点总览

| # | Mode | 问题 | 偏移量 |
|---|------|------|--------|
| 1 | 1,4 | avatarY 公式错误 | avatar 偏上 ≈ (nameFontSize/2)px |
| 2 | 5,6 | titleLabel Y 公式错误 | name 贴顶/贴底，不居中于 avatar |
| 3 | 5 | avatar↔name 左右排反 | 左右镜像 |
| 4 | ALL | `applyPositionOffset` 偏移整个 frame vs 123456.c 只偏移 titleLabel | 整体平移 |
| 5 | ALL | titleLabel Y 通用公式错误 | 各 mode 均有偏差 |

### 偏移 1：mode 1、4 的 avatarY 公式

```objc
// 项目 L111-116
CGFloat avatarY;
if (mode == 1 || mode == 2 || mode == 3 || mode == 7) {
    avatarY = (totalH - avatarSize) * 0.5;      // ← 纯居中
} else {
    CGFloat contentH = avatarSize + 2 + nameFontSize + 1;
    avatarY = (totalH - contentH) * 0.5;         // ← 考虑 name 高度
}
```

**问题**：mode 1（显示自己头像）和 mode 4（对方头像+名字在下方）不应该用纯居中公式。因为这两模式都有 name 在 avatar 下方展示，需要给 name 留空间。

**123456.c 公式**（L11190-11199）：

```c
if (uVar5 - 4 < 4 || uVar5 == 1) {  // modes 1,4,5,6,7
    if (mode > 4) {  // modes 5,6,7 → avatar 纯居中
        bVar1 = true;
        dVar18 = (totalH - avatarSize) / 2;
    }
    // modes 1,4 → avatar 偏上，给 name 留空间
    dVar18 = (totalH - (avatarSize + 1 + 1 + nameFontSize + 1)) / 2 + 1;
}
```

| Mode | 项目 avatarY | 123456.c avatarY | 差异 |
|------|-------------|------------------|------|
| 1 | (H-S)/2 | (H-(S+N+3))/2+1 | 偏下了 ≈N/2 |
| 4 | (H-S)/2 | (H-(S+N+3))/2+1 | 偏下了 ≈N/2 |
| 5,6 | (H-(S+N+3))/2 | (H-S)/2 | 偏上了 ≈N/2 |

**修复**：

```objc
CGFloat avatarY;
if (mode == 1 || mode == 4) {
    // ★ modes 1,4: avatar 偏上给 name 留空间（对齐 123456.c）
    CGFloat contentH = avatarSize + 3 + nameFontSize;  // 123456.c: 1+1+1=3 个间隔
    avatarY = (totalH - contentH) * 0.5 + 1;
} else {
    // modes 0,2,3,5,6,7: avatar 纯居中
    avatarY = (totalH - avatarSize) * 0.5;
}
```

### 偏移 2：mode 5、6 titleLabel Y 公式

```objc
// 项目 mode 5 L161：name 居中于整个 view
self.titleLabel.frame = CGRectMake(sepX, (totalH - nameFontSize) * 0.5, nameW, nameFontSize);

// 项目 mode 6 L177：同样居中于 view
self.titleLabel.frame = CGRectMake(avatarSize + halfSpacing, (totalH - nameFontSize) * 0.5, ...);
```

**123456.c 公式**（L11610）：
```
titleLabel.Y = (avatarSize - nameFontSize) / 2 + avatarY - namePositionOffset
```

**通俗解释**：123456.c 让 name 文字相对于 **avatar 中心** 对齐，而项目把 name 放在 **view 中心**。当 avatar 不是 view 中心时（如 mode 5），name 就会偏移。

**修复**：所有 titleLabel 的 Y 公式改为相对 avatar 居中：

```objc
CGFloat nameY = avatarY + (avatarSize - nameFontSize) * 0.5 - config.chatNicknameOffsetY;
```

### 偏移 3：mode 5 avatar 和 name 左右排反

```objc
// 项目 mode 5：avatar 在左，name 在右
self.leftAvatarView.frame = CGRectMake(0, avatarY, avatarSize, avatarSize);
self.titleLabel.frame = CGRectMake(sepX, nameY, nameW, nameFontSize);
```

**123456.c mode 5**（L11292-11308）：
```
// name 在左，avatar 在右！
leftAvatar.frame.x = nameWidth + (totalW - (avatarSize + nameWidth + 8)) / 2 + 8
titleLabel.frame.x = (totalW - (avatarSize + nameWidth + 8)) / 2 + horizontalOffset
titleLabel.textAlignment = NSTextAlignmentLeft
```

**修复 mode 5**：

```objc
if (mode == 5) {
    // ⭐ name 在左，avatar 在右（对齐 123456.c）
    self.leftAvatarView.frame = CGRectMake(
        nameWidth + (totalW - avatarSize - nameWidth - 8) * 0.5 + 8,
        avatarY, avatarSize, avatarSize);
    self.titleLabel.frame = CGRectMake(
        (totalW - avatarSize - nameWidth - 8) * 0.5 + config.chatNicknameOffsetX,
        nameY, nameWidth, nameFontSize);
    self.titleLabel.textAlignment = NSTextAlignmentLeft;
}
```

### 偏移 4：applyPositionOffset 偏移范围错

```objc
// 项目 L537-543：偏移整个 titleView 的 frame
- (void)applyPositionOffset {
    CGRect f = self.frame;
    f.origin.x += config.chatHorizontalOffset;
    f.origin.y += config.chatVerticalOffset;
    self.frame = f;
}
```

**123456.c** 用的 `com.wechat.enhance.chat.name.position.offset` 和 `com.wechat.enhance.chat.name.horizontal.offset` 仅偏移 **titleLabel**，不偏移头像。

```c
// L11599-11646：仅对 titleLabel 施加 offset
titleLabel.frame.x = baseX + nameHorizontalOffset;
titleLabel.frame.y = (avatarSize - nameFontSize) / 2 + avatarY - namePositionOffset;
```

**修复**：

```objc
- (void)applyPositionOffset {
    // ★ 仅偏移 titleLabel，不对 frame 整体偏移（对齐 123456.c）
    PluginConfig *config = [PluginConfig shared];
    CGRect f = self.titleLabel.frame;
    f.origin.x += config.chatNicknameOffsetX;
    f.origin.y -= config.chatNicknameOffsetY;
    self.titleLabel.frame = f;
}
```

### 偏移 5：titleLabel Y 通用公式错误

| Mode | 项目 nameY | 123456.c nameY | 关系 |
|------|-----------|----------------|------|
| 0,6 | (H-N)/2 | (S-N)/2 + Y_av | = (H-N)/2 当 Y_av=(H-S)/2 ✓ 巧合正确 |
| 4  | 不存在？| (S-N)/2 + Y_av - offset | ✗ |
| 5  | (H-N)/2 | (S-N)/2 + Y_av - offset | ✗ |

通用修复公式（所有 mode）：

```objc
// ★ 所有 mode 统一用此公式
CGFloat nameY = avatarY + (avatarSize - nameFontSize) * 0.5 - config.chatNicknameOffsetY;
```

---

## 新问题 #3（严重）：PluginConfig.m save — "很多都保存无效"

> 涉及文件：[PluginConfig.m](file:///www/wwwroot/ios/WeChatPlugin/Config/PluginConfig.m)

### 根因

`[config save]` 方法中没有写入任何聊天顶栏属性到 NSUserDefaults。虽然 `loadDefaults` 会读取默认值，但 `save` 不写 → 任何修改永远不会持久化 → 重启后所有设置还原为默认值。

**黑名单保存的额外说明**：黑名单编辑器（ChatTopBarBlacklistEditorVC）通过 NSUserDefaults 直接写入 `WCP_ChatAvatarBlacklist` key，且与 `isContactInBlacklist` 查询 key 一致，理论可正常工作。但如果 PluginConfig.save 没有正确同步，其他属性（模式、尺寸、偏移等）重启后全部丢失。

### 修复（追加到 save 方法末尾）

```objc
// ========== 聊天顶栏（必须写入，否则重启丢失）==========
[d setBool:_showChatAvatar forKey:KEY(@"ShowChatAvatar")];
[d setBool:_avatarTapFeedback forKey:KEY(@"AvatarTapFeedback")];
[d setBool:_showAddTime forKey:KEY(@"ShowAddTime")];
[d setBool:_showGroupMemberCount forKey:KEY(@"ShowGroupMemberCount")];
[d setInteger:_chatDisplayMode forKey:KEY(@"ChatDisplayMode")];
[d setFloat:_chatAvatarSize forKey:KEY(@"ChatAvatarSize")];
[d setFloat:_chatAvatarSpacing forKey:KEY(@"ChatAvatarSpacing")];
[d setFloat:_chatAvatarCornerRadius forKey:KEY(@"ChatAvatarCornerRadius")];
[d setFloat:_chatNicknameFontSize forKey:KEY(@"ChatNicknameFontSize")];
[d setFloat:_chatSeparatorSize forKey:KEY(@"ChatSeparatorSize")];
[d setFloat:_chatTitleViewWidth forKey:KEY(@"ChatTitleViewWidth")];
[d setFloat:_chatVerticalOffset forKey:KEY(@"ChatVerticalOffset")];
[d setFloat:_chatHorizontalOffset forKey:KEY(@"ChatHorizontalOffset")];
[d setFloat:_chatNicknameOffsetY forKey:KEY(@"ChatNicknameOffsetY")];
[d setFloat:_chatNicknameOffsetX forKey:KEY(@"ChatNicknameOffsetX")];
if (_chatSeparatorText) {
    [d setObject:_chatSeparatorText forKey:KEY(@"ChatSeparatorText")];
}
if (_chatGroupMemberCountSuffix) {
    [d setObject:_chatGroupMemberCountSuffix forKey:KEY(@"ChatGroupMemberCountSuffix")];
}
if (_chatAddTimeSuffixFormat) {
    [d setObject:_chatAddTimeSuffixFormat forKey:KEY(@"ChatAddTimeSuffixFormat")];
}
```

### 额外说明：黑名单 editor 的 saveAction

[ChatTopBarBlacklistEditorVC.m L174-180](file:///www/wwwroot/ios/WeChatPlugin/Settings/Controllers/ChatTopBarBlacklistEditorVC.m#L174-L180) 保存 key 为 `WCP_ChatAvatarBlacklist`，与 [ChatTopBarHook.m L22-23](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/ChatTopBarHook.m#L22-L23) 的 `isContactInBlacklist` 读取 key 一致。黑名单自身保存逻辑正确，但由于直接写 NSUserDefaults 且调用 `synchronize`，即使 PluginConfig.save 没写黑名单 key，黑名单仍可正常工作。

但如果你发现黑名单保存后不生效，检查步骤：
1. 确认 Contact 的 `m_nsUsrName` 与黑名单中录入的 wxid **完全一致**（含大小写）
2. 确认编辑器中每行一个 wxid，没有多余空格
3. 确认 wxid 包含 `@chatroom` 后缀（群聊）

---

## 旧问题：loadWeChatAvatarForWxid: 用错方法 → 头像不加载

（上一次已分析，本次确认仍未修复）

[L434-L451](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L434-L451)：
- 项目用 `CContactMgr.getContactByName:` → `m_avatarImage`（内存缓存，大概率 nil）
- 123456.c 用 `MMHeadImageMgr.getHeadImage:withCategory:`（实际获取头像）

```objc
// 修复后
- (UIImage *)loadWeChatAvatarForWxid:(NSString *)wxid {
    Class serviceCenter = objc_getClass("MMServiceCenter");
    if (!serviceCenter) return nil;
    id center = ((id (*)(Class, SEL))objc_msgSend)(serviceCenter, NSSelectorFromString(@"defaultCenter"));
    id headImageMgr = ((id (*)(id, SEL, Class))objc_msgSend)(center,
        NSSelectorFromString(@"getService:"), objc_getClass("MMHeadImageMgr"));
    if (!headImageMgr) return nil;
    return ((UIImage *(*)(id, SEL, id, id))objc_msgSend)(headImageMgr,
        NSSelectorFromString(@"getHeadImage:withCategory:"), wxid, @0);
}
```

---

## 完整修复清单（按优先级）

| # | 文件 | 行 | 问题 | 严重度 | MRC注意 |
|---|------|----|------|--------|---------|
| 1 | CSChatAvatarTitleView.m | L441 | loadWeChatAvatarForWxid: → MMHeadImageMgr | **严重** | 无 |
| 2 | CSChatAvatarTitleView.m | L626-631 | playHapticFeedback → autorelease | **严重** | ⚠️ MRC |
| 3 | PluginConfig.m | save | 追加 18 个属性持久化 | **严重** | 无 |
| 4 | CSChatAvatarTitleView.m | L109-116 | avatarY 公式区分 mode 1,4 vs 5,6 | **中** | 无 |
| 5 | CSChatAvatarTitleView.m | L135-162 | mode 5 左右排反 | **中** | 无 |
| 6 | CSChatAvatarTitleView.m | L161/L177 | titleLabel Y 公式改相对 avatar 居中 | **中** | 无 |
| 7 | CSChatAvatarTitleView.m | L537-543 | applyPositionOffset 仅偏移 titleLabel | **中** | 无 |
| 8 | CSChatAvatarTitleView.m | L597-612 | presentUserInfo...增加 ContactInfoViewController fallback | **中** | 无 |
| 9 | CSChatAvatarTitleView.m | L411-432 | GIF → createAnimatedImageFromGIFData | **中** | 无 |
| 10 | CSChatAvatarTitleView.m | L368-392 | getSelfWxid 移除 CSetting 路径 | **低** | 无 |
| 11 | CSChatAvatarTitleView.m | L398-409 | DefaultHead 移到 updateAvatars | **低** | 无 |

---

## MRC 特别注意事项

项目使用 MRC 编译，以上修复中涉及以下关键点：

```objc
// ❌ 错误：立即 release → 对象销毁，后续访问 crash/haptic 不触发
UIImpactFeedbackGenerator *g = [[X alloc] init];
[g doSomething];
[g release];

// ✅ 正确：autorelease → runloop 结束时释放
UIImpactFeedbackGenerator *g = [[[X alloc] init] autorelease];
[g doSomething];

// ✅ 正确：手动配对 retain/release
id x = [obj retain];  // retain count +1
[self setProperty:x];
[x release];          // retain count -1（property 持有）
```

`presentUserInfoPopoverWithContact:` 中 `init + setM_contact:` + push 后 `[vc release]` 是正确的 MRC 释放模式。

---

## 布局对比速查表

```
Mode 0: 对方头像(左) + 昵称(右)
  项目: avatar=(0, Y_av), name=(S+h/2, Y_name)
  123456.c: avatar=(0, Y_av), name=(S+h/2, Y_name) ✓ 一致

Mode 1: 自己头像(中)、无昵称
  项目: avatar=(centerX, (H-S)/2)        ← avatarY 偏下
  123456.c: avatar=(centerX, (H-(S+N+3))/2+1)  ← avatar 偏上给 name 留位

Mode 2: 双方头像(中)、昵称在下方
  123456.c has separator/icon between avatars → dual layout
  项目: dual layout with optional separator ✓ 一致

Mode 3: 对方头像(中)、无昵称
  项目: avatar=(centerX, (H-S)/2) ✓ 正确

Mode 4: 对方头像(中)、昵称在下方
  项目: avatar=(centerX, (H-S)/2)        ← avatarY 偏下
  123456.c: avatar=(centerX, (H-(S+N+3))/2+1)  ← 正确

Mode 5: 昵称(左) + 对方头像(右)
  项目: avatar(左) + name(右)            ← 左右排反！
  123456.c: name(左) + avatar(右)         ← 正确

Mode 6: 对方头像(左) + 昵称(右)
  项目: ✓ 正确（同 123456.c）

Mode 7: 双方头像重叠模式
  项目: leftAvatar(0,Y), rightAvatar(offset, Y+S-small+(S*0.15))
  123456.c: leftAvatar(0,Y), rightAvatar(S*0.3, Y+S-small+(S*0.15))
  ✓ 基本一致（double 精度微小差异忽略）
```