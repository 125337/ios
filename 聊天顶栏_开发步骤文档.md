# 聊天顶栏功能 — 开发步骤文档（第二十五次修正版：完全对齐微信优化调用时序）

> **本次背景**：v24 删除 layoutIfNeeded 后仍然闪退。原因不在某个具体调用，而在 **调用时序**——我们在 viewWillAppear（视图过渡期间）首次加载头像，而微信优化在 viewDidLoad（视图未出现时）就完成了预加载。
>
> **核心发现**：微信优化的 `updateAvatars` 在 **viewDidLoad 和 viewWillAppear 各调用一次**。viewDidLoad 预加载头图到 MMHeadImageMgr 缓存（+ 黄白助手 m_nsNickName hook 存入 HBConfigUtil），viewWillAppear 时已是 no-op 路径。我们在 viewDidLoad 中不调 updateAvatars，viewWillAppear 中才首次加载——文件 I/O + m_nsNickName hook + 头图加载全在过渡期发生。
>
> **项目用 MRC 编译**

---

## 一、微信优化 vs Mio v25：逐行精确对比

### 1.1 viewDidLoad 对比

| 步骤 | 微信优化 (FUN_00014c74) | Mio v25 |
|------|------------------------|---------|
| 调原始 viewDidLoad | ✅ | ✅ |
| 保存原始 titleView → associatedObject | ✅ | ✅ |
| 检查开关 | ✅ chat_avatar_display_enabled | ✅ showChatAvatar |
| 检查黑名单 | ✅ | ✅ |
| 创建 TitleView | ✅ CSChatAvatarTitleView | ✅ MioChatAvatarTitleView |
| setChatController: | ✅ | ✅ |
| setTitleView: | ✅ | ✅ |
| **updateAvatars** | **✅ L13343** | **✅ L79（新增）** |
| removeFromSuperview | ❌ | ❌ | 已对齐 |
| layoutSubviews | ❌ | ❌ | 与 微信优化 一致 |

### 1.2 viewWillAppear 对比

| 步骤 | 微信优化 (FUN_00014ed0) | Mio v25 |
|------|------------------------|---------|
| 调原始 | ✅ | ✅ |
| disable 时恢复原始 titleView | ✅ | ✅ |
| isKindOfClass 守卫 | ✅ | ✅ |
| 创建 View → setTitleView: | ✅ (缺失时) | ✅ (缺失时) |
| **loadSeparatorIcon** | **✅ FUN_000c7700** | **✅ L123（新增）** |
| **loadSeparatorText** | **✅ FUN_000c7720** | **✅ L124（新增）** |
| **setNeedsLayout** | **✅ FUN_000d0c20** | **✅ L125** |
| **updateAvatars** | **✅ FUN_000d0ba0** | **✅ L126** |
| **setNeedsDisplay** | **✅ FUN_000c2d20** | **✅ L127** |

**调用序列完全一致**：`loadSeparatorIcon → loadSeparatorText → setNeedsLayout → updateAvatars → setNeedsDisplay`

---

## 二、根因解释

### 2.1 为什么 v24 仍然崩溃

v24 虽然在 viewWillAppear 中去掉了 `layoutIfNeeded`，但崩溃仍在——因为触发点不是 layout，而是 **updateAvatars 内部的首次加载**：

```
viewWillAppear (过渡动画进行中)
  → updateAvatars (首次调用)
    → m_nsNickName → 黄白助手 hook 激活，更新 HBConfigUtil
    → MMHeadImageMgr 首次加载头像 (同步网络/磁盘I/O)
    → GIF 文件读取 + 解码
    → loadSeparatorIcon/loadSeparatorGIF (读取 NSUserDefaults)
    → 设置 image/text → 改变子视图内容 → 导航栏感知变化
      → 导航栏过渡态被扰动
        → CoreAnimation commit → WCRefine → 黄白助手 → 微信优化 trampoline → crash
```

### 2.2 为什么微信优化不崩溃

```
viewDidLoad (视图未在屏幕，无过渡)
  → updateAvatars (首次调用)
    → MMHeadImageMgr 加载头像 → 存入缓存 ✅
    → m_nsNickName → 黄白助手 hook → 存入 HBConfigUtil ✅
    → 导航栏不在过渡态，无扰动

... push 动画完成，导航栏稳定 ...

viewWillAppear (过渡已完成，导航栏稳定)
  → loadSeparatorIcon (从内存读，极快)
  → loadSeparatorText (从内存读，极快)
  → setNeedsLayout (异步)
  → updateAvatars (第二次 → MMHeadImageMgr 命中缓存，near-no-op)
  → setNeedsDisplay (异步)
```

**微信优化的 updateAvatars 在 viewDidLoad 预加载完成所有 I/O 和第三方 hook 交互，viewWillAppear 时走快速缓存路径，过渡期间不发生实质性变化。**

---

## 三、v25 修改内容

### viewDidLoad L78-80（新增 updateAvatars）
```objc
[[self navigationItem] setTitleView:view];
[(MioChatAvatarTitleView *)view updateAvatars];  // ← 新增：预加载到缓存
[view release];
```

### viewWillAppear L123-127（重排调用序列）
```objc
[(MioChatAvatarTitleView *)currentTitle loadSeparatorIcon];
[(MioChatAvatarTitleView *)currentTitle loadSeparatorText];
[currentTitle setNeedsLayout];
[(MioChatAvatarTitleView *)currentTitle updateAvatars];
[currentTitle setNeedsDisplay];
```

---

## 四、历史版本修正汇总

| 版本 | 修正内容 | 结果 |
|------|---------|------|
| v9-v19 | 各种尝试 | 仍崩溃 |
| v20 | 检测微信优化 → 共存模式 | **方向错误** |
| v21 | 移除 push hook + removeFromSuperview | **方向正确** |
| v22 | 深度复查 + 双dylib 根因 | 双dylib 仍加载导致误判 |
| v23 | 单dylib 仍崩 → MioChatAvatarTitleView 差异 | 仍崩溃 |
| v24 | 删除 layoutIfNeeded + layoutSubviews | 仍崩溃（首次加载仍在过渡期） |
| **v25** | **完全对齐微信优化调用时序：viewDidLoad 预加载 updateAvatars → viewWillAppear 缓存路径** | **待验证** |