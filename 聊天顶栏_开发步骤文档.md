# 聊天顶栏功能 — 开发步骤文档（第十一次修正版）

> 基于 [聊天顶栏功能_完整实现文档.md](file:///www/wwwroot/ios/聊天顶栏功能_完整实现文档.md) 的 123456.c 反编译分析  
> 本次重点：**定位到项目实际代码** — crash 根源在 [CSContactInfoPopoverController.m L50](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m#L50)，第九次文档的 `objc_msgSendSuper` 错误被原样抄入项目  
> **项目用 MRC 编译**

---

## Crash 日志分析

### 基本信息

| 项目 | 值 |
|------|-----|
| 异常类型 | `EXC_BAD_ACCESS` (SIGSEGV) |
| 崩溃地址 | `0x0000000000000010` — 非法内存访问 |
| 崩溃线程 | 主线程 (Thread 0) |
| 崩溃位置 | `objc_msgSendSuper2 + 24` → 来自 `Mio_arm64-1.dylib + 38960` (项目的 viewDidLoad) |
| 微信版本 | 8.0.60 |
| 系统版本 | iPhone OS 17.2.1 |

### 调用栈还原

```
objc_msgSendSuper2                    ← ★ 崩溃点：访问地址 0x10 时 SIGSEGV
  ↑ 被调用自
image[17]+38960 (Mio_arm64-1.dylib)  ← CSContactInfoPopoverController 的 viewDidLoad
  ↑ 被调用自
-[UIViewController _sendViewDidLoad...]
  ↑
-[UIViewController loadViewIfRequired]
  ↑
-[UIViewController view]              ← presentViewController 触发 loadView → viewDidLoad
  ↑
-[UIViewController _setPresentationController:]
  ↑
-[UIViewController _presentViewController:...]
```

### 根因分析（定位到实际项目代码）

第九次文档中的错误代码被原样复制到了项目 [CSContactInfoPopoverController.m L50](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m#L50)：

```objc
// ★ 项目 L49-50 — 这是真正的 crash 行
- (void)viewDidLoad {
    ((void (*)(id, SEL))objc_msgSendSuper)(self, @selector(viewDidLoad));  // ← CRASH!
```

`objc_msgSendSuper` 的签名是：
```c
id objc_msgSendSuper(struct objc_super *super, SEL op, ...)
//                     ↑ 第一个参数是指向结构体的指针，不是 receiver！
```

传 `self` 进去后，`objc_msgSendSuper` 把 `self` 对象的**内存**当成 `struct objc_super` 来读：
- `super.receiver` = 读 `self` 的前 8 字节 = `self` 的 **isa 指针**（类对象，不是实例）
- `super.super_class` = 读 `self` 的 isa 之后的 8 字节 = 第一个 IVAR 的随机值

类对象 + 随机超类 → 调用 `viewDidLoad` → `EXC_BAD_ACCESS` 地址 `0x10`。

**注意**：同一文件 `[super init]` 写对了（[L28](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m#L28)），只有 `[super viewDidLoad]` 抄了文档错误代码。

### 修复方案

**只需改 1 行**（[L50](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m#L50)）：

```objc
// 改前（crash）：
((void (*)(id, SEL))objc_msgSendSuper)(self, @selector(viewDidLoad));
// 改后（正确）：
[super viewDidLoad];
```

`[super viewDidLoad]` 编译器自动生成正确的 `objc_msgSendSuper2` 调用，和 123456.c 的行为一致。

### 额外需要补上的 2 个点

#### 1. iPhone 上 popover 会变全屏

当前 [presentUserInfoPopoverWithContact:sourceView:](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L620-L631) 设置了 `modalPresentationStyle = UIModalPresentationPopover`，但没有设 delegate。iPhone 上 UIKit 会默认把 popover 自动适配为全屏 modal。

需要在三个文件各改/加：

[CSContactInfoPopoverController.h](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.h#L4) — 加上协议 `<UIAdaptivePresentationControllerDelegate>`

[CSContactInfoPopoverController.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m) — 加方法（123456.c L118766 返回 -1）：
```objc
- (UIModalPresentationStyle)adaptivePresentationStyleForPresentationController:
        (UIPresentationController *)controller {
    return UIModalPresentationNone;
}
```

[CSChatAvatarTitleView.m L631](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m#L631) 后加一行：
```objc
popPC.delegate = popover;
```

#### 2. 123456.c 的 super 调用方式（仅参考）

123456.c L116584 构建栈上的 `struct objc_super2` 然后调 `_objc_msgSendSuper2(&local_30, "viewDidLoad")`。项目用 `[super viewDidLoad]` 编译器生成完全相同的调用，不需要手写。

---

## 信息行数据对照表（contact KVC → 123456.c 参考）

| 显示字段 | contact KVC key | 123456.c 方法 | 兜底值 |
|---------|----------------|--------------|--------|
| 昵称 | `m_nsNickName` | — | — |
| 类型 | 判断 wxid | `getChatTypeForID_:` L117091 | "私聊" |
| 微信 | `m_nsUsrName` | 直接取 | — |
| 备注 | `m_nsRemark` → `m_nsRemarkName` | `getRemarkForContact_:` L117120 | "未设置" |
| 性别 | `m_uiSex` | `getGenderForContact_:` L117158 | "未知" |
| 地区 | `m_nsProvince` + `m_nsCity` | `getLocationForContact_:` L117196 | "未设置" |
| 签名 | `m_nsSignature` | `getSignatureForContact_:` L117247 | "未设置" |

---

## 本次修复清单（不创建新文件，仅改现有代码）

### 文件 1：[CSContactInfoPopoverController.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.m)

**改 1 行**（L50）：
```objc
// 删掉这行：
    ((void (*)(id, SEL))objc_msgSendSuper)(self, @selector(viewDidLoad));
// 替换为：
    [super viewDidLoad];
```

**加 1 个方法**（在 `@implementation` 内任意位置，建议放在 `dealloc` 后面）：
```objc
- (UIModalPresentationStyle)adaptivePresentationStyleForPresentationController:
        (UIPresentationController *)controller {
    return UIModalPresentationNone;
}
```

### 文件 2：[CSContactInfoPopoverController.h](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSContactInfoPopoverController.h)

**改 1 行**（L4）：
```objc
@interface CSContactInfoPopoverController : UIViewController <UIAdaptivePresentationControllerDelegate>
```

### 文件 3：[CSChatAvatarTitleView.m](file:///www/wwwroot/ios/WeChatPlugin/Modules/ChatTopBar/CSChatAvatarTitleView.m)

**加 1 行**（在 L631 `popPC.backgroundColor = ...` 之后）：
```objc
popPC.delegate = popover;
```

---

## 先前回顾（已修复项状态）

| # | 内容 | 状态 |
|---|------|------|
| 1 | avatarY 公式：mode 1,4 偏上 | ✓ |
| 2 | nameY = avatarY + (S-N)/2 - offsetY | ✓ |
| 3 | mode 5：name(左) + avatar(右) | ✓ |
| 4 | applyPositionOffset：仅 titleLabel | ✓ |
| 5 | loadWeChatAvatarForWxid: → MMHeadImageMgr | ✓ |
| 6 | createAnimatedImageFromGIFData | ✓ |
| 7 | getSelfWxid：仅 CContactMgr | ✓ |
| 8 | loadAvatarWithPriorityForWxid: 无 DefaultHead | ✓ |
| 9 | presentUserInfo... ContactInfoViewController fallback | ✓ |
| 10 | PluginConfig.save 18 个属性 | ✓ |
| 11 | PluginConfig.loadDefaults 全部读取 | ✓ |
| 12 | avatarTapFeedback 默认 YES | ✓ |
| 13 | UI 已实现（CSContactInfoPopoverController 全部方法已写）| ✓ |
| 14 | **修复 crash：L50 `objc_msgSendSuper` → `[super viewDidLoad]`** | **P0** |
| 15 | **加 `adaptivePresentationStyle... → UIModalPresentationNone`** | **P0** |
| 16 | **加 `popPC.delegate = popover`** | **P0** |
| 17 | **加 `<UIAdaptivePresentationControllerDelegate>` 协议** | **P0** |

---

## 当前项目文件 → 修改对照速查

| 文件 | 行号 | 当前代码 | 改为 |
|------|------|---------|------|
| `CSContactInfoPopoverController.m` | L50 | `objc_msgSendSuper(self, @selector(viewDidLoad))` | `[super viewDidLoad]` |
| `CSContactInfoPopoverController.m` | dealloc后 | 无 | 加 `adaptivePresentationStyleForPresentationController:` 方法 |
| `CSContactInfoPopoverController.h` | L4 | `@interface ... : UIViewController` | 加 `<UIAdaptivePresentationControllerDelegate>` |
| `CSChatAvatarTitleView.m` | L631后 | 无 | `popPC.delegate = popover;` |