# 聊天顶栏功能 — 开发步骤文档（第十四次修正版）

> 本次重点：
> - **Crash 分析**：会话列表页滑动闪退（`WeChat-2026-05-27-104743.ips`）
> - **UI 修复**：圆角卡片样式 / 内容对齐 / 主页跳转无反应
> - **深度对比 123456.c**（微信优化 1.6.5）
> **项目用 MRC 编译**

---

## 一、Crash 分析：会话列表页滑动闪退

### 1.1 Crash 摘要

| 项目 | 值 |
|------|-----|
| 文件 | `WeChat-2026-05-27-104743.ips` |
| 异常类型 | `EXC_BAD_ACCESS` (SIGSEGV) |
| 崩溃地址 | `0x0000000000000054`（NULL + 0x54 偏移） |
| 崩溃线程 | 主线程 (com.apple.main-thread) |
| 触发场景 | 会话列表页上下滑动 |
| 微信版本 | 8.0.60 (build 8.0.60.35) |
| 系统版本 | iPhone OS 17.2.1 |

### 1.2 关键调用栈

```
dispatch_async (libdispatch)
  → WeChat 内部帧（多个，imageIndex 27）
    → -[UITableView _notifyWillDisplayCell:forIndexPath:]  (UIKitCore)
      → -[UITableView _createPreparedCellForGlobalRow:withIndexPath:willDisplay:]
        → -[UITableView _updateVisibleCellsForRanges:createIfNecessary:]
          → -[UITableView _updateVisibleCellsNow:]
```

### 1.3 崩溃位置分析

崩溃地址 `0x54`（84 十进制）表示**对 NULL 指针偏移 0x54 处进行访问**。这通常意味着某个 Objective-C 对象的属性（在 +0x54 偏移处）被访问时，对象本身已经是 NULL/野指针。

崩溃发生在 UIKit 内部 `_updateVisibleCellsForRanges:createIfNecessary:` 链中，这是 UITableView 刷新可见 cell 的标准流程。访问的是 cell/delegate/datasource 相关的内部属性。

### 1.4 根因分析

**直接原因**：在会话列表 UITableView 刷新 cell 时，某个 delegate/datasource 引用已变成野指针，导致 UIKit 访问 `NULL + 0x54`。

**排查方向**：

**1) 当前项目（Mio_arm64-1.dylib）—— 非直接原因**

- ChatTopBarHook 仅 hook 了 `BaseMsgContentViewController`（聊天对话页）的 `viewDidLoad` 和 `viewWillAppear:`
- **没有 hook 会话列表页的任何方法**
- CSContactInfoPopoverController 是一个独立的 UIViewController，通过 modal present 展示，不注入到会话列表
- crash 调用栈中**没有 Mio_arm64-1.dylib 的帧**

**2) 已加载的多插件环境 —— 极可能是根因**

crash log 显示同时加载了以下插件 dylib：

| dylib | 说明 |
|-------|------|
| `微信优化1.6.5.dylib` | **hook 了 `tableView:cellForRowAtIndexPath:`**（123456.c L30220, L34372） |
| `WCRefine 1.4.4.dylib` | 界面美化插件 |
| `MikotoHelper.dylib` | 多功能助手 |
| `锤子助手2.4.1.dylib` | 多功能助手 |
| `黄白助手 2.0-9.dylib` | 多功能助手 |
| `微信助手 3.9-5.dylib` | 多功能助手 |
| `HBB9.1.2.dylib` | 功能增强 |
| `插件收纳.dylib` | 插件管理 |

**微信优化 1.6.5 的 `tableView:cellForRowAtIndexPath:` hook** 是全局性的（hook 了所有 UITableView），这意味着会话列表页的 UITableView 也会经过这个 hook。当多个插件同时对 UITableView 的 delegate/datasource 进行 hook 或修改时，极易产生野指针。

### 1.5 建议处理方案

**方案 A（推荐）**：排查 123456.c 中 `tableView:cellForRowAtIndexPath:` hook（L30220 的 `FUN_000323f0` 和 L34372 的 `FUN_0003b270`），确认这两个 hook 在 cell 不存在时是否正确处理了 nil 返回值，以及是否有对 `_sections` 等数据结构的空值保护。

**方案 B**：在 ChatTopBarHook 的 `install` 方法中增加保护——在 `presentUserInfoPopoverWithContact:sourceView:` 调用前确认 `self.chatController` 非 nil 且类型正确。但目前这是**聊天页**的保护，与**会话列表页** crash 无关。

**方案 C**：检查是否存在第 8 个以上插件的多插件互斥问题。建议关闭其他插件，仅保留 Mio_arm64-1.dylib 测试是否仍 crash。

---

## 二、UI 问题深度对比与修复

以下三项均与 `CSContactInfoPopoverController.m` v2（UITableView 架构版）有关。

### 2.1 问题一：圆角卡片样式不一致

#### 用户反馈

> "微信优化的弹窗列表项是圆角卡片，我们不是"

#### 123456.c 实现

123456.c 的弹窗使用 `UITableViewStyleGrouped`（L116659: `initWithFrame:style:2`），该样式**自动**为每个 section 生成圆角分组卡片效果。

同时，123456.c 注册的是 WeChat 内部的 `CSSettingTableViewCell`（L116691）：
```c
// L116691: 注册 CSSettingTableViewCell
FUN_000c9220(&objc::class_t::CSSettingTableViewCell, extraout_x1_04,
             *(undefined8 *)(param_1 + lVar4));
```

`CSSettingTableViewCell` 是微信内置的 cell 子类，自带左 label + 右 detail 的自动布局，不需要手动设置 textLabel/detailTextLabel 的样式。

另外，123456.c 有**多个 section**：
- Section 0：头像区域（`addAvatarSection_`，L116841）
- Section 1：基本信息（`addBasicInfoSection_`，L116897），包含 主页/微信/备注/性别/地区/签名
- Section 2（群聊才有）：群成员信息（`addGroupInfoSection_`，L116987）
- Section 2（公众号才有）：公众号信息（`addOfficialAccountInfoSection_`，L117040）

每个 section 自动显示为独立的圆角卡片。

#### 项目当前实现问题

`CSContactInfoPopoverController.m L90-92`：
```objc
Class cellClass = objc_getClass("CSSettingTableViewCell");
if (!cellClass) cellClass = [UITableViewCell class];
[tableView registerClass:cellClass forCellReuseIdentifier:@"Cell"];
```

**问题 A**：`[tableView registerClass:[UITableViewCell class] forCellReuseIdentifier:]` 创建的 cell 是 **`UITableViewCellStyleDefault`** 样式——**没有 `detailTextLabel` 属性**！因此 L158-160 的：

```objc
cell.detailTextLabel.text = [self valueForInfoKey:item[@"key"]];   // detailTextLabel = nil!
cell.detailTextLabel.font = ...;
cell.detailTextLabel.textColor = ...;
```

这些赋值全部发给 `nil`，**value 文字不显示**。最终 cell 只有 `textLabel`（label 名），看起来不像微信的卡片样式。

**问题 B**：只有 1 个 section（L145 `return 1`），所有 7 行挤在同一个卡片里，不像微信优化的多个独立卡片。

#### 修复方案

**Step 1 — Cell 创建方式改为 `initWithStyle:reuseIdentifier:`**：

```objc
// 删掉 L90-92 的 registerClass，改为手动创建 cell：

- (UITableViewCell *)tableView:(UITableView *)tableView cellForRowAtIndexPath:(NSIndexPath *)indexPath {
    // 检查 CSSettingTableViewCell 是否可用
    static Class cssCellClass = nil;
    static dispatch_once_t onceToken;
    dispatch_once(&onceToken, ^{
        cssCellClass = objc_getClass("CSSettingTableViewCell");
    });

    UITableViewCell *cell;
    if (cssCellClass) {
        // 微信内部类可用 → 使用 CSSettingTableViewCell（自带左右 Label 布局）
        cell = [tableView dequeueReusableCellWithIdentifier:@"CSSettingCell"];
        if (!cell) {
            cell = [[cssCellClass alloc] initWithStyle:UITableViewCellStyleValue1
                                       reuseIdentifier:@"CSSettingCell"];
        }
    } else {
        // 降级 → 手动创建 UITableViewCellStyleValue1（有 textLabel + detailTextLabel）
        cell = [tableView dequeueReusableCellWithIdentifier:@"Cell"];
        if (!cell) {
            cell = [[UITableViewCell alloc] initWithStyle:UITableViewCellStyleValue1
                                          reuseIdentifier:@"Cell"];
        }
    }

    NSDictionary *item = s_infoItems()[indexPath.row];
    cell.textLabel.text = item[@"label"];
    cell.textLabel.font = [UIFont systemFontOfSize:14];
    cell.detailTextLabel.text = [self valueForInfoKey:item[@"key"]];
    cell.detailTextLabel.font = [UIFont systemFontOfSize:13];
    cell.detailTextLabel.textColor = [UIColor colorWithRed:0.56 green:0.56 blue:0.58 alpha:1.0];
    cell.accessoryType = UITableViewCellAccessoryNone;
    cell.selectionStyle = UITableViewCellSelectionStyleDefault;

    return cell;
}
```

**关键**：`UITableViewCellStyleValue1` 是系统自带样式，`textLabel` 左对齐（黑色），`detailTextLabel` 右对齐（灰色）——这就是微信风格的 label-value 布局。

**Step 2 — 多个 section**（可选，还原微信优化风格）：

如果想像微信优化那样把头像做成独立的 section 0，其余信息为 section 1：

```objc
- (NSInteger)numberOfSectionsInTableView:(UITableView *)tableView {
    return 2;  // section 0 = 头像, section 1 = 信息列表
}

- (NSInteger)tableView:(UITableView *)tableView numberOfRowsInSection:(NSInteger)section {
    if (section == 0) return 1;           // 头像行
    return (NSInteger)s_infoItems().count; // 信息行
}
```

头像 section 的行高需要特殊处理：
```objc
- (CGFloat)tableView:(UITableView *)tableView heightForRowAtIndexPath:(NSIndexPath *)indexPath {
    if (indexPath.section == 0) return 170;  // 123456.c: avatar cell height = 170
    return 46;
}
```

---

### 2.2 问题二：信息对齐错误 & 头像未居中

#### 用户反馈

> "微信优化的卡片里面的消息是靠左，我们是靠右，然后头像区域也是靠右，没有居中"

#### 123456.c 实现

**消息对齐**：123456.c 使用 `UITableViewCellStyleValue1`（L118046: `style:1`）创建 detail cell，该样式原生就是 `textLabel` 左对齐 + `detailTextLabel` 右对齐。

**头像区域**：123456.c 的 header 是一个**空白 UIView**（L116700-116721）——只有 clearColor 背景，没有头像！头像实际是 table view 的 **section 0 row 0**（`createAvatarCellForTableView_withItem_`，L117801），用 Auto Layout 约束居中放置 avatar imageView。

#### 项目当前实现问题

**问题 A — 消息对齐**：同 2.1 的问题 A，`registerClass:[UITableViewCell class]` 导致 `detailTextLabel` 为 nil，value 文字不显示，只有 `textLabel`（label 名）。如果 `CSSettingTableViewCell` 不存在，cell 使用的是 `UITableViewCellStyleDefault`，没有左-label、右-value 的布局。

**问题 B — 头像偏移**：`CSContactInfoPopoverController.m L100`：

```objc
- (UIView *)createHeaderView {
    CGFloat width = self.view.bounds.size.width;   // ← 问题在这里！
    ...
    avatar.frame = CGRectMake((width - 60) / 2, 24, 60, 60);
```

问题在于 `createHeaderView` 在 `viewDidLoad` 中被调用。此时 `self.view.bounds.size.width` 是**全屏宽度**（iPhone 上约 390pt），而不是 popover 的实际宽度（272pt）。

这意味着：
- `avatar.frame.origin.x = (390 - 60) / 2 = 165`
- popover 宽度只有 272pt
- 头像中心在 165 + 30 = 195，popover 中心在 136
- 头像偏右约 59pt

同样，昵称 label 宽度 `width - 40 = 350`，远超 272 的 popover 宽度。

#### 修复方案

```objc
- (UIView *)createHeaderView {
    // 使用 popover 的实际宽度 272（与 preferredContentSize 一致），
    // 而不是 self.view.bounds.size.width（viewDidLoad 时是全屏宽度）
    CGFloat width = 272;   // 或使用 self.preferredContentSize.width

    UIView *header = [[UIView alloc] initWithFrame:CGRectMake(0, 0, width, 190)];

    // 头像居中
    UIImageView *avatar = [[UIImageView alloc] init];
    avatar.frame = CGRectMake((width - 60) / 2, 24, 60, 60);   // x = (272-60)/2 = 106 ✓
    avatar.layer.cornerRadius = 30;
    avatar.clipsToBounds = YES;
    avatar.contentMode = UIViewContentModeScaleAspectFill;
    if (self.avatarImage) {
        avatar.image = self.avatarImage;
    }
    [header addSubview:avatar];
    [avatar release];

    // 昵称居中
    UILabel *nickname = [[UILabel alloc] init];
    nickname.frame = CGRectMake(16, 94, width - 32, 28);   // 16 左右 margin
    nickname.textAlignment = NSTextAlignmentCenter;
    nickname.font = [UIFont boldSystemFontOfSize:18];
    id name = contactValueForKey(self.contact, @"m_nsNickName");
    nickname.text = name ?: @"";
    [header addSubview:nickname];
    [nickname release];

    return header;
}
```

同样修复 `createFooterView`：
```objc
- (UIView *)createFooterView {
    CGFloat width = 272;
    UIView *footer = [[UIView alloc] initWithFrame:CGRectMake(0, 0, width, 40)];
    UILabel *tip = [[UILabel alloc] init];
    tip.text = @"点击信息项复制到剪贴板";
    tip.font = [UIFont systemFontOfSize:11];
    tip.textColor = [UIColor grayColor];
    tip.textAlignment = NSTextAlignmentCenter;
    tip.frame = CGRectMake(0, 10, width, 20);
    [footer addSubview:tip];
    [tip release];
    return footer;
}
```

---

### 2.3 问题三：点击主页进入信息页没反应

#### 用户反馈

> "点击主页进入信息页没反应，不会进入信息页"

#### 123456.c 实现（L118351-118405）

```c
// handleProfileNavigation_ 的关键逻辑：
lVar5 = _objc_getClass("ContactInfoViewController");   // ← 这个类名！
if ((lVar4 != 0) && (lVar5 != 0)) {
    // alloc init ContactInfoViewController
    auVar7 = _objc_alloc_init();                        // [[ContactInfoViewController alloc] init]
    uVar6 = auVar7._0_8_;
    FUN_000ce560(uVar6, auVar7._8_8_, lVar4, &cf_m_contact);  // [vc setContact:contact]

    // 定义 dismiss 完成后的 block
    // Block 内：
    //   1. 从 weak self 获取 presenting VC
    //   2. 如果是 UINavigationController → push
    //   3. 否则获取 topViewController → push

    FUN_000c5200(param_1, auVar7._8_8_, 1, &local_78);  // dismissViewControllerAnimated:completion:
}
```

关键点：
- 类名是 **`ContactInfoViewController`**（不是 `WCUserInfoViewController`）
- 初始化方式：`[[ContactInfoViewController alloc] init]` + `setContact:`
- dismiss 后在 completion block 中 push 新 VC

#### 项目当前实现问题

`CSContactInfoPopoverController.m L279-299`：

```objc
- (void)onHomepageTapped {
    ...
    Class infoVC = objc_getClass("WCUserInfoViewController");  // ← 错误的类名！
    if (!infoVC) return;  // 类不存在 → 静默返回，无任何提示
    ...
}
```

**两个错误**：

| 项目 | 项目当前 | 123456.c（正确） |
|------|----------|-----------------|
| 类名 | `WCUserInfoViewController` | `ContactInfoViewController` |
| 初始化 | `initWithUsrName:contact:` | `init` + `setContact:` |

因为 `WCUserInfoViewController` 这个类在微信中**根本不存在**，所以 `objc_getClass` 返回 nil，代码在 `if (!infoVC) return;` 处静默退出，用户看到的现象就是"点主页没反应"。

#### 修复方案

```objc
- (void)onHomepageTapped {
    Class infoVC = objc_getClass("ContactInfoViewController");  // ← 修正类名
    if (!infoVC) {
        // 降级：尝试用 WCContactInfoViewController（某些微信版本的类名）
        infoVC = objc_getClass("WCContactInfoViewController");
    }
    if (!infoVC) {
        // 最终降级：提示用户
        UIAlertController *alert = [UIAlertController
            alertControllerWithTitle:nil
            message:@"当前微信版本不支持直接跳转资料页"
            preferredStyle:UIAlertControllerStyleAlert];
        [alert addAction:[UIAlertAction actionWithTitle:@"确定" style:UIAlertActionStyleDefault handler:nil]];
        [self presentViewController:alert animated:YES completion:nil];
        return;
    }

    // 创建 ContactInfoViewController
    id vc = ((id (*)(Class, SEL))objc_msgSend)(infoVC, NSSelectorFromString(@"alloc"));
    vc = ((id (*)(id, SEL))objc_msgSend)(vc, NSSelectorFromString(@"init"));
    if (!vc) return;

    // 设置 contact
    ((void (*)(id, SEL, id))objc_msgSend)(vc,
        NSSelectorFromString(@"setContact:"), self.contact);

    // dismiss 当前 popover，然后 push
    [self dismissViewControllerAnimated:YES completion:^{
        UIViewController *presenting = self.presentingViewController;
        if (!presenting) return;

        // 尝试从 presenting VC 找 navigationController
        UINavigationController *nav = presenting.navigationController;
        if (!nav) {
            // presenting VC 本身可能就是 nav
            if ([presenting isKindOfClass:[UINavigationController class]]) {
                nav = (UINavigationController *)presenting;
            }
        }
        if (nav) {
            [nav pushViewController:vc animated:YES];
        }
        [vc release];
    }];
}
```

---

## 三、完整修改对照：CSContactInfoPopoverController.m

以下是需要修改的全部位置汇总：

### 3.1 `viewDidLoad`（L70-95）— 删除 registerClass

```objc
// 删除以下 3 行（L90-92）：
// Class cellClass = objc_getClass("CSSettingTableViewCell");
// if (!cellClass) cellClass = [UITableViewCell class];
// [tableView registerClass:cellClass forCellReuseIdentifier:@"Cell"];
```

### 3.2 `createHeaderView`（L99-126）— 宽度硬编码为 272

```objc
// L100: CGFloat width = self.view.bounds.size.width;
// 改为：
CGFloat width = 272;

// L117: nickname.frame = CGRectMake(20, 94, width - 40, 28);
// 改为：
nickname.frame = CGRectMake(16, 94, width - 32, 28);
```

### 3.3 `createFooterView`（L128-139）— 宽度硬编码为 272

```objc
// L129: CGFloat width = self.view.bounds.size.width;
// 改为：
CGFloat width = 272;
```

### 3.4 `cellForRowAtIndexPath:`（L152-163）— 手动创建 cell

改为 2.1 修复方案中的手动 `initWithStyle:UITableViewCellStyleValue1` 方式。

### 3.5 `onHomepageTapped`（L279-299）— 修正类名和初始化

改为 2.3 修复方案中的 `ContactInfoViewController` + `init` + `setContact:` 方式。

---

## 四、123456.c 完整架构一览（供参考）

```
CSUserInfoPopoverController
├── viewDidLoad
│   ├── [super viewDidLoad]
│   ├── setupUI()                              // L116585
│   └── if (contact != nil) updateUIWithContact_()  // L116586-116588
│
├── setupUI
│   ├── self.view.backgroundColor = systemGroupedBackgroundColor
│   ├── _sections = [[NSMutableArray alloc] init]
│   ├── _tableView = [[UITableView alloc] initWithFrame:style:2]  // Grouped
│   ├── tableView.delegate = self
│   ├── tableView.dataSource = self
│   ├── tableView.backgroundColor = whiteColor
│   ├── tableView.separatorInset = (18,0,18,0)
│   ├── tableView.rowHeight = 46
│   ├── tableView.estimatedRowHeight = UITableViewAutomaticDimension
│   ├── tableView.showsVerticalScrollIndicator = NO
│   ├── tableView.contentInset = (8,8,8,8)
│   ├── tableView.tableHeaderView = createHeaderView()   // 空 UIView
│   ├── tableView.tableFooterView = createFooterView()   // 空 UIView
│   ├── [self.view addSubview:tableView]
│   └── [tableView registerClass:CSSettingTableViewCell]  // ★ 微信内部 cell
│
├── updateUIWithContact_
│   ├── [_sections removeAllObjects]
│   ├── addAvatarSection_(contact)            // section 0
│   ├── addBasicInfoSection_(contact)         // section 1
│   ├── if (chatroom) addGroupInfoSection_()  // section 2
│   └── if (gh_) addOfficialAccountInfoSection_()
│   └── [_tableView reloadData]
│
├── addAvatarSection_
│   └── 创建 CSSettingSection，含 1 个 CSSettingItem
│       ├── item.title = 昵称 (或 "微信用户")
│       ├── item.subTitle = 微信号
│       └── item 关联 "contact" associatedObject
│
├── addBasicInfoSection_
│   └── 创建 CSSettingSection，含 N 个 CSSettingItem
│       ├── [0] "主页"  — 关联 "profileContact" associatedObject
│       ├── [1] "微信"  — value = wxid
│       ├── [2] "备注"  — value = remark
│       ├── [3] "性别"  — value = gender (非群聊)
│       ├── [4] "地区"  — value = location (非群聊)
│       └── [5] "签名"  — value = signature (非群聊)
│
├── numberOfSections → _sections.count
├── numberOfRowsInSection → section.items.count
│
├── cellForRowAtIndexPath
│   ├── if (section == 0) → createAvatarCellForTableView_withItem_
│   │   └── UITableViewCellStyleDefault + 居中头像 + 昵称/微信号 Label
│   │       行高 170pt（L118455）
│   └── else → createDetailCellForTableView_withItem_atIndexPath_
│       └── UITableViewCellStyleValue1 ★ 左 label + 右 detail
│           行高 UITableViewAutomaticDimension
│
├── didSelectRowAtIndexPath
│   ├── section 0 row 0 → handleProfileNavigation_ ★
│   └── other → 检查 item.value 是否包含 "主页"/"微信" 等关键词
│       ├── 包含特定关键词 → handleProfileNavigation_
│       ├── 可复制 → copyTextToClipboard_ + showCopySuccessToast
│       └── 不可复制 → 忽略
│
├── handleProfileNavigation_
│   ├── 从 item 获取 objc_getAssociatedObject("profileContact")
│   ├── objc_getClass("ContactInfoViewController")  ★
│   ├── [[ContactInfoViewController alloc] init]
│   ├── [vc setContact:contact]
│   └── [self dismiss... completion:^{ [nav push:vc] }]
│
└── present (由 CSChatAvatarTitleView 调用)
    ├── popover = [[CSUserInfoPopoverController alloc] init]
    ├── [popover setupWithContact:contact]
    ├── popover.modalPresentationStyle = UIModalPresentationPopover (7)
    ├── popover.preferredContentSize = (272, 400)
    ├── popPC.sourceView = sourceView
    ├── popPC.permittedArrowDirections = Up
    └── [parentVC presentViewController:popover ...]
```

---

## 五、改造总结

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 圆角卡片样式不对 | `registerClass:[UITableViewCell class]` → Default 样式，无 detailTextLabel | 手动 `initWithStyle:UITableViewCellStyleValue1` |
| 2 | 内容/头像未居中 | `self.view.bounds.size.width` 在 viewDidLoad 时为全屏宽度 390 ≠ popover 宽度 272 | 硬编码 `272`（与 preferredContentSize 一致） |
| 3 | 点主页无反应 | `objc_getClass("WCUserInfoViewController")` 类不存在，静默 return | 改为 `ContactInfoViewController`，加降级提示 |

| Crash | 根因 | 建议 |
|-------|------|------|
| 会话列表滑动闪退 | 多插件环境，`微信优化1.6.5` 的全局 `tableView:cellForRowAtIndexPath:` hook 与 UIKit cell 生命周期冲突 | 关闭其他插件测试隔离；检查 123456.c L30220/L34372 的 hook 空值保护 |

---

## 六、MRC 注意事项

1. `dispatch_once` 中 `[@[...] retain]` — 静态变量需在 dylib 卸载时 `release`，或改用不需要 retain 的字面量方式
2. `[[UITableViewCell alloc] initWithStyle:reuseIdentifier:]` 返回 retain count +1，需在赋值给 cell 后 release（或使用 autorelease）
3. `onHomepageTapped` 中 dismiss completion block 捕获 `self` — MRC 下 block 不会自动 retain `self`，需确认 dismiss 后 self 仍在 completion 执行前有效