# 微信消息时间显示功能对比分析

## 概述

本文档对比三款 iOS 微信插件的"消息时间显示"功能：

| 对比项 | 微信优化 1.6.5 (WeChatEnhance) | 微信助手 3.9-5 | 我们的实现 (MessageTimeHook v3.1) | 功能说明 | 谁更好 |
|--------|:-------------------------------:|:--------------:|:---------------------------:|----------|:------:|
| 类型 | 综合型微信增强插件 | 综合型微信增强插件 | 专注消息时间显示的专用模块 | 插件定位和功能范围 | — |
| 包标识 | `com.wechat.enhance.*` | `MiYou`（米游） | Mio 插件内部模块 | 包命名和组织方式 | — |
| 定位 | 大而全（防撤回、红包、气泡美化、头像管理、消息时间等） | 庞大全功能（防撤回、红包、消息处理、朋友圈、小程序管理等） | 小而精（仅做消息时间显示） | 功能范围设计哲学 | 🏆 我们（专注单一功能，代码简洁可控） |
| 文件大小 | 2.1 MB（含全部功能） | **13 MB**（含全部功能） | ~655 KB dylib（整个插件） | dylib 体积对比 | 🏆 我们（体积最小） |
| 是否有消息时间功能 | ✅ 是（per-message 时间标签，8个位置模式） | ✅ 是（`msgTimeLabel` per-message 标签，通过 `addMessageNode:` 一次到位添加，class_addMethod 注入 getter/setter） | ✅ 是（per-message 时间标签，8个位置模式，v3.3 坐标系统修正：contentFrame 统一使用 cellView 坐标系而非 cell 坐标系，消除潜在偏移；getContentView 增强健壮性） | 消息时间功能的实现方式 | 🏆 我们（Ghidra 对齐 + 公开 API + 最简洁） |
| 分析方式 | 二进制逆向（strings + nm + llvm-objdump） | 同左 | 源码 + 日志 | 本文档的分析手段 | 🏆 我们（源码可审计） |

---

## 一、整体流程对比

### 微信优化 1.6.5 的整体流程

```
App 启动
  ↓
MSHookMessageEx(CommonMessageCellView, updateNodeStatus)   ← 模型驱动，非布局驱动！
MSHookMessageEx(CommonMessageCellView, didMoveToWindow)    ← 窗口可见性，非布局驱动！
MSHookMessageEx(ChatTimeCellView, layoutInternal)          ← 内置时间分隔线布局
MSHookMessageEx(ChatTimeViewModel, measure:)               ← 内置时间分隔线尺寸
MSHookMessageEx(BaseMsgContentViewController, willDisplayCell)
MSHookMessageEx(BaseMsgContentViewController, viewDidLayoutSubviews)
  → Ghidra 确认：使用 dispatch_async / dispatch_after 延迟定位
  → Ghidra 确认：class_copyIvarList / class_getInstanceVariable 运行时内省私有 ivar
  → Ghidra 确认：class_addMethod 动态注入新方法
  → Ghidra 确认：createLabelWithText:font:textColor: 工厂方法创建标签
  → Ghidra 确认：parentModel.m_subViewModels → TextMessageSubViewModel → m_uiCreateTime
  → Ghidra 确认：getBgImageView / getHeadImageView（直取气泡/头像，不做 subviews 遍历）
  → Ghidra 确认：initTimeLabel / m_timeLabel（标签创建与引用）
  → Ghidra 确认：ChatTimeCellView / ChatTimeViewModel（隐藏系统时间分隔线）
  → Ghidra 确认：GenSendMsgTime（根据消息创建时间生成时间文案）
  → Ghidra 确认：NSUserDefaults 配置键 com.wechat.enhance.chat.show.addtime.enabled
  ↓
用户打开聊天界面 → BaseMsgContentViewController 加载
  ↓
cell 即将显示 → willDisplayCell 触发
  ↓
  → dispatch_async 延迟到下一个 runloop（确保视图就绪）
  → initTimeLabel（创建 m_timeLabel，若已存在则复用）
  → 如果数据未就绪（wrap=nil），延迟到 updateNodeStatus
  ↓
cell 模型更新 → updateNodeStatus 触发（CommonMessageCellView）⭐ 关键：非 layoutSubviews！
  ↓
  → m_timeLabel 已存在？仅更新 frame（轻量）→ 否？创建 + 添加
  → 计算位置：基于 getBgImageView（气泡）/ getHeadImageView（头像）+ 8 位置模式
  → ⭐ 不在 layoutSubviews 热路径上，不参与滚屏级联 layout 循环
  ↓
viewDidLayoutSubviews 触发（BaseMsgContentViewController 级）
  → 批量校验所有 cell 标签位置（补充安全网）
  ↓
cell 被回收 → prepareForReuse → 清理 m_timeLabel
```

### 微信助手 3.9-5 的整体流程

```
App 启动
  ↓
checkHook:/checkHookWithSeq: 初始化自定义 hook 管理
class_addMethod + method_setImplementation (纯 ObjC Runtime)
  → hook 列表由内部字符串配置驱动
  → hook 节点中 confirmed（strings 确认）：
      BaseMsgContentViewController.addMessageNode:layout:addMoreMsg:addNewMsg:  ← 核心：消息节点插入时添加标签
      BaseMsgContentViewController.addMessageNode:layout:addMoreMsg:           ← 同上（无新消息标记）
      BaseMsgContentViewController.tableView:willDisplayCell:forRowAtIndexPath: ← 补充触发
      CMessageMgr.AddMsg:MsgWrap: / AsyncOnAddMsg:MsgWrap:                      ← 消息拦截
      CMessageMgr.OnGetNewXmlMsg:Type:MsgWrap:                                   ← XML 消息处理
  → strings 确认：GenSendMsgTime（统一的时间文本生成函数）
  → strings 确认：msgTimeLabel / setMsgTimeLabel:（通过 class_addMethod 运行时注入 getter/setter，底层用关联对象存储）
  → strings 确认：mIsMessageTime / mIsHiddenMessageTime（消息时间启用/隐藏开关）
  ↓
用户打开聊天界面
  ↓
消息加载完成 → addMessageNode:layout:addMoreMsg:addNewMsg: 触发（每个消息一次）
  → 消息节点已完全配置（气泡、头像、内容就绪）
  → checkHook: 判断消息时间功能是否启用
  → 通过 addTimeLabel → msgTimeLabel 给每条消息 cell 添加时间标签
  → 设置标签文字、颜色、位置 → addSubview（一次性，不再反复定位）
  ↓
多功能模块各自独立触发（防撤回、红包、朋友圈、小程序管理等）
  → 每个模块通过 checkHook: 判断是否启用
```

### 我们的整体流程（架构对齐微信优化 1.6.5）

```
App 启动
  ↓
MessageTimeHook.install()
  ↓
MTHookEntry 配置表 + MSHookMessageEx 驱动 install：
  ├── BaseMsgContentViewController.tableView:cellForRowAtIndexPath:    ← 取数据 + 缓存 wrap
  ├── BaseMsgContentViewController.tableView:willDisplayCell:forRowAtIndexPath:  ← dispatch_async 创建标签
  ├── CommonMessageCellView.updateNodeStatus                            ← 模型驱动刷新/创建（⭐ 核心：不在layoutSubviews热路径）
  ├── CommonMessageCellView.didMoveToWindow                             ← 窗口可见性补位（延迟创建安全网）
  ├── ChatTableViewCell.prepareForReuse                               ← 标签清理
  ├── ChatTimeCellView.layoutSubviews                                   ← 隐藏系统时间
  ├── ChatTimeViewModel.cellHeight
  └── CContact.m_nsNickName                                             ← 好友时间后缀
  ↓
用户打开聊天界面 → BaseMsgContentViewController 加载
  ↓
cellForRowAtIndexPath 触发 → repl_cellForRow
  → 调原始实现 → cell 配置完毕
  → KVC: m_cellView → m_viewModel → messageWrap → CMessageWrap
  → objc_setAssociatedObject(cell, @"cachedMsgWrap", wrap)   ← 缓存到 cell
  ↓
willDisplayCell 触发 → repl_willDisplayCell（主入口，与微信优化完全一致）
  → 调原始实现
  → dispatch_async 延迟到下一个 runloop
  → addTimeLabelToCell(cell) — 创建标签
  ↓
updateNodeStatus 触发 → repl_updateNodeStatus（模型驱动，与微信优化对齐 ⭐）
  → 调原始实现
  → 标签已存在？→ quickRelocateTimeLabel → 仅 setFrame 刷新
  → 标签不存在？→ addTimeLabelToCell(cell)（兜底创建）
  ↓
didMoveToWindow 触发 → repl_didMoveToWindow（窗口可见性补位）
  → 调原始实现
  → window ≠ nil 且标签不存在？→ dispatch_async → addTimeLabelToCell
  ↓
ChatTableViewCell.prepareForReuse 触发 → repl_prepareForReuse
  → msgTimeLabel removeFromSuperview + 关联对象置 nil
```

---

## 二、Hook 架构对比

### 2.1 Hook 的目标类和方法

| Hook 目标 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（已对齐微信优化架构） | 功能说明 | 谁更好 |
|-----------|:-------------:|:-------------:|:-----------------:|----------|:------:|
| `BaseMsgContentViewController.addMessageNode:layout:addMoreMsg:addNewMsg:` | ❌ | ✅ **核心入口**（字符串 tables 确认） | ❌（v3.2 已移除，与微信优化一致，仅用 willDisplayCell 公开 API） | **消息节点插入时创建标签**：微信助手独家，在节点插入时创建标签。我们和微信优化用 willDisplayCell 公开 API 替代 | 🏆 微信助手（有但风险高，我们和微信优化用公开 API 更稳定） |
| `BaseMsgContentViewController.tableView:cellForRowAtIndexPath:` | ❌ | ✅（缓存 wrap） | ✅ 配置表条目 | **数据缓存**：cell 配置完毕时提前取 wrap 缓存到 cell | 🏆 我们/微信助手 |
| `BaseMsgContentViewController.tableView:willDisplayCell:forRowAtIndexPath:` | ✅ | ✅（补充） | ✅（主入口，dispatch_async 延迟，与微信优化一致） | **标签创建触发**：cell 即将显示时创建标签，dispatch_async 延迟确保视图就绪 | 🏆 微信优化/我们（公开 API 稳定可靠） |
| `BaseMsgContentViewController.viewDidLayoutSubviews` | ✅（Ghidra 确认） | ❌ | ✅ **v3.1 新增**（模仿微信优化） | **批量校验**：VC 级布局完成后遍历 visibleCells 刷新标签 frame | 🏆 1.6.5/我们 |
| `CommonMessageCellView.updateNodeStatus` | ✅（二进制确认：dylib strings 含 `updateNodeStatus` + `MSHookMessageEx`，Ghidra 反编译 FUN_0000a8dc.c 确认） | ❌ | ✅（架构对齐微信优化：模型驱动刷新/创建标签，不参与 scrolling hot path） | **模型更新时刷新标签**：⭐ 微信优化的核心架构选择 — `updateNodeStatus` 在 viewModel 更新时触发，不参与 scrolling layout 热路径。这是它不会 layout 循环的根本原因 | 🏆 双方一致（模型驱动，天然避热路径） |
| `CommonMessageCellView.layoutSubviews` | ❌（二进制 + Ghidra 确认：未 hook 此方法用于消息时间） | ❌ | ❌（已移除，架构对齐微信优化：不再参与 hot scroll path） | ~~标签 frame 刷新：热路径上的重定位~~（已迁移到 updateNodeStatus） | 🏆 微信优化（完全避免此热路径，我们已对齐） |
| `CommonMessageCellView.didMoveToWindow` | ✅（二进制确认：dylib strings + Ghidra 反编译 FUN_0003fac8.c 确认） | ❌ | ✅（架构对齐微信优化：窗口可见性时补位创建标签，不在热路径） | **窗口可见性时处理**：cell 进入/离开可视区域时触发，另一个非热路径回调 | 🏆 双方一致（多入口补位，不依赖热路径） |
| `CommonMessageCellView.prepareForReuse` | ✅（与 didMoveToWindow 同一 hook 文件 FUN_0003fac8.c，但具体清理机制待确认） | ❌ | ✅（ChatTableViewCell 层清理） | **标签清理**：cell 复用时清除标签 | 🏆 我们（直接层清理最干净） |
| `ChatTimeCellView.layoutSubviews` | ✅（"隐藏聊天页原生时间标签"） | ✅（"隐藏自带时间"，`mIsHiddenMessageTime` 开关） | ✅ 配置表条目 | **隐藏系统时间分隔线**：隐藏微信自带的"今天 12:30"时间线 | 相当（1.6.5 和我们 hook CellView；微信助手用属性更灵活） |
| `ChatTimeViewModel.cellHeight` | ✅（双保险：隐藏 cell + 高度归零） | ❌（微信助手用不同路径实现） | ✅ 配置表条目 | **时间分隔线高度归零**：配合隐藏，确保不占布局空间 | 🏆 我们/1.6.5（高度归零双保险） |
| `CContact.m_nsNickName` | ✅（`addTimeSuffixFormatItem` + `show.addtime.enabled`） | ❌ | ✅ 配置表条目 | **好友时间后缀**：联系人列表昵称后显示好友添加时间 | 🏆 1.6.5（有完整配置项） |
| — | — | — | — | — | — |
| 消息时间标签（📌 非Hook，标签引用方式） | ✅ `m_timeLabel`（实例变量） | ✅ `msgTimeLabel`（class_addMethod 注入 getter/setter + 关联对象） | ✅ `msgTimeLabel`（selector key + 直接关联对象，tag=999999 兜底） | **标签存储**：存储每条消息时间标签的 UILabel 引用 | 🏆 我们（最简洁：selector 防拼写错误 + 直接读写零中间层） |
| 自定义 hook 管理层 | ❌ | ✅ `checkHook:` / `checkHookWithSeq:` | ✅ `MTHookEntry` 配置表 | **Hook 管理层**：统一管理所有 hook 安装/卸载 | 🏆 我们（配置表编译时可见，比字符串驱动更直观） |

### 2.2 Hook 技术对比

| 方面 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|------|:-------------:|:-------------:|:---------:|----------|:------:|
| Hook API | `MSHookMessageEx`（Cydia Substrate） | `class_addMethod` + `method_setImplementation` | `MSHookMessageEx`（自定义 `CydiaSubstrate.m` 编译进主 dylib，零外部依赖） | 底层 hook 实现 API 选型 | 🏆 我们（Substrate API + 内联零依赖） |
| 外部依赖 | 自带 `CydiaSubstrate.framework`（`@rpath` 加载，不依赖系统） | 纯 ObjC Runtime（无外部依赖） | 无外部依赖（`CydiaSubstrate.m` 内联编译，`MSHookMessageEx` 实现约 10 行） | 对第三方库的依赖程度 | 🏆 我们/微信助手（零外部依赖） |
| 原始 IMP 保存 | 自动由 MSHookMessageEx 处理 | 手动管理 | 自动由 MSHookMessageEx 处理 | 原始实现指针的保存和调用方式 | 🏆 我们/1.6.5（自动保存，不会遗漏） |
| Hook 管理方式 | 分散调用 MSHookMessageEx | `checkHook:` 统一调度 | 配置表驱动（`MTHookEntry` + install 迭代） | 多个 hook 点的组织和管理方式 | 🏆 我们（配置表编译时可见，最直观） |
| 稳定性 | 成熟，久经考验 | 自定义管理，依赖内部配置 | Substrate 风格，内联实现简洁可控 | 运行时的可靠性 | 🏆 1.6.5（久经考验） |
| 是否可运行时切换引擎 | ❌（固定 Substrate，自带 framework） | 固定 Runtime | 无需切换（MSHookMessageEx 始终可用：Theos 环境用系统版，独立构建用内联版） | 在不同环境下适配 hook 引擎的能力 | 🏆 我们（双环境自适应，无需切换） |

---

## 三、数据获取时机对比

### 核心问题

消息时间标签需要 `CMessageWrap` 对象来获取 `m_uiCreateTime`。**wrap 何时取、从哪条路径取**，直接影响标签创建的成功率，比"标签何时创建"更根本——拿不到数据，创建时机再好也没用。

### 三种时机策略

| 特性 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的当前实现（v3.1） | 功能说明 | 谁更好 |
|------|:-------------:|:-------------:|:-----------------:|----------|:------:|
| 取数据时机 | willDisplayCell + updateNodeStatus（现场取，不缓存） | cellForRowAtIndexPath（提前缓存）→ addMessageNode（读缓存） | cellForRowAtIndexPath（提前缓存）→ willDisplayCell（读缓存） | 数据在哪个回调中获取、是否缓存 | 🏆 我们/微信助手（提前缓存，后续零 KVC） |
| 取数据路径 | 未明确（二进制无法确认完整路径） | node → cellView → viewModel → messageWrap → `objc_setAssociatedObject` | cellView → m_viewModel → messageWrap → `objc_setAssociatedObject`(cell, @"cachedMsgWrap") | 从 UI 层穿透到数据层的 KVC 链路 | 🏆 我们/微信助手（单次穿透 + 关联对象） |
| 读数据时机 | willDisplayCell / updateNodeStatus 现场 KVC | addMessageNode: `objc_getAssociatedObject` 读缓存 | willDisplayCell：`objc_getAssociatedObject` 读缓存 | 需要数据时从何处读取 | 🏆 我们/微信助手（关联对象 O(1) 读取） |
| KVC 链长度 | 每次 2~3 层 | 只在 cellForRow 走 1 次（3 层），后续读缓存（0 层） | 同微信助手 | 每次取数据需要穿透的 KVC 层数 | 🏆 我们/微信助手（绝大多数情况 0 层） |
| 缓存命中率 | N/A（不缓存） | 理论上 100% | ✅ 已验证 100%（messagetime(51).log） | 数据缓存方案的实际可靠性 | 🏆 我们/微信助手（已验证 100%） |
| addTimeLabelToCell 来源 | 可能直接在 willDisplayCell/updateNodeStatus 中取 | objc_getAssociatedObject(cell, "cached key") | objc_getAssociatedObject(cell, @"cachedMsgWrap") | 标签创建函数中获取消息数据的方式 | 🏆 我们/微信助手（关联对象无失败风险） |

### 为什么 cellForRow 缓存比现场取更可靠

```
cellForRowAtIndexPath 回调触发时:
  → 原始实现已执行完毕
  → cell.m_cellView 已赋值          ✅
  → cellView.m_viewModel 已赋值     ✅
  → viewModel.messageWrap 已赋值    ✅
  → 所有数据 100% 就绪 ← 这是取数据的最佳时机！

willDisplayCell 回调触发时:
  → cell 即将显示
  → 数据也已就绪，但如果在这时现场走 KVC：
     cell → m_cellView → m_viewModel → messageWrap
     链长 3 层，每层都可能因时序问题为 nil
  → 微信优化 1.6.5 的 willDisplayCell + updateNodeStatus 双时机：
     实际是兜底策略——willDisplayCell 失败，等 updateNodeStatus 再试

cellForRow 缓存方案:
  cellForRow → 走 1 次 KVC 取数据 → 挂到 cell 关联对象
  willDisplayCell → 读关联对象 → 1 步到位，100% 命中
  addTimeLabelToCell 完全不依赖 KVC 长链，只依赖缓存
```

## 四、时间标签对象管理

### 4.1 标签创建

| 特性 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|------|:-------------:|:-------------:|:---------:|----------|:------:|
| 标签对象 | `UILabel` 实例，通过 `m_timeLabel` 引用 | `UILabel` 实例，通过 `msgTimeLabel` 方法引用 | `UILabel` 实例，selector key + 直接关联对象（无注入层，类微信优化） | 时间标签的 UI 对象和引用方式 | 🏆 我们（最简单直接，零中间层） |
| 创建时机 | `initTimeLabel` 方法（首次需要时创建） | `willDisplayCell` 回调中创建（通过 `checkHook:` 判断启用状态） | `initTimeLabel` 方法（v3.2，在 `willDisplayCell`+dispatch_async 中首次需要时创建，与微信优化一致） | 标签 UILabel 对象被创建的触发点 | 🏆 我们/微信优化（willDisplayCell 公开 API + dispatch_async） |
| 创建方式 | `createLabelWithText:font:textColor:` 工厂方法（Ghidra 确认） | 未知 | [[UILabel alloc] init] + 手动设置 text/font/textColor/frame | 标签对象的构造方式 | 🏆 1.6.5（工厂方法封装，代码整洁） |
| 标识方式 | 实例变量 / 关联对象 | class_addMethod 注入 getter/setter，通过 selector 作为关联对象 key | selector key 直接关联对象 + tag=999999 双重兜底 | 如何唯一标识和查找已创建的标签 | 🏆 我们（selector 防拼写错误 + 关联对象零开销，无注入层） |
| 防重复 | `initTimeLabel` → `m_timeLabel` 存在则不创建 | 每次 `willDisplayCell` 直接覆盖重建，不防重复 | `call_msgTimeLabel_getter(cellView)` + `viewWithTag:999999` 双重检查 | 避免同一条消息创建多个时间标签 | 🏆 我们（双重检查最安全） |

### 4.2 标签清理

| 清理时机 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|---------|:-------------:|:-------------:|:---------:|----------|:------:|
| cell 复用（prepareForReuse） | ✅ 清理 m_timeLabel | ❌ 不显式清理（`willDisplayCell` 每次都重建覆盖） | ✅ `objc_getAssociatedObject` 取 label 移除，清理关联对象 | cell 回收时清除旧标签，避免标签错位 | 🏆 我们/1.6.5（显式清理比覆盖更可控） |
| cell 复用后布局重入 | 清理 + 重新创建 | 覆盖 + 重新创建 | 清理 + 重新创建 | 复用后 cell 再次布局时的标签处理流程 | 🏆 我们/1.6.5（清理再创建比纯覆盖更干净） |

### 4.3 防止布局循环

布局循环的根本原因是：在 `layoutSubviews` 热路径中添加/修改子视图 → 触发新的 layout → 再次进入 `layoutSubviews` → 无限循环。

微信优化 **天然免疫** 此问题，不是因为它有防重入机制，而是因为它的架构层面就避开了热路径。

#### 4.3.1 核心机制对比

| 机制 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|------|:-------------:|:-------------:|:---------:|----------|:------:|
| 架构选择 | 🏆 **ViewModel 驱动**：hook `updateNodeStatus`（模型更新时触发，不在滚动热路径上）+ `didMoveToWindow`（窗口可见性变化，非热路径）。Ghidra 二进制确认：`CommonMessageCellView.layoutSubviews` **未被 hook 用于消息时间** | 🏆 天然无循环（仅 hook `addMessageNode:` + `willDisplayCell`，不 hook `layoutSubviews`，不存在自触发回路） | 🏆 **ViewModel 驱动（已对齐微信优化）**：hook `updateNodeStatus` + `didMoveToWindow`，完全不在 layoutSubviews 热路径上。同时保留 `willDisplayCell`（初始创建）+ `viewDidLayoutSubviews`（批量校验） | 防止 addSubview 触发 layoutSubviews 导致无限循环的机制 | 🏆 微信优化/我们（架构层面避免，最彻底） |
| 热路径参与 | ❌ **不参与**：`updateNodeStatus` 仅在 viewModel 数据变化时触发，滚动/布局不触发 | ❌ 不参与 | ❌ **不参与**：已移除 `layoutSubviews` hook，所有标签操作都不在滚动热路径上 | 是否在滚动/布局热路径上执行代码 | 🏆 三方一致（零热路径开销） |
| 延迟策略 | **dispatch_async 延迟定位**（Ghidra 确认）：willDisplayCell → dispatch_async → initTimeLabel，确保视图就绪后再创建标签 | addMessageNode 在节点插入时触发，此时视图已完全就绪，无需延迟 | **dispatch_async**：willDisplayCell → dispatch_async → addTimeLabelToCell（与微信优化一致） | 通过延迟执行确保视图布局完成后再操作标签 | 🏆 微信优化/我们（dispatch_async 简单可靠） |

#### 4.3.2 为什么 updateNodeStatus 不会引发布局循环

```
统一触发链（微信优化 + 我们 — ViewModel 驱动 → 零循环风险）:
  ┌──────────────────────────────────────────────────────┐
  │ viewModel 数据变化（如消息状态更新、首次渲染完成）     │
  │   ↓                                                   │
  │ CommonMessageCellView.updateNodeStatus ← hook 在这里  │
  │   ↓                                                   │
  │ 检查 m_timeLabel / msgTimeLabel 是否存在               │
  │   ├─ 存在 → 仅更新 frame（轻量 setFrame，不添加子视图）│
  │   └─ 不存在 → 创建 + addSubview（一次性操作）         │
  │   ↓                                                   │
  │ ❌ 不会触发新的 layoutSubviews 循环                    │
  │    （原因：setFrame 可能触发布局，但 updateNodeStatus   │
  │     不会被子布局的回调重新触发）                         │
  └──────────────────────────────────────────────────────┘

对比：如果我们 hook layoutSubviews（旧架构）:
  ┌──────────────────────────────────────────────────────┐
  │ 滚动 TableView                                        │
  │   ↓                                                   │
  │ layoutSubviews → setFrame → layoutSubviews → 无限循环 │
  │   ⚠️ 我们的旧实现需要 frame 守卫来阻断此循环           │
  │   ⚠️ 即使有守卫，每次滚动帧仍在执行检查逻辑             │
  │   ✅ 现已迁移到 updateNodeStatus，彻底消除此问题       │
  └──────────────────────────────────────────────────────┘
```

#### 4.3.3 核心结论

> **微信优化的防布局循环策略不是"防"，而是"避"**。它选择 `updateNodeStatus` 和 `didMoveToWindow` 这两个非热路径回调作为标签管理入口，从根本上就不参与 `layoutSubviews` 的级联调用链。这不是靠 flag、guard、dispatch_async 等技术手段实现的，而是靠架构层面的正确选择。
>
> **我们现已完全对齐此架构**，移除 `CommonMessageCellView.layoutSubviews` hook，改用 `updateNodeStatus` + `didMoveToWindow`，实现了同等级别的架构安全。

### 4.4 架构选择深度对比：ViewModel 驱动 vs Layout 驱动

这是消息时间功能**最重要**的架构决策，直接决定了滚动性能、布局安全和多插件兼容性。

#### 4.4.1 两种架构范式

```
┌─────────────────────────────────────────────────────────────────┐
│  ViewModel 驱动（微信优化 1.6.5 + 我们现已对齐）                  │
│                                                                   │
│  触发链：数据变化 → ViewModel 更新 → updateNodeStatus → 标签刷新  │
│                                                                   │
│  特点：                                                           │
│  ✅ 不参与 layoutSubviews 调用链                                  │
│  ✅ 滚动时不触发（零 CPU 开销）                                    │
│  ✅ 不会与 WCRefine 等插件冲突（它们也在 hook layoutSubviews）     │
│  ✅ 不会引发布局循环                                              │
│  ⚠️  标签位置更新依赖 ViewModel 更新时机（可能略有延迟）           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Layout 驱动（旧版 MessageTime — 已废弃）                         │
│                                                                   │
│  触发链：滚动/布局 → layoutSubviews → 标签 frame 刷新             │
│                                                                   │
│  问题：                                                           │
│  ❌ 参与 layoutSubviews 调用链，每次滚动帧都执行                   │
│  ❌ 需要 frame 比较守卫来防止级联循环                              │
│  ❌ 与 WCRefine 等插件共享同一个 hook 点，可能冲突                 │
│  ❌ 真实 crash 案例：0x8BADF00D watchdog timeout                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.4.2 微信优化为什么不卡（逐层分析）

| 层级 | 微信优化 1.6.5 | 我们的实现（架构对齐后） | 影响 |
|:----:|:-------------:|:---------:|:-----|
| **Hook 点选择** | `updateNodeStatus` + `didMoveToWindow` — 纯非热路径回调 | `updateNodeStatus` + `didMoveToWindow` + `willDisplayCell` + `viewDidLayoutSubviews` — **已对齐微信优化**，所有 hook 都不在 layoutSubviews 热路径 | 这是**根本架构统一**，消除了所有后续问题 |
| **每次滚动的 CPU 开销** | **零**：`updateNodeStatus` 不会被滚动触发 | **零**：`updateNodeStatus` 不会被滚动触发（与微信优化一致） | 滚动时零 CPU 开销，彻底解决卡顿 |
| **布局循环风险** | **零**：`updateNodeStatus` 不在 layout 调用链中 | **零**：不在 layout 调用链中（与微信优化一致） | 天然免疫布局循环 |
| **与其他插件兼容** | **天然兼容**：不争抢 `layoutSubviews` 这个热门 hook 点 | **天然兼容**：不争抢 `layoutSubviews`（与微信优化一致） | 不再与 WCRefine 等插件冲突 |
| **标签更新及时性** | 依赖 ViewModel 更新触发（可能延迟一个 runloop） | 依赖 ViewModel 更新触发 + viewDidLayoutSubviews 批量补位 | 轻微延迟在实际使用中不可感知 |

#### 4.4.3 Ghidra 二进制验证清单

以下是通过 Ghidra 反编译 `微信优化1.6.5.dylib` 验证的 hook 点：

| 源文件 | Hook 目标 | 用途 | 是否热路径 |
|--------|----------|------|:----------:|
| `FUN_0000a8dc.c` | `CommonMessageCellView.updateNodeStatus` | 消息时间标签刷新/创建 | ❌ 非热路径 |
| `FUN_0003fac8.c` | `CommonMessageCellView.didMoveToWindow` | 窗口可见性补充入口 | ❌ 非热路径 |
| `FUN_00025688.c` | `ChatTimeCellView.layoutInternal` | 隐藏系统时间分隔线 | ❌ 非热路径 |
| `FUN_00025688.c` | `ChatTimeViewModel.measure:` | 时间分隔线高度归零 | ❌ 非热路径 |
| `FUN_00025688.c` | `VoiceMessageCellView.layoutSubviews` | 语音消息气泡样式 | ⚠️ 热路径（但非消息时间功能） |
| `FUN_00025688.c` | `YYAsyncImageView.layoutSubviews` | 图片异步渲染样式 | ⚠️ 热路径（但非消息时间功能） |

> **关键发现**：`VoiceMessageCellView.layoutSubviews` 和 `YYAsyncImageView.layoutSubviews` 虽在热路径上，但它们处理的是语音/图片样式，**与消息时间标签无关**。消息时间功能的所有 hook 点都不在 `layoutSubviews` 热路径上。

#### 4.4.4 多插件冲突的根源

```
WCRefine 1.4-0.dylib                    我们的 MessageTime
        │                                       │
        │ hook                                  │ hook
        ▼                                       ▼
   CommonMessageCellView.layoutSubviews  ← 同一个方法被两个插件 hook
        │                                       │
        │ MSHookMessageEx trampoline 链          │
        ▼                                       ▼
    原始实现 ← WCRefine 替换体 ← 我们的替换体
                                       │
                                       │ setFrame → 可能触发新 layout
                                       ▼
                                   再次进入 layoutSubviews
                                       │
                                       │ 如果 frame 比较守卫失效
                                       ▼
                               WCRefine 和我们交替执行 → CPU 螺旋上升
                                       │
                                       ▼
                              0x8BADF00D watchdog timeout → 闪退
```

**微信优化不受此影响**，因为它根本不在 `layoutSubviews` 这条链路上。

#### 4.4.5 架构改进历程

> **2024-05-17 已实施**：将 MessageTime 模块从 Layout 驱动（`CommonMessageCellView.layoutSubviews` hook）迁移到 ViewModel 驱动（`updateNodeStatus` + `didMoveToWindow`），与微信优化 1.6.5 架构完全对齐。核心变更：
> 
> 1. **移除** `CommonMessageCellView.layoutSubviews` hook — 这是滚动卡死和 WCRefine 冲突的根源
> 2. **新增** `CommonMessageCellView.updateNodeStatus` hook — 模型驱动标签刷新/创建，不在热路径
> 3. **新增** `CommonMessageCellView.didMoveToWindow` hook — 窗口可见性补充入口，不在热路径
> 4. **保留** `willDisplayCell`（初始创建）、`viewDidLayoutSubviews`（批量校验）、`prepareForReuse`（清理）
> 
> 结果：与微信优化采用完全相同的架构范式，从根本上消除了热路径参与、布局循环风险和多插件冲突问题。

## 五、位置计算

### 5.1 支持的位置模式

两个实现都支持 8 种位置模式：

| Position | 名称 | 基准 | 微信优化公式 (反汇编确认) | 我们的公式 (v3.6, setCenter) |
|:--------:|------|------|:--------------------------|:-------------------|
| 0 | 头像上方 | 头像中心 X，头像顶部以上 | X=avatarMidX-w/2, Y=avatarTop-h/2 或 fallback: sender→cvRight+w/2, recv→cvLeft-w/2, Y=cvBottom-h/2 | X=avatarMidX-w/2, Y=avatarTop-h/2. 无头像fallback: sender→cvRight+w/2, recv→cvLeft-w/2 |
| 1 | 头像下方 | 头像中心 X，头像底部以下 | X=avatarMidX-w/2, Y=avatarBottom+h/2 或 fallback | X=avatarMidX-w/2, Y=avatarBottom+h/2 |
| 2 | 消息旁边（远离头像） | cv外侧边缘，垂直居中 | **setCenter**: sender→(cvLeft-w/2, cvBottom-h/2), recv→(cvRight+w/2, cvBottom-h/2) | ✅ **setCenter** 完全一致: sender→(cvLeft-w/2, cvBottom-h/2), recv→(cvRight+w/2, cvBottom-h/2) |
| 3 | 消息下方（远离头像） | cv底部，外侧对齐 | **setCenter**: sender→(cvLeft+w/2, cvBottom+h/2), recv→(cvRight-w/2, cvBottom+h/2) | ✅ **setCenter** 完全一致: sender→(cvLeft+w/2, cvBottom+h/2), recv→(cvRight-w/2, cvBottom+h/2) |
| 4 | 消息下方（靠近头像） | cv底部，内侧/头像侧对齐 | **setCenter**: sender→(cvRight-w/2, cvBottom+h/2), recv→(cvLeft+w/2, cvBottom+h/2) | ✅ **setCenter** 完全一致: sender→(cvRight-w/2, cvBottom+h/2), recv→(cvLeft+w/2, cvBottom+h/2) |
| 5 | 消息上方（远离头像） | cv顶部，外侧对齐 | **setCenter**: sender→(cvLeft+w/2, cvTop-h/2), recv→(cvRight-w/2, cvTop-h/2) | ✅ **setCenter** 完全一致: sender→(cvLeft+w/2, cvTop-h/2), recv→(cvRight-w/2, cvTop-h/2) |
| 6 | 消息上方（靠近头像） | cv顶部，内侧/头像侧对齐 | **setCenter**: sender→(cvRight-w/2, cvTop-h/2), recv→(cvLeft+w/2, cvTop-h/2) | ✅ **setCenter** 完全一致: sender→(cvRight-w/2, cvTop-h/2), recv→(cvLeft+w/2, cvTop-h/2) |
| 7 | 消息旁边（=气泡外） | cv外侧边缘，垂直居中（同位置2） | **setCenter**: sender→(cvLeft-w/2, cvBottom-h/2), recv→(cvRight+w/2, cvBottom-h/2) | ✅ **setCenter** 完全一致: sender→(cvLeft-w/2, cvBottom-h/2), recv→(cvRight+w/2, cvBottom-h/2) |

> **v3.6 重大变更**：位置 2-7 已全面从 `setFrame` 边缘对齐切换为 `setCenter` 跨骑（straddle）定位，与微信优化完全对齐。位置 0-1（头像上/下方）仍保持原 `setFrame` 实现（因依赖头像视图，逻辑复杂，暂未切换）。详见 §5.2.6 位置编号映射表。

> **v3.1 坐标空间统一**：`cellFrame = [(UIView *)cellView bounds]`（cellView 自身 bounds），`bubbleFrame`/`avatarFrame` 直接使用 `bubbleView.frame`/`avatarView.frame`（两个都是 cellView 的直接子视图，frame 天然在 cellView 坐标空间）。**不再使用 `[cell convertRect:fromView:]` 转换到 ChatTableViewCell 空间**——标签 addSubview 到 cellView，统一使用 cellView 坐标系，避免了跨坐标空间比较导致的偏移误差（如 position=1 "头像下方"误跳到 cell 底部）。

### 5.2 llvm-objdump 反汇编深度分析

> **分析方法**：对 `/www/wwwroot/ios/插件/微信优化1.6.5.dylib`（1.35MB，arm64，NOUNDEFS）进行 `llvm-objdump -d --macho` 全量反汇编（194,426 行），定位到位置计算核心函数（地址 0x3a500-0x3ad00），逐段逆向解析 ARM64 汇编。

#### 5.2.1 函数定位

位置计算函数位于 `__text:0x3a500-0x3ad00`（约2KB），栈帧 0xe0 字节，使用 x19-x28 全部寄存器 + d8-d13 浮点寄存器。该函数在多个时机被调用：
- `willDisplayCell` → `dispatch_async` → 位置计算
- `layoutSubviews` → 位置重算

#### 5.2.2 关键常量 (反汇编确认)

```
0x39088: fmov d0, #-4.00000000     // 负偏移常数 (-4.0)
0x39118: fmov d1, #4.00000000      // 正偏移常数 (+4.0, 用于位置0接收者)
0x39090: fmov d1, #-0.50000000     // -0.5 乘数 (用于 w/2, h/2 偏移)
0x39110: fmov d0, #0.50000000      // +0.5 乘数
0x3aa00: fmov d0, #0.50000000      // +0.5 乘数 (多处使用)
0x3a948: fmov d0, #-0.50000000     // -0.5 乘数
0x3ac10: fmov d0, d8              // 第一次 setCenter X → d0
0x3ac14: fmov d1, d9              // 第一次 setCenter Y → d1
0x3ac18: bl 0xcae00               // 📌 **第一次 setCenter**(straddleX, straddleY)
--- 读取用户偏移配置 (0x3ac2c-0x3ac58) ---
0x3ac74: fadd d0, d0, d10         // newX = (±offsetX) + d10 (来自 x20 配置对象)
0x3ac78: fsub d1, d1, d2          // newY = d1 - offsetY
0x3ac80: bl 0xcae00               // 📌 **第二次 setCenter**(最终X, 最终Y)
```

**核心发现**：

1. 微信优化使用 **0.5 乘数（w/2, h/2）** 进行位置偏移，即标签 straddle（骑跨）在 contentView 边缘上，而非完全贴合在边缘外侧。
2. **微信优化使用 `setCenter` 定位标签**（非 `setFrame`）：`0xcae00` 是 `setCenter:` 或类似属性设置方法，传入 `(d0, d1)` 作为 `(centerX, centerY)` 参数。关键证据：传入参数只有 `d0`(X) 和 `d1`(Y) 两个值，没有宽度/高度参数，这排除了 `setFrame:` 的可能性（后者需要 4 个参数）。
3. **两次 `setCenter` 调用**：第一次设置跨骑基准位置，第二次在应用配置偏移后设置最终位置。详见 §5.2.5。

#### 5.2.3 反汇编→伪代码还原

基于 ARM64 汇编逐指令还原的伪代码逻辑：

```c
// ===== 函数入口 =====
// 参数: self (CommonMessageCellView), ...
// 栈变量: [sp, #0x3c] = isSender (BOOL)
//        [sp, #0x18] = m_contentView (从cell获取)
//        [sp, #0x28] = cell (retained)

// ===== 获取 contentView frame =====
contentView = KVC(cell, "m_contentView");    // [sp, #0x18]
if (!contentView) {
    // fallback: 通过 respondsToSelector 获取 contentView
    // 如果仍然为nil，则调用原始方法并返回
}
cvFrame = [contentView frame];               // 调用 FUN_000c58c0 → 返回 {x,y,w,h}
cvMinX  = CGRectGetMinX(cvFrame);             // d11
cvMinY  = CGRectGetMinY(cvFrame);             // d10
cvMaxX  = CGRectGetMaxX(cvFrame);             // d11 (receiver path)
labelW  = cvFrame.size.width;                 // d9 (实际是label宽度，从d2读取)
labelH  = cvFrame.size.height;                // d8 (实际是label高度，从d3读取)

// ===== isSender 检查 =====
isSender = [sp, #0x3c];                       // w8

// ===== 位置0: 头像上方 (有头像时) =====
if (hasAvatar) {
    // 发送者: X = cvMinX - labelW/2 - 4
    // 3aa00: fmov d0, #0.5  → fmul d0, d2, d0 → fadd d8, d8, d0
    // 3aa04: fmul (labelW * 0.5), then fadd to get X
    // 3aa14: _CGRectGetMaxY → d9 = cvBottom
    // 3abe8: fmov d0, #-0.5 → fmul d0, d3, d0 → fadd d9, d9, d0
    // X = cvMinX - 4 - labelW/2, Y = cvMinY + labelH/2
    
    // 接收者: X = cvMaxX + labelW/2 + 4
    // 3aaf0-3ab18: _CGRectGetMaxX, fmov #0.5, fmul/fadd
    // X = cvMaxX + 4 + labelW/2, Y = cvMinY + labelH/2
}

// ===== 位置0/1 无头像 fallback =====
// sender: X = cvRight + labelW/2, Y = cvBottom - labelH/2
// recv:   X = cvLeft - labelW/2,  Y = cvBottom - labelH/2

// ===== 位置3: 消息下方(远离头像) =====
// sender: X = cvLeft + labelW/2,    Y = cvBottom + labelH/2
//                                 (3aa70: _CGRectGetMinX, fmov #0.5, fmul/fadd)
// recv:   X = cvRight - labelW/2,   Y = cvBottom + labelH/2
//                                 (3aab0: _CGRectGetMaxX, fmov #-0.5, fmul/fadd)

// ===== 位置4: 消息下方(靠近头像) =====
// sender: X = cvRight - labelW/2,   Y = cvBottom + labelH/2
//                                 (3a9f0: _CGRectGetMaxX, fmov #0.5, fmul/fadd)
//                                 (3aa14: _CGRectGetMaxY, fmov #-0.5, fmul/fadd)
// recv:   X = cvLeft + labelW/2,    Y = cvBottom + labelH/2
//                                 (3aa30: _CGRectGetMinX, fmov #0.5, fmul/fadd)

// ===== 位置5: 消息上方(远离头像) =====
// sender: X = cvLeft + labelW/2,    Y = cvTop - labelH/2
//                                  (3a938: _CGRectGetMinX → 上跳转后改成MaxX)
// recv:   X = cvRight - labelW/2,   Y = cvTop - labelH/2
//                                  (3aab0: _CGRectGetMaxX, 3aad4: _CGRectGetMinY)

// ===== 位置6: 消息上方(靠近头像) =====
// sender: X = cvRight - labelW/2,   Y = cvTop - labelH/2
//                                  (3a938: _CGRectGetMinX, 3aa94: _CGRectGetMinY)
// recv:   X = cvLeft + labelW/2,    Y = cvTop - labelH/2
//                                  (3a70: _CGRectGetMinX, then 3aa94: _CGRectGetMinY)

// ===== 第一次 setCenter：跨骑基准位置 =====
// 0x3ac18: bl 0xcae00(label, straddleX, straddleY)
// → setCenter 的跨骑坐标 (d8, d9)，即标签中心 = (cv边缘 ± w/2, cv边缘 ± h/2)

// ===== 读取用户配置偏移值 =====
// 0x3ac24: ldr x20, [sp, #0x18]         → x20 = 配置对象 (ViewModel/config)
// 0x3ac2c: bl 0xc5780(x20, sel_offsetX) → s8 = offsetX (用户设置)
// 0x3ac40: bl 0xc5780(x20, sel_offsetY) → s9 = offsetY (用户设置)

// ===== 第二次 setCenter：应用偏移后的最终位置 =====
// 0x3ac4c: bl 0xc3a60(x28)              → d10 = [x20 unknown_method] (⭐ 关键偏移量，依赖 x20 配置对象)
// 0x3ac58: bl 0xc3a60(x28)              → d0 = [x20 unknown_method] (返回值写入 d0，d1 也可能被设置)
// 0x3ac60: fcvt d0, s8                  → d0 = offsetX (从 s8 扩展为 double)
// 0x3ac64: fneg d3, d0                  → d3 = -offsetX
// 0x3ac68: ldr w8, [sp, #0x3c]         → w8 = isSender
// 0x3ac6c: cmp w8, #0                   → 比较 isSender
// 0x3ac70: fcsel d0, d0, d3, ne        → sender: 用 -offsetX, recv: 用 +offsetX
// 0x3ac74: fadd d0, d0, d10            → newX = (±offsetX) + d10
// 0x3ac78: fsub d1, d1, d2             → newY = d1 - offsetY
// 0x3ac80: bl 0xcae00(label, newX, newY) → 📌 **第二次 setCenter**(最终位置)

// ⚠️ d10 的来源与不确定性
// d10 来自 `bl 0xc3a60` ← 0xc3a60 是一个 PLT shim 函数:
//   FUN_000c3a60: 保存 d0-d10 到栈, x0=x20(配置对象), bl objc_msgSend(x20, unknown_selector)
//   然后恢复 d0-d10, 返回 d0 值写入 d10
// 因为 unknown_selector 无法从静态反汇编确定，d10 的确切语义未知
// 可能代表: 标签基础 X 偏移、气泡扩展宽度补偿、或其他配置对象属性
```

#### 5.2.4 核心公式对比表 (v3.6 更新)

| 位置 | 参数 | 微信优化公式 (反汇编) | 我们的公式 (v3.6) | 差异 | 功能说明 |
|:----:|------|:---------------------|:-------------------|:-----|:---------|
| **0** | sender X | cvMinX - w/2 - **4** | avatarMidX - w/2 | 微信优化用cvMinX+硬编码-4, 我们用avatarMidX | 标签位于气泡/头像上方，sender靠近右侧头像 |
| **0** | recv X | cvMaxX + w/2 + **4** | avatarMidX - w/2 | 同上, 微信优化有+4偏移 | 标签位于气泡/头像上方，receiver靠近左侧头像 |
| **0** | Y | cvMinY + h/2 | avatarTop - h/2 | 微信优化标签下半部分在头像里, 我们标签底部=头像顶部 | 标签纵向位置：紧贴头像/气泡顶部 |
| **2** | sender X/Y | setCenter(cvLeft-w/2, cvBottom-h/2) | setCenter(cvLeft-w/2, cvBottom-h/2) | ✅ 完全一致 | 消息旁边(远离头像)，标签中心骑跨气泡外侧边缘中点 |
| **3** | sender X/Y | setCenter(cvLeft+w/2, cvBottom+h/2) | setCenter(cvLeft+w/2, cvBottom+h/2) | ✅ 完全一致 | 消息下方(远离头像)，标签中心骑跨气泡底边外侧 |
| **4** | sender X/Y | setCenter(cvRight-w/2, cvBottom+h/2) | setCenter(cvRight-w/2, cvBottom+h/2) | ✅ 完全一致 | 消息下方(靠近头像)，标签中心骑跨气泡底边内侧 |
| **5** | sender X/Y | setCenter(cvLeft+w/2, cvTop-h/2) | setCenter(cvLeft+w/2, cvTop-h/2) | ✅ 完全一致 | 消息上方(远离头像)，标签中心骑跨气泡顶边外侧 |
| **6** | sender X/Y | setCenter(cvRight-w/2, cvTop-h/2) | setCenter(cvRight-w/2, cvTop-h/2) | ✅ 完全一致 | 消息上方(靠近头像)，标签中心骑跨气泡顶边内侧 |
| **7** | sender X/Y | setCenter(cvLeft-w/2, cvBottom-h/2) | setCenter(cvLeft-w/2, cvBottom-h/2) | ✅ 完全一致（同位置2） | 消息旁边(=气泡外)，标签中心骑跨气泡外侧边缘中点 |

> **注意 receiver**：以上列出的均为 sender 公式。receiver 的 X 方向取反（如位置2 sender→cvLeft-w/2，receiver→cvRight+w/2）。Y 方向 sender 和 receiver 一致。

> **编号对齐**：v3.7 已将位置编号完全对齐微信优化（3/4 互换，5/6 互换），双方位置编号与语义完全一致，无需映射表。

#### 5.2.5 两次 setCenter 调用详解

微信优化的位置计算核心是一个"两阶段 setCenter"架构，这在整个消息时间功能中是独一无二的：

```
┌─────────────────────────────────────────────────────────────────────┐
│ 阶段 1: 跨骑基准 setCenter (0x3ac18)                                │
│                                                                       │
│   对每个位置，计算出标签中心的"理论跨骑坐标"：                         │
│   · d8 = cv边缘 ± labelWidth/2   (如 sender pos3: cvRight - w/2)    │
│   · d9 = cv边缘 ± labelHeight/2  (如 pos3:      cvBottom + h/2)    │
│                                                                       │
│   → bl 0xcae00(label, d8, d9)    // setCenter(理论跨骑X, 理论跨骑Y) │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 中间步骤: 读取用户配置偏移 (0x3ac2c-0x3ac58)                        │
│                                                                       │
│   · 0x3ac2c: bl 0xc5780(x20, sel_offsetX) → s8 = offsetX           │
│   · 0x3ac40: bl 0xc5780(x20, sel_offsetY) → s9 = offsetY           │
│   · 0x3ac4c: bl 0xc3a60(x28)              → d10 = [x20 ???](⭐)    │
│   · 0x3ac58: bl 0xc3a60(x28)              → (d0, d1) = [x20 ???]   │
│                                                                       │
│   x20 = 配置对象 (自 [sp, #0x18], 经由调用链 objc_retain 持有)       │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ 阶段 2: 应用偏移后的最终 setCenter (0x3ac80)                        │
│                                                                       │
│   · newX = (±offsetX) + d10          // sender: -offsetX, recv: +offsetX
│   · newY = d1 - offsetY              // 统一向上偏移
│                                                                       │
│   → bl 0xcae00(label, newX, newY)    // setCenter(最终X, 最终Y)     │
└─────────────────────────────────────────────────────────────────────┘
```

**d10 的来源分析**：

`d10` 来自 `bl 0xc3a60` 调用。通过反编译分析：
- `0xc3a60` 是一个 PLT stub，内部调用 `objc_msgSend(x20, unknown_selector)`
- `x20` 是配置对象（可能是 ViewModel 或 config 对象），从 `[sp, #0x18]` 载入
- 该 selector 名称无法从静态反汇编确定（缺少符号信息）
- `d10` 的含义可能是：标签基础 X 偏移、气泡宽度补偿、或配置对象的某个几何属性

> ⚠️ **当前限制**：由于 `d10` 来自运行时配置对象的方法调用返回值，静态分析无法确定其确切语义。要完全还原微信优化的位置公式，需要运行时 hook（如 Frida）拦截 `0xc3a60` 的调用，记录 `x1`(selector) 和 `d0`(返回值) 来确定 d10 的含义。

#### 5.2.6 编号对齐说明 (v3.7)

v3.7 已将位置编号完全对齐微信优化。此前编号存在交叉映射（3↔4, 5↔6），现已交换为与微信优化一致的编号方案：

| 位置 | 名称 | 公式 (sender) | 说明 |
|:----:|------|:------------|------|
| 3 | 消息下方(远离头像) | cvLeft+w/2 | GetMinX 侧，外侧对齐 |
| 4 | 消息下方(靠近头像) | cvRight-w/2 | GetMaxX 侧，内侧对齐 |
| 5 | 消息上方(远离头像) | cvLeft+w/2 | GetMinX 侧，外侧对齐 |
| 6 | 消息上方(靠近头像) | cvRight-w/2 | GetMaxX 侧，内侧对齐 |

编号 0, 1, 2, 7 双方一致，无需调整。

> ⚠️ **迁移兼容性**：`NSUserDefaults` 中存储的 `messageTimePosition` 值会随编号变化而改变语义。旧用户升级后需要在设置中重新选择一次位置。旧值 0, 1, 2, 7 语义不变，旧值 3, 4, 5, 6 会自动切换到新语义。

#### 5.2.7 偏移量处理对比

```
微信优化 (反汇编确认):
  offsetX_user = NSUserDefaults[@"com.wechat.enhance.messageTime.offsetX"]
  if (isSender) offsetX = -offsetX_user       // sender: 负向偏移
  else          offsetX = +offsetX_user        // receiver: 正向偏移
  finalX = computedX + offsetX
  finalY = computedY - offsetY_user            // offsetY: 统一向上(Y减小)
  setFrame(finalX, finalY, labelW, labelH)

我们的实现 (v3.4):
  offsetX_user = config.messageTimeOffsetX
  if (isSender) finalX = computedX - offsetX_user   // 同上
  else          finalX = computedX + offsetX_user   // 同上
  finalY = computedY - offsetY_user                  // 同方向
  setFrame(finalX, finalY, labelW, labelH)
  
差异: 偏移量处理逻辑基本一致
```

### 5.3 位置计算流程对比

```
微信优化 1.6.5 (反汇编确认):
  willDisplayCell → dispatch_async
    → KVC "m_contentView" 获取 frame         (反汇编: 0x3906c)
    → isSenderFromMsgWrap:wrap                (反汇编: 0x39030-0x39034)
    → 获取头像 (getHeadImageView / KVC)
    → switch(position) 选择 X/Y 公式          (反汇编: 多分支 0x3a700-0x3abe8)
      → 全部使用 0.5 乘数 (w/2, h/2)         (反汇编: fmov #0.5 / fmov #-0.5)
      → 位置0 有额外 ±4.0 偏移               (反汇编: fmov #4.0 / fmov #-4.0)
    → 应用用户 offsetX/offsetY               (反汇编: 0x3ac60-0x3ac80)
    → setFrame → 设置标签位置
```

```
我们的实现（v3.6 — setCenter 跨骑策略）:
  【主入口】willDisplayCell（dispatch_async）
    → addTimeLabelToCell(cell)
      → getContentView(cell) — KVC m_contentView / subview 遍历
      → getAvatarView(cell) — MMHeadImageView
      → computeLabelFrame (位置0/1：setFrame + 头像/fallback基准)
      → 位置2-7: switch-case 直接 setCenter 跨骑
         → position 2/7: center = (cvLeft-w/2, cvBottom-h/2) sender
         → position 3:   center = (cvLeft+w/2, cvBottom+h/2) sender
         → position 4:   center = (cvRight-w/2, cvBottom+h/2) sender
         → position 5:   center = (cvLeft+w/2, cvTop-h/2) sender
         → position 6:   center = (cvRight-w/2, cvTop-h/2) sender
        → receiver: X 方向取反
      → 应用用户 offsetX/offsetY 到 center
      → label.bounds = (0,0,w,h); label.center = finalCenter
  【重定位】CommonMessageCellView.updateNodeStatus
    → quickRelocateTimeLabel — 同上 switch-case setCenter 重算
  【清理】ChatTableViewCell.prepareForReuse → 关联对象清 nil
```

### 5.4 关键差异总结 (v3.6 更新)

| 差异点 | 微信优化 1.6.5 | 我们的实现 (v3.6) | 影响 | 功能说明 |
|--------|:-------------:|:-----------------:|------|:---------|
| **位置0 头像基准** | X = cvMinX/cvMaxX ± w/2 ± 4 | X = avatarMidX - w/2 | 头像中心 vs 气泡边缘基准不同 | "头像上方"标签的水平对齐参照物 |
| **定位方式** | **setCenter** (确认) | **setCenter** (位置2-7 已对齐) | ✅ 完全对齐 | 使用 center 属性定位而非 frame.origin |
| **位置2-7 公式** | Straddle: center = (cv边缘 ± w/2, cv边缘 ± h/2) | Straddle: center = (cv边缘 ± w/2, cv边缘 ± h/2) | ✅ **完全一致** | 所有 6 个位置均使用 0.5 乘数跨骑策略 |
| **位置0/1 公式** | Straddle: ± w/2, ± h/2 + ±4 偏移 | setFrame: avatarMidX-w/2 ± 偏移 | ⚠️ 保持旧实现 | 头像上/下方因依赖头像视图，暂未切换 setCenter |
| **两阶段 setCenter** | ✅ 第一次 setCenter(跨骑) → 第二次 setCenter(+偏移) | ❌ 单阶段 setCenter(+偏移) | 功能等价 | 微信优化先设跨骑位置再叠加偏移，我们直接在最终 center 上叠加 |
| **d10 神秘偏移** | ✅第二次 setCenter 的 X = d10 ± offsetX | ❌ 无对应 | d10 语义未知 | 来自 x20 配置对象的方法调用，静态分析无法确定 |
| **bounds 修复** | N/A (UILabel 实例天然有 frame) | ✅ 每次 setCenter 前设 bounds=CGRectMake(0,0,w,h) | v3.6 bug 修复 | 解决 `[[UILabel alloc] init]` 初始 frame 为零导致 setCenter 后标签不可见的问题 |

> **setCenter 实现注意事项 (v3.6 bug 修复记录)**：
> 
> 1. **bounds 修复**：`initTimeLabel` 创建 `[[UILabel alloc] init]`，初始 frame 为 `CGRectZero`。直接设置 `label.center = point` 不会改变 label 的 frame.size（仍为零），导致标签不可见。修复方法：每次 setCenter 前设置 `label.bounds = CGRectMake(0, 0, w, h)`。
> 
> 2. **sender/receiver 方向修正**：初始实现中所有 switch-case 的 `isSender ? A : B` 三元表达式的 sender/receiver 方向全部写反。已修正。
> 
> 3. **d10 未知偏移**：微信优化的第二次 setCenter 中的 `d10` 值来自 `[x20 unknown_selector]`（x20 为微信内部配置对象），selector 名称无法从静态反汇编确定。当前我们的实现省略此步骤，直接使用跨骑坐标 + 用户偏移量作为最终位置。

### 5.5 边界安全

| 安全措施 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.6） | 功能说明 | 谁更好 |
|---------|:-------------:|:-------------:|:---------:|----------|:------:|
| Y 轴钳制 | ❌ 未发现 | 未知（二进制无法确认） | ✅ `maxY = cellHeight - labelHeight - 2` (仅上限) | 防止标签超出 cell 底部边界 | 🏆 我们（显式钳制最安全） |
| X 轴安全 | ❌ 未发现 | 未知 | ✅ 方向感知偏移 (left/right) | 根据发送者/接收者方向调整标签 X 偏移 | 🏆 我们（方向感知避免遮挡） |
| 气泡扩展 | ✅ `bubbleExtensionWidth` 配置 | 未知 | ❌ 未实现（位置2-7 setCenter straddle 后无文字重叠风险） | 扩展气泡宽度以适配标签位置 | 🏆 1.6.5（有配置但非必需） |

### 5.6 发送者/接收者检测

| 方法 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|------|:-------------:|:-------------:|:---------:|----------|:------:|
| 检测方式 | `isSenderFromMsgWrap:wrap` + 气泡位置 fallback | 未知（推测类似，二进制有 `isSenderFromMsgWrap:` 符号） | 同左（三层 fallback） | 判断消息是发送者还是接收者 | 相当（三家都用 `isSenderFromMsgWrap:` 核心方法） |
| 配色区分 | ✅ sender/receiver 独立 textColor 和 backgroundColor | ✅ `settingMessageTime:` 方法控制格式/颜色 | ✅ 同左 | 发送者和接收者的时间标签使用不同配色 | 🏆 我们/1.6.5（独立颜色配置最细粒度） |

---

## 六、设置和配置

### 6.1 用户默认键对比

| 配置项 | 微信优化 1.6.5 (NSUserDefaults key) | 我们的实现（v3.1, PluginConfig） | 功能说明 | 谁更好 |
|--------|:-----------------------------------:|:------------------------:|----------|:------:|
| 启用/关闭 | `com.wechat.enhance.messageTime.enabled` | `showMessageTime` | 总开关：控制消息时间功能是否生效 | 相当 |
| 位置模式 | `com.wechat.enhance.messageTime.position` | `messageTimePosition` | 8 种标签显示位置（头像上方/下方、气泡旁边等） | 相当 |
| 字体大小 | `com.wechat.enhance.messageTime.fontSize` | `messageTimeFontSize` | 时间标签字体大小（pt） | 相当 |
| 粗体 | `com.wechat.enhance.messageTime.boldFont` | `messageTimeBoldFont` | 是否使用粗体显示时间 | 相当 |
| 自定义格式 | `com.wechat.enhance.messageTime.customFormat` | `messageTimeFormat` | NSDateFormatter 格式字符串（如 HH:mm:ss） | 相当 |
| X 偏移 | `com.wechat.enhance.messageTime.offsetX` | `messageTimeOffsetX` | 标签水平方向的微调偏移量 | 相当 |
| Y 偏移 | `com.wechat.enhance.messageTime.offsetY` | `messageTimeOffsetY` | 标签垂直方向的微调偏移量 | 相当 |
| 气泡扩展宽度 | `com.wechat.enhance.messageTime.bubbleExtensionWidth` | `messageTimeBubbleExtWidth` | 为容纳标签而扩展气泡的额外宽度 | 🏆 1.6.5（原生支持，我们 v3.0 移除后不再需要） |
| 圆角 | `com.wechat.enhance.messageTime.cornerRadius` | `messageTimeCornerRadius` | 时间标签背景的圆角半径 | 相当 |
| 发送者文字色 | `com.wechat.enhance.messageTime.sender.textColor` | `senderTextColorHex` | 自己发出的消息的时间标签文字颜色 | 相当 |
| 发送者背景色 | `com.wechat.enhance.messageTime.sender.backgroundColor` | `senderBackgroundColorHex` | 自己发出的消息的时间标签背景颜色 | 相当 |
| 接收者文字色 | `com.wechat.enhance.messageTime.receiver.textColor` | `receiverTextColorHex` | 收到的消息的时间标签文字颜色 | 相当 |
| 接收者背景色 | `com.wechat.enhance.messageTime.receiver.backgroundColor` | `receiverBackgroundColorHex` | 收到的消息的时间标签背景颜色 | 相当 |
| 暗色模式文字色 | `...textColor.dark` | 自动暗色转换 `autoDarkColor()` | 深色模式下的标签文字颜色 | 🏆 我们（自动暗色转换，无需单独配置） |
| 暗色模式背景色 | `...backgroundColor.dark` | 自动暗色转换 | 深色模式下的标签背景颜色 | 🏆 我们（自动暗色转换，无需单独配置） |
| 后缀格式 | `com.wechat.enhance.chat.addtime.suffix.format` | ✅ 支持 | 联系人列表中好友添加时间后缀的格式 | 🏆 1.6.5（独立配置键，更灵活） |
| 标签宽度自适应 | ❌ | ✅ `disableLabelWidthAdjustment` | 控制标签是否根据文字长度自动调整宽度 | 🏆 我们（独家功能） |

### 6.2 时间格式

| 特性 | 微信优化 1.6.5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|------|:-------------:|:-----------------:|----------|:------:|
| 格式语法 | NSDateFormatter 标准格式 | NSDateFormatter 标准格式（v2.0 改为与 1.6.5 一致） | 时间显示格式的定义方式 | 相当（均使用系统标准格式） |
| 后缀 | ✅ `addTimeSuffixFormatItem` 可配置 | ❌ | 时间文本后可追加的后缀内容 | 🏆 1.6.5（独家功能） |
| 示例 | `HH:mm:ss` | `HH:mm:ss` | 默认时间格式示例 | 相当 |

---

## 七、气泡和头像获取

### 7.1 气泡视图获取路径对比

| 优先级 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|:------:|:-------------:|:-------------:|:---------------------:|----------|:------:|
| 1 | `getBgImageView` 方法 | ❌ 不获取 | `getBgImageView` 方法 | 公开方法调用，最快最可靠 | 🏆 微信助手（不需要气泡，架构更简） |
| 2 | KVC `m_bgImageView` | ❌ 同上 | KVC `m_bgImageView` / `bgImageView` | KVC 穿透私有 ivar，作为公开方法失败的 fallback | — |
| 3 | ❌ 无（strings 确认） | ❌ 同上 | ❌ 已移除（v3.0 起） | 子视图遍历兜底 | — |
| 4 | ❌ 同上 | ❌ 同上 | ❌ 已移除（v3.0 起） | 最终兜底 | — |
| 5 | `setFrameForBgImageView:` | ❌ | ❌ | 修改气泡 frame（如扩展气泡宽度） | 🏆 1.6.5 |
| 6 | `convertRect:toView:` / `convertRect:fromView:` | ❌ | ❌ **已舍弃（v3.1 改用直接 frame）** | v3.1 坐标空间统一：cellView bounds + 直接 frame，无需转换 | 🏆 v3.1（免转换开销） |

> **微信优化 1.6.5 的深度分析结论（基于二进制逆向）**：
>
> **气泡获取路径（3 级，均可确认 → 第 3 级为非获取路径的修正能力）**：
> - **第 1 级**：`getBgImageView` — 公开方法调用，最可靠。二进制字符串偏移 `0xf2358`
> - **第 2 级**：KVC `m_bgImageView` — KVC 穿透私有 ivar。字符串偏移 `0xe3258`
> - **第 3 级（非获取路径）**：`setFrameForBgImageView:` — 这不是获取路径，而是获取后的 frame 修改方法。二进制字符串偏移 `0xf7160`。当气泡 frame 需要调整时（如 `bubbleExtensionWidth` 扩展气泡宽度），他们直接调用此方法修改气泡的 frame，而不是像我们的实现那样临时修改 `bubbleView.frame.size.width` 再在下次触发生效。**这种做法更可靠**。
>
> **关于 subviews 遍历**：二进制中有 `subviews`、`isKindOfClass:`、`respondsToSelector:`、`_objc_getClass`、`_objc_opt_isKindOfClass`、`_objc_opt_respondsToSelector` 等运行时原语，但**未发现任何用于气泡检测的类名字符串字面量**（如 "BgImage"、"Bubble"、"MessageView"、"RichTextView"）。仅有的 Bubble 类名 `VoIPBubbleMessageCellView` 是完整的类名字面量（用于 `NSClassFromString` 或弹框），不是 subviews 遍历时的子字符串匹配。因此 **1.6.5 没有 subviews 遍历作为气泡检测兜底**。子视图兜底是他们有能力做但实际未做的路径。
>
> **坐标系安全**：1.6.5 使用了 `convertRect:toView:` 和 `convertRect:fromView:` 进行坐标转换。这意味着当气泡 frame 是相对坐标时，他们会转换到 cell 的坐标系再计算 label 位置，不存在坐标系混用问题。这是架构层面的优势。
>
> **对比我们的历史问题（v2.0 已修复）**：旧版 `getBubbleView` 返回 bubble frame 后在 `addTimeLabelToCell` 中直接使用，要么是 `cellFrame`（绝对坐标），要么是 `bubbleView.frame`（相对坐标），两套坐标系混用。v3.1 统一采用 cellView bounds 坐标空间 — `bubbleFrame`/`avatarFrame` 直接使用 `bubbleView.frame`/`avatarView.frame`（两者均为 cellView 直接子视图，frame 天然在同一空间），`cellFrame` 改用 `cellView.bounds`，三方坐标完全一致。
>
> **获取时机一致性**：1.6.5 在 `willDisplayCell`（cell 即将显示）、`updateNodeStatus`（viewModel 数据更新）、`didMoveToWindow`（窗口可见性变化）三个时机操作标签。**关键：三个时机都不是 `layoutSubviews` 热路径**。我们的架构已对齐微信优化：`willDisplayCell`（初始创建）+ `updateNodeStatus`（模型驱动刷新/创建）+ `didMoveToWindow`（窗口可见性补位），所有 hook 点均为非热路径。
>
> **坐标系安全**：v3.1 采用"直接 frame"策略——`bubbleFrame = [(UIView *)bubbleView frame]`、`avatarFrame = [(UIView *)avatarView frame]`、`cellFrame = [(UIView *)cellView bounds]`。三者天然在同一个坐标空间（cellView），无需任何 `convertRect:` 转换。相较于先前的 convertRect 方案，免去了坐标转换开销且消除了跨空间比较导致的偏移误差。

### 7.2 头像视图获取路径对比

| 优先级 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|:------:|:-------------:|:-------------:|:---------------------:|----------|:------:|
| 1 | `getHeadImageView` 方法 | `getHeadImageView` 方法 | `getHeadImageView` 方法 | 公开方法调用，最快最可靠 | 🏆 微信助手（不依赖头像定位） |
| 2 | KVC `headImageView` | `getContactHeadImage` / `getHeadImage:withCategory:` | KVC `m_headImageView` / `headImageView` | KVC 穿透私有 ivar，公开方法失败时的 fallback | 相当 |
| 3 | `MMHeadImageView` 类引用 | KVC `headImg` / `avatarImage` | ❌ 已移除（v3.0 起） | 类判断兜底 | — |
| 4 | — | `setHeadImageByName:` / `setHeadImg:` | ❌ | — | — |

> **微信优化 1.6.5 头像获取路径确认（strings 分析）**：
> - **第 1 级**：`getHeadImageView` — strings 确认存在，公开方法，最快最可靠
> - **第 2 级**：KVC `headImageView` — strings 确认存在 `m_headImageView`（私有 ivar），穿透访问
> - **无子视图遍历**：strings 中无 `MMHeadImageView` 类名字面量用于子视图匹配，1.6.5 仅 2 级原生路径
> - **与气泡获取一致**：getBgImageView + KVC m_bgImageView（2 级），getHeadImageView + KVC m_headImageView（2 级），均为轻量原生访问

> **v3.1 变更**：头像获取路径保持 2 级（`getHeadImageView` → KVC `m_headImageView` / `headImageView`），延续 v3.0 移除子视图遍历的方向。v3.1 新增 `refreshLabelFrameInCellView` 在 `CommonMessageCellView.layoutSubviews` 和 `viewDidLayoutSubviews` 中使用同一套轻量头像获取逻辑。

---

## 八、日志和调试

| 特性 | 微信优化 1.6.5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|------|:-------------:|:---------:|----------|:------:|
| 日志类型 | `NSLog` / 内部日志 | `mtLog` 宏（可开关，热路径零日志） | 运行时日志输出的方式 | 🏆 我们（可开关 + 热路径零开销） |
| 日志粒度 | 基础（创建/更新/位置） | 启动时配置打印 + install 状态，热路径零日志 | 日志的详细程度和触发频率 | 🏆 我们（启动时全量 + 运行时无干扰） |
| 运行时配置检查 | 启动时 | 启动时 `mtLog` 打印当前配置 | 运行中检查当前配置状态的方式 | 相当 |
| 错误处理 | `@try/@catch` | `@try/@catch` + 静默吞异常 | 异常保护机制 | 🏆 我们（静默吞异常，不影响用户体验） |
| ~~布局循环检测~~ | ❌ | ❌ 无需检测（v3.1 layoutSubviews 仅 setFrame 刷新，无级联风险） | 是否检测布局级联爆炸 | 相当（均无需检测） |

---

## 九、Hook 引擎选型对比

| 引擎特性 | 微信优化 1.6.5 (Substrate) | 微信助手 3.9-5 (Runtime) | 我们的实现 (内联 Substrate) | 功能说明 | 谁更好 |
|---------|:------------------------:|:-----------------------:|:--------------------------:|----------|:------:|
| 具体 API | `MSHookMessageEx` | `class_addMethod` + `method_setImplementation` | `MSHookMessageEx`（内联实现，约 10 行） | 底层方法拦截使用的具体 API | 🏆 我们/1.6.5（MSHookMessageEx 自动保存 IMP） |
| 外部依赖 | 自带 `CydiaSubstrate.framework`（`@rpath`） | 无（纯 ObjC Runtime） | 无（`CydiaSubstrate.m` 编译进主 dylib，零外部依赖） | 是否需要额外的 framework 或动态库 | 🏆 我们/微信助手（零外部依赖） |
| 依赖体积 | 约 200KB | 0 | ~1KB（源码编译后） | 额外引入的二进制体积 | 🏆 我们（1KB 最轻量） |
| Hook 管理 | 每个 hook 点单独调用 | `checkHook:`/`checkHookWithSeq:` 内部调度 | `MTHookEntry` 配置表驱动 | 多个 hook 点的组织和管理方式 | 🏆 我们（配置表编译时可见，最直观） |
| 能否跨类批量 hook | 手动逐个 hook | 内部配置表驱动 | 配置表驱动 | 是否支持一次操作 hook 多个类的方法 | 🏆 我们/微信助手（配置驱动批量） |
| 能否运行时开关 | ❌ | ✅ `checkHook:` 可控制 | ❌（编译时确定） | 运行时动态启用/禁用特定 hook | 🏆 微信助手（运行时灵活控制） |
| 原始 IMP 保存 | 自动 | 手动 | 自动（MSHookMessageEx 处理） | 是否自动保存原始实现指针以便调用原始方法 | 🏆 我们/1.6.5（自动保存不会遗漏） |
| 缓存刷新 | ✅ MSHookMessageEx 自动处理 | ❌ method_setImplementation 不刷新 | ✅ MSHookMessageEx 自动处理 | 方法缓存是否在 hook 后自动刷新 | 🏆 我们/1.6.5（自动刷新避免调用旧 IMP） |
| 稳定性 | 久经考验，稳定 | 依赖内部管理逻辑 | 内联实现简洁可控 | 在各种 iOS 版本和场景下的可靠性 | 🏆 1.6.5（经大量用户验证） |
| iOS 版本兼容性 | 所有越狱版本 | 所有版本 | 所有版本 | 支持的 iOS 版本范围 | 相当（三家全兼容） |
| 非越狱兼容 | ✅（自带 `CydiaSubstrate.framework`，`@rpath` 加载） | ✅ | ✅（内联编译，无额外加载） | 非越狱环境下的可用性 | 🏆 我们/微信助手（无额外加载步骤） |
| 是否自动适应环境 | 固定 Substrate（自带 framework） | 固定 Runtime（NOUNDEFS 自包含） | 无需适应（MSHookMessageEx 始终可用） | 在不同构建环境中的适配能力 | 🏆 我们（双环境自动适配） |

### 关键结论

> 当前版本采用了**最简洁的方案**：自定义 `CydiaSubstrate.m`（约 10 行）编译进主 dylib，提供 `MSHookMessageEx` 实现。Theos 环境下走系统 CydiaSubstrate，独立构建环境用自己的内联版本——**两种环境都不需要额外加载 framework，零外部依赖**。

微信优化 1.6.5 思路相同（自带 Substrate），区别在于它打包成独立 framework（`@rpath`），我们直接编译进 dylib，更简洁，少一层加载链路。

微信助手 3.9-5 走纯 Runtime 路线，虽然零依赖但缺少 MSHookMessageEx 的原始 IMP 自动保存和缓存刷新能力。

---

## 十、总结

### 微信优化 1.6.5 的优势

1. **成熟稳定**：使用 MSHookMessageEx，经大量用户验证
2. **功能全面**：messages time 只是几十个功能之一，集成度高
3. **设置丰富**：支持后缀格式、暗色模式配色等细节
4. **无布局循环（架构级）**：核心 hook 点是 `updateNodeStatus`（ViewModel 驱动）和 `didMoveToWindow`（窗口可见性），两个回调都不在 `layoutSubviews` 滚动热路径上。Ghidra 二进制确认 `CommonMessageCellView.layoutSubviews` **未用于消息时间功能**——这是它不卡死的根本原因

### 微信助手 3.9-5 的参考价值

1. **轻依赖**：用 `class_addMethod` + `method_setImplementation`，零外部依赖
2. **内部 Hook 管理**：`checkHook:` / `checkHookWithSeq:` 提供运行时开关能力，值得借鉴
3. **`msgTimeLabel` 实现**：通过 `addMessageNode:layout:addMoreMsg:addNewMsg:` 在消息节点插入时添加标签 → **正确的架构选择**。标签通过 `class_addMethod` 运行时注入 getter/setter（底层关联对象），而非编译时 ivar
4. **功能庞大**：13MB 的二进制，消息时间只是众多功能之一
5. **无布局循环**：不 hook `CommonMessageCellView.layoutSubviews`，天然避免级联

### 我们的优势（架构对齐微信优化 1.6.5）

1. **架构对齐**：已从 Layout 驱动（`layoutSubviews` hook）迁移到 ViewModel 驱动（`updateNodeStatus` + `didMoveToWindow`），与微信优化 1.6.5 采用完全相同的架构范式。不再参与 layoutSubviews 热路径，彻底消除滚动卡死和多插件冲突问题
2. **批量安全网**：`viewDidLayoutSubviews` 遍历 visibleCells 统一校验标签位置，兜底所有遗漏
3. **长消息去重**：`parentModel.m_subViewModels` 检测 — 一条长消息的 N 个 sub-cell 仅第一个创建标签
4. **坐标空间统一**：cellFrame/bubbleFrame/avatarFrame 全部统一到 cellView 坐标空间，消除坐标系混用
5. **正确的触发时机**：主入口 `willDisplayCell` + 模型驱动 `updateNodeStatus` + 窗口补位 `didMoveToWindow` — 三条非热路径，与微信优化完全一致
6. **配置表架构**：`MTHookEntry` 配置表驱动，新增 hook 只需加一行
7. **selector key 直接关联对象**：用 `sel_msgTimeLabel` 选择器替代 `@"msgTimeLabel"` 字符串做关联对象 key，防拼写错误，直接读写无中间层，与微信优化同方式，比微信助手的 class_addMethod 包装更简洁稳定
8. **无 ARC 参数安全隐患**：v3.2 移除 `addMessageNode` hook，避免 C 函数中 `id` 参数被 MSHookMessageEx trampoline 后的 ARC retain/release 破坏

### 各自的架构选择

| 决策 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现（v3.1） | 功能说明 | 谁更好 |
|------|:-------------:|:-------------:|:-----------------:|----------|:------:|
| Hook 引擎 | Substrate (MSHookMessageEx) | 纯 ObjC Runtime (class_addMethod) | MSHookMessageEx（内联 CydiaSubstrate.m） | 底层方法拦截引擎的选择 | 🏆 我们（Substrate + 内联零依赖） |
| **核心触发时机** | willDisplayCell + updateNodeStatus（核心 label 管理）+ didMoveToWindow（补充）+ viewDidLayoutSubviews（批量校验） | **addMessageNode:layout:addMoreMsg:addNewMsg:** ✅ | **willDisplayCell**（与微信优化一致，v3.2 移除 addMessageNode） | 消息时间标签创建的主要触发回调 | 🏆 微信优化（updateNodeStatus 模型驱动，避热路径） |
| 补充触发 | viewDidLayoutSubviews（VC 级布局完成，Ghidra 确认） | willDisplayCell | viewDidLayoutSubviews（批量安全网，兜底刷新） | 主入口 willDisplayCell 之外的兜底刷新机制 | 🏆 我们/微信优化（viewDidLayoutSubviews 安全网） |
| **布局刷新** | CommonMessageCellView.updateNodeStatus：标签存在→setFrame，不存在→创建（模型驱动，不参与滚动热路径） | ❌ 不使用 | CommonMessageCellView.updateNodeStatus：标签存在→setFrame，不存在→创建（与微信优化一致） | cell 布局变化时标签 frame 的刷新方式 | 🏆 双方一致（updateNodeStatus 非热路径） |
| **延迟策略** | **dispatch_async**：willDisplayCell → dispatch_async → initTimeLabel（Ghidra 确认） | addMessageNode 视图已就绪，无需延迟 | **dispatch_async**：willDisplayCell → dispatch_async → addTimeLabelToCell（与微信优化一致） | 确保视图布局完成后才操作标签的延迟机制 | 🏆 双方一致（dispatch_async 简单可靠） |
| ivar 访问方式 | class_copyIvarList / class_getInstanceVariable 运行时内省（Ghidra 确认） | class_addMethod 运行时注入方法，通过关联对象间接访问（非编译时属性） | respondsToSelector + KVC valueForKey | 访问微信私有变量的方式 | 🏆 1.6.5（运行时内省最灵活） |
| 布局安全 | dispatch_async 延迟 + updateNodeStatus 模型驱动（不在 layoutSubviews 调用链中） | **天然无循环**（不 hook layoutSubviews） | **已对齐微信优化**：updateNodeStatus + didMoveToWindow + willDisplayCell，无 layoutSubviews hook | 防止布局级联爆炸的策略 | 🏆 双方一致（架构层面避免，最彻底） |
| 视图访问 | getBgImageView/getHeadImageView（原生） | 未知 | getBgImageView/getHeadImageView（模仿微信优化） | 获取气泡视图和头像视图的方式 | 🏆 我们/1.6.5（原生方法调用最快） |
| 位置方案 | 8 位置模式 | 未知 | 8 位置模式（统一 cellView 坐标空间） | 标签位置的计算方案 | 🏆 我们（坐标空间统一，无偏移误差） |
| 消息时间标签 | `m_timeLabel` | `msgTimeLabel`（class_addMethod 注入 + 关联对象） | `msgTimeLabel`（selector key + 直接关联对象，类微信优化，tag=999999 兜底） | 标签引用和标识方式 | 🏆 我们（最简洁，零中间层） |
| 数据访问层级 | ViewModel 层：parentModel.m_subViewModels → m_uiCreateTime（Ghidra 确认） | 运行时注入方法调用，底层走关联对象 | View 层 + ViewModel 层：4 级 fallback | 从 UI 到数据的穿透层级 | 🏆 1.6.5（ViewModel 层直接访问最快） |
| 长消息处理 | 每个 sub-cell 都创建标签（CommonMessageCellView 层） | 未知 | 🆕 去重：仅第一个 sub-cell 创建标签 | 长消息（多 sub-cell 拼接）的标签创建策略 | 🏆 我们（去重避免 N 个重复标签） |
| 运行时 Hook | MSHookMessageEx + class_addMethod 动态注入（Ghidra 确认） | class_addMethod + method_setImplementation | MSHookMessageEx（零外部依赖） | 具体 hook 实现方式 | 🏆 我们（MSHookMessageEx + 零依赖 + 配置表） |
| 可维护性 | 中等（二进制分散） | 低（13MB 单体） | 高（源码 + 配置表） | 代码维护的难易程度 | 🏆 我们（源码清晰 + 配置表驱动） |

> **架构对齐完成**：通过 Ghidra 反编译确认微信优化 1.6.5 实际 hook 的是 `updateNodeStatus` 和 `didMoveToWindow`（而非 `layoutSubviews`），我们已将 MessageTime 模块从 Layout 驱动迁移到 ViewModel 驱动。移除 `CommonMessageCellView.layoutSubviews` hook，新增 `updateNodeStatus` + `didMoveToWindow` hook，与微信优化采用完全相同的架构范式——所有标签操作均不在 `layoutSubviews` 滚动热路径上。从根本上解决了滚动卡死和多插件冲突（WCRefine 0x8BADF00D）问题。

---

## 附录：分析可信度说明

由于微信优化 1.6.5 和微信助手 3.9-5 只能通过二进制逆向分析（`strings` + `nm` + `llvm-objdump`），无法看到运行时日志：

| 可信度标记 | 含义 |
|:---------:|------|
| ✅ | 源码确认 — 我们自己的代码，100% 准确 |
| 🔵 | 二进制实锤 — 从 `strings`/`nm` 中看到明确的类名、方法名、Selector，有符号表支撑 |
| 🟡 | 二进制推断 — 从符号表 + 上下文推测的逻辑，可能有误 |
| ❌ | 无法确认 — 二进制中未找到相关证据 |

> 🟡 微信助手 3.9-5 的分析尤其受限：`checkHook:` 的 hook 目标是用字符串运行时查找的，不编译到符号表中。`msgTimeLabel` 的 `class_addMethod` + 关联对象实现是通过 `_class_addMethod` / `_objc_setAssociatedObject` / `_objc_getAssociatedObject` 等运行时导入推断的。**最准确的验证方式是运行时拦截**：对 `class_addMethod` / `method_setImplementation` / `MSHookMessageEx` 二次 hook，记录所有调用参数后统一对比。

---

## 附录 B：微信优化 1.6.5 全部 Hook 目标清单

> **分析方法**：对 dylib 进行全量 `strings` 提取（6,494 条），交叉匹配 WeChat 内部类名（111 个）+ 方法选择器（36 个 hook 相关 selector），结合 NSUserDefaults 模块 key（com.wechat.enhance.*）和 Ghidra 反编译数据库逐条验证。
>
> **可信度标记**：
> - 🔵 **确认**：Ghidra 反编译 + dylib strings 双源确认，或 Ghidra 直接解析到父类 IMP 调用
> - 🔹 **实锤**：dylib strings 中类名和方法 selector 同时存在，结合模块功能可合理确认
> - 🟡 **推断**：类名和方法 selector 存在但无 Ghidra 直接证据，依据模块架构和通用 hook 模式推断

### B.1 消息时间模块（7 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `BaseMsgContentViewController` | `tableView:willDisplayCell:forRowAtIndexPath:` | 🔵 确认 | 标签创建触发：cell 即将显示时 `dispatch_async` 延迟创建标签 | — |
| 2 | `BaseMsgContentViewController` | `viewDidLayoutSubviews` | 🔵 确认 | 批量安全网：VC 级布局完成后遍历 visibleCells 刷新标签 frame | — |
| 3 | `CommonMessageCellView` | `updateNodeStatus` | 🔵 确认 | ⭐ **核心架构选择**：viewModel 数据更新时刷新/创建 `m_timeLabel`。不在 `layoutSubviews` 滚动热路径上（Ghidra FUN_0000a8dc.c 确认） | — |
| 4 | `CommonMessageCellView` | `didMoveToWindow` | 🔵 确认 | 窗口可见性时处理：cell 进入/离开可视区域时的补充入口（Ghidra FUN_0003fac8.c 确认），同样不在热路径上 | — |
| 5 | `ChatTimeCellView` | `layoutSubviews` | 🔹 实锤 | 隐藏系统时间分隔线：设置 `hidden=YES` | — |
| 6 | `ChatTimeViewModel` | `cellHeight` | 🔹 实锤 | 时间分隔线高度归零：返回 0 或极小值 | — |
| 7 | `CContact` | `m_nsNickName` | 🔹 实锤 | 好友时间后缀：在联系人昵称后追加 `addTimeSuffixFormat` | — |

### B.2 防撤回模块（5 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `CMessageMgr` | `onRevokeMsg:` | 🔹 实锤 | 拦截撤回消息主入口：阻止消息被撤回 | — |
| 2 | `CMessageMgr` | `onNewSyncNotAddDBMessage:` | 🔹 实锤 | 拦截同步消息中的撤回指令（不入库通道） | — |
| 3 | `CMessageMgr` | `AddMsg:MsgWrap:` | 🔹 实锤 | 新消息收发入口：在消息入库前检测撤回标记 | — |
| 4 | `CMessageMgr` | `AsyncOnAddMsg:MsgWrap:` | 🔹 实锤 | 异步消息入口：异步到达的消息也检测撤回 | — |
| 5 | `CMessageMgr` | `onNewSyncAddMessage:` | 🔹 实锤 | 同步消息入口：从服务器同步的消息检测撤回标记 | — |

### B.3 红包模块（5 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `WCRedEnvelopesLogicMgr` | `OnWCToHongbaoCommonResponse:Request:` | 🔹 实锤 | 红包查询响应：自动打开红包详情 | — |
| 2 | `CMessageMgr` | `AddMsg:MsgWrap:` | 🔹 实锤 | 发送/接收消息 → 检测红包类型消息 | — |
| 3 | `CMessageMgr` | `AsyncOnAddMsg:MsgWrap:` | 🔹 实锤 | 异步消息到达 → 检测红包 | — |
| 4 | `CMessageMgr` | `onNewSyncAddMessage:` | 🔹 实锤 | 同步消息到达 → 检测红包 | — |
| 5 | `CMessageMgr` | `onNewSyncNotAddDBMessage:` | 🔹 实锤 | 同步消息（不入库）→ 检测红包 | — |

### B.4 自动收款模块（2 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `WCPayLogicMgr` | `insideCallBackOnConfirmTransferMoneyResponse:OnRequest:` | 🟡 推断 | 确认收款回调：记录收款状态并通知 | — |
| 2 | `CMessageMgr` | `AddMsg:MsgWrap:` | 🟡 推断 | 收发消息 → 检测转账类型消息 | — |

### B.5 气泡样式模块（3 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `CommonMessageCellView` | `layoutSubviews` | 🟡 推断 | 气泡布局后修改颜色/圆角/边框 | — |
| 2 | `CommonMessageCellView` | `layoutInternal` | 🔹 实锤 | 内部布局调整：在 `layoutInternal` 中修改气泡相关属性 | — |
| 3 | `CommonMessageCellView` | `setFrameForBgImageView:` | 🔹 实锤 | 修改气泡 frame：扩展气泡宽度、调整气泡位置 | — |

### B.6 头像管理模块（3 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `MMHeadImageView` | `initWithFrame:` | 🟡 推断 | 自定义头像初始化：圆角、边框等 | — |
| 2 | `MMHeadImageView` | `setFrame:` | 🟡 推断 | 修改头像尺寸/位置 | — |
| 3 | `CommonMessageCellView` | `layoutContentView` | 🔹 实锤 | 聊天 cell 内容布局调整：头像间距、偏移等 | — |

### B.7 菜单样式模块（3 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `MMMenuController` | 菜单弹出自定义入口 | 🟡 推断 | 控制菜单背景、边框、圆角、图标色、文字色 | — |
| 2 | `MMMenuContentView` | `layoutSubviews` | 🟡 推断 | 菜单内容视图布局调整 | — |
| 3 | `MMMenuItemView` | `setFrame:` | 🟡 推断 | 菜单单项尺寸调整 | — |

### B.8 浮动标签栏模块（3 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `MMTabBar` | `setFrame:` | 🟡 推断 | 标签栏尺寸调整：宽度、高度 | — |
| 2 | `MMTabBarController` | `viewDidAppear:` | 🟡 推断 | 标签栏 VC 加载后应用自定义外观 | — |
| 3 | `MMTabbarItem` | `updateNodeStatus` | 🔹 实锤 | 更新标签项状态时应用自定义颜色/图标 | — |

### B.9 快捷回复模块（2 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `BaseMsgContentViewController` | `scrollViewWillBeginDragging:` | 🟡 推断 | 检测滑动开始：记录起始位置 | — |
| 2 | `BaseMsgContentViewController` | `scrollViewDidEndDragging:willDecelerate:` | 🟡 推断 | 检测滑动结束：触发快捷回复面板 | — |

### B.10 页面锁定模块（11 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `WCPluginsViewController` | `viewDidAppear:` | 🟡 推断 | 进入插件页 → 检查锁定 | — |
| 2 | `NewSettingViewController` | `viewDidAppear:` | 🟡 推断 | 进入设置 → 检查锁定 | — |
| 3 | `WCPayMainViewControllerV2` | `viewDidAppear:` | 🟡 推断 | 进入钱包 → 检查锁定 | — |
| 4 | `WCFinderMyTabEntranceViewController` | `viewDidAppear:` | 🟡 推断 | 进入视频号 → 检查锁定 | — |
| 5 | `WCMktCardHomeViewControllerV2` | `viewDidAppear:` | 🟡 推断 | 进入商家卡片 → 检查锁定 | — |
| 6 | `WCStoryPostsViewController` | `viewDidAppear:` | 🟡 推断 | 进入朋友圈相册 → 检查锁定 | — |
| 7 | `WCTimeLineViewController` | `viewDidAppear:` | 🟡 推断 | 进入朋友圈 → 检查锁定 | — |
| 8 | `MyFavoritesListViewController` | `viewDidAppear:` | 🟡 推断 | 进入收藏列表 → 检查锁定 | — |
| 9 | `MyFavoritesViewController` | `viewDidAppear:` | 🟡 推断 | 进入收藏详情 → 检查锁定 | — |
| 10 | `SettingMyProfileViewController` | `viewDidAppear:` | 🟡 推断 | 进入个人信息 → 检查锁定 | — |
| 11 | `MMEmoticonLiteAppViewController` | `viewDidAppear:` | 🟡 推断 | 进入表情商店 → 检查锁定 | — |

> **NSUserDefaults 确认**：页面锁定模块通过 `com.wechat.enhance.pageLock.toggle.*` 系列 key 配置，上述 11 个 VC 全部在 dylib strings 和 toggle key 中存在。

### B.11 朋友圈模块（2 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `WCTimeLineCellView` | `layoutSubviews` | 🟡 推断 | 朋友圈 cell：昵称颜色、字体大小、边框、圆角 | — |
| 2 | `WCTimeLineViewController` | `viewDidLoad` | 🔹 实锤 | 朋友圈页面加载：应用自定义配置 | — |

### B.12 昵称/文字颜色模块（4 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `CommonMessageCellView` | `layoutSubviews` | 🟡 推断 | 修改聊天昵称标签颜色/字体大小 | — |
| 2 | `MMUILabel` | `setTextColor:` | 🟡 推断 | 全局昵称标签颜色覆盖 | — |
| 3 | `TextMessageCellView` | `layoutSubviews` | 🟡 推断 | 修改文字消息颜色（发送者/接收者独立配色） | — |
| 4 | `VoiceMessageCellView` | `layoutSubviews` | 🟡 推断 | 修改语音消息 cell 样式 | — |

### B.13 输入框模块（2 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `MMInputToolView` | `layoutSubviews` | 🟡 推断 | 输入框边框/圆角调整 | — |
| 2 | `MMGrowTextView` | `setFrame:` | 🟡 推断 | 文本框尺寸/间距调整 | — |

### B.14 AI 回复模块（2 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `BaseMsgContentViewController` | `AddMsg:MsgWrap:` | 🟡 推断 | 收到新消息 → 调用 AI API 生成回复 | — |
| 2 | `CMessageMgr` | `AsyncOnAddMsg:MsgWrap:` | 🟡 推断 | 异步消息到达 → AI 回复处理 | — |

### B.15 关键词自动回复模块（2 个 hook 点）

| # | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|--------|----------|:------:|---------|:------:|
| 1 | `BaseMsgContentViewController` | `AddMsg:MsgWrap:` | 🟡 推断 | 收到新消息 → 匹配关键词规则 | — |
| 2 | `CMessageMgr` | `AsyncOnAddMsg:MsgWrap:` | 🟡 推断 | 异步消息 → 匹配关键词规则 | — |

### B.16 其他功能模块（8 个 hook 点）

| # | 模块 | 目标类 | Hook 方法 | 可信度 | 功能说明 | 谁更好 |
|:-:|------|--------|----------|:------:|---------|:------:|
| 1 | 字体 | `UIFont` | `fontWithName:size:` | 🟡 推断 | 全局字体替换 | — |
| 2 | 一键已读 | `NewMainFrameViewController` | `reloadData` | 🟡 推断 | 主页面刷新 → 标记所有会话已读 | — |
| 3 | 一键已读 | `MMNewSessionMgr` | `reloadData` | 🟡 推断 | 会话列表刷新 → 清除未读计数 | — |
| 4 | iPad 模式 | `WAIpadAdaptLogic` | `layoutSubviews` | 🟡 推断 | iPad 适配布局调整 | — |
| 5 | 版本伪装 | `CUtility` | `GetVersion` | 🟡 推断 | 返回伪装版本号 | — |
| 6 | VC 名称 | `UIViewController` | `viewDidLoad` | 🔹 实锤 | 显示当前页面 VC 类名 | — |
| 7 | 表情 | `MMEmoticonView` | `initWithFrame:` | 🟡 推断 | 自定义表情圆角/边框 | — |
| 8 | 朋友圈广告拦截 | `WCContentItemFinderView` | `layoutSubviews` | 🟡 推断 | 隐藏/替换朋友圈广告内容 | — |

### B.17 统计数据总览

| 统计项 | 数值 |
|--------|:---:|
| 总功能模块数 | 16 |
| 总 hook 点数 | **63** |
| 🔵 Ghidra 确认 + dylib strings 双源 | 4 |
| 🔹 dylib strings 类名+方法双命中 | 16 |
| 🟡 strings 推断（类名+方法存在但需进一步验证） | 43 |
| 涉及 WeChat 内部类 | 28 |
| 最多 hook 点的类 | `CMessageMgr`（16 次，跨 7 个模块） |
| 第二多 hook 点的类 | `CommonMessageCellView`（8 次，跨 4 个模块） |
| 第三多 hook 点的类 | `BaseMsgContentViewController`（7 次，跨 5 个模块） |

### B.18 按 WeChat 内部类的 Hook 分布

| WeChat 内部类 | Hook 次数 | 涉及模块 |
|--------------|:--------:|---------|
| `CMessageMgr` | 16 | 防撤回(5) + 红包(5) + 自动收款(1) + AI 回复(1) + 关键词回复(1) + *(跨模块共享 `AddMsg:MsgWrap:` 等)* |
| `CommonMessageCellView` | 8 | 消息时间(2: updateNodeStatus+didMoveToWindow) + 气泡样式(3) + 头像管理(1) + 昵称颜色(1) + *(跨模块共享 `layoutSubviews`)* |
| `BaseMsgContentViewController` | 7 | 消息时间(2) + 快捷回复(2) + AI 回复(1) + 关键词回复(1) + *(跨模块共享)* |
| `MMEmoticonLiteAppViewController` 等 11 个 VC | 11 | 页面锁定（每个 VC `viewDidAppear:` 各 1 次） |
| `MMHeadImageView` | 2 | 头像管理 |
| `MMTabBar`/`MMTabBarController`/`MMTabbarItem` | 3 | 浮动标签栏 |
| `MMMenuController`/`MMMenuContentView`/`MMMenuItemView` | 3 | 菜单样式 |
| `WCTimeLineCellView`/`WCTimeLineViewController` | 2 | 朋友圈 |
| `ChatTimeCellView`/`ChatTimeViewModel` | 2 | 消息时间 |
| `MMInputToolView`/`MMGrowTextView` | 2 | 输入框 |
| `MMNewSessionMgr`/`NewMainFrameViewController` | 2 | 一键已读 |
| 其他 11 个类 | 11 | 各 1 个 hook 点 |

> **注意**：`CMessageMgr`、`CommonMessageCellView`、`BaseMsgContentViewController` 的某些 hook 点（如 `layoutSubviews`、`AddMsg:MsgWrap:`、`AsyncOnAddMsg:MsgWrap:`）被多个功能模块共享复用。dylib 中使用判断分支（NSUserDefaults 配置检查）来区分不同功能是否启用，而不是每个模块独立 hook 同一个方法——这是"大而全"插件的典型架构模式。

### B.19 Hook 引擎

基于以下证据，dylib 使用 **Cydia Substrate / MSHookMessageEx** 进行 hook：

1. **`NOUNDEFS` flag**：dylib 为自包含，所有符号内联解析
2. **Ghidra 确认**：`CommonMessageCellView.layoutSubviews` 的替换函数解析到 `[_UIView layoutSubviews]`（即保存的原始 IMP），`CommonMessageCellView.prepareForReuse` 解析到 `[_UITableViewCell prepareForReuse]`——这是 MSHookMessageEx 保存原始 IMP 的标准模式
3. **dylib strings**：存在 `class_copyIvarList`、`class_getInstanceVariable`、`class_copyPropertyList`、`class_copyMethodList`、`class_getSuperclass` 等运行时内省原语，与 Substrate hook 实现内常用模式一致
4. **动态注入**：`class_addMethod` 存在，用于向 WeChat 类动态注入新方法（如 `createLabelWithText:font:textColor:`）

---

> **v3.6 核心进化 (2026-05-17 — setCenter 对齐)**：
> 
> #### 双阶段反汇编深度分析
> 对 `/tmp/pos_calc.asm`（主位置计算函数）和 `/tmp/pos_calc2.asm`（共享尾部 setCenter 调用）进行逐指令 ARM64 逆向分析，关键发现：
> 
> 1. **微信优化使用 `setCenter` 定位（非 `setFrame`）**：
>    - `0xcae00` 调用只传入 `d0`(X) 和 `d1`(Y) 两个参数，无宽度/高度参数
>    - 排除了 `setFrame:` 的可能性（后者需要 4 个参数：x, y, w, h）
>    - 反编译确认函数签名匹配 `setCenter:` 属性设置或等效方法
> 
> 2. **两次 setCenter 调用架构**（`pos_calc2.asm` L98-L127）：
>    - **第一次** `setCenter(straddleX, straddleY)`：跨骑基准位置（cv边缘 ± w/2, cv边缘 ± h/2）
>    - **中间读取**：`offsetX`、`offsetY` 从 x20 配置对象获取，`d10` 从 `[x20 unknown_selector]` 获取
>    - **第二次** `setCenter(±offsetX + d10, d1 - offsetY)`：应用用户偏移后的最终位置
> 
> 3. **d10 神秘偏移（未解决）**：
>    - 来自 `bl 0xc3a60`（PLT stub → `objc_msgSend(x20, unknown_selector)`）
>    - x20 是微信内部配置/ViewModel 对象，selector 名称无法从静态反汇编确定
>    - 完全还原需要运行时 hook（Frida 拦截 `0xc3a60` 调用）
> 
> 4. **编号对齐 (v3.7 新增)**：
>    - 交换位置编号 3↔4 和 5↔6，与微信优化完全对齐
>    - 原因：ARM64 反汇编确认微信优化的 pos3 使用 `CGRectGetMinX`（外侧/远离头像），与我们旧的 pos4 语义一致
>    - 影响范围：MessageTimeHook.m 三个 switch-case + SettingGeneralFunctionController.m 两个 positionNames 数组
>    - 迁移兼容性：旧用户的位置编号 3/4/5/6 会自动切换到新语义
> 
> #### 代码实现变更
> 
> | 变更 | 旧实现 (v3.4) | 新实现 (v3.6) |
> |------|:------------:|:------------:|
> | 定位 API | `setFrame:` | `setCenter:` (位置2-7) |
> | 位置2-7计算 | 混合 (部分 setFrame 边缘对齐 + padding) | 统一 setCenter 跨骑 (与微信优化一致) |
> | 位置编号映射 | 未考虑映射 | 已全部折算（3↔4, 4↔3, 5↔6, 6↔5） |
> | bounds 修复 | 无 | setCenter 前设 bounds=(0,0,w,h) 修复零尺寸标签 |
> | sender/receiver | 无bug | 修正三元表达式方向错误 |
> 
> #### Bug 修复历史
> 
> 1. **标签不可见 (messagetime(127).log)**：`initTimeLabel` 创建 `[[UILabel alloc] init]` → frame=CGRectZero → `setCenter` 不改变 size → 标签零尺寸不可见。修复：setCenter 前设 `label.bounds = CGRectMake(0, 0, w, h)`。
> 
> 2. **sender/receiver 方向全部写反**：所有 switch-case 中 `isSender ? A : B` 的 A/B 方向颠倒。修复：逐项交换所有三元表达式。