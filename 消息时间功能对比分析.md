# 微信消息时间显示功能对比分析

## 概述

本文档对比三款 iOS 微信插件的"消息时间显示"功能：

| 对比项 | 微信优化 1.6.5 (WeChatEnhance) | 微信助手 3.9-5 | 我们的实现 (MessageTimeHook) |
|--------|:-------------------------------:|:--------------:|:---------------------------:|
| 类型 | 综合型微信增强插件 | 综合型微信增强插件 | 专注消息时间显示的专用模块 |
| 包标识 | `com.wechat.enhance.*` | `MiYou`（米游） | Mio 插件内部模块 |
| 定位 | 大而全（防撤回、红包、气泡美化、头像管理、消息时间等） | 庞大全功能（防撤回、红包、消息处理、朋友圈、小程序管理等） | 小而精（仅做消息时间显示） |
| 文件大小 | 2.1 MB（含全部功能） | **13 MB**（含全部功能） | ~655 KB dylib（整个插件） |
| 是否有消息时间功能 | ✅ 是（per-message 时间标签，8个位置模式） | ✅ 是（`msgTimeLabel` per-message 标签，通过 `willDisplayCell` 添加） | ✅ 是（per-message 时间标签，8个位置模式） |
| 分析方式 | 二进制逆向（strings + nm + llvm-objdump） | 同左 | 源码 + 日志 |

---

## 一、整体流程对比

### 微信优化 1.6.5 的整体流程

```
App 启动
  ↓
MSHookMessageEx(CommonMessageCellView, layoutSubviews)     ← Substrate 风格 hook
MSHookMessageEx(CommonMessageCellView, prepareForReuse)
MSHookMessageEx(BaseMsgContentViewController, willDisplayCell)
  ↓
用户打开聊天界面 → BaseMsgContentViewController 加载
  ↓
cell 即将显示 → willDisplayCell 触发
  ↓
  → 尝试 addTimeLabel = initTimeLabel（初始化标签）
  → 如果数据未就绪（wrap=nil），延迟到 layoutSubviews
  ↓
cell 布局完成 → layoutSubviews 触发（CommonMessageCellView）
  ↓
  → m_timeLabel 已存在？更新 frame → 否？创建 + 添加
  → 计算位置（基于气泡、头像、8个位置模式）
  ↓
cell 被回收 → prepareForReuse → 清理 m_timeLabel
```

### 微信助手 3.9-5 的整体流程

```
App 启动
  ↓
checkHook:/checkHookWithSeq: 初始化自定义 hook 管理
class_addMethod + method_setImplementation (纯 ObjC Runtime)
  → hook 列表由内部字符串配置驱动
  ↓
用户打开聊天界面
  ↓
tableView:willDisplayCell:forRowAtIndexPath: / collectionView:willDisplayCell:forItemAtIndexPath:
  → checkHook: 判断消息时间功能是否启用
  → 通过 msgTimeLabel 属性给每条消息 cell 添加时间标签
  → settingMessageTime: 控制标签格式/位置/开关
  ↓
多功能模块各自独立触发（防撤回、红包、朋友圈、小程序管理等）
  → 每个模块通过 checkHook: 判断是否启用
```

### 我们的整体流程

```
App 启动
  ↓
MessageTimeHook.install()
  ↓
MTHookEntry 配置表 + MSHookMessageEx 驱动 install：
  ├── ChatTableViewCell.prepareForReuse     ← MSHookMessageEx
  ├── ChatTimeCellView.layoutSubviews
  ├── ChatTimeViewModel.cellHeight
  ├── CContact.m_nsNickName
  └── CommonMessageCellView.layoutSubviews  ← 优先，有 fallback 列表
  ↓
用户打开聊天界面 → BaseMsgContentViewController 加载
  ↓
cell 布局完成 → CommonMessageCellView.layoutSubviews 触发
  ↓
  → orig_CommonMessageCellView_layoutSubviews → 气泡/头像就位
  → 有时间标签？→ mt_updateLabelFrame（更新位置）
  → 无时间标签？→ addTimeLabelToCell（创建 + 添加）
  ↓
cell 被回收 → ChatTableViewCell.prepareForReuse
  → 清理标签、关联对象
  → 复用时 layoutSubviews 重新创建
```

---

## 二、Hook 架构对比

### 2.1 Hook 的目标类和方法

| Hook 目标 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现 | 功能说明 | 谁更好 |
|-----------|:-------------:|:-------------:|:---------:|----------|:------:|
| `CommonMessageCellView.layoutSubviews` | ✅ MSHookMessageEx | ❌ | ✅ MSHookMessageEx | **消息时间标签**：每条消息旁显示发送时间 | 🏆 我们（环境自适应） |
| `CommonMessageCellView.prepareForReuse` | ✅ MSHookMessageEx | ❌ | ❌（通过 ChatTableViewCell 清理） | **标签清理**：cell 复用时清除旧标签 | 🏆 我们（cell 层级清理比子视图层更可靠） |
| `ChatTableViewCell.layoutSubviews` | ❌ | ❌ | ❌（已删除） | （曾用于双层 hook，已优化删除） | — |
| `ChatTableViewCell.prepareForReuse` | ❌（挂在 `CommonMessageCellView` 上） | ❌（不清理，每次 willDisplayCell 直接重建） | ✅ 配置表条目 | **标签清理**：cell 层统一清理标签+关联对象 | 🏆 我们（唯一在 cell 层显式清理的） |
| `ChatTimeCellView.layoutSubviews` | ✅（"隐藏聊天页原生时间标签"） | ✅（"隐藏自带时间"，`mIsHiddenMessageTime` 开关） | ✅ 配置表条目 | **隐藏系统时间分隔线**：隐藏微信自带的"今天 12:30"时间线 | 相当（1.6.5 和我们 hook CellView；微信助手用属性更灵活） |
| `ChatTimeViewModel.cellHeight` | ✅（双保险：隐藏 cell + 高度归零） | ❌（微信助手用不同路径实现） | ✅ 配置表条目 | **时间分隔线高度归零**：配合隐藏，确保不占布局空间 | 🏆 我们/1.6.5（高度归零双保险） |
| `CContact.m_nsNickName` | ✅（`addTimeSuffixFormatItem` + `show.addtime.enabled`） | ❌ | ✅ 配置表条目 | **好友时间后缀**：联系人列表昵称后显示好友添加时间 | 🏆 1.6.5（有完整配置项） |
| `BaseMsgContentViewController.willDisplayCell` | ✅（仅 `tableView:willDisplayCell:`） | ✅（`checkHook:` 字符串驱动，二进制未见 `BaseMsgContentViewController` 直接引用） | ❌（已删除） | **提前触发**：cell 快显示时创建标签（数据常未就绪，需配合 layoutSubviews） | 🏆 他们（双时机兜底，但我们删掉是为了减少空跑） |
| `BaseMsgContentViewController.collectionView:willDisplayCell:` | ❌（1.6.5 只有 tableView 版本） | ✅（有 `collectionView:willDisplayCell:` 选择器） | ❌（已删除） | 同上，CollectionView 版本 | — |
| 消息时间标签 | ✅ `m_timeLabel` | ✅ `msgTimeLabel`（编译时属性，最快） | ✅ `messageTimeLabel`（关联对象 + tag=999999 双重兜底） | **标签属性**：存储每条消息时间标签的 UILabel | 🏆 微信助手（编译时 ivar 最快最干净） |
| 自定义 hook 管理层 | ❌ | ✅ `checkHook:` / `checkHookWithSeq:` | ✅ `MTHookEntry` 配置表 | **Hook 管理层**：统一管理所有 hook 安装/卸载 | 🏆 我们（配置表编译时可见，比字符串驱动更直观） |

### 2.2 Hook 技术对比

| 方面 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现 |
|------|:-------------:|:-------------:|:---------:|
| Hook API | `MSHookMessageEx`（Cydia Substrate） | `class_addMethod` + `method_setImplementation` | `MSHookMessageEx`（自定义 `CydiaSubstrate.m` 编译进主 dylib，零外部依赖） |
| 外部依赖 | 自带 `CydiaSubstrate.framework`（`@rpath` 加载，不依赖系统） | 纯 ObjC Runtime（无外部依赖） | 无外部依赖（`CydiaSubstrate.m` 内联编译，`MSHookMessageEx` 实现约 10 行） |
| 原始 IMP 保存 | 自动由 MSHookMessageEx 处理 | 手动管理 | 自动由 MSHookMessageEx 处理 |
| Hook 管理方式 | 分散调用 MSHookMessageEx | `checkHook:` 统一调度 | 配置表驱动（`MTHookEntry` + install 迭代） |
| 稳定性 | 成熟，久经考验 | 自定义管理，依赖内部配置 | Substrate 风格，内联实现简洁可控 |
| 是否可运行时切换引擎 | ❌（固定 Substrate，自带 framework） | 固定 Runtime | 无需切换（MSHookMessageEx 始终可用：Theos 环境用系统版，独立构建用内联版） |

---

## 三、时间标签对象管理

### 3.1 标签创建

| 特性 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现 |
|------|:-------------:|:-------------:|:---------:|
| 标签对象 | `UILabel` 实例，通过 `m_timeLabel` 引用 | `UILabel` 实例，通过 `msgTimeLabel` 属性引用 | `UILabel` 实例，通过 `objc_getAssociatedObject(cell, "messageTimeLabel")` 引用 |
| 创建时机 | `initTimeLabel` 方法（首次需要时创建） | `willDisplayCell` 回调中创建（通过 `checkHook:` 判断启用状态） | `addTimeLabelToCell` 中创建（仅 `layoutSubviews`） |
| 标识方式 | 实例变量 / 关联对象 | 编译时属性（`msgTimeLabel` 符号表可见） | tag=999999 + 关联对象双重校验 |
| 防重复 | `initTimeLabel` → `m_timeLabel` 存在则不创建 | 每次 `willDisplayCell` 直接覆盖重建，不防重复 | Identifier 去重：`createTime_msgType_wrapPtr` |

### 3.2 标签清理

| 清理时机 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现 |
|---------|:-------------:|:-------------:|:---------:|
| cell 复用（prepareForReuse） | ✅ 清理 m_timeLabel | ❌ 不显式清理（`willDisplayCell` 每次都重建覆盖） | ✅ 移除 label，清理关联对象 |
| cell 复用后布局重入 | 清理 + 重新创建 | 覆盖 + 重新创建 | 清理 + 重新创建 |

### 3.3 防止布局循环

| 机制 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现 |
|------|:-------------:|:-------------:|:---------:|
| 方式 | 未发现显式防重入（hook 后依赖 iOS 自动终止） | 天然无循环（仅 hook `willDisplayCell`，不 hook `layoutSubviews`，不存在自触发回路） | 无防重入（v9 已删除，依赖 iOS 自然终止） |

---

## 四、位置计算

### 4.1 支持的位置模式

两个实现都支持 8 种位置模式：

| Position | 名称 | 基准 |
|:--------:|------|------|
| 0 | 头像上方 | 头像中心 X，头像顶部以上 |
| 1 | 头像下方 | 头像中心 X，头像底部以下 |
| 2 | 消息旁边（远离头像侧） | 气泡外侧边缘，垂直居中 |
| 3 | 消息下方（远离头像侧） | 气泡底部，外侧对齐 |
| 4 | 消息下方（靠近头像侧） | 气泡底部，内侧对齐 |
| 5 | 消息上方（远离头像侧） | 气泡顶部，外侧对齐 |
| 6 | 消息上方（靠近头像侧） | 气泡顶部，内侧对齐 |
| 7 | 消息内部（气泡右下角） | 气泡内部右下 |

### 4.2 位置计算流程对比

```
微信优化 1.6.5:
  willDisplayCell/initTimeLabel
    → 获取 bubble frame（getBgImageView / m_bgImageView）
    → 获取 avatar frame（getHeadImageView / m_headImageView）
    → 计算 label frame
    → 设置 frame
    → 添加到 cell
```

```
我们的实现:
  layoutSubviews
    → 获取 bubble frame（getBubbleView：支持6种路径）
    → 获取 avatar frame（getAvatarView：支持5种路径）
    → 计算 label frame（8种位置，方向感知偏移）
    → Y 轴边界钳制（防止标签超出 cell 可见区域）
    → 设置 label.frame
    → 添加到 cell（如果尚未添加）
```

### 4.3 边界安全

| 安全措施 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现 |
|---------|:-------------:|:-------------:|:---------:|
| Y 轴钳制 | ❌ 未发现 | 未知（二进制无法确认） | ✅ `maxY = cellHeight - labelHeight - 2` |
| X 轴安全 | ❌ 未发现 | 未知 | ✅ 方向感知偏移 (left/right) |
| 气泡扩展 | ✅ `bubbleExtensionWidth` 配置 | 未知 | ✅ `messageTimeBubbleExtWidth` 配置 |

### 4.4 发送者/接收者检测

| 方法 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现 |
|------|:-------------:|:-------------:|:---------:|
| 检测方式 | `isSenderFromMsgWrap:wrap` + 气泡位置 fallback | 未知（推测类似，二进制有 `isSenderFromMsgWrap:` 符号） | 同左（三层 fallback） |
| 配色区分 | ✅ sender/receiver 独立 textColor 和 backgroundColor | ✅ `settingMessageTime:` 方法控制格式/颜色 | ✅ 同左 |

---

## 五、设置和配置

### 5.1 用户默认键对比

| 配置项 | 微信优化 1.6.5 (NSUserDefaults key) | 我们的实现 (PluginConfig) |
|--------|:-----------------------------------:|:------------------------:|
| 启用/关闭 | `com.wechat.enhance.messageTime.enabled` | `showMessageTime` |
| 位置模式 | `com.wechat.enhance.messageTime.position` | `messageTimePosition` |
| 字体大小 | `com.wechat.enhance.messageTime.fontSize` | `messageTimeFontSize` |
| 粗体 | `com.wechat.enhance.messageTime.boldFont` | `messageTimeBoldFont` |
| 自定义格式 | `com.wechat.enhance.messageTime.customFormat` | `messageTimeFormat` |
| X 偏移 | `com.wechat.enhance.messageTime.offsetX` | `messageTimeOffsetX` |
| Y 偏移 | `com.wechat.enhance.messageTime.offsetY` | `messageTimeOffsetY` |
| 气泡扩展宽度 | `com.wechat.enhance.messageTime.bubbleExtensionWidth` | `messageTimeBubbleExtWidth` |
| 圆角 | `com.wechat.enhance.messageTime.cornerRadius` | `messageTimeCornerRadius` |
| 发送者文字色 | `com.wechat.enhance.messageTime.sender.textColor` | `senderTextColorHex` |
| 发送者背景色 | `com.wechat.enhance.messageTime.sender.backgroundColor` | `senderBackgroundColorHex` |
| 接收者文字色 | `com.wechat.enhance.messageTime.receiver.textColor` | `receiverTextColorHex` |
| 接收者背景色 | `com.wechat.enhance.messageTime.receiver.backgroundColor` | `receiverBackgroundColorHex` |
| 暗色模式文字色 | `...textColor.dark` | 自动暗色转换 `autoDarkColor()` |
| 暗色模式背景色 | `...backgroundColor.dark` | 自动暗色转换 |
| 后缀格式 | `com.wechat.enhance.chat.addtime.suffix.format` | ✅ 支持 |
| 标签宽度自适应 | ❌ | ✅ `disableLabelWidthAdjustment` |

### 5.2 时间格式

| 特性 | 微信优化 1.6.5 | 我们的实现 |
|------|:-------------:|:---------:|
| 格式语法 | NSDateFormatter 标准格式 | `{HH}:{mm}` 模板语法（支持 UTC 修正） |
| 后缀 | ✅ `addTimeSuffixFormatItem` 可配置 | ❌ |
| 示例 | `HH:mm:ss` | `{HH}:{mm}` → 自动替换为本地时间 |

---

## 六、气泡和头像获取

### 6.1 气泡视图获取路径对比

| 优先级 | 微信优化 1.6.5 | 我们的实现 |
|:------:|:-------------:|:---------:|
| 1 | `getBgImageView` 方法 | `getBgImageView` 方法 |
| 2 | - | KVC `m_bgImageView` / `bgImageView` |
| 3 | - | subviews 遍历（BgImage/Bubble/MessageView/RichTextView） |
| 4 | - | fallback: 直接使用 cellView |

### 6.2 头像视图获取路径对比

| 优先级 | 微信优化 1.6.5 | 我们的实现 |
|:------:|:-------------:|:---------:|
| 1 | `getHeadImageView` 方法 | `getHeadImageView` 方法 |
| 2 | - | `leftAvatarView` / `rightAvatarView` 属性 |
| 3 | - | KVC `m_headImageView` / `headImgView` / `m_avatarView` / `avatarImageView` |
| 4 | - | subviews 遍历（Head/Avatar + respondsToSelector:@selector(image)） |

---

## 七、日志和调试

| 特性 | 微信优化 1.6.5 | 我们的实现 |
|------|:-------------:|:---------:|
| 日志类型 | `NSLog` / 内部日志 | `mtLog` 宏（可开关） |
| 日志粒度 | 基础（创建/更新/位置） | 详细（子视图结构、KVC 路径、调试信息） |
| 运行时配置检查 | 启动时 | 每次调用都打印当前配置 |
| 错误处理 | `@try/@catch` | `@try/@catch` + 详细异常日志 |
| 布局循环检测 | ❌ | ✅ 忙标志统计 |

---

## 八、Hook 引擎选型对比

| 引擎特性 | 微信优化 1.6.5 (Substrate) | 微信助手 3.9-5 (Runtime) | 我们的 v6 (Runtime) | 我们的当前版本 (内联 Substrate) |
|---------|:------------------------:|:-----------------------:|:------------------:|:--------------------------:|
| 具体 API | `MSHookMessageEx` | `class_addMethod` + `method_setImplementation` | `class_addMethod` + `method_setImplementation` | `MSHookMessageEx`（内联实现，约 10 行） |
| 外部依赖 | 自带 `CydiaSubstrate.framework`（`@rpath`） | 无（纯 ObjC Runtime） | 无（纯 ObjC Runtime） | 无（`CydiaSubstrate.m` 编译进主 dylib，零外部依赖） |
| 依赖体积 | 约 200KB | 0 | 0 | ~1KB（源码编译后） |
| Hook 管理 | 每个 hook 点单独调用 | `checkHook:`/`checkHookWithSeq:` 内部调度 | `hookCellForTime` + install 集中管理 | `MTHookEntry` 配置表驱动 |
| 能否跨类批量 hook | 手动逐个 hook | 内部配置表驱动 | `hookCellForTime(name)` 批量 hook | 配置表 + fallback 数组 |
| 能否运行时开关 | ❌ | ✅ `checkHook:` 可控制 | ❌（编译时确定） | ❌（编译时确定） |
| 原始 IMP 保存 | 自动 | 手动 | 手动（`gOrigIMPs` 字典） | 自动（MSHookMessageEx 处理） |
| 缓存刷新 | ✅ MSHookMessageEx 自动处理 | ❌ method_setImplementation 不刷新 | ❌ method_setImplementation 不刷新 | ✅ MSHookMessageEx 自动处理 |
| 稳定性 | 久经考验，稳定 | 依赖内部管理逻辑 | 依赖代码质量 | 内联实现简洁可控 |
| iOS 版本兼容性 | 所有越狱版本 | 所有版本 | 所有版本 | 所有版本 |
| 非越狱兼容 | ✅（自带 `CydiaSubstrate.framework`，`@rpath` 加载） | ✅ | ✅ | ✅（内联编译，无额外加载） |
| 是否自动适应环境 | 固定 Substrate（自带 framework） | 固定 Runtime（NOUNDEFS 自包含） | 固定 Runtime | 无需适应（MSHookMessageEx 始终可用） |

### 关键结论

> 当前版本采用了**最简洁的方案**：自定义 `CydiaSubstrate.m`（约 10 行）编译进主 dylib，提供 `MSHookMessageEx` 实现。Theos 环境下走系统 CydiaSubstrate，独立构建环境用自己的内联版本——**两种环境都不需要额外加载 framework，零外部依赖**。

微信优化 1.6.5 思路相同（自带 Substrate），区别在于它打包成独立 framework（`@rpath`），我们直接编译进 dylib，更简洁，少一层加载链路。

微信助手 3.9-5 走纯 Runtime 路线，和我们的 v6 版本一致，虽然零依赖但缺少 MSHookMessageEx 的原始 IMP 自动保存和缓存刷新能力。

## 九、演化历程对比

### 微信优化 1.6.5（稳定版）

```
原始实现（有限迭代）:
  willDisplayCell（首次触发）
  + layoutSubviews（二次确认）
  → 无明显布局循环（因 hook 类少）
  → 无显式防重入
  → 标准 Substrate hook
```

### 我们的实现（持续迭代优化）

```
v1（33.log）:
  willDisplayCell + ChatTableViewCell.layoutSubviews + CommonMessageCellView.layoutSubviews
  → 233次调用，147次wrap=nil失败
  → 标签Y轴超出cell边界（看不见）
  → 严重布局循环

v2（35.log）: 删除 willDisplayCell
  → 减少~87次无效调用
  → Y轴钳制修复 → 标签可见
  → 布局循环减轻但仍在（ChatTableViewCell ↔ CommonMessageCellView 双层循环）

v3（37.log）: 删除 ChatTableViewCell.layoutSubviews
  → addTimeLabelToCell调用降至54次
  → wrap=nil归零
  → 布局循环减轻为单层（CommonMessageCellView 自循环）

v4（39.log）: 防重入忙标志（CommonMessageCellView.self）
  → mt_updateLabelFrame归零
  → 日志降至819行
  → bug: 忙标志清理时机错位（cell复用后标签不创建）

v5（41.log）: 忙标志移到 targetCell（ChatTableViewCell）
  → 修复复用问题
  → 但 [super layoutSubviews] 自身触发重布局 → 残留循环

v6（43.log）: 忙标志检查移到 [super layoutSubviews] 之前
  → 忙的轮次连 [super] 都不调
  → 彻底打断循环
  → 日志降至1365行，840次[layoutSubviews]（均为忙检查log，无实际工作）

v7（配置表重构）: 替换为 Substrate MSHookMessageEx + 配置表架构
  → 删除 gOrigIMPs 字典 → 每个 hook 独立的静态函数指针
  → 删除 hookCellForTime 动态循环 → MTHookEntry 配置表
  → 删除 block-based IMP → 预定义 C 函数
  → 新增 ChatTimeCellView.layoutSubviews、ChatTimeViewModel.cellHeight、CContact.m_nsNickName
  → 单元视图 fallback 列表（CommonMessageCellView 优先，逐个降级）
  → crash: 独立注入环境无 Substrate → MSHookMessageEx 符号不存在闪退

v8（messagetime(45).log + revoke(8).log）: dlsym 混合引擎修复
  → 删除 #import <substrate.h>，改用 dlsym(RTLD_DEFAULT, "MSHookMessageEx")
  → 找到 Substrate → 使用 MSHookMessageEx（稳定 + 缓存刷新）
  → 没找到 → 降级 method_setImplementation（兼容独立注入）
  → 删除本地 substrate.h 文件
  → 删除 "消息时间功能对比分析.md" 从 .gitignore（纳入版本管理）
  → 闪退修复，两种环境均可运行

v9（当前版本）: 删除防重入机制
  → 删除 kLayoutBusyKey 忙标志检查（repl_CommonMessageCellView_layoutSubviews）
  → 删除 prepareForReuse 中的忙标志清理
  → 每次 layoutSubviews 都执行标签创建/更新（不再跳过多轮）
  → 简化逻辑，依赖 iOS 自然终止布局循环

v10（内联 Substrate 架构）: CydiaSubstrate.m 直接编译进主 dylib
  → 不再单独构建 CydiaSubstrate.framework（解决 CI 链接错误）
  → 不再使用 dlsym 动态检测（MSHookMessageEx 始终可用）
  → 创建 substrate.h 头文件（供独立构建环境的 #import <substrate.h> 使用）
  → CydiaSubstrate.m 作为首个 .o 编译进 dylib，零外部依赖
  → Theos 环境走系统 CydiaSubstrate，独立构建走内联实现
  → 相比微信优化 1.6.5 的独立 framework 方案更简洁，少一层 @rpath 加载
```

---

## 十、总结

### 微信优化 1.6.5 的优势

1. **成熟稳定**：使用 MSHookMessageEx，经大量用户验证
2. **功能全面**：messages time 只是几十个功能之一，集成度高
3. **设置丰富**：支持后缀格式、暗色模式配色等细节
4. **无布局循环**：因为只 hook CommonMessageCellView，不 hook ChatTableViewCell，天然避免了双层循环

### 微信助手 3.9-5 的参考价值

1. **轻依赖**：用 `class_addMethod` + `method_setImplementation`，零外部依赖
2. **内部 Hook 管理**：`checkHook:` / `checkHookWithSeq:` 提供运行时开关能力，值得借鉴
3. **`msgTimeLabel` 实现**：通过 `willDisplayCell` 添加 per-message 时间标签（和我们触发时机不同）
4. **功能庞大**：13MB 的二进制，消息时间只是众多功能之一

### 我们的优势

1. **专注深入**：消息时间功能更细致（Y轴钳制、方向感知偏移、自适应宽度）
2. **日志调试**：更详细的日志系统（子视图结构、KVC路径）
3. **边界安全**：Y轴钳制确保标签始终在 cell 可见区域内
4. **更丰富的路径查找**：气泡/头像查找有更多 fallback 路径，兼容更多微信版本
5. **混合 Hook 引擎**：`dlsym` + Substrate/Runtime 回退，自适应运行环境
6. **配置表架构**：`MTHookEntry` 配置表 + fallback 列表，新增 hook 只需加一行

### 各自的架构选择

| 决策 | 微信优化 1.6.5 | 微信助手 3.9-5 | 我们的实现 |
|------|:-------------:|:-------------:|:---------:|
| Hook 引擎 | Substrate (MSHookMessageEx) | 纯 ObjC Runtime (class_addMethod) | MSHookMessageEx（内联 CydiaSubstrate.m） |
| 触发时机 | willDisplayCell + layoutSubviews | willDisplayCell（通过 `checkHook:`） | 仅 layoutSubviews |
| 布局安全 | 依赖 iOS 自然终止 | 天然无循环（不 hook layoutSubviews） | 无防重入（v9 已删除） |
| 位置安全 | 无边界检查 | 未知（二进制无法确认） | Y轴钳制 |
| 消息时间标签 | `m_timeLabel` | `msgTimeLabel`（编译时属性） | `messageTimeLabel`（关联对象 + tag） |
| 可维护性 | 中等（二进制分散） | 低（13MB 单体） | 高（源码 + 配置表） |

> 三个插件分别代表三种思路：微信优化 1.6.5（通用 Substrate，打包 framework）、微信助手 3.9-5（堆功能不挑引擎，纯 Runtime）、我们的实现（专注消息时间 + 配置表架构 + 内联 Substrate 零外部依赖）。**当前 v10 架构：用 MSHookMessageEx 的接口风格，但实现完全自控——CydiaSubstrate.m 仅 10 行代码编译进 dylib，Theos 环境走系统版，独立构建走内联版，兼顾稳定性和可移植性。**

---

## 附录：分析可信度说明

由于微信优化 1.6.5 和微信助手 3.9-5 只能通过二进制逆向分析（`strings` + `nm` + `llvm-objdump`），无法看到运行时日志：

| 可信度标记 | 含义 |
|:---------:|------|
| ✅ | 源码确认 — 我们自己的代码，100% 准确 |
| 🔵 | 二进制实锤 — 从 `strings`/`nm` 中看到明确的类名、方法名、Selector，有符号表支撑 |
| 🟡 | 二进制推断 — 从符号表 + 上下文推测的逻辑，可能有误 |
| ❌ | 无法确认 — 二进制中未找到相关证据 |

> 🟡 微信助手 3.9-5 的分析尤其受限：`checkHook:` 的 hook 目标是用字符串运行时查找的，不编译到符号表中。`msgTimeLabel` 属性存在于二进制中但具体使用方式是通过连续二进制上下文推断的。**最准确的验证方式是运行时拦截**：对 `class_addMethod` / `method_setImplementation` / `MSHookMessageEx` 二次 hook，记录所有调用参数后统一对比。